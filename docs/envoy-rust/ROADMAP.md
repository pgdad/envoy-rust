# envoy-rust Roadmap

## Schema

Each phase row has the columns:

- **id** — stable numeric identifier (`00`, `01`, …; sub-phases `NN.1`, `NN.2`).
- **title** — one-line human-readable name.
- **depends-on** — space-separated phase ids that must be `done` before this one can enter `in-progress`.
- **status** — one of `planned | in-progress | blocked | done`.
- **sub-phases** — if the phase has been split (§6 of `BOOTSTRAP_PROMPT.md`), list the child ids here.
- **summary** — the differential surface this phase is expected to light up when it lands.

Rules:

- This file is **append-only history** — rows are never deleted. Only the `status` and `sub-phases` columns are mutated.
- `STATE.md` names the single active phase. A phase enters `in-progress` only when `STATE.md` points at it.
- When a phase is split, its own `status` becomes `in-progress` while its sub-phases land. The parent flips to `done` only after all sub-phases are `done`.

---

## MVP Trunk — phases 00 through 08

Phases 00–08 ship *in order*: each adds a primitive the next relies on. Splitting (§6) is still permitted inside any of them.

| id | title | depends-on | status | sub-phases | summary |
|---|---|---|---|---|---|
| 00 | Bootstrap: Cargo workspace layout, `rust-toolchain.toml`, `deny.toml`, CI, Docker reference Envoy, differential harness skeleton, `ENVOY_TARGET.md` pin, trivial echo fixture | — | done | — | harness boots; one TCP echo fixture green |
| 01 | Static bootstrap config loader (node, admin, static_resources skeleton) | 00 | done | — | config parses; admin `/ready` behaves like Envoy |
| 02 | Listener + TCP proxy filter + static cluster + round-robin LB (plaintext) | 01 | done | 02.1, 02.2 | TCP proxy fixture green |
| 02.1 | Config schema + cluster manager + echo-server helper | 01 | done | — | no new fixture; config parser + envoy-cluster + tcp-echo-server helper + fuzz corpus extended |
| 02.2 | Listener + TCP proxy filter + fixture 0003 + phase-01 rollovers I4/M1 | 02.1 | done | — | fixture 0003-tcp-proxy green; parent phase 02 flips done |
| 03 | Downstream TLS termination + upstream TLS origination + SNI | 02 | done | 03.1, 03.2 | TLS TCP fixture green |
| 03.1 | envoy-tls foundation + downstream TLS termination (single cert) + fixture 0004 | 02 | done | — | fixture 0004-tls-downstream green; envoy-tls scaffold + transport_socket schema + downstream TLS dispatch + harness PKI land |
| 03.2 | Upstream TLS origination + multi-cert SNI cert selection + tls-echo-server helper + fixtures 0005 + 0006 | 03.1 | done | — | fixtures 0005-tls-upstream + 0006-tls-sni green; parent phase 03 flips done |
| 04 | HTTP connection manager (HTTP/1.1) + route match + router filter + direct_response | 03 | done | 04.1, 04.2, 04.3 | HTTP/1.1 routing fixture green |
| 04.1 | HTTP/1.1 codec + HCM scaffold + minimal routing + direct_response + fixture 0007 | 03 | done | — | fixture 0007-http1-direct-response green; envoy-http1 crate + HCM as network filter + RouteConfig schema (prefix + path matchers) + direct_response action + harness Driver::Http1 land |
| 04.2 | HTTP route header matcher fan-out (all 7 modes) + ADR-0021 (regex permitted foundation) | 04.1 | done | — | no new fixture; fixture 0007 amended to exercise a matcher route; envoy-config gains all 7 HeaderMatcher modes + StringMatcher + invert_match; regex dep landed under ADR-0021 |
| 04.3 | Upstream HTTP/1.1 origination + router proxy arm + http1-echo-server helper + fixture 0008 | 04.2 | done | — | fixture 0008-http1-router-upstream green; envoy-http1::Client lands; router filter's Route arm proxies to cluster; parent phase 04 flips done |
| 05 | HTTP/2 downstream + upstream (low-level framer, own conn mgr) | 04 | done | 05.1, 05.2, 05.3, 05.4 | HTTP/2 fixture green; `h2spec` above threshold |
| 05.1 | Fixture-hardening preamble: `ClusterType::StrictDns` + 5-fixture coordinated edit + closes phase-02.1 REVIEW I3 (phase-04.3 REVIEW C-1 partially closed; full close deferred to follow-up sub-phase) | 04 | done | — | no new fixture; envoy-config schema gains `STRICT_DNS` cluster type; envoy-cluster `from_bootstrap` lifted to `async` with `tokio::net::lookup_host` STRICT_DNS branch; 5 fixture YAMLs flipped `STATIC` → `STRICT_DNS`; phase-02.1 REVIEW I3 closed; ADR-0023 landed; Docker-gated 5-fixture re-baseline NOT met (0008 surfaces a different defect post-flip; closure deferred to a follow-up sub-phase per REVIEW.md §5 R1) |
| 05.2 | `envoy-http2` foundation + downstream H2C HCM + fixture 0009 + `h2spec` ≥95% gate | 05.1 | done | — | fixture 0009-http2-direct-response green; envoy-http2 crate (sole dep on `h2`) + listener-side `Http2ProtocolOptions` + HCM-on-H2 dispatch + harness `Driver::Http2` + tests/conformance/h2spec/ runner at ≥95% pass |
| 05.3 | Upstream H2C origination + router H2-arm + http2-echo-server helper + fixture 0010 | 05.2 | done | — | fixture 0010-http2-router-upstream green; envoy-http2::Client lands; router H2-arm dispatches H1-or-H2 by cluster.upstream_protocol; parent phase 05 flips done |
| 05.4 | Fixture-hardening follow-up: 6 root-cause fixes substantively closing phase-04.3 REVIEW C-1 (helper bind 0.0.0.0; `dns_lookup_family: V4_ONLY`; envoy-config DnsLookupFamily schema; STRICT_DNS settle-time bump; envoy-http1 CL: 0 suppression; `tls_inspector` listener filter) | 05.1 | done | — | no new fixture; all 5 affected Docker-gated fixtures (0003/0004/0005/0006/0008) restored to green simultaneously + 3 unaffected (0001/0002/0007) remain green; envoy-config gains `Cluster.dns_lookup_family` field + `DnsLookupFamily` enum + `Listener.listener_filters` parse-and-ignore field; envoy-http1::client suppresses synthetic `content-length: 0` on empty-body GET (RFC 7230 §3.3.2 + Envoy v1.33 parity); 3 echo-server helpers bind 0.0.0.0; STRICT_DNS settle time 500ms → 2000ms for `host_gateway = true` fixtures; ADR-0024/0025/0026 landed; sibling under parent-05 (NOT a child of 05.1) per the 05.1 state-6 disposition decision |
| 06 | Access log (file sink, Envoy default format) + stats + Prometheus admin endpoint | 05 | done | 06.1, 06.2, 06.3 | access log + Prometheus fixtures green |
| 06.1 | `envoy-stats` foundation + `envoy-admin` HCM-backed listener migration + Prometheus exposition + fixture 0011 | 05 | done | — | fixture 0011-admin-stats-prometheus green; envoy-stats crate (counter/gauge primitives, hierarchical StatsRegistry, Prometheus text-exposition emitter) + envoy-admin crate (HCM-backed admin listener; `/ready` + `/stats` + `/stats/prometheus`; HTTP/1.1 only) + phase-01 admin migration + representative stats wiring (one counter per layer) + envoy-config schema additions (`Admin.access_log_path` parse-and-ignore; `HttpConnectionManagerConfig.stat_prefix` parse-and-consume) + harness `Driver::AdminScrape` + `BodyRule::PrometheusExposition` + first-time population of BEHAVIOR_CONTRACT.md `Stat-name mapping` initial entries |
| 06.2 | `envoy-accesslog` foundation + Envoy default-format emitter + HCM access-log wiring + fixture 0012 | 06.1 | done | — | fixture 0012-access-log-file-sink green; envoy-accesslog crate (AccessLogRecord, FileSink, hand-rolled default-format emitter, hand-rolled ISO-8601 timestamp emitter; `Sink` trait deferred per parent SPEC §3 D8.2 option (c)) + envoy-config schema additions (`HttpConnectionManagerConfig.access_log:` block; file-sink-only validator gate; `ConfigError::UnsupportedAccessLogType`) + HCM on-response-complete wiring (fire-and-forget) + harness `Driver::Http1WithAccessLog` + per-token `AccessLogLineRule` + first-time population of BEHAVIOR_CONTRACT.md `Access log field mapping` (14 default-format token rows) |
| 06.3 | Comprehensive stats wiring + 05.3 REVIEW I1 closure + parent-06 close | 06.2 | done | — | no new fixture; fixture 0011 expectations.yaml extended; comprehensive Envoy stat tree wired at HCM/router/listener/cluster (per-response-class HCM counters; connection-lifetime gauges; upstream-rq counters; access-log line counter; listener accept-failure counter); 05.3 REVIEW I1 (silent H1-listener × H2-cluster misnegotiation) closed at Task 1 via `ConfigError::Http2ClusterFromHttp1Listener` parse-time validator gate; BEHAVIOR_CONTRACT.md `Stat-name mapping` extended; parent phase 06 flips done at this sub-phase's state-6 commit per ROADMAP-schema invariant |
| 07 | Filter chain framework: iteration protocol, per-route config, extension registry | 06 | done | 07.1, 07.2 | framework fixtures green; trivial pluggable filter covers all iteration states |
| 07.1 | `envoy-filter` foundation + HCM filter-chain wiring (H1 5-writer-arm refactor + H2 finalize_h2_stream refactor) + terminal-router validator | 06 | done | — | no new fixture; regression-equivalence proven via all 12 existing fixtures (0001-0012) green simultaneously at Docker-gated CI; framework crate + HCM integration land as the foundation slice |
| 07.2 | `envoy.filters.http.header_mutation` filter + fixture 0013 + parent-07 close-out | 07.1 | done | — | fixture 0013-http-filter-header-mutation green; HeaderMutation bilaterally verified on decode + encode; parent phase 07 flips done |
| 08 | Minimum admin API (config_dump, stats, clusters, listeners, ready, server_info) + graceful drain | 07 | done | 08.1, 08.2 | admin + drain fixtures green |
| 08.1 | Admin endpoint surface (config_dump, server_info, clusters, listeners) + 06.1 carryforward closures (I2/M1/M4) + fixture 0014 | 07 | done | — | fixture 0014-admin-config-dump-server-info green; envoy-admin gains 4 new GET endpoints + AdminEndpoint::dispatch refactor + Bootstrap Serialize derive cascade; harness gains BodyRule::JsonShape + BodyRule::TextLines |
| 08.2 | Endpoint-triggered drain (drain_listeners, healthcheck/fail, healthcheck/ok) + DrainState + listener observation + fixture 0015 + parent-08 close (MVP-trunk close) | 08.1 | done | — | fixture 0015-admin-drain-listeners green; envoy-listener gains DrainState module re-exported from envoy-admin; 3 new POST admin endpoints + listener accept-loop drain observation + 3 new drain-related gauges; parent phase 08 flips done; MVP trunk 00→08 complete |

---

## Feature Families — phases 09 and onward (headings only)

These are seeded as headings only. Each family becomes one or more concrete phase rows when it enters `in-progress`, at which point it is brainstormed and split (§6) as reality demands. Do **not** expand them into per-phase rows prematurely.

### HTTP filters family

Header manipulation, cors, compression, fault, local+global rate limit, jwt_authn, rbac, ext_authz, ext_proc, oauth2, csrf, buffer, lua, wasm, adaptive concurrency, admission control, bandwidth limit.

| id | title | depends-on | status | sub-phases | summary |
|---|---|---|---|---|---|
| 09 | envoy.filters.http.local_ratelimit + fixture 0016 + 07.2 REVIEW M1 close | 07 | done | — | fixture 0016-http-filter-local-rate-limit green; envoy-filter gains LocalRateLimitFilter (hand-rolled token bucket + decode-side StopAndSend with 429 + body `local_rate_limited` per ADR-0033) + HttpFilterInstance::LocalRateLimit variant; envoy-config gains LocalRateLimit + TokenBucket schema + 4 new ConfigError variants; 07.2 REVIEW M1 closed (severed `position` plumbing deleted); ADR-0033 mid-execution corrective sequence per upstream Envoy v1.33 empirical parity |
| 10 | envoy.filters.http.rbac + fixture 0017 + 09 REVIEW M2 + M3 close | 07 | done | — | fixture 0017-http-filter-rbac green; envoy-filter gains RbacFilter (hand-rolled recursive tree-walk evaluator + Allow/Deny actions + Any/Header/AndRules/OrRules/NotRule Permission + Any/Header/AndIds/OrIds/NotId Principal + decode-side StopAndSend with 403 + body `"RBAC: access denied"` (19 bytes per ADR-0034) via ADR-0033 H1 HCM decorate_filter_synth_response helper) + HttpFilterInstance::Rbac variant; envoy-config gains Rbac + Policy + Permission + Principal schema + 6 new ConfigError variants; 09 REVIEW M2 closed (ADR-0033 Consequences amendment per preferred close shape (a)) + M3 closed (Task 7 backstop tokio::process::Command + kill_on_drop discipline) |
| 11 | envoy.filters.http.fault (abort) + fixture 0018 + 09 REVIEW M2 impl close | 07 | done | — | fixture 0018-http-filter-fault green (H2 listener); envoy-filter gains FaultFilter (operator-configured HTTP-status abort + optional header-match gate reusing 04.2 HeaderMatcher + deterministic 0%/100% percentage + decode-side StopAndSend) + HttpFilterInstance::Fault variant; envoy-config gains FaultConfig + FaultAbort + FractionalPercent + DenominatorType schema + ~3 new ConfigError variants (fractional percentage rejected per deterministic-only scope); envoy-http2 gains decorate_filter_synth_response_h2 helper closing the 09 REVIEW M2 H2 HCM decoration gap (implementation arm; first HTTP-filter exercised on H2) |
| 22 | envoy.filters.http.jwt_authn (RS256, inline JWKS, single-provider rules) + new envoy-jwt crate + fixture 0030 | 07 | done | — | (done; SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/22-http-filter-jwt-authn/`, scope locked by ADR-0055, §6.2 verified + reconciled by ADR-0056; CLOSED 2026-06-09 at the state-6 close-out) fixture 0030-http-filter-jwt-authn green (H1 listener; all 30 Docker-gated fixtures green simultaneously); the FOURTH concrete HTTP-filter-family phase (after 09 local_ratelimit / 10 rbac / 11 fault) and the FIRST application-layer crypto in the project; envoy-filter gains JwtAuthnFilter (sixth HttpFilterInstance variant; self-contained rule matching via rules[].match reusing 04.x RouteMatch — needs NO per-route typed_per_filter_config; default Authorization: Bearer extraction; iss/aud/exp/nbf validation; decode-side StopAndSend 401 with Envoy-faithful failure bodies + WWW-Authenticate, decorated by the existing H1/phase-11-H2 filter-synth helpers); NEW isolated envoy-jwt crate (JWT/JWKS parse + RS256 verify + claim validation; hand-rolled base64url; uses aws-lc-rs — the D-3.2 permitted crypto provider, reinterpreted by ADR-0055 to cover JWT signature verification — as a direct dep; clean leaf DAG, the envoy-health isolation precedent); envoy-config gains JwtAuthnConfig + JwtProvider + RequirementRule + JwtRequirement (+ minimal DataSource) schema + provider-ref/JWKS-parse/RouteMatch validators + ~4 ConfigError variants; conditional http.<stat_prefix>.jwt_authn.{allowed,denied} stats (§6.2-verified namespace); a new envoy-jwt fuzz target over the JWKS/JWT parse surface (§7.4); static pre-signed test tokens (valid far-future-exp / tampered / expired-past-exp) + inline JWKS committed under the fixture inputs (deterministic, zero clock-sensitivity); minimum-viable scope per ADR-0055 (defers remote JWKS + caching, ES256/HS256/other algorithms, requires_any/all/allow_missing combinators, custom token sources, payload/claim forwarding, clock_skew config, JWT cache, and per-route typed_per_filter_config [the natural cors close site]); scope locked by ADR-0055 at the state-1 brainstorm; §6.2-verified at the state-2 PLAN-write (LOCAL Docker — no virtiofs/inotify dependency: the aws-lc-rs RSA-verify API path [the split-gate swing factor], the per-class 401 body bytes, the WWW-Authenticate value, the stat namespace, the on-success forwarding + no-matching-rule dispositions); §6.2 RESOLVED at the state-2 PLAN-write (ADR-0056 FIRED — crypto-API is the clean PublicKeyComponents::verify path [no DER/feature-flag → SINGLE-PHASE, ADR-0057 unfired]; material divergences: audience-not-allowed is 403 not 401, WWW-Authenticate is the dynamic Bearer realm="http://<Host><path>", body strings corrected, no-matching-rule=ALLOW, forward default strips Authorization, stat namespace confirmed); 12-task single-phase PLAN; fixture 0030 drives 5 probes [200/401-missing/401-verify-fails/401-expired/403-audience]; REVIEW.md APPROVED (0 Critical / 0 Important / 7 non-gating Minors M22-1…M22-7; all 4 concern-clusters CLEAN; all 5 named focus items verified — the sixth consecutive clean state-5 after 17, 18, 19, 20, 21); differential evidence = CI anchor `27179560746` (all 30 Docker-gated fixtures 0001–0030 + h2spec ≥95% + both fuzzers [parse_bootstrap + jwt_parse], green on Linux); after this phase the HTTP-filters family's surface covers local_ratelimit (09) + rbac (10) + fault (11) + jwt_authn (22) |
| 24 | envoy.filters.http.csrf (per-route typed_per_filter_config — the SECOND consumer) + fixture 0032 | 04 07 23 | done | — | (done; SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/24-http-filter-csrf/`, scope locked by ADR-0060 at the state-1 brainstorm, §6.2 verified + reconciled by **ADR-0061** at the state-2 PLAN-write [single-phase, ADR-0062 reserved-but-unfired]; REVIEW.md APPROVED [0 Critical / 0 Important / 4 non-gating Minor M24-1…M24-4]; CLOSED 2026-06-13 at the state-6 close-out. **ADR-0061 reshape of the projection below:** Envoy's CSRF uses ONE required-`filter_enabled` `CsrfPolicy` at BOTH the chain AND per-route levels (NOT a near-empty `Csrf` chain entry — `HttpFilterTypedConfig::Csrf(CsrfPolicy)` + a new `RuntimeFractionalPercent`); origin comparison is on scheme-stripped `host[:port]` (not scheme+host+port); the chain policy is an always-applied base a per-route policy REPLACES; 403 body is `Invalid origin` [14 bytes]; stats are mutually exclusive.) fixture 0032-http-filter-csrf green (all 32 Docker-gated fixtures 0001–0032 green simultaneously, verified at the AUTHORITATIVE Linux CI anchor `27457698815` at code-HEAD `9b0e7b925`); the SIXTH concrete HTTP-filter-family phase (after 09 local_ratelimit / 10 rbac / 11 fault / 22 jwt_authn / 23 cors) and the SECOND consumer of the phase-23 per-route `typed_per_filter_config` infrastructure (proving it generalizes additively per the §6.3 anti-pattern — phase 23 registered only the `Cors` variant); envoy-config gains a `Csrf(CsrfPolicy)` variant on `PerFilterConfig` (the second, after `Cors`) + a near-empty filter-chain `Csrf` entry on `HttpFilterTypedConfig` + a `CsrfPolicy` schema (`additional_origins: Vec<StringMatcher>` reusing the 04.x `StringMatcher` + `filter_enabled` honored ONLY at deterministic 0%/100% per the phase-11 fault precedent) + reuse of the existing `PerRouteConfigForAbsentFilter` all-fatal validator + any new ConfigError variant for a non-deterministic `filter_enabled`; envoy-filter gains `CsrfFilter` (the EIGHTH `HttpFilterInstance` variant; the FIRST decode-side-only per-route filter — for modify methods [POST/PUT/DELETE/PATCH + the non-safe set, §6.2-verified] compute the source origin [`Origin`, fallback `Referer`] vs the target origin [`Host`/`:authority`], valid if equal or matches `additional_origins` → Continue, else/missing-source → `Decision::StopAndSend` 403 + the Envoy CSRF failure body decorated by the existing H1/phase-11-H2 filter-synth helpers; per-route policy threaded via the existing phase-23 `apply_route_config` hook — NO HCM change); conditional `http.<stat_prefix>.csrf.{request_valid,request_invalid,missing_source_origin}` stats (§6.2-verified namespace); fixture 0032 drives 5 deterministic header-only probes over an H1 listener proxying to a real upstream cluster (per ADR-0058 L6) [same-origin-POST→200 / evil-origin-POST→403 / additional-allowed-POST→200 / safe-GET→200 / no-source-POST→403]; an in-process backstop; a new `parse_bootstrap` fuzz seed (no new fuzz target — CSRF reuses `StringMatcher`); minimum-viable scope per ADR-0060 (defers `filter_enabled` runtime fractional gating + `shadow_enabled` shadow mode, vhost-level config + the route>vhost most-specific-config cascade, `typed_per_filter_config` for filters OTHER than cors/csrf, streaming/trailers + gRPC-Web CSRF); scope locked by ADR-0060 at the state-1 brainstorm; the lightest filter phase since the framework was completed (the cross-cutting HCM route-early-resolution risk landed at phase 23 — phase 24 is a near-pure additive plug-in); §6.1 split valve held in nominal reserve as 24.1 (schema + wiring + validator) / 24.2 (CsrfFilter + stats + fixture 0032 + close) and DID NOT fire; ADR-0061 FIRED at the §6.2 reconciliation (state-2 PLAN-write), ADR-0062 reserved-but-unfired for the split |
| 23 | envoy.filters.http.cors + per-route typed_per_filter_config infrastructure + fixture 0031 | 04 07 | done | — | fixture 0031-http-filter-cors green; the FIFTH concrete HTTP-filter-family phase (after 09 local_ratelimit / 10 rbac / 11 fault / 22 jwt_authn) and the FIRST per-route (route-scoped) filter configuration in the project; envoy-config gains a `typed_per_filter_config` map on `Route` + a `PerFilterConfig` `@type`-tagged enum (only the `Cors` variant registered this phase) + a `CorsPolicy` schema (`allow_origin_string_match` reusing the 04.x `StringMatcher` + `allow_methods`/`allow_headers`/`expose_headers`/`max_age`/`allow_credentials`) + the near-empty filter-chain `Cors` entry + ~2 ConfigError variants; the HCM request-processing ordering changes so the matched route is resolved BEFORE / available to the filter pipeline (today H1 `hcm.rs:612` runs the pipeline before `build_response` matches the route at `:1199`), threading the matched route's per-filter config into the pipeline on BOTH H1 + H2 — inert for every non-CORS filter (regression-equivalence: all 30 pre-existing fixtures stay green, the 07.1 foundation-slice property since no existing filter reads the route); envoy-filter gains `CorsFilter` (seventh `HttpFilterInstance` variant; decode-side CORS-preflight short-circuit [`OPTIONS` + `Origin` + `Access-Control-Request-Method` → 200 + `access-control-*` via `Decision::StopAndSend`, decorated by the existing H1/phase-11-H2 filter-synth helpers] + encode-side actual-request response decoration [`access-control-allow-origin` etc. via the phase-07.2 header_mutation push pattern] — the first genuinely encode-active feature filter); conditional `cors.{origin_valid,origin_invalid}` stats (§6.2-verified namespace); fixture 0031 drives 4 probes [preflight-OPTIONS(allowed)→200+headers / GET(allowed)→200+allow-origin / GET(disallowed)→200+no-allow-origin / GET(no-origin)→200+unchanged] over an H1 listener — deterministic, header-only, zero timing/crypto; a new `parse_bootstrap` fuzz seed (no new fuzz target — CORS reuses `StringMatcher`); in-process backstop; minimum-viable scope per ADR-0057 (defers `filter_enabled`/`shadow_enabled` runtime gating, `allow_private_network_access`, vhost-level config + the route>vhost most-specific-config cascade, `typed_per_filter_config` for filters OTHER than cors [each slots into `PerFilterConfig` additively], the deprecated inline `route.cors` field, gRPC-Web `bypass_cors_preflight`); scope locked by ADR-0057 at the state-1 brainstorm; the per-route-config infrastructure is the gating unlock for cors AND the future per-route-configured filters (csrf/buffer + per-route fault/rbac/local_ratelimit overrides); §6.1 split valve held in reserve as 23.1 (per-route-config schema + HCM route-early-resolution+threading — a foundation slice, no new fixture) / 23.2 (CorsFilter + variant + stats + fixture 0031 + close); ADR-0058 reserved for the §6.2 reconciliation, ADR-0059 for the split |
| 25 | envoy.filters.http.buffer (per-route typed_per_filter_config — the THIRD consumer) + H1 request-body forwarding + fixture 0033 | 04 05 07 23 | done | 25.1, 25.2 | (parent — **SPLIT into `25.1` + `25.2` per ADR-0064** at the state-2 PLAN-write; §6.2 verified + reconciled by **ADR-0063** [413 body `Payload Too Large` 17B via the existing decorate helpers; reject strictly `> max_request_bytes`; NO buffer-scoped stats → SPEC §2.1 row DROPPED, the over-limit 413 reflected only in the generic HCM `downstream_rq_too_large`; `Buffer`/`BufferPerRoute` wire shapes confirmed as projected; absent-`buffer` per-route config accepted-inert by Envoy → reuse the generic `PerRouteConfigForAbsentFilter` validator verbatim]; parent flips `done` only after both sub-phases are `done`. SPEC at `docs/envoy-rust/phases/25-http-filter-buffer/`, scope locked by ADR-0062 at the state-1 brainstorm) target: fixture 0033-http-filter-buffer green on an H1 listener proxying to a real `http1-echo-server` cluster (all 33 Docker-gated fixtures 0001–0033 green simultaneously); the SEVENTH concrete HTTP-filter-family phase (after 09 local_ratelimit / 10 rbac / 11 fault / 22 jwt_authn / 23 cors / 24 csrf), the THIRD consumer of the phase-23 per-route `typed_per_filter_config` infrastructure (after cors+csrf), AND the project's FIRST body-dependent HTTP filter (the easy header-only filter vein is exhausted — all 7 shipped filters are header-only decode-side decisions; the framework is headers-only with no streaming decode_data hook). **Two-part scope (§6.1 split LIKELY):** Part A (projected 25.1) closes the pre-existing H1 request-body-forwarding gap — today the H1 router forwards an always-empty body upstream (`hcm.rs:356`) and drains+discards the downstream Content-Length body (`:678-697`), so envoy-rust cannot proxy an H1 POST body at all; Part A reads the body into a `Bytes`, populates `FilterRequest.body`, and forwards it upstream (regression-equivalence: all 32 existing fixtures green; the 07.1/12.1/14.1/23.1 foundation-slice pattern; chunked/streaming bodies remain a non-goal). Part B (projected 25.2) adds the `BufferFilter` (ninth `HttpFilterInstance` variant; decode-side-only `max_request_bytes`/413 guard reusing the existing H1/H2 filter-synth helpers; per-route `BufferPerRoute` disable/limit-override threaded via the existing phase-23 `apply_route_config` hook — NO HCM change) + `Buffer { max_request_bytes }` chain config + `BufferPerRoute` as the third `PerFilterConfig` variant + reuse of the `PerRouteConfigForAbsentFilter` validator + conditional `http.<stat_prefix>.buffer.*` stats (namespace §6.2-verified) + fixture 0033 (5 deterministic header+body probes: within-limit→200+echoed-body / over-limit→413 / per-route-disabled→200 / per-route-lowered-limit→413 / GET-no-body→200) + an in-process backstop + a new `parse_bootstrap` fuzz seed (no new fuzz target). H2 already buffers+forwards request bodies so the filter works on H2 for free (no H2 fixture required this phase). Minimum-viable scope per ADR-0062 (defers chunked/streaming bodies, the generic streaming decode_data framework hook, encode-side buffering, vhost-level BufferPerRoute + the route>vhost cascade, typed_per_filter_config for filters OTHER than cors/csrf/buffer, gRPC/trailers); scope locked by ADR-0062 at the state-1 brainstorm **(done - parent CLOSED at the 25.2 state-6 close-out 2026-06-15: both sub-phases `25.1` + `25.2` `done`; all 33 Docker-gated fixtures `0001`-`0033` green simultaneously; state-4 §7.5 gate GREEN [AUTHORITATIVE Linux CI `27510477930` at `fde99b984`]; state-5 `REVIEW.md` APPROVED 0 Critical / 0 Important / 4 Minor; artifacts at `docs/envoy-rust/phases/25.2-http-filter-buffer/`, parent SPEC at `docs/envoy-rust/phases/25-http-filter-buffer/`)** |
| 25.1 | H1 request-body forwarding (Content-Length-delimited) — read the downstream body into a `Bytes` before the filter pipeline, expose it as `FilterRequest.body`, forward it upstream (no new fixture) | 04 07 | done | — | Part A of parent `25` (the ADR-0064 split). The FOUNDATION SLICE closing the pre-existing H1 request-body-forwarding gap: today the H1 router forwards an always-empty body upstream (`crates/envoy-http1/src/hcm.rs:356`) and drains-and-discards the downstream Content-Length body (`:678-697`), so envoy-rust cannot proxy an H1 POST body at all (a gap since phase 04.3). D1: relocate the body read to BEFORE the filter pipeline (accumulate into a `Bytes`, set `req.body`), forward `out_req.body = req.body.clone()` (per-attempt, replay-safe, ADR-0044), and remove the discard-drain. Load-bearing invariant: **all 32 existing Docker-gated fixtures (0001–0032) green simultaneously** — an always-active hot-path change, but `body_len == 0` for every existing bodyless / `content-length: 0` fixture so the relocated read is a body-wise no-op (regression-equivalent). NO new fixture, NO new crate, NO new dependency; chunked/streaming request bodies remain a non-goal (the `:656` 501 rejection is preserved). The body-forwarding capability is verified by an in-process capturing-upstream unit test; it is DIFFERENTIALLY proven by 25.2's fixture 0033 (within-limit→200+echoed body) — the 07.1→07.2 foundation-slice-exercised-by-its-consumer pattern. SPEC + PLAN + PROGRESS at `docs/envoy-rust/phases/25.1-h1-request-body-forwarding/`; scope locked by ADR-0064 at the state-2 PLAN-write/split. (done; SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/25.1-h1-request-body-forwarding/`; state-3 subagent-driven implementation [chain `20ce64010`…`0c8b66512`], state-4 §7.5 gate GREEN [AUTHORITATIVE Linux CI anchor `27476243121` at code-HEAD `ca023ce48` — all 32 Docker-gated fixtures 0001–0032 + h2spec ≥95%; local Docker differential corroborated], and state-5 code review [REVIEW.md APPROVED — 0 Critical / 0 blocking-Important / Minor-only M25.1-1+M25.1-2 carried to `25.2`] all complete; CLOSED 2026-06-13 at the state-6 close-out. Parent row `25` stays `in-progress` until `25.2` also closes) |
| 25.2 | envoy.filters.http.buffer (BufferFilter + `Buffer` chain config + `BufferPerRoute` — the THIRD `typed_per_filter_config` consumer) + fixture 0033 + parent-25 close | 23 25.1 | done | — | Part B of parent `25` (the ADR-0064 split). The §6.2 wire shapes are LOCKED by **ADR-0063**: the 413 over-limit reply is `Payload Too Large` (17 bytes, no trailing newline) + `content-type: text/plain` via the existing `decorate_filter_synth_response{,_h2}` helpers; reject strictly `> max_request_bytes`; **NO buffer-scoped stats** (the SPEC §2.1 row DROPPED — the over-limit 413 is reflected only in the generic HCM `downstream_rq_too_large`, not asserted by the fixture); `Buffer { max_request_bytes: u32 }` chain config + `BufferPerRoute` oneof `{ disabled, buffer }` as the third `PerFilterConfig` variant; reuse the generic `PerRouteConfigForAbsentFilter` validator verbatim (buffer covered for free). D2–D6: `BufferFilter` (the ninth `HttpFilterInstance` variant, decode-side-only: `body.len() > effective_max → 413` else Continue) + schema + variant/`build`/`apply_route_config` dispatch (NO HCM change) + fixture 0033 (H1 → real `http1-echo-server`; 5 probes: within-limit→200+echo / over-limit→413 / per-route-disabled→200 / per-route-lowered→413 / GET-no-body→200) + an `http1_probe_list` POST-body harness extension + an in-process backstop + a new `parse_bootstrap` fuzz seed (no new fuzz target). Flips parent `25` to `done` at its state-6 close. SPEC at `docs/envoy-rust/phases/25.2-http-filter-buffer/`; the 25.2 state-2 PLAN-write authors `PLAN.md` AFTER 25.1 closes. Residual (ADR-0063): verify the absent / `0` / malformed `max_request_bytes` disposition at the 25.2 PLAN-write (projected all-fatal per ADR-0049) **(done; SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/25.2-http-filter-buffer/`; state-3 subagent-driven implementation [T1-T7, commit chain `ecc674a9d`…`b624b8288`], state-4 §7.5 gate GREEN [all 33 Docker-gated fixtures `0001`-`0033` green simultaneously; AUTHORITATIVE Linux CI `27510477930` at `fde99b984`; h2spec 173 passed / 0 failed; `parse_bootstrap` fuzz 815,824 runs / 0 crash], and state-5 code review [REVIEW.md APPROVED - 0 Critical / 0 Important / 4 Minor, all non-blocking] all complete; CLOSED 2026-06-15 at the state-6 close-out, flipping parent `25` to `done`. The 4 Minor carry-forwards roll into phase-26 planning)** |
| 31 | envoy.filters.http.cdn_loop (RFC 8586 `CDN-Loop` request-header filter: parse → count `cdn_id` → reject-on-loop or append-and-forward) + fixture 0039 | 07 | done | — | (DONE — CLOSED 2026-06-21 at the phase-31 state-6 close-out: **REVIEW.md APPROVED [0 Critical / 0 Important / 3 Minor]**; the **§7.5 gate GREEN at the AUTHORITATIVE Linux CI run `27915239054` @ `a2051b2`** — fixture `0039-http-filter-cdn-loop` cross-proxy STRONG byte-exact + all `0001`-`0038` green simultaneously + h2spec ≥95% + the `parse_bootstrap`/`cdn_loop_parse` fuzz + build/clippy/fmt/test/deny; the 7 task commits `71e43cd`…`583e7c2` + the state-4 CI-fuzz-wiring fix `a2051b2` + the state-5 close `177a866`; `#![forbid(unsafe_code)]` holds; SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/31-http-filter-cdn-loop/`. Original in-progress brainstorm/PLAN scope below — SPEC + PLAN at `docs/envoy-rust/phases/31-http-filter-cdn-loop/`, scope locked by **ADR-0076** at the state-1 brainstorm; **§6.2 EMPIRICALLY VERIFIED LOCALLY at the state-2 PLAN-write — ADR-0077 LOCKS the wire facts** [against live `envoyproxy/envoy:v1.33.0`: loop→**502** `The server has detected a loop between CDNs.` (44B, `cdn_loop_detected`), malformed→**400** `Invalid CDN-Loop header in request.` (35B, `invalid_cdn_loop_header`), append `cdn_id` **COMMA-ONLY** (the SPEC §1.3 comma-space example CORRECTED), case-SENSITIVE parameter-ignoring match, strict RFC-7230 token grammar (quoted-string/non-tchar id → 400; empty list entries NOT malformed), multi-`CDN-Loop`-header coalescing, ALL config-validity (empty/comma/invalid `cdn_id`) boot-fatal, NO stat]; all four headline projections CONFIRMED → the STRONG differential FIRES (cross-proxy byte-identical reject/append); PLAN authored (~6–8 tasks, plan-reviewed → APPROVED); **§6.1 single-phase confirmed, ADR-0078 UNFIRED**; the state-3 implementation is the next session [`superpowers:subagent-driven-development`]; the NINTH concrete HTTP-filter-family phase, after 09 local_ratelimit / 10 rbac / 11 fault / 13 header_mutation / 22 jwt_authn / 23 cors / 24 csrf / 25 buffer; CONTINUES the HTTP-filters family now that the Load-balancing family's three DETERMINISTIC byte-exact policies [ring_hash 28 + maglev 29 + subset 30] are all done and the remaining LB policies need a contract-relaxation ADR [least_request/random] or active-HC health state [priority/panic/locality-weighted]. cdn_loop is a decode-side, header-only, FULLY self-contained filter [RFC 8586 `CDN-Loop`]: parse the inbound `CDN-Loop` header [a comma-separated `cdn-info` list], count occurrences of this CDN's configured `cdn_id`, and either REJECT the request [`count > max_allowed_occurrences`, default 0 → a forwarding loop; projected 502] / reject a malformed header [projected 400] / else APPEND `cdn_id` to the `CDN-Loop` header and forward upstream. **The differential is byte-exact + DETERMINISTIC** [the appended `cdn_id` + the reject body are static config / fixed strings → cross-proxy byte-identical, with none of the per-side host-address dependence that weakened the 28/29 consistent-hash endpoint-strings or the rejected `stateful_session` cookie] and **LOCALLY observable** [a normal request/response, no file-watch/reload trigger — like 22/23/28/29/30]. D-items: a `CdnLoopConfig { cdn_id, max_allowed_occurrences = 0 }` schema + a `CdnLoop` variant in the `@type`-tagged HTTP-filter config enum [`type.googleapis.com/envoy.extensions.filters.http.cdn_loop.v3.CdnLoopConfig`] + a `cdn_id` validator [`crates/envoy-config/src/bootstrap.rs`]; a small RFC 8586 `CDN-Loop` header parser [the correctness gate]; a `CdnLoopFilter` [the 9th `HttpFilterInstance` variant, `crates/envoy-filter/`] decode-side count/append/reject reusing the existing decode-side `Decision::StopAndSend` + H1/phase-11-H2 filter-synth local-reply decorators + the request-header-append path [NO new pipeline machinery]; fixture `0039-http-filter-cdn-loop` [an H1 listener + the cdn_loop filter + an echo `--body-marker` backend; 4 probes — no-header→200+append / self-`cdn_id`-present→loop-reject / foreign-id→200+append-after / malformed→parse-reject]; an in-process backstop; a `parse_bootstrap` fuzz seed [+ a dedicated CDN-Loop-parser fuzz target if the state-2 PLAN-write decides]; a BEHAVIOR_CONTRACT "HTTP filters" cdn_loop subsection. Load-bearing invariant: all 38 pre-existing fixtures `0001`–`0038` stay green [the filter is INERT when not in the chain — the 07.1 foundation-slice regression-equivalence property; no existing filter reads `CDN-Loop`]. minimum-viable scope per ADR-0076 [defers per-route `typed_per_filter_config` for cdn_loop (additive later, the cors/csrf precedent); RFC 8586 `cdn-info` PARAMETER semantics beyond counting; other deterministic HTTP filters (header_to_metadata/set_metadata — internal-metadata output, need a `%DYNAMIC_METADATA%` access-log operator; stateful_session — non-byte-exact cookie; compressor/decompressor — byte-identical-gzip risk; grpc_*/ext_authz/ext_proc/lua/wasm/oauth2 — gRPC-blocked or external)]. The core differential risk [envoy-rust must replicate Envoy's EXACT reject status/body + `CDN-Loop` append formatting] is de-risked by the state-2 §6.2 LOCAL reconnaissance [record the four probe outcomes + the parser strictness + the config-validity disposition against `envoyproxy/envoy:v1.33.0`]; **ADR-0077 reserved** for the §6.2 reconciliation, **ADR-0078 reserved** for the §6.1 split [projected NOT to fire — ~600–900 LoC / ~6–8 tasks, comparable to csrf/buffer]. The SPEC was spec-reviewed by a fresh subagent → APPROVED [0 issues; the loop→502 / malformed→400 projections independently confirmed against Envoy + RFC 8586]. The state-2 PLAN-write is the next session [`superpowers:writing-plans`]) |

### Network filters family

Redis, mongo, kafka_broker, thrift, zookeeper [scope TBD], echo, direct_response, sni_cluster, rbac network.

### Load balancing family

least_request, random, ring_hash, maglev, subset LB, locality-weighted LB, priority load balancing, panic thresholds.

| id | title | depends-on | status | sub-phases | summary |
|---|---|---|---|---|---|
| 28 | Load-balancing family opener: `RING_HASH` consistent-hash LB (route header `hash_policy` + XX_HASH ring) + fixture 0036 | 02 04 06 21 | done | — | (done; SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/28-lb-ring-hash/`, scope locked by **ADR-0069** at the state-1 brainstorm; **§6.2 EMPIRICALLY VERIFIED LOCALLY at the state-2 PLAN-write — ADR-0070 FIRED**: the ring algorithm CRACKED + EXACTLY VALIDATED 36/36 against live Envoy v1.33.0 → STRONG differential target [cross-proxy byte-identical backend selection]: xxHash64 seed 0; per-host ring key `"{ip:port}_{i}"` [load-bearing `_`]; `min_ring_size/num_hosts` replicas; sorted ring; `bisect_left` wrap; empty-present header → hashed [not the random fallback]; XX_HASH-only [MURMUR_HASH_2 all-fatal]; **§6.1 single-phase confirmed, ADR-0071 UNFIRED**; REVIEW.md APPROVED [0 Critical / 0 Important / 3 non-gating Minor M28-1…M28-3]; CLOSED 2026-06-19 at the state-6 close-out — fixture 0036-lb-ring-hash cross-proxy STRONG differential green + all 35 pre-existing fixtures 0001–0035 green simultaneously [the `pick()` round-robin path a no-op → regression-equivalence]; state-4 §7.5 gate GREEN [AUTHORITATIVE Linux CI `27837455306` @ `4b31144`]; state-5 REVIEW.md APPROVED 0C/0I/3-Minor [the thirteenth consecutive clean state-5, after 17–27]) the FIRST concrete Load-balancing-family row — opens the family the upstream-robustness / subset-LB / priority work builds on. The project has shipped only `ROUND_ROBIN` since phase 02 (`crates/envoy-cluster/src/cluster.rs:322` `pick()`; the lone `LbPolicy { RoundRobin }` at `crates/envoy-config/src/bootstrap.rs:290`). **`RING_HASH` chosen as the only DIFFERENTIABLE LB opener** — the differential contract needs byte-exact selection equivalence, which is provable only for a DETERMINISTIC policy (consistent-hashing maps a request hash key → a fixed host); `random`/`least_request` (P2C-random) are non-deterministic and defer, `maglev` (heavier permutation-table) defers. A cluster `lb_policy: RING_HASH` builds a ketama-style hash ring over its endpoints; a request carrying a route-level **header** `hash_policy` (`{ header: { header_name } }`) routes to the endpoint the hashed header value maps to (same key → same backend). **Differential is LOCALLY observable** (a normal request/response, no file-watch/reload trigger — unlike the phase-26/27 reload phases that were Linux-CI-only). D-items: a `LbPolicy::RingHash` + `RingHashLbConfig` (min/max ring size; accept-XX_HASH-only) cluster config; a route `hash_policy: Vec<HashPolicy>` (header source) on `RouteAction_Route` (today `{ cluster, retry_policy }`, `bootstrap.rs:1303`); **xxHash64 (XX_HASH, seed 0) written from scratch** (D-3.2; no hash crate exists/permitted); the ring build + next-clockwise lookup; the request hash key threaded through `pick_endpoint()`/`pick()` (the 2 per-request call sites H1 `hcm.rs:392` / H2 `hcm.rs:184` — round-robin path unchanged, key ignored); the hash-policy-absent fallback; health/outlier compose; fixture 0036 (one `RING_HASH` cluster, two distinguishable backends [the phase-27 `--body-marker` two-backend harness seed], a header `hash_policy`, a hash-key sweep asserting same-key→same-backend bilaterally + spread) + an in-process backstop + a BEHAVIOR_CONTRACT "LB selection" extension + a `parse_bootstrap` fuzz seed. Load-bearing invariant: all 35 pre-existing fixtures `0001`–`0035` stay green (the `pick()` dispatch is a no-op for `ROUND_ROBIN` clusters — regression-equivalence). minimum-viable scope per ADR-0069 (defers maglev; least_request/random; MURMUR_HASH_2; cookie/source-IP/query/filter-state hash sources + `terminal`/multi-policy/regex_rewrite; weighted-ring replicas; `use_hostname_for_hashing`; RING_HASH+EDS-hot-reload re-ring composition; subset/locality/priority LB; CDS/LDS hot-reload; the gRPC/ADS transport [still ADR-0014/H2-trailers-blocked]). The core differential risk (envoy-rust must replicate Envoy's EXACT ring + header-hash to select the SAME backend) is de-risked by the state-2 §6.2 LOCAL reconnaissance (sweep keys against `envoyproxy/envoy:v1.33.0`, record key→backend mappings, reverse-engineer the ring construction) with a documented same-key-stability fallback equivalence if exact cross-proxy identity proves intractable; **ADR-0070 FIRED** at the state-2 PLAN-write (the §6.2 reconciliation — the ring algorithm cracked + validated 36/36, STRONG target; the cross-proxy-identity risk did NOT materialize), **ADR-0071 UNFIRED** (the §6.1 split did NOT fire — single phase; ~1000–1300 LoC / 10 tasks) |
| 29 | Load-balancing family: `MAGLEV` consistent-hash LB (the phase-28 route header `hash_policy` + XX_HASH; Maglev permutation table replaces the ring) + the M28-3 discriminator refactor + fixture 0037 | 02 04 06 21 28 | done | — | (DONE — closed at the phase-29 state-6 deterministic close-out; SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/29-lb-maglev/`, scope locked by **ADR-0071** at the state-1 brainstorm; **§6.2 EMPIRICALLY VERIFIED LOCALLY at the state-2 PLAN-write — ADR-0072 FIRED**: the Maglev table algorithm CRACKED + EXACTLY VALIDATED [from-scratch replica reproduced live Envoy v1.33.0 80/80 at default `table_size`, 64/64 at M=17] → STRONG differential target [cross-proxy byte-identical backend selection, as phase 28]: host key `ip:port` [no `_i` suffix]; `offset = xxHash64(key, seed 0) % M`, `skip = xxHash64(key, seed 1) % (M-1) + 1` [seed-1 the cracked unknown]; `permutation[j] = (offset + j·skip) % M`; config-order round-robin populate [earlier host wins]; lookup `table[xxHash64(header_value, seed 0) % M]`; `table_size` default 65537 / prime / max 5000011 [non-prime + over-max all-fatal; `maglev_lb_config`-on-non-MAGLEV accept-and-ignored = Envoy parity + the phase-28 ring precedent, correcting the SPEC's fatal projection]; **§6.1 single-phase confirmed, ADR-0073 UNFIRED** [8 tasks / ~450–550 LoC]; the state-3 implementation is the next session [`superpowers:subagent-driven-development`]) the SECOND concrete Load-balancing-family row — the second (and last) DETERMINISTIC, byte-exact-differentiable LB policy, after phase-28 `RING_HASH`. **`MAGLEV` chosen as the maximal-reuse next step** — its REQUEST side is IDENTICAL to `RING_HASH` (the same route `hash_policy` header → xxHash64 → `Option<u64>` key threaded through `pick()`), so the entire phase-28 request-hash plumbing + config pattern + xxHash64-from-scratch + the two-distinguishable-backend fixture harness are reused; only the TABLE-BUILD differs (Google's Maglev prime-sized permutation table replaces the ketama ring). `random`/`least_request` (P2C) are non-deterministic → not byte-exact-differentiable, deferred. **Differential is LOCALLY observable** (a normal request/response, no file-watch/reload trigger — unlike the phase-26/27 reload phases). D-items: a `LbPolicy::Maglev` + `MaglevLbConfig` (`table_size` default 65537, prime, max 5000011) cluster config + validators (non-prime / over-max / config-on-non-MAGLEV-cluster → all-fatal per ADR-0049); a new `crates/envoy-cluster/src/maglev.rs` — the Maglev permutation-table build (per-host `offset`/`skip` from host-key hashes; round-robin populate) + lookup (`table[request_hash % table_size]`), reusing the phase-28 `xxhash.rs`; **the M28-3 discriminator refactor** — replace the phase-28 `ring: Option<HashRing>` (`ring.is_some()` == "is RING_HASH", `cluster.rs:209`) with an explicit hash-LB dispatch (e.g. `hash_lb: Option<HashLb>` with `HashLb { Ring, Maglev }`), keeping BOTH the round-robin path AND the existing RING_HASH path (fixture 0036) behavior-identical; the route `hash_policy` + request-hash threading REUSED AS-IS; fixture 0037 (one `MAGLEV` cluster, two distinguishable backends [the phase-27 `--body-marker` seed], a header `hash_policy`, a hash-key sweep asserting same-key→same-backend bilaterally + spread; clone of fixture-0036's shape incl. `discover_host_lan_ip`/`{{BACKEND_IP}}`) + an in-process backstop + a BEHAVIOR_CONTRACT "LB selection" extension (a MAGLEV row + the M28-1 doc clarification folded) + a `parse_bootstrap` fuzz seed. Load-bearing invariant: all 36 pre-existing fixtures `0001`–`0036` stay green (the M28-3 refactor is no-op for ROUND_ROBIN + behavior-identical for the RING_HASH fixture 0036 — regression-equivalence). minimum-viable scope per ADR-0071 (defers least_request/random; MURMUR_HASH_2; non-header hash sources + `terminal`/multi-policy/regex_rewrite; weighted Maglev; MAGLEV+EDS-hot-reload re-table; MAGLEV+HC/OD differential; subset/locality/priority LB; CDS/LDS hot-reload; the gRPC/ADS transport [still ADR-0014/H2-trailers-blocked]). The core differential risk (envoy-rust must replicate Envoy's EXACT Maglev table + the already-matched header-hash to select the SAME backend) is de-risked by the state-2 §6.2 LOCAL reconnaissance (sweep keys against `envoyproxy/envoy:v1.33.0`, record key→backend mappings, reverse-engineer the table-build hash inputs / `offset`-`skip` derivation / prime-`table_size` handling / populate order) with a documented same-key-stability fallback equivalence if exact cross-proxy identity proves intractable; **ADR-0072 reserved** for the §6.2 reconciliation, **ADR-0073 reserved** for the §6.1 split (projected NOT to fire — the surface is smaller than phase 28's since the request-hash plumbing + xxHash64 + the fixture harness already exist; ~700–1000 LoC / ~8 tasks). **CLOSED at state-6: the 8 task code commits `40f4e39`…`d4e31f5` landed; state-5 `REVIEW.md` APPROVED — 0 Critical / 0 Important / 2 Minor (M29-1/M29-2 → phase-30 carry-forwards: the shared `Http1HashSweep` differential driver's RING_HASH-worded `bail!` diagnostics, cosmetic/failure-output-only); §7.5 gate (a)-(f) COMPLETE at the AUTHORITATIVE Linux CI `27851283501` @ `1f2ad7b` (fixture `0037-lb-maglev` cross-proxy STRONG + all `0001`–`0036` green); ADR-0072 locked the algorithm; ADR-0073 UNFIRED (single-phase); the 14th consecutive clean state-5. `#![forbid(unsafe_code)]` holds.** |
| 30 | Load-balancing family: `subset LB` — metadata-based endpoint-subset LB (`Cluster.lb_subset_config` + endpoint `envoy.lb` metadata + route `metadata_match`) + fixture 0038 | 02 04 06 28 29 | done | — | (DONE — closed at the phase-30 state-6 deterministic close-out [REVIEW.md APPROVED 0C/0I/3 Minor; §7.5 gate (a)-(f) COMPLETE at the AUTHORITATIVE Linux CI run 27881837635 @ 1acf78c — fixture 0038-lb-subset cross-proxy route-selection STRONG green + all 0001-0037 green + h2spec + the parse_bootstrap/jwt_parse fuzz; the 9 task commits 9e6eb6e…2783e85]; SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/30-lb-subset/`, scope locked by **ADR-0073** at the state-1 brainstorm; **§6.2 EMPIRICALLY VERIFIED LOCALLY at the state-2 PLAN-write — ADR-0074 FIRED**: the subset-LB selection + fallback semantics CRACKED against live Envoy v1.33.0 → STRONG differential target [cross-proxy byte-identical backend selection]: SUPERSET match (endpoint `envoy.lb` metadata ⊇ route `metadata_match`); selector used = the one whose `keys` SET EQUALS the metadata_match keys; NO_FALLBACK no-match → `503 no healthy upstream`, ANY_ENDPOINT → round-robin all, DEFAULT_SUBSET → the default_subset subset; TWO SPEC-projection corrections — (1) subset-config validation is NOT startup-fatal [empty selectors/keys, uncovered default_subset all BOOT → envoy-rust ACCEPTS, per ADR-0049 fatal-only-where-Envoy-rejects], (2) `lb_subsets_active/created`=66 non-portable → emit NO subset stat [differential ignore-lists them]; **§6.1 single-phase confirmed, ADR-0075 UNFIRED** [~9 tasks / ~900–1100 LoC — lighter than projected since validation collapsed + no stat task]; the state-3 implementation is the next session [`superpowers:subagent-driven-development`]) the THIRD concrete Load-balancing-family row, after `RING_HASH` 28 + `MAGLEV` 29 — original brainstorm scope below: the THIRD concrete Load-balancing-family row, after `RING_HASH` 28 + `MAGLEV` 29. **`subset LB` chosen as the next DETERMINISTIC, byte-exact-differentiable LB step** — unlike the remaining LB policies it needs NEITHER a contract-relaxation ADR (`least_request`/`random` are non-deterministic P2C/random) NOR active-HC health state (`priority`/`panic-thresholds`/`locality-weighted` cannot be exercised differentially without HC/outlier — all deferred). subset LB is **NOT a `LbPolicy` value**: it is an orthogonal layer that, BEFORE the cluster's existing `lb_policy` picks, NARROWS the candidate endpoints to those whose `envoy.lb` metadata is a SUPERSET of the matched route's `metadata_match`, then runs the inner `lb_policy` (MVP ROUND_ROBIN) within the subset. **Differential is LOCALLY observable** (a normal request/response, no file-watch/reload trigger — like 28/29). D-items: endpoint `metadata: Option<Metadata>` (the `envoy.lb` namespace) on `LbEndpoint`; `Cluster.lb_subset_config: Option<LbSubsetConfig>` (`subset_selectors` [keys], `fallback_policy` [NO_FALLBACK default / ANY_ENDPOINT / DEFAULT_SUBSET], `default_subset`); route `RouteAction.metadata_match: Option<Metadata>`; a new `crates/envoy-cluster/src/subset.rs` (build the subset index over endpoints per selector; lookup by the route `metadata_match` → the matching subset; superset-match); the route `metadata_match` threaded to `pick()`/`pick_endpoint()` (the phase-28 `Option<u64>` hash-key threading is the template; a cluster with no `lb_subset_config` ignores it → no-op regression); the three `fallback_policy` dispositions (NO_FALLBACK → 503 no-healthy-upstream; ANY_ENDPOINT → all hosts; DEFAULT_SUBSET → the default subset); fixture 0038 (one subset cluster, two distinguishable backends [the `--body-marker` seed] with distinct `envoy.lb` metadata [`stage: prod` / `stage: canary`], routes with `metadata_match` asserting same-route→same-backend bilaterally + a fallback probe; clone of fixture-0037's shape incl. `discover_host_lan_ip`/`{{BACKEND_IP}}`) + an in-process backstop + a BEHAVIOR_CONTRACT "LB selection" subset row + a `parse_bootstrap` fuzz seed. Load-bearing invariant: all 37 pre-existing fixtures `0001`–`0037` stay green (the subset narrowing is a no-op when `lb_subset_config` is absent — regression-equivalence for the round-robin AND RING_HASH/MAGLEV paths). minimum-viable scope per ADR-0073 (defers least_request/random; priority/panic-thresholds/locality-weighted LB; subset+consistent-hash-inner-LB; multiple overlapping selectors / per-selector fallback / single_host_per_subset / list_as_any / metadata_fallback_policy / scale_locality_weight; subset+EDS-hot-reload; subset+HC/OD differential; CDS/LDS hot-reload; the gRPC/ADS transport [still ADR-0014/H2-trailers-blocked]). The core differential risk (envoy-rust must replicate Envoy's EXACT subset selection + fallback to select the SAME backend) is de-risked by the state-2 §6.2 LOCAL reconnaissance (record `metadata_match`→backend mappings + the fallback dispositions + the wire shape against `envoyproxy/envoy:v1.33.0`); **ADR-0074 reserved** for the §6.2 reconciliation, **ADR-0075 reserved** for the §6.1 split (projected NOT to fire but the closest call since phase 22 — ~1100–1500 LoC / ~12–15 tasks; the seam is 30.1 config+engine / 30.2 pick-threading+fallback+fixture+close). Weighs the open M29-1/M29-2 carry-forwards (the shared `Http1HashSweep` driver's RING_HASH-worded diagnostics — fold if fixture 0038 reuses that driver). The state-2 PLAN-write is the next session [`superpowers:writing-plans`]) |

### Upstream robustness family

Active health checks HTTP/TCP/gRPC/custom, outlier detection variants, circuit breakers, retries + hedging, per-protocol connection pooling.

| id | title | depends-on | status | sub-phases | summary |
|---|---|---|---|---|---|
| 12 | active HTTP health checking (cluster health_checks) + endpoint ejection + fixture 0019 (engages 06.3 REVIEW I2 synthetic-backend) | 02 04 | done | 12.1, 12.2 | fixture 0019-upstream-active-health-check green; envoy-cluster gains per-endpoint EndpointHealth state machine + pick() unhealthy-exclusion + panic-threshold honoring; new active HTTP health-check task (new envoy-health crate; reuses envoy-http1::Client) probes Cluster.health_checks per interval and drives healthy/unhealthy thresholds; envoy-config gains HealthCheck + HttpHealthCheck + CommonLbConfig + Percent schema + ~4-6 ConfigError variants (TCP/gRPC/custom + multiple health checks rejected per minimum-viable scope); cluster.<name>.health_check.* + membership_healthy stats; first synthetic-backend harness primitive (06.3 REVIEW I2 down-payment; full I2 closure remains with connection pooling); SPLIT into 12.1 + 12.2 at state-2 PLAN-write per ~1800-2200 LoC estimate (ADR-0036) |
| 12.1 | cluster health_checks schema + EndpointHealth state machine + pick() unhealthy-exclusion + panic threshold + health_check stats foundation slice (no new fixture) | 02 04 | done | — | no new fixture; regression-equivalence via the 18 existing fixtures (0001-0018) green simultaneously (machinery inert when health_checks unconfigured — the 05.1/07.1 foundation-slice pattern); envoy-config gains HealthCheck + HttpHealthCheck + CommonLbConfig + Percent schema + ~4-6 ConfigError variants (reuses parse_duration + Int64Range); envoy-cluster gains per-endpoint EndpointHealth (initial state Unhealthy/pending per §6.2 verification) + pick() unhealthy-exclusion + panic threshold (default 50%, strictly-below, Percent {value}); membership_healthy gauge; parse_bootstrap corpus 18→19 |
| 12.2 | active HTTP health-check probe task (new envoy-health crate) + no-healthy-upstream body reconciliation + fixture 0019 + synthetic-backend harness + parent-12 close | 12.1 | done | — | fixture 0019-upstream-active-health-check green; all 19 Docker-gated fixtures green simultaneously; new envoy-health crate (clean DAG over envoy-cluster + envoy-http1 + envoy-config + envoy-stats + tokio; no cycle) owns the first periodic-background probe task (reuses envoy-http1::Client; GET /healthz; Envoy/HC); health_check.{attempt,success,failure} counters; no-healthy-upstream synth-503 body reconciled empty → "no healthy upstream" (19 bytes) per ADR-0037; first synthetic health-aware backend harness primitive + settle-then-probe driver (06.3 REVIEW I2 down-payment, NOT full closure); in-process backstop (both convergence directions); parent phase 12 flips done |
| 13 | per-protocol upstream connection pooling (H1 + H2) + `cluster.<name>.upstream_cx_total` tightening to value-exact + synthetic 5xx backend + fixture 0020 (fully closes 06.3 REVIEW I2) | 04 05 06 12 | done | 13.1, 13.2 | fixture 0020-upstream-connection-pooling-and-per-class-counters green; envoy-http1 + envoy-http2 each gain a per-cluster/per-endpoint connection pool (H1 idle-keep-alive list; H2 multiplexed-streams pool); router-arm dispatch routes through pool acquire/release rather than per-call connect; cluster.<name>.upstream_cx_total disposition tightens to value-exact (fully closes 06.3 REVIEW I2 (b)); per-class downstream_rq_3xx/4xx/5xx + cluster.<name>.upstream_rq_5xx wire bilateral coverage via fixture 0020 (fully closes 06.3 REVIEW I2 (a)); envoy-config gains Cluster.circuit_breakers schema (max_connections; phase-13 supports DEFAULT priority only) + ~3-4 ConfigError variants; new pool stats cluster.<name>.upstream_cx_destroy + upstream_cx_http1_total + upstream_cx_http2_total; configurable-status synthetic-backend harness primitive extends the 12.2 HealthAwareHttp1Backend pattern; SPLIT into 13.1 + 13.2 at state-2 PLAN-write per ~1800-2400 LoC estimate (ADR-0038) |
| 13.1 | H1 connection pool primitive + envoy-config circuit_breakers schema + H1 router-arm pool integration + fixture 0020 + per-class counter wire bilateral coverage (06.3 REVIEW I2 (a) closed) | 04 05 06 12 | done | — | fixture 0020-upstream-connection-pooling-and-per-class-counters green; envoy-http1 gains H1Pool primitive (per-cluster per-endpoint idle keep-alive list; PoolGuard RAII with ConnGaugeGuard reuse; idle sweeper as the second periodic-background primitive); envoy-config gains Cluster.circuit_breakers schema (thresholds[0].{priority?, max_connections?}; DEFAULT priority only; phase-13-deferred fields rejected by deny_unknown_fields) + ~3 ConfigError variants; H1 router-arm dispatch through pool.acquire() rather than per-call Client::connect (upstream_cx_total increment-site migrates from hcm.rs:514 into pool); new pool stats cluster.<name>.upstream_cx_destroy + upstream_cx_http1_total; configurable-status synthetic backend extension (--per-path flag on the 12.2 health-aware-http1-backend); Driver::Http1KeepAlive harness variant (single downstream conn N-request driver — the discriminating-observable shape per §6.2 item-iv); fixture 0020 asserts per-class downstream_rq_{2,3,4,5}xx + cluster.upstream_rq_{2,3,4,5}xx byte-exact bilaterally (FULLY CLOSES 06.3 REVIEW I2 (a)); in-process H1 backstop; parse_bootstrap corpus 20→21; H2 pool + upstream_cx_total contract tightening + parent-13 close defer to 13.2 |
| 13.2 | H2 connection pool + cluster.<name>.upstream_cx_total tightening to value-exact + fixture 0021 + parent-13 close (06.3 REVIEW I2 FULLY CLOSED) | 13.1 | done | — | fixture 0021-upstream-h2-connection-pooling green; all 21 Docker-gated fixtures (0001-0021) green simultaneously; envoy-http2 gains H2Pool primitive (per-cluster per-endpoint connection list with active_streams tracking — peer SETTINGS_MAX_CONCURRENT_STREAMS honored; PoolGuard RAII with ConnGaugeGuard reuse; idle sweeper as the third periodic-background primitive); H2 router-arm dispatch through pool.acquire() (upstream_cx_total increment-site migrates from hcm.rs:280+291 into pool); new pool stat cluster.<name>.upstream_cx_http2_total; BEHAVIOR_CONTRACT cluster.<name>.upstream_cx_total row tightens from name-required,value-may-differ to value-exact for H1+H2 (TCP-proxy carved out — TCP pool defers; FULLY CLOSES 06.3 REVIEW I2 (b)); in-process H2 backstop; parent phase 13 flips done; reuses existing http2-echo-server helper as fixture 0021 backend |
| 14 | outlier detection (consecutive_5xx + consecutive_gateway_failure) + ejection sweeper + fixture 0022 | 04 06 12 13 | done | 14.1, 14.2 | fixture 0022-upstream-outlier-detection-consecutive-5xx green; envoy-cluster gains per-endpoint EndpointEjection state machine + pick() ejection-exclusion + outlier.rs module + OutlierEjectionSweeper (fourth periodic-background primitive sharing tokio_util::sync::CancellationToken cancellation discipline) + OutlierManager external sibling registry; envoy-http1 + envoy-http2 router-arms gain Cluster::record_response hook at the response-receipt site (synth-status bypass nuanced per ADR-0041 §6.2 item-9: connect-failure synth paths DO call record_response; pick()->None synth-503 does NOT); envoy-config gains OutlierDetection schema + ~3-4 ConfigError variants (success-rate-based + failure-percentage-based + consecutive_local_origin_failure + enforcing_* knobs rejected per minimum-viable scope); cluster.<name>.outlier_detection.{ejections_active, ejections_enforced_total, ejections_overflow, ejections_detected_consecutive_5xx, ejections_enforced_consecutive_5xx, ejections_detected_consecutive_gateway_failure, ejections_enforced_consecutive_gateway_failure} 7-name minimum-viable stat subset per ADR-0041 §6.2 item-2 revision; reuses 12.x EndpointHealth pick() seam + 12.2 no-healthy-upstream synth-503 path + 13.x configurable-status synthetic backend + 13.1 Driver::Http1KeepAlive; SPLIT into 14.1 + 14.2 at state-2 PLAN-write per ADR-0040 (~1500-2100 LoC estimate triggers the §6.1 gate) |
| 14.1 | OutlierDetection schema + validator + EndpointEjection state machine + pick() AND-composition + outlier-detection stats foundation slice (no new fixture) | 04 06 12 | done | — | NO new fixture; regression-equivalence via the 21 existing Docker-gated fixtures (0001-0021) green simultaneously (machinery inert when outlier_detection unconfigured — the 05.1/07.1/12.1 foundation-slice pattern); envoy-config gains OutlierDetection + per-cluster outlier_detection field schema + ~3-4 ConfigError variants (deny_unknown_fields rejects success_rate_* + failure_percentage_* + consecutive_local_origin_failure + enforcing_* + max_ejection_time*); envoy-cluster gains per-endpoint EndpointEjection sibling to 12.1 EndpointHealth (initial state: implicitly never-ejected per §6.2 item-3) + Cluster::record_response declared but uncalled at 14.1 + connect-failure classifier (consecutive_5xx + consecutive_gateway_failure both tick per ADR-0041 §6.2 item-9); Cluster::pick() AND-composes !is_ejected() filter with the 12.1 !is_unhealthy() filter; 7-name minimum-viable stat subset wired against StatsRegistry per ADR-0041 §6.2 item-2 revision; parse_bootstrap corpus 21→22 (recommended at 14.1; optional defer to 14.2) |
| 15 | circuit breakers — `max_connections` observability (`upstream_cx_overflow` + `circuit_breakers.default.cx_open`) + faithful `max_pending_requests: 0` reject path + overflow differential fixture 0023 | 04 05 06 13 | done | — | fixture 0023-upstream-circuit-breaker-max-pending-requests green (all 23 Docker-gated fixtures green simultaneously); the `max_connections` cap ENFORCEMENT already landed at phase 13 (ADR-0042) — phase 15 closes the OBSERVABILITY + DIFFERENTIAL-COVERAGE gap. **§6.2 empirical verification (ADR-0043) RE-SCOPED the phase:** `max_pending_requests:0` rejects EVERY request (not just over-cap) via `upstream_rq_pending_overflow` (NOT `upstream_cx_overflow`), `upstream_cx_total:0`, body = the 81-byte `…reset reason: overflow` + `x-envoy-overloaded:true` — the SPEC's `{200,503}`/`upstream_cx_overflow:1` premise was empirically false. envoy-config `Thresholds` gains `max_pending_requests` (accept 0, reject >0 = `UnsupportedNonZeroMaxPendingRequests`); envoy-http1 + envoy-http2 pools gain a faithful `max_pending_requests:0` reject-on-establish gate + `cluster.<name>.upstream_rq_pending_overflow` counter + the `cluster.<name>.upstream_cx_overflow` counter + `cluster.<name>.circuit_breakers.default.cx_open` gauge (edge-driven, inert-when-unconfigured); a `synth_overflow()` 503 helper reconciles the overflow body (H1 + H2; corrects a pre-existing H2 overflow-502→503 bug); fixture 0023 drives a SINGLE GET (timing-robust, no concurrency/slow-backend) asserting the bilateral 503 + 81-byte body + `upstream_rq_pending_overflow:1`; fixture 0020 gains inert-0 cx_overflow/cx_open assertions; in-process backstop validates both overflow paths + cx_open both edges. The SPEC's `Driver::Http1Concurrent`/`--hold-ms` DROPPED; the `max_pending_requests>0` queue + `{200,200}` bilateral overflow fixture + per-cluster `cx_open` reconciliation + `max_requests`/`max_retries`/`track_remaining`/multi-priority deferred. Landed as a SINGLE un-split phase (~1100-1250 LoC) |
| 14.2 | response-receipt hook (H1+H2 router-proxy arms) + OutlierEjectionSweeper + fixture 0022 + in-process backstop + parent-14 close | 14.1 | done | — | fixture 0022-upstream-outlier-detection-consecutive-5xx green; all 22 Docker-gated fixtures (0001-0022) green simultaneously; envoy-http1::router + envoy-http2::hcm gain Cluster::record_response call at response-receipt site (AFTER upstream_rq_total/upstream_rq_5xx increments fire, BEFORE downstream response write); connect-failure synth path also calls record_response (consecutive_5xx + consecutive_gateway_failure both tick for the picked endpoint that failed); no-healthy-upstream pick()->None synth-503 path short-circuits (no record_response call); envoy-cluster gains outlier.rs module + OutlierEjectionSweeper (FOURTH periodic-background primitive after 12.2 active-HC scheduler + 13.1 H1 pool idle sweeper + 13.2 H2 pool idle sweeper; identical tokio_util::sync::CancellationToken cancellation discipline + pub async fn shutdown(self)) + OutlierManager external sibling registry to ClusterManager (mirrors H1PoolManager/H2PoolManager/envoy-health::Scheduler verbatim); fixture 0022 reuses 13.x health-aware-http1-backend + 13.1 Driver::Http1KeepAlive verbatim (request 1-3: status 500 + body `server error\n` + x-envoy-upstream-service-time PRESENT; request 4: status 503 + body `no healthy upstream` (19 bytes) + x-envoy-upstream-service-time ABSENT; counter bilateral assertions per ADR-0041 §6.2 item-6); in-process H1 backstop exercises BOTH eject + un-eject convergence directions per parent §6.4; parent phase 14 flips done |
| 16 | HTTP retry policy (`retry_on`: 5xx/gateway-error/connect-failure/reset/retriable-status-codes + `num_retries` + `retriable_status_codes`) + `upstream_rq_retry{,_success,_limit_exceeded}` stats + `x-envoy-attempt-count` + fixture 0024 | 04 05 06 13 14 | done | — | fixture 0024-upstream-retry-on-5xx green (all 24 Docker-gated fixtures green simultaneously); envoy-config gains `RouteAction_Route.retry_policy: Option<RetryPolicy>` schema (`retry_on` comma-token parse + `num_retries` default 1 + `retriable_status_codes`; deferred fields `per_try_timeout`/`retry_back_off`/`retry_priority`/`retry_host_predicate`/`retriable_headers`/`retry_budget` rejected by `deny_unknown_fields`) + ~3-5 ConfigError variants; envoy-http1 + envoy-http2 router arms gain a retry loop wrapping the pick→acquire→send→receive dispatch seam (H1 `hcm.rs:465-706`; H2 `hcm.rs:238-481`); re-pick honors 12.x health + 14.x ejection; body-replay-safe (H1 empty-body / H2 buffered-clone — no new buffering machinery, per ADR-0044); 3-name minimum-viable `cluster.<name>.upstream_rq_retry{,_success,_limit_exceeded}` stat subset (inert-when-unconfigured) + the per-attempt `upstream_rq_total`/`upstream_rq_5xx` counting reconciliation (§6.2-CRITICAL); `x-envoy-attempt-count` downstream-response header; new STATEFUL fail-then-succeed synthetic-backend harness primitive (extends the 12.x/13.x health-aware-http1-backend) + reuses Http1/Http1ProbeList/Http2 drivers (no new Driver); default retry back-off (timing not differentially asserted); minimum-viable scope per ADR-0044 (defers `per_try_timeout`, custom back-off, retry budgets/`upstream_rq_retry_overflow`, retry-host-predicate/priority, `x-envoy-retry-on` overrides, vhost-level policy, gRPC retry, hedging); Landed as a SINGLE un-split phase (10 implementation tasks + state-4 verification; the §6.1 split gate did NOT fire at the state-2 PLAN-write — ADR-0046 unconsumed; REVIEW.md APPROVED with 1 Important fixed in-review at 995445b52) |
| 17 | circuit-breaker budgets — `max_retries` retry breaker + `max_requests` request breaker + `track_remaining` gauges + `upstream_rq_retry_overflow` + `circuit_breakers.default.{rq_retry_open,rq_open,remaining_retries,remaining_rq}` + fixture 0025 | 04 05 06 13 15 16 | done | — | fixture 0025-upstream-circuit-breaker-retry-budget green (all 25 Docker-gated fixtures green simultaneously); envoy-config `Thresholds` gains `max_requests`/`max_retries`/`track_remaining` (deferred fields `max_connection_pools`/`retry_budget` rejected by `deny_unknown_fields`); envoy-cluster gains cluster-scoped budget primitives (`active_retries`/`active_requests` RAII acquire/release guards + conditional stat registration — the ADR-0046 §5.4 ownership boundary: budget state lives in `envoy-cluster::Cluster`, NOT the per-protocol pools); envoy-http1 + envoy-http2 gain a retry-budget gate at the phase-16 retry-decision points (overflow → `upstream_rq_retry_overflow` ticks + the would-be-retried response surfaces downstream verbatim) + a request-budget gate at dispatch entry (overflow → the phase-15 81-byte 503 overflow local reply + `x-envoy-overloaded`; backend never contacted); deterministic zero-cap differential anchoring per ADR-0046 §0 (no concurrency-based fixtures; the >0-cap concurrency regime covered by the in-process backstop only) — CONFIRMED empirically at the state-2 PLAN-write §6.2 verification, with two assertion re-anchorings per ADR-0047 (the breaker `*_open` gauges are momentary → asserted value-exact at 0; the `max_requests`-overflow local reply co-fires `upstream_rq_pending_overflow` + `upstream_rq_5xx` while `upstream_cx_total` stays a known prefetch divergence [unasserted]; bonus: M16-3 resolved — `x-envoy-attempt-count` IS emitted on overflow local replies); reuses `Driver::Http1KeepAlive` + the 16.x stateful backend (no new Driver, no new helper); minimum-viable scope per ADR-0046 (defers the `max_pending_requests > 0` pending queue [owner: a future pool-hardening + pending-queue phase], `retry_budget` percent budgets, `max_connection_pools`, multi-priority HIGH thresholds, concurrency-regime differential fixtures, `per_try_timeout`); after this phase the Upstream-robustness family is complete in minimum-viable form |

### HTTP/3 + QUIC family

quinn transport, downstream H3 listener, upstream H3 cluster, `h3spec` gate.

### gRPC family

gRPC bridge, gRPC-Web, gRPC-JSON transcoding, interop conformance.

### xDS / dynamic config family

ADS, delta xDS, LDS, CDS, RDS, EDS, SDS, RTDS, reconnection, initial-fetch timeout.

| id | title | depends-on | status | sub-phases | summary |
|---|---|---|---|---|---|
| 18 | xDS family opener: file-based CDS (`dynamic_resources.cds_config.path_config_source`) — initial-load-only + `cluster_manager.cds.*` stats + `ClustersConfigDump` + fixture 0026 | 01 02 04 06 08 | done | — | fixture 0026-xds-file-based-cds green (all 26 Docker-gated fixtures green simultaneously); envoy-config `Bootstrap` gains `dynamic_resources: Option<DynamicResources>` (`cds_config.path_config_source` only; `lds_config`/`ads_config`/`api_config_source`/`watched_directory` rejected by `deny_unknown_fields`) + a CDS-file parser (YAML `resources:` envelope reusing the existing `Cluster` schema per the ADR-0014 shim — NO protos/prost/tonic at this scope); the dynamic clusters merge into the effective cluster list at config-load time (initial-load-only — `ClusterManager` stays immutable post-construction; every downstream subsystem [pools/TLS/health/outlier/budgets/retries/validators] treats dynamic clusters identically to static per ADR-0048 §5.3); `cluster_manager.cds.{update_success,update_failure}` + `cluster_manager.{cluster_added,active_clusters}` stats (conditional registration — inert when `dynamic_resources` unconfigured); admin `/config_dump` gains the `ClustersConfigDump` entry (`dynamic_active_clusters`; conditional emission — fixture 0014 untouched); harness gains CDS-file per-side rendering + container mounting (`{{CDS_PATH}}` substitution; the TLS-PKI `with_copy_to` pattern); fixture 0026 (static listener + HCM route → a cluster defined ONLY in the CDS file; `Driver::Http1KeepAlive` data-plane probe + named-stat scrape — deterministic, zero timing sensitivity); in-process backstop (happy + missing-file/malformed-file/name-collision negative paths); fuzz seed `dynamic_resources_cds.yaml` (corpus 28→29); BEHAVIOR_CONTRACT "xDS wire state machine" section FIRST population (filesystem transport); minimum-viable scope per ADR-0048 (defers file watching/hot-reload [the family's prime follow-up], file-based LDS/RDS/EDS/SDS/RTDS, the gRPC/ADS transport + envoy-protos supersession of ADR-0014, delta xDS, initial_fetch_timeout); **§6.2-verified at the state-2 PLAN-write with four reconciliations per ADR-0049** (the `@type`-required + extension-driven CDS envelope [envoy-rust: always-YAML]; ALL envoy-rust CDS load errors fatal at startup [vs Envoy's missing-fatal / parse-warn / semantic-warn 3-way split]; static WINS on static/dynamic name collision [the projected `DuplicateClusterName` reject dropped]; the `node.id`+`node.cluster` and `validate_clusters: false` bootstrap prerequisites — `RouteConfiguration` gains the parse-and-accept `validate_clusters` field, with envoy-rust enforcing references via defer-then-revalidate-post-merge); the stat subset grows 4 → 6 names (+ `cds.update_attempt`/`cds.update_rejected`); single un-split phase (~1480–1600 LoC / 11 tasks; ADR-0050 not fired) |
| 19 | xDS family: file-based LDS (`dynamic_resources.lds_config.path_config_source`) — initial-load-only + `listener_manager.lds.*` stats + `ListenersConfigDump` + fixture 0027 (the canonical LDS+CDS filesystem topology) | 01 02 04 06 08 18 | done | — | fixture 0027-xds-file-based-lds green (all 27 Docker-gated fixtures green simultaneously); envoy-config `DynamicResources` gains `lds_config: Option<ConfigSource>` (reusing the phase-18 `ConfigSource`/`PathConfigSource` structs verbatim; `ads_config`/`api_config_source`/`watched_directory` still rejected by `deny_unknown_fields`) + an LDS-file parser (the phase-18 `@type`-tagged envelope pattern with `type.googleapis.com/envoy.config.listener.v3.Listener` resources, reusing the existing `Listener` schema); `load_dynamic_resources` merges dynamic listeners into the effective listener list at config-load time (initial-load-only; ordering invariant: CDS + LDS merges complete BEFORE the post-merge re-validation, so an LDS-supplied listener's routes may reference a CDS-supplied cluster); `Bootstrap::all_listeners()` accessor + envoy-bin consumer migration; conditional `listener_manager.{lds.update_attempt,lds.update_success,lds.update_failure,lds.update_rejected,listener_added}` stats (+ `total_listeners_active` tightened to bilateral) — inert when `lds_config` unconfigured; admin `/config_dump` gains the conditional `ListenersConfigDump` entry; harness gains `{{LDS_PATH}}` per-side rendering/mounting (generalizing the phase-18 CDS machinery; the combined-source scans gain the LDS rendition as a third source); fixture 0027 (zero static listeners + one static cluster + LDS file [listener with routes `/static` → static cluster, `/dynamic` → CDS cluster] + CDS file — Envoy's documented canonical filesystem-dynamic-config topology; `Driver::Http1KeepAlive` probes + named-stat scrape — deterministic, zero timing sensitivity); in-process backstop (happy + missing/malformed-LDS-file/listener-name-collision/unresolved-route negative paths); fuzz seed `dynamic_resources_lds.yaml` (corpus 29→30); BEHAVIOR_CONTRACT LDS stat rows + filesystem-transport LDS extension + ListenersConfigDump row; minimum-viable scope per ADR-0050 (defers file watching/hot reload [the family's prime follow-up — ROI strictly improved by this phase], file-based RDS/EDS/SDS/RTDS, multi-listener spawning, listener drain/in-place-update, the gRPC/ADS transport + the ADR-0014 protos supersession, delta xDS); **§6.2-verified at the state-2 PLAN-write with ZERO reconciliation ADR** (all three ADR-0051 trigger items CONFIRMED: the LDS envelope mirrors the CDS envelope; Envoy's LDS negative-path split mirrors its CDS split [envoy-rust's all-fatal posture extends per ADR-0049 decision 2 / ADR-0050]; the `configs[]` order is Bootstrap[0] Clusters[1] Listeners[2] — fixture 0026's `configs[1]` assertion holds; ADR-0051 unconsumed); refinement lock-ins: LDS route_configs need NO `validate_clusters: false` [Envoy skips cluster validation for dynamic listeners; envoy-rust's defer-then-revalidate divergence recorded], `listener_create_success` is per-worker [never asserted], no `version_info` in `active_state`, per-side LDS templates [`lds-envoy.yaml`/`lds-envoy-rust.yaml` — the HCM payload carries the established Envoy-only field-set divergence]; single un-split phase (~1250–1450 LoC / 11 tasks; ADR-0052 not fired); REVIEW.md APPROVED (0 Critical / 0 Important / 6 non-gating Minors M19-1…M19-6; all 4 concern-clusters CLEAN; all 5 named focus items verified — the third consecutive clean state-5 after 17, 18); differential evidence = CI anchor `26903181658` (all 27 Docker-gated fixtures 0001–0027 + h2spec ≥95% + the 200k-run fuzz on the 30-seed corpus, green on Linux); after this phase the xDS / dynamic config family's filesystem-transport surface covers BOTH CDS + LDS |
| 20 | xDS family: file-based RDS (HCM `rds.config_source.path_config_source`) — initial-load-only + per-HCM `http.<stat_prefix>.rds.<route_config_name>.*` stats + `RoutesConfigDump` + fixture 0028 | 01 02 04 06 08 18 | done | — | (done; SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/20-xds-file-based-rds/`, scope locked by ADR-0051, §6.2 verified + reconciled by ADR-0052; CLOSED 2026-06-04 at the state-6 close-out) fixture 0028-xds-file-based-rds green (all 28 Docker-gated fixtures green simultaneously); envoy-config `HttpConnectionManagerConfig` gains `rds: Option<Rds>` (`Rds { route_config_name, config_source }` reusing the phase-18 `ConfigSource`/`PathConfigSource` structs verbatim; `api_config_source`/`ads`/`watched_directory` rejected by `deny_unknown_fields`) and `route_config` becomes `Option<RouteConfiguration>` validated EXACTLY-ONE-OF with `rds` (the first HCM-scoped — vs bootstrap-scoped — dynamic resource) + an RDS-file parser (the phase-18/19 `@type`-tagged envelope pattern with `type.googleapis.com/envoy.config.route.v3.RouteConfiguration` resources, reusing the existing `RouteConfiguration` schema; the §6.2-flagged opportunity to consolidate `cds.rs`+`lds.rs`+RDS into a resource-parametric `xds_file.rs` per phase-19 REVIEW M19-1); `load_dynamic_resources` walks every HCM, loads + name-selects each `rds`-configured HCM's RouteConfiguration, and populates the effective `route_config` at config-load time (initial-load-only; merge-ordering invariant extended: CDS clusters merge BEFORE the RDS-supplied route's references are re-validated, so an RDS route may reference a CDS cluster); conditional per-HCM `http.<stat_prefix>.rds.<route_config_name>.{update_attempt,update_success,update_failure,update_rejected,config_reload}` stats (a NEW per-HCM registration topology distinct from the manager-level CDS/LDS families) — inert when no HCM uses `rds`; admin `/config_dump` gains the conditional `RoutesConfigDump` entry; harness gains `{{RDS_PATH}}` rendering/mounting (generalizing the phase-18/19 machinery); fixture 0028 (static listener whose HCM is `rds`-configured + one static cluster + RDS file [RouteConfiguration with routes `/static` → static cluster, `/dynamic` → CDS cluster] + CDS file — exercises the cluster-before-route-revalidation ordering; `Driver::Http1KeepAlive` probes + named-stat scrape — deterministic, zero timing sensitivity); in-process backstop (happy + missing/malformed-RDS-file / `route_config_name`-mismatch / unresolved-route / both-route-sources / neither-route-source negative paths); fuzz seed `hcm_rds_route_config.yaml` (curated corpus 30→31); BEHAVIOR_CONTRACT RDS stat rows + filesystem-transport RDS extension + RoutesConfigDump row; minimum-viable scope per ADR-0051 (defers file watching/hot reload [the family's prime follow-up — ROI improved again: one watching phase now lights up CDS+LDS+RDS], scoped_routes/SRDS/VHDS, the LDS+RDS composition showcase, multiple-`rds`-HCM / multiple-RouteConfig-per-file, file-based EDS/SDS/RTDS, the gRPC/ADS transport + the ADR-0014 protos supersession, delta xDS); **§6.2-verified at the state-2 PLAN-write with reconciliation ADR-0052** (the 11-item checklist run against `envoyproxy/envoy:v1.33.0`: items 1 [envelope/`rds`-on-HCM shape] + 4/6 [negative-path/`route_config_name`-mismatch disposition] CONFIRM the projections [the RDS envelope mirrors CDS/LDS; the 3-way missing-fatal/malformed-`update_failure`/semantic-`update_rejected` split → envoy-rust all-fatal]; item 5 DIVERGES → **ADR-0052 fires**: Envoy's `/config_dump` `configs[]` order is Bootstrap[0]/Clusters[1]/Listeners[2]/ScopedRoutes[3]/Routes[4]/Secrets[5] — `RoutesConfigDump` at index 4 not the projected 3 [Clusters[1]/Listeners[2] NOT displaced — fixtures 0026/0027 hold], reconciled by a per-side `JsonSubtreeRule` path override in the harness; refinement lock-ins: `config_reload` ticks at initial load [stays in the 5-name subset], the RDS file is SHAREABLE [no per-side template, unlike LDS], an RDS→CDS route needs NO `validate_clusters: false` [RDS behaves like LDS], both/neither route source are fatal on BOTH proxies; single un-split phase ~1100–1450 LoC / 11 tasks, ADR-0053 [split] does NOT fire); REVIEW.md APPROVED (0 Critical / 0 Important / 4 non-gating Minors M20-S5-1…M20-S5-4; all 4 concern-clusters CLEAN; all 5 named focus items verified — the fourth consecutive clean state-5 after 17, 18, 19); differential evidence = CI anchor `26967529584` (all 28 Docker-gated fixtures 0001–0028 + h2spec ≥95% + the 31-seed fuzz, green on Linux); after this phase the xDS / dynamic config family's filesystem-transport surface covers the FULL CDS+LDS+RDS data-plane triad |
| 21 | xDS family: file-based EDS (`Cluster.eds_cluster_config.eds_config.path_config_source`) — initial-load-only + per-cluster `cluster.<name>.update_*` EDS-subscription stats + `EndpointsConfigDump` + fixture 0029 | 01 02 04 06 08 18 | done | — | (done; SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/21-xds-file-based-eds/`, scope locked by ADR-0053, §6.2 verified + reconciled by ADR-0054; CLOSED 2026-06-06 at the state-6 close-out) fixture 0029-xds-file-based-eds green (all 29 Docker-gated fixtures green simultaneously); envoy-config `ClusterType` gains `EDS` + `Cluster` gains `eds_cluster_config: Option<EdsClusterConfig>` (`EdsClusterConfig { eds_config, service_name }` reusing the phase-18 `ConfigSource`/`PathConfigSource` structs verbatim; `api_config_source`/`ads`/`watched_directory` rejected by `deny_unknown_fields`) and `load_assignment` becomes `Option<LoadAssignment>` validated EXACTLY-ONE-OF-AND-CONSISTENT-WITH-`cluster_type` (the first CLUSTER-scoped — vs bootstrap-/HCM-scoped — dynamic resource) + an EDS-file parser (the phase-18/19/20 `@type`-tagged envelope pattern with `type.googleapis.com/envoy.config.endpoint.v3.ClusterLoadAssignment` resources, reusing the existing `LoadAssignment` schema; the M19-1/M20-T6-a consolidation pressure now at N=4 — `cds.rs`+`lds.rs`+`rds.rs`+EDS); `load_dynamic_resources` walks every (static + CDS) cluster, loads + name-selects each EDS cluster's `ClusterLoadAssignment` (by `service_name`-or-cluster-name), and populates the effective `load_assignment` at config-load time (initial-load-only; merge-ordering invariant extended: CDS clusters merge BEFORE the EDS endpoint pass); conditional per-cluster `cluster.<name>.{update_attempt,update_success,update_failure,update_empty}` stats (extending the EXISTING per-cluster namespace owned by phase-12.1 `membership_*`) — inert when no cluster is EDS; admin `/config_dump` gains the conditional `EndpointsConfigDump` entry; harness gains `{{EDS_PATH}}` rendering/mounting (generalizing the phase-18/19/20 machinery — the EDS file carries the BACKEND ENDPOINT marker, so the combined-source backend-detection scans MUST include the EDS rendition); fixture 0029 (static listener + inline route → a static cluster `eds_backend` with `type: EDS` whose endpoints come ONLY from the EDS file; `Driver::Http1KeepAlive` probe + named-stat scrape — deterministic, single endpoint, zero timing sensitivity); in-process backstop (happy + missing/malformed-EDS-file / no-matching-ClusterLoadAssignment / EDS-cluster-with-load_assignment / non-EDS-cluster-with-eds_cluster_config / neither-endpoint-source negative paths); fuzz seed `cluster_eds.yaml` (curated corpus 31→32); BEHAVIOR_CONTRACT EDS stat rows + filesystem-transport EDS extension + EndpointsConfigDump row; minimum-viable scope per ADR-0053 (single-endpoint, initial-load-only; defers file watching/hot reload [the family's prime follow-up — ROI now at its best: one watching phase lights up CDS+LDS+RDS+EDS; its §6.2 MUST run on Linux CI per ADR-0049 Provenance, unlike this initial-load phase which verifies LOCALLY], multi-endpoint/locality-weighted/priority EDS [needs the LB distinguishable-backend infra], endpoint health-status-in-EDS, the EDS-cluster-supplied-by-CDS composition showcase, SDS/RTDS, scoped_routes/SRDS/VHDS, the gRPC/ADS transport + the ADR-0014 protos supersession, delta xDS, initial_fetch_timeout); scope locked by ADR-0053 at the state-1 brainstorm; **§6.2-verified at the state-2 PLAN-write with reconciliation ADR-0054 (FIRES): item 5 [the `EndpointsConfigDump`] DIVERGES materially — Envoy OMITS it from the default `/config_dump` (only `?include_eds` surfaces it) and classifies file-based EDS endpoints under `static_endpoint_configs[]` not `dynamic_endpoint_configs[]` → reconciled by scraping `/config_dump?include_eds` + a per-side `JsonSubtreeRule` (reused from ADR-0052) + an admin query-string strip; item 1 adds a NUMERIC-IP constraint on the EDS endpoint (hostname rejected) → the EDS file is a shared template with a per-side `{{EDS_BACKEND_IP}}` marker [Envoy=discovered numeric host-gateway IP / envoy-rust=127.0.0.1] + a host-gateway-IP discovery step; item 2/4 — missing-FILE-PATH fatal on both, but malformed→`update_failure`/missing-CLA→`update_rejected`/empty→`update_empty` warm-and-503 on Envoy vs envoy-rust all-fatal (recorded, backstop-only); item 6a — Envoy ACCEPTS EDS+inline `load_assignment` vs envoy-rust stricter-reject; item 10 — STATIC clusters emit `update_*` at 0 on Envoy (pre-existing, tolerated); the envelope/`service_name`-selection/wire-shape/STATIC+EDS validation hard-exits MATCH; the §2.1 subset locked at `cluster.<name>.update_{attempt,success,failure,empty}` (1/1/0/0) + the `upstream_rq_total` data-plane witness (the membership gauges are NOT asserted — envoy-rust gates `membership_healthy` on health_checks + has no `membership_total`, a recorded narrowing vs Envoy)**; single un-split phase confirmed (~1150–1500 LoC / 11 tasks; ADR-0055 [split] reserved-but-unfired); REVIEW.md APPROVED (0 Critical / 0 Important / 7 non-gating Minors M21-1…M21-7; all 4 concern-clusters CLEAN; all 5 named focus items verified — the fifth consecutive clean state-5 after 17, 18, 19, 20; the one reviewer-rated Important [M21-1, the CDS+EDS post-merge re-validation hole] downgraded to non-gating, owned by the future CDS+EDS composition phase); differential evidence = CI anchor `27043160204` (all 29 Docker-gated fixtures 0001–0029 + h2spec ≥95% + the 200k-run fuzz on the 32-seed corpus, green on Linux); after this phase the xDS / dynamic config family's filesystem-transport surface covers the CDS+LDS+RDS+EDS core file-based resource quartet |
| 26 | xDS family: file-based RDS HOT RELOAD (`rds.config_source.path_config_source` re-read on file change) — the family's prime follow-up; poll-based watch + atomic route-table swap + per-reload `rds.*` counter advancement + `RoutesConfigDump` version update + fixture 0034 | 01 04 06 08 12 20 | done | — | (done; SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/26-xds-rds-hot-reload/`, scope locked by ADR-0065 at the state-1 brainstorm, §6.2 verified + reconciled by **ADR-0066** at the state-3 Task 1; REVIEW.md APPROVED [0 Critical / 0 Important / 8 non-gating Minor M26-1…M26-8]; CLOSED 2026-06-18 at the state-6 close-out — all 34 Docker-gated fixtures 0001–0034 green simultaneously [fixture 0034-xds-rds-hot-reload Linux-CI-authoritative per ADR-0049 + the existing fixture 0028 gained the idle-watcher regression witness proving the route-table-handle migration behavior-preserving]; state-4 §7.5 gate GREEN [AUTHORITATIVE Linux CI `27708943522`]; state-5 REVIEW.md APPROVED 0C/0I/8-Minor) the FIFTH concrete xDS-family phase (after 18 CDS / 19 LDS / 20 RDS / 21 EDS) and the **FIRST phase that mutates a running manager's state post-construction** (every prior xDS phase loaded dynamic resources ONCE at startup and kept managers immutable — ADR-0048 §5.3 / 0050 / 0051 / 0053). Lands the xDS family's repeatedly-deferred PRIME FOLLOW-UP — hot reload — at its minimum-viable increment: a running HCM whose route table is RDS-supplied picks up an edited RDS file WITHOUT a restart, re-routing live traffic, observable via the per-HCM `http.<stat_prefix>.rds.<route_config_name>.{update_attempt,update_success,config_reload}` counters advancing PAST their initial-load values + `RoutesConfigDump` reflecting the new version. **RDS first because its live mutation is the LEAST invasive** — the route table is already `route_config: Arc<RouteConfiguration>` (`crates/envoy-http1/src/hcm.rs:122`) and route matching is per-request stateless, so hot-reload = an atomic pointer swap (NO drain / socket churn / pool/health/outlier lifecycle — the reasons CDS/LDS/EDS hot-reload defer). D1 migrate `HCMConfig.route_config` Arc → a shared swappable handle (dep-free `RwLock<Arc<…>>`/`tokio::sync::watch`, NO new `arc-swap` dep); D2 an `RdsWatcher` — the FIFTH periodic-background primitive (poll mtime, `CancellationToken` discipline, mirroring `envoy-health::Scheduler` + the 13.x/14.2 sweepers); D3 reload pipeline (re-parse via existing `rds.rs` → re-validate route→cluster refs against the immutable live cluster set → atomic swap; tick `rds.*` per reload; **warm-reject keeping the last-good table on a bad reload — the one ADR-0049 startup-all-fatal carve-out**); D4 per-reload counter increments; D5 `RoutesConfigDump` version/`last_updated` update read through the swappable handle; D6 the one new harness capability (mid-test fixture-file rewrite + settle-then-probe, the 12.2 wait-for-convergence pattern); D7 fixture 0034 (two distinguishable real `http1-echo-server` clusters; three-phase pre-reload→reload→post-reload + a bad-reload probe; **Linux-CI-authoritative per ADR-0049** — the reload trigger is unobservable on macOS Docker virtiofs, the phase-21 watching-class precedent); D8 in-process backstop (deterministic local complement via `tokio::time`, drives the negative paths + in-flight isolation) + BEHAVIOR_CONTRACT extensions. **Minimum-viable hot-reload adds ZERO new config schema** (Envoy's file xDS is always-watching; envoy-rust watches every `rds`-configured HCM) UNLESS §6.2 reveals Envoy needs `watched_directory` for the chosen file-change operation. Regression witness: the existing fixture `0028-xds-file-based-rds` gains an IDLE watcher (file never edited mid-test → counters/data-plane unchanged → 0028 stays green; the load-bearing proof the handle migration is behavior-preserving). minimum-viable scope per ADR-0065 (defers CDS/LDS/EDS hot-reload [harder live mutations, layer onto this watcher+swap primitive], inotify-exactness/`watched_directory`, cluster warming, configurable poll interval, scoped_routes/SRDS/VHDS hot-reload, SDS/RTDS, the gRPC/ADS transport + the ADR-0014 protos supersession, delta xDS, `initial_fetch_timeout`); SPEC at `docs/envoy-rust/phases/26-xds-rds-hot-reload/`, scope locked by ADR-0065 at the state-1 brainstorm; ADR-0066 reserved for the §6.2 reconciliation (PLAUSIBLE — `watched_directory`/reload-counter/bad-reload/config_dump shapes), ADR-0067 for the §6.1 split (projected NOT to fire; ~1200–1600 LoC / ~10–13 tasks) |
| 27 | xDS family: file-based EDS HOT RELOAD (`eds_cluster_config.eds_config.path_config_source` re-read on atomic-rename) — the first of the ADR-0065-deferred CDS/LDS/EDS hot-reload layers; reuses phase-26's watcher + atomic-swap + mid-test-atomic-rename harness; atomic endpoint-set swap + per-reload `cluster.<name>.update_*` advancement + `EndpointsConfigDump` update + fixture 0035 | 01 02 06 08 21 26 | done | — | (done; SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/27-xds-eds-hot-reload/`, scope locked by **ADR-0067** at the state-1 brainstorm; **§6.2 EMPIRICALLY VERIFIED at the state-2 PLAN-write — ADR-0068 FIRED**: the empty-assignment disposition MIRRORS Envoy's apply-empty→503 rather than warm-rejecting [safe — `pick()` already handles empty + the 503 path exists], the `EndpointsConfigDump` has no `last_updated` [D5 shrinks to read-through-handle], `update_rejected` promoted to emitted, EDS+HC/OD = no watcher; **§6.1 single-phase confirmed, ADR-0069 UNFIRED**; REVIEW.md APPROVED [0 Critical / 0 Important / 3 non-gating Minor M27-1…M27-3]; CLOSED 2026-06-19 at the state-6 close-out — all 35 Docker-gated fixtures 0001–0035 green simultaneously [fixture 0035-xds-eds-hot-reload native-Linux-CI-authoritative per ADR-0049/0066 + the existing fixture 0029-xds-file-based-eds gained the idle-watcher regression witness proving the endpoint-handle migration behavior-preserving]; state-4 §7.5 gate GREEN [AUTHORITATIVE Linux CI `27818702552` @ `acac6d4`]; state-5 REVIEW.md APPROVED 0C/0I/3-Minor [the twelfth consecutive clean state-5, after 17–26]) the SIXTH concrete xDS-family phase (after 18 CDS / 19 LDS / 20 RDS / 21 EDS / 26 RDS-hot-reload) and the **SECOND post-construction live-mutation phase** (phase 26 hot-swapped a per-HCM route table; this hot-swaps a per-cluster endpoint set). Lands the FIRST of the three ADR-0065-deferred hot-reload layers (CDS/LDS/EDS), **EDS chosen as the LEAST invasive** — an EDS reload mutates ONLY a cluster's endpoint `Vec<SocketAddr>` (`crates/envoy-cluster/src/cluster.rs:80`, read every request by the round-robin LB `pick()` `:274` — per-request stateless, the closest analog to phase-26's stateless route-table swap); the cluster lifecycle (pools/health/outlier/budgets/listeners) stays immutable. A running cluster whose endpoint set is EDS-supplied picks up an edited EDS file WITHOUT a restart, re-targeting live traffic, observable via the per-cluster `cluster.<name>.{update_attempt,update_success}` counters advancing PAST their initial-load values (1/1 → 2/2) + `EndpointsConfigDump` reflecting the new assignment. **Reuses phase-26's infrastructure wholesale** — the periodic poll-watch primitive (`crates/envoy-http1/src/rds_watcher.rs`, generalized — the M19-1/M20/M21 + now-two-watchers `XdsFileWatcher` factoring), the atomic-swap-handle pattern, the warm-reject reload pipeline, and the mid-test atomic-rename rewrite + settle-then-probe harness capability — so NO new crate, NO new dep, NO new reload/settle harness machinery. D1 migrate `Cluster.endpoints` Vec → a shared swappable handle (dep-free `RwLock<Arc<…>>`/`tokio::sync::watch`; LB reads through it; cursor-bounds-safe on a shrinking set); D2 the EDS file watcher (generalize the phase-26 watcher / a sibling, likely relocated to a shared crate since EDS concerns `envoy-cluster`); D3 reload pipeline (re-parse via existing `eds.rs` → re-validate the assignment → atomic swap; tick `cluster.<name>.update_*` per reload; warm-reject keeping the last-good set — reusing the phase-26 §5.5 carve-out); D4 per-reload counter increments; D5 `EndpointsConfigDump` `last_updated`/assignment read through the swappable handle; D6 the one new harness capability (a SECOND distinguishable backend — an intra-cluster endpoint swap is invisible through one echo backend [`membership_total` not emitted `cluster.rs:963`, `membership_healthy` HC-gated]; the minimal seed of the deferred LB distinguishable-backend harness); D7 fixture 0035 (two distinguishable real `http1-echo-server` backends; one `type: EDS` cluster `eds_backend` with NO HC/NO OD; three-phase pre-reload→reload→post-reload + a bad-reload probe; **native-Linux-CI-authoritative per ADR-0049/0066** — the reload trigger is unobservable under virtiofs); D8 in-process backstop (negative paths + in-flight isolation + cursor-bounds) + BEHAVIOR_CONTRACT extensions. **Minimum-viable scope = PLAIN clusters only (no active HC / no outlier detection)** — the endpoint-index-aligned `endpoint_health`/`outlier_detection` arrays + per-`(cluster,endpoint)` health-probe tasks would need spawn/drain/rebuild on a count-changing reload (the same per-endpoint lifecycle churn that defers CDS), so the EDS+HC/OD aligned-array rebuild defers. Regression witness: the existing fixture `0029-xds-file-based-eds` gains an IDLE endpoint-watcher (file never edited mid-test → counters/data-plane unchanged → 0029 stays green; the load-bearing proof the endpoint-handle migration is behavior-preserving). minimum-viable scope per ADR-0067 (defers CDS/LDS hot-reload [heavier live mutations], the EDS+HC/OD aligned-array rebuild, `LbEndpoint` weight/health-status/locality/priority reload, cluster warming, configurable poll interval, SDS/RTDS, the gRPC/ADS transport + the ADR-0014 protos supersession, delta xDS, `initial_fetch_timeout`); **ADR-0068 FIRED** at the state-2 PLAN-write (the §6.2 reconciliation — observability = second backend; empty-assignment MIRRORS apply-empty→503; config_dump no `last_updated`; `update_rejected` emitted; EDS+HC/OD = no watcher), **ADR-0069 UNFIRED** (the §6.1 split did NOT fire — single phase; ~900–1200 LoC / 9 tasks) |

### Observability family

gRPC ALS, OTLP access log, OTel/Zipkin/Jaeger/Datadog/XRay tracing, stats sinks, tap filter.

| id | title | depends-on | status | sub-phases | summary |
|---|---|---|---|---|---|
| 32 | Observability family opener: configurable access-log **command-operator formatter** (`envoy-accesslog` hardcoded default-format emitter → parsed `%COMMAND%` substitution engine + a per-`FileAccessLog` `log_format` text-format field + a curated deterministic operator set) + fixture 0040 | 06 | done | — | (DONE — CLOSED 2026-06-22 at the phase-32 state-6 close-out: **REVIEW.md APPROVED [0 Critical / 0 Important / 6 Minor, all carry-forward]**; the **§7.5 gate (a)-(e) GREEN at the AUTHORITATIVE Linux CI run `27941931062` @ `ecb62d3`** — fixture `0040-accesslog-command-operators` cross-proxy byte-exact custom-format access-log line + all `0001`-`0039` green simultaneously (incl. the byte-identical `0012` regression-equivalence witness) + h2spec ≥95% + the 4 fuzz targets 0 crashes (incl. the new `accesslog_format_parse`) + build/clippy/fmt/test/deny clean; shipped across the 8 TDD task commits `7917c8a`…`cb7a191` + the state-4 STATE commit `bac972c` + the state-5 code-review close `a21b47e` + the state-6 close (THIS commit); `#![forbid(unsafe_code)]` holds; SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/32-accesslog-command-operators/`. **ROADMAP rows `00`-`32` ALL `done`** (`32` is the highest defined row). Original in-progress brainstorm/PLAN scope below — SPEC + PLAN at `docs/envoy-rust/phases/32-accesslog-command-operators/`, scope locked by **ADR-0078** at the state-1 brainstorm; the §6.2 wire facts EMPIRICALLY VERIFIED LOCALLY at the state-2 PLAN-write against `envoyproxy/envoy:v1.33.0` and LOCKED by **ADR-0079** [ALL headline projections CONFIRMED — modern `log_format.text_format_source.inline_string` wire path; absent-value `-`; `:N` byte-trunc-after-paren + `?`-alt grammar; boot-fatal unknown/malformed-operator validity — + 2 clarifications: the trailing-newline rule (Envoy emits a custom `inline_string` verbatim, no auto-`\n` → the `FileSink::emit` verbatim refactor) and stray-single-`%`-is-boot-fatal (`%%`-only literal-`%`)]; SPEC spec-reviewed → APPROVED [0 issues]; PLAN (8 TDD tasks) plan-reviewed → APPROVED [2 advisory recs folded]; **§6.1 single-phase confirmed, ADR-0080 UNFIRED**; the state-3 implementation is the next session [`superpowers:subagent-driven-development`]) target: fixture `0040-accesslog-command-operators` green on an H1 listener whose file access logger configures a custom `log_format` exercising the curated DETERMINISTIC operators (`%REQ(NAME?ALT):N%`/`%RESP(NAME)%`/`%PROTOCOL%`/`%RESPONSE_CODE%`/`%RESPONSE_FLAGS%`/`%BYTES_RECEIVED%`/`%BYTES_SENT%`/`%UPSTREAM_HOST%`) in a non-default order with literals — cross-proxy **byte-identical** per-token (the non-deterministic `%START_TIME%`/`%DURATION%` operators, if used, asserted by the existing `Iso8601Format`/`DurationMs` allow-list rules), the strong differential target; **the FIRST concrete Observability-family row** (the project has emitted only the hardcoded Envoy-v3 default format since phase 06.2 — `crates/envoy-accesslog/src/default_format.rs`, a field-to-string concatenator, NOT a parser). This phase generalizes that into a real command-operator engine that parses a configurable format string (`%OPERATOR%` / `%OPERATOR(args)%` / `%OPERATOR(args):N%` + plain-text literals + `%%` escape) once at config-load and evaluates the compiled format against the existing 15-field `AccessLogRecord` per request, and RE-EXPRESSES the default format AS a parsed string fed through the same engine so **fixture `0012` stays green UNCHANGED** (the 07.1 foundation-slice regression-equivalence witness). **Differential is DETERMINISTIC + byte-exact + LOCALLY observable** (an access-log file scrape on a normal request/response via the existing `Driver::Http1WithAccessLog` — NO file-watch/reload trigger, NOT Linux-CI-only like phases 26/27). **CRITICAL-PATH UNLOCK** — the command-operator engine is the gating dependency for the deferred `header_to_metadata`/`set_metadata` HTTP filters (their dynamic-metadata output is differentially observable only via a future `%DYNAMIC_METADATA%` operator that slots additively into this engine) and for the rest of the Observability family; it is the single pick that re-opens the blocked HTTP-filters vein (the easy header-only-byte-exact vein is exhausted at phase 31). D-items: a command-operator parser + a compiled-format evaluator in `crates/envoy-accesslog/` (refactoring `default_format.rs`); a `log_format` text-format field on `FileAccessLog` (exact wire path — modern `log_format.text_format_source.inline_string` vs deprecated `text_format`/`format` — §6.2-VERIFY) + a format-string validator + a new `ConfigError` variant for a malformed/unknown operator (boot-fatal per ADR-0049, §6.2-VERIFY); the curated deterministic operator set resolving against a fixed name→field allow-list (the record has NO generic header map); fixture `0040` + the unchanged fixture `0012` regression witness + an in-process backstop (parser oracle + the default-format byte-equality round-trip) + a generic custom-format harness comparator (the existing `tokenize_default_format` is default-specific) + a **dedicated `accesslog_format_parse` fuzz target** (§7.4 — a new parser ships a fuzz target; wire it into `ci.yml` in state-3 per the new-fuzz-target discipline) + a `parse_bootstrap` seed + a BEHAVIOR_CONTRACT "Access log field mapping" extension. Load-bearing invariant: all 39 pre-existing fixtures `0001`–`0039` stay green (the engine is inert/default for every listener without a `log_format`; the default format is byte-preserved — regression-equivalence). minimum-viable scope per ADR-0078 (defers `json_format`/`typed_json_format`; the non-chosen wire path; `%DYNAMIC_METADATA%`/`%FILTER_STATE%` [no metadata emitter yet — the future unlock]; the route-name/cluster-name/SNI/`%RESPONSE_CODE_DETAILS%`/`%TRAILER%`/address operators that need new `AccessLogRecord` plumbing; per-side timing operators beyond the allow-listed proof; `omit_empty_values`/`content_type`/custom `formatters`; the gRPC-ALS/OTLP/tracing/stats-sink/tap surfaces). The core differential risk (excluding the non-deterministic timing operators) is handled by the allow-list discipline that fixture `0012` already proved; the exact wire path + per-operator output byte forms + absent-value rendering + the unknown-operator config-validity disposition are de-risked by the state-2 §6.2 LOCAL reconnaissance against `envoyproxy/envoy:v1.33.0`; **ADR-0079 reserved** for the §6.2 reconciliation, **ADR-0080 reserved** for the §6.1 split (projected NOT to fire — ~700–1100 LoC / ~7–9 tasks, comparable to cdn_loop/csrf/buffer; the natural seam is `32.1` parser+engine+config+validator+default-re-expression / `32.2` deterministic-operator wiring + fixture 0040 + comparator + fuzz + BEHAVIOR_CONTRACT + close) |
| 33 | Observability family: dynamic-metadata unlock — `envoy.filters.http.set_metadata` HTTP filter (static-value metadata emitter) + a per-request dynamic-metadata store + the `%DYNAMIC_METADATA(namespace:key)%` access-log command-operator + fixture 0041 | 06 07 32 | done | — | (DONE — CLOSED 2026-06-23 at the phase-33 state-6 close-out: **REVIEW.md APPROVE-WITH-MINORS [0 Critical / 0 Important / 3 Minor — M33-1 (H1 `hcm.rs:1211` `.clone()`→move) + M33-2 (doc-pointer line drift in `command_operator.rs`/`record.rs`) carry-forward, M33-3 noted-only]**; the **§7.5 gate (a)-(e) GREEN at the AUTHORITATIVE Linux CI run `28021495767` @ `72fe40d`** (`build + test + lint` + `fuzz` jobs both `success`) — fixture `0041-http-set-metadata-dynamic-metadata` cross-proxy byte-exact `%DYNAMIC_METADATA%` access-log line (present `tier=prod` + absent `-`, the anti-echo present/absent probe pair) + all `0001`-`0040` green simultaneously (incl. the byte-identical `0012` default-format regression-equivalence witness) + h2spec ≥95% (the §3.5/2 known-failure correct on CI) + the 4 fuzz targets 0 crashes (the existing `accesslog_format_parse` + `parse_bootstrap` with the new phase-33 seeds — NO new target) + build/clippy/fmt/test/deny clean. Shipped across the 12 TDD task commits `433ab4f`…`4a59579` + the state-3 STATE commit `dd9d80d` + the state-4 clippy fix `883cf8a` + the state-4 STATE commit `ca88f83` + the state-5 code-review close `72fe40d` + the state-6 close (THIS commit); `#![forbid(unsafe_code)]` holds; NO new crate/dependency/fuzz-target; SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/33-set-metadata-dynamic-metadata/`. **ROADMAP rows `00`-`33` ALL `done`** (`33` is the highest defined row → STATE.md is now "awaiting next planning"). §A facts locked by **ADR-0081** at the state-2 PLAN-write [two MATERIAL §6.2 divergences from the SPEC projection: the `@type` is `…set_metadata.v3.Config` NOT `…v3.SetMetadata`; and `%DYNAMIC_METADATA%` carries NO `:N` truncation — boot-fatal in Envoy — so `Op::DynamicMetadata` has no `truncate` field]; the **§6.1 split did NOT fire (ADR-0082 reserved-but-UNFIRED)**. Original in-progress brainstorm/PLAN scope below — scope locked by **ADR-0080** at the state-1 brainstorm; the state-2 PLAN-write is the next session [`superpowers:writing-plans`], which runs the §6.2 LOCAL reconnaissance and fires **ADR-0081**) the SECOND concrete Observability-family row (after 32 `command-operator formatter`), landing the **dynamic-metadata critical-path unlock that phase 32 enabled**: phase 32 built the access-log command-operator engine but DEFERRED the `%DYNAMIC_METADATA%` operator because no filter emits dynamic metadata yet (ADR-0078 §2.2). This phase lands the smallest end-to-end differentially-testable metadata loop: (1) a **per-request dynamic-metadata store** — an additive default-empty `dynamic_metadata` field (projected string-only `BTreeMap<String, BTreeMap<String, String>>`, a plain std type, no new crate / no shared Value enum) on `FilterRequest` (`crates/envoy-filter/src/types.rs`) + a sibling field on `AccessLogRecord` (`crates/envoy-accesslog/src/record.rs`), threaded at BOTH independent record-build sites — H1 (`crates/envoy-http1/src/hcm.rs` ~1189) AND H2 (`crates/envoy-http2/src/hcm.rs` ~888, a symmetric capture-and-forward — H2 builds its OWN record and currently drops its `FilterRequest` after `decode_headers`, so it does NOT inherit the record-build from H1); (2) the **`envoy.filters.http.set_metadata` filter** — the 11th `HttpFilterInstance` variant, decode-side, which merges its operator-configured STATIC value into `req.dynamic_metadata` under a namespace and returns `Decision::Continue` (NEVER `StopAndSend` — observability plumbing, not a gate; encode-side inert), following the `cdn_loop`/`header_mutation` add-a-decode-side-filter pattern + a `HttpFilterTypedConfig::SetMetadata` config variant + a `validate_http_filters` arm; (3) the **`%DYNAMIC_METADATA(namespace:key)%` operator** — a new `Op::DynamicMetadata { namespace, key, truncate }` in `command_operator.rs` resolving `record.dynamic_metadata.get(ns)?.get(key)`, absent → the `-` sentinel, composing with the existing `:N`-truncation machinery. **`set_metadata` chosen over `header_to_metadata`** (the latter the pure-additive follow-up reusing this store + operator) because its written value is a config literal → the trivial-est metadata emitter, focusing the phase on the reusable STORE + OPERATOR infrastructure rather than filter logic. **Differential is DETERMINISTIC + byte-exact + LOCALLY observable** — the static-config metadata value renders a byte-identical access-log line cross-proxy (the existing `Driver::Http1AccessLogByteExact` file scrape on a normal request/response, NO file-watch/reload trigger, NOT Linux-CI-only). **CRITICAL-PATH UNLOCK** — re-opens the blocked HTTP-filters family (the largest remaining vein) via dynamic metadata: `header_to_metadata`, jwt_authn `payload_in_metadata`, rbac dynamic-metadata conditions, ext_authz/ext_proc metadata all become reachable once metadata is emittable + loggable. fixture `0041-http-set-metadata-dynamic-metadata` (an H1 listener with a `[set_metadata, router]` chain + a `%DYNAMIC_METADATA%`-bearing `log_format`; present-key + absent-key probes; ≥2 probes) + an in-process backstop (multi-namespace/overwrite/absent-path resolution — the phase-32 fixture-vs-backstop duty split) + a `parse_bootstrap` seed + a BEHAVIOR_CONTRACT "Access log field mapping" extension. Load-bearing invariant: all 40 pre-existing fixtures `0001`–`0040` stay green simultaneously (`set_metadata` INERT outside a chain that contains it — the 07.1 foundation-slice property; the store is additive default-empty; the command-operator engine is byte-preserved for every format not using `%DYNAMIC_METADATA%`, incl. `0012` default-format byte-identical). minimum-viable scope per ADR-0080 (defers the `header_to_metadata` filter [the natural next pick]; non-string metadata Values + the JSON-composite rendering; nested-path resolution `ns:key:sub…`; `%FILTER_STATE%`; `typed_metadata`; per-route `typed_per_filter_config` for `set_metadata`; `set_metadata` `allow_overwrite` advanced semantics; metadata consumers other than the access log [jwt_authn/rbac/ext_authz]; `json_format`/`typed_json_format`/gRPC-ALS/OTLP/tracing/stats-sink/tap). The core differential risk — the EXACT byte form of a resolved string metadata value (raw `prod` vs JSON-quoted `"prod"`) + the absent-value rendering + the `set_metadata` config wire shape (older `metadata_namespace`+`value` vs newer `key`+`value`+`allow_overwrite`) + the `%DYNAMIC_METADATA%` arg-grammar separator + the malformed-config disposition (boot-fatal per ADR-0049, projected) — is de-risked by the state-2 §6.2 LOCAL reconnaissance against `envoyproxy/envoy:v1.33.0`; **ADR-0081 reserved** for the §6.2 reconciliation, **ADR-0082 reserved** for the §6.1 split (projected NOT to fire — ~800–1100 LoC / ~9–11 tasks, comparable to cdn_loop/ring_hash; the natural seam is `33.1` store + `FilterRequest`/`AccessLogRecord` plumbing + `set_metadata` filter + config + validator / `33.2` `%DYNAMIC_METADATA%` operator + fixture 0041 + BEHAVIOR_CONTRACT + fuzz/seed + close). The SPEC was spec-reviewed by a fresh subagent → see REVIEW disposition at state-1 close. The state-2 PLAN-write is the next session [`superpowers:writing-plans`]) |
| 34 | Observability / HTTP-filters family: request-driven metadata — `envoy.filters.http.header_to_metadata` HTTP filter (request-header → dynamic-metadata extraction) + fixture 0042 | 06 07 33 | done | — | (DONE — CLOSED 2026-06-24 at the phase-34 state-6 close-out: **REVIEW.md APPROVE-WITH-MINORS [0 Critical / 0 Important / 3 Minor — M34-1 (no overwrite/last-write-wins unit test in `crates/envoy-filter/src/header_to_metadata.rs`) + M34-2 (the A2 default namespace exercised only at the config-parse layer) + M34-3 (cosmetics: T5 redundant `use tempfile::tempdir`; BEHAVIOR_CONTRACT `A-missing`-vs-`A5` heading; README `generate_request_id … load-bearing`) — all carry-forward]**; the **§7.5 gate (a)-(f) COMPLETE: (a)-(e) GREEN at the AUTHORITATIVE Linux CI run `28062068794` @ `7c19803`** (`build + test + lint` + `fuzz` jobs both `success`) — fixture `0042-http-header-to-metadata` cross-proxy byte-exact `%DYNAMIC_METADATA%` access-log line (header-present + header-missing anti-echo probe pair, the present/missing PAIR guarding the store round-trip) + all `0001`-`0041` green simultaneously (incl. the byte-identical `0012` default-format + `0041` set_metadata regression-equivalence witnesses) + h2spec ≥95% (the §3.5/2 known-failure correct on CI; NO HTTP/2 codec change) + the EXISTING `parse_bootstrap`/`accesslog_format_parse` fuzz targets 0 crashes with the new phase-34 `hcm_header_to_metadata.yaml` seed (NO new target, `ci.yml` unchanged) + build/clippy/fmt/test/deny clean; (f) REVIEW.md approved at the state-5 code-review (CI run `28068092893`). Shipped across the 7 TDD task commits `bf42699`…`48f8086` + the state-3 STATE commit `7c19803` + the state-4 STATE commit `dcfe4ea` + the state-5 code-review close `44f3b81` + the state-6 close (THIS commit); `#![forbid(unsafe_code)]` holds; NO new crate/dependency/fuzz-target; SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/34-header-to-metadata/`. **ROADMAP rows `00`-`34` ALL `done`** (`34` is the highest defined row → STATE.md is now "awaiting next planning"). §A facts locked by **ADR-0084** at the state-2 PLAN-write [two MATERIAL §6.2 divergences from the SPEC projection: the default `metadata_namespace` (when omitted) is **`envoy.filters.http.header_to_metadata`** (the filter canonical name) NOT the projected `envoy.lb`; and on `on_header_present`, when both a static `value` and a present header exist, the **static `value` WINS** over the header value]; the **§6.1 split did NOT fire** (~7 tasks). Original in-progress brainstorm/PLAN scope below — scope locked by **ADR-0083** at the state-1 brainstorm; §A facts locked by **ADR-0084** at the state-2 PLAN-write) the THIRD concrete row consuming the phase-33 dynamic-metadata infrastructure (after 32 `command-operator formatter` + 33 `set_metadata`+store+`%DYNAMIC_METADATA%`): the **request-driven metadata emitter** that phase 33 named "the natural next pick". Lands the `envoy.filters.http.header_to_metadata` filter — the 12th `HttpFilterInstance` variant, decode-side, `Continue`-only — which on `decode_headers` evaluates configured `request_rules` against the request headers and merges the extracted **string** value into `req.dynamic_metadata` under a `metadata_namespace`→`key` (on_header_present: the header value or a static `value` override; on_header_missing: a static fallback), following the `set_metadata`/`cdn_loop` add-a-decode-side-filter pattern + a `HeaderToMetadataConfig` + a `HttpFilterTypedConfig::HeaderToMetadata` variant (`@type …header_to_metadata.v3.Config`) + a `validate_http_filters` arm. **REUSES the phase-33 dynamic-metadata store + the `%DYNAMIC_METADATA(namespace:key)%` operator + the H1/H2 capture-before-drop threading UNCHANGED** (no new infrastructure — `envoy-accesslog` is untouched; the threading already carries any `req.dynamic_metadata`). **Differential is DETERMINISTIC + byte-exact + LOCALLY observable** — the probe controls the header value, so the `%DYNAMIC_METADATA(ns:key)%` render of the extracted value is byte-identical cross-proxy (the existing `Driver::Http1AccessLogByteExact` file scrape, NO reload trigger, NOT Linux-CI-only); a STRONGER request-driven differential than `set_metadata`'s static literal. fixture `0042-http-header-to-metadata` (H1 `direct_response` listener with a `[header_to_metadata, router]` chain + a `%DYNAMIC_METADATA%`-bearing `log_format`; header-present + header-missing probes via the existing `AccessLogByteExactProbe.extra_headers`; ≥2 probes — the present/missing PAIR guards the store round-trip) + an in-process backstop (present/missing/override/multi-rule + the H2 thread) + a `parse_bootstrap` seed + a BEHAVIOR_CONTRACT extension. Load-bearing invariant: all 41 pre-existing fixtures `0001`–`0041` stay green (the filter is INERT outside its chain; the store/operator/threading are byte-preserved, incl. `0012` default-format + `0041` set_metadata byte-identical). **thin reuse-slice MVP per ADR-0083** (request_rules only, string-only on_header_present + on_header_missing; defers `response_rules` [encode-side], typed NUMBER/PROTOBUF values + the non-string Value-enum, `encode: BASE64`, `regex_value_rewrite`, the `remove` header-mutation, per-route `typed_per_filter_config`, and metadata consumers [jwt_authn/rbac/ext_authz]). The differential risk — the `request_rules`/`Rule`/`KeyValuePair` wire shape + the default `metadata_namespace` (`envoy.lb`) + the on_header_present value-vs-override precedence + the on_header_missing semantics + the malformed-config disposition (boot-fatal per ADR-0049, projected) — is de-risked by the state-2 §6.2 LOCAL reconnaissance against `envoyproxy/envoy:v1.33.0`; **ADR-0084 reserved** for the §6.2 reconciliation; the **§6.1 split projected NOT to fire** (~600–900 LoC / ~7–9 tasks, smaller than phase 33 since it adds no infrastructure — the natural seam if forced is `34.1` filter+config+validator+variant / `34.2` fixture 0042 + BEHAVIOR_CONTRACT + seed + close). The SPEC was spec-reviewed by a fresh subagent → APPROVE-WITH-MINORS (3 presentational nits folded in-review). The state-2 PLAN-write is the next session [`superpowers:writing-plans`]) |
| 35 | Observability / HTTP-filters family: dynamic-metadata CONSUMER — `envoy.filters.http.rbac` `metadata` Permission/Principal condition (reads the phase-33 dynamic-metadata store, written mid-chain by an upstream producer filter) + fixture 0043 | 06 07 10 33 34 | done | — | (DONE — CLOSED 2026-06-25 at the phase-35 state-6 close-out: **REVIEW.md APPROVE [0 Critical / 0 Important; sole carry-forward M35-1 (the pre-existing-SafeRegex-in-RBAC-values limitation — a `safe_regex` inside an RBAC `metadata`/`header` matcher `value` is config-load-accepted but NOT compiled by the RBAC validation path → a runtime `ValueMatcher::matches`/header match on a SafeRegex value would panic; not exercised by fixture `0043` which uses `exact`; clean fix home = compile RBAC SafeRegex at `rbac.rs` lowering time covering BOTH `header` + `metadata`, a future RBAC-StringMatcher phase)]**; the **§7.5 gate (a)-(f) COMPLETE: (a)-(e) GREEN at the AUTHORITATIVE Linux CI run `28125206968` @ `d1cf8a3`** (`build + test + lint` job `83287136008`: 114 `test result: ok` / 0 FAILED + `fuzz` job `83287136031`, both `success`) — fixture `0043-http-rbac-dynamic-metadata` cross-proxy byte-exact (the producer-before-consumer `[header_to_metadata, rbac, router]` H1 chain over a `direct_response` route: `X-Tier: prod`→`200`+`ok\n` ALLOW / `X-Tier: dev`/absent→`403`+`RBAC: access denied` 19B DENY) + all `0001`-`0042` green simultaneously (incl. the byte-identical `0017` rbac-header-only + `0012` default-format + `0041` set_metadata + `0042` header_to_metadata regression-equivalence witnesses — the `metadata` matcher is an ADDITIVE enum variant no existing config uses) + h2spec ≥95% (NO HTTP/2 codec change) + the EXISTING `parse_bootstrap` fuzz target 0 crashes with the new phase-35 `hcm_rbac_metadata.yaml` seed (NO new target, `ci.yml` unchanged) + build/clippy/fmt/test/deny clean; (f) REVIEW.md approved at the state-5 code-review (verdict APPROVE, 0 Critical / 0 Important; CI run `28131626377` @ `e8e463f`). Shipped across the 6 TDD task commits `c05426f`…`80c3256` + the clippy doc-fix `c9822ce` + the final-minors fold `177391c` + the state-3 STATE commit `d1cf8a3` + the state-4 STATE commit `bc13090` + the state-5 code-review close `e8e463f` + the state-6 close (THIS commit); `#![forbid(unsafe_code)]` holds; NO new crate/dependency/fuzz-target/`HttpFilterInstance` variant; SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/35-rbac-dynamic-metadata/`. **ROADMAP rows `00`-`35` ALL `done`** (`35` is the highest defined row → STATE.md is now "awaiting next planning"). §A facts locked by **ADR-0086** at the state-2 PLAN-write [THREE MATERIAL §6.2 divergences from the SPEC projection: (i) the `rbac.v3.Permission.metadata` / `Principal.metadata` fields are DEPRECATED-but-functional at v1.33.0 — a non-differential boot warning; (ii) a MULTI-SEGMENT `path` is ACCEPTED by Envoy → envoy-rust's flat string-only store rejects it boot-fatal (`path.len() != 1` → `ConfigError::RbacMetadataMatcherInvalid`, stricter); (iii) a NON-`string_match` `value` is ACCEPTED by Envoy → envoy-rust's string-only `ValueMatcher` rejects it boot-fatal via its hand-rolled "exactly one key" visitor (stricter)]; the **§6.1 split did NOT fire** (~6 tasks). Original in-progress brainstorm/PLAN scope below — scope locked by **ADR-0085** at the state-1 brainstorm; §A facts locked by **ADR-0086** at the state-2 PLAN-write) the **FIRST dynamic-metadata CONSUMER** — closing the produce→consume loop the phase-33/34 metadata arc teed up (after 32 `command-operator formatter` + 33 `set_metadata`+store+`%DYNAMIC_METADATA%` + 34 `header_to_metadata`, all PRODUCERS whose only sink was the access log). Extends the EXISTING phase-10 `envoy.filters.http.rbac` filter (NO new `HttpFilterInstance` variant) with a **`metadata` Permission/Principal condition**: a string-only `MetadataMatcher { filter, path: [{ key }], value: { string_match: StringMatcher } }` (config: a `Metadata(MetadataMatcher)` variant on the `Permission` + `Principal` enums via the existing hand-rolled "exactly one map key" `Deserialize` visitors + the matcher struct trio, single-segment `path`, `string_match`-only, `deny_unknown_fields`; runtime: `RuntimePermission::Metadata`/`RuntimePrincipal::Metadata` + eval arms in `eval_permission`/`eval_principal` reading `req.dynamic_metadata.get(filter)?.get(path[0].key)` + the existing `lower_*` arms — the RBAC decision matrix + the `403`+`RBAC: access denied` local reply UNCHANGED). The load-bearing mechanism: the decode pipeline threads ONE `&mut FilterRequest` through every filter in chain order (`crates/envoy-filter/src/pipeline.rs:77`), so RBAC reads the `dynamic_metadata` an upstream producer wrote IN THE SAME PASS — the fixture chain `[header_to_metadata, rbac, router]` (producer before consumer) is REQUIRED. **Differential is byte-exact + DETERMINISTIC + LOCALLY observable** — the probe controls the request header → the metadata value → the RBAC verdict: `X-Tier: prod` → metadata match → ALLOW → `200`+`direct_response` body; `X-Tier: dev`/absent → no match → DENY → `403`+`RBAC: access denied` (19B), both byte-identical cross-proxy (the existing fixture-`0017` status+body comparator + the `extra_headers` probe; NO reload trigger, NOT Linux-CI-only). REUSES the phase-10 RBAC engine + the 04.x `StringMatcher` + the phase-33 store + the phase-34 `header_to_metadata` producer + the decode threading UNCHANGED (no new infrastructure). fixture `0043-http-rbac-dynamic-metadata` (H1 listener, `[header_to_metadata, rbac, router]` chain, an `action: ALLOW` single-policy with the `metadata` condition; ≥2 probes: header-present-match→200 + header-mismatch/absent→403) + an in-process backstop (present-match / absent-namespace / absent-key / value-mismatch / and_rules-or_rules-not_rule compose / Principal / DENY-inversion / the mid-chain producer→consumer thread) + a `parse_bootstrap` seed (no new fuzz target — reuses the serde parse path) + a BEHAVIOR_CONTRACT "HTTP filters" extension. Load-bearing invariant: all 42 pre-existing fixtures `0001`–`0042` stay green (the `metadata` matcher is an additive enum variant no existing config uses; the store/producers/operator/threading are byte-preserved, incl. `0017` rbac header-only + `0012` default-format + `0041` set_metadata + `0042` header_to_metadata). minimum-viable scope per ADR-0085 (defers non-string `ValueMatcher` variants [null/double/bool/present/list/or_match — `present_match` the cheapest follow-up]; multi-segment/nested `path`; `MetadataMatcher.invert`; the other not-yet-shipped RBAC condition types [IP/port/url_path/SNI]; `shadow_rules`; per-route `typed_per_filter_config`; other producers/consumers [jwt_authn `payload_in_metadata`, ext_authz/ext_proc — gRPC-blocked]; `json_format`/gRPC-ALS/OTLP/tracing/stats-sink/tap). The core differential risk (the `MetadataMatcher`/`ValueMatcher` wire shape + the `filter`→namespace correspondence + the present/absent match semantics + the StringMatcher modes + the config-validity disposition) is de-risked by the state-2 §6.2 LOCAL reconnaissance against `envoyproxy/envoy:v1.33.0`; **ADR-0086 reserved** for the §6.2 reconciliation; the **§6.1 split projected NOT to fire** (~500–900 LoC / ~7–9 tasks, comparable to `header_to_metadata`/`cdn_loop`/`csrf` — the natural seam if forced is `35.1` schema+visitors+runtime / `35.2` fixture 0043 + BEHAVIOR_CONTRACT + seed + close). The SPEC was spec-reviewed by a fresh subagent → see REVIEW disposition at state-1 close. The state-2 PLAN-write is the next session [`superpowers:writing-plans`] |
| 36 | Observability / HTTP-filters family: RBAC matcher-value enrichment — the `present_match` `ValueMatcher` variant on the phase-35 RBAC `metadata` condition (F1) + `safe_regex` `StringMatcher` compilation on the RBAC path / header + metadata values, closing carry-forward M35-1 (F2) + fixture 0044 | 06 07 10 33 34 35 | done | — | (DONE — CLOSED 2026-06-25 at the phase-36 state-6 close-out: **REVIEW.md APPROVE [0 Critical / 0 Important / 2 Minor — M36-2 (`ValueMatcher::matches(&str)` now production-dead + stale "Kept for the value-present call sites" doc-comment; cleanup-only) + M36-3 (present-but-empty correctness enforced one filter UPSTREAM by `header_to_metadata`; a one-line `eval_metadata` comment would harden it; doc-only) — both carry-forward; carry-forward M35-1 CONSUMED by F2]**; the **§7.5 gate (a)-(f) COMPLETE: (a)-(e) GREEN at the AUTHORITATIVE Linux CI run `28199106154` @ `19c3fe9`** (`build + test + lint` job `83533644207` + `fuzz` job `83533644384`, both `success`) — fixture `0044-http-rbac-matcher-value-enrichment` cross-proxy byte-exact on all 4 probes (F1 present/absent: `x-present`→key-present→`200`+`direct_response` body / absent→key-absent→`403`+`RBAC: access denied` 19B; F2 anchored `safe_regex` `^(prod|staging)$`: `staging`→`200` / `dev`→`403`) + all `0001`-`0043` green simultaneously (incl. the byte-identical `0043` rbac-metadata-`exact` + `0017` rbac header-only + `0012`/`0041`/`0042` regression-equivalence witnesses — F1 `present_match` is an additive `ValueMatcher` variant no existing config uses; the F2 SafeRegex-compilation fix is behavior-preserving for every non-SafeRegex RBAC matcher) + h2spec ≥95% (NO HTTP/2 codec change) + the EXISTING `parse_bootstrap` fuzz target 0 crashes with the new phase-36 `rbac_present_match.yaml` + `rbac_safe_regex.yaml` seeds (NO new target, `ci.yml` unchanged) + build/clippy/fmt/test/deny clean; (f) REVIEW.md approved at the state-5 code-review (verdict APPROVE, 0 Critical / 0 Important / 2 Minor; CI run `28203230624` @ `3e1346f`). Shipped across the 7 TDD task commits `0e41640`…`e8f618e` + the state-3 STATE commit `19c3fe9` + the state-4 STATE commit `b1b5044` + the state-5 code-review close `3e1346f` + the state-6 close (THIS commit); `#![forbid(unsafe_code)]` holds; NO new crate/dependency/fuzz-target/`HttpFilterInstance` variant/`ConfigError`/`FilterError` variant; SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/36-rbac-matcher-value-enrichment/`. **ROADMAP rows `00`-`36` ALL `done`** (`36` is the highest defined row → STATE.md is now "awaiting next planning"). §A facts locked by **ADR-0088** at the state-2 PLAN-write [the two material divergences A1 (F1 match = `present && want` — `present_match: false` NEVER matches, correctly diverging from BOTH the SPEC's `present == want` projection AND the `HeaderMatcherMode::PresentMatch` `want ? present : true` precedent in the same file) / A3b (the ANCHORED `^(prod|staging)$` pattern LOCKED everywhere — fixture + both fuzz seeds + backstops — so partial==full and the M36-1 unanchored-SafeRegex partial-vs-full divergence stays deferred)]; **F2 closed carry-forward M35-1** (every RBAC-reachable SafeRegex now compiled at `rbac.rs` LOWERING time on the owned clone, both `Permission`/`Principal` `Header` + the phase-35 `metadata` `value`; `lower_permission`/`lower_principal` made fallible, threaded through `build_from_config` → a malformed RBAC regex is BOOT-fatal not a first-request panic; the panic-regression guard genuinely panicked on the pre-fix tree); the **§6.1 split did NOT fire** (~7 tasks; **ADR-0089 reserved-but-UNFIRED**). Original in-progress brainstorm/PLAN scope below — scope locked by **ADR-0087** at the state-1 brainstorm; §A facts locked by **ADR-0088** at the state-2 PLAN-write) the SECOND row enriching the dynamic-metadata-CONSUMER vein phase 35 opened (after 35 landed the FIRST consumer — the RBAC `metadata` Permission/Principal condition, string-`exact`-only), and the row that FOLDS the freshest carry-forward **M35-1**. Two byte-exact, DETERMINISTIC, LOCALLY-observable matcher-VALUE enrichments on the EXISTING phase-10 `envoy.filters.http.rbac` filter — **NO new `HttpFilterInstance` variant, NO new infrastructure** (reuses the phase-10 RBAC engine + decision matrix + `403`/`RBAC: access denied` local reply, the 04.x `StringMatcher`/`SafeRegex` + the `regex` foundation [ADR-0021], the phase-33 dynamic-metadata store, the phase-34 `header_to_metadata` producer, and the decode-pipeline shared-`&mut FilterRequest` threading UNCHANGED): **(F1)** add a `PresentMatch(bool)` variant to the `ValueMatcher` enum (`crates/envoy-config/src/bootstrap.rs` hand-rolled `Deserialize`/`Serialize` + `KEYS` grows to `["string_match", "present_match"]`; all other oneof keys stay `unknown_field`→boot-fatal, stricter than Envoy) — match on KEY PRESENCE not value content (ADR-0086 §A6 confirmed Envoy accepts `present_match` and named it "the cheapest §2.2 additive follow-up"); restructure `eval_metadata` (`crates/envoy-filter/src/rbac.rs`) so `PresentMatch(want)` compares presence against the bool (it CANNOT route through `ValueMatcher::matches(value: &str)`, which only runs when a value is present); differential: header-present→metadata-key-present→`200`+`direct_response` body / header-absent→key-absent→`403`+`RBAC: access denied` 19B. **(F2)** compile every RBAC-reachable `SafeRegex` into `SafeRegex::compiled` (`Arc<regex::Regex>`) at `rbac.rs` LOWERING time (`lower_permission`/`lower_principal` — the M35-1-named fix home, a `&mut`-capable site unlike the immutable-borrow config validator), covering BOTH the `Permission::Header`/`Principal::Header` `safe_regex_match` mode AND the phase-35 `metadata` `value: { string_match: { safe_regex } }` — **closing M35-1, a LATENT RUNTIME PANIC** where `StringMatcher::matches` (`crates/envoy-config/src/matcher.rs:90`) does `sr.compiled.as_ref().expect(...)` but the RBAC validation path never compiled it (NOTE `bootstrap.rs:4040-4044`), so a `safe_regex` value in ANY RBAC matcher panics on the first matching request (fixture 0043 used `exact`, so it never fired); a malformed RBAC regex becomes BOOT-fatal (not a first-request panic), mirroring the route-config `validate_header_matcher` compiler; differential: regex-match→`200` / regex-miss→`403`. fixture `0044-http-rbac-matcher-value-enrichment` (H1 `direct_response` listener + a `[header_to_metadata, rbac, router]` chain; F1 present/absent probe pair + F2 regex-match/miss probe pair) + an in-process backstop (present/absent/`present_match: false`/compose-in-and_rules-or_rules-not_rule/Principal/DENY-inversion + a `safe_regex` METADATA and a `safe_regex` HEADER matcher matching/rejecting WITHOUT panicking — the M35-1 panic-regression guard + a malformed-regex boot-fatal) + `parse_bootstrap` seeds (no new fuzz target — both reuse the serde/`regex` parse paths) + a BEHAVIOR_CONTRACT "Phase 36" extension (the `present_match` presence semantics + the now-compiled RBAC SafeRegex, superseding the M35-1 limitation note). Load-bearing invariant: all 43 pre-existing fixtures `0001`–`0043` stay green (the `present_match` variant is additive, no existing config uses it; the SafeRegex-compilation fix is behavior-preserving for every non-SafeRegex RBAC matcher and a no-op for the already-compiling route-config walk — incl. `0043` rbac-metadata-`exact` + `0017` rbac header-only + `0012`/`0041`/`0042` byte-identical). minimum-viable scope per ADR-0087 (defers the remaining non-string `ValueMatcher` variants [`null`/`double`/`bool`/`list`/`or` — need the shared non-string-Value generalization]; multi-segment/nested `path` [`path.len() == 1` validator UNCHANGED]; `MetadataMatcher.invert`; the `google_re2` engine knobs; the other not-yet-shipped RBAC condition types [IP/port/url_path/SNI]; `shadow_rules`; per-route `typed_per_filter_config`; other producers/consumers [jwt_authn `payload_in_metadata`, ext_authz/ext_proc — gRPC-blocked]; `json_format`/gRPC-ALS/OTLP/tracing/stats-sink/tap). The core differential risk (the `present_match` wire shape/semantics + present-empty disposition + the `safe_regex` RBAC verdicts + the regex-dialect parity + the non-string-Value config-validity dispositions) is de-risked by the state-2 §6.2 LOCAL reconnaissance against `envoyproxy/envoy:v1.33.0`; **ADR-0088 reserved** for the §6.2 reconciliation; the **§6.1 split projected NOT to fire** (~400–800 LoC / ~6–9 tasks; **ADR-0089 reserved** — the natural seam if forced is `36.1` F2 RBAC-SafeRegex correctness slice [backstop-only + the panic-regression guard] / `36.2` F1 `present_match` + fixture 0044 + BEHAVIOR_CONTRACT + seeds + close). The SPEC was spec-reviewed by a fresh subagent → see REVIEW disposition at state-1 close. The state-2 PLAN-write is the next session [`superpowers:writing-plans`] |
| 37 | Observability / HTTP-filters family: RBAC condition-TYPE widening — the `url_path` Permission/Principal condition (Envoy `type.matcher.v3.PathMatcher`: `url_path: { path: { <StringMatcher> } }`, matching the request path with the `?query` stripped) on the EXISTING phase-10 `envoy.filters.http.rbac` filter + fixture 0045 | 06 07 10 | done | — | (DONE — CLOSED 2026-06-26 at the phase-37 state-6 close-out: **REVIEW.md APPROVED [0 Critical / 0 Important / 1 new Minor M37-2]** at the state-5 code-review [commit `d3ce688`, doc-only CI run `28229588636` `completed/success`]; the **§7.5 phase-done gate (a)-(f) GREEN at the AUTHORITATIVE Linux CI run `28228815294` @ `cfe03b5`** [the state-4 verification commit, `completed/success` — fixture `0045-http-rbac-url-path` cross-proxy byte-exact on the match (`/allowed`→200+`direct_response` body) / miss (→403+`RBAC: access denied` 19B) / query-strip (`/allowed?x=1`→200) probes + all `0001`-`0044` green simultaneously + h2spec ≥95% (NO HTTP/2 codec change) + the EXISTING `parse_bootstrap` fuzz target 0 crashes with the new `hcm_rbac_url_path.yaml` seed (NO new target, `ci.yml` unchanged) + build/clippy/fmt/test/deny clean]; the per-task commit range `3b69a78`..`b5b420c` [T1 PathMatcher / T2 Permission::UrlPath / T3 Principal::UrlPath / T4 backstop / T5 config-validity §D / T6 fixture 0045 + differential wrapper / T7 parse_bootstrap seed + BEHAVIOR_CONTRACT] + the doc-only state markers `f73cc25` (state-3) / `cfe03b5` (state-4) / `d3ce688` (state-5) + this state-6 close; **§A-§D ground truth locked by ADR-0090**; the **§6.1 split did NOT fire** [~7 tasks / ~350-550 LoC; **ADR-0091 reserved-but-UNFIRED** — stays available for a future split trigger]; NO new `HttpFilterInstance` variant / crate / dependency / fuzz-target / `ConfigError` variant; `#![forbid(unsafe_code)]` holds; no `Cargo.toml`/`Cargo.lock` change. NEW carry-forward **M37-2** [the `lower_principal` `url_path` arm is not exercised end-to-end through `build_from_config` — trivially symmetric to the covered Permission arm; coverage-only, non-blocking] + **M37-1** [`#fragment` rejected at the H1 codec as a 400 before `url_path`; OUT of scope]; **M36-1** [unanchored SafeRegex partial-vs-full] anchored-LOCKED, NOT consumed. SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/37-rbac-url-path-condition/`. Original in-progress brainstorm/PLAN scope below — state-2 PLAN-write COMPLETE: SPEC authored + spec-reviewed → APPROVED [scope locked by **ADR-0089**]; the state-2 PLAN-write ran the §6.2 LOCAL reconnaissance against live `envoyproxy/envoy:v1.33.0` [the path-extraction is **query-strip ONLY** — Envoy applies NO percent-decode / dot-segment / slash-merge / case normalization by default at this pin; Principal acceptance + the `PathMatcher` wire shape + RE2-FULL-match `safe_regex` + all four config-validity cases boot-fatal-ON-BOTH all CONFIRMED], fired **ADR-0090** [§6.2 reconciliation — the projection CONFIRMED across the board; **NO new `ConfigError` variant** needed (serde + the lowering compile cover every validity case); new carry-forward **M37-1** = `#fragment` rejected at the H1 codec as a 400 before `url_path`], authored `PLAN.md` [7 TDD tasks, plan-reviewed by a fresh subagent → ✅ APPROVED after a JSON-round-trip + differential-wrapper fix], and confirmed the **§6.1 split does NOT fire** [~7 tasks / ~350–550 LoC; **ADR-0091 stays UNFIRED**]. SPEC + PLAN at `docs/envoy-rust/phases/37-rbac-url-path-condition/`. The state-3 implementation is the next session [`superpowers:executing-plans` / `subagent-driven-development`]) the row that WIDENS the RBAC condition-TYPE axis (after phases 35/36 deepened the matcher-VALUE axis — the `metadata` condition, then `present_match` + `safe_regex` on it). Adds the `url_path` condition type to BOTH the `Permission` and `Principal` enums on the EXISTING phase-10 RBAC filter — **NO new `HttpFilterInstance` variant, NO new infrastructure, NO metadata store, NO producer chain** (reuses the phase-10 RBAC engine + decision matrix + `403`/`RBAC: access denied` 19B local reply, the 04.x `StringMatcher`/`SafeRegex` + the `regex` foundation [ADR-0021], and the phase-36 compile-RBAC-SafeRegex-at-lowering fallible-lowering path UNCHANGED). **`url_path` chosen as the cheapest-strong next leaf** — the ONLY not-yet-shipped RBAC condition type that reads a request attribute the filter ALREADY holds (`FilterRequest.path`, `crates/envoy-filter/src/types.rs:28-30`), so it needs ZERO new connection-context plumbing; the connection-context conditions (`destination_ip`/`destination_port`/`source_ip`/`direct_remote_ip`/`remote_ip`/`requested_server_name`-SNI) all need the downstream socket address / TLS SNI threaded into the filter and defer. **Differential is byte-exact + DETERMINISTIC + LOCALLY observable** — the probe controls the request-line path → the RBAC verdict: a matching path → `200` + the route `direct_response` body; a non-matching path → `403` + `RBAC: access denied` (19B), byte-identical cross-proxy (the existing fixture-`0017`/`0043`/`0044` status+body comparator with path-varying probes; NO reload trigger, NOT Linux-CI-only). The **load-bearing differential richness is the query-strip semantic**: Envoy matches `url_path` against the path with the `?query` removed, whereas envoy-rust's existing route matcher compares the raw request-target byte-for-byte (`crates/envoy-http1/src/hcm.rs:1510-1521`, `path == p`; nothing strips the query today — `crates/envoy-http1/src/codec.rs:104-106`), so a `/allowed?x=1` probe matches `url_path: { path: { exact: "/allowed" } }` (query stripped) but would NOT under a whole-request-target exact match — the fixture proves genuine query-stripped path-extraction, not a header alias. D-items: a `UrlPath(PathMatcher)` variant on the `Permission` enum (`crates/envoy-config/src/bootstrap.rs:1409-1417`) + the `Principal` enum (`:1491-1504`) — the hand-rolled "exactly one map key" `Deserialize`/`Serialize` visitors + the `KEYS` arrays each grow by `"url_path"`; a thin `PathMatcher { path: StringMatcher }` config struct (the phase-35 `MetadataMatcher` precedent); `RuntimePermission::UrlPath`/`RuntimePrincipal::UrlPath` + an `eval` arm in `eval_permission`/`eval_principal` (`crates/envoy-filter/src/rbac.rs:74-110`) resolving the query-stripped `req.path` then `StringMatcher::matches`; a query-strip helper (substring before the first `?`); the two `lower` arms reusing the phase-36 fallible SafeRegex-at-lowering compile (`lower_permission`/`lower_principal`, `:247-326`) so a `safe_regex` `url_path` value is BOOT-fatal on a bad pattern, not a first-request panic; fixture `0045-http-rbac-url-path` (H1 `direct_response` listener + a plain `[rbac, router]` chain — NO producer needed; an `action: ALLOW` single-policy `url_path` Permission; probes: match → 200 / miss → 403 / query-strip `/allowed?x=1` → 200) + an in-process backstop (Principal, composition in `and_rules`/`or_rules`/`not_rule`, DENY-inversion, an ANCHORED `safe_regex` `url_path` [M36-1 — partial==full], malformed-regex + empty-`PathMatcher` boot-fatal) + a `parse_bootstrap` fuzz seed (no new fuzz target — reuses the serde + `regex` parse paths) + a BEHAVIOR_CONTRACT "HTTP filters" RBAC `url_path` subsection. Load-bearing invariant: all 44 pre-existing fixtures `0001`-`0044` stay green simultaneously (the `url_path` variant is additive, no existing config uses it; nothing in the RBAC engine or path/route machinery changes — incl. `0043`/`0044` rbac-metadata + `0017` rbac header-only + `0012`/`0041`/`0042` byte-identical). minimum-viable scope per ADR-0089 (defers the connection-context RBAC conditions [need new socket-address/SNI plumbing]; `url_path` path-normalization beyond query-strip [if §6.2 shows Envoy percent-decodes / strips `#fragment` / normalizes `//`,`/./`,`/../`,case — the fixture locks already-normalized paths, recording any residual gap as a named carry-forward]; the unanchored-SafeRegex partial-vs-full gap [M36-1 — weighed, anchored-locked, NOT consumed]; the non-string `ValueMatcher` variants + multi-segment metadata `path` + `MetadataMatcher.invert`; `shadow_rules`; per-route `typed_per_filter_config`; the rest of the Observability family [`json_format`/gRPC-ALS/OTLP/tracing/stats-sink/tap]). The core differential risk (the EXACT path Envoy matches `url_path` against — query-strip only vs further normalization — + the `PathMatcher` wire shape + the `Principal`-`url_path` acceptance + the `safe_regex` verdicts + the config-validity dispositions) is de-risked by the state-2 §6.2 LOCAL reconnaissance against `envoyproxy/envoy:v1.33.0` (sweep `/allowed?x=1`, `/allowed#frag`, `/al%6Cowed`, `/allowed/../allowed`, `//allowed`, `/allowed/` against a `url_path: { exact: "/allowed" }` policy, recording ALLOW/DENY for each); **ADR-0090 reserved** for the §6.2 reconciliation, **ADR-0091 reserved** for the §6.1 split (projected NOT to fire — ~350–700 LoC / ~6–9 tasks, comparable to `header_to_metadata`/phase-36; the natural seam if forced is `37.1` config schema + `PathMatcher` + validator + parse-layer tests / `37.2` runtime `eval`/`lower` + query-strip + fixture 0045 + BEHAVIOR_CONTRACT + seed + close). The SPEC was spec-reviewed by a fresh subagent → APPROVED; the PLAN was plan-reviewed by a fresh subagent → ✅ APPROVED. **§6.2 RESULT (ADR-0090):** the path-extraction is query-strip-ONLY (the MVP projection CONFIRMED — no further normalization at v1.33.0 defaults); `safe_regex` is RE2-FULL-match against the stripped path; the empty/path-less `PathMatcher` + unknown sub-key + malformed-regex are boot-fatal on BOTH proxies; NO new `ConfigError` variant; new carry-forward M37-1 (`#fragment` codec-400). §6.1 split did NOT fire (ADR-0091 UNFIRED). The state-3 implementation is the next session [`superpowers:executing-plans` / `subagent-driven-development`] |
| 38 | Observability family: `json_format` access-log encoder — a `FileAccessLog.log_format.json_format` JSON-object output mode reusing the phase-32 command-operator engine over the existing `AccessLogRecord` (ZERO new connection plumbing / request attribute / `HttpFilterInstance` variant) + fixture 0046 | 06 32 | done | — | (DONE — CLOSED 2026-06-27 at the phase-38 state-6 close-out: **REVIEW.md APPROVE [0 Critical / 0 Important / 4 new Minor M38-1..M38-4]** at the state-5 code-review [commit `4dd0482`, doc-only **CI run `28289653535` `completed/success`**]; the **§7.5 phase-done gate (a)-(f) GREEN at the AUTHORITATIVE Linux CI run `28282724653` @ `5665c3e`** [the state-4 verification commit, `completed/success` — fixture `0046-accesslog-json-format` cross-proxy byte-exact JSON line `{"bytes_rcvd":0,"bytes_sent":3,"flags":"-","method":"GET","mixed":"code-200","path":"/","protocol":"HTTP/1.1","status":200,"upstream":null}` + all `0001`-`0045` green simultaneously [incl. the byte-identical `0012` default-format + `0040` text-custom + `0041`/`0042` `%DYNAMIC_METADATA%` regression-equivalence witnesses] + h2spec ≥95% [NO HTTP/2 codec change] + the EXISTING `parse_bootstrap` (new `json_format_logger.yaml` seed) + `accesslog_format_parse` fuzz targets 0 crashes [NO new target, `ci.yml` unchanged] + build/clippy/fmt/test/deny clean]; the implementation commit range `56184a9`..`44f177a` [T1 `SubstitutionFormatString {text_format_source\|json_format}` oneof (BTreeMap, sorted) / T2 exactly-one-of `log_format` validator + `ConfigError::AmbiguousLogFormat` / T3 hand-rolled JSON string escaper / T4 per-operator typed JSON value encoder (number/string/null) / T5 `CompiledJsonFormat` compile + sorted-object render / T6 `LogFormat` enum (Text\|Json) on `FileSink` via `Into` / T7 HCM wires `LogFormat` from config / T8 fixture 0046 / T9 BEHAVIOR_CONTRACT json_format subsection + `parse_bootstrap` seed] + the doc-only state markers `5665c3e` (state-4) / `4dd0482` (state-5) + this state-6 close; **§A-§F ground truth locked by ADR-0092** [keys SORTED by UTF-8 bytes (`BTreeMap`); values TYPE-INFERRED (single numeric op→number, single string op→quoted, absent single op→`null`, mixed/literal→quoted string with `-`); compact separators + trailing `\n` + standard-JSON hand-rolled escaping; empty map VALID (`{}\n`); exactly-one-of boot-fatal both/neither → ONE new `ConfigError::AmbiguousLogFormat`]; the **§6.1 split did NOT fire** [~9 tasks; **ADR-0093 reserved-but-UNFIRED** — stays available for a future split trigger]; NO new crate / dependency / fuzz-target; exactly ONE new `ConfigError` variant (`AmbiguousLogFormat`); `#![forbid(unsafe_code)]` holds; no `Cargo.toml`/`Cargo.lock` change. NEW carry-forwards **M38-1** [duplicated Req/Resp resolve+truncate across the text/JSON render paths] + **M38-2** [`%DYNAMIC_METADATA%` single-op JSON quoting unverified cross-proxy / diverges from raw text] + **M38-3** [empty-value-string `""` edge untested] + **M38-4** [typed-JSON-path test gaps] — all coverage/maintainability-only, non-blocking. SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/38-accesslog-json-format/`. **ROADMAP rows `00`-`38` ALL `done`** (`38` is the highest defined row → STATE.md is now "awaiting next planning"). Original in-progress brainstorm/PLAN scope below — SPEC at `docs/envoy-rust/phases/38-accesslog-json-format/`, scope locked by **ADR-0091** at the state-1 brainstorm) the SECOND-and-onward Observability-family ENCODER row (after 32 the text command-operator formatter; 33/34/35/36/37 ran the dynamic-metadata + RBAC veins). Lands the `json_format` output mode that phase 32 EXPLICITLY DEFERRED (ADR-0078 §2.2; `crates/envoy-config/src/bootstrap.rs:682,695`) and ADR-0089 named "a fine FUTURE Observability pick": Envoy's `SubstitutionFormatString` is a oneof carrying — alongside the existing `text_format_source` (phase 32) — `json_format`, a `google.protobuf.Struct` map of string keys → per-value command-operator format strings, emitting ONE JSON object per request instead of a text line. **`json_format` chosen as the cheapest-strong next leaf** — a new OUTPUT ENVELOPE over an engine + record + sink + harness that ALL already exist: it slots additively beside `text_format_source` on `SubstitutionFormatString` (`bootstrap.rs:697`), compiles each map value through the EXISTING `envoy_accesslog::parse_format`/`CompiledFormat` engine, and assembles a single-line JSON object; reuses the existing `AccessLogRecord` (16 fields incl. `dynamic_metadata`), the verbatim-`FileSink` (phase-32 format-owns-its-newline refactor), and the `Driver::Http1WithAccessLog`/`AccessLogByteExactProbe` differential harness (the `0040`/`0041`/`0042` byte-exact log-line scrape). **NO new connection-context plumbing, NO new request attribute, NO metadata store, NO producer chain, NO new `HttpFilterInstance` variant / crate / dependency.** The connection-context RBAC conditions (need socket-address/SNI plumbing) and the remaining non-string `ValueMatcher` variants (need a shared non-string-Value generalization) were rejected as heavier-per-LoC; `typed_json_format` (typed unquoted values) was rejected as the strictly-larger follow-up over the same encoder. **Differential is byte-exact + DETERMINISTIC + LOCALLY observable** — the probe drives a fixed request → a fixed `AccessLogRecord` → a byte-identical JSON line on both proxies (an access-log scrape on a normal request/response, NO file-watch/reload trigger, NOT Linux-CI-only); the load-bearing richness is the JSON envelope (the deterministic operators fixture `0040` proved byte-exact as a TEXT line must now render byte-exact inside a JSON OBJECT — forcing replication of Envoy's exact key ordering, value string-quoting, JSON escaping, compact separators, and the per-object terminator). D-items: widen `SubstitutionFormatString` to a `{text_format_source | json_format}` oneof + an exactly-one validator (the `DataSource` "exactly one of {filename, inline_string}" cardinality pattern is the template); model `json_format` as an insertion-order-preserving map (projected `Vec<(String,String)>` — key ordering is byte-load-bearing); a JSON encoder in `envoy-accesslog` (compile-each-value via `parse_format` + assemble-`{…}` + JSON-escape) slotting into the existing `FileSink.format` field with `emit` UNCHANGED; per-value config validation via the EXISTING `parse_format` → the EXISTING `ConfigError::InvalidAccessLogFormat` (projected NO new variant); fixture `0046-accesslog-json-format` (an H1 listener with a `json_format` file logger over a curated DETERMINISTIC operator set, whole-line byte-exact) + an in-process backstop (key-order preservation, JSON escaping, absent-value rendering, the text-path-unchanged round-trip) + a `parse_bootstrap` seed (no new fuzz target — the value strings reuse `parse_format`/`accesslog_format_parse`) + a BEHAVIOR_CONTRACT "Access log field mapping" json_format subsection. Load-bearing invariant: all 45 pre-existing fixtures `0001`-`0045` stay green simultaneously (the JSON encoder is a sibling the text/default loggers never enter; `0012` default + `0040` text-custom + `0041`/`0042` `%DYNAMIC_METADATA%` byte-identical). minimum-viable scope per ADR-0091 (defers `typed_json_format`; nested/non-string values; `json_format_options`(sort)/`omit_empty_values`/`content_type`; the deprecated `text_format`/`format` scalars; any new record field/operator; gRPC ALS / OTLP / tracing / stats sinks / tap). The core differential risk — the EXACT JSON shape: above all **key ordering** (insertion-order `Vec` vs sorted `BTreeMap`) and **string-vs-typed value emission** (does plain `json_format` quote `%RESPONSE_CODE%`→`"200"` or type-infer?), plus the separators / per-object `\n` / absent-value rendering / escaping / config-validity dispositions — is de-risked by the state-2 §6.2 LOCAL reconnaissance against `envoyproxy/envoy:v1.33.0`; **ADR-0092 reserved** for the §6.2 reconciliation, **ADR-0093 reserved** for the §6.1 split (projected NOT to fire — ~400–700 LoC / ~6–9 tasks, smaller than phase 32 since the engine + config + harness already exist; the natural seam if forced is `38.1` config schema + oneof + validator / `38.2` JSON encoder + `FileSink` wiring + fixture 0046 + close). The SPEC was spec-reviewed by a fresh subagent → APPROVED (0 Critical / 0 Important; 3 Minor folded). The state-2 PLAN-write is the next session [`superpowers:writing-plans`], which runs the §6.2 reconnaissance and fires ADR-0092 |
| 39 | Observability family: nested / non-string `json_format` values — make the phase-38 `json_format` encoder RECURSIVE (nested JSON objects + lists as `Struct` values, the way Envoy's `SubstitutionFormatString.json_format` is a full `google.protobuf.Struct`) over the EXISTING `CompiledJsonFormat` encoder + `AccessLogRecord` + `FileSink` + harness (ZERO new connection plumbing / request attribute / operator / `HttpFilterInstance` variant) + fixture 0047 | 06 32 38 | done | — | (DONE — CLOSED 2026-06-27 at the phase-39 state-6 close-out: **REVIEW.md APPROVE [0 Critical / 0 Important / 2 new Minor M39-1/M39-2]** at the state-5 code-review [commit `06e9396`]; the **§7.5 phase-done gate (a)-(f) GREEN at the AUTHORITATIVE Linux CI run `28295620744` @ `fffc297`** [the state-3 implementation commit, `completed/success` — the full differential suite `0001`-`0047` incl. the new `0047-accesslog-json-nested` cross-proxy byte-exact nested JSON line `{"arequest":{"aaa":200,"method":"GET","zpath":"/"},"blist":["GET",200,null],"mtop":"code-200","zouter":"HTTP/1.1"}` + the `0046` flat regression witness byte-identical + h2spec ≥95% [NO HTTP/2 codec change] + the `parse_bootstrap`/`accesslog_format_parse` fuzz targets 0 crashes with the new `json_format_nested.yaml` seed [NO new target, `ci.yml` unchanged] + build/clippy/fmt/test/deny clean]; the implementation commit range `4dbde13`..`f2e4767` [T1 recursive `JsonFormatValue` config enum (`#[serde(untagged)]` {Null,Bool,Format,Array,Object}; numeric literal → boot-reject) + T2 recursive per-leaf validator / T3 accesslog-side `JsonValueInput` mirror (NO `envoy-accesslog`→`envoy-config` dep) + recursive `CompiledJsonValue` / T4 recursive byte-exact `render` (the phase-38 leaf helpers `encode_json_value`/`encode_single_op`/`json_escape_into` reused VERBATIM; folds M38-3/M38-4) / T5 HCM `JsonFormatValue`→`JsonValueInput` bridge / T6 fixture `0047` byte-exact differential / T7 `json_format_nested.yaml` `parse_bootstrap` seed + BEHAVIOR_CONTRACT] + the doc-only state markers `8270a97` (state-2 PLAN-write) / `fffc297` (state-3 STATE-advance) / `025d17f` (state-4) / `06e9396` (state-5) + this state-6 close; **§A-§H ground truth locked by ADR-0094** [per-level key sorting SORTED by UTF-8 bytes (`BTreeMap` each level); list element order = CONFIG order (`Vec`, NOT sorted); at-depth type inference = the SAME phase-38 per-leaf rule; non-string scalar leaves NATIVE-TYPED (`bool`→`true`/`false`, `null`→`null`), NUMERIC literals DEFERRED as CF-39-1 (protobuf-`double` rabbit hole); compact separators + ONE trailing `\n`; empty `{}`/`[]` preserved; malformed-nested-op boot-fatal via the EXISTING `ConfigError::InvalidAccessLogFormat`; exactly-one-of + empty-top-map UNCHANGED]; the **§6.1 split did NOT fire** [~7 tasks / ~350-550 LoC; **ADR-0095 reserved-but-UNFIRED** — stays available for a future split trigger]; NO new crate / dependency / fuzz-target / `ConfigError` variant; `#![forbid(unsafe_code)]` holds; no `Cargo.toml`/`Cargo.lock` change. NEW carry-forwards **M39-1** [mirror-enum sync doc-pointer `JsonValueInput`↔`JsonFormatValue`] + **M39-2** [unbounded `json_format` recursion depth at boot — note if a future phase accepts `json_format` from a DYNAMIC xDS/RDS source] + **CF-39-1** [numeric literal `json_format` leaves boot-rejected; `bool`/`null` supported] — all non-blocking. SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/39-accesslog-json-nested-values/`. **ROADMAP rows `00`-`39` ALL `done`** (`39` is the highest defined row → STATE.md is now "awaiting next planning"). ORIGINAL state-1 brainstorm scope below — IN-PROGRESS — state-1 brainstorm COMPLETE 2026-06-27: SPEC authored + spec-reviewed by a fresh subagent → **APPROVED [0 Critical / 0 Important; 3 Minor M-1/M-2/M-3 folded]**; scope locked by **ADR-0093** at the state-1 brainstorm) the THIRD Observability-family encoder row — the strictly-larger deepening of the SAME `json_format` encoder phase 38 shipped. Phase 38 (ADR-0091/0092) landed `json_format` as a FLAT `BTreeMap<String,String>` (`crates/envoy-config/src/bootstrap.rs:708`) → a flat sorted JSON object with per-leaf type inference (number/string/null) all locked byte-exact vs v1.33.0. But Envoy's `json_format` is a recursive `Struct`: a value may itself be a NESTED OBJECT (key→value map) or a LIST, to arbitrary depth — phase 38 EXPLICITLY DEFERRED this ("nested / non-string `Struct` values (lists, nested objects)", ADR-0092 Scope-correction). **`nested json_format` chosen as the cheapest-strong next leaf** — a recursion refactor of ONE existing config value type + ONE existing output encoder over an engine/record/sink/harness that ALL already exist: **ZERO new connection-context plumbing, ZERO new request attribute, NO metadata store, NO producer chain, NO new operator, NO new `HttpFilterInstance` variant / crate / dependency**, and projected NO new `ConfigError` variant. The only genuinely-new code is the recursion: a recursive config enum (`JsonFormatValue = Format(String) | Object(BTreeMap<…>) | Array(Vec<…>)`, serde `#[serde(untagged)]`) + a recursive compiled enum (`CompiledJsonValue = Leaf | Object | Array`) + a recursive `render` REUSING the phase-38 leaf helpers (`encode_json_value`/`encode_single_op`/`json_escape_into`) VERBATIM. The connection-context RBAC conditions (need socket-address/SNI plumbing) and the non-string `ValueMatcher` variants (need a shared non-string-Value generalization) were rejected as heavier-per-LoC; `omit_empty_values`/`json_format_options.sort_properties`/`content_type` rejected as cheaper-but-weaker knobs (own future homes). **Differential is byte-exact + DETERMINISTIC + LOCALLY observable** — the probe drives a fixed request → a fixed `AccessLogRecord` → a byte-identical NESTED JSON line on both proxies (an access-log scrape on a normal request/response, NO file-watch/reload trigger, NOT Linux-CI-only); the load-bearing richness is the RECURSION (the deterministic operators phase 38 proved byte-exact in a FLAT object must now render byte-exact inside NESTED objects + lists — forcing replication of Envoy's exact recursive `Struct` shape: keys SORTED at EACH object level, list order = config order, the SAME per-leaf type inference at depth, compact separators, ONE trailing `\n` on the whole top-level object). D-items: the recursive `json_format` config model (`bootstrap.rs:704-708` value type `String`→`JsonFormatValue`, untagged serde); the recursive `CompiledJsonFormat`/`CompiledJsonValue` encoder (`crates/envoy-accesslog/src/json_format.rs`, leaf helpers reused); the per-leaf-recursive config validator (`bootstrap.rs:4371` region, recurse + `parse_format` per leaf → EXISTING `ConfigError::InvalidAccessLogFormat`); `FileSink`/`LogFormat`/`hcm.rs:1269` UNCHANGED except `from_map`'s arg type; fixture `0047-accesslog-json-nested` (H1 `direct_response` listener + a nested-object + list json_format over a curated DETERMINISTIC operator set, whole-line byte-exact) + an in-process backstop (nested objects, lists, per-level key-sorting, list-order preservation, at-depth type inference, JSON escaping at depth, empty `{}`/`[]`, depth ≥3, flat-map round-trip byte-unchanged) + a `parse_bootstrap` nested seed (NO new fuzz target) + a BEHAVIOR_CONTRACT "Access log field mapping" recursive-shape update. Load-bearing invariant: fixture `0046` (flat) stays byte-identical after the recursion refactor + all `0001`-`0045` stay green simultaneously (the recursion is internal to the JSON encoder; text/default loggers never enter it). minimum-viable scope per ADR-0093 (defers `json_format_options.sort_properties`; `omit_empty_values`; `content_type`; non-string SCALAR typed leaves beyond fixture/backstop need; the deprecated `text_format`/`format` scalars; any new record field/operator; gRPC ALS / OTLP / tracing / stats sinks / tap). The core differential risk — the recursive wire shape (per-LEVEL key sorting, list-order preservation, at-depth type inference) + the non-string SCALAR leaf disposition (a literal YAML number/bool/null `Struct` value — emitted typed, stringified, or rejected?) + degenerate empty-nested dispositions — is de-risked by the state-2 §6.2 LOCAL reconnaissance against `envoyproxy/envoy:v1.33.0`; **ADR-0094 reserved** for the §6.2 reconciliation, **ADR-0095 reserved** for the §6.1 split (projected NOT to fire — ~350-600 LoC / ~6-8 tasks, SMALLER than phase 38 since the JSON envelope + type classifier + config oneof all already exist; the natural seam if forced is `39.1` recursive config model + serde + validator + parse-layer tests / `39.2` recursive encoder + fixture 0047 + BEHAVIOR_CONTRACT + seed + close). SPEC at `docs/envoy-rust/phases/39-accesslog-json-nested-values/`. The state-2 PLAN-write is the next session [`superpowers:writing-plans`], which runs the §6.2 reconnaissance and fires ADR-0094 |
| 40 | Observability family: `omit_empty_values` knob on `SubstitutionFormatString` — when `true`, the access logger OMITS log entries whose substituted value is "empty" (instead of emitting the placeholder); a config `bool` + a drop-empty render-pass over the EXISTING phase-38/39 recursive `json_format` encoder + `AccessLogRecord` + `FileSink` + harness (ZERO new connection plumbing / request attribute / operator / `HttpFilterInstance` variant) + fixture 0048 | 06 32 38 39 | done | — | (DONE — CLOSED 2026-06-27 at the phase-40 state-6 close-out: **REVIEW.md APPROVE [0 Critical / 0 Important / 1 new Minor M40-1]** at the state-5 code-review [commit `b38b9b5`]; the **§7.5 phase-done gate (a)-(f) GREEN at the AUTHORITATIVE Linux CI run `28297297375` @ `c4f95b1`** [the state-3 implementation commit, `completed/success` — the full differential suite `0001`-`0048` incl. the new `0048-accesslog-omit-empty` cross-proxy byte-exact sentinel-swap line `{"method":"GET","proto":"HTTP/1.1","single_up":null,"up":"up=","xff":"x="}` + all `0001`-`0047` green simultaneously [default-off byte-preservation, incl. `0046` flat + `0047` nested] + h2spec ≥95% [NO HTTP/2 codec change] + the `parse_bootstrap`/`accesslog_format_parse` fuzz targets 0 crashes with the new `omit_empty_values.yaml` seed [NO new target, `ci.yml` unchanged] + build/clippy/fmt/test/deny clean]; the implementation commit range `86971ce`..`0114afa` [T1 `omit_empty_values: bool` config field / T2 thread `omit_empty` into `render_value_segments`→`render_op` (the four `.unwrap_or("-")` → `.unwrap_or(if omit {""} else {"-"})`) + text `CompiledFormat` / T3 thread into the recursive `json_format` render (`encode_single_op` UNCHANGED) / T4 HCM wiring / T5 fixture `0048` differential / T6 `omit_empty_values.yaml` `parse_bootstrap` seed + BEHAVIOR_CONTRACT] + the doc-only state markers `cccaaaf` (state-2 PLAN-write) / `c4f95b1` (state-3 STATE-advance) / `cba5f4b` (state-4) / `b38b9b5` (state-5) + this state-6 close; **§A-§E ground truth locked by ADR-0096** [the §6.2 recon FIRES — `omit_empty_values` does NOT drop keys (the SPEC's "drop-empty" projection VOID); it SWAPS the absent-operator `-` sentinel for the EMPTY STRING `""` in the command-operator MULTI-SEGMENT render, for BOTH `text_format` + `json_format`, recursively; single-operator-TYPED values (→`null`) UNAFFECTED; plain `bool`, NO new `ConfigError` variant]; the **§6.1 split did NOT fire** [~6 tasks / ~120-250 LoC; **ADR-0097 reserved-but-UNFIRED** — stays available for a future split trigger]; NO new crate / dependency / fuzz-target / `ConfigError` variant; `#![forbid(unsafe_code)]` holds; no `Cargo.toml`/`Cargo.lock` change. NEW carry-forward **M40-1** [the fixture-`0048` README/expectations overstate `0047` as the flag-off multi-segment `-`-sentinel cross-proxy witness — doc-accuracy only; the in-process backstop covers it] — non-blocking. SPEC + PLAN + PROGRESS + REVIEW at `docs/envoy-rust/phases/40-accesslog-omit-empty-values/`. **ROADMAP rows `00`-`40` ALL `done`** (`40` is the highest defined row → STATE.md is now "awaiting next planning"). ORIGINAL state-1 brainstorm scope below — IN-PROGRESS — state-1 brainstorm COMPLETE 2026-06-27: SPEC authored + spec-reviewed by a fresh subagent → **APPROVED [0 Critical / 0 Important; the `omit_empty_values` field existence CONFIRMED empirically-observed on the live v1.33.0 oneof roster per ADR-0092 §C — only its semantics are §6.2-VERIFY; 1 strengthening citation folded]**; scope locked by **ADR-0095** at the state-1 brainstorm) the FOURTH Observability access-log-encoder row — the cheapest-strong `SubstitutionFormatString` knob remaining over the now-recursive `json_format` encoder. Envoy's `SubstitutionFormatString` carries (alongside `text_format_source`/`json_format`) a scalar `bool omit_empty_values` (observed live at v1.33.0, ADR-0092 §C) which DROPS access-log entries whose substituted value is empty. **`omit_empty_values` chosen as the cheapest-strong next leaf** — a new RENDER OPTION over an encoder + record + sink + harness that ALL already exist: a `omit_empty_values: bool` field (`#[serde(default)]`→`false`) on `SubstitutionFormatString` (`bootstrap.rs:704-709`) + a drop-empty pass in the `json_format` render (when `true`, drop each entry whose value is "empty"; when `false`, the phase-38/39 render VERBATIM). **NO new connection plumbing, NO new request attribute, NO new operator, NO metadata store, NO new `HttpFilterInstance` variant / crate / dependency.** `sort_properties` (weak — default already sorted, ADR-0092 §A), `content_type` (not log-line-observable), CF-39-1 numeric literals (protobuf-`double` rabbit hole), and the gRPC-ALS/OTLP Observability surfaces (need new sink infrastructure) were rejected as weaker-per-LoC or heavier. **Differential is byte-exact + DETERMINISTIC + LOCALLY observable** — the probe drives a fixed request → a fixed `AccessLogRecord` → a byte-identical access-log line (empty-valued keys DROPPED) on both proxies (an access-log scrape, NO file-watch/reload trigger, NOT Linux-CI-only); the load-bearing richness is the drop-empty semantics (envoy-rust must drop EXACTLY the keys Envoy drops, per the §6.2 "empty" predicate, and re-assemble the survivors with the same compact `{…}` + trailing `\n`). D-items: add `omit_empty_values: bool` to `SubstitutionFormatString` (`deny_unknown_fields` retained, the exactly-one validator UNCHANGED); thread the flag into the compiled format (projected a field on `CompiledJsonFormat`/`LogFormat` set at construction); a drop-empty branch in the `json_format` render (the phase-38/39 leaf-render UNCHANGED — a value is rendered, then KEPT or DROPPED); fixture `0048-accesslog-omit-empty` (an H1 `direct_response` listener + a `json_format` mixing present [`%REQ(:METHOD)%`/`%PROTOCOL%`/`%RESPONSE_CODE%`] + empty/absent [`%UPSTREAM_HOST%`→absent on direct_response] keys with `omit_empty_values: true` → the empty keys dropped, whole-line byte-exact, + a flag-off all-keys control sub-case) + an in-process backstop (the empty-predicate edges, the recursive disposition, the default-off round-trip) + a `parse_bootstrap` `omit_empty_values` seed (no new fuzz target) + a BEHAVIOR_CONTRACT "Access log field mapping" `omit_empty_values` subsection. Load-bearing invariant: `omit_empty_values` defaults `false` → all 47 pre-existing fixtures `0001`-`0047` (incl. `0012` default + `0040` text-custom + `0046` flat-JSON + `0047` nested-JSON) stay green simultaneously (the flag-off render is byte-identical to phases 38/39). minimum-viable scope per ADR-0095 (defers `sort_properties`; `content_type`; CF-39-1; the deprecated `text_format`/`format` scalars; new record field/operator; gRPC ALS / OTLP / tracing / stats sinks / tap; and — if §6.2 shows it heavy — the `text_format` interaction). The core differential risk (the EXACT "empty" predicate — null vs the `-` sentinel vs empty string, for single-op-typed vs mixed/literal values + the recursive-vs-top-level disposition + the text-format interaction + the all-empty `{}` disposition) is de-risked by the state-2 §6.2 LOCAL reconnaissance against `envoyproxy/envoy:v1.33.0`; **ADR-0096 reserved** for the §6.2 reconciliation, **ADR-0097 reserved** for the §6.1 split (projected NOT to fire — ~150-350 LoC / ~5-7 tasks, smaller than phases 38/39; the natural seam if forced is `40.1` config field + flag plumbing / `40.2` drop-empty render + fixture 0048 + close). The SPEC was spec-reviewed by a fresh subagent → APPROVED (0 Critical / 0 Important; 1 strengthening citation folded). The state-2 PLAN-write is the next session [`superpowers:writing-plans`], which runs the §6.2 reconnaissance and fires ADR-0096 |
| 41 | Observability family: the `%REQ_WITHOUT_QUERY(NAME[?ALT])[:N]%` access-log command operator — like `%REQ%` but with the `?query` string stripped from the resolved value (designed for `%REQ_WITHOUT_QUERY(:PATH)%` → the request path without its query); a new `Op` variant + parse keyword + render arm over the EXISTING command-operator engine + encoders + harness (ZERO new record field / connection plumbing / `HttpFilterInstance` variant) + fixture 0049 | 06 32 38 | in-progress | — | (IN-PROGRESS — state-1 brainstorm COMPLETE 2026-06-27: SPEC authored + spec-reviewed by a fresh subagent → **APPROVED [0 Critical / 0 Important; 2 Minor M-1 (encode_single_op line citation) / M-2 (ADR reclaim note) folded]**; scope locked by **ADR-0097** at the state-1 brainstorm) the FIFTH Observability access-log row — a NEW OPERATOR after the encoder-knob vein (text/json/recursive-json/omit_empty_values) mined out. Envoy's access-log vocabulary includes `REQ_WITHOUT_QUERY` — identical grammar to `REQ` (`(NAME[?ALT])[:N]`) but the substituted value has everything from the first `?` removed. **`%REQ_WITHOUT_QUERY%` chosen as the cheapest-strong next leaf** — a new `Op::ReqWithoutQuery { name, alt, truncate }` (mirroring `Op::Req`, `command_operator.rs:40`) parsed by the EXISTING `parse_header_op` + a new keyword dispatch (`:231`), resolved by the EXISTING `resolve_req` (`:549`) with the `?ALT` fallback, then the query stripped (substring before the first `?` — the trivial `strip_query` from `rbac.rs:96`, re-implemented in-crate, NO cross-crate dep) and the `:N` truncation applied; plus a `ReqWithoutQuery` arm on `encode_single_op` (`json_format.rs:224`, mirroring the `Req` arm). The value it strips (`record.path` = the `:path`/request-target, which INCLUDES the query — `record.rs:36-38`) is ALREADY in the record. **NO new `AccessLogRecord` field, NO new connection plumbing, NO new request attribute, NO new crate / dependency / `ConfigError` variant.** `sort_properties` (risks an empty/no-op differential — default already sorted), the RBAC connection-context conditions (need socket-address plumbing AND produce an environment-fragile IP differential), the gRPC-ALS/OTLP sinks (new transport infra), and CF-39-1 (protobuf-`double` rabbit hole) were rejected as weaker-per-LoC, fragile, or heavier. **Differential is byte-exact + DETERMINISTIC + LOCALLY observable** — the probe drives `GET /p?x=1&y=2` → `%REQ_WITHOUT_QUERY(:PATH)%`→`/p` beside `%REQ(:PATH)%`→`/p?x=1&y=2`, byte-identical cross-proxy (an access-log scrape, NO file-watch/reload trigger, NOT Linux-CI-only); the load-bearing richness is the query-strip semantics + its interaction with `:N` truncation + `?ALT` fallback. D-items: `Op::ReqWithoutQuery` + the `"REQ_WITHOUT_QUERY"` keyword dispatch + the resolve→strip→truncate render + the `encode_single_op` arm + a one-line in-crate query-strip; fixture `0049-accesslog-req-without-query` (an H1 listener with a format containing `%REQ_WITHOUT_QUERY(:PATH)%` beside `%REQ(:PATH)%`, whole-line byte-exact) + an in-process backstop (strip on a `?`-value; no-`?` no-op; `:N` truncate AFTER strip; `?ALT`-then-strip; absent → sentinel/null; text + json single-op-typed) + an `accesslog_format_parse`/`parse_bootstrap` seed (no new fuzz target) + a BEHAVIOR_CONTRACT note. Load-bearing invariant: `%REQ_WITHOUT_QUERY%` is a NEW operator no existing fixture uses → all 48 pre-existing fixtures `0001`-`0048` stay green simultaneously (existing operator render paths byte-preserved). minimum-viable scope per ADR-0097 (defers `%RESP_WITHOUT_QUERY%` [N/A — no response query]; `sort_properties`/`content_type`; CF-39-1; the other unimplemented operators [each needs a new record field]; gRPC ALS / OTLP / tracing / stats sinks / tap). The core differential risk (the EXACT strip point + the strip-vs-truncate ORDER + the header allow-list scope + the `?ALT` interaction + the absent/json-typed disposition) is de-risked by the state-2 §6.2 LOCAL reconnaissance against `envoyproxy/envoy:v1.33.0`; **ADR-0098 reserved** for the §6.2 reconciliation, **ADR-0099 reserved** for the §6.1 split (projected NOT to fire — ~120-250 LoC / ~5-6 tasks; the natural seam if forced is `41.1` parse + Op variant / `41.2` render + strip + fixture 0049 + close). The SPEC was spec-reviewed by a fresh subagent → APPROVED (0 Critical / 0 Important; 2 Minor folded). The state-2 PLAN-write is the next session [`superpowers:writing-plans`], which runs the §6.2 reconnaissance and fires ADR-0098 |

### Runtime + hot restart family

### WASM host family

Own multi-phase sub-project; ABI, engine binding, proxy-wasm conformance.

### Deprecated / edge features

Explicit out-of-scope ADRs unless later re-opened.
