# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase

**id:** `24` — `24-http-filter-csrf` (the HTTP-filters family's SIXTH concrete phase — `envoy.filters.http.csrf`; the SECOND consumer of the phase-23 per-route `typed_per_filter_config` infrastructure), lifecycle **state-1-complete / state-2-next**. ROADMAP rows `00`–`23` are all `done`; row `24` is `planned` (it flips to `in-progress` at the next session's state-2 PLAN-write per invariant 4.1.3).
**slug:** `24-http-filter-csrf`
**directory:** `docs/envoy-rust/phases/24-http-filter-csrf/` — carries **`SPEC.md` only** (the state-1 brainstorm output; `PLAN.md` + `PROGRESS.md` + `REVIEW.md` do NOT exist yet — the state-2 PLAN-write writes `PLAN.md` next session). The five most-recently-closed phase directories — 23 `23-http-filter-cors`, 22 `22-http-filter-jwt-authn`, 21 `21-xds-file-based-eds`, 20, 19 — each remain CLOSED carrying all 4 artifacts.

**status:** **PHASE 24 (`24-http-filter-csrf`, the `csrf` HTTP filter — the SECOND per-route `typed_per_filter_config` consumer) STATE-1 NEW-PHASE BRAINSTORM COMPLETE at THIS commit** (lifecycle state-1-complete / state-2-next). This commit is the **phase-24 state-1 NEW-PHASE brainstorm** per `BOOTSTRAP_PROMPT.md` §5 state 0→1 + `SKILL_ROUTING.md` + the new-feature-family-phase brainstorm precedents (phase-12…23: a single state-0/1 brainstorm collapses BOTH states — adds the ROADMAP row AND creates the phase directory + `SPEC.md` + a scoping ADR in ONE docs-only commit). It (a) picks the next phase — **`24 — envoy.filters.http.csrf`** (the HTTP-filters family, the richest remaining §9 vein with ≈11 filters undone; `csrf` chosen as the SECOND per-route `typed_per_filter_config` consumer — the §6.3 anti-pattern demands a second consumer to prove the phase-23 infra generalizes additively beyond its single `Cors` variant; chosen over `buffer` [body-buffering machinery — higher risk], per-route overrides of existing filters [lower new-surface value; the route>vhost cascade is deferred], `compression` [gzip dep + non-byte-exact bodies], `ext_authz`/`ext_proc` [external service / gRPC-blocked], the load-balancing family [distribution/hash-exact testing]); (b) adds ROADMAP row `24` (`status: planned`) beneath the "HTTP filters family" §9 heading (the SIXTH concrete row there, after 09/10/11/22/23); (c) creates `docs/envoy-rust/phases/24-http-filter-csrf/` + writes `SPEC.md`; (d) lands **ADR-0060** (the scoping ADR — the pick + the minimum-viable scope + the alternatives-rejected analysis; see the `### Phase-24 state-1 brainstorm` Notes subsection); (e) advances this STATE Active phase from "AWAITING NEXT PLANNING" to `24` state-1-complete / state-2-next. **Key scoping finding (ADR-0060; SPEC §0; read-only recon at HEAD `3b7cf1384`): the cross-cutting architectural risk that made phase 23 borderline is GONE** — phase 23 already landed the HCM route-early-resolution + the `apply_route_config` per-route threading (`resolve_route` at `crates/envoy-http1/src/hcm.rs:1195`; `FilterPipeline::apply_route_config` `crates/envoy-filter/src/pipeline.rs:66` → `instance.rs:167` → `cors.rs:115`) + the `PerRouteConfigForAbsentFilter` validator (`crates/envoy-config/src/lib.rs:531`), so **phase 24 makes NO shared-HCM change** — it is a near-pure additive plug-in (an enum arm + a filter module). **Scope (ADR-0060; SPEC §3):** a second `PerFilterConfig` variant `Csrf(CsrfPolicy)` (`bootstrap.rs:787`) + a near-empty filter-chain `Csrf` entry on `HttpFilterTypedConfig` (`:741`) + a `CsrfPolicy` schema (`additional_origins: Vec<StringMatcher>` reusing the 04.x `StringMatcher` + `filter_enabled` honored ONLY at deterministic 0%/100% per the phase-11 fault precedent); `CsrfFilter` (the EIGHTH `HttpFilterInstance` variant; the FIRST decode-side-only per-route filter — for modify methods compute the source origin [`Origin`, fallback `Referer`] vs the target origin [`Host`/`:authority`], valid if equal or matches `additional_origins` → Continue, else/missing-source → `Decision::StopAndSend` 403 + the Envoy CSRF failure body decorated by the existing H1/phase-11-H2 filter-synth helpers; per-route policy threaded via the existing `apply_route_config` hook); conditional `http.<stat_prefix>.csrf.{request_valid,request_invalid,missing_source_origin}` stats (namespace §6.2-verified); fixture `0032-http-filter-csrf` (H1 proxying to a real upstream cluster per ADR-0058 L6; 5 deterministic header-only probes — same-origin-POST→200 / evil-origin-POST→403 / additional-allowed-POST→200 / safe-GET→200 / no-source-POST→403); an in-process backstop; a new `parse_bootstrap` fuzz seed (NO new fuzz target — CSRF reuses `StringMatcher`); BEHAVIOR_CONTRACT csrf rows. DEFER per ADR-0060 / SPEC §4: `filter_enabled` runtime fractional gating + `shadow_enabled` shadow mode, vhost-level config + the route>vhost most-specific-config cascade, `typed_per_filter_config` for filters OTHER than cors/csrf, streaming/trailers + gRPC-Web CSRF. **§6.1 split-gate: projected single-phase (~900–1300 LoC / ~8–11 tasks — comfortably inside the gate and LIGHTER than phase 23 since the cross-cutting HCM change is already done); the split valve is held in nominal reserve as `24.1` [schema + `PerFilterConfig::Csrf`/`HttpFilterInstance::Csrf` wiring + validator] / `24.2` [the `CsrfFilter` + stats + fixture 0032 + backstop + close]; the split ADR would be ADR-0062 but is very unlikely to fire.** **DECISIONS.md ledger head: ADR-0060** (count 61; next available **ADR-0061**, reserved for the §6.2 reconciliation [most-likely trigger: the config shape (chain-vs-route `filter_enabled`) or the modify-method set]; **ADR-0062** reserved for the split). **ADR-0014 REMAINS IN FORCE** (the YAML-native shim — CSRF does not engage the xDS protos); **ADR-0028 REMAINS OPEN** — phase 24 does not engage it. **THIS COMMIT touches exactly 5 docs files** (the brainstorm set + STATE_HISTORY for the ADR-0035 relocations): (1) **CREATE** `docs/envoy-rust/phases/24-http-filter-csrf/SPEC.md`; (2) **MODIFY** `docs/envoy-rust/ROADMAP.md` (add row `24` `status: planned`); (3) **MODIFY** `docs/envoy-rust/DECISIONS.md` (append **ADR-0060**); (4) **MODIFY** `docs/envoy-rust/STATE.md` (this file — top-pointer advance + `### Phase-24 state-1 brainstorm` Notes subsection; the prior "AWAITING NEXT PLANNING" blocks RELOCATED to STATE_HISTORY.md); (5) **MODIFY** `docs/envoy-rust/STATE_HISTORY.md` (the ADR-0035 relocations of the superseded phase-23-close-out top-section blocks). No production code / test / fixture / PLAN / PROGRESS / REVIEW / BEHAVIOR_CONTRACT / Cargo change; no `unsafe`. The brainstorm commit is docs-only so the CI run is vacuous-green (the project's differential evidence remains the phase-23 state-4 CI anchor `27317400787` at code-HEAD `ff3721fd1`). Per §5.1 (one state per session) this brainstorm session EXITS after this commit; the NEXT session writes `PLAN.md` (state 2 — `superpowers:writing-plans` scoped to the phase-24 SPEC, with the §6.2 empirical verification against `envoyproxy/envoy:v1.33.0` running LOCALLY).

> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases + the active phase's prior sub-state pointers, incl. the phase-23 state-1 brainstorm pointer) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill

Per `BOOTSTRAP_PROMPT.md` §5 state 2 + `SKILL_ROUTING.md` (state 2 — SPEC.md exists, PLAN.md does not): the next session runs **`superpowers:writing-plans`** to author `docs/envoy-rust/phases/24-http-filter-csrf/PLAN.md` (+ the `PROGRESS.md` skeleton + the Task-1 preamble, in one standalone pre-Task-1 commit per the 06.2→23 cadence). The PLAN-write (a) runs the **SPEC §6.2 empirical verification LOCALLY** against `envoyproxy/envoy:v1.33.0` (CSRF has no virtiofs/inotify dependency) — resolving the config shape FIRST (gates D1: the chain-vs-route `filter_enabled` requirement), then the modify-method set / source-target origin computation + `Referer` fallback / 403 failure-body bytes + `content-type` / stat namespace + semantics / `filter_enabled` disposition; (b) lands inline **ADR-0061** if any item diverges materially (the config shape + the modify-method set are the most likely triggers); (c) evaluates the §6.1 split gate (**single-phase recommended** — the cross-cutting HCM change already landed at phase 23; split reserved as ADR-0062 but very unlikely); (d) flips **ROADMAP row `24` `planned → in-progress`** (invariant 4.1.3) + advances this STATE to state-2-complete / state-3-next. Per §5.1 (one state per session) the PLAN-write is the NEXT session's single state; the state-3 execution arc (`superpowers:subagent-driven-development`, SERIAL dispatch) is the session after.

> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases + the active phase's prior sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit

**Phase-24 state-1 NEW-PHASE brainstorm commit (THIS commit):** picks + scopes phase `24` (`envoy.filters.http.csrf` — the SECOND per-route `typed_per_filter_config` consumer), creating `docs/envoy-rust/phases/24-http-filter-csrf/SPEC.md` + appending **ADR-0060** (the scoping ADR) + adding **ROADMAP row `24` `status: planned`** beneath the "HTTP filters family" §9 heading (invariant 4.1.2 — a new row enters `planned`; no existing row flips) + advancing **STATE → phase-24 active / state-1-complete / state-2-next** (rewrites `## Active phase` + `## Next expected skill` to point at the state-2 PLAN-write `superpowers:writing-plans`), relocating the now-superseded phase-23-close-out top-section narratives verbatim to `STATE_HISTORY.md` (ADR-0035 / §4.1 inv. 9). **No code change** (doc-only: CREATE `SPEC.md`; MODIFY `ROADMAP.md` + `DECISIONS.md` + `STATE.md` + `STATE_HISTORY.md`). **DECISIONS.md ledger head: ADR-0060** (count 61; next available **ADR-0061**, reserved for the §6.2 reconciliation; **ADR-0062** reserved for the §6.1 split). ADR-0014 in force; ADR-0028 open. The brainstorm commit is docs-only so there is NO CI obligation (the project's differential evidence remains the phase-23 state-4 CI anchor `27317400787` at code-HEAD `ff3721fd1`). Per §5.1 the NEXT session is the phase-24 state-2 PLAN-write (see `## Next expected skill`).

> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases + the active phase's prior sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated

2026-06-11 (phase-24 **state-1 NEW-PHASE brainstorm** — `superpowers:brainstorming`. Picked + scoped phase `24` = **`envoy.filters.http.csrf`** [the HTTP-filter-family's SIXTH concrete phase; the SECOND per-route `typed_per_filter_config` consumer, proving the phase-23 infra generalizes additively per the §6.3 anti-pattern]. Created `docs/envoy-rust/phases/24-http-filter-csrf/SPEC.md`; appended **ADR-0060** [scoping]; added ROADMAP row `24` `status: planned`; advanced STATE → phase-24 active / state-1-complete / state-2-next [rewrote `## Active phase` + `## Next expected skill` to point at the state-2 PLAN-write `superpowers:writing-plans`]; relocated the superseded phase-23-close-out top-section narratives verbatim to `STATE_HISTORY.md` per ADR-0035. Minimum-viable scope: decode-side modify-method origin guard [`Origin`/`Referer` vs `Host` + `additional_origins` via `StringMatcher`], 403 on mismatch, `filter_enabled` deterministic-only; defers `shadow_enabled` / runtime fractional gating / the route>vhost cascade / other per-route filter variants. NO HCM change [phase 23 landed the route-threading] → the lightest filter phase since the framework completed; §6.1 single-phase projected. Doc-only [CREATE `SPEC.md`; MODIFY `ROADMAP.md` + `DECISIONS.md` + `STATE.md` + `STATE_HISTORY.md`]; no code change. DECISIONS.md ledger head **ADR-0060** [count 61; next available **ADR-0061** reserved for the §6.2 reconciliation; **ADR-0062** reserved for the split]. ADR-0014 in force; ADR-0028 open. Per §5.1 the state-2 PLAN-write is the NEXT session.)

> Historical `## Last updated` notes — every superseded last-updated note (all closed phases + the active phase's prior sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: one state per session; do not chain states. State-6 close-out commits (this commit) touch ROADMAP.md + STATE.md only and carry no code changes; the next session writes PLAN.md for the next active phase per `superpowers:writing-plans`.
- The reviewer's R2 disposition decision (option (a) retroactive split of 05.1 vs option (b) free-standing post-05.1 sub-phase) was settled at the 05.1 state-6 commit in favor of option (b); 05.4 is the chosen sibling sub-phase. Future-reviewers reading STATE.md should understand that 05.1 is structurally closed at the preamble landing; 05.4 is a SIBLING under parent-05, not a child of 05.1; and the execution order ran 05.1 → 05.4 → 05.2 → 05.3, with 05.3 the closing sub-phase that flips parent-05 to `done`.

### Phase-24 state-1 brainstorm

- **Phase pick + scope (ADR-0060).** Phase `24` = **`envoy.filters.http.csrf`**, the HTTP-filter-family's SIXTH concrete phase (after 09 local_ratelimit / 10 rbac / 11 fault / 22 jwt_authn / 23 cors) and the **SECOND consumer of the phase-23 per-route `typed_per_filter_config` infrastructure**. The pick is driven by the §6.3 anti-pattern: phase 23 landed the per-route-config infra but registered only the `Cors` variant; a second tested consumer is needed to prove it generalizes additively. `csrf` is the standout: deterministic, header-only, locally-verifiable, reuses existing infra, and — decisively — the cross-cutting HCM route-early-resolution risk that made phase 23 borderline is GONE (phase 23 landed `resolve_route` + the `apply_route_config` threading), so phase 24 is a near-pure additive plug-in (an enum arm + a filter module), the lightest filter phase since the framework completed.
- **Alternatives rejected at the brainstorm:** `buffer` (request-body-buffering machinery — higher risk; deferred to its own phase); per-route overrides of existing `fault`/`rbac`/`local_ratelimit` (lower new-surface value; the interesting route>vhost `mostSpecificPerFilterConfig` cascade was deferred at phase 23); `compression` (gzip foundations-ADR + non-byte-exact body comparison); `ext_authz`/`ext_proc` (external auth service / gRPC still H2-trailers-blocked); load-balancing family (non-deterministic distribution / byte-exact hash-ring reproduction / distinguishable-backend harness). The phase-23 M23-6/M23-7 connection-handling hardening was offered as a fold-in foundation slice and DECLINED — kept as M-track follow-ups to keep phase 24 single-purpose.
- **Minimum-viable scope (SPEC §3):** the second `PerFilterConfig::Csrf(CsrfPolicy)` variant + a near-empty filter-chain `Csrf` entry + a `CsrfPolicy` schema (`additional_origins: Vec<StringMatcher>` reusing 04.x + `filter_enabled` deterministic-0%/100% only per the phase-11 fault precedent); `CsrfFilter` (eighth `HttpFilterInstance` variant; first decode-side-only per-route filter — modify-method origin guard, `Origin`/`Referer` vs `Host` + `additional_origins`, 403 `StopAndSend` on mismatch/missing-source via the existing filter-synth helpers; per-route policy via the existing `apply_route_config` hook — NO HCM change); `http.<stat_prefix>.csrf.{request_valid,request_invalid,missing_source_origin}` stats; fixture `0032-http-filter-csrf` (H1, real upstream cluster per ADR-0058 L6, 5 deterministic probes); in-process backstop; a `parse_bootstrap` fuzz seed (no new target). The existing `PerRouteConfigForAbsentFilter` validator is reused verbatim.
- **Deferred (SPEC §4):** `filter_enabled` runtime fractional gating + `shadow_enabled` shadow mode; vhost-level config + the route>vhost cascade; `typed_per_filter_config` for filters OTHER than cors/csrf; streaming/trailers + gRPC-Web CSRF.
- **§6.1 split posture:** projected single-phase (~900–1300 LoC / ~8–11 tasks; lighter than phase 23). Split valve reserved as 24.1 (schema + wiring + validator) / 24.2 (CsrfFilter + stats + fixture + close) under ADR-0062 — very unlikely to fire.
- **§6.2 (deferred to the state-2 PLAN-write, runs LOCALLY):** resolve the config shape FIRST (gates D1 — the chain-vs-route `filter_enabled` requirement); then the modify-method set / source-target origin computation + `Referer` fallback / 403 failure-body bytes + `content-type` + missing-source disposition / stat namespace + semantics / `filter_enabled` disposition / the per-route-for-absent-filter reuse. Inline ADR-0061 lands if any item diverges (config shape + modify-method set are the most likely triggers).
- **ADR posture:** ledger head **ADR-0060** (this brainstorm; count 61). **ADR-0061** reserved for the §6.2 reconciliation; **ADR-0062** reserved for the split. ADR-0014 in force; ADR-0028 open.

> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
