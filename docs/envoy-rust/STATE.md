# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase


**id:** **76.1** — `76.1-redirect-config-surface` (the CONFIG SURFACE slice of the `Route.redirect` action: the `RedirectAction` schema, the five-value `RedirectResponseCode` enum, the THIRD `RouteAction` variant, the widened three-way `Route` action-cardinality check, the two intra-`RedirectAction` oneof validators, both `Serialize` arms, and a `parse_bootstrap` fuzz corpus seed — NO runtime behaviour and NO new fixture). **§5 STATE-2 PLAN-WRITE IS COMPLETE AT THIS COMMIT** — `PLAN.md` exists with **8** TDD-ordered numbered tasks covering all 26 `SPEC.md` §6 obligations (T-R1..T-R10 reject-direction, T-A1..T-A7 accept-direction, T-C1..T-C9 config-surface mechanics), and **ADR-0170** records the §6.2 empirical reconciliation. **THE §6.1 GATE DID *NOT* FIRE: NO FURTHER SPLIT.** The re-derived estimate is **≈515 net `crates/`+`tests/` LoC at 8 tasks** against the ~1500 / ~25 thresholds — 34% of the LoC gate, the smallest phase in the measured band. **The implementation half is not projected but MEASURED at net 129 LoC** (`bootstrap.rs` 101 + `lib.rs` 20 + `hcm.rs` 8) by applying the plan's own literal Rust in a scratch worktree; only the test half (≈348) is projected. Calibration was RE-DERIVED, not inherited: three of the seven inherited phase figures were reproduced exactly from `git diff --numstat <state-2>..<close-out> -- crates/ tests/` (**74→1981, 75.1→1413, 75.2→897**), validating both the method and the band. ROADMAP row `76.1` flips `planned` → `in-progress` at this commit (the status cell ONLY, per §4.1(2) and the 75.2 state-2 precedent); parent row `76` stays `in-progress`; row `76.2` stays `planned`. Census is **107** rows / **104** `done` / **2** `in-progress` / **1** `planned`. **The mission is NOT complete and no `stop` file exists** (ADR-0167 DECISION 2).
**slug:** `76.1-redirect-config-surface`. Parent: `76-route-redirect-action` (still `in-progress`; it flips to `done` only after BOTH `76.1` and `76.2` are `done`). Sibling: `76.2-redirect-runtime-fixture` (`planned`, depends on `76.1`, MUST NOT start before `76.1` is `done`).
**directory:** `docs/envoy-rust/phases/76.1-redirect-config-surface/` — now holds **`SPEC.md` + `PLAN.md`** (1685 lines, 8 tasks), i.e. §5 **state 3**. `PROGRESS.md` and `REVIEW.md` do not exist. The sibling `docs/envoy-rust/phases/76.2-redirect-runtime-fixture/` still holds **`SPEC.md` ONLY** and is **NOT the active unit**. The parent `docs/envoy-rust/phases/76-route-redirect-action/` holds its original `SPEC.md`, still **BYTE-UNCHANGED** (ADR-0169 DECISION 5) — and **it has no `PLAN.md` and never will** (§6.2 step 1). The three FROZEN predecessor directories are `phases/75-headermatcher-absence-parity/`, `75.1-headermatcher-absence-engine-route/` and `75.2-headermatcher-absence-accesslog/` — all CLOSED; do not re-open, re-grade or edit them.

**status:** **SUB-PHASE `76.1` §5 STATE-2 PLAN-WRITE COMPLETE (THIS session). ADR-0170 FIRED. NO FURTHER SPLIT.** This session ran `superpowers:writing-plans` scoped to `76.1` on `HEAD == 533dcebcd6190022e4c78a83abdd9f1af5afa544`, whose CI was RE-CONFIRMED here on the FULL 40-char SHA. **WHAT IT TOUCHED — FOUR state-ledger files plus ONE new `PLAN.md`, and NOTHING else:** `phases/76.1-redirect-config-surface/PLAN.md` (NEW, 1685 lines), `ROADMAP.md` (row `76.1` status cell only), `DECISIONS.md` (**ADR-0170**, new HEAD of the newest-first block), `STATE.md` + `STATE_HISTORY.md` (the ADR-0035 relocation, **14** lines on a PER-FILE delta check). **NO `crates/` file, NO `tests/` file, NO fixture, NO test entrypoint and NO `BEHAVIOR_CONTRACT.md` line was touched in the committed tree, and no landed ADR was edited.** **THE PLAN'S OWN LITERAL RUST WAS PRE-FLIGHTED FOR REAL, NOT MERELY WRITTEN** — every code block was applied in a scratch `git worktree` detached at `533dceb` and gated: `cargo fmt --all -- --check` **exit 0, zero diff**; `cargo clippy --workspace --all-targets --all-features -- -D warnings` **exit 0, ZERO warnings** with **155** `Checking` lines on a forced re-run (real work, not cached); `cargo build -p envoy-config -p envoy-http1` exit 0 with 71 `Compiling` lines; `cargo test -p envoy-config --lib` **653 passed / 0 failed**; `cargo test -p envoy-http1 --lib` **187 passed / 0 failed**. **AND THE DESIGN WAS VALIDATED SEMANTICALLY, NOT ONLY SYNTACTICALLY: ALL 21 MEASURED CELLS PRODUCE THE CORRECT VERDICT** — J1-J7 plus T-R8/T-R9/T-R10 all REJECT; A1-A4/A6, `https_redirect: false` ALONE, and all five `response_code` names all ACCEPT; `port_redirect: 70000` round-trips VERBATIM. The three pre-existing tests most at risk from the widened cardinality/unknown-field messages (`rejects_route_with_both_direct_response_and_route`, `rejects_route_with_neither_direct_response_nor_route`, `rejects_route_with_unknown_top_level_key`) ALL STILL PASS — they assert substring containment of `direct_response`, `route` and `exactly one`, which the three-way messages preserve. **THE PRE-FLIGHT FOUND TWO EDIT SITES THE `76.1/SPEC.md` §4.4 ENUMERATION MISSES, AND BOTH ARE HARD COMPILE ERRORS:** (1) `crates/envoy-config/src/lib.rs:14` is an EXPLICIT, alphabetically-sorted `pub use bootstrap::{…}` re-export list — **NOT a glob** — so `RedirectAction` and `RedirectResponseCode` must be added there or `envoy-http1` cannot name the type (`error[E0432]: unresolved import`); and (2) the `hcm.rs:2361` test-module import must gain `RedirectAction` (`error[E0422]`). **A THIRD PRE-FLIGHT FINDING IS A `fmt` TRAP:** inserting two names into that `use` block makes rustfmt **REFLOW THE WHOLE TAIL of the block** (~7 lines, `+29 −9` not `+2 −1`); the first pre-flight pass FAILED `cargo fmt --all -- --check` with a 66-line diff for exactly this reason, so the plan tells the implementer to let `cargo fmt` own it and NEVER hand-wrap. **ONE INHERITED FIGURE IS REFUTED:** `crates/envoy-config/src/bootstrap.rs` is **20 397 lines**, NOT the "~14 400" that both `76.1/SPEC.md` §0 and the session handoff assert — harmless (it only motivates re-anchoring) but do not propagate it. Every other `file:line` citation in `76.1/SPEC.md` was re-verified on disk by TEXT: **all TRUE except four PARTIALs on RANGE only** (the visitor accumulators are `:2438-2444` not `:2436-2444`; `RouteAction_Route` is `:2190-2211` with FOUR fields; the validator loop is `:3975-4058` with its `match` at `:3981-4019`; `canonical_reason` opens at `:188` inside `:183-215`) — **every construct the plan transcribes was located and quoted verbatim, and the SPEC's two CORRECTIONS of the parent SPEC were themselves CONFIRMED CORRECT** (the cardinality site really does use `M::Error::custom`, not a `ConfigError`; `Serialize for Route` at `:2529-2552` and `Serialize for RouteAction` at `:2554-2570` really are TWO separate impls and `Route::serialize` does NOT delegate). Independently RE-MEASURED this session: `ConfigError` has **exactly 123** variants (two ways) and this phase takes it to **125**; `fuzz/.gitignore` is **66** lines with **63** `!` lines and **63** tracked seeds (→ 67 / 64 / 64); **85** fixture dirs and **85** differential test files; the redirect vocabulary is **ZERO-hit greenfield** across all of `crates/`. A second action dispatch exists at `bootstrap.rs:4053` (`if let RouteAction::Route(..)`) which does **NOT** break on a new variant — **leave it alone**. Clippy emitted **13** `Checking` lines on the envoy-config-only re-run, the ADR-0150 seam witness figure: **no 14th line, the seam holds.**

_Historical (the phase-76 §5 STATE-2 SPLIT Active-phase `**id:**` + `**slug:**` + `**directory:**` + `**status:**` blocks + that block's own `_Historical_` pointer line — superseded by THIS sub-phase-76.1 §5 STATE-2 PLAN-WRITE, which advances `76.1` from state 2 to state 3; relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this commit per ADR-0035. **The `**slug:**` line is relocated at a within-sub-phase state advance too**, because its parent/sibling status prose is superseded, which is why the relocation set here is **14** lines, matching ADR-0168 DECISION 6 and ADR-0169.)_
> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases incl. the phase-51 state-3/4/5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill


**The next session runs the §5 STATE-3 IMPLEMENTATION** (`superpowers:executing-plans`, per the `SKILL_ROUTING.md` state machine) **for SUB-PHASE `76.1`**, whose `SPEC.md` AND `PLAN.md` both exist and whose implementation has not started — that is state 3's unambiguous detection rule (`PROGRESS.md` does not exist). **It is scoped to `76.1`, NOT to the parent `76` and NOT to `76.2`.** **Use `superpowers:executing-plans`, NOT `superpowers:subagent-driven-development`** — `PLAN.md`'s "Execution stance" section explains why: seven of the eight tasks edit `crates/envoy-config/src/bootstrap.rs`, and Tasks 3-5 form a strict compile-order chain (adding the third `RouteAction` variant breaks FOUR non-exhaustive `match` sites at once, so the workspace does not build again until all four are handled), so a worktree fan-out would collide on one file and the cargo lock serialises the runs anyway. **It is a SEPARATE session and THIS PLAN-write deliberately did NOT chain into it** (§5.1; ADR-0127 — the context that wrote an artifact must not grade or extend it): no `crates/` or `tests/` file was touched in the committed tree, and no `PROGRESS.md` was opened. **TDD per `superpowers:test-driven-development` on EVERY task (D-3.1: tests first, no exceptions), appending to `PROGRESS.md` per task.** **TWO tasks pin ALREADY-CORRECT code and will go GREEN immediately — Task 4's `all_five_preexisting_route_keys_still_parse` and most of Task 6** — so honour TDD's RED with the MUTATION checks the plan specifies (Task 4 step 5, Task 6 step 3), grep `Compiling envoy-config` to prove a forced rebuild rather than a stale-binary FALSE PASS, and REVERT + re-grep each mutation before committing. Task 6's Mutation B is the valuable one: it must RED `route_action_serialize_round_trips_the_redirect_key` while LEAVING `route_serialize_round_trips_the_redirect_key` GREEN — if BOTH go red you mutated the wrong impl.

**SUB-PHASE `76.1` SCOPE AT A GLANCE** (full detail in `76.1/SPEC.md`; the executable detail, with pre-flighted literal Rust, in `76.1/PLAN.md`). **THE 8 TASKS:** (1) `RedirectResponseCode` — the five-value enum + `status() -> u16`; (2) `RedirectAction` — the struct; (3) `RouteAction::Redirect` + BOTH `Serialize` arms + the `pub use` re-export + the honest `synth_501` placeholder (the "compiles again" task — it CANNOT be split, because the new variant breaks four `match` sites simultaneously); (4) the `Route` visitor — accumulator, `"redirect"` key arm, SIX-name unknown-field list, three-way cardinality, widened `expecting`; (5) the two oneof validators + two `ConfigError` variants; (6) end-to-end accept direction + both `Serialize` round-trips; (7) the `parse_bootstrap` corpus seed + its `!`-un-ignore line; (8) local gate rehearsal + `PROGRESS.md`. **THE `Option`s ARE LOAD-BEARING — DO NOT "SIMPLIFY" THEM.** Upstream's oneofs are exclusive on FIELD PRESENCE, not value: `https_redirect: false` + `scheme_redirect: "ftp"` REJECTS while `https_redirect: false` ALONE ACCEPTS, and `path_redirect: ""` + `prefix_rewrite` REJECTS. So the validators must test `.is_some()` — **never** truthiness and **never** `!s.is_empty()`. A `#[serde(default)] bool` loses presence and mints a NEW divergence; T-R8 is the test that catches it. **HOUSE IDIOM CONFIRMED FOR THE ENUM:** CamelCase variants + `#[serde(rename_all = "SCREAMING_SNAKE_CASE")]` + `#[default]`, per the landed `LbSubsetFallbackPolicy` (`bootstrap.rs:366-373`) and `HashFunction` (`:439-450`) — clippy-clean with NO `#[allow(non_camel_case_types)]`, and VERIFIED that serde renders all five wire names correctly so **no explicit `#[serde(rename)]` is needed** (unlike `HashFunction::MurmurHash2`). **THE THREE-WAY CARDINALITY WORDING IS DELIBERATE:** `neither is present` is kept VERBATIM from the landed message, and `both are present` becomes `more than one is present`; a catch-all `_` arm keeps the match at five arms instead of eight tuple combinations. **DIFFERENTIAL SURFACE = THE REJECT DIRECTION** (seven MEASURED upstream rejections J1-J7) **plus regression-equivalence over the 85 existing fixtures** — the 05.1/07.1/12.1/14.1 foundation-slice pattern. **NO new fixture; gate (a) is VACUOUSLY met and `PROGRESS.md` must say so explicitly** so a reviewer does not read the absence as an oversight. **FOUR ACCEPTANCES envoy-rust MUST NOT REJECT:** `port_redirect: 0`, `port_redirect: 70000` (**NO PGV upper bound — do NOT add a `1..=65535` check**), `host_redirect: ""`, `scheme_redirect: ""`. **NO new fuzz target, so NO `ci.yml` edit** — gate (d) is met by the existing `parse_bootstrap` run, but the new seed needs its explicit `!`-un-ignore line or it is silently untracked (verify with `git ls-files`). **NON-GOALS (each deliberately boot-fatal):** ALL runtime behaviour (that is `76.2` — the pure `location`-builder, the dedicated `synth_redirect`, the real dispatch arm, the `prefix_rewrite` `:path` mutation, fixture `0086`, the contract bank and the parent close); `regex_rewrite` inside `redirect` (boot-fatal via `deny_unknown_fields`, which is HOW J2 rejects here — a different mechanism from upstream's oneof error, the same VERDICT); the `route:`-arm rewrite family; `internal_redirect_policy`; `non_forwarding_action`; `weighted_clusters`; `cluster_header`; route-level `timeout`; route/vhost/route-config-level header mutation; and every open carry-forward including **CF-76-1**.


**Standing traps** (enduring). **STATE: SUB-PHASE `76.1` IS THE ACTIVE UNIT — `76.1-redirect-config-surface`, at §5 STATE 3; the parent `76` was OPENED at the §5 state-0/1 pick (ADR-0168), SPLIT at its state-2 PLAN-write (ADR-0169) and is STILL `in-progress`. `76.1`'s directory holds `SPEC.md` + `PLAN.md` (8 tasks, ADR-0170, NO further split); `PROGRESS.md` DOES NOT EXIST and the NEXT session is the §5 STATE-3 IMPLEMENTATION via `superpowers:executing-plans` (NOT subagent-driven — one file, a compile-order chain), a SEPARATE session (§5.1; ADR-0127). ROADMAP census is 107 rows / 104 `done` / 2 `in-progress` / 1 `planned`. The mission is NOT complete; do NOT create a `stop` file.** **`DECISIONS.md` is NOT chronological AND is too large to Read whole** — 3944 lines but ~310 000 tokens (single-line ADR blocks), so a whole-file Read is REFUSED by the tool; chunk it by `grep -n '^## ADR-NNNN'` + offset/limit (memory `decisions-md-not-chronological`). It runs ascending `ADR-0001..0100`, then a NEWEST-FIRST block. **Ledger head is ADR-0170; next available ADR-0171 — state the head as MEASURED, never inherited** (the §R9 error recorded as N-8 claimed "next available ADR-0165" when that session's own commit had already fired ADR-0165). `ROADMAP.md` / `BEHAVIOR_CONTRACT.md` (**3645** lines) / `STATE_HISTORY.md` and `STATE.md` itself all exceed the single-read limit — chunk them. **`STATE.md` top-section blocks are single multi-thousand-character lines.** An Edit whose `old_string` stops mid-line silently leaves the superseded tail glued on — span the FULL line or script it, then grep the old phrase (must be 0) (memory `state-md-blocks-are-single-lines-edit-hazard`). **MEASURE a line before editing it; never inherit a figure and never assume a range.** This Standing-traps line's MEASURED series is `da06059` **14653** → `1da0662` **16577** → `b369f9c` **20519** → `0c541d7` **23222** → `c02f31b`/`9bd18bd` **25930** → `361a005` **30754** → `533dceb` **31738** → this commit **33948** characters, TRIMMED once at the 75.2 close-out under ADR-0160. **The inherited "~32 500" figure for `533dceb` was WRONG — it MEASURED 31738.** MEASURE it yourself every time. **ADR-0035 relocation checks are DELTA-based AND PER-FILE:** a relocated line must go to **0** in `STATE.md` and **+1** in `STATE_HISTORY.md`; a COMBINED count is invariant by construction and FALSE-PASSES (memory `state6-relocation-check-must-be-delta-based`). Also assert §4.1(9)(d) directly (relocated multiset == `old STATE.md − new STATE.md`) and that 0 history lines were lost. **CAPTURE THE SUPERSEDED LINES FROM A PRE-EDIT BACKUP, BEFORE MUTATING ANYTHING** — capturing after rewriting archives the NEW text and fails the check (memory `adr0035-capture-superseded-lines-before-mutating`). **Back BOTH files up first**, and match `STATE_HISTORY.md` archive headers by **EXACT equality** — a prefix match hits per-phase variants and aborts AFTER `STATE.md` was rewritten (memory `state-history-archive-headers-need-exact-match`). Run the check against the FULL superseded set **INCLUDING the `### Doctrine reminders` §5.1 bullet, which is NOT exempt**; a CLOSE-OUT also retires the `**slug:**` line, so its set is 14, not 13. Per **ADR-0160** the enduring Standing-traps line and the doctrine bullet are rewritable only after relocating the superseded text verbatim. **Any ROADMAP row edit preserves all 6 cells and escapes `\|`.** Rows `36`/`38`/`39`/`52`/`54`/`66`/`70` already carry unescaped pipes — **do NOT "fix" them** (append-only). Only `66` and `70` actually MISCLASSIFY under a naive `awk -F'|' $5` (their stray pipe precedes the status column), so that sweep reports `done=100`; splitting on `' | '` WITH the spaces is robust, **status is field 4, not 5**, and yields the correct 104/104 census (memory `roadmap-rows-need-escaped-pipes`). A close-out flips the STATUS CELL ONLY (memory `roadmap-closeout-flips-status-cell-only`). **`next-prompt.txt` is gitignored** (`.gitignore:13`) — refresh it on disk, do NOT `git add` it, and do not copy landed narratives that list it among a commit's contents. **The ubiquitous "MISSION.md §9 feature families" citation is FALSE** — `MISSION.md` has no §9; the 11 families live at `ROADMAP.md:58`. Verify there; never "fix" archived occurrences. **Adjudicate any sweep with `--no-fail-fast` + a FULL-output redirect, NEVER `tail`** (memory `never-pipe-verification-runs-through-tail`; a bare `cargo test --workspace` aborts at the first failing BINARY, and `cargo build | tail -5` drops the `Compiling <crate>` line and fakes a stale-binary FALSE PASS). Run it 2-3× and DIFF the failing SET. **`0 passed; N filtered out` is NOT a pass** and `error: no test target named …` exits 101 exactly like a real RED — assert on the `N passed` count, never the exit code (memory `cargo-test-p-name-false-green-filtered-out`); **read the failure TEXT, always.** **THE CI-TOTAL RECIPE IS NOT SELF-VALIDATING, AND ITS AWK FIELDS ARE 4 AND 6.** `grep -o 'test result: ok\. …'` discards `test result: FAILED.` lines before `awk` sees them, so its `failed=0` is true BY CONSTRUCTION — use `grep -oE 'test result: (ok|FAILED)\. …'` and assert the binary count separately. **`grep -o` strips the CI log's job/timestamp prefix, so the counts land at `$4`/`$6`; using `$5`/`$7` returns a clean-looking `binaries: 162 passed=0 failed=0`, a VACUOUS result — disbelieve a zero.** Last derived: `binaries: 162 passed=2105 failed=0`. **The same blindness bit a SECOND recipe** — `PROGRESS.md:3641`'s `[a-z]+` cannot match `FAILED` (carried-forward finding M-4); use `(ok|FAILED|ignored)`. **THE LOCAL HOST-FLAKE SET IS A STABLE CORE OF FIVE PLUS AN *OPEN-ENDED* STARTUP-RACE TAIL WHOSE SIZE CARRIES NO SIGNAL (ADR-0164).** **CORE (fails DETERMINISTICALLY in isolation — that determinism IS the environmental signature):** the four `access_log_*_upstream_reset` binaries (`TcpCloseBackend` IPv6-unreachable) and `admin_config_dump_server_info` (the `192.168.65.2` bridge-IP family). **TAIL: every member PASSES in isolation — the OPPOSITE signature.** Measured membership `1 / 0 / 2`, then `1 / 15 / 0`, union **SIXTEEN**; the core stayed at exactly five and was the exact INTERSECTION of all three sweeps. **Adjudicate any RED by ADR-0164's four-part test** (assertion never reached + passes in isolation + absent from some sweep + untouched by the phase's surface), **never by membership in `PLAN.md:1600`'s list**. **A WHOLE-SWEEP STARTUP WAVE CAN PREEMPT A CORE MEMBER'S NORMAL ASSERTION — classify by ISOLATION, never by failure TEXT.** The arithmetic identity to re-check every sweep: `local passed + failed == CI passed` (**2105**). **RE-DERIVE EVERY INHERITED CENSUS — ELEVEN have now been caught wrong or incomplete.** Current, INDEPENDENTLY RE-CONFIRMED 7/7 at the state-5 RE-REVIEW: **85** fixture dirs, **85** differential test files, **104** ROADMAP rows (**104** `done` after this close-out), **22 of 22** crate roots carrying `#![forbid(unsafe_code)]` (**enumerate members from the ROOT `Cargo.toml`, never a `crates/*/src/*.rs` glob** — D-3.8 HOLDS), **21**-line `known-failures.txt`, **5** fuzz targets, **75** tracked corpus files (`parse_bootstrap` 63, `accesslog_format_parse` 8, `jwt_parse` 3, `grpc_health_decode` 1, **`cdn_loop_parse` 0** — CF-75-5). **Treat every number you inherit as suspect anyway.** **Beware VACUOUS greps.** A fixture census against `tests/differential/tests/fixtures/` returns a clean-looking **0** (fixtures live at `tests/fixtures/`); a fuzz-target census against `crates/envoy-config/fuzz/fuzz_targets/` returns **1** (the five span FIVE crates); the CI-total awk field error returns `passed=0`. **And the opposite:** a grep can legitimately return >0 because a record QUOTES the thing it supersedes. **Adjudicate by LINE and by FILE, never by COUNT.** **DERIVE fixture→test-binary maps FROM THE TREE, never from the fixture name** (`grep -rl "tests/fixtures/00NN" tests/differential/tests/*.rs`): `0079`/`0080` are `access_log_and_filter`/`access_log_or_filter`, which a name-guessed grep misses. **Likewise derive a failing test's BINARY from its panic line.** **33 test-binary NAMES are duplicated between `tests/differential/tests/` and `crates/envoy-bin/tests/` — always pass `-p differential` (or the right `-p`), never a bare `--test <name>`.** **A SUBAGENT FINDING IS A CLAIM, NOT A RESULT — RE-VERIFY EVERY ONE ON DISK.** At the last review two of four dimensions returned figures that were WRONG, WEAKER, or WEAKER-THAN-REALITY (one finding was found WORSE at HEAD than reported). Prefer MEASURING a hand-derivation over quoting it. **A `_Historical_`/"see below" POINTER MUST POINT AT SOMETHING ON DISK** (D-3.4). **Mutation-check hygiene:** any tree-MUTATING probe belongs in its OWN scratch `git worktree` created `--detach` at HEAD (memories `mutation-checks-collide-with-parallel-subagents`, `worktree-subagents-get-stale-base` — a parallel agent's `git checkout` can silently revert an in-place mutation); re-grep the mutation as STILL PRESENT after the run; grep `Compiling <crate>` to prove a forced rebuild (**`cargo clippy` prints `Checking`, NOT `Compiling`**); and ALWAYS run an UNMUTATED CONTROL from the SAME worktree — a RED saying `not accept-ready within 10s: Connection refused` NEVER REACHED AN ASSERTION and is NOT evidence. **A PLAN-specified mutation can target the wrong cell** — an early `return` in a match arm makes arm-ORDER and arm-BODY mutations probe DIFFERENT cells. Remove only YOUR OWN worktree. **PLAN.md example code trips clippy** — the plan's literal Rust repeatedly fails the plan's OWN `clippy -D warnings` and `cargo fmt --check` and can cite helper fns that don't exist; catch it in the state-3 pre-flight. **A §5.2 re-entry usually pins ALREADY-correct code**, so the new test passes immediately — honour TDD's RED with a mutation check and record THAT as the RED evidence. **Mechanical fan-out scripts corrupt doc comments** — grep a commit's `+` lines for `///`. **THE 75.1 ENGINE IS LANDED AND IS THE PREMISE OF EVERYTHING ON THIS SURFACE — do NOT re-derive, revert or "simplify" it.** `HeaderMatcher::matches` (`crates/envoy-config/src/matcher.rs`) is an exhaustive tuple `match (&self.mode, value)` whose `(_, None) => return false` arm sits **AFTER** the `PresentMatch` arm and **BEFORE** every value arm, closed by an XOR with `invert_match`. The rule in full is `BEHAVIOR_CONTRACT.md` Phase 75 §A; `present_match: false` means **"the header must be ABSENT"**, not "no presence requirement". **`BEHAVIOR_CONTRACT.md` §D IS CORRECTED — do NOT "restore" the old wording and do NOT re-raise I-1.** The TRUE statement, now on disk: **the arm ORDER is guarded in-process AND cross-proxy** (by `0083` p07/p12 and `0085` probe 2; the one fixture that CANNOT catch it is `0084`). §D keeps a `> **History**` blockquote QUOTING the superseded sentence, and append-only **ADR-0162** carries the same false sentence corrected FORWARD by **ADR-0165** — never edit it. **A grep for the old text hits 7 files and EVERY hit is legitimate EXCEPT `PROGRESS.md:449`** (carried-forward finding M-7: un-flagged live state-3 narrative, append-only so correctly NOT retro-edited, superseded by §D and ADR-0165). **Adjudicate by LINE and by FILE, never by COUNT.** **FOUR ENGINE MUTATIONS ARE ON RECORD AND THEY HIT DIFFERENT CELLS — never substitute one for another.** (a) Hoisting `(_, None)` to the TOP: in-process `645 passed; 4 failed`, `0084` **GREEN** (`exact_match` is a VALUE mode the hoist does not change), **`0085` RED**, **`0083` RED at p07** — a single `0083` run witnesses **p07 ONLY**, because the `http1_probe_list` driver aborts at the FIRST failing probe (so a mutation breaking two cells cannot be witnessed by one run; cite a second fixture instead of claiming both). (b) `(_, None) => return false` → `(_, None) => false` is the true **D1** revert and REDs `0084` (`envoy_rust=2, envoy=1`). (c) `PresentMatch` → `if *want_present { v.is_some() } else { true }` is the pre-75.1 **D2** revert and REDs `0085`. (d) The polarity swap (`==` → `!=`) and the XOR drop RED both fixtures. Read ADR-0162 + ADR-0165 and `BEHAVIOR_CONTRACT.md` Phase 75 §D before touching any of them. **TWO CONFLATION TRAPS — do NOT unify them** (`BEHAVIOR_CONTRACT.md` Phase 75 §E and §F). **`HeaderMatcher.present_match`** vs **`ValueMatcher.present_match`** (RBAC / access-log metadata, fixture `0044`), where `present_match: false` NEVER matches — a DIFFERENT and CORRECT rule; since 75.1 the two AGREE in THREE of four cells and differ in exactly ONE. And **`HeaderMatcher.invert_match`** vs **`MetadataMatcher.invert`** (CF-74-1) — MEASURED accepted-but-INERT upstream, so it stays boot-fatal here. **The trap is live:** the only near-miss in the 21-RED census was `0082`, which also carries `string_match: { exact: "1" }` but under `metadata_filter: { matcher: … }`, i.e. `ValueMatcher` — a different message 75.1's commit `723bc3b` never touched. **ACCESS-LOG FIXTURE-AUTHORING RULE, standing: EVERY probe in an `http1_access_log_byte_exact` fixture must carry a DISTINCT `path:`** (route at `match: { prefix: "/" }`). A byte-exact fixture whose probes share one `path:` renders identical lines, so count+equality cannot attribute the survivor and a polarity swap passes GREEN. Rationale + evidence: `BEHAVIOR_CONTRACT.md` Phase 75 §G, **whose rule now explicitly binds EXISTING fixtures too**. **DO NOT collapse `0084`/`0085` back to a shared path.** **`0078`/`0079`/`0080` STILL carry that blindness (CF-75-4) — recorded, do NOT fix it opportunistically** (§6.3; owner is whoever next touches the access-log fixture family). `0081`/`0082`/`0040` are NOT blind (second discriminator). §G's census names 10 of the **31** members, and that is SAFE — the 21 unnamed are 18 single-probe (vacuously safe) plus `0041`/`0042`/`0056`, which carry distinct paths. **`%REQ(<GATING-HEADER>)%` IS BOOT-FATAL** — `%REQ(NAME)%` is allow-list gated to seven names in `REQ_ALLOW_LIST` (`crates/envoy-accesslog/src/command_operator.rs`). **`%REQ(:PATH)%` IS allow-listed** (the parser lowercases before the membership test), which is why the distinct-path fix costs nothing at runtime. **The byte-exact access-log driver takes ONE log file per side** (`AccessLogPaths` is two scalars under `deny_unknown_fields` and only one parent dir is bind-mounted), so a multi-sink access-log fixture is infeasible — use a sibling PAIR for two polarities. **Never weaken a fixture; never trim `known-failures.txt`** (21 lines — this host scores h2spec 3.5/2 as PASS where CI does not, so trimming on local evidence would break CI). **Do NOT add `on_header_missing` to fixtures `0081` OR `0082`** (ADR-0155 PV-6 — envoy-rust requires a `value` there, which would make the key RESOLVE and silently vacate the witness while the fixture stayed GREEN). **`0081` has THREE probes and expects TWO logged lines, byte-distinct so ORDER is pinned** — do not touch a probe, an `expect_logged` value or the probe ORDER. Do NOT disturb the **38** existing access-log fixtures. **THE h2spec GATE IS *NOT* VACUOUS IN CI — SETTLED (ADR-0163), do NOT re-raise it.** Locally it self-skips silently and `cargo test` swallows the notice, so a local green needs `--nocapture`. The genuinely real adjacent gap: **`.github/workflows/ci.yml:67` runs `cargo test --workspace` with NO `--no-fail-fast`** (verified on disk), so on a RED CI run conformance status is UNKNOWN, not green. Banked as **CF-75-3**; **owner is its OWN phase — do NOT widen into it.** **CF-75-6 IS OPEN (ADR-0166 DECISION 4) — the `envoy-bin` fatal-startup EPHEMERAL-PORT-REUSE family is ACTIVELY FLAKY IN CI, not merely locally.** MEASURED: `grep -rn 'must NEVER accept' crates/*/tests/*.rs tests/*/tests/*.rs` finds FOUR sites — `crates/envoy-bin/tests/xds_file_based_{cds,eds,lds,rds}.rs` — one shared `async fn assert_fatal_startup(…)` helper each carrying `TcpStream::connect(hcm_addr).is_err()`, with **17 call sites** (cds 2, eds 6, lds 3, rds 6). Each asserts a DEAD process's FREED ephemeral port never accepts, so any parallel bind flips `connect()` to `Ok` and the assertion FALSE-fails. **Two DIFFERENT members hit CI on two CONSECUTIVE docs-only commits:** `lds_route_to_unknown_cluster_is_fatal` (`xds_file_based_lds.rs:417`, port `43093`) on `c02f31b` run `30362142044` attempt 1, and `neither_route_source_is_fatal` (`xds_file_based_rds.rs:543`, port `32787`) on `9bd18bd` run `30363490501` attempt 1. Both same-SHA reruns → `success` at 15/13; both commits are Markdown-only, so neither RED can be the commit's doing. **IF IT REDS YOUR COMMIT: read the failure TEXT, measure the executable diff, and RERUN THE SAME SHA — do NOT weaken a test and do NOT trim `known-failures.txt`.** **Recorded, NOT fixed** (§6.3); it interacts with CF-75-3 and a single test-isolation phase could own both. **CF-75-2 is OPEN and OUT OF SCOPE:** upstream COMMA-JOINS duplicate header values before value matching while envoy-rust matches only the FIRST occurrence; the PRESENCE axis is PARITY, so none of 75.1's rule is affected. It spans all SIX value modes across all FIVE consumers and needs its OWN measured phase — do NOT "fix" it opportunistically. **It is recorded in THIS FILE, *NOT* in `BEHAVIOR_CONTRACT.md`** (`grep -c` there = **0**). **CF-72-2 and CF-75-1 are BANKED, NOT fixed** (`BEHAVIOR_CONTRACT.md` phase-72 `**§D` three members + `**§G`); owner is a future `HeaderMatcher` wire-shape-parity phase. **CF-74-6:** the wrapped `match_if_key_not_found: { value: <bool> }` spelling is accepted AND honored upstream but BOOT-FATAL here — a documented REJECT-direction load-parity gap; **do NOT "fix" it** (house precedent is bare-only, ADR-0063 / ADR-0133). **The ADR-0150 seam must keep holding:** `envoy-accesslog` has ZERO workspace deps (its `Cargo.toml` declares only `tokio`, `bytes`, `tracing`, `thiserror`) and MUST NOT depend on `envoy-config`. Matchers cross the seam as injected **trait objects** (`Arc<dyn MetadataMatch>`, `Arc<dyn HeaderMatch>`); `LogFilter` has NO `Eq`/`PartialEq` — do not add either. **The `13 Compiling` figure is the seam's witness** — 10 direct + 2 transitive dependents of `envoy-config` + itself = 13; a **14th** line would mean the seam had been BREACHED. **Do NOT revert landed work** — phases `00`-`74`, NOR sub-phases `75.1`/`75.2`, NOR the parallel workstream's `envoy-http2` header-list bound. Never add `rbac` to `is_terminal_network_filter`; never reject a top-level `filters: []` sink list; preserve ADR-0016 + ADR-0124 + ADR-0131 + ADR-0133 + ADR-0136/0137 + ADR-0139 + ADR-0140-0143 + ADR-0144-0149 + ADR-0150 through **ADR-0167**. **ADR-0028 is NOT lifted.** The phase-73 `< 2` rejection on `and_filter`/`or_filter { filters }` is a composition-arm PGV rule, **NOT** the top-level `filters: []` sink-list rule — do NOT "relax" it. **This host's Docker BIND MOUNTS are STALE-CACHED** — use a FRESH FILENAME/DIRECTORY for every config revision (memory `docker-bind-mounts-stale-cached-on-this-host`). **`--volumes-from` does NOT retrieve a stopped container's `/tmp`** — use `docker cp`. **Port-map with `docker -p`, NOT `--network host`** (the host-net namespace isn't shared here). **A backend-reachability probe needs the backend as a SIBLING container on a shared docker network** (host-spawned backends are unreachable; `host.docker.internal` is IPv6-only). **This host has virtiofs and no inotify**, so bind-mount file-watch/reload tests are native-CI-authoritative. **Backend-routing fixtures go RED locally** because this host routes the backend via `192.168.65.2`, not the allow-listed `192.168.65.254`/`172.17.0.1` — CI is authoritative. **Upstream Envoy will NOT create an access-log directory**, and access-log flush is TIMER-driven (~10 s) — hand-rolled probes need a graceful `docker stop -t 15`. **YAML 1.1:** an unquoted `cluster: y` under `node:` parses as boolean `true`, which upstream rejects. **`--mode validate` probes wire-shape networking-free** (`docker run envoyproxy/envoy:vX --mode validate` binds no sockets) but CANNOT answer runtime matching semantics or whether a field is INERT. **`cargo build -p envoy-bin` before ANY local differential** (the harness runs `target/debug/envoy-bin`, not release — a new config key REDs with a stale `unknown field`). **A MASS differential RED** (`client error (Connect)`, dozens at once) = the Docker daemon down (`sudo setfacl -m u:esa:rw /dev/kvm && systemctl --user restart docker-desktop`) — **but a mass wave of `never became accept-ready` with the daemon UP is CONTENTION**, a different thing. **A ~1-3 s green on a backend-free fixture is NORMAL**, not a silent skip (prove it by polling `docker ps` mid-run and by a negative control in the MAIN tree). **A clippy/build green finishing in ~1 s is PARTIALLY CACHED** — `touch` the changed files and re-run. **`cargo fuzz` runs from the CRATE dir, not the repo root, and takes a DIRECTORY, not a file**; a NEW fuzz target is NOT auto-discovered — wire it into the `ci.yml` fuzz job by hand; a NEW corpus seed needs an explicit `!`-un-ignore line or it is silently untracked (verify via `git ls-files`). **`cargo deny check` can red on a fresh unrelated RustSec advisory** — patch-bump the dep (`cargo update -p X --precise`), not a phase regression; its `license-not-encountered` warnings are unmatched ALLOWANCES in `deny.toml`, NOT violations. **envoy-bin writes `ConfigError` to STDOUT** and takes only `-c <path>`. **A `pkill -f <pattern>` whose pattern also matches the invoking shell's own command line kills that shell** — kill by PID or use `pgrep -x`. **Confirm CI with the FULL 40-char SHA** — `--commit <short-sha>` silently returns `[]`, which looks like "CI never ran" (memory `gh-run-list-commit-needs-full-sha`) — and check the **STEP COUNTS (15 for `build + test + lint`, 13 for `fuzz`)**, not merely the conclusion. A job `cancelled` with `runner_name:""` + `steps:0` never got a runner → rerun the SAME SHA (memory `ci-run-cancelled-with-no-runner-is-starvation`); `cancelled` with FULL step counts is your own follow-up push cancelling the older run and is benign. **Do not push twice in quick succession.** CI has no `paths-ignore`, so docs-only pushes DO build. **A GitHub outage can persist >1h on this host** (fetch/gh/push all time out with `No route to host`) — that is NOT an auth error; finish locally, background-retry the push, and hand any unpushed commits to the next session. **CONCURRENCY — A PARALLEL WORKSTREAM IS ACTIVE.** `.claude/worktrees/agent-*` git worktrees plus a long-running container on `envoyproxy/envoy:v1.33.0` and a sibling's testcontainers reaper. **LEAVE THEM ALONE**, and **EXCLUDE those worktrees from any repo-wide `find`/census/grep** — they duplicate every `fuzz_targets` dir and will inflate counts; prefer `git ls-files`, or enumerate workspace members from the root `Cargo.toml`. **Re-check `git fetch origin --prune` at session start AND again immediately before your commit** — a clean cold-start `git status` can go dirty mid-session from a sibling; defer to the further-along session and remove only your own artifacts (memory `concurrent-loop-sessions-race-on-phase-pick`). **A SESSION WITHOUT A COMMIT ADVANCES NOTHING** — scratch outputs are session-scoped and unrecoverable; if cut short, label numbers PREDICTIONS, not results. Cold-start reads `STATE.md` FIRST (disk-authoritative). **One state per session; do not chain** (§5.1), and the state-6 close-out and the next-phase state-0/1 PICK are ALWAYS separate sessions. **A REVIEWER MUST NOT FIX WHAT IT GRADES, and the context that wrote an artifact must not grade it** (§5.1; **ADR-0127**, which also records the ONE human-authorized 5→6 chaining exception at phase 66 and why 0→1, 1→2, 2→3, 3→4 and 4→5 must NEVER be chained) — **do not read the git history as licence to chain.** Parallelism is permitted strictly INSIDE one state (READ-ONLY review dimensions; the main session is the SOLE writer of the state ledger and makes every decisive measurement itself), never ACROSS states, and a state-6 close-out is ONE INDIVISIBLE UNIT, SOLO. **PHASE-76-SPECIFIC (new at this pick, all MEASURED against `envoyproxy/envoy:v1.33.0`).** **`location` is NOT on the `HEADER_ALLOW_LIST`** — that constant has exactly THREE entries (`server`, `date`, `x-envoy-upstream-service-time`) at `tests/differential/src/lib.rs:1177-1181`, and `diff_headers` (`:1193`) compares every non-allow-listed name **value-exact**. That is WHY fixture `0086` needs no new harness machinery — **do NOT add `location` to the allow-list**, which would silently vacate the whole witness. **THE AUTHORITY ASYMMETRY IS THE TRAP:** `host_redirect` DROPS the request's original port while a scheme-only change PRESERVES it, and `port_redirect` overrides in both cases WITHOUT normalising a redundant `:443`. **The query is PRESERVED by default even when `path_redirect` replaces the path wholesale**; `strip_query: true` drops it. **`%RESPONSE_CODE_DETAILS%` for a redirect is `direct_response`** — the SAME string envoy-rust already emits at `crates/envoy-http1/src/hcm.rs:2112`, so reuse the existing constant and add NO new detail string. **`%RESPONSE_FLAGS%` is `-`.** **`prefix_rewrite` MUTATES the logged `:path` while `path_redirect` does NOT.** **`port_redirect` has NO PGV upper bound** — `70000` both validates and renders verbatim, so do **NOT** add a `1..=65535` bound. **H2 has NO route resolver of its own** (`crates/envoy-http2/src/hcm.rs:475`/`:518` call H1's), so ONE dispatch arm at `crates/envoy-http1/src/hcm.rs:2110` serves both codecs — and conversely a bug there hits both. **`Http1ProbeList` ABORTS AT THE FIRST FAILING PROBE**, so one red run names ONE probe; cite a second fixture rather than claiming two cells from one run. **PREFIX-OVERLAP SILENTLY SHADOWS A PROBE** — a recon cell was lost when `prefix: "/scheme"` preceded `prefix: "/schemehost"`; every probe in `0086` must select a DIFFERENT route AND carry a DISTINCT `path:`. **CF-76-1 IS OPEN (NEW at this pick, ADR-0168 DECISION 4) and OUT OF SCOPE:** upstream STRIPS the query before route path matching — `match: { path: "/exact" }` MATCHES `/exact?q=1` (201) and `/exact?` (201) but NOT `/exact%3Fq=1` (404, because a percent-encoded `?` is not a delimiter) — while envoy-rust compares the RAW target byte-for-byte (`route_matches` does `path == p` at `crates/envoy-http1/src/hcm.rs:2155-2166`, called with `req.path` at `:2028` and `:2094`; `crates/envoy-http1/src/codec.rs:26-28` says "byte-for-byte (no normalization)"). It is SILENT, PRE-EXISTING, on the most-exercised surface in the project, and pinned by NO fixture. **Do NOT fix it opportunistically** — it needs its own measured phase because it interacts with `%REQ(:PATH)%` logging, the upstream-forwarded target and phase 76's own query rule. Phase 76 keeps the boundary clean by using a `prefix:`-matched route wherever a probe carries a query. **CF-75-2's JOIN RULE IS NOW MEASURED (ADR-0168 DECISION 5) — the divergence itself is STILL OPEN and STILL OUT OF SCOPE.** Upstream joins duplicate values with a **single comma and NO following space** (`exact_match: "v,v"` MATCHES, `"v, v"` does NOT); individual values are **OWS-trimmed before joining**; `safe_regex_match: "^v,v$"` and `suffix_match: ",v"` both MATCH, so every value mode sees the joined string; `range_match` does NOT match a joined `"1,2"` (joining precedes the numeric parse) though a single `5` does; and `cookie` is NOT special-cased. The fix is **SINGLE-SITE** — all five consumers pass the full header slice into one function and the collapse happens once at `crates/envoy-config/src/matcher.rs:40-43` — with one wrinkle: H2 **pre-collapses `host`** at `crates/envoy-http2/src/request.rs:65-74`. **THE ADR-0035 RELOCATION SET IS 14 LINES AT A PICK, NOT 13** (ADR-0168 DECISION 6). The inherited handoff asserted 13-at-a-pick and "only a close-out makes it 14"; that is measurably FALSE — at the previous pick `git show 5d78df4 -- docs/envoy-rust/STATE.md` is `23 14` and the removed set INCLUDES the `**slug:**` line, because naming a new active phase supersedes the `(none)` placeholder just as retiring a phase does. The close-out `1014524` is likewise `14 14`. **MEASURE the set against a pre-edit backup; never inherit its size.** **CANDIDATE SIZINGS BANKED AT THIS PICK, so the next pick need not re-measure them:** **CF-75-6** = FOUR files (`crates/envoy-bin/tests/xds_file_based_{cds,eds,lds,rds}.rs`), **17** `assert_fatal_startup` call sites (cds 2, eds 6, lds 3, rds 6), the helper executable-IDENTICAL across all four modulo one string literal, ~244 duplicated lines, **plus 5 further same-class sites** (`xds_file_based_lds.rs:483`, `tests/differential/src/backend.rs:755` and `:876`, `crates/envoy-bin/src/direct_response.rs:176`, `crates/envoy-bin/src/echo.rs:114`); the root cause is the reserve-then-drop `reserve_port()` at `crates/envoy-bin/tests/common/mod.rs:22-29`, and that module is ALREADY imported by all four files, so the de-dup needs no new file. **CF-75-3** = a one-line `--no-fail-fast` addition at `.github/workflows/ci.yml:67`, the ONLY `cargo test` in CI; the file has ZERO `continue-on-error` and ZERO `if:`, so a RED at or before the Docker pre-pull (`:51-64`) erases the entire test step and a RED test also loses `cargo deny check` (`:75`). **CF-75-4** = the blind set is EXACTLY THREE fixtures (`0078`/`0079`/`0080`); `0081`/`0082` share `path: /x` but are NOT blind (a `%DYNAMIC_METADATA%` field discriminates); and the record's "~6 lines each" UNDERSTATES it — all three match at `match: { path: "/x" }` in BOTH config files, so the route must change to `prefix: "/"` across 9-12 files. **`sni_cluster` remains correctly rejected** — it needs a `tls_inspector` LISTENER-filter subsystem envoy-rust wholly lacks (`Listener.listener_filters` is parse-and-ignore), the same measurement that rejected it at the phase-67 pick. **[ADDED AT THE PHASE-76 STATE-2 SPLIT — four MEASURED traps for the redirect surface.]** **(1) A REDIRECT RESPONSE HAS NO `content-type`.** Upstream emits exactly `location`/`date`/`server`/`connection`/`content-length`; a `direct_response` control emits `content-length`/`content-type`/`date`/`server`/`connection`. envoy-rust's shared `synth_with` (`crates/envoy-http1/src/hcm.rs:2185-2204`) ALWAYS emits `content-type`, so **the redirect arm MUST NOT reuse it** — `diff_headers` compares the lowercased NAME SET first (`tests/differential/src/lib.rs:1209-1215`) and would bail `only-in-envoy-rust=["content-type"]`. **(2) `canonical_reason` (`crates/envoy-http1/src/response.rs:188-215`) IS MISSING 303/307/308** — they fall through to `_ => "OK"`, so a 303 redirect emits `HTTP/1.1 303 OK`. **The differential fixture CANNOT catch this** (the harness parses the status CODE only; the reason phrase is not in the equivalence matrix), so it needs an IN-PROCESS pin. **(3) THE `RedirectAction` ONEOFS ARE EXCLUSIVE ON FIELD PRESENCE, NOT VALUE** — `https_redirect: false` + `scheme_redirect: "ftp"` REJECTS, `https_redirect: false` ALONE ACCEPTS, `path_redirect: ""` + `prefix_rewrite` REJECTS. Model `Option<bool>` / `Option<String>`; a `#[serde(default)] bool` loses presence and mints a NEW divergence. **(4) THE AUTHORITY IN `location` COMES FROM THE `Host` HEADER, NOT THE SOCKET** — proved with `Host: envoy-rust.test:1234` against a different listen port. That is WHY fixture `0086` can byte-compare `location` across two differently-ported proxies. `host_redirect` DROPS the original port; a scheme-only change PRESERVES it; `port_redirect` overrides both and does NOT normalise a redundant `:443`. **AND THE STANDING FIXTURE RULE FOR `76.2`:** every probe carries a DISTINCT `path:`, every route uses a NON-OVERLAPPING `prefix:` (a parent-recon cell was lost when `prefix: "/scheme"` shadowed `prefix: "/schemehost"`), and every query-bearing probe MUST use a `prefix:`-matched route to stay clean of **CF-76-1**. `Http1ProbeList` ABORTS AT THE FIRST FAILING PROBE, so one red run names ONE probe. **NEVER add `location` to the 3-entry `HEADER_ALLOW_LIST`** — it would silently vacate the entire witness. **[ADDED AT THE SUB-PHASE-76.1 STATE-2 PLAN-WRITE — five pre-flight findings, each a real compile/gate failure caught by APPLYING the plan's Rust rather than only writing it.]** **(1) `crates/envoy-config/src/lib.rs:14` IS AN EXPLICIT, ALPHABETICALLY-SORTED `pub use bootstrap::{…}` RE-EXPORT LIST, NOT A GLOB** — a new public `bootstrap.rs` type is invisible to every other crate until it is added there (`error[E0432]: unresolved import`). `76.1/SPEC.md` §4.4's "five edit sites" enumeration MISSES this. **(2) INSERTING INTO THAT `use` BLOCK MAKES RUSTFMT REFLOW ITS WHOLE TAIL** (~7 lines; the diff is `+29 −9`, not `+2 −1`). Let `cargo fmt` own it; a hand-wrapped list fails `cargo fmt --all -- --check` in CI. **(3) A TEST-MODULE IMPORT IS A SEPARATE EDIT SITE** — `crates/envoy-http1/src/hcm.rs:2361`'s `use envoy_config::{…}` needs the new type too (`error[E0422]`). **(4) `crates/envoy-config/src/bootstrap.rs` IS 20 397 LINES, NOT "~14 400"** — both `76.1/SPEC.md` §0 and the inherited handoff assert the wrong figure; harmless but do not propagate it. **(5) A SECOND `RouteAction` DISPATCH AT `bootstrap.rs:4053` (`if let RouteAction::Route(..)`) DOES NOT BREAK ON A NEW VARIANT** — the compiler forces exactly FOUR sites (`Serialize for Route`, `Serialize for RouteAction`, `validate_hcm`'s `match &r.action`, `build_response_in`'s `match &route.action`); leave `:4053` alone. **AND THE STANDING METHOD NOTE:** a state-2 PLAN-write can PRE-VALIDATE its own design end-to-end in a scratch `git worktree` — all 21 MEASURED verdict cells, `fmt`, `clippy -D warnings` and both crates' test suites — which converts the LoC estimate's implementation half from a projection into a MEASUREMENT (129 net LoC here) and is the cheapest way to catch a nonexistent helper fn. **`hcm_route_config` DOES NOT EXIST** — the real navigation helper is `first_route_action(&Bootstrap) -> &RouteAction` at `bootstrap.rs:10273`, alongside `route_action_yaml` (`:10227`), `BACKEND_CLUSTER` (`:10258`) and `NO_CLUSTERS` (`:10268`).
_Historical (the phase-76 §5 STATE-2 SPLIT `## Next expected skill` pointer + the superseded `**SUB-PHASE `76.1` SCOPE AT A GLANCE**` block + the superseded `**Standing traps**` line + that block's own `_Historical_` pointer line — superseded by THIS sub-phase-76.1 §5 STATE-2 PLAN-WRITE; relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this commit per ADR-0035. Per ADR-0160 the enduring Standing-traps line is rewritable only after relocating its superseded text verbatim, and since ADR-0160 landed at `3f0ec89` EVERY state commit archives it.)_
> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases incl. the phase-37 state-1..5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit


**Sub-phase 76.1 §5 STATE-2 PLAN-WRITE — `PLAN.md` written with 8 TDD-ordered tasks on a re-derived ≈515 net LoC; the §6.1 gate did NOT fire, so NO further split; ADR-0170 records the reconciliation.** FIVE files, ALL Markdown, ZERO executable lines: `phases/76.1-redirect-config-surface/PLAN.md` (NEW, 1685 lines), `ROADMAP.md` (row `76.1` status cell `planned` → `in-progress`, all five other cells byte-identical), `DECISIONS.md` (**ADR-0170** inserted as the new HEAD of the newest-first block; ADR-0169 left byte-unchanged), and `STATE.md` + `STATE_HISTORY.md` (the ADR-0035 relocation, **14** lines on a PER-FILE delta check). **NO `crates/` or `tests/` file was touched in the committed tree** — the plan's literal Rust was pre-flighted in a throwaway `git worktree` which was removed. The parent `phases/76-route-redirect-action/SPEC.md` and `phases/76.1-redirect-config-surface/SPEC.md` are both BYTE-UNCHANGED. Predecessor `HEAD` was `533dcebcd6190022e4c78a83abdd9f1af5afa544`.

_Historical (the phase-76 §5 STATE-2 SPLIT Last-commit block + that session's own `_Historical_` pointer line — superseded by THIS sub-phase-76.1 §5 STATE-2 PLAN-WRITE; relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this commit per ADR-0035.)_
> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases incl. the phase-37 state-1..5 sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated


2026-07-29 (sub-phase-76.1 **§5 STATE-2 PLAN-WRITE COMPLETE — 8 TDD-ORDERED TASKS ON A RE-DERIVED ≈515 NET LoC; THE §6.1 GATE DID *NOT* FIRE AND THERE IS NO FURTHER SPLIT.** `ADR-0170` records the §6.2 empirical reconciliation: the re-derived estimate, the two `SPEC.md` §4.4 edit sites it MISSES, the rustfmt-reflow trap, the refuted "~14 400-line `bootstrap.rs`" figure, and the pre-validated design shape. The estimate's implementation half is **MEASURED at net 129 LoC**, not projected, by applying the plan's own Rust in a scratch worktree where `fmt` was clean, `clippy -D warnings` emitted ZERO warnings, `envoy-config` ran **653 passed / 0 failed**, `envoy-http1` **187 passed / 0 failed**, and **all 21 MEASURED verdict cells came out correct**. Calibration was re-derived: **74→1981, 75.1→1413, 75.2→897** reproduced exactly. The active unit is still **`76.1`**, now at §5 **state 3**. ROADMAP census: **107** rows / **104** `done` / **2** `in-progress` / **1** `planned`. The mission is NOT complete and NO `stop` file was created.)

_Historical (the phase-76 §5 STATE-2 SPLIT Last-updated note + that session's own `_Historical_` pointer line — superseded by THIS sub-phase-76.1 §5 STATE-2 PLAN-WRITE; relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this commit per ADR-0035.)_
> Historical `## Last updated` notes — every superseded last-updated note (all closed phases incl. the phase-37 state-1..5 sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsection for fully-closed phase 64 (the `### Phase-64 state-5 code-review` narrative — phase 64 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-64 state-6 close-out when ROADMAP row `64` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 63 (the `### Phase-63 state-5 code-review` narrative — phase 63 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-63 state-6 close-out when ROADMAP row `63` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 61 (the `### Phase-61 state-1 brainstorm` / `### Phase-61 state-2 PLAN-write` / `### Phase-61 state-3 implementation` / `### Phase-61 state-4 verification` / `### Phase-61 state-5 code-review` narratives, relocated at the phase-61 state-6 close-out when ROADMAP row `61` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 58 (the `### Phase-58 state-1 brainstorm` / `### Phase-58 state-2 PLAN-write` / `### Phase-58 state-3 implementation` / `### Phase-58 state-4 verification` / `### Phase-58 state-5 code-review` narratives, relocated at the phase-58 state-6 close-out when ROADMAP row `58` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 57 (the `### Phase-57 state-1 brainstorm` / `### Phase-57 state-2 PLAN-write` / `### Phase-57 state-3 implementation` / `### Phase-57 state-4 verification` / `### Phase-57 state-5 code-review` narratives, relocated at the phase-57 state-6 close-out when ROADMAP row `57` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 42 (the `### Phase-42 state-5 code-review (active phase)` narrative — phase 42 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-42 state-6 close-out when ROADMAP row `42` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 43 (the `### Phase-43 state-5 code-review (active phase)` narrative — phase 43 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-43 state-6 close-out when ROADMAP row `43` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 44 (the `### Phase-44 state-5 code-review (active phase)` narrative — phase 44 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-44 state-6 close-out when ROADMAP row `44` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 45 (the `### Phase-45 state-5 code-review (active phase)` narrative — phase 45 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-45 state-6 close-out when ROADMAP row `45` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 46 (the `### Phase-46 state-5 code-review (active phase)` narrative — phase 46 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-46 state-6 close-out when ROADMAP row `46` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 47 (the `### Phase-47 state-5 code-review (active phase)` narrative — phase 47 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-47 state-6 close-out when ROADMAP row `47` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 48 (the `### Phase-48 state-5 code-review (active phase)` narrative — phase 48 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-48 state-6 close-out when ROADMAP row `48` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 49 (the `### Phase-49 state-5 code-review (active phase)` narrative — phase 49 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-49 state-6 close-out when ROADMAP row `49` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 50 (the `### Phase-50 state-3 implementation (active phase)` narrative — phase 50 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-50 state-6 close-out when ROADMAP row `50` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 51 (the `### Phase-51 state-3 implementation (active phase)` narrative — phase 51 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-51 state-6 close-out when ROADMAP row `51` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 53 (the `### Phase-53 state-1 brainstorm` narrative — phase 53 kept a single active-phase Notes subsection at "state-1 brainstorm" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-1 subsection remained in `## Notes` at close — relocated at the phase-53 state-6 close-out when ROADMAP row `53` flipped `done`, together with the inline superseded state-3 narrative) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 54 (the `### Phase-54 state-1 brainstorm` / `### Phase-54 state-2 PLAN-write` / `### Phase-54 state-3 implementation` / `### Phase-54 state-4 verification` / `### Phase-54 state-5 code-review` narratives, relocated at the phase-54 state-6 close-out when ROADMAP row `54` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 55 (the `### Phase-55 state-5 code-review (active phase)` narrative — phase 55 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state individually superseded in place and relocated to STATE_HISTORY.md at its own transition], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-55 state-6 close-out when ROADMAP row `55` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 56 (the `### Phase-56 state-5 code-review (active phase)` narrative — phase 56 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state individually superseded in place and relocated to STATE_HISTORY.md at its own transition], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-56 state-6 close-out when ROADMAP row `56` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 66 (the `### Phase-66 state-5 code-review (active phase)` narrative — phase 66 used a rename-in-place active-phase Notes subsection across its state-0/1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-66 state-6 close-out when ROADMAP row `66` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed sub-phase 67.1 (the `### Phase-67.1 §5 state-5 code-review` narrative — sub-phase 67.1 used a rename-in-place active-phase Notes subsection across its state-2..5 + §5.2 state-3 re-entry arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-67.1 state-6 close-out when ROADMAP row `67.1` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed sub-phase 67.2 (the `### Phase-67.2 §5 state-5 re-review` narrative — sub-phase 67.2 used a rename-in-place active-phase Notes subsection across its state-2..5 + §5.2 state-3 re-entry + state-4-re-verification + state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review subsection remained in `## Notes` at close — relocated at the phase-67.2 state-6 close-out when ROADMAP row `67.2` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
> Historical Notes subsection for fully-closed phase 68 (the `### Phase-68 §5 state-5 code-review` narrative — phase 68 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-68 §5 state-6 close-out when ROADMAP row `68` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 69 (the `### Phase-69 §5 state-5 re-review` narrative — phase 69 used a rename-in-place active-phase Notes subsection across its state-3 → §5.2 state-3 re-entry → state-4 re-verification → state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review subsection remained in `## Notes` at close — relocated at the phase-69 §5 state-6 close-out when ROADMAP row `69` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 70 (the `### Phase-70 §5 state-5 re-review (2nd)` narrative — phase 70 used a rename-in-place active-phase Notes subsection across its state-1..5 + two-§5.2-re-entry + two-state-4-re-verification + two-state-5-re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review-(2nd) subsection remained in `## Notes` at close — relocated at the phase-70 §5 state-6 close-out when ROADMAP row `70` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 71 (the `### Phase-71 §5 state-5 code-review` narrative — phase 71 used a rename-in-place active-phase Notes subsection across its state-0/1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-71 §5 state-6 close-out when ROADMAP row `71` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 72 (the `### Phase-72 §5 state-5 re-review` narrative — phase 72 used a rename-in-place active-phase Notes subsection across its state-1..5 + §5.2 state-3 re-entry + state-4 re-verification + state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 re-review subsection remained in `## Notes` at close — relocated at the phase-72 §5 state-6 close-out when ROADMAP row `72` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 73 (the `### Phase-73 §5 state-5 code-review` narrative — phase 73 used a rename-in-place active-phase Notes subsection across its state-0/1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-73 §5 state-6 close-out when ROADMAP row `73` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 74 (the `### Phase-74 §5 state-5 re-review (2nd)` narrative — phase 74 used a rename-in-place active-phase Notes subsection across its state-0/1..5 + TWO §5.2 state-3 re-entry + TWO state-4 re-verification + TWO state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review-(2nd) subsection remained in `## Notes` at close — relocated at the phase-74 §5 state-6 close-out when ROADMAP row `74` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed sub-phase 75.1 (the `### Phase-75.1 §5 state-5 code-review` narrative — sub-phase 75.1 used a rename-in-place active-phase Notes subsection across its state-2..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the sub-phase-75.1 §5 state-6 close-out when ROADMAP row `75.1` flipped `done`; parent row `75` deliberately stays `in-progress` until sibling `75.2` is `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Phase-76 §5 state-0/1 brainstorm (active phase)

- **Pick:** the `Route.redirect` action (`envoy.config.route.v3.RedirectAction`) — the THIRD `Route.action` oneof arm, which envoy-rust WHOLLY LACKS (`RouteAction` is a two-variant enum at `crates/envoy-config/src/bootstrap.rs:2178`; the `Route` visitor accepts five keys at `:2483-2494`), so a `redirect:` route upstream SERVES is boot-fatal here. Pick + scope: **ADR-0168**. **ADR-0169 RESERVED-UNFIRED** for a `76.1`/`76.2` split.
- **The recon drove the pick, and it also produced two findings that outlived it.** `CF-76-1` (NEW): upstream STRIPS the query before route path matching while envoy-rust matches the RAW target — silent, pre-existing, on the most-exercised surface in the project, pinned by no fixture; NOT fixed here, owner is its own phase. And `CF-75-2`'s join rule, which its own record flagged as UNMEASURED, is now measured (single comma, no space, OWS-trimmed, seen by every value mode, precedes the numeric parse, `cookie` not special-cased) — the divergence stays OPEN and out of scope.
- **Four measured properties, not intuitions, decided it.** Backend-free (zero clusters — so locally verifiable on a host where backend-routing fixtures go RED); zero new harness machinery (`location` is not allow-listed, so `Http1ProbeList` compares it value-exact for free); one dispatch seam that serves BOTH codecs (H2 calls H1's resolver); and 15 deterministic runtime cells + 7 reject-direction rules.
- **The elegant fit:** `%RESPONSE_CODE_DETAILS%` for a redirect is measured as `direct_response` — the string envoy-rust ALREADY emits from its `DirectResponse` arm — so the new arm reuses the existing constant and adds no access-log surface at all.
- **Scope line = the §6.1 LoC gate.** `regex_rewrite` inside `redirect` is a NON-GOAL (measured working upstream, but it needs substitution machinery) and stays boot-fatal, the house posture already used for `hash_policy`/`retry_policy`/`domains`. Projected ~1340 net LoC / ~16-20 tasks: under both thresholds but within ~11% of the LoC gate, so the PLAN-write must re-derive it.
- **Honest gaps.** `SPEC.md` §7 records SEVEN explicitly NOT-MEASURED items; the fixture-critical ones (302/308 reason phrases, the full response-header set) must be measured at the PLAN-write. One recon probe was silently shadowed by prefix overlap and had to be re-measured — prefix overlap is a live hazard for fixture `0086`.
- **Next:** the §5 **state-2 PLAN-write** (a SEPARATE session per §5.1) — re-verify this SPEC's citations on disk, measure the two open cells, re-derive the estimate, then author `PLAN.md`.

### Phase-76 §5 state-2 SPLIT — sub-phase `76.1` is the active unit

- **The §6.1 LoC gate FIRED and `ADR-0169` consumed its RESERVED slot.** Phase `76` is split into `76.1-redirect-config-surface` and `76.2-redirect-runtime-fixture`. **No `PLAN.md` was written for phase `76` and none ever will be** — §6.2 step 1 says *stop, do not continue writing the oversize plan*. Do not read the parent's missing `PLAN.md` as an unexpected state under §5.1 Step E.
- **The estimate was RE-DERIVED and CALIBRATED, not inherited.** ≈1530 net `crates/`+`tests/` LoC at ≈20-24 tasks, against a ~1500 gate, benchmarked on the measured net LoC of seven recent phases (68→950, 69→1540, 70→1372, 73→873, 74→1981, 75.1→1413, 75.2→897). The parent SPEC had projected ~1340.
- **It rose because measurement added four obligations the parent SPEC lacked:** no `content-type` on a redirect response (so `synth_with` is unusable for it); `canonical_reason` missing 303/307/308 (and the fixture cannot catch it); the oneofs being exclusive on field PRESENCE not value (so `Option<bool>`, not `bool`); and the `prefix_rewrite` `:path` mutation needing `&mut Request` across 8 call sites.
- **Both parent-SPEC §7 cells this session owed are MEASURED:** `FOUND` → `302 Found`, `PERMANENT_REDIRECT` → `308 Permanent Redirect`, and the full redirect header set.
- **The parent `76/SPEC.md` is BYTE-UNCHANGED** (ADR-0169 DECISION 5) — it is the measured record of the state-0/1 recon. The two sub-phase SPECs are self-contained per D-3.4, and they state the parent's two corrections rather than editing them in.
- **Next:** the §5 **state-2 PLAN-write for `76.1`** (a SEPARATE session per §5.1) — re-verify `76.1/SPEC.md`'s citations on disk, write TDD-ordered tasks for T-R1..T-R10 / T-A1..T-A7 / T-C1..T-C9, and pre-flight the PLAN's own literal Rust against `cargo fmt --check` + `clippy -D warnings`.

### Sub-phase 76.1 §5 state-2 PLAN-write (active phase)

- **`PLAN.md` is written: 8 TDD-ordered tasks, ≈515 net LoC re-derived, and the §6.1 gate did NOT fire.** `ADR-0170` records the §6.2 empirical reconciliation. `76.1` moves from §5 state 2 to state 3; the next session is the state-3 implementation via `superpowers:executing-plans`, a SEPARATE session.
- **The estimate's implementation half is MEASURED, not projected.** Applying the plan's own literal Rust in a scratch `git worktree` gave **net 129 LoC** (`bootstrap.rs` 101 + `lib.rs` 20 + `hcm.rs` 8); only the test half (≈348) plus the fuzz seed (38) is projected. Calibration was re-derived rather than inherited — **74→1981, 75.1→1413, 75.2→897** all reproduced exactly.
- **The design was PRE-VALIDATED semantically, not just syntactically.** In that worktree: `cargo fmt --all -- --check` clean, `clippy --workspace --all-targets --all-features -- -D warnings` ZERO warnings (155 `Checking` lines, forced), `envoy-config` **653 passed / 0 failed**, `envoy-http1` **187 passed / 0 failed**, and **all 21 MEASURED verdict cells correct** (J1-J7 + T-R8/T-R9/T-R10 reject; A1-A4/A6 + `https_redirect: false` alone + all five `response_code` names accept; `port_redirect: 70000` verbatim).
- **Three pre-flight findings the `SPEC.md` does not carry, each a real failure:** `lib.rs:14` is an EXPLICIT non-glob `pub use` re-export list (a missed edit site → `E0432`); inserting into it makes rustfmt REFLOW the block's whole tail (`+29 −9`, and the first pre-flight pass genuinely failed `fmt --check`); and `hcm.rs:2361`'s test-module import is a THIRD edit site (`E0422`).
- **One inherited figure REFUTED:** `crates/envoy-config/src/bootstrap.rs` is **20 397** lines, not the "~14 400" both the SPEC §0 and the handoff assert. Every other SPEC citation verified TRUE on disk, with four PARTIALs on RANGE only; the SPEC's two corrections of the parent SPEC were themselves CONFIRMED.
- **Next:** the §5 **state-3 implementation for `76.1`** (a SEPARATE session per §5.1) — `superpowers:executing-plans`, NOT subagent-driven (one file, a compile-order chain), TDD per task, appending to `PROGRESS.md`, and honouring TDD's RED with the plan's specified mutation checks for the two tasks that pin already-correct code.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: **one state per session; do not chain states.** **PARENT PHASE `76` WAS SPLIT (§6.1, ADR-0169) AND IS STILL `in-progress`; THE ACTIVE UNIT IS SUB-PHASE `76.1` (`76.1-redirect-config-surface`), NOW AT §5 STATE 3** — its directory holds `SPEC.md` + `PLAN.md` but no `PROGRESS.md`, so the next session runs `superpowers:executing-plans` scoped to **`76.1`**, NOT to the parent `76` and NOT to `76.2`. **The active unit is always the lowest-numbered sub-phase that is not `done`.** **NO `PLAN.md` EXISTS OR WILL EVER EXIST FOR PHASE `76` ITSELF** — §6.2 step 1 stops the oversize plan, so the parent's state-2 output was the SPLIT, not a plan; do not read the parent's missing `PLAN.md` as an unexpected state under §1 Step E. **`76.2` MUST NOT START until `76.1` is `done`.** **The state-2 PLAN-write and the state-3 implementation are ALWAYS separate sessions** (ADR-0127: the context that wrote an artifact must not be the one that extends or grades it), and so are 3→4, 4→5 and the state-6 close-out.

- **On the §5.1 rule and its ONE recorded exception (read `ADR-0127`).** The phase-66 state-6 close-out was run in the SAME session as its state-5 code-review, at **explicit human instruction**. That was a one-off, human-authorized override; `BOOTSTRAP_PROMPT.md`, `MISSION.md` and `SKILL_ROUTING.md` were **not modified**, and **§5.1 remains binding on every autonomous session.** ADR-0127 records why the 5→6 transition alone was safe to chain (the close-out changes no code, re-runs no gate, and consumes only disk facts) and why **0→1, 1→2, 2→3, 3→4 and 4→5 must never be chained** — each carries a genuine self-review hazard, since the context that wrote an artifact would be grading it. Per memory `closeout-and-pick-are-separate-sessions`, the next-phase pick is STILL a separate session. Do not read the git history as licence to chain.

> Historical Notes subsection for fully-closed sub-phase 67.3 (the `### Phase-67.3 §5 state-5 re-review` narrative — sub-phase 67.3 used a rename-in-place active-phase Notes subsection across its state-2..5 + §5.2 state-3 re-entry + state-4-re-verification + state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review subsection remained in `## Notes` at close — relocated at the phase-67.3 §5 state-6 close-out when ROADMAP row `67.3` [and parent row `67`] flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 65 (the `### Phase-65 state-5 code-review` narrative — phase 65 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-65 state-6 close-out when ROADMAP row `65` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsection for fully-closed phase 52 (the `### Phase-52 state-5 code-review (active phase)` narrative — phase 52 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2/3/4 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-52 state-6 close-out when ROADMAP row `52` flipped `done`, together with the standing 05.1/05.4 future-reviewer note it carried) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 41 (the `### Phase-41 state-5 code-review (active phase)` narrative — phase 41 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the §6.2-recon PIVOT from `%REQ_WITHOUT_QUERY%` to `%ROUTE_NAME%` landed at state-2; the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-41 state-6 close-out when ROADMAP row `41` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 40 (the `### Phase-40 state-5 code-review (active phase)` narrative — phase 40 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-40 state-6 close-out when ROADMAP row `40` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 39 (the `### Phase-39 state-5 code-review (active phase)` narrative — phase 39 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-39 state-6 close-out when ROADMAP row `39` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
> Historical Notes subsection for fully-closed phase 38 (the `### Phase-38 state-2 PLAN-write (active phase)` narrative — phase 38 used a single active-phase Notes subsection for its whole arc; the state-3/4/5 sub-state narratives were superseded in the four-top-section archive blocks per-session, so only this state-2 subsection remained in `## Notes` at close — relocated at the phase-38 state-6 close-out when ROADMAP row `38` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) and for parent phase 25 (the `### Phase-25 state-1 brainstorm` pick + recon-finding narrative, relocated at the 25.2 state-6 close-out when parent `25` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 26 (the `### Phase-26 state-1 brainstorm` pivot/rejected-alternatives/key-scoping narrative, relocated at the phase-26 state-6 close-out when row `26` flipped to `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 27 (the `### Phase-27 state-1 brainstorm` / `### Phase-27 state-2 PLAN-write` / `### Phase-27 state-4 verification` narratives + the now-consumed `### Phase-27 carry-forwards` [M26-1..M26-8] block, relocated at the phase-27 state-6 close-out when row `27` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 28 (the `### Phase-28 state-1 brainstorm` / `### Phase-28 state-2 PLAN-write` / `### Phase-28 state-3 implementation + state-4 verification` / `### Phase-28 state-5 code review` narratives + the now-consumed `### Phase-28 carry-forwards` [M27-1..M27-3] block, relocated at the phase-28 state-6 close-out when row `28` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 29 (the now-consumed `### Phase-29 carry-forwards` [M28-1..M28-3] block + the `### Phase-29 state-1 brainstorm` / `### Phase-29 state-2 PLAN-write` / `### Phase-29 state-3 implementation` / `### Phase-29 state-4 verification` / `### Phase-29 state-5 code review` narratives, relocated at the phase-29 state-6 close-out when row `29` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 30 (the `### Phase-30 carry-forwards` [M29-1/M29-2, which fed phase 30 but were NOT consumed — they continue as phase-31 carry-forwards] block + the `### Phase-30 state-1 brainstorm` / `### Phase-30 state-2 PLAN-write` / `### Phase-30 state-3 implementation` / `### Phase-30 state-4 verification` / `### Phase-30 state-5 code review` narratives, relocated at the phase-30 state-6 close-out when row `30` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 31 (the `### Phase-31 carry-forwards` [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 — open Minors from the phase-30 REVIEW.md that fed phase 31 but were NOT consumed; they continue as carry-forwards for the next phase that touches the differential hash-sweep driver / the config parser] block + the `### Phase-31 state-1 brainstorm` / `### Phase-31 state-2 PLAN-write` / `### Phase-31 state-3 implementation` / `### Phase-31 state-4 verification` / `### Phase-31 state-5 code review` narratives, relocated at the phase-31 state-6 close-out when row `31` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 32 (the `### Phase-32 carry-forwards` block — the open Minors that fed phase 32 but were NOT consumed: the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3; they continue as carry-forwards for the future phase that touches their surface — re-listed in `## Next expected skill` above alongside the 6 new phase-32 REVIEW.md Minors M32-1…M32-6), relocated at the phase-32 state-6 close-out when row `32` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phases 33+34 (the `### Phase-33 carry-forwards` block — the active-phase carry-forwards Notes that lived through phases 33 and 34: the now-CONSUMED 6 phase-32 REVIEW.md Minors M32-1…M32-6 [folded + landed at the phase-33 state-3] + the "Other still-live carry-forwards" list [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3 + the HTTP-filters-family (1)-(4) — those still-live ones re-listed in `## Next expected skill` above alongside the new phase-34 Minors M34-1/M34-2/M34-3 + the phase-33 M33-1/M33-2]), relocated at the phase-34 state-6 close-out when row `34` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 35 (the `### Phase-35 state-1 brainstorm` / `### Phase-35 state-2 PLAN-write` / `### Phase-35 state-3 implementation` / `### Phase-35 state-4 verification` / `### Phase-35 state-5 code-review` narratives, relocated at the phase-35 state-6 close-out when ROADMAP row `35` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 36 (the `### Phase-36 state-5 code-review` narrative — phase 36 used a rename-in-place Notes discipline, so the state-1..4 sub-state narratives were superseded in place each session and only the four top-section blocks were relocated per-session — plus the now-CLOSED detailed M35-1 carry-forward bullet [CONSUMED by phase-36 F2], relocated at the phase-36 state-6 close-out when ROADMAP row `36` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 37 (the `### Phase-37 state-3 implementation` active-phase narrative, relocated at the phase-37 state-6 close-out when ROADMAP row `37` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### HTTP-filters-family carry-forwards (from the `25.2` REVIEW.md - NOT yet consumed; weigh whenever the HTTP-filters family is re-entered)

> These were never obligations on the xDS phase 26; they remain live for whenever an HTTP-filters-family phase resumes.

- **(1) [non-goal - architectural]** Over-limit request bodies are FULLY buffered before the 413 rejection (no streaming watermark). Documented deferred non-goal; differentially byte-identical to Envoy for the bounded fixture sizes. Revisit only if a streaming `decode_data` watermark path is ever planned.
- **(2) [doc precision]** The BEHAVIOR_CONTRACT 413-row "verified byte-exact against v1.33.0" phrasing - fixture `0033` is H1-only; the H2 over-limit path is covered by the in-process synth-decorator backstop, NOT differentially. Consider narrowing the phrasing if an H2 over-limit fixture is ever added.
- **(3) [coverage]** No standalone `== effective route limit` unit assertion (the boundary is exercised only via the over/under probes).
- **(4) [coverage]** No differential at-limit (`==`) probe in `0033` (within-limit `<` and over-limit `>` are both covered; the exact boundary is not differentially probed).
- _(2)-(4) are cheap polish, (1) is architectural and only relevant to a future streaming phase._
