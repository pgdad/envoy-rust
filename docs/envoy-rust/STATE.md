# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase


**id:** **65** (`65-accesslog-h2-rcd-upstream-reset`) — witness the deterministic H2 upstream-reset `%RESPONSE_CODE_DETAILS%` string `upstream_reset_before_response_started{connection_termination}` byte-exact on the H2 upstream-disconnect-before-headers 503 path, and migrate the H2 `UC` `%RESPONSE_FLAGS%` derivation from the phase-64 `reset_for_log_h2` boolean onto the now-unique rcd (retiring the boolean). The H2 analogue of phase 54 (which did EXACTLY this for the H1 side, consuming M53-1) — CONSUMES carry-forward **M64-1**. **STATE-4 VERIFICATION COMPLETE this session** — the full §7.5 gate ran; **(a)-(e) are MET**, all evidence quoted verbatim into `PROGRESS.md`'s `# State-4 verification` section. The state-5 code-review is the next session — see `## Next expected skill`.
**slug:** `65-accesslog-h2-rcd-upstream-reset`
**directory:** `docs/envoy-rust/phases/65-accesslog-h2-rcd-upstream-reset/` — `SPEC.md` (spec-reviewed, LOCK AS-IS) + `PLAN.md` (6 tasks) + `PROGRESS.md` (state-3 log + the state-4 gate evidence) present; `REVIEW.md` not yet created.

**status:** **PHASE 65 STATE-4 VERIFICATION COMPLETE — §7.5 (a)-(e) MET; state-5 code-review is next.** This session executed the §5 state-4 verification (`superpowers:verification-before-completion`): (1) cold-started fully per `BOOTSTRAP_PROMPT.md` §1 (all six required files + `SPEC.md`/`PLAN.md`/`PROGRESS.md` read in full), confirmed `git status --porcelain` clean, `HEAD` = `origin/main` = `f66fe9c` (phase-65 state-3 CI-adjudication commit, unmoved), confirmed `PLAN.md` + `PROGRESS.md` present and `REVIEW.md` ABSENT (the §5 state-4 detection rule), and that no sibling had advanced phase 65 (no `REVIEW.md`; ROADMAP row `65` still `in-progress`); (2) ran the full §7.5 gate and quoted **every** command's output verbatim into `PROGRESS.md`. **The authoritative CI run is `28986078817`, whose `headSha` is `f66fe9c` — the EXACT tree under gate** (working tree clean), conclusion **success**. **(a) MET** — `test access_log_h2_rcd_upstream_reset ... ok`: fixture `0070` witnesses the deterministic rcd byte-exact cross-proxy against real Envoy v1.33.0. **(b) MET** — that same run has **145 `test result:` lines, ZERO non-`ok`, zero `FAILED`**; 70 fixtures / 70 auto-discovered differential test binaries, so `0001`-`0070` are green SIMULTANEOUSLY; `0069`'s `rf:"UC"` is byte-identical (additivity). **(c) MET** — `test h2spec_pass_rate_gate ... ok` on CI; that test IS the gate (`PASS_RATE_GATE = 0.95` + the lockstep known-failure check), and `known-failures.txt` was **NOT trimmed** (`git diff d5b6dd4..HEAD` on it → 0 lines). Locally h2spec is absent → the runner takes its documented `eprintln!`-skip path, so (c) is CI-only by construction. **(d) MET** — no new fuzz target; `ci.yml` and `*fuzz*` diffs across `d5b6dd4..HEAD` are **empty**; CI's fuzz job (4 pre-existing targets) is success. **(e) MET** — `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check`, `cargo deny check` all **exit 0** locally (deny's 3 `unmatched license allowance` lines are pre-existing non-fatal warnings); `cargo test --workspace` is **green on CI** (0 failures). **Local `cargo test --workspace` REDs were adjudicated by RUNNING THE SUITE THREE TIMES and comparing the failing SET, not by trusting the state-3 report:** an **invariant core of exactly 5 targets** fails every run (`0070`/`0069`/`0062`/`0061` close-backend + `admin_config_dump_server_info`), while the **tail VARIES** (12 targets in run 2, 6 in run 3 with different membership) — a failing set whose membership changes across identical invocations of an unchanged tree is non-deterministic by construction (`differential-fixtures-flake-under-parallel-load`). Isolated re-runs prove the close-backend divergence is **entirely reference-side**: envoy-rust emits the exact target line while the reference container reports an IPv6 `Network is unreachable` connect-failure `rf:"UF"`, never reaching the backend (`tcpclosebackend-ipv6-unreachable-host-flake`). **A SIXTH target, `-p envoy-http2 --lib`, failed in run 1 only** — NOT in the state-3 adjudicated set, so it was investigated: its test name was **not captured** (run 1's output was piped through `tail`, truncating the `failures:` block — stated plainly, not guessed), and it did **not reproduce in 54 subsequent executions** (8× isolated, 40× the only network-dependent test `send_request_maps_h2_handshake_failure_to_typed_error`, 8× under a concurrent full-Docker differential load reproducing run 1's exact conditions), nor in workspace runs 2/3, nor on CI. Not a phase-65 regression; `systematic-debugging` was correctly NOT escalated to (no rerun re-failed the SAME test). **The `!retry_limit_exceeded_for_log_h2` guard's MUTATION CHECK was independently RE-RUN this session** (a state-3 report is not evidence): guard deleted → the negative backstop FAILED with exactly the predicted `rcd:"…{connection_termination}"` line; guard restored → `ok`, tree byte-identical. Red-green confirmed. `grep -rn "reset_for_log_h2" crates/ BEHAVIOR_CONTRACT.md` → no output. NO fixture was weakened; `known-failures.txt` untrimmed. `#![forbid(unsafe_code)]` holds. **NO new ADR fired** (**ADR-0123 stays reserved-but-UNFIRED** — no §6.2 reconciliation, no §A-§G fact overturned). **ONE non-blocking doc-precision finding was raised for the state-5 REVIEW** (see `## Notes`). **DECISIONS.md ledger head: ADR-0122** (count 122; next-available **ADR-0123**). ADR-0014 in force; ADR-0028 open; ADR-0049 governs config-validity. The next session is the **§5 state-5 code-review** (`superpowers:requesting-code-review`) — see `## Next expected skill`.

> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases incl. the phase-51 state-3/4/5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill


Per `BOOTSTRAP_PROMPT.md` §5 state 5 + `SKILL_ROUTING.md`: phase 65 (`65-accesslog-h2-rcd-upstream-reset`) is now **VERIFIED** (implementation complete, `PROGRESS.md` carries the state-4 §7.5 evidence, §7.5 (a)-(e) MET) but **NOT yet reviewed** (`REVIEW.md` absent) → the next session runs the **§5 state-5 code-review** (`superpowers:requesting-code-review`), producing `docs/envoy-rust/phases/65-accesslog-h2-rcd-upstream-reset/REVIEW.md`. Per the standing convention, dispatch a **fresh `general-purpose` subagent with NO prior session context**, hand it the full phase-65 commit range (`d5b6dd4..HEAD`) plus `SPEC.md`/`PLAN.md`/`PROGRESS.md`, and instruct it to independently re-derive every load-bearing claim **from the current source, not from commit messages**: the §A guarded rcd-set and its `!retry_limit_exceeded_for_log_h2` guard, the §B rcd-match arm + the 5-site `reset_for_log_h2` retirement (incl. `finalize_h2_stream`'s dropped parameter), the `0001`-`0069` additivity re-grep, the fixture `0070` per-side deltas, and the §E BEHAVIOR_CONTRACT edits. Per §5.2, if `REVIEW.md` comes back with issues the phase re-enters at **state 3** (NOT 4). Per §5.1, ONE state per session — do NOT also run the state-6 close-out (memory `closeout-and-pick-are-separate-sessions`).

**A doc-precision finding is ALREADY QUEUED for that review** (raised at state-4, deliberately not fixed there): `BEHAVIOR_CONTRACT.md`'s two touched rows disagree about when carry-forward **M56-1** closed — the `%RESPONSE_FLAGS%` row (`:1020`) ends *"…the boolean discriminator + its `finalize_h2_stream` parameter were RETIRED — **CLOSING carry-forward M56-1**"*, attaching the closure to phase 65, while the `%RESPONSE_CODE_DETAILS%` row (`:1031`), `STATE.md`, and the phase-64 close-out all say M56-1 closed at **phase 64**. Provenance: the `— **CLOSING carry-forward M56-1**` clause is **pre-existing phase-64 text** (verified present at `d5b6dd4`), where it attached to phase 64's own `UC` witness; phase 65's Task 5 inserted a sentence *between* that narrative and the clause, so the em-dash now binds to the phase-65 retirement. Cosmetic, zero behavioral impact, no `%RESPONSE_FLAGS%`/`%RESPONSE_CODE_DETAILS%` value affected — but the contract is the canonical reference for TODAY's rules (layout-invariant 5), so it belongs in `REVIEW.md` as a Minor. Full write-up in `PROGRESS.md` § *"Finding for the state-5 code-review"*.

**CI IS AUTHORITATIVE** (memory `envoy-rust-state4-ci-first-execution`). The state-4 gate was discharged against CI run **`28986078817`** (`headSha f66fe9c`, conclusion **success**, 145 `test result:` lines / 0 non-`ok`). Known LOCAL-RED expectations, all re-adjudicated at state-4 by running the workspace suite three times and comparing the failing set: an **invariant core** of `0061`/`0062`/`0069`/`0070` (close-backend — memories `tcpclosebackend-ipv6-unreachable-host-flake`, `differential-host-bridge-ip-192-168-65-2`) + `admin_config_dump_server_info`, plus a **varying tail** (`differential-fixtures-flake-under-parallel-load`). `cargo build -p envoy-bin` MUST be re-run before any local differential (memory `differential-harness-uses-debug-envoy-bin`). CI itself carries documented startup-race flakes (`xds-eds-hot-reload-admin-ready-startup-flake`, `eds-fatal-startup-test-port-reuse-flake`, `rds-no-rds-is-inert-startup-flake`, `upstream-h2-connection-pooling-backend-ready-flake`) → `gh run rerun <id> --failed` → green; escalate to `superpowers:systematic-debugging` ONLY if a rerun re-fails the SAME test deterministically. **NEVER weaken a fixture or trim `known-failures.txt` to make a local run green** (memory `h2spec-3-5-2-preface-host-sensitive`).

**Open carry-forward Minors (fold into the phase that next touches each surface; NONE blocks):** **M64-2** (the `hcm.rs:236` stale `synth_h2_502` mention, from the phase-64 REVIEW.md, cosmetic — lives in the SAME file phase 65 edits; phase 65 did NOT fold it in — the `hcm.rs:236` comment region was never touched, so M64-2 stays LIVE) + **M64-3** (the pre-existing idle-probe-connection inefficiency in `Http2CloseBackend`/`Http2EchoBackend`, from the phase-64 REVIEW.md, bounded/harmless) + **M57-1** (the `content-length` header omission on `synth_h2_no_healthy_upstream()` vs. its sibling `synth_h2_overflow()`, from the phase-57 REVIEW.md) + **M55-1** (the `hcm.rs:948`/`:7221` "M50-C deferred" stale comments IN `crates/envoy-http1/src/hcm.rs`; still live, unrelated) + **M53-2** (BEHAVIOR_CONTRACT `:36/:289/:296/:387` "reset 503" reads global but older doc prose still says synth-502 — add an "(H1)" qualifier; left live) + **M53-3** (the un-reconned `retry_on:reset`+exhausted URX+UC body-wording mismatch) + **M48-2** + **M42-1** + the `DC`/retry-budget-overflow slices of **M45-2** + the phase-58 candidate carry-forward (H2 request-budget arm's own differential fixture) + **M40-1** + **M39-1/M39-2** + **M38-1/M38-2** + **CF-39-1** + M37-* + M36-* + M34-* + M33-* + the empty-`metadata_match` doc-comment + M29-*/M30-* + the phase-31 cosmetics + the HTTP-filters-family (1)-(4). **[M64-1 CONSUMED by phase 65 (the §A rcd-set + §B/§F derive migration LANDED at the state-3 implementation; the differential witness is now DISCHARGED at the state-4 gate — fixture `0070` is `... ok` on CI run `28986078817` @ `f66fe9c`); M56-1 FULLY CLOSED by phase 64; M63-1 CONSUMED by phase 64; M50-C + M54-1 CONSUMED by phase 55; M53-1 CONSUMED by phase 54; M52-1 by phase 53; M52-2 + the `UF` slice of M45-2 + M51-1 by phase 52; the `URX` slice (H1) by phase 51; M45-1 by phase 56.]** **Sibling H1-perf-work phases 59/60/61-perf/62** exist on disk (disjoint surface, other sessions' concern) — not this thread's, unless STATE.md names one active.

> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases incl. the phase-37 state-1..5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit


**Phase-65 state-4 verification — run the full §7.5 (a)-(f) gate, quote every command's output into PROGRESS.md, advance STATE:** the §5 state-4 verification (`superpowers:verification-before-completion`). Cold-started clean (`HEAD` = `origin/main` = `f66fe9c`; `PLAN.md` + `PROGRESS.md` present + `REVIEW.md` absent → the state-4 detection rule; no sibling had advanced phase 65). **§7.5 (a)-(e) are MET; (f) is state-5.** The authoritative evidence is CI run **`28986078817`**, whose `headSha` is **`f66fe9c` — the exact tree under gate** (clean working tree), conclusion **success**: `access_log_h2_rcd_upstream_reset ... ok` (a), **145 `test result:` lines with ZERO non-`ok` and zero `FAILED`** across 70/70 differential fixtures (b), `h2spec_pass_rate_gate ... ok` (c — that test *is* the ≥95% gate plus the known-failure lockstep; `known-failures.txt` untrimmed), empty `ci.yml`/`*fuzz*` diffs + a green 4-target fuzz job (d). Locally build/clippy/fmt/deny all **exit 0** (e). **Local `cargo test --workspace` REDs were adjudicated by RUNNING THE SUITE THREE TIMES and comparing the failing SET rather than trusting the state-3 report:** an invariant core of 5 targets fails every run (the 4 close-backend fixtures + `admin_config_dump_server_info`) while the tail VARIES (12 targets, then 6, different membership) — non-deterministic by construction. Isolated re-runs show the close-backend divergence is **entirely reference-side** (envoy-rust emits the exact target line; real Envoy reports an IPv6 `Network is unreachable` connect-failure `UF`, never reaching the backend). **A sixth run-1 target, `-p envoy-http2 --lib`, was NOT in the adjudicated set** — its test name was not captured (run-1 output was truncated by `tail`), it did **not reproduce in 54 subsequent executions** (incl. 8 under the identical concurrent-Docker load and 40 of the only network-dependent test), and it is green on CI; reported honestly as an unreproduced load flake, not hand-waved. **The `!retry_limit_exceeded_for_log_h2` guard's mutation check was INDEPENDENTLY RE-RUN** (guard deleted → negative backstop FAILED exactly as predicted → restored → `ok`, tree byte-identical), so the guard is provably pinned. **THIS commit = PROGRESS.md (the `# State-4 verification` evidence section) + STATE + STATE_HISTORY + next-prompt.txt** (NO src/fixture/contract change — docs-only). Phase 65 stays `in-progress`. NO fixture weakened; `known-failures.txt` untrimmed. `#![forbid(unsafe_code)]` holds. **ONE non-blocking doc-precision Minor raised for state-5** (BEHAVIOR_CONTRACT's two rows disagree on when M56-1 closed; the clause is pre-existing phase-64 text that phase 65's insertion re-attached). **NO new ADR fired this session** (ADR-0122 governs the phase; **ADR-0123 reserved-but-UNFIRED**). **DECISIONS.md ledger head: ADR-0122** (count 122; next-available **ADR-0123**). ADR-0014 in force; ADR-0028 open. The next session is the §5 state-5 code-review (`superpowers:requesting-code-review`).

> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases incl. the phase-37 state-1..5 sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated


2026-07-09 (phase-65 **STATE-4 VERIFICATION COMPLETE — the full §7.5 gate ran; (a)-(e) MET, all outputs quoted verbatim into `PROGRESS.md`; the state-5 code-review is next.** The §5 state-4 verification [`superpowers:verification-before-completion`]. Cold-started clean [`HEAD` = `origin/main` = `f66fe9c`; `PLAN.md` + `PROGRESS.md` present + `REVIEW.md` absent → the state-4 detection rule; no sibling had advanced phase 65]. **Authoritative CI run `28986078817`, `headSha f66fe9c` — the EXACT tree under gate — conclusion success.** **(a)** `access_log_h2_rcd_upstream_reset ... ok` → fixture `0070` witnesses `upstream_reset_before_response_started{connection_termination}` byte-exact cross-proxy vs real Envoy v1.33.0. **(b)** 145 `test result:` lines, ZERO non-`ok`, zero `FAILED`; 70 fixtures / 70 differential test binaries → `0001`-`0070` green SIMULTANEOUSLY; `0069`'s `rf:"UC"` byte-identical. **(c)** `h2spec_pass_rate_gate ... ok` [the test asserts `>= 0.95` + the known-failure lockstep]; h2spec absent locally → documented `eprintln!`-skip, so (c) is CI-only; `known-failures.txt` NOT trimmed. **(d)** no new fuzz target — `ci.yml` + `*fuzz*` diffs empty across `d5b6dd4..HEAD`; CI fuzz job green. **(e)** build/clippy/fmt/deny exit 0 locally [deny's 3 `unmatched license allowance` lines are pre-existing non-fatal warnings]; `cargo test --workspace` green on CI. **Local REDs adjudicated EMPIRICALLY, not by trusting the state-3 report:** the workspace suite was run THREE times — an invariant core of 5 targets fails every run [4 close-backend fixtures + `admin_config_dump_server_info`], the tail VARIES [12 then 6, different membership] ⇒ non-deterministic. Isolated re-runs prove the close-backend divergence is **entirely reference-side** [envoy-rust emits the exact target line; real Envoy reports IPv6 `Network is unreachable`/`UF`]. **A sixth run-1 target `-p envoy-http2 --lib` was investigated, not waved through:** name uncaptured [run-1 output truncated by `tail`], unreproduced across **54** executions incl. 8 under identical concurrent-Docker load, green on CI ⇒ load flake, not a regression; `systematic-debugging` correctly not escalated to. **The guard mutation check was INDEPENDENTLY RE-RUN** [deleted → FAIL as predicted → restored → ok; tree byte-identical]. **NO fixture weakened.** `#![forbid(unsafe_code)]` holds. **ONE non-blocking doc-precision Minor queued for state-5** [BEHAVIOR_CONTRACT `:1020` vs `:1031` disagree on when M56-1 closed; the clause is pre-existing phase-64 text re-attached by phase 65's insertion]. **NO new ADR fired** [**ADR-0123 stays reserved-but-UNFIRED**]. **DECISIONS.md ledger head: ADR-0122** [count 122; next-available ADR-0123]. ADR-0014 in force; ADR-0028 open. The next session is the §5 state-5 code-review [`superpowers:requesting-code-review`].)


> Historical `## Last updated` notes — every superseded last-updated note (all closed phases incl. the phase-37 state-1..5 sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsection for fully-closed phase 64 (the `### Phase-64 state-5 code-review` narrative — phase 64 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-64 state-6 close-out when ROADMAP row `64` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 63 (the `### Phase-63 state-5 code-review` narrative — phase 63 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-63 state-6 close-out when ROADMAP row `63` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 61 (the `### Phase-61 state-1 brainstorm` / `### Phase-61 state-2 PLAN-write` / `### Phase-61 state-3 implementation` / `### Phase-61 state-4 verification` / `### Phase-61 state-5 code-review` narratives, relocated at the phase-61 state-6 close-out when ROADMAP row `61` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 58 (the `### Phase-58 state-1 brainstorm` / `### Phase-58 state-2 PLAN-write` / `### Phase-58 state-3 implementation` / `### Phase-58 state-4 verification` / `### Phase-58 state-5 code-review` narratives, relocated at the phase-58 state-6 close-out when ROADMAP row `58` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 57 (the `### Phase-57 state-1 brainstorm` / `### Phase-57 state-2 PLAN-write` / `### Phase-57 state-3 implementation` / `### Phase-57 state-4 verification` / `### Phase-57 state-5 code-review` narratives, relocated at the phase-57 state-6 close-out when ROADMAP row `57` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 42 (the `### Phase-42 state-5 code-review (active phase)` narrative — phase 42 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-42 state-6 close-out when ROADMAP row `42` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 43 (the `### Phase-43 state-5 code-review (active phase)` narrative — phase 43 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-43 state-6 close-out when ROADMAP row `43` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 44 (the `### Phase-44 state-5 code-review (active phase)` narrative — phase 44 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-44 state-6 close-out when ROADMAP row `44` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 45 (the `### Phase-45 state-5 code-review (active phase)` narrative — phase 45 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-45 state-6 close-out when ROADMAP row `45` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 46 (the `### Phase-46 state-5 code-review (active phase)` narrative — phase 46 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-46 state-6 close-out when ROADMAP row `46` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 47 (the `### Phase-47 state-5 code-review (active phase)` narrative — phase 47 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-47 state-6 close-out when ROADMAP row `47` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 48 (the `### Phase-48 state-5 code-review (active phase)` narrative — phase 48 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-48 state-6 close-out when ROADMAP row `48` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 49 (the `### Phase-49 state-5 code-review (active phase)` narrative — phase 49 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-49 state-6 close-out when ROADMAP row `49` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 50 (the `### Phase-50 state-3 implementation (active phase)` narrative — phase 50 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-50 state-6 close-out when ROADMAP row `50` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 51 (the `### Phase-51 state-3 implementation (active phase)` narrative — phase 51 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-51 state-6 close-out when ROADMAP row `51` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 53 (the `### Phase-53 state-1 brainstorm` narrative — phase 53 kept a single active-phase Notes subsection at "state-1 brainstorm" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-1 subsection remained in `## Notes` at close — relocated at the phase-53 state-6 close-out when ROADMAP row `53` flipped `done`, together with the inline superseded state-3 narrative) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 54 (the `### Phase-54 state-1 brainstorm` / `### Phase-54 state-2 PLAN-write` / `### Phase-54 state-3 implementation` / `### Phase-54 state-4 verification` / `### Phase-54 state-5 code-review` narratives, relocated at the phase-54 state-6 close-out when ROADMAP row `54` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 55 (the `### Phase-55 state-5 code-review (active phase)` narrative — phase 55 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state individually superseded in place and relocated to STATE_HISTORY.md at its own transition], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-55 state-6 close-out when ROADMAP row `55` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 56 (the `### Phase-56 state-5 code-review (active phase)` narrative — phase 56 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state individually superseded in place and relocated to STATE_HISTORY.md at its own transition], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-56 state-6 close-out when ROADMAP row `56` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: one state per session; do not chain states. Phase 65 (`65-accesslog-h2-rcd-upstream-reset`) is ACTIVE at **state-4 verification COMPLETE** (THIS session ran the full §7.5 gate; (a)-(e) MET, evidence quoted into `PROGRESS.md`). The next session runs the §5 state-5 code-review (`superpowers:requesting-code-review`, producing `REVIEW.md`) — do NOT also run the state-6 close-out in that session.

### Phase-65 state-4 verification (active phase)

- **Pick (ADR-0122): consume carry-forward M64-1** — witness the deterministic H2 upstream-reset `%RESPONSE_CODE_DETAILS%` string `upstream_reset_before_response_started{connection_termination}` byte-exact, and migrate the H2 `UC` `%RESPONSE_FLAGS%` derivation from the phase-64 `reset_for_log_h2` boolean onto the now-unique rcd (retiring the boolean). The H2 analogue of phase 54 (ADR-0111), which did EXACTLY this for the H1 side (consuming M53-1).
- **STATE-4 COMPLETE — the full §7.5 gate ran; (a)-(e) MET, every command's output quoted verbatim into `PROGRESS.md`'s `# State-4 verification` section.** (f) `REVIEW.md` is state-5, deliberately not run (§5.1). The authoritative evidence is **CI run `28986078817`, `headSha f66fe9c` — the EXACT tree under gate** (clean working tree), conclusion **success**: `access_log_h2_rcd_upstream_reset ... ok` (a); **145 `test result:` lines, ZERO non-`ok`, zero `FAILED`** across 70/70 fixtures ⇒ `0001`-`0070` green SIMULTANEOUSLY, `0069` byte-identical (b); `h2spec_pass_rate_gate ... ok` — that test IS the ≥95% gate plus the known-failure lockstep, and `known-failures.txt` was NOT trimmed (c); empty `ci.yml`/`*fuzz*` diffs + a green 4-target fuzz job (d); build/clippy/fmt/deny exit 0 locally, `cargo test --workspace` green on CI (e). h2spec is absent locally → the runner takes its documented `eprintln!`-skip, so (c) is **CI-only by construction**.
- **Prior-session reports were treated as claims, not evidence — two things were RE-VERIFIED from scratch.** (i) **The guard mutation check was independently RE-RUN:** ` && !retry_limit_exceeded_for_log_h2` deleted → `h2_retry_exhausted_reset_keeps_via_upstream_rcd_and_renders_urx` FAILED with exactly the predicted `rcd:"upstream_reset_before_response_started{connection_termination}"` line; guard restored → `ok`; `git diff --stat` empty. The negative backstop provably pins the guard and is not vacuous. (ii) **The local-RED set was re-derived by running `cargo test --workspace` THREE times and comparing the failing SET**, rather than inheriting the state-3 adjudication.
- **The local-RED adjudication, empirically grounded.** An **invariant core of exactly 5 targets** fails in every run — `0070`/`0069` (H2 close-backend), `0062`/`0061` (H1 close-backend), `admin_config_dump_server_info`. The **tail VARIES**: 12 targets in run 2 (adding `access_log_h2_urx_retry_exhausted`, `access_log_json_nested`, `access_log_response_code_details`, `http_filter_jwt_authn`, `rbac_url_path`, `upstream_active_health_check`, `upstream_outlier_detection_consecutive_5xx`), 6 in run 3 (adding only `access_log_rf_no_route`). A failing set whose membership changes across identical invocations of an unchanged tree is **non-deterministic by construction** (`differential-fixtures-flake-under-parallel-load`); every varying target is `... ok` on CI. Isolated re-runs show the close-backend divergence is **entirely reference-side**: envoy-rust emits the exact target line, while the reference Envoy container reports an IPv6 (`fdc4:…`) `Network is unreachable` connect-failure with `rf:"UF"` — it never completed a handshake, so it never observed a reset (`tcpclosebackend-ipv6-unreachable-host-flake`; the OS-derived rcd text proves it is the connect-failure family, M45-2, not the reset path).
- **The sixth run-1 target `-p envoy-http2 --lib` — reported HONESTLY, not waved through.** It is NOT in the state-3 adjudicated set. **Its test name was never captured** (run 1's output was piped through `tail`, truncating the `failures:` block) — stated plainly rather than guessed. It did **not reproduce in 54 subsequent executions**: 8× isolated (`88 passed; 0 failed`), 40× the crate's only network-dependent test `send_request_maps_h2_handshake_failure_to_typed_error` (the documented `envoyrust-h2-handshake-test-host-flake`) → 0/40, and 8× **while a full `cargo test -p differential` Docker run saturated the host**, reproducing run 1's exact load conditions. Workspace runs 2 and 3 and CI are all green. Adjudication: a load-induced non-deterministic failure of the `envoy-http2` unit suite, not a phase-65 regression — both phase-65-authored backstops pass in all 54 runs. `superpowers:systematic-debugging` was correctly NOT escalated to (escalation requires a rerun to re-fail the SAME test deterministically; no rerun failed at all).
- **The `crates/` change is a NET SIMPLIFICATION:** one guarded rcd-set added; the `reset_for_log_h2` boolean removed at all 5 sites (decl, post-loop set, call-site arg, `finalize_h2_stream` parameter, derive branch) together with its 3 comment blocks; one rcd-match arm added. `finalize_h2_stream` now takes two booleans, not three. Both protocols now share the identical derivation split: `{NR, UH, UO, UC}` rcd-derived, `{URX, UF}` boolean-derived.
- **Load-bearing additivity — CONFIRMED EMPIRICALLY, not merely argued:** fixture `0069`'s subject-side access-log line is byte-identical post-migration (`{"method":"GET","proto":"HTTP/2","rc":503,"rf":"UC"}`); `UC` now arrives via the rcd-match instead of the retired boolean. Fixture `0070`'s subject side emits the exact target line, keys in UTF-8 byte order per ADR-0094 §A (confirming PLAN-VERIFY §3.7's key-order correction).
- **§F sweep discipline — one documented deviation from PLAN Task 6 Step 2.** That step's `grep -rn "reset_for_log_h2" crates/ tests/ …` → "no output" is unsatisfiable alongside PLAN Task 3 Step 2's own prescribed comments, which spell the identifier. Resolved by wording the new comments as "the phase-64 **boolean discriminator** was RETIRED" (matching the wording PLAN Task 5 already prescribes for `BEHAVIOR_CONTRACT.md`). The one surviving repo-wide hit — `tests/fixtures/0069-…/README.md:18` — is **backward-looking phase-64 historical narrative**, which SPEC §F explicitly orders left verbatim (D-3.4/D-3.5 no-retroactive-rewrite); only its stale ACTIVE-state claim (M64-1 "still open") was corrected, additively. `crates/` + `BEHAVIOR_CONTRACT.md` are fully swept. **No §6.2 ADR fired** — a PLAN-step precision issue, not an overturned §A-§G fact.
- **`admin_config_dump_server_info` remains adjudicated environmental** — it fails with the `backend::192.168.65.2:<port>::*` per-endpoint-counter signature of memory `differential-host-bridge-ip-192-168-65-2`, was **proven pre-existing at the pre-phase commit `d5b6dd4` in a detached `git worktree`** at state-3, and is `... ok` on CI at `f66fe9c`. NO fixture was weakened; `known-failures.txt` untrimmed (memory `h2spec-3-5-2-preface-host-sensitive`).
- **ONE non-blocking doc-precision finding, RAISED for the state-5 REVIEW and deliberately NOT fixed at state-4** (§5.2: fixes re-enter at state-3; §5.1 forbids chaining states). `BEHAVIOR_CONTRACT.md`'s two touched rows disagree about when carry-forward **M56-1** closed: the `%RESPONSE_FLAGS%` row (`:1020`) ends *"…the boolean discriminator + its `finalize_h2_stream` parameter were RETIRED — **CLOSING carry-forward M56-1**"* (attaching closure to phase 65), while the `%RESPONSE_CODE_DETAILS%` row (`:1031`), this file, and the phase-64 close-out all say M56-1 closed at **phase 64**. **Provenance established, not assumed:** the `— **CLOSING carry-forward M56-1**` clause is **pre-existing phase-64 text** (verified present at `d5b6dd4`), where it attached to phase 64's own `UC` witness; phase 65's Task 5 inserted a sentence *between* that narrative and the clause, so the em-dash now binds to the phase-65 retirement. A grammatical re-attachment, not a new claim — cosmetic, zero behavioral impact, no `%RESPONSE_FLAGS%`/`%RESPONSE_CODE_DETAILS%` value affected. It belongs in `REVIEW.md` as a Minor because the contract is the canonical reference for TODAY's rules (layout-invariant 5).
- **ADR-0123 stays reserved-but-UNFIRED** (no §6.2 reconciliation fired; no §A-§G fact overturned). NO new `Op`/`AccessLogRecord` field/crate/dependency/`ConfigError` variant/test-harness/`ci.yml` change. `#![forbid(unsafe_code)]` holds. **NO `%RESPONSE_FLAGS%` value changed** — the witnessed H2 flag stays `UC`; only its DERIVATION moved. `grep -rn "reset_for_log_h2" crates/ docs/envoy-rust/BEHAVIOR_CONTRACT.md` → no output.
- **Next session = §5 state-5 code-review** (`superpowers:requesting-code-review`, producing `REVIEW.md`; dispatch a fresh no-context `general-purpose` subagent over `d5b6dd4..HEAD`). Per §5.1 one state per session — the code-review was deliberately NOT run this session, and the state-6 close-out is the session after (memory `closeout-and-pick-are-separate-sessions`).


> Historical Notes subsection for fully-closed phase 52 (the `### Phase-52 state-5 code-review (active phase)` narrative — phase 52 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2/3/4 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-52 state-6 close-out when ROADMAP row `52` flipped `done`, together with the standing 05.1/05.4 future-reviewer note it carried) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 41 (the `### Phase-41 state-5 code-review (active phase)` narrative — phase 41 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the §6.2-recon PIVOT from `%REQ_WITHOUT_QUERY%` to `%ROUTE_NAME%` landed at state-2; the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-41 state-6 close-out when ROADMAP row `41` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 40 (the `### Phase-40 state-5 code-review (active phase)` narrative — phase 40 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-40 state-6 close-out when ROADMAP row `40` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 39 (the `### Phase-39 state-5 code-review (active phase)` narrative — phase 39 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-39 state-6 close-out when ROADMAP row `39` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
> Historical Notes subsection for fully-closed phase 38 (the `### Phase-38 state-2 PLAN-write (active phase)` narrative — phase 38 used a single active-phase Notes subsection for its whole arc; the state-3/4/5 sub-state narratives were superseded in the four-top-section archive blocks per-session, so only this state-2 subsection remained in `## Notes` at close — relocated at the phase-38 state-6 close-out when ROADMAP row `38` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) and for parent phase 25 (the `### Phase-25 state-1 brainstorm` pick + recon-finding narrative, relocated at the 25.2 state-6 close-out when parent `25` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 26 (the `### Phase-26 state-1 brainstorm` pivot/rejected-alternatives/key-scoping narrative, relocated at the phase-26 state-6 close-out when row `26` flipped to `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 27 (the `### Phase-27 state-1 brainstorm` / `### Phase-27 state-2 PLAN-write` / `### Phase-27 state-4 verification` narratives + the now-consumed `### Phase-27 carry-forwards` [M26-1..M26-8] block, relocated at the phase-27 state-6 close-out when row `27` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 28 (the `### Phase-28 state-1 brainstorm` / `### Phase-28 state-2 PLAN-write` / `### Phase-28 state-3 implementation + state-4 verification` / `### Phase-28 state-5 code review` narratives + the now-consumed `### Phase-28 carry-forwards` [M27-1..M27-3] block, relocated at the phase-28 state-6 close-out when row `28` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 29 (the now-consumed `### Phase-29 carry-forwards` [M28-1..M28-3] block + the `### Phase-29 state-1 brainstorm` / `### Phase-29 state-2 PLAN-write` / `### Phase-29 state-3 implementation` / `### Phase-29 state-4 verification` / `### Phase-29 state-5 code review` narratives, relocated at the phase-29 state-6 close-out when row `29` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 30 (the `### Phase-30 carry-forwards` [M29-1/M29-2, which fed phase 30 but were NOT consumed — they continue as phase-31 carry-forwards] block + the `### Phase-30 state-1 brainstorm` / `### Phase-30 state-2 PLAN-write` / `### Phase-30 state-3 implementation` / `### Phase-30 state-4 verification` / `### Phase-30 state-5 code review` narratives, relocated at the phase-30 state-6 close-out when row `30` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 31 (the `### Phase-31 carry-forwards` [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 — open Minors from the phase-30 REVIEW.md that fed phase 31 but were NOT consumed; they continue as carry-forwards for the next phase that touches the differential hash-sweep driver / the config parser] block + the `### Phase-31 state-1 brainstorm` / `### Phase-31 state-2 PLAN-write` / `### Phase-31 state-3 implementation` / `### Phase-31 state-4 verification` / `### Phase-31 state-5 code review` narratives, relocated at the phase-31 state-6 close-out when row `31` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 32 (the `### Phase-32 carry-forwards` block — the open Minors that fed phase 32 but were NOT consumed: the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3; they continue as carry-forwards for the future phase that touches their surface — re-listed in `## Next expected skill` above alongside the 6 new phase-32 REVIEW.md Minors M32-1…M32-6), relocated at the phase-32 state-6 close-out when row `32` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phases 33+34 (the `### Phase-33 carry-forwards` block — the active-phase carry-forwards Notes that lived through phases 33 and 34: the now-CONSUMED 6 phase-32 REVIEW.md Minors M32-1…M32-6 [folded + landed at the phase-33 state-3] + the "Other still-live carry-forwards" list [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3 + the HTTP-filters-family (1)-(4) — those still-live ones re-listed in `## Next expected skill` above alongside the new phase-34 Minors M34-1/M34-2/M34-3 + the phase-33 M33-1/M33-2]), relocated at the phase-34 state-6 close-out when row `34` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 35 (the `### Phase-35 state-1 brainstorm` / `### Phase-35 state-2 PLAN-write` / `### Phase-35 state-3 implementation` / `### Phase-35 state-4 verification` / `### Phase-35 state-5 code-review` narratives, relocated at the phase-35 state-6 close-out when ROADMAP row `35` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 36 (the `### Phase-36 state-5 code-review` narrative — phase 36 used a rename-in-place Notes discipline, so the state-1..4 sub-state narratives were superseded in place each session and only the four top-section blocks were relocated per-session — plus the now-CLOSED detailed M35-1 carry-forward bullet [CONSUMED by phase-36 F2], relocated at the phase-36 state-6 close-out when ROADMAP row `36` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 37 (the `### Phase-37 state-3 implementation` active-phase narrative, relocated at the phase-37 state-6 close-out when ROADMAP row `37` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### HTTP-filters-family carry-forwards (from the `25.2` REVIEW.md - NOT yet consumed; weigh whenever the HTTP-filters family is re-entered)

> These were never obligations on the xDS phase 26; they remain live for whenever an HTTP-filters-family phase resumes.

- **(1) [non-goal - architectural]** Over-limit request bodies are FULLY buffered before the 413 rejection (no streaming watermark). Documented deferred non-goal; differentially byte-identical to Envoy for the bounded fixture sizes. Revisit only if a streaming `decode_data` watermark path is ever planned.
- **(2) [doc precision]** The BEHAVIOR_CONTRACT 413-row "verified byte-exact against v1.33.0" phrasing - fixture `0033` is H1-only; the H2 over-limit path is covered by the in-process synth-decorator backstop, NOT differentially. Consider narrowing the phrasing if an H2 over-limit fixture is ever added.
- **(3) [coverage]** No standalone `== effective route limit` unit assertion (the boundary is exercised only via the over/under probes).
- **(4) [coverage]** No differential at-limit (`==`) probe in `0033` (within-limit `<` and over-limit `>` are both covered; the exact boundary is not differentially probed).
- _(2)-(4) are cheap polish, (1) is architectural and only relevant to a future streaming phase._
