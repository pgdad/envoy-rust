# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase


**id:** **66-network-filter-direct-response** (ROADMAP row `66`, `in-progress`) — the **Network-filters family OPENER**, the first row that family has ever had. Phase 66 implements `envoy.filters.network.direct_response`, differentially witnesses it **byte-exact** via a NEW fixture `0071` driven by a NEW `Driver::TcpDirectResponse` (the harness's first read-to-EOF raw-TCP driver), and closes the project's network-filter **terminal-validation** gap. Pick + scope locked by **ADR-0123**; the §6.2 V-3 reconciliation locked by **ADR-0124**. **STATE-5 CODE-REVIEW COMPLETE this session — `REVIEW.md` is APPROVED (0 Critical / 0 Important / 5 Minor).** The §5 state-6 close-out is the next session — see `## Next expected skill`.
**slug:** network-filter-direct-response
**directory:** `docs/envoy-rust/phases/66-network-filter-direct-response/` (`SPEC.md` + `PLAN.md` + `PROGRESS.md` + **`REVIEW.md`** all present; `REVIEW.md` states `**Status: APPROVED**` on its first content line — the §5 state-6 detection rule).

**status:** **PHASE 66 STATE-5 CODE-REVIEW COMPLETE — `REVIEW.md` is APPROVED (0 Critical / 0 Important / 5 Minor); the state-6 close-out is next.** This session ran the §5 state-5 code-review via `superpowers:requesting-code-review`. Cold-started fully per `BOOTSTRAP_PROMPT.md` §1; confirmed `git status --porcelain` clean, branch `main`, `HEAD` = `origin/main` = `ba2cb9e` (the phase-66 state-4 verification, unmoved — `git fetch origin --prune` showed `0 0` ahead/behind and no sibling had written `REVIEW.md`), and confirmed the §5 state-5 detection rule. **NO production code changed this session — the code-review is read-only over the tree** (re-confirmed after every subagent returned: `git status --porcelain` empty, `HEAD` unmoved). **The §7.5 gate was NOT re-run** — it ran at state-4 and PASSES, with every command's output quoted verbatim in `PROGRESS.md` §"§5 state-4 VERIFICATION". Gate **(f) is now SATISFIED**: `REVIEW.md` is APPROVED. **Method:** FOUR fresh `general-purpose` subagents with NO prior session context were dispatched concurrently over DISJOINT slices of the review range `5e3afb9..ba2cb9e` (15 commits, 20 files, +1938/−44) — (1) the `direct_response.rs` data plane + the `main.rs` dispatch arm; (2) `envoy-config`'s schema + validate arm + terminal pre-pass; (3) the `Driver::TcpDirectResponse` driver + fixture `0071`; (4) the out-of-PLAN `upstream_h2_connection_pooling.rs` change + the fuzz seed + `BEHAVIOR_CONTRACT.md` + `ROADMAP.md`. Each was handed `SPEC.md`/`PLAN.md`/the governing ADRs, was **explicitly forbidden from re-litigating the six settled decisions** (the ADR-0124 drain + its mutation check; the `echo` `typed_config` asymmetry; the `DirectResponseConfig`-vs-route-level-`DirectResponse` naming; the `is_terminal` predicate over a `len <= 1` check; the immutable pre-pass ordering; BLOCK-66-1) while permitted to critique HOW each is implemented, and each was given an adversarial question designed to fail if the code were merely plausible. **All four answered "Ready to merge? Yes"** independently. **Dispatcher-side re-verification (agent reports are NOT evidence)** — every load-bearing claim was independently re-run by this session: (i) `direct_response::serve` (`direct_response.rs:36-74`) is **structurally identical** to `echo::serve` (`echo.rs:21-59`) — both files read in full; (ii) ADR-0126's fix is intact (`--quiet` survives only in a comment at `:98`, `PREBUILD_TIMEOUT = 240s` at `:80`, the 30s readiness budget intact at `:247`); (iii) **M66-2's four latent siblings CONFIRMED and SHARPENED** — all four not only nest a `cargo run --manifest-path`, they **also still pass `--quiet` under the same 30s budget**; (iv) an empty network `filters: []` chain **is** accepted (the only `filters.is_empty()` guard, `bootstrap.rs:3618`, is `validate_http_filters`, i.e. HTTP filters INSIDE HCM). **One reviewer filed the `JoinSet`-non-reaping finding as Important; it is recorded verbatim and DOWNGRADED to Minor (M66-3) with reasons stated, not silently softened** — `serve()` is structurally identical to `echo.rs`'s, so it is a property of the pinned structural model, not code phase 66 authored; it has zero differential observable; and fixing it in `direct_response.rs` alone would break the "echo is the model" invariant. The correct unit of repair is both files together. **§5.2 disposition: the phase does NOT re-enter state 3** — 0 Critical / 0 Important, per the phase-64 (0/0/2 Minor) and phase-65 (0/0/1 Minor) precedents, both of which closed at state-6 without re-entry. **No Minor touches a fixture, a logged value, an upstream-Envoy observable, or the §7.5 gate.** **Reviewer strengths worth carrying forward:** the terminal pre-pass was proven **complete**, including the path nobody had checked — the listener loop chains static AND dynamic listeners (`bootstrap.rs:2967-2969`) and `load_dynamic_resources` (`lib.rs:844`) re-invokes `bootstrap::validate` (`lib.rs:1053`), so **LDS/xDS-loaded listeners get the terminal check too**; fixture `0071` is a **non-vacuous** witness (an empty-vs-empty `byte_exact` compare WOULD pass vacuously, but the fixture configures a non-empty 39-byte payload the authoritative upstream reference deterministically emits); the `main.rs` `anyhow::bail!("validator guarantees…")` invariant **is** actually guaranteed, and the `filters.first()`-only read is safe **because** terminal validation makes any ≥2-filter chain invalid — the two halves of the phase interlock; the out-of-PLAN test change **did not weaken the test** (30s budget unchanged, timeout still fatal, stderr read genuinely bounded, child reaped via `kill_on_drop`); and **all four `BEHAVIOR_CONTRACT.md` clauses match the code** (no clause overstates the implementation). **NEW carry-forwards opened by `REVIEW.md`: M66-3** (the `JoinSet` never reaps during normal operation + the drain's unbounded per-connection read — **PRE-EXISTING, shared verbatim with `echo.rs`**; repair both files together), **M66-4** (a doc-precision overstatement at `direct_response.rs:93-94`: the drain is bounded only ON SHUTDOWN, not per-connection), **M66-5** (two **unwitnessed** network-chain config edges: an empty `filters: []` chain is accepted, and `response: {}` is rejected — per D-3.3 this review records envoy-rust's behavior and **declines to assert what Envoy does**, since neither was probed), **M66-6** (test-coverage gaps, chiefly **no dynamic/LDS-listener terminal test** — the phase's most load-bearing property currently rests on manual tracing), **M66-7** (cosmetics). **M66-2 stays OPEN, description SHARPENED** (all four siblings also pass `--quiet`; a single mechanical sweep neutralizes the class). **Numbering note: M66-1 was never allocated** — ADR-0126 opened `M66-2` directly; the ledger advances monotonically and does not backfill. **Carry-forward disposition: phase 66 CONSUMES none**; CF-66-1, CF-66-2, M66-2 and the five new M66-3…M66-7 all stay OPEN; every pre-existing Minor stays LIVE and **NONE blocks**. **No fixture was weakened; `known-failures.txt` was not trimmed; the ADR-0124 drain and its mutation check were not re-litigated or deleted; the `echo` `typed_config` asymmetry was not "fixed"; BLOCK-66-1 was not re-opened.** `#![forbid(unsafe_code)]` holds (D-3.8). The **§6.1 split gate does not apply** at state-5. **DECISIONS.md ledger head: ADR-0126** (unchanged — **no ADR was written this session**; none was needed, and the M66-2 sharpening is a carry-forward description update, NOT an amendment to the append-only ADR-0126, which it confirms rather than contradicts); next-available **ADR-0127**, unreserved. ADR-0123 REFINED by ADR-0124, not superseded; ADR-0014 in force; ADR-0028 open; ADR-0049 governs config-validity; ADR-0006/ADR-0007 remain the reason a NEW driver — rather than a widened `TcpEcho` — is correct.

> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases incl. the phase-51 state-3/4/5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill


Per `BOOTSTRAP_PROMPT.md` §5 state 6 + `SKILL_ROUTING.md`: phase 66 (`66-network-filter-direct-response`) is **REVIEWED AND APPROVED** — `SPEC.md` + `PLAN.md` + `PROGRESS.md` + `REVIEW.md` all present, and `REVIEW.md`'s first content line reads `**Status: APPROVED**` (0 Critical / 0 Important / 5 Minor) → the next session runs the **§5 state-6 CLOSE-OUT**. Per §5.1, ONE state per session.

**The state-6 close-out, concretely:**
- Flip ROADMAP row `66` `in-progress` → `done` (append to its `summary` cell; **escape any new `|` as `\|`** — memory `roadmap-rows-need-escaped-pipes`).
- Relocate the `### Phase-66 state-5 code-review (active phase)` `## Notes` subsection to `STATE_HISTORY.md` per ADR-0035, using the **DELTA-BASED** byte-preservation check (memory `state6-relocation-check-must-be-delta-based`): the prescribed "no relocated line may be duplicated" check FALSE-FAILS, because boilerplate (the `**slug:**` line, etc.) recurs once per archived block. Compare occurrence COUNTS before/after (delta == +1) and allowlist the `### Doctrine reminders` §5.1 bullet and the `### Phase-66` heading.
- `STATE.md` → "awaiting next planning".
- **Do NOT chain into the next-phase pick.** Per memory `closeout-and-pick-are-separate-sessions`, the state-0/1 pick is a SEPARATE session after the close-out, even if a handoff prompt bundles them.

**Do NOT re-run the §7.5 gate.** It ran at state-4 and PASSES; gate (f) was satisfied at state-5. Evidence is quoted verbatim in `PROGRESS.md` §"§5 state-4 VERIFICATION". `cargo test --workspace` costs ~12 min and exits 101 on this host for already-adjudicated environmental reasons.

**Open carry-forward Minors (fold into the phase that next touches each surface; NONE blocks).** **NEW, opened by the phase-66 `REVIEW.md`:** **M66-3** (`serve()` never reaps completed `JoinSet` tasks during normal operation, and the drain's per-connection `read()` is unbounded — **PRE-EXISTING, shared verbatim with `echo.rs`**; repair `echo.rs` + `direct_response.rs` TOGETHER or the "echo is the model" invariant breaks) + **M66-4** (`direct_response.rs:93-94` doc-precision: the drain is bounded only ON SHUTDOWN, not per-connection) + **M66-5** (two **UNWITNESSED** network-chain config edges — an empty `filters: []` chain is accepted; `response: {}` is rejected — envoy-rust's behavior is recorded and Envoy's is deliberately NOT asserted, per D-3.3; probe the pinned image with `--mode validate` before matching or recording a divergence) + **M66-6** (test-coverage: chiefly **no dynamic/LDS-listener terminal test** — the phase's most load-bearing property rests on manual tracing; also no 3-filter chain, no multi-chain/multi-listener case, no concurrent-connection or large-payload `direct_response` test) + **M66-7** (cosmetics: the differential runner lacks its `//!` header; the `drive_tcp_direct_response` timeout `bail!` omits `out.len()`). **M66-1 was never allocated** — ADR-0126 opened `M66-2` directly; the ledger does not backfill.

**M66-2 stays OPEN, description SHARPENED by the review:** the four latent compile-inside-a-readiness-budget siblings (`upstream_connection_pooling`, `upstream_active_health_check`, `upstream_outlier_detection`, `tests/differential/src/backend.rs`) not only nest a `cargo run --manifest-path` — **all four also still pass `--quiet` under the same 30s readiness budget**, so all four remain silent-on-stall. `--quiet`, not the stdio posture, is the silencing factor. A single mechanical sweep (hoist each build out of the budget; drop `--quiet`) neutralizes the class. This is a carry-forward description update, **NOT** an amendment to the append-only ADR-0126, which it confirms.

**Opened by ADR-0123 §2.2:** **CF-66-1** (`inline_bytes`/`filename` `DataSource` arms unsupported, loudly rejected) + **CF-66-2** (no generic network-filter chain iteration protocol; M66-3 and M66-5 are natural companions). **Still LIVE:** **M64-2** (the `crates/envoy-http2/src/hcm.rs:236` stale `synth_h2_502` mention) + **M64-3** + **M65-1** + **M57-1** + **M55-1** + **M53-2** + **M53-3** + **M48-2** + **M42-1** + the `DC`/retry-budget-overflow slices of **M45-2** + the phase-58 candidate carry-forward + **M40-1** + **M39-1/M39-2** + **M38-1/M38-2** + **CF-39-1** + M37-* + M36-* + M34-* + M33-* + the empty-`metadata_match` doc-comment + M29-*/M30-* + the phase-31 cosmetics + the HTTP-filters-family (1)-(4). **Phase 66 CONSUMES none of these.** **Pre-existing ROADMAP cosmetic defect (do NOT "fix"):** rows `36`/`38`/`39`/`52`/`54` carry unescaped `|` characters inside their landed `summary` cells; ROADMAP is append-only and D-3.5 preserves landed summaries verbatim.

**Do NOT re-litigate the drain** (ADR-0124), **do NOT "fix" the `echo` `typed_config` asymmetry** (ADR-0014), **do NOT re-open BLOCK-66-1** (ADR-0126), **never weaken a fixture, and never trim `tests/conformance/h2spec/known-failures.txt`** (memory `h2spec-3-5-2-preface-host-sensitive` — local h2spec scores invalid-preface 3.5/2 as PASS while CI fails it). Never pipe a verification run through `tail` (memory `never-pipe-verification-runs-through-tail`). Confirm CI with the FULL 40-char SHA (memory `gh-run-list-commit-needs-full-sha`); a run whose jobs are `cancelled` with `runner_name:""` and `steps:0` is runner starvation, not a code failure.

**Mission is NOT complete** — the rest of the Network filters family (redis, mongo, kafka_broker, thrift, zookeeper, sni_cluster, rbac network) plus HTTP filters, load balancing, upstream robustness, HTTP/3+QUIC, gRPC, xDS/dynamic config, observability, runtime/hot-restart, and the WASM host remain unbuilt. **Do NOT create a `stop` file.**

> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases incl. the phase-37 state-1..5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit

**Phase-66 state-5 code-review — `REVIEW.md` lands APPROVED (0 Critical / 0 Important / 5 Minor); §7.5 gate (f) is SATISFIED:** the §5 state-5 code-review (`superpowers:requesting-code-review`). Cold-started clean (`git status --porcelain` empty; branch `main`; `HEAD` = `origin/main` = `ba2cb9e`, the phase-66 state-4 verification, unmoved; `git fetch origin --prune` → `0 0` ahead/behind, no sibling had written `REVIEW.md`). **NO production code changed — the review is read-only over the tree** (re-confirmed after every subagent returned). **The §7.5 gate was NOT re-run** — it passed at state-4 and `PROGRESS.md` carries its verbatim evidence. FOUR fresh `general-purpose` subagents with NO prior session context reviewed DISJOINT slices of `5e3afb9..ba2cb9e` (15 commits, 20 files): the data plane + dispatch arm; the config schema + terminal pre-pass; the differential driver + fixture `0071`; the out-of-PLAN test change + fuzz seed + docs. Each was forbidden from re-litigating the six settled decisions (drain, `echo` asymmetry, the `DirectResponseConfig` naming, the `is_terminal` predicate, the pre-pass ordering, BLOCK-66-1) yet free to critique their implementation, and each carried an adversarial question. **All four returned "Ready to merge? Yes."** **Dispatcher-side re-verification (agent reports are NOT evidence):** `direct_response::serve` ≡ `echo::serve` structurally (both read in full); ADR-0126's fix intact (`--quiet` only in a comment at `:98`, `PREBUILD_TIMEOUT = 240s` at `:80`, the 30s budget at `:247`); **M66-2's four siblings CONFIRMED + SHARPENED — all four also pass `--quiet` under the same 30s budget**; the empty-`filters: []` acceptance confirmed (the sole `is_empty()` guard is `validate_http_filters`). **One reviewer's Important finding (the `JoinSet` never reaps during normal operation) is recorded VERBATIM and DOWNGRADED to Minor M66-3, with the reasoning stated** — it is inherited verbatim from the pinned `echo.rs` model, has no differential observable, and must be fixed in both files together. **§5.2: no re-entry at state 3** (0 Critical / 0 Important; phase-64 and phase-65 precedents). **THIS commit = `REVIEW.md` (new) + STATE + STATE_HISTORY** — no src/fixture/test/ROADMAP change (row `66` flips `done` at state-6, not now). (`next-prompt.txt` refreshed on disk but **gitignored** at `.gitignore:9`, so NOT in this commit.) No fixture weakened; `known-failures.txt` untouched; the ADR-0124 drain and its mutation check neither re-litigated nor deleted; BLOCK-66-1 not re-opened. `#![forbid(unsafe_code)]` holds. **CONSUMES no carry-forward; opens M66-3…M66-7; CF-66-1, CF-66-2, M66-2 stay OPEN.** **DECISIONS.md ledger head: ADR-0126 (unchanged — no ADR was written this session);** next-available **ADR-0127**, unreserved. The next session is the §5 state-6 close-out (flip ROADMAP row `66` → `done`; relocate the phase-66 Notes subsection to `STATE_HISTORY.md`; `STATE.md` → "awaiting next planning").

> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases incl. the phase-37 state-1..5 sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated

2026-07-09 (phase-66 **state-5 CODE-REVIEW COMPLETE; `REVIEW.md` is APPROVED [0 Critical / 0 Important / 5 Minor]; awaiting the state-6 close-out**. Ran via `superpowers:requesting-code-review` against `HEAD` = `origin/main` = `ba2cb9e`, working tree clean, no sibling ahead. **NO production code changed — the code-review is read-only over the tree; no ADR was written [none was needed].** **The §7.5 gate was NOT re-run** [it passed at state-4; `PROGRESS.md` carries the verbatim evidence]. **Gate (f) is now SATISFIED.** FOUR fresh `general-purpose` subagents with NO prior context reviewed DISJOINT slices of `5e3afb9..ba2cb9e` [15 commits, 20 files, +1938/−44], each forbidden from re-litigating the six settled decisions but free to critique HOW each is implemented, and each given an adversarial question. **All four answered "Ready to merge? Yes."** **Agent reports are not evidence, so every load-bearing claim was independently re-run by this session:** `direct_response::serve` is structurally IDENTICAL to `echo::serve` [both read in full]; ADR-0126's fix is intact [`--quiet` only in a comment, `PREBUILD_TIMEOUT = 240s`, the 30s budget untouched]; **M66-2's four siblings CONFIRMED and SHARPENED — all four also still pass `--quiet` under the same 30s budget**; an empty `filters: []` network chain IS accepted [the only `is_empty()` guard is `validate_http_filters`]. **One reviewer's Important finding [the `JoinSet` never reaps] is recorded verbatim and DOWNGRADED to Minor M66-3 with reasons** — it is shared verbatim with `echo.rs`, has zero differential observable, and must be repaired in both files together or the "echo is the model" invariant breaks. **§5.2: the phase does NOT re-enter state 3** [0 Critical / 0 Important; the phase-64 and phase-65 precedents both closed at state-6 with open Minors]. **Key strengths independently established:** the terminal pre-pass is COMPLETE — it also covers LDS/xDS-loaded listeners via `load_dynamic_resources`' re-validation [a path nobody had checked, and one that still rests on manual tracing rather than a test → **M66-6**]; fixture `0071` is a NON-VACUOUS witness [an empty/empty `byte_exact` compare would pass vacuously, but the payload is a non-empty 39 bytes]; the `main.rs` "validator guarantees" invariant really is guaranteed, and `filters.first()` is safe BECAUSE terminal validation makes ≥2-filter chains invalid — the phase's two halves interlock; the out-of-PLAN `upstream_h2_connection_pooling.rs` change did NOT weaken the test; and all four `BEHAVIOR_CONTRACT.md` clauses match the code. **NEW carry-forwards: M66-3** [`JoinSet` non-reaping + unbounded per-connection drain — PRE-EXISTING, shared with `echo.rs`], **M66-4** [doc-precision: the drain is bounded only ON SHUTDOWN], **M66-5** [two UNWITNESSED config edges — empty `filters: []` accepted; `response: {}` rejected — envoy-rust's behavior recorded, Envoy's deliberately NOT asserted, per D-3.3], **M66-6** [test-coverage, chiefly the missing dynamic/LDS terminal test], **M66-7** [cosmetics]. **M66-1 was never allocated; the ledger does not backfill.** **CONSUMES no carry-forward;** CF-66-1, CF-66-2, M66-2 and M66-3…M66-7 all stay OPEN; every pre-existing Minor stays LIVE and NONE blocks. No fixture weakened, `known-failures.txt` not trimmed, the drain not re-litigated, the `echo` asymmetry not "fixed", BLOCK-66-1 not re-opened. `#![forbid(unsafe_code)]` holds. **DECISIONS.md ledger head: ADR-0126, unchanged** [next-available **ADR-0127**, unreserved]. ADR-0123 REFINED by ADR-0124, not superseded; ADR-0014 in force; ADR-0028 open; ADR-0049 governs config-validity. The next session is the §5 state-6 close-out [flip ROADMAP row `66` → `done`; relocate the phase-66 Notes subsection; STATE → "awaiting next planning"].)

> Historical `## Last updated` notes — every superseded last-updated note (all closed phases incl. the phase-37 state-1..5 sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsection for fully-closed phase 64 (the `### Phase-64 state-5 code-review` narrative — phase 64 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-64 state-6 close-out when ROADMAP row `64` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 63 (the `### Phase-63 state-5 code-review` narrative — phase 63 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-63 state-6 close-out when ROADMAP row `63` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 61 (the `### Phase-61 state-1 brainstorm` / `### Phase-61 state-2 PLAN-write` / `### Phase-61 state-3 implementation` / `### Phase-61 state-4 verification` / `### Phase-61 state-5 code-review` narratives, relocated at the phase-61 state-6 close-out when ROADMAP row `61` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 58 (the `### Phase-58 state-1 brainstorm` / `### Phase-58 state-2 PLAN-write` / `### Phase-58 state-3 implementation` / `### Phase-58 state-4 verification` / `### Phase-58 state-5 code-review` narratives, relocated at the phase-58 state-6 close-out when ROADMAP row `58` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 57 (the `### Phase-57 state-1 brainstorm` / `### Phase-57 state-2 PLAN-write` / `### Phase-57 state-3 implementation` / `### Phase-57 state-4 verification` / `### Phase-57 state-5 code-review` narratives, relocated at the phase-57 state-6 close-out when ROADMAP row `57` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 42 (the `### Phase-42 state-5 code-review (active phase)` narrative — phase 42 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-42 state-6 close-out when ROADMAP row `42` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 43 (the `### Phase-43 state-5 code-review (active phase)` narrative — phase 43 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-43 state-6 close-out when ROADMAP row `43` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 44 (the `### Phase-44 state-5 code-review (active phase)` narrative — phase 44 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-44 state-6 close-out when ROADMAP row `44` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 45 (the `### Phase-45 state-5 code-review (active phase)` narrative — phase 45 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-45 state-6 close-out when ROADMAP row `45` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 46 (the `### Phase-46 state-5 code-review (active phase)` narrative — phase 46 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-46 state-6 close-out when ROADMAP row `46` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 47 (the `### Phase-47 state-5 code-review (active phase)` narrative — phase 47 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-47 state-6 close-out when ROADMAP row `47` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 48 (the `### Phase-48 state-5 code-review (active phase)` narrative — phase 48 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-48 state-6 close-out when ROADMAP row `48` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 49 (the `### Phase-49 state-5 code-review (active phase)` narrative — phase 49 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-49 state-6 close-out when ROADMAP row `49` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 50 (the `### Phase-50 state-3 implementation (active phase)` narrative — phase 50 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-50 state-6 close-out when ROADMAP row `50` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 51 (the `### Phase-51 state-3 implementation (active phase)` narrative — phase 51 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-51 state-6 close-out when ROADMAP row `51` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 53 (the `### Phase-53 state-1 brainstorm` narrative — phase 53 kept a single active-phase Notes subsection at "state-1 brainstorm" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-1 subsection remained in `## Notes` at close — relocated at the phase-53 state-6 close-out when ROADMAP row `53` flipped `done`, together with the inline superseded state-3 narrative) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 54 (the `### Phase-54 state-1 brainstorm` / `### Phase-54 state-2 PLAN-write` / `### Phase-54 state-3 implementation` / `### Phase-54 state-4 verification` / `### Phase-54 state-5 code-review` narratives, relocated at the phase-54 state-6 close-out when ROADMAP row `54` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 55 (the `### Phase-55 state-5 code-review (active phase)` narrative — phase 55 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state individually superseded in place and relocated to STATE_HISTORY.md at its own transition], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-55 state-6 close-out when ROADMAP row `55` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 56 (the `### Phase-56 state-5 code-review (active phase)` narrative — phase 56 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state individually superseded in place and relocated to STATE_HISTORY.md at its own transition], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-56 state-6 close-out when ROADMAP row `56` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Phase-66 state-5 code-review (active phase)

- **`REVIEW.md` is APPROVED — 0 Critical / 0 Important / 5 Minor.** Its FIRST content line reads `**Status: APPROVED**`, which is the §5.1 state-detection signal. The §5 state-6 close-out is next. **Per §5.2 the phase does NOT re-enter state 3** (that asymmetry fires only on Critical/Important issues); the phase-64 (0/0/2 Minor) and phase-65 (0/0/1 Minor) precedents both closed at state-6 without re-entry.
- **The §7.5 gate is DONE and it PASSES, and gate (f) is now satisfied. Do NOT re-run it** — every command's output is quoted verbatim in `PROGRESS.md` §"§5 state-4 VERIFICATION". Re-running `cargo test --workspace` costs ~12 min and exits 101 on this host for already-adjudicated environmental reasons.
- **One reviewer filed the `JoinSet`-non-reaping finding as Important. It was DOWNGRADED to Minor (M66-3) with reasons recorded, not silently softened.** Justification, verified by reading both files in full: `direct_response::serve` (`direct_response.rs:36-74`) is **structurally identical** to `echo::serve` (`echo.rs:21-59`) — neither reaps completed tasks during normal operation, and neither bounds the per-connection read. It is therefore a property of the **pinned structural model**, not code phase 66 authored; it has **zero differential observable**; and repairing `direct_response.rs` alone would break the "echo is the model" invariant that ADR-0123 §2.1(B)(6) rests on. **Repair both files together**, in the phase that next touches the network-filter data plane (natural home: CF-66-2).
- **The terminal pre-pass is COMPLETE, including the path nobody had checked.** The listener loop chains static AND dynamic listeners (`bootstrap.rs:2967-2969`), and `load_dynamic_resources` (`lib.rs:844`) re-invokes `bootstrap::validate` (`lib.rs:1053`) — so **LDS/xDS-loaded listeners get the terminal check too**; a chain cannot escape it. This is the phase's most load-bearing property and it currently rests on **manual tracing, not a test** (carry-forward **M66-6**).
- **Fixture `0071` is a NON-VACUOUS witness.** `assert_body_rule`'s `ByteExact` is a bare `if envoy_body != rust_body { bail! }`, so an empty-vs-empty compare WOULD pass vacuously — but the fixture configures a **non-empty 39-byte payload** that the authoritative upstream reference deterministically emits, so any regression that drops, truncates or mutates it is caught.
- **M66-2 is CONFIRMED and SHARPENED** (a carry-forward description update, NOT an amendment to the append-only ADR-0126, which it confirms): all four latent siblings — `upstream_connection_pooling.rs`, `upstream_active_health_check.rs`, `upstream_outlier_detection.rs`, `tests/differential/src/backend.rs` — not only nest a `cargo run --manifest-path`, they **also still pass `--quiet` under the same 30s readiness budget**. `--quiet`, not the stdio posture, is the silencing factor in every case. A single mechanical sweep (hoist the build out of the budget; drop `--quiet`) neutralizes the class.
- **M66-5 records envoy-rust's behavior on two config edges and DELIBERATELY DECLINES to assert Envoy's** (D-3.3: the contract is what Envoy *does*, and neither edge was probed): an empty network `filters: []` chain **is accepted** (the only `filters.is_empty()` guard, `bootstrap.rs:3618`, is `validate_http_filters` — HTTP filters INSIDE HCM, not network chains), and `response: {}` is rejected with `missing field inline_string`. A future phase should probe the pinned image with `--mode validate` before matching or recording a divergence.
- **Numbering: M66-1 was never allocated** — ADR-0126 opened `M66-2` directly. The new REVIEW.md Minors start at **M66-3**. The ledger advances monotonically and does not backfill lapsed numbers. Do not "fix" the gap.
- **The drain is PINNED (ADR-0124) and CI-green. Do not re-litigate it.** `direct_response_once` does `write_all` → `flush` → `shutdown()` [FIN] → **read-and-discard until EOF** → drop. Commenting out the drain loop makes `post_eof_client_write_is_accepted_not_reset` fail with `Os {code: 32, kind: BrokenPipe}`. Fixture `0071` CANNOT catch this (its driver never writes after EOF) — the in-process test is the only guard. **Do not remove it.**
- **`envoy_config::DirectResponse` already existed** — it is the HCM **route-level** `direct_response` action (phase 04). The phase-66 network-filter type is deliberately `DirectResponseConfig`; `TypedConfig::DirectResponse` is an enum *variant* in a different namespace. `BEHAVIOR_CONTRACT.md`'s `## Network filters` section (`:229`) opens with a do-not-conflate banner.
- **`cargo build -p envoy-bin` before ANY local differential run** (memory `differential-harness-uses-debug-envoy-bin`) — the harness executes `target/debug/envoy-bin`.
- **Do NOT "fix" the `echo` `typed_config` asymmetry** (upstream requires it; envoy-rust forbids it — the pre-existing ADR-0014 YAML shim behind fixture `0001`'s two-sided divergence). `direct_response` needs no shim: both sides of fixture `0071` are identical.
- **BLOCK-66-1 is CLEARED (ADR-0126).** Do not re-add `--quiet` to `spawn_backend`, do not remove the pre-build, do not widen the 30s readiness budget. *Log-reading trap:* CI interleaves ANSI escapes between `Compiling` and the crate name — grep the crate name alone.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: one state per session; do not chain states. Phase 66 (`66-network-filter-direct-response`) is the ACTIVE phase — ROADMAP row `66` is `in-progress`. The state-4 verification completed THIS session and the §7.5 gate PASSES. `SPEC.md`, `PLAN.md` and `PROGRESS.md` all exist and `REVIEW.md` does not, so the next session is the §5 state-5 code-review (`superpowers:requesting-code-review` → `REVIEW.md`) — it must NOT chain into the state-6 close-out. **Per §5.2, if `REVIEW.md` raises issues, re-enter at state 3, NOT state 4.** Row `66` flips to `done` only at state-6. ROADMAP rows `00`-`58` + `61` + `63` + `64` + `65` are `done` (`59`/`60`/`62` are intentional gaps with no rows).

> Historical Notes subsection for fully-closed phase 65 (the `### Phase-65 state-5 code-review` narrative — phase 65 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-65 state-6 close-out when ROADMAP row `65` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsection for fully-closed phase 52 (the `### Phase-52 state-5 code-review (active phase)` narrative — phase 52 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2/3/4 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-52 state-6 close-out when ROADMAP row `52` flipped `done`, together with the standing 05.1/05.4 future-reviewer note it carried) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 41 (the `### Phase-41 state-5 code-review (active phase)` narrative — phase 41 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the §6.2-recon PIVOT from `%REQ_WITHOUT_QUERY%` to `%ROUTE_NAME%` landed at state-2; the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-41 state-6 close-out when ROADMAP row `41` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 40 (the `### Phase-40 state-5 code-review (active phase)` narrative — phase 40 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-40 state-6 close-out when ROADMAP row `40` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 39 (the `### Phase-39 state-5 code-review (active phase)` narrative — phase 39 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-39 state-6 close-out when ROADMAP row `39` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
> Historical Notes subsection for fully-closed phase 38 (the `### Phase-38 state-2 PLAN-write (active phase)` narrative — phase 38 used a single active-phase Notes subsection for its whole arc; the state-3/4/5 sub-state narratives were superseded in the four-top-section archive blocks per-session, so only this state-2 subsection remained in `## Notes` at close — relocated at the phase-38 state-6 close-out when ROADMAP row `38` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) and for parent phase 25 (the `### Phase-25 state-1 brainstorm` pick + recon-finding narrative, relocated at the 25.2 state-6 close-out when parent `25` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 26 (the `### Phase-26 state-1 brainstorm` pivot/rejected-alternatives/key-scoping narrative, relocated at the phase-26 state-6 close-out when row `26` flipped to `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 27 (the `### Phase-27 state-1 brainstorm` / `### Phase-27 state-2 PLAN-write` / `### Phase-27 state-4 verification` narratives + the now-consumed `### Phase-27 carry-forwards` [M26-1..M26-8] block, relocated at the phase-27 state-6 close-out when row `27` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 28 (the `### Phase-28 state-1 brainstorm` / `### Phase-28 state-2 PLAN-write` / `### Phase-28 state-3 implementation + state-4 verification` / `### Phase-28 state-5 code review` narratives + the now-consumed `### Phase-28 carry-forwards` [M27-1..M27-3] block, relocated at the phase-28 state-6 close-out when row `28` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 29 (the now-consumed `### Phase-29 carry-forwards` [M28-1..M28-3] block + the `### Phase-29 state-1 brainstorm` / `### Phase-29 state-2 PLAN-write` / `### Phase-29 state-3 implementation` / `### Phase-29 state-4 verification` / `### Phase-29 state-5 code review` narratives, relocated at the phase-29 state-6 close-out when row `29` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 30 (the `### Phase-30 carry-forwards` [M29-1/M29-2, which fed phase 30 but were NOT consumed — they continue as phase-31 carry-forwards] block + the `### Phase-30 state-1 brainstorm` / `### Phase-30 state-2 PLAN-write` / `### Phase-30 state-3 implementation` / `### Phase-30 state-4 verification` / `### Phase-30 state-5 code review` narratives, relocated at the phase-30 state-6 close-out when row `30` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 31 (the `### Phase-31 carry-forwards` [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 — open Minors from the phase-30 REVIEW.md that fed phase 31 but were NOT consumed; they continue as carry-forwards for the next phase that touches the differential hash-sweep driver / the config parser] block + the `### Phase-31 state-1 brainstorm` / `### Phase-31 state-2 PLAN-write` / `### Phase-31 state-3 implementation` / `### Phase-31 state-4 verification` / `### Phase-31 state-5 code review` narratives, relocated at the phase-31 state-6 close-out when row `31` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 32 (the `### Phase-32 carry-forwards` block — the open Minors that fed phase 32 but were NOT consumed: the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3; they continue as carry-forwards for the future phase that touches their surface — re-listed in `## Next expected skill` above alongside the 6 new phase-32 REVIEW.md Minors M32-1…M32-6), relocated at the phase-32 state-6 close-out when row `32` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phases 33+34 (the `### Phase-33 carry-forwards` block — the active-phase carry-forwards Notes that lived through phases 33 and 34: the now-CONSUMED 6 phase-32 REVIEW.md Minors M32-1…M32-6 [folded + landed at the phase-33 state-3] + the "Other still-live carry-forwards" list [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3 + the HTTP-filters-family (1)-(4) — those still-live ones re-listed in `## Next expected skill` above alongside the new phase-34 Minors M34-1/M34-2/M34-3 + the phase-33 M33-1/M33-2]), relocated at the phase-34 state-6 close-out when row `34` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 35 (the `### Phase-35 state-1 brainstorm` / `### Phase-35 state-2 PLAN-write` / `### Phase-35 state-3 implementation` / `### Phase-35 state-4 verification` / `### Phase-35 state-5 code-review` narratives, relocated at the phase-35 state-6 close-out when ROADMAP row `35` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 36 (the `### Phase-36 state-5 code-review` narrative — phase 36 used a rename-in-place Notes discipline, so the state-1..4 sub-state narratives were superseded in place each session and only the four top-section blocks were relocated per-session — plus the now-CLOSED detailed M35-1 carry-forward bullet [CONSUMED by phase-36 F2], relocated at the phase-36 state-6 close-out when ROADMAP row `36` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 37 (the `### Phase-37 state-3 implementation` active-phase narrative, relocated at the phase-37 state-6 close-out when ROADMAP row `37` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### HTTP-filters-family carry-forwards (from the `25.2` REVIEW.md - NOT yet consumed; weigh whenever the HTTP-filters family is re-entered)

> These were never obligations on the xDS phase 26; they remain live for whenever an HTTP-filters-family phase resumes.

- **(1) [non-goal - architectural]** Over-limit request bodies are FULLY buffered before the 413 rejection (no streaming watermark). Documented deferred non-goal; differentially byte-identical to Envoy for the bounded fixture sizes. Revisit only if a streaming `decode_data` watermark path is ever planned.
- **(2) [doc precision]** The BEHAVIOR_CONTRACT 413-row "verified byte-exact against v1.33.0" phrasing - fixture `0033` is H1-only; the H2 over-limit path is covered by the in-process synth-decorator backstop, NOT differentially. Consider narrowing the phrasing if an H2 over-limit fixture is ever added.
- **(3) [coverage]** No standalone `== effective route limit` unit assertion (the boundary is exercised only via the over/under probes).
- **(4) [coverage]** No differential at-limit (`==`) probe in `0033` (within-limit `<` and over-limit `>` are both covered; the exact boundary is not differentially probed).
- _(2)-(4) are cheap polish, (1) is architectural and only relevant to a future streaming phase._
