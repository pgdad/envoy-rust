# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase


**id:** **PHASE 50 (`50-accesslog-rf-overflow`) — STATE-2 PLAN-WRITE COMPLETE (PLAN written + plan-reviewed by a FRESH subagent → ✅ APPROVED, 0 issues; ADR-0107 governs; §6.1 split did NOT fire; ROADMAP row `50` `in-progress`); STATE-3 IMPLEMENTATION NEXT.** ROADMAP rows `00`-`49` are ALL `done`; `50` is `in-progress` (the highest defined row). The active phase is `50-accesslog-rf-overflow` — the FOURTEENTH Observability access-log row: the THIRD non-`-` `%RESPONSE_FLAGS%` value `UO` (UpstreamOverflow) byte-exact on the circuit-breaker overflow 503 path (`max_pending_requests: 0`). This session ran the §5 state-2 PLAN-write (`superpowers:writing-plans`): finalized the §A overflow-outcome discriminator + set-site (the `hcm.rs:990-1002` retry-loop consumption site + the `attempt.outcome.is_none()` overflow discriminator covering BOTH pool arms `:508`/`:515`) + the §A′ budget-arm tag at `:923` (DECIDED to tag the `max_requests` arm too for a uniformly-1:1 derive — SPEC §3.1's recommended default; in-process-backstopped, differential witness deferred → new carry-forward M50-C) + the §B derive arm `Some("upstream_reset_before_response_started{overflow}") => "UO"` at `:1237`; ran the §6.2 recon (CONFIRMED every SPEC §A-§F fact against the tree — `outcome:None` at EXACTLY `:439`/`:640`, both pool arms collapse to one outcome, the budget arm bypasses the loop, the M49-2 `synth_404` citations are `:1553`/`:1572`); authored `PLAN.md` (4 TDD tasks / ~70-120 LoC; folded M50-A + M50-B + M49-2 + M49-3) — plan-reviewed by a FRESH `superpowers:code-reviewer` subagent (no session history) which verified all line numbers/symbols/edits against the real tree → **✅ APPROVED, 0 issues** (one advisory folded: M50-C must flag the budget-arm rcd STRING as unverified, not just the flag). **NO code change this state-2 session** (PLAN + STATE + STATE_HISTORY only). The next session runs the **§5 state-3 implementation** (`superpowers:subagent-driven-development` / `superpowers:executing-plans`, TDD per `superpowers:test-driven-development`) — see `## Next expected skill`.
**slug:** 50-accesslog-rf-overflow
**directory:** `docs/envoy-rust/phases/50-accesslog-rf-overflow/` (`SPEC.md` + `PLAN.md` present — `PLAN.md` is the state-2 output, plan-reviewed by a FRESH subagent → ✅ APPROVED 0 issues; `PROGRESS.md`/`REVIEW.md` not yet written)

**status:** **PHASE 50 STATE-2 PLAN-WRITE COMPLETE (THIS session) — STATE-3 IMPLEMENTATION NEXT.** This session executed the §5 state-2 PLAN-write (`superpowers:writing-plans`): (1) finalized the **§A overflow-outcome discriminator + set-site** — at the H1 retry-loop result-consumption site `hcm.rs:990-1002`, replace the unconditional `via_upstream` (`:995`) with an `attempt.outcome.is_none()`-discriminated set (overflow → `Some("upstream_reset_before_response_started{overflow}")`, else `via_upstream`), covering BOTH pool arms (`max_connections` `:508` + `max_pending_requests` `:515`); (2) **DECIDED §A′** (resolves SPEC §3.1): ALSO tag the pre-route request-budget arm `:923` (`max_requests`) for a uniformly-1:1 derive — the SPEC's twice-stated recommended default; in-process-backstopped only, differential witness deferred → **new carry-forward M50-C** (the budget-arm rcd STRING, not just the `UO` flag, is the unverified part — re-recon before witnessing); (3) finalized the **§B derive arm** `Some("upstream_reset_before_response_started{overflow}") => "UO"` at `hcm.rs:1237`; (4) ran the **§6.2 recon** — CONFIRMED every SPEC §A-§F fact against the tree: `outcome:None` at EXACTLY two `AttemptResult` constructions (`:439` no-healthy/`endpoint:None` + `:640` overflow/`endpoint:Some`) so `attempt.outcome.is_none()` ⟺ overflow within the `endpoint:Some` branch; `attempt.endpoint` is `Option<SocketAddr>` (Copy) so the `if let` does not move `attempt`; the budget arm `:923` bypasses the loop (rcd stays `None` → renders `null`/`-`); the M49-2 `synth_404` arms are host-miss `:1553` + route-miss `:1572`; the overflow circuit-breaker contract row `:37` stale-cites `:542`/`:569` (actual `:503`/`:510` — M49-3 re-anchor); the §3.3 fixture list incl. `0051`/`0052` (M50-B); (5) authored `PLAN.md` (4 TDD tasks: T1 §A+§B pool-overflow backstop, T2 §A′ budget-arm tag + backstop, T3 fixture `0058` + differential test, T4 BEHAVIOR_CONTRACT §E; ~70-120 LoC; folded M50-A pool.rs:801 harness ref + M50-B + M49-2 + M49-3) — plan-reviewed by a FRESH `superpowers:code-reviewer` subagent (no session history) which independently verified all line numbers, symbols (`cluster_mgr_with_endpoint_max_requests`, `spawn_fail_then_ok_upstream`, `H1PoolManager::for_bootstrap`, `FileSink::new`, …), and code edits against the real tree → **✅ APPROVED, 0 issues** (one advisory folded into M50-C). **§6.1 split did NOT fire** (4 tasks / ~70-120 LoC ≪ 25 tasks / 1500 LoC; **ADR-0108 stays reserved-but-unfired**). **NO §6.2-reconciliation ADR** — the recon overturned NO SPEC fact (the §A′ budget-arm decision follows the SPEC's recommended default; it does not overturn a fact). **NO code change this state-2 session** (PLAN + STATE + STATE_HISTORY only). `#![forbid(unsafe_code)]` holds. **Open carry-forward Minors (NONE blocks; weigh when each surface is next touched):** **M50-C** (NEW — the request-budget `max_requests` overflow UO/rcd is in-process-backstopped only; differential witness + an rcd-string re-recon deferred) + **M50-A** + **M50-B** (FOLDED into `PLAN.md` this session) + **M49-2** (derive-comment `route_not_found` citation — FOLDED into PLAN T1; CONSUMED at state-3) + **M49-3** (the `:1232`→`:1237`/`:1225` derive-site + overflow-row anchors — FOLDED into PLAN T1/T4; CONSUMED at state-3) + **M48-2** (informational encode-side coupling) + **M42-1** (failure-path/`%RESPONSE_FLAGS%` vocabulary — continued) + **M45-1** (H2 failure-path details + an H2 access-log differential driver) + **M45-2** (connect-failure non-deterministic rcd + the `UF`/`DC`/`URX` flags — the `UO` slice CONSUMED by phase 50) + M40-1 + M39-1/M39-2 + M38-2/M38-1 + CF-39-1 + M37-2/M37-1 + M36-* + M34-* + M33-* + the empty-`metadata_match` doc-comment + M29-1/M29-2 + M30-1/M30-2 + the phase-31 cosmetics + the HTTP-filters-family (1)-(4). **[M49-1 + M48-1 CONSUMED at the phase-49 state-3 implementation.]** **DECISIONS.md ledger head: ADR-0107** (count 107; next-available **ADR-0108**, reserved for the phase-50 §6.1 split — still unfired). ADR-0014 in force; ADR-0028 open; ADR-0049 governs config-validity. The next session is the **§5 state-3 implementation** (`superpowers:subagent-driven-development` / `superpowers:executing-plans`).
> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases incl. the phase-37 state-1..5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill

Per `BOOTSTRAP_PROMPT.md` §5 state 3 + `SKILL_ROUTING.md`: phase 50 (`50-accesslog-rf-overflow`) is `in-progress` at state-2-complete — `docs/envoy-rust/phases/50-accesslog-rf-overflow/SPEC.md` + `PLAN.md` are present (`PLAN.md` is the state-2 output, plan-reviewed → ✅ APPROVED 0 issues) + ADR-0107 governs + ROADMAP row `50` `in-progress`, but NO `PROGRESS.md` → the next session is the **§5 state-3 implementation** (`superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans`), TDD per `superpowers:test-driven-development` on every task (D-3.1). Execute `PLAN.md`'s **4 TDD tasks IN ORDER**: (T1) RED pool-overflow in-process backstop `h1_pool_overflow_access_log_carries_uo_flag` (wires `pool_mgr: Some(...)` with `max_connections:1`/`max_pending_requests:0`) → §A discriminator at `hcm.rs:990-1002` (the `attempt.outcome.is_none()` set) + §B derive arm at `:1237` + the derive-comment M49-2/M49-3 citation fixes → GREEN; (T2) RED budget-overflow backstop `h1_request_budget_overflow_access_log_carries_uo_flag` (`max_requests:0`, `pool_mgr: None`) → §A′ tag at `:923` → GREEN; (T3) fixture `0058-accesslog-rf-overflow` (4 files, STATIC cluster + dead `127.0.0.1:1` + circuit_breakers, one `GET /` probe, `expected_status: 503`, json_format `{rc,rcd,rf}`) + differential test `access_log_rf_overflow.rs` (clone of `access_log_rf_no_healthy.rs`); (T4) BEHAVIOR_CONTRACT §E (the `%RESPONSE_FLAGS%` row `:1020` + the `%RESPONSE_CODE_DETAILS%` row `:1031` + the overflow row `:37` M49-3 re-anchor `:542`/`:569`→`:503`/`:510`). Append to `PROGRESS.md` on each task. **The §A discriminator form + the §A′ budget-arm decision are DECIDED (do NOT re-litigate)** — see `PLAN.md` §6.2 recon + the §3.1 resolution. The Docker differential (`0058` green + `0001`-`0057` byte-identical) is CI-authoritative at the state-4 §7.5 gate (the session AFTER state-3) — rebuild the DEBUG `envoy-bin` before any local differential (memory `differential-harness-uses-debug-envoy-bin`); do NOT block state-3 on a local Docker run (memory `envoy-rust-state4-ci-first-execution`). §5.1: ONE state per session — STOP after the implementation + `PROGRESS.md`; state-4 verification is the SESSION AFTER. **PUSH per-session + confirm CI.**

**Open carry-forward Minors (fold into the phase that next touches each surface; NONE blocks):** **M50-C** (NEW this state-2 — the request-budget `max_requests` overflow UO/rcd, set by §A′ at `hcm.rs:923`, is in-process-backstopped only [T2]; its differential witness is deferred — and the unverified part is the rcd STRING `upstream_reset_before_response_started{overflow}`, not just the `UO` flag: re-recon the rcd against live Envoy before witnessing, do NOT assume the pool-arm string carries over) + **M50-A** + **M50-B** (FOLDED into `PLAN.md` this state-2 — M50-A → the `pool.rs:801` harness ref in T1; M50-B → the exhaustive fixture list incl. `0051`/`0052` in the §6.2 recon) + **M49-2** (derive-comment `route_not_found` citation drift — FOLDED into PLAN T1 Step 4, CONSUMED at state-3) + **M49-3** (the `hcm.rs:1232`→`:1237`/`:1225` derive-site + the overflow-row `:542`/`:569` anchors — FOLDED into PLAN T1/T4, CONSUMED at state-3) + **M48-2** (informational encode-side coupling) + **M42-1** (failure-path / `%RESPONSE_FLAGS%` vocabulary — continued) + **M45-1** (H2 failure-path details + an H2 access-log differential driver) + **M45-2** (connect-failure non-deterministic rcd + the `UF`/`DC`/`URX` flags — the `UO` slice CONSUMED by phase 50) + **M39-1/M39-2** + **M38-2/M38-1** + **CF-39-1** + **M40-1** + M37-2/M37-1 + M36-1/M36-2/M36-3 + M34-1/M34-2/M34-3 + M33-1/M33-2 + the empty-`metadata_match` doc-comment + M29-1/M29-2 + M30-1/M30-2 + the phase-31 cosmetics + the HTTP-filters-family (1)-(4). **[M49-1 + M48-1 CONSUMED at the phase-49 state-3 implementation.]**

**Phases 49/48/47/46/45/44/43/42/41/40/39/38/37/36/35/34 are fully closed + CI-GREEN — do NOT re-verify.** ROADMAP rows `00`-`49` ALL `done`; `50` is `in-progress` (the active phase) → the next session is the §5 state-3 implementation (`superpowers:subagent-driven-development` / `superpowers:executing-plans` for phase 50); §5.1 one state per session.
> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases incl. the phase-37 state-1..5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit

**Phase-50 state-2 PLAN-write — PLAN.md (plan-reviewed → ✅ APPROVED) + STATE advance to state-3-next (THIS commit):** the §5 state-2 PLAN-write (`superpowers:writing-plans`) for phase 50 (`50-accesslog-rf-overflow`). (1) Finalized **§A** (the `attempt.outcome.is_none()` overflow discriminator at the retry-loop consumption site `hcm.rs:990-1002`, covering both pool arms `:508`/`:515`), **§A′** (DECIDED to also tag the request-budget `:923` `max_requests` arm — SPEC §3.1's recommended default; in-process-backstopped, differential witness deferred → new carry-forward **M50-C**), and **§B** (the one derive arm `Some("upstream_reset_before_response_started{overflow}") => "UO"` at `:1237`). (2) Ran the §6.2 recon — CONFIRMED every SPEC §A-§F fact against the tree (`outcome:None` at EXACTLY `:439`/`:640`; the budget arm bypasses the loop; the M49-2 `synth_404` arms are `:1553`/`:1572`; the overflow contract row `:37` re-anchor `:542`/`:569`→`:503`/`:510`; the M50-B fixture list incl. `0051`/`0052`). (3) Authored `PLAN.md` (4 TDD tasks / ~70-120 LoC; folded M50-A + M50-B + M49-2 + M49-3) — plan-reviewed by a FRESH `superpowers:code-reviewer` subagent → **✅ APPROVED, 0 issues** (one advisory folded into M50-C: the budget-arm rcd STRING is the unverified part). **§6.1 split did NOT fire** (ADR-0108 stays reserved-but-unfired). **NO §6.2-reconciliation ADR** (no SPEC fact overturned). **NO code change this state-2 session** (PLAN + STATE + STATE_HISTORY only). `#![forbid(unsafe_code)]` holds. **THIS commit = PLAN.md + STATE + STATE_HISTORY.** **DECISIONS.md ledger head: ADR-0107** (count 107; next-available **ADR-0108**, reserved-but-unfired for the phase-50 §6.1 split). ADR-0014 in force; ADR-0028 open. The next session is the §5 state-3 implementation (`superpowers:subagent-driven-development` / `superpowers:executing-plans`).
> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases incl. the phase-37 state-1..5 sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated

2026-06-29 (phase-50 **STATE-2 PLAN-WRITE COMPLETE — STATE-3 IMPLEMENTATION NEXT**. Executed the §5 state-2 PLAN-write (`superpowers:writing-plans`): (1) finalized **§A** (the `attempt.outcome.is_none()` overflow discriminator at `hcm.rs:990-1002`, both pool arms `:508`/`:515`), **§A′** [DECIDED to also tag the request-budget `:923` `max_requests` arm — SPEC §3.1's recommended default; in-process-backstopped, differential witness deferred → new carry-forward **M50-C**], **§B** (the one derive arm at `:1237`); (2) ran the §6.2 recon — CONFIRMED every SPEC §A-§F fact against the tree [`outcome:None` at `:439`/`:640`; the budget arm bypasses the loop; the M49-2 `synth_404` arms `:1553`/`:1572`; the overflow row `:37` re-anchor `:542`/`:569`→`:503`/`:510`; M50-B fixture list incl. `0051`/`0052`]; (3) authored `PLAN.md` [4 TDD tasks / ~70-120 LoC; folded M50-A + M50-B + M49-2 + M49-3] — plan-reviewed by a FRESH `superpowers:code-reviewer` subagent → ✅ APPROVED 0 issues [advisory folded into M50-C: the budget-arm rcd STRING is the unverified part]. **§6.1 split did NOT fire** [ADR-0108 stays reserved-but-unfired]. **NO §6.2-reconciliation ADR** [no SPEC fact overturned]. **NO code change this state-2 session** [PLAN + STATE + STATE_HISTORY only]. Carry-forwards: M50-C [new] + M50-A/M50-B [FOLDED into PLAN] + M49-2/M49-3 [FOLDED into PLAN, CONSUMED at state-3] + M48-2 + M42-1 + M45-1 + M45-2 [`UF`/`DC`/`URX`; the `UO` slice CONSUMED] + older stay live. `#![forbid(unsafe_code)]` holds. **DECISIONS.md ledger head: ADR-0107** [count 107; next-available ADR-0108]. ADR-0014 in force; ADR-0028 open. The next session is the §5 state-3 implementation [`superpowers:subagent-driven-development` / `superpowers:executing-plans`].)
> Historical `## Last updated` notes — every superseded last-updated note (all closed phases incl. the phase-37 state-1..5 sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 42 (the `### Phase-42 state-5 code-review (active phase)` narrative — phase 42 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-42 state-6 close-out when ROADMAP row `42` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 43 (the `### Phase-43 state-5 code-review (active phase)` narrative — phase 43 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-43 state-6 close-out when ROADMAP row `43` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 44 (the `### Phase-44 state-5 code-review (active phase)` narrative — phase 44 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-44 state-6 close-out when ROADMAP row `44` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 45 (the `### Phase-45 state-5 code-review (active phase)` narrative — phase 45 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-45 state-6 close-out when ROADMAP row `45` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 46 (the `### Phase-46 state-5 code-review (active phase)` narrative — phase 46 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-46 state-6 close-out when ROADMAP row `46` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 47 (the `### Phase-47 state-5 code-review (active phase)` narrative — phase 47 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-47 state-6 close-out when ROADMAP row `47` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 48 (the `### Phase-48 state-5 code-review (active phase)` narrative — phase 48 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-48 state-6 close-out when ROADMAP row `48` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 49 (the `### Phase-49 state-5 code-review (active phase)` narrative — phase 49 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-49 state-6 close-out when ROADMAP row `49` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Phase-50 state-2 PLAN-write (active phase)

- **Pick (LOCKED, ADR-0107):** `UO` (UpstreamOverflow) — the THIRD non-`-` `%RESPONSE_FLAGS%` value, byte-exact on the circuit-breaker overflow 503 path (`max_pending_requests: 0`), after `NR` (phase 48) + `UH` (phase 49). SPEC + PLAN at `docs/envoy-rust/phases/50-accesslog-rf-overflow/`.
- **§A set-site (finalized):** the H1 retry-loop result-consumption site `hcm.rs:990-1002` — replace the unconditional `via_upstream` (`:995`) with an `attempt.outcome.is_none()`-discriminated set (overflow → `Some("upstream_reset_before_response_started{overflow}")`, else `via_upstream`). `attempt.outcome.is_none()` ⟺ pool-overflow within the `endpoint:Some` branch (recon: `outcome:None` at EXACTLY `:439` no-healthy/`endpoint:None` + `:640` overflow/`endpoint:Some`). Covers BOTH pool arms (`max_connections` `:508` + `max_pending_requests` `:515`). `attempt.endpoint` is `Option<SocketAddr>` (Copy) → the `if let` does not move `attempt`.
- **§A′ DECISION (resolves SPEC §3.1):** ALSO tag the pre-route request-budget arm `hcm.rs:923` (`max_requests`, the `BudgetAcquisition::Rejected` block that bypasses the loop → rcd stays `None` today) for a uniformly-1:1 derive — the SPEC's twice-stated recommended default. In-process-backstopped (T2); differential witness deferred → **M50-C** (the rcd STRING is the unverified part, not just the flag).
- **§B derive arm (finalized):** add `Some("upstream_reset_before_response_started{overflow}") => "UO"` at `hcm.rs:1237`; the `route_not_found`/`no_healthy_upstream` arms unchanged → `0056`/`0057` byte-identical. Fold M49-2 (`synth_404` arms `:1553`/`:1572`) + M49-3 (keep the `:1225` anchor) in the comment block.
- **plan-review:** a FRESH `superpowers:code-reviewer` subagent independently verified all line numbers, symbols, and code edits against the real tree → ✅ APPROVED 0 issues (one advisory folded into M50-C). `PLAN.md` = 4 TDD tasks / ~70-120 LoC; §6.1 split did NOT fire.
- **state-3 implementation is the next session** (`superpowers:subagent-driven-development` / `superpowers:executing-plans`, TDD): execute the 4 tasks IN ORDER, append `PROGRESS.md` per task. §5.1: ONE state per session.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: one state per session; do not chain states. Phase 50 (`50-accesslog-rf-overflow`) is `in-progress` at state-1-complete (SPEC written + spec-reviewed → APPROVED; ADR-0107 fired; ROADMAP row `50` `in-progress`); the next session is the §5 state-2 PLAN-write (`superpowers:writing-plans`) — finalize the §A overflow-outcome discriminator + set-site + the §B derive-extension form + run the §6.2 recon + author `PLAN.md`; do NOT also start the state-3 implementation that session.
- The reviewer's R2 disposition decision (option (a) retroactive split of 05.1 vs option (b) free-standing post-05.1 sub-phase) was settled at the 05.1 state-6 commit in favor of option (b); 05.4 is the chosen sibling sub-phase. Future-reviewers reading STATE.md should understand that 05.1 is structurally closed at the preamble landing; 05.4 is a SIBLING under parent-05, not a child of 05.1; and the execution order ran 05.1 → 05.4 → 05.2 → 05.3, with 05.3 the closing sub-phase that flips parent-05 to `done`.

> Historical Notes subsection for fully-closed phase 41 (the `### Phase-41 state-5 code-review (active phase)` narrative — phase 41 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the §6.2-recon PIVOT from `%REQ_WITHOUT_QUERY%` to `%ROUTE_NAME%` landed at state-2; the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-41 state-6 close-out when ROADMAP row `41` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 40 (the `### Phase-40 state-5 code-review (active phase)` narrative — phase 40 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-40 state-6 close-out when ROADMAP row `40` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 39 (the `### Phase-39 state-5 code-review (active phase)` narrative — phase 39 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-39 state-6 close-out when ROADMAP row `39` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
> Historical Notes subsection for fully-closed phase 38 (the `### Phase-38 state-2 PLAN-write (active phase)` narrative — phase 38 used a single active-phase Notes subsection for its whole arc; the state-3/4/5 sub-state narratives were superseded in the four-top-section archive blocks per-session, so only this state-2 subsection remained in `## Notes` at close — relocated at the phase-38 state-6 close-out when ROADMAP row `38` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) and for parent phase 25 (the `### Phase-25 state-1 brainstorm` pick + recon-finding narrative, relocated at the 25.2 state-6 close-out when parent `25` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 26 (the `### Phase-26 state-1 brainstorm` pivot/rejected-alternatives/key-scoping narrative, relocated at the phase-26 state-6 close-out when row `26` flipped to `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 27 (the `### Phase-27 state-1 brainstorm` / `### Phase-27 state-2 PLAN-write` / `### Phase-27 state-4 verification` narratives + the now-consumed `### Phase-27 carry-forwards` [M26-1..M26-8] block, relocated at the phase-27 state-6 close-out when row `27` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 28 (the `### Phase-28 state-1 brainstorm` / `### Phase-28 state-2 PLAN-write` / `### Phase-28 state-3 implementation + state-4 verification` / `### Phase-28 state-5 code review` narratives + the now-consumed `### Phase-28 carry-forwards` [M27-1..M27-3] block, relocated at the phase-28 state-6 close-out when row `28` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 29 (the now-consumed `### Phase-29 carry-forwards` [M28-1..M28-3] block + the `### Phase-29 state-1 brainstorm` / `### Phase-29 state-2 PLAN-write` / `### Phase-29 state-3 implementation` / `### Phase-29 state-4 verification` / `### Phase-29 state-5 code review` narratives, relocated at the phase-29 state-6 close-out when row `29` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 30 (the `### Phase-30 carry-forwards` [M29-1/M29-2, which fed phase 30 but were NOT consumed — they continue as phase-31 carry-forwards] block + the `### Phase-30 state-1 brainstorm` / `### Phase-30 state-2 PLAN-write` / `### Phase-30 state-3 implementation` / `### Phase-30 state-4 verification` / `### Phase-30 state-5 code review` narratives, relocated at the phase-30 state-6 close-out when row `30` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 31 (the `### Phase-31 carry-forwards` [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 — open Minors from the phase-30 REVIEW.md that fed phase 31 but were NOT consumed; they continue as carry-forwards for the next phase that touches the differential hash-sweep driver / the config parser] block + the `### Phase-31 state-1 brainstorm` / `### Phase-31 state-2 PLAN-write` / `### Phase-31 state-3 implementation` / `### Phase-31 state-4 verification` / `### Phase-31 state-5 code review` narratives, relocated at the phase-31 state-6 close-out when row `31` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 32 (the `### Phase-32 carry-forwards` block — the open Minors that fed phase 32 but were NOT consumed: the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3; they continue as carry-forwards for the future phase that touches their surface — re-listed in `## Next expected skill` above alongside the 6 new phase-32 REVIEW.md Minors M32-1…M32-6), relocated at the phase-32 state-6 close-out when row `32` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phases 33+34 (the `### Phase-33 carry-forwards` block — the active-phase carry-forwards Notes that lived through phases 33 and 34: the now-CONSUMED 6 phase-32 REVIEW.md Minors M32-1…M32-6 [folded + landed at the phase-33 state-3] + the "Other still-live carry-forwards" list [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3 + the HTTP-filters-family (1)-(4) — those still-live ones re-listed in `## Next expected skill` above alongside the new phase-34 Minors M34-1/M34-2/M34-3 + the phase-33 M33-1/M33-2]), relocated at the phase-34 state-6 close-out when row `34` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 35 (the `### Phase-35 state-1 brainstorm` / `### Phase-35 state-2 PLAN-write` / `### Phase-35 state-3 implementation` / `### Phase-35 state-4 verification` / `### Phase-35 state-5 code-review` narratives, relocated at the phase-35 state-6 close-out when ROADMAP row `35` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 36 (the `### Phase-36 state-5 code-review` narrative — phase 36 used a rename-in-place Notes discipline, so the state-1..4 sub-state narratives were superseded in place each session and only the four top-section blocks were relocated per-session — plus the now-CLOSED detailed M35-1 carry-forward bullet [CONSUMED by phase-36 F2], relocated at the phase-36 state-6 close-out when ROADMAP row `36` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 37 (the `### Phase-37 state-3 implementation` active-phase narrative, relocated at the phase-37 state-6 close-out when ROADMAP row `37` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### HTTP-filters-family carry-forwards (from the `25.2` REVIEW.md - NOT yet consumed; weigh whenever the HTTP-filters family is re-entered)

> These were never obligations on the xDS phase 26; they remain live for whenever an HTTP-filters-family phase resumes.

- **(1) [non-goal - architectural]** Over-limit request bodies are FULLY buffered before the 413 rejection (no streaming watermark). Documented deferred non-goal; differentially byte-identical to Envoy for the bounded fixture sizes. Revisit only if a streaming `decode_data` watermark path is ever planned.
- **(2) [doc precision]** The BEHAVIOR_CONTRACT 413-row "verified byte-exact against v1.33.0" phrasing - fixture `0033` is H1-only; the H2 over-limit path is covered by the in-process synth-decorator backstop, NOT differentially. Consider narrowing the phrasing if an H2 over-limit fixture is ever added.
- **(3) [coverage]** No standalone `== effective route limit` unit assertion (the boundary is exercised only via the over/under probes).
- **(4) [coverage]** No differential at-limit (`==`) probe in `0033` (within-limit `<` and over-limit `>` are both covered; the exact boundary is not differentially probed).
- _(2)-(4) are cheap polish, (1) is architectural and only relevant to a future streaming phase._
