# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase


**id:** **75.1** — `75.1-headermatcher-absence-engine-route`, the FIRST sub-phase of parent **75** (`75-headermatcher-absence-parity`, cross-cutting correctness: `HeaderMatcher` ABSENCE-SEMANTICS parity with `envoyproxy/envoy:v1.33.0`). **PARENT PHASE 75 WAS SPLIT AT ITS §5 STATE-2 PLAN-WRITE (THIS session) under `BOOTSTRAP_PROMPT.md` §6.1 — ADR-0157 — on a RE-DERIVED ~1910 net LoC against the ~1500 gate.** No `PLAN.md` was written for parent 75; per §6.2 step 1 the oversize plan is ABANDONED, not completed. ROADMAP parent row `75` stays `in-progress` with its `sub-phases` cell now `75.1, 75.2`; row `75.1` is `in-progress` (this file names it); row `75.2` is `planned` and DEPENDS ON 75.1. **THE NEXT STATE IS 75.1's §5 state-2 PLAN-write, a SEPARATE session** — `75.1/SPEC.md` exists and `75.1/PLAN.md` does not, which is state 2 exactly. **ADR-0157 (the split) and ADR-0158 (the §6.2 empirical reconciliation) BOTH FIRED this session**; ledger head is now **ADR-0158**, next available **ADR-0159**. Next NEW fixture ids: **0083** (75.1), then **0084** + **0085** (75.2) — the parent SPEC's "next NEW fixture numbers 0083 + 0084" is SUPERSEDED, because the access-log path needs TWO fixtures. Fixture count at this session: **82** (unchanged — no fixture was created).
**slug:** `75.1-headermatcher-absence-engine-route` (parent `75-headermatcher-absence-parity`; sibling `75.2-headermatcher-absence-accesslog`).
**directory:** `docs/envoy-rust/phases/75.1-headermatcher-absence-engine-route/` — currently `SPEC.md` ONLY (authored SOLO this session; no `PLAN.md`, no `PROGRESS.md`, no `REVIEW.md` yet). The sibling `docs/envoy-rust/phases/75.2-headermatcher-absence-accesslog/` also holds `SPEC.md` ONLY and must NOT be worked until 75.1 is `done`. The PARENT `docs/envoy-rust/phases/75-headermatcher-absence-parity/SPEC.md` REMAINS ON DISK as the parent record and is a FROZEN artifact — it is NOT edited; the three corrections this session measured against it are carried by ADR-0158 and by the two sub-phase SPECs (D-3.5 append-only). The CLOSED phase 74's artifacts at `docs/envoy-rust/phases/74-accesslog-metadata-filter/` are reference, not obligation.

**status:** **PARENT PHASE 75 SPLIT — §5 state-2 PLAN-WRITE COMPLETE (THIS session); ACTIVE SUB-PHASE 75.1 AWAITS ITS OWN state-2 PLAN-write.** Cold-started fully per `BOOTSTRAP_PROMPT.md` §1 on `HEAD == 5d78df443461d002db5ce9cc9d6b238fe1de6b66` with `git status --porcelain` clean, branch `main`, and `git fetch origin --prune` showing `origin/main` at the SAME SHA at session start AND again immediately before the commit. CI on that SHA was already confirmed green (run `30142246106`, both jobs at full step counts). **This session did the §5 state-2 work and then FIRED THE §6.1 VALVE.** It re-measured the parent SPEC's PV-1..PV-10 against the LIVE tree rather than inheriting them: PV-1/PV-2 as MAIN-SESSION live cross-proxy probes (upstream `envoyproxy/envoy:v1.33.0` in Docker with `-p` PORT-MAPPING per memory `state0-recon-docker-needs-port-mapping`, envoy-rust as the DEBUG `target/debug/envoy-bin` host subprocess with `cargo build -p envoy-bin` FIRST per memory `differential-harness-uses-debug-envoy-bin`, backend-free `clusters: []` + `direct_response` throughout, a FRESH FILENAME per config revision per the stale-bind-mount trap, and a graceful `docker stop -t 15` + `docker cp` for the access-log flush); PV-3..PV-10 as THREE READ-ONLY recon fan-outs (told NOT to write and NOT to run `cargo` — the lock serializes), every load-bearing finding of which the main session RE-VERIFIED on disk before adopting (ADR-0127). **PV-1 REPRODUCED EXACTLY** and was EXTENDED — the empty-VALUE control was measured for ALL 13 probes (the parent SPEC had it for `present_match: true` only): 65 cells, **14 divergent** (6 = D1's absent cell on every value mode; 4 = p08 `present_match: false` + invert, every PRESENT cell; 4 = p12 `present_match: false`, every PRESENT cell), and the empty-value column is FULL PARITY except p12 — so the axis is PRESENCE, not emptiness, on both proxies. **PV-2 REPRODUCED and was EXTENDED from the parent SPEC's UPSTREAM-ONLY table to BOTH PROXIES**, plus a ninth sink (s9 = non-inverted `present_match: true`) and an `/empty` request; every cell matches the route matrix CELL-FOR-CELL, so the rule is UNIFORM across subsystems, and s6 independently re-confirms `treat_missing_header_as_empty` is HONORED upstream, not merely accepted. **PV-3 CONFIRMED with ZERO line drift** across all five call sites, the ADR-0150 seam and the H2 `resolve_route` delegation (one citation correction: the `network_rbac.rs` Permission arm is `:125-131`, not `:123-129`). **THREE CORRECTIONS to the parent SPEC, recorded in ADR-0158 rather than edited into the frozen artifact:** **C1 (the load-bearing one)** — there is a **THIRD** divergence-encoding test, `present_match_false_returns_true_when_present` (`matcher.rs:342`), which asserts D2's wrong behavior and whose comment at `:343` states the wrong rule verbatim, so the AMEND LIST IS THREE TESTS NOT TWO — and the parent SPEC's claim that "`present_match: false` has no behavioral test anywhere" is **FALSE**; **C2** — `BEHAVIOR_CONTRACT.md:1878-1880` calls the HeaderMatcher rule `want ? present : true` and a "MATERIAL DIVERGENCE" from `ValueMatcher`, which goes stale (the two AGREE for the present case and still DIFFER for the absent case), a site the parent SPEC's §6 did not scope; **C3** — the `codec_type` claim is a real MECHANIC (`bootstrap.rs:1103` has no serde default) but NOT a per-side divergence, since all 82 fixtures write it on BOTH sides. **THE DECISIVE FINDING, and the proximate cause of the split:** the byte-exact access-log driver supports **exactly ONE log file per side** — `AccessLogPaths` (`tests/differential/src/lib.rs:1088-1093`) is `{ envoy, envoy_rust }` under `deny_unknown_fields`, the arm reads exactly those two paths, only the envoy-side PARENT DIR is bind-mounted (`lib.rs:4019`), and across all 82 fixtures the max sink count in any config is **1** (verified by the main session itself, not just by a subagent). That **REFUTES the parent SPEC §5's multi-sink `0084` design** under its own R-0.7 "both drivers reused with ZERO change" constraint, so the access-log path needs **TWO** fixtures (`0084` for D1, `0085` for D2), mirroring the `0081`/`0082` sibling-pair precedent. **PV-9 (the load-bearing regression hunt) found the COMPLETE break set: FOUR in-process test assertions across THREE tests in ONE file, and NOTHING else** — ZERO risk to every fixture YAML (`invert_match` appears in ZERO `.yaml` anywhere; no fixture exercises `HeaderMatcher.present_match`), ZERO risk to every fuzz corpus seed (`parse_bootstrap` is PARSE-ONLY and never calls `HeaderMatcher::matches`; `present_match: false` appears in ZERO of the 57 `present_match` corpus files), ZERO risk from `Default` (neither `HeaderMatcher` nor `HeaderMatcherMode` derives it and the deserializer requires exactly one mode key), and ZERO risk to the whole `ValueMatcher`/RBAC/metadata surface. **PV-8 RE-DERIVED ~1910 net LoC against the ~1500 gate** — over ROBUSTLY, not marginally (~1650 even excluding the newly-forced `0085`), dominated by fixture `0083` at ~560 where the parent SPEC §8 projected 255, a number measured against comparables ON DISK (`0007` is 183 lines for ONE matcher and TWO probes; `0083` carries EIGHT matchers and ~24 probes). **SPLIT FIRED on exactly the line the parent SPEC §8 named in advance**, which the driver finding independently reinforces: **75.1** = the mode-scoped engine fix + SIX corrected doc comments + THREE amended tests + THREE strengthened guards + the in-process engine matrix + consumer propagation tests across all FIVE call sites + fixture `0083` + the §C rewrite + C2 + the two in-scope citation fixes (~1210 LoC / ~12-14 tasks); **75.2** = fixtures `0084`+`0085` + the `present_match`-polarity contract subsection + the CF-72-2/CF-75-1 banking + the M74-31 five-site fold (~725 LoC / ~6-8 tasks). Both halves are independently green and differentially witnessed, so §6.3 holds. **NO code changed, NO `crates/`, NO `tests/`, NO fixture, NO `ci.yml`, NO `known-failures.txt` — docs-only.** **No `stop` file** (the `ROADMAP.md` `## Feature Families` families at `ROADMAP.md:58` remain largely unbuilt, so the mission is NOT complete).

_Historical (the phase-75 §5 STATE-0/1 NEXT-PHASE PICK Active-phase `**id:**` + `**slug:**` + `**directory:**` + `**status:**` blocks + that block's own `_Historical_` pointer line — superseded by this phase-75 §5 STATE-2 PLAN-WRITE → §6.1 SPLIT; relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this session per ADR-0035.)_
> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases incl. the phase-51 state-3/4/5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill


**The next session runs the §5 STATE-2 PLAN-WRITE FOR SUB-PHASE 75.1** (`superpowers:writing-plans`, per the `SKILL_ROUTING.md` state machine — `docs/envoy-rust/phases/75.1-headermatcher-absence-engine-route/SPEC.md` exists and its `PLAN.md` does not, which is state 2 exactly). **It plans 75.1 ONLY** — the mode-scoped engine fix, the doc-comment corrections, the three amended tests, the three strengthened guards, the in-process engine matrix, the five consumer propagation tests, differential fixture `0083` and the `BEHAVIOR_CONTRACT.md` §C rewrite. **It must NOT plan 75.2** (fixtures `0084`/`0085` + the contract bank), which is a separate row, `planned`, depending on 75.1. **Do NOT re-run the parent phase's §6.1 adjudication** — it is settled by ADR-0157 on a re-derived ~1910 LoC, and 75.1's own re-derived ~1210 LoC / ~12-14 tasks sits comfortably inside the gate; re-derive it once against the live tree as PV-8 discipline requires, but expect it to hold. **Do NOT re-measure PV-1..PV-10 from scratch** — they were re-measured THIS session and the results are banked in ADR-0158 and in `75.1/SPEC.md` §2.3/§3/§10 with file:line citations; spot-verify the citations the PLAN depends on (they were accurate to the line this session) and re-run only what the plan's own diffs touch. Write `PLAN.md` **SOLO**; fan out only genuinely independent read-only recon. Run a clippy pre-flight over the PLAN's own literal Rust (memory `plan-md-example-code-trips-clippy` — the plan's example code repeatedly fails the plan's OWN `-D warnings` gate: `derivable_impls`, `collapsible_if`). It does NOT implement anything (that is state-3, a SEPARATE session).

**SUB-PHASE 75.1 SCOPE AT A GLANCE** (full detail in `75.1/SPEC.md`; every behavioral claim MEASURED — the route matrix and the access-log sink table were re-measured cross-proxy at the state-2 PLAN-write). **THE MEASURED RULE:** `present_match(want)` → `(present == want) ^ invert_match`; **every other mode → `false` when the header is ABSENT** (`invert_match` is NOT applied to a missing header); else `mode_matches(value) ^ invert_match`. An EMPTY VALUE counts as PRESENT. **IN scope for 75.1 — the engine and the ROUTE path.** Replace the uniform `mode_result ^ self.invert_match` at `crates/envoy-config/src/matcher.rs:52` with that rule, closing **D1** (= CF-72-1: a VALUE matcher — `exact_match`/`prefix_match`/`suffix_match`/`safe_regex_match`/`range_match`/`string_match` — + `invert_match` + ABSENT header; upstream DROPS, envoy-rust KEEPS; confirmed on all SIX modes) and **D2** (`present_match: false` means the header must be ABSENT upstream, but is UNCONDITIONALLY TRUE in-tree at `matcher.rs:47`, with the wrong rule stated verbatim in the doc comment at `:44-45`; it fires on a **plain, NON-inverted, single-line** matcher). **PRESERVE P1** (`present_match: true` + invert = FULL PARITY in both cells) — **a naive uniform "absent ⇒ DROP" fix BREAKS it and mints a NEW divergence**; the guards are `matcher.rs:425` and `matcher.rs:463`, and the latter's own doc comment instructs the fixer to preserve it. **AMEND THREE tests** (not two — ADR-0158 C1): `pv4_value_matcher_absent_plus_invert_kept_diverges_from_upstream` (`matcher.rs:432`; assertions `:449` + `:457`), `header_match_trait_delegates_to_inherent_engine` (`matcher.rs:489`; assertion `:503`), and **`present_match_false_returns_true_when_present` (`matcher.rs:342`)**. **KEEP GREEN** `matcher.rs:425`, `:463` and `:348` (the last yields the right answer for the wrong stated reason — restate its rationale, do not flip it). **CORRECT SIX doc comments:** `matcher.rs:44-45`, `matcher.rs:61-63` (the ADR-0150 seam doc asserts the old XOR as a design GUARANTEE), `matcher.rs:330`, `bootstrap.rs:3142-3143`, `bootstrap.rs:3119-3121`, and `crates/envoy-accesslog/src/filter.rs:135-138` — per memory `mechanical-fanout-scripts-corrupt-doc-comments`, `cargo fmt` does NOT reflow `///`/`//!`/`//` lines, so wrap-check each BY HAND and grep the commit's `+` lines for `///`. Add the in-process engine matrix (7 modes × 3 presence states × 2 invert polarities + the empty-VALUE control) and consumer propagation tests across all FIVE call sites — `envoy-http1/src/hcm.rs:2165` (route, serving BOTH H1 and H2 via `envoy_http1::hcm::resolve_route`, called at `envoy-http2/src/hcm.rs:475`), `envoy-filter/src/rbac.rs:60`, `envoy-filter/src/fault.rs:76`, `envoy-filter/src/jwt_authn.rs:185`, `envoy-accesslog/src/filter.rs:139` — the last via `Arc<dyn HeaderMatch>` so the ADR-0150 trait object is exercised, not only the inherent method. **Network RBAC is NOT in radius** (`network_rbac.rs:125-131` / `:151-157` bind `(_)` behind a `debug_assert!`); nor are cors/csrf (they call `StringMatcher::matches`). NEW backend-free fixture **`0083`** (`kind: http1_probe_list`, `clusters: []` + `direct_response`, 8 matchers × ~3 request variants) — **the FIRST differential witness of `invert_match` AND of `HeaderMatcher.present_match` in the entire corpus.** Rewrite `BEHAVIOR_CONTRACT.md` §C (`:2357-2377` — verified exact; do not cross the `**§D …**` boundary at `:2379`) from an ACCEPTED divergence into the parity rule with CF-72-1 CLOSED, correct `:1878-1880` (C2), and fix exactly TWO stale `matcher.rs:51`→`:52` citations: `BEHAVIOR_CONTRACT.md:2369` and the IN-SOURCE one at `matcher.rs:439`. **ADDS NO CONFIG SURFACE** — `HeaderMatcher` already carries `name` + `mode` + `invert_match` and already deserializes AND validates `present_match: false`; the entire fix is behavioral. **§7.4: NO new fuzz target, NO new corpus seed, NO `ci.yml` step** (CONFIRMED this session, not inherited). **OUT of 75.1:** the access-log fixtures `0084`/`0085`, the `present_match`-polarity contract subsection, the CF-75-1 and CF-72-2 rows and the M74-31 fold — all **75.2**. Also out (unchanged): CF-72-2's three REJECT-direction members (name-only `{ name }`, `treat_missing_header_as_empty`, the top-level `contains_match` arm), which boot-fatal here so a differing config never runs and which **cannot be differentially witnessed until implemented**; and `exact_match: ""` (CF-75-1). **THE DOMINANT §7.5 RISK IS GATE (b), NOT (a)** — a shared-engine change under five subsystems; the new fixture will pass, the danger is a PRE-EXISTING witness. PV-9 enumerated the complete break set (four assertions, three tests, one file) and found ZERO fixture and ZERO corpus risk, but still watch `0007-http1-direct-response` (the only other route-header-matching witness), `0017-http-filter-rbac`, `0018-http-filter-fault` and `0078`-`0082`.


**Standing traps** (enduring). Cold-start reads `STATE.md` FIRST (disk-authoritative). Confirm CI with the **FULL 40-char SHA** (memory `gh-run-list-commit-needs-full-sha`); a job `cancelled` with `runner_name:""` + `steps:0` never got a runner — rerun the same SHA (memory `ci-run-cancelled-with-no-runner-is-starvation`). `next-prompt.txt` is gitignored — refresh on disk, do NOT `git add` it. The documented host-flake set is CI-authoritative (adjudicate with `--no-fail-fast` + full-output redirect — never `tail`; isolation re-runs name the target binary, `0 passed` is NOT a pass; a MASS differential RED (`client error (Connect)`) is the Docker daemon down — memory `docker-desktop-down-after-reboot-kvm-acl`). `cargo build -p envoy-bin` before ANY local differential (memory `differential-harness-uses-debug-envoy-bin`); `cargo fuzz` from the CRATE dir — and note it takes a **DIRECTORY, not a file** (passing a seed file errors `The required directory … does not exist`); a NEW fuzz corpus seed needs a `!`-un-ignore line (memory `fuzz-corpus-seed-gitignored-by-default`, verified with `git ls-files` — now **63** tracked seeds). Do NOT revert landed `67`-`74` work NOR the parallel workstream's `envoy-http2` header-list bound; never add `rbac` to `is_terminal_network_filter`; never reject `filters: []`; preserve ADR-0016/0124/0131/0133/0136/0137/0139/0140-0143/0144-0149/**0150**/**0151**/**0152**/**0153**/**0154**/**0155**; **ADR-0028 NOT lifted.** Do NOT disturb the **36** access-log fixtures (the 34 prior incl. `0076`-`0080`, plus the new `0081`/`0082`) and do NOT relax the phase-73 composition-arm `< 2` rule. Never weaken a fixture; **never trim `known-failures.txt`** (memory `h2spec-3-5-2-preface-host-sensitive`). Any ROADMAP row edit preserves 6 cells + escapes `\|`; rows `36`/`38`/`39`/`52`/`54` are already malformed — do NOT "fix" them (append-only). `DECISIONS.md` is NOT chronological (memory `decisions-md-not-chronological`; ledger head **ADR-0155**). **envoy-bin writes `ConfigError` to STDOUT.** `#![forbid(unsafe_code)]` holds at every crate root. `STATE.md` top-section blocks are single 3000-8000 char lines — script-edit + 0×-grep (memory `state-md-blocks-are-single-lines-edit-hazard`); ADR-0035 relocation checks are DELTA-based, never "no duplicates" (memory `state6-relocation-check-must-be-delta-based`). `cargo deny` can red on a fresh unrelated RustSec advisory (patch-bump the dep — memory `cargo-deny-reds-on-unrelated-advisory`). **The ADR-0150 seam held through this phase:** `envoy-accesslog` still has ZERO workspace deps; the new `MetadataMatch` matcher is an injected **trait object** exactly like `LogFilter::Header`'s `Arc<dyn HeaderMatch>`; `LogFilter` still has NO `Eq`/`PartialEq`. CF-72-1 is MODE-SCOPED (memory `envoy-headermatcher-invert-absent-is-mode-dependent`): value-matcher+invert+absent DIVERGES, `present_match`+invert+absent is PARITY — a future fixer MUST preserve the `present_match` KEEP. **Phase-74-specific:** `metadata_filter`'s `matcher.invert` is a DIFFERENT field on a DIFFERENT message — MEASURED accepted-but-INERT upstream, so it stays boot-fatal here (CF-74-1); do NOT "implement" it. **Fixture `0082` must keep OMITTING `on_header_missing`** (ADR-0155 PV-6) — envoy-rust requires a `value` on it, which would make the key RESOLVE and silently vacate the key-not-found witness. **The SAME trap now applies to `0081`**, which since the §5.2 re-entry carries a THIRD probe sending NO `x-a`: adding `on_header_missing` there would make that key resolve and vacate the `match_if_key_not_found` default-`true` witness. **`0081` now has THREE probes and expects TWO logged lines** (`STATUS=200 PATH=/x M=-` then `STATUS=200 PATH=/x M=1`, byte-distinct so ORDER is pinned, kept-LAST preserved by placing the new probe SECOND) — a stale `target/debug/envoy-bin` will mis-report it, so `cargo build -p envoy-bin` before ANY local differential. **CF-74-6:** the wrapped `match_if_key_not_found: { value: <bool> }` spelling is accepted+honored upstream and BOOT-FATAL here — a documented REJECT-direction load-parity gap pinned by `metadata_filter_deserialize_round_trip_and_defaults`; do NOT "fix" it by making `Option<bool>` wrapper-accepting (that would lose the absent-vs-explicit-`false` distinction `unwrap_or(true)` depends on). **PHASE-75-SPECIFIC (added at this pick).** **The fix MUST be MODE-SCOPED** — `present_match` + `invert_match` is measured PARITY in both cells, so a naive uniform "absent ⇒ DROP" BREAKS it and mints a NEW divergence; the guard tests are `matcher.rs:425` and `matcher.rs:463`, and the latter's own doc comment instructs the fixer to preserve it. **Honor TDD's RED with a mutation check** that breaks the mode-scoping and watches the P1 guard go RED — run it in a **scratch `git worktree`**, never in the main tree with parallel subagents active (memory `mutation-checks-collide-with-parallel-subagents`), and grep the run for `Compiling` before believing a green (memory `mutation-check-needs-forced-rebuild`; note `clippy` prints `Checking`, not `Compiling`). **TWO CONFLATION TRAPS, do NOT unify them:** `HeaderMatcher.present_match` (this phase) vs `ValueMatcher.present_match` (RBAC/metadata — `bootstrap.rs:1704` and `BEHAVIOR_CONTRACT.md:1863-1885` record that `present_match: false` NEVER matches there, a DIFFERENT and CORRECT rule); and `HeaderMatcher.invert_match` (this phase) vs `MetadataMatcher.invert` (CF-74-1 — accepted-but-INERT upstream, boot-fatal here, must NOT be "implemented"). **Recon traps banked this session:** this host's Docker bind mounts are **STALE-CACHED** — after editing a config in a bind-mounted directory the container kept reading the PREVIOUS contents (verified with `docker run --entrypoint sed`), so `--mode validate` reported a PGV error for a constraint the on-disk file no longer violated; **use a FRESH FILENAME for every config revision**. `--volumes-from` does NOT retrieve a stopped container's `/tmp` — use `docker cp`. envoy-rust requires an explicit `codec_type` on the HCM where upstream defaults it. A parallel workstream's container (`quizzical_goldstine`, image `envoyproxy/envoy:v1.33.0`) was running and was LEFT ALONE; only this session's own `er-recon-*` containers were removed. **PHASE-75.1/75.2-SPECIFIC (added at the §6.1 SPLIT).** **Parent 75 has NO `PLAN.md` and never will** — §6.2 step 1 abandons the oversize plan; do not "finish" it. **The parent `75-headermatcher-absence-parity/SPEC.md` is a FROZEN artifact** — the three corrections ADR-0158 measured against it (C1 the third divergence-encoding test, C2 the stale `BEHAVIOR_CONTRACT.md:1878-1880` cross-reference, C3 the `codec_type` non-divergence) are carried by that ADR and by the sub-phase SPECs; do NOT edit it, and do NOT re-derive the split. **75.2 MUST NOT START until 75.1 is `done`** — both its fixtures assert POST-fix behavior and would be RED on a pre-75.1 tree. **The byte-exact access-log driver takes exactly ONE log file per side** (`AccessLogPaths`, `tests/differential/src/lib.rs:1088-1093`, `deny_unknown_fields`; only the envoy-side parent dir is bind-mounted at `lib.rs:4019`; max sinks in any of the 82 fixtures = 1) — a multi-sink access-log fixture is INFEASIBLE without a driver change, which is why 75.2 has TWO fixtures and why the next ids are **0083**, **0084**, **0085**. **The amend list is THREE tests, not two** (`matcher.rs:342` in addition to `:432` and `:489`). **Correct exactly TWO `matcher.rs:51` citations** (`BEHAVIOR_CONTRACT.md:2369` and the IN-SOURCE `matcher.rs:439`) out of the 26 in the repo — the five in `DECISIONS.md` (`:2479`/`:2546`/`:2555`/`:2624`/`:2631`, plus its already-stale `:373`/`:397`) are APPEND-ONLY, as are the historical phase docs and `STATE_HISTORY.md`. **Upstream Envoy will not CREATE an access-log directory** — a `path:` under a nonexistent dir is boot-fatal (`unable to open file … No such file or directory`); the differential harness creates and `chmod 0o777`s both parent dirs itself (`lib.rs:3991-4014`), so this bites only hand-rolled probes. **YAML 1.1 booleans:** an unquoted `cluster: y` under `node:` parses as boolean `true` and upstream's JSON-proto path then rejects the whole bootstrap (`@ node.cluster: string, … unexpected character 't'`) while envoy-rust's serde accepts it — quote scalar node fields in hand-rolled probe configs. **A `pkill -f <pattern>` whose pattern also matches the invoking shell's own command line kills that shell** — kill by PID or use `pgrep -x`. **Concurrency:** a parallel workstream was running containers named `p75f-s2` / `p75s-arm1a`..`arm4` on image `envoyproxy/envoy:contrib-v1.37.2` — **NOT this project's `v1.33.0` pin**, so they are not authoritative for anything here; they and `quizzical_goldstine` were LEFT ALONE, and only this session's own `er-pv*` containers were removed.
_Historical (the phase-75 §5 STATE-0/1 NEXT-PHASE PICK `## Next expected skill` pointer + the superseded `PHASE-75 SCOPE AT A GLANCE` block + that block's own `_Historical_` pointer line — superseded by this phase-75 §5 STATE-2 PLAN-WRITE → §6.1 SPLIT; relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this session per ADR-0035. The Standing-traps block is ENDURING and is carried forward IN PLACE with the 75.1/75.2 additions APPENDED, not relocated; the parent-phase scope block is replaced by the SUB-PHASE 75.1 SCOPE block.)_
> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases incl. the phase-37 state-1..5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit


**Phase-75 §5 STATE-2 PLAN-WRITE → §6.1 SPLIT — parent 75 split into 75.1 + 75.2; ADR-0157 (split) + ADR-0158 (reconciliation) fired; NO `PLAN.md` for parent 75; NO code changed:** one DOCS-ONLY commit on top of the phase-75 state-0/1 pick `5d78df443461d002db5ce9cc9d6b238fe1de6b66`, touching SIX files — the NEW `docs/envoy-rust/phases/75.1-headermatcher-absence-engine-route/SPEC.md`, the NEW `docs/envoy-rust/phases/75.2-headermatcher-absence-accesslog/SPEC.md`, `DECISIONS.md` (ADR-0157 + ADR-0158, inserted at the head of the newest-first block above ADR-0156), `ROADMAP.md` (parent row `75`'s `sub-phases` cell `—` → `75.1, 75.2` — the ONLY mutable columns per §4.1 invariant 2, its append-only summary cell untouched — plus TWO APPENDED rows `75.1` [`in-progress`] and `75.2` [`planned`, depends-on `75.1`]; all three verified at 7 pipes / 6 cells, and the seven pre-existing malformed rows `36`/`38`/`39`/`52`/`54`/`66`/`70` left untouched), `STATE.md` and `STATE_HISTORY.md`. **NO `crates/`, NO `tests/`, NO fixture, NO `expectations.yaml`, NO `ci.yml`, NO conformance file, and NO edit to the frozen parent `75/SPEC.md` or to any landed ADR.** Fixture count stays **82**; `access_log`-carrying **36**; corpus seeds **63**; `known-failures.txt` **21** lines. Ledger head is now **ADR-0158**, next available **ADR-0159**. `#![forbid(unsafe_code)]` holds at all 22 member roots — untouched. **Carry-forward movement: none consumed** (CF-72-1 is still only SCOPED for consumption, by 75.1; M74-31 is now scoped to 75.2; CF-75-1 and the extended CF-72-2 are scoped to 75.2's contract bank). Next: **75.1's §5 state-2 PLAN-write** (a SEPARATE session).

_Historical (the phase-75 §5 STATE-0/1 NEXT-PHASE PICK Last-commit block + that session's own `_Historical_` pointer line — superseded by this phase-75 §5 STATE-2 PLAN-WRITE → §6.1 SPLIT Last-commit block; relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this session per ADR-0035.)_
> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases incl. the phase-37 state-1..5 sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated


2026-07-25 (phase-75 **§5 STATE-2 PLAN-WRITE COMPLETE — §6.1 SPLIT FIRED: parent 75 → 75.1 + 75.2; ADR-0157 + ADR-0158 FIRED; NO `PLAN.md` written for parent 75**. Confirmed `git status --porcelain` clean, branch `main`, `HEAD` = the phase-75 state-0/1 pick `5d78df443461d002db5ce9cc9d6b238fe1de6b66` with `origin/main` at the SAME SHA at session start and again immediately before the commit, so no sibling had advanced; CI on that SHA was already green (run `30142246106`). Re-measured the parent SPEC's PV-1..PV-10 against the LIVE tree rather than inheriting them — PV-1/PV-2 as MAIN-SESSION live cross-proxy probes on BOTH proxies, PV-3..PV-10 via THREE read-only recon fan-outs whose load-bearing findings the main session re-verified on disk (ADR-0127). **The re-measurement changed the phase's shape three times over.** (1) PV-1 reproduced exactly but the EXTENDED empty-value control across all 13 probes established presence-not-emptiness on both proxies; PV-2 was extended from upstream-only to BOTH proxies plus a ninth sink and matched the route matrix cell-for-cell. (2) **THREE corrections to the parent SPEC** (ADR-0158): C1 a THIRD divergence-encoding test at `matcher.rs:342` — so the amend list is THREE not two, and the SPEC's "`present_match: false` has no behavioral test anywhere" is FALSE; C2 a stale `BEHAVIOR_CONTRACT.md:1878-1880` cross-reference the SPEC's §6 did not scope; C3 the `codec_type` claim is a real mechanic but not a per-side divergence. (3) **The decisive finding: the byte-exact access-log driver takes exactly ONE log file per side**, which REFUTES the parent SPEC §5's multi-sink `0084` design under its own "zero driver change" constraint — verified by the main session itself (`AccessLogPaths` at `tests/differential/src/lib.rs:1088-1093`; single bind-mount at `:4019`; max 1 sink across all 82 fixtures) — so the access-log path needs TWO fixtures and the next ids are `0083`, `0084`, `0085`. **PV-8 re-derived ~1910 net LoC against the ~1500 gate** (dominated by fixture `0083` at ~560 where §8 said 255, measured against on-disk comparables), over the gate robustly rather than marginally, so **the §6.1 valve FIRED on exactly the line the parent SPEC §8 named in advance** — 75.1 = engine fix + all in-process tests + fixture `0083` + the §C rewrite (~1210 LoC); 75.2 = fixtures `0084`+`0085` + the polarity subsection + the CF-75-1/CF-72-2 bank + the M74-31 fold (~725 LoC). Both halves independently green and differentially witnessed, so §6.3 holds; the rejected alternatives (ship anyway, trim the coverage that would reproduce the original defect, change the driver, split fix-from-witness, split three ways) are recorded in ADR-0157. PV-9 found the COMPLETE break set — four assertions, three tests, one file — with ZERO fixture and ZERO corpus risk. Wrote both sub-phase `SPEC.md`s **SOLO** (subagents returned EVIDENCE only, ADR-0127). Appended ROADMAP rows `75.1`/`75.2` and mutated ONLY parent row `75`'s `sub-phases` cell [all three rows verified at 7 pipes / 6 cells; the malformed rows `36`/`38`/`39`/`52`/`54`/`66`/`70` untouched — memory `roadmap-rows-need-escaped-pipes`]. Fired **ADR-0157** (the split, claiming the number ADR-0156 reserved for exactly this) and **ADR-0158** (the reconciliation), both inserted at the head of the newest-first block [memory `decisions-md-not-chronological`]. Relocated the four superseded top-section blocks into `STATE_HISTORY.md` per ADR-0035 [archive headers selected by EXACT equality, memory `state-history-archive-headers-need-exact-match`; both files backed up first; the preservation check DELTA-based, memory `state6-relocation-check-must-be-delta-based`] and renamed the active-phase Notes subsection in place. The Standing-traps block was carried forward IN PLACE with 75.1/75.2 additions APPENDED — nothing removed. NO code changed. **Do NOT chain into 75.1's state-2 PLAN-write** [a SEPARATE session per §5.1]; **no `stop` file** [the `ROADMAP.md` `## Feature Families` families at `ROADMAP.md:58` remain largely unbuilt and the carry-forward set is live]. Next ADR **ADR-0159**. Live carry-forwards: **CF-72-2** [extended], **CF-75-1**, CF-74-1/2/3/4/6 [CF-74-5 CLOSED], CF-73-1, N73-R2, M73-R1/M73-R2, M71-3, M71-6/7/8, M70-R4/R9, M69-A..I, CF-69-1/2/3/5, M68-1, M-1, CF-67-3/5/6/7, M74-3..M74-14, M74-16, M74-17/18/20/21/22/26/27/28/29, M74-30..M74-39, the older Minors + the HTTP-filters-family (1)-(4). **CF-72-1 is SCOPED FOR CONSUMPTION by 75.1** and **M74-31 by 75.2**, but neither is consumed until its sub-phase lands.)

_Historical (the phase-75 §5 STATE-0/1 NEXT-PHASE PICK Last-updated note + that session's own `_Historical_` pointer line — superseded by this phase-75 §5 STATE-2 PLAN-WRITE → §6.1 SPLIT Last-updated note; relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this session per ADR-0035.)_
> Historical `## Last updated` notes — every superseded last-updated note (all closed phases incl. the phase-37 state-1..5 sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsection for fully-closed phase 64 (the `### Phase-64 state-5 code-review` narrative — phase 64 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-64 state-6 close-out when ROADMAP row `64` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 63 (the `### Phase-63 state-5 code-review` narrative — phase 63 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-63 state-6 close-out when ROADMAP row `63` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 61 (the `### Phase-61 state-1 brainstorm` / `### Phase-61 state-2 PLAN-write` / `### Phase-61 state-3 implementation` / `### Phase-61 state-4 verification` / `### Phase-61 state-5 code-review` narratives, relocated at the phase-61 state-6 close-out when ROADMAP row `61` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 58 (the `### Phase-58 state-1 brainstorm` / `### Phase-58 state-2 PLAN-write` / `### Phase-58 state-3 implementation` / `### Phase-58 state-4 verification` / `### Phase-58 state-5 code-review` narratives, relocated at the phase-58 state-6 close-out when ROADMAP row `58` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 57 (the `### Phase-57 state-1 brainstorm` / `### Phase-57 state-2 PLAN-write` / `### Phase-57 state-3 implementation` / `### Phase-57 state-4 verification` / `### Phase-57 state-5 code-review` narratives, relocated at the phase-57 state-6 close-out when ROADMAP row `57` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 42 (the `### Phase-42 state-5 code-review (active phase)` narrative — phase 42 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-42 state-6 close-out when ROADMAP row `42` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 43 (the `### Phase-43 state-5 code-review (active phase)` narrative — phase 43 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-43 state-6 close-out when ROADMAP row `43` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 44 (the `### Phase-44 state-5 code-review (active phase)` narrative — phase 44 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-44 state-6 close-out when ROADMAP row `44` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 45 (the `### Phase-45 state-5 code-review (active phase)` narrative — phase 45 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-45 state-6 close-out when ROADMAP row `45` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 46 (the `### Phase-46 state-5 code-review (active phase)` narrative — phase 46 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-46 state-6 close-out when ROADMAP row `46` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 47 (the `### Phase-47 state-5 code-review (active phase)` narrative — phase 47 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-47 state-6 close-out when ROADMAP row `47` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 48 (the `### Phase-48 state-5 code-review (active phase)` narrative — phase 48 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-48 state-6 close-out when ROADMAP row `48` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 49 (the `### Phase-49 state-5 code-review (active phase)` narrative — phase 49 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-49 state-6 close-out when ROADMAP row `49` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 50 (the `### Phase-50 state-3 implementation (active phase)` narrative — phase 50 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-50 state-6 close-out when ROADMAP row `50` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 51 (the `### Phase-51 state-3 implementation (active phase)` narrative — phase 51 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-51 state-6 close-out when ROADMAP row `51` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 53 (the `### Phase-53 state-1 brainstorm` narrative — phase 53 kept a single active-phase Notes subsection at "state-1 brainstorm" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-1 subsection remained in `## Notes` at close — relocated at the phase-53 state-6 close-out when ROADMAP row `53` flipped `done`, together with the inline superseded state-3 narrative) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 54 (the `### Phase-54 state-1 brainstorm` / `### Phase-54 state-2 PLAN-write` / `### Phase-54 state-3 implementation` / `### Phase-54 state-4 verification` / `### Phase-54 state-5 code-review` narratives, relocated at the phase-54 state-6 close-out when ROADMAP row `54` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 55 (the `### Phase-55 state-5 code-review (active phase)` narrative — phase 55 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state individually superseded in place and relocated to STATE_HISTORY.md at its own transition], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-55 state-6 close-out when ROADMAP row `55` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 56 (the `### Phase-56 state-5 code-review (active phase)` narrative — phase 56 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state individually superseded in place and relocated to STATE_HISTORY.md at its own transition], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-56 state-6 close-out when ROADMAP row `56` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 66 (the `### Phase-66 state-5 code-review (active phase)` narrative — phase 66 used a rename-in-place active-phase Notes subsection across its state-0/1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-66 state-6 close-out when ROADMAP row `66` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed sub-phase 67.1 (the `### Phase-67.1 §5 state-5 code-review` narrative — sub-phase 67.1 used a rename-in-place active-phase Notes subsection across its state-2..5 + §5.2 state-3 re-entry arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-67.1 state-6 close-out when ROADMAP row `67.1` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed sub-phase 67.2 (the `### Phase-67.2 §5 state-5 re-review` narrative — sub-phase 67.2 used a rename-in-place active-phase Notes subsection across its state-2..5 + §5.2 state-3 re-entry + state-4-re-verification + state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review subsection remained in `## Notes` at close — relocated at the phase-67.2 state-6 close-out when ROADMAP row `67.2` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
> Historical Notes subsection for fully-closed phase 68 (the `### Phase-68 §5 state-5 code-review` narrative — phase 68 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-68 §5 state-6 close-out when ROADMAP row `68` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 69 (the `### Phase-69 §5 state-5 re-review` narrative — phase 69 used a rename-in-place active-phase Notes subsection across its state-3 → §5.2 state-3 re-entry → state-4 re-verification → state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review subsection remained in `## Notes` at close — relocated at the phase-69 §5 state-6 close-out when ROADMAP row `69` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 70 (the `### Phase-70 §5 state-5 re-review (2nd)` narrative — phase 70 used a rename-in-place active-phase Notes subsection across its state-1..5 + two-§5.2-re-entry + two-state-4-re-verification + two-state-5-re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review-(2nd) subsection remained in `## Notes` at close — relocated at the phase-70 §5 state-6 close-out when ROADMAP row `70` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 71 (the `### Phase-71 §5 state-5 code-review` narrative — phase 71 used a rename-in-place active-phase Notes subsection across its state-0/1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-71 §5 state-6 close-out when ROADMAP row `71` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 72 (the `### Phase-72 §5 state-5 re-review` narrative — phase 72 used a rename-in-place active-phase Notes subsection across its state-1..5 + §5.2 state-3 re-entry + state-4 re-verification + state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 re-review subsection remained in `## Notes` at close — relocated at the phase-72 §5 state-6 close-out when ROADMAP row `72` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 73 (the `### Phase-73 §5 state-5 code-review` narrative — phase 73 used a rename-in-place active-phase Notes subsection across its state-0/1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-73 §5 state-6 close-out when ROADMAP row `73` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 74 (the `### Phase-74 §5 state-5 re-review (2nd)` narrative — phase 74 used a rename-in-place active-phase Notes subsection across its state-0/1..5 + TWO §5.2 state-3 re-entry + TWO state-4 re-verification + TWO state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review-(2nd) subsection remained in `## Notes` at close — relocated at the phase-74 §5 state-6 close-out when ROADMAP row `74` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Phase-75 §5 state-2 PLAN-write → §6.1 SPLIT (active sub-phase 75.1)

- **The §6.1 valve FIRED.** The parent `SPEC.md` §8 projected ~1230 net LoC / ~15-18 tasks and said outright that the valve was "genuinely live at state-2", requiring PV-8 to adjudicate on a RE-DERIVED number. Re-derived against the live tree: **~1910 net LoC** against the ~1500 gate — over ROBUSTLY, not marginally (~1650 even excluding the newly-forced fixture `0085`). The dominant error was fixture `0083` at **~560** where §8 said 255, a number now measured against comparables ON DISK: `0007-http1-direct-response` is 183 lines across its four files for ONE matcher and TWO probes, while `0083` carries EIGHT matchers and ~24 probes. Split on **exactly the line §8 named in advance** (ADR-0157).
- **The re-measurement, not the estimate, is what forced the split.** PV-1..PV-10 were re-measured rather than inherited: PV-1/PV-2 as MAIN-SESSION live cross-proxy probes on BOTH proxies, PV-3..PV-10 via three read-only recon fan-outs whose load-bearing findings the main session re-verified on disk (ADR-0127). The **decisive** finding is that the byte-exact access-log driver takes **exactly ONE log file per side** (`AccessLogPaths`, `tests/differential/src/lib.rs:1088-1093`, under `deny_unknown_fields`; only the envoy-side parent dir is bind-mounted at `lib.rs:4019`; max 1 sink across all 82 fixtures), which **REFUTES the parent SPEC §5's multi-sink `0084` design** under that same SPEC's own R-0.7 "both drivers reused with ZERO change" constraint. One sink carries one `header_filter`, so the access-log path needs **TWO** fixtures — a genuinely separable slice with its own design questions, which is what makes the split natural rather than arbitrary.
- **THREE corrections to the parent SPEC, carried by ADR-0158 rather than edited into the frozen artifact.** **C1** is load-bearing: `present_match_false_returns_true_when_present` (`matcher.rs:342`) asserts D2's wrong behavior and states the wrong rule in its own comment, so **the amend list is THREE tests, not two**, and the SPEC's claim that "`present_match: false` has no behavioral test anywhere" is **FALSE** — the test that pins the bug was sitting 90 lines above the ones the SPEC did name. **C2**: `BEHAVIOR_CONTRACT.md:1878-1880` calls the HeaderMatcher rule `want ? present : true` and a "MATERIAL DIVERGENCE" from `ValueMatcher`, which goes stale on both counts. **C3**: the `codec_type` claim is a real mechanic but NOT a per-side divergence.
- **PV-9 turned the phase's headline risk from a fear into an enumeration.** The complete set of things that break is FOUR in-process assertions across THREE tests in ONE file. ZERO risk to every fixture YAML (`invert_match` appears in ZERO `.yaml` anywhere in the repo; no fixture exercises `HeaderMatcher.present_match`), ZERO risk to every fuzz corpus seed (`parse_bootstrap` is PARSE-ONLY and never calls `HeaderMatcher::matches`), ZERO risk from `Default` (neither type derives it; the deserializer requires exactly one mode key), and ZERO risk to the `ValueMatcher`/RBAC/metadata surface. Gate (b) remains the right posture, but the surface is now known rather than feared.
- **The guard still governs the fix.** `present_match: true` + `invert_match` is MEASURED PARITY in both cells, and a naive uniform "absent ⇒ DROP" fix BREAKS it and mints a NEW divergence — phase 72 proved that by mutation check. Guards: `matcher.rs:425` and `matcher.rs:463`, the latter's own doc comment instructing the fixer to preserve it. 75.1's state-3 must honor TDD's RED with a mutation check that breaks the mode-scoping and watches `:463` go RED, run in a scratch `git worktree`.
- **Next:** **75.1's §5 state-2 PLAN-write** (a SEPARATE session per §5.1) — plan 75.1 ONLY (engine fix + in-process tests + fixture `0083` + the §C rewrite). Do NOT re-adjudicate the split, do NOT plan 75.2, and do NOT start 75.2 until 75.1 is `done`.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: **one state per session; do not chain states.** **PARENT PHASE 75 WAS SPLIT (§6.1, ADR-0157); the ACTIVE SUB-PHASE IS 75.1** (`75.1-headermatcher-absence-engine-route`; ROADMAP parent row `75` is `in-progress` with `sub-phases` = `75.1, 75.2`, row `75.1` is `in-progress`, row `75.2` is `planned` and depends-on `75.1`). ADR-0156 + **ADR-0157** (the split) + **ADR-0158** (the §6.2 reconciliation) are all FIRED; ledger head **ADR-0158**, next **ADR-0159**. **Parent 75 has NO `PLAN.md` and never will** — §6.2 step 1 abandons the oversize plan; do not "finish" it, and do not edit the FROZEN parent `75/SPEC.md`. `75.1/SPEC.md` exists and `75.1/PLAN.md` does not, so the next session is **75.1's §5 state-2 PLAN-write** (`superpowers:writing-plans`) — see `## Next expected skill` and the SUB-PHASE 75.1 SCOPE block above. It plans **75.1 ONLY** and must NOT re-adjudicate the split (settled on a re-derived ~1910 LoC; 75.1 itself re-derives to ~1210 LoC / ~12-14 tasks, comfortably inside the gate) nor re-measure PV-1..PV-10 from scratch (re-measured at the split and banked in ADR-0158 + `75.1/SPEC.md` with file:line citations that were accurate to the line). **Do NOT chain into state-3. Do NOT start 75.2 until 75.1 is `done`** — both of 75.2's fixtures assert POST-fix behavior and would be RED on a pre-75.1 tree. This sub-phase changes a SHARED matching engine read by FIVE subsystems, so the dominant §7.5 risk is gate **(b)** (pre-existing fixtures and tests), not gate (a) — though PV-9 enumerated the complete break set as FOUR in-process assertions across THREE tests in ONE file, with ZERO fixture and ZERO fuzz-corpus risk. The fix MUST be mode-scoped: `present_match` + `invert_match` is MEASURED PARITY and a naive uniform absent-DROP would break it. **The amend list is THREE tests, not two** (`matcher.rs:342` in addition to `:432` and `:489` — ADR-0158 C1). Live carry-forwards: **CF-72-2** (extended with the `contains_match` member), the NEW **CF-75-1**, CF-74-1/2/3/4/6 (**CF-74-5 CLOSED**), CF-73-1, N73-R2, M73-R1/M73-R2, M71-3, M71-6/7/8, M70-R4/R9, M69-A..I, CF-69-1/2/3/5, M68-1, M-1, CF-67-3/5/6/7, M74-3..M74-14, M74-16, M74-17/18/20/21/22/26/27/28/29, M74-30..M74-39, the older Minors + the HTTP-filters-family (1)-(4); **CF-72-1 is SCOPED FOR CONSUMPTION by 75.1** and **M74-31 by 75.2**, but neither is consumed until its sub-phase lands. **The mission is NOT complete** — the ROADMAP.md `## Feature Families` families (`ROADMAP.md:58`) remain largely unbuilt, so **no `stop` file**.
- **On the §5.1 rule and its ONE recorded exception (read `ADR-0127`).** The phase-66 state-6 close-out was run in the SAME session as its state-5 code-review, at **explicit human instruction**. That was a one-off, human-authorized override; `BOOTSTRAP_PROMPT.md`, `MISSION.md` and `SKILL_ROUTING.md` were **not modified**, and **§5.1 remains binding on every autonomous session.** ADR-0127 records why the 5→6 transition alone was safe to chain (the close-out changes no code, re-runs no gate, and consumes only disk facts) and why **0→1, 1→2, 2→3, 3→4 and 4→5 must never be chained** — each carries a genuine self-review hazard, since the context that wrote an artifact would be grading it. Per memory `closeout-and-pick-are-separate-sessions`, the next-phase pick is STILL a separate session. Do not read the git history as licence to chain.

> Historical Notes subsection for fully-closed sub-phase 67.3 (the `### Phase-67.3 §5 state-5 re-review` narrative — sub-phase 67.3 used a rename-in-place active-phase Notes subsection across its state-2..5 + §5.2 state-3 re-entry + state-4-re-verification + state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review subsection remained in `## Notes` at close — relocated at the phase-67.3 §5 state-6 close-out when ROADMAP row `67.3` [and parent row `67`] flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 65 (the `### Phase-65 state-5 code-review` narrative — phase 65 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-65 state-6 close-out when ROADMAP row `65` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsection for fully-closed phase 52 (the `### Phase-52 state-5 code-review (active phase)` narrative — phase 52 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2/3/4 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-52 state-6 close-out when ROADMAP row `52` flipped `done`, together with the standing 05.1/05.4 future-reviewer note it carried) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 41 (the `### Phase-41 state-5 code-review (active phase)` narrative — phase 41 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the §6.2-recon PIVOT from `%REQ_WITHOUT_QUERY%` to `%ROUTE_NAME%` landed at state-2; the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-41 state-6 close-out when ROADMAP row `41` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 40 (the `### Phase-40 state-5 code-review (active phase)` narrative — phase 40 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-40 state-6 close-out when ROADMAP row `40` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 39 (the `### Phase-39 state-5 code-review (active phase)` narrative — phase 39 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-39 state-6 close-out when ROADMAP row `39` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
> Historical Notes subsection for fully-closed phase 38 (the `### Phase-38 state-2 PLAN-write (active phase)` narrative — phase 38 used a single active-phase Notes subsection for its whole arc; the state-3/4/5 sub-state narratives were superseded in the four-top-section archive blocks per-session, so only this state-2 subsection remained in `## Notes` at close — relocated at the phase-38 state-6 close-out when ROADMAP row `38` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) and for parent phase 25 (the `### Phase-25 state-1 brainstorm` pick + recon-finding narrative, relocated at the 25.2 state-6 close-out when parent `25` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 26 (the `### Phase-26 state-1 brainstorm` pivot/rejected-alternatives/key-scoping narrative, relocated at the phase-26 state-6 close-out when row `26` flipped to `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 27 (the `### Phase-27 state-1 brainstorm` / `### Phase-27 state-2 PLAN-write` / `### Phase-27 state-4 verification` narratives + the now-consumed `### Phase-27 carry-forwards` [M26-1..M26-8] block, relocated at the phase-27 state-6 close-out when row `27` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 28 (the `### Phase-28 state-1 brainstorm` / `### Phase-28 state-2 PLAN-write` / `### Phase-28 state-3 implementation + state-4 verification` / `### Phase-28 state-5 code review` narratives + the now-consumed `### Phase-28 carry-forwards` [M27-1..M27-3] block, relocated at the phase-28 state-6 close-out when row `28` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 29 (the now-consumed `### Phase-29 carry-forwards` [M28-1..M28-3] block + the `### Phase-29 state-1 brainstorm` / `### Phase-29 state-2 PLAN-write` / `### Phase-29 state-3 implementation` / `### Phase-29 state-4 verification` / `### Phase-29 state-5 code review` narratives, relocated at the phase-29 state-6 close-out when row `29` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 30 (the `### Phase-30 carry-forwards` [M29-1/M29-2, which fed phase 30 but were NOT consumed — they continue as phase-31 carry-forwards] block + the `### Phase-30 state-1 brainstorm` / `### Phase-30 state-2 PLAN-write` / `### Phase-30 state-3 implementation` / `### Phase-30 state-4 verification` / `### Phase-30 state-5 code review` narratives, relocated at the phase-30 state-6 close-out when row `30` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 31 (the `### Phase-31 carry-forwards` [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 — open Minors from the phase-30 REVIEW.md that fed phase 31 but were NOT consumed; they continue as carry-forwards for the next phase that touches the differential hash-sweep driver / the config parser] block + the `### Phase-31 state-1 brainstorm` / `### Phase-31 state-2 PLAN-write` / `### Phase-31 state-3 implementation` / `### Phase-31 state-4 verification` / `### Phase-31 state-5 code review` narratives, relocated at the phase-31 state-6 close-out when row `31` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 32 (the `### Phase-32 carry-forwards` block — the open Minors that fed phase 32 but were NOT consumed: the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3; they continue as carry-forwards for the future phase that touches their surface — re-listed in `## Next expected skill` above alongside the 6 new phase-32 REVIEW.md Minors M32-1…M32-6), relocated at the phase-32 state-6 close-out when row `32` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phases 33+34 (the `### Phase-33 carry-forwards` block — the active-phase carry-forwards Notes that lived through phases 33 and 34: the now-CONSUMED 6 phase-32 REVIEW.md Minors M32-1…M32-6 [folded + landed at the phase-33 state-3] + the "Other still-live carry-forwards" list [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3 + the HTTP-filters-family (1)-(4) — those still-live ones re-listed in `## Next expected skill` above alongside the new phase-34 Minors M34-1/M34-2/M34-3 + the phase-33 M33-1/M33-2]), relocated at the phase-34 state-6 close-out when row `34` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 35 (the `### Phase-35 state-1 brainstorm` / `### Phase-35 state-2 PLAN-write` / `### Phase-35 state-3 implementation` / `### Phase-35 state-4 verification` / `### Phase-35 state-5 code-review` narratives, relocated at the phase-35 state-6 close-out when ROADMAP row `35` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 36 (the `### Phase-36 state-5 code-review` narrative — phase 36 used a rename-in-place Notes discipline, so the state-1..4 sub-state narratives were superseded in place each session and only the four top-section blocks were relocated per-session — plus the now-CLOSED detailed M35-1 carry-forward bullet [CONSUMED by phase-36 F2], relocated at the phase-36 state-6 close-out when ROADMAP row `36` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 37 (the `### Phase-37 state-3 implementation` active-phase narrative, relocated at the phase-37 state-6 close-out when ROADMAP row `37` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### HTTP-filters-family carry-forwards (from the `25.2` REVIEW.md - NOT yet consumed; weigh whenever the HTTP-filters family is re-entered)

> These were never obligations on the xDS phase 26; they remain live for whenever an HTTP-filters-family phase resumes.

- **(1) [non-goal - architectural]** Over-limit request bodies are FULLY buffered before the 413 rejection (no streaming watermark). Documented deferred non-goal; differentially byte-identical to Envoy for the bounded fixture sizes. Revisit only if a streaming `decode_data` watermark path is ever planned.
- **(2) [doc precision]** The BEHAVIOR_CONTRACT 413-row "verified byte-exact against v1.33.0" phrasing - fixture `0033` is H1-only; the H2 over-limit path is covered by the in-process synth-decorator backstop, NOT differentially. Consider narrowing the phrasing if an H2 over-limit fixture is ever added.
- **(3) [coverage]** No standalone `== effective route limit` unit assertion (the boundary is exercised only via the over/under probes).
- **(4) [coverage]** No differential at-limit (`==`) probe in `0033` (within-limit `<` and over-limit `>` are both covered; the exact boundary is not differentially probed).
- _(2)-(4) are cheap polish, (1) is architectural and only relevant to a future streaming phase._
