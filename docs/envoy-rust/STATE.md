# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase

**id:** `25.2` — `25.2-http-filter-buffer` (Part B of parent phase `25` `envoy.filters.http.buffer` — the `BufferFilter` + `Buffer` chain config + `BufferPerRoute` per-route override + fixture `0033` + parent-25 close; the ADR-0064 split sibling of the now-closed `25.1`), lifecycle **state-5-complete / state-6-next** (`SPEC.md` + `PLAN.md` + `PROGRESS.md` + `REVIEW.md` ALL present [PROGRESS records all 7 task entries T1–T7 + `## State-4 verification` + `## State-5 code review`; `REVIEW.md` verdict APPROVED — 0 Critical / 0 Important / 4 Minor] → all 4 phase artifacts present → §5 **state 6**, the deterministic close-out). Sub-phase **`25.1` is CLOSED** (state-6-complete). ROADMAP rows `00`–`24` + `25.1` are all `done`; parent row `25` is `in-progress` (sub-phases `25.1, 25.2`; flips `done` when `25.2` closes); row `25.2` stays `planned` (the §5 state machine — not the ROADMAP status — tracks transitions; ROADMAP rows `25.2` AND parent `25` both advance to `done` at the `25.2` state-6 close per BOOTSTRAP §6.1 step 4 — the NEXT session).
**slug:** `25.2-http-filter-buffer`
**directory:** `docs/envoy-rust/phases/25.2-http-filter-buffer/` — carries **all 4 artifacts: `SPEC.md` + `PLAN.md` + `PROGRESS.md` + `REVIEW.md`** (PROGRESS records the 7 state-3 task entries T1–T7 + the `## State-4 verification` + `## State-5 code review` entries; `REVIEW.md` APPROVED). The now-closed sibling `docs/envoy-rust/phases/25.1-h1-request-body-forwarding/` carries all 4 artifacts (`REVIEW.md` APPROVED); the parent dir `docs/envoy-rust/phases/25-http-filter-buffer/` carries the parent `SPEC.md`.

**status:** **SUB-PHASE 25.2 (`25.2-http-filter-buffer`) STATE-5 CODE REVIEW COMPLETE — `REVIEW.md` APPROVED at THIS commit** (lifecycle state-5-complete / state-6-next). This session ran `superpowers:requesting-code-review` over the full `25.2` implementation (base `41ae9a12d` = `ecc674a9d~1` → HEAD `132d32cec`), dispatching two INDEPENDENT read-only review subagents SERIALLY (`feedback_serial_subagent_dispatch`): one correctness/scope, one tests/quality. **Verdict: Ready to merge YES — 0 Critical / 0 Important / 4 Minor** (all non-blocking — documented non-goals or doc/coverage polish; authored to `REVIEW.md` + recorded in PROGRESS `## State-5 code review`). Both reviewers independently confirmed: strict-`>` u64-safe length comparison against the REAL buffered body; 17-byte 413 `Payload Too Large` with content-type/length stamped by the synth decorators (not the filter); required-`u32` `max_request_bytes` all fatal/accept cases tested (`@type` × `deny_unknown_fields` safe); per-route disable/lower/empty-fallback wiring correct + NOT swallowed by the `_ => {}` catch-all; HCM `.min(64 KiB)` reservation bound behavior-preserving; NO new stats; no `unsafe`; tests assert real distinct behavior (the M25.1-2 split test genuinely forces the multi-read reassembly loop; the `0033` differential fixture has no false-green path). **The 4 Minor carry-forwards** (into phase-26 planning; none block the close): (1) over-limit bodies fully buffered before rejection — explicitly documented deferred non-goal, differentially byte-identical; (2) BEHAVIOR_CONTRACT 413-row "verified byte-exact against v1.33.0" phrasing — `0033` is H1-only, H2 covered by the in-process backstop; (3) no standalone `== route limit` unit assertion; (4) no differential at-limit (`==`) probe. **State-5 is read-only** — no build/test gate re-ran (the state-4 §7.5 gate already proved green at `fde99b984`; AUTHORITATIVE Linux CI `27510477930` SUCCESS per ADR-0049). The review surfaced NO finding forcing a re-plan/contract change → **NO new ADR** (ADR-0065 stays UNFIRED). **DECISIONS.md ledger head: ADR-0064** (count 65; next available **ADR-0065**). ADR-0014 in force; ADR-0028 open. No `unsafe`. Per §5.1 (one state per session) this session EXITS after the STATE advance; the NEXT session runs `25.2`'s state-6 deterministic close-out (no skill — BOOTSTRAP §6.1 step 4) flipping ROADMAP rows `25.2` AND parent `25` to `done` and advancing STATE to phase `26`.

> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases + the active phase's prior sub-state pointers, incl. the phase-25 state-1 brainstorm pointer) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill

Per `BOOTSTRAP_PROMPT.md` §5 state 6 + `SKILL_ROUTING.md`: with all 4 `25.2` phase artifacts present (`SPEC.md` + `PLAN.md` + `PROGRESS.md` + `REVIEW.md` APPROVED — 0 Critical / 0 blocking-Important) → §5 **state 6**, the next session runs the **state-6 deterministic close-out (NO skill — BOOTSTRAP §6.1 step 4)**. The close-out: (1) flips **BOTH ROADMAP row `25.2` AND parent row `25` to `done`** in `docs/envoy-rust/ROADMAP.md` (parent `25` closes because `25.1` is already `done` and `25.2` is its last sub-phase); (2) advances `STATE.md` to the next phase **`26`** (read `ROADMAP.md` for the next planned row — its SPEC/brainstorm lifecycle restarts at state-1); (3) relocates the superseded `25.2` top-section blocks + any `## Notes` subsection verbatim to `STATE_HISTORY.md` (ADR-0035 / §4.1 inv. 9); (4) carries the 4 `REVIEW.md` Minor recommendations forward into phase-26 planning (the BEHAVIOR_CONTRACT 413-row phrasing tweak; the `==` unit + differential boundary probes; the full-body-buffering streaming residual). NO production/test change at the close-out (docs-only). Per §5.1 the state-6 close-out is the NEXT session's single state.

> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases + the active phase's prior sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit

**Phase-25.2 state-5 code review — STATE advance (THIS commit):** the docs/STATE commit advancing `25.2` from state-5 to **state-5-complete / state-6-next** after the state-5 code review APPROVED. This session ran `superpowers:requesting-code-review` over the full `25.2` implementation (base `41ae9a12d` = `ecc674a9d~1` → HEAD `132d32cec`), dispatching two INDEPENDENT read-only review subagents SERIALLY (correctness/scope + tests/quality). **Verdict: Ready to merge YES — 0 Critical / 0 Important / 4 Minor** (all non-blocking — documented non-goals + doc/coverage polish). `REVIEW.md` authored (APPROVED) + a `## State-5 code review` entry appended to `PROGRESS.md`. THIS commit advances `STATE.md` to `25.2` state-6-next (next: the state-6 deterministic close-out, no skill — BOOTSTRAP §6.1 step 4) and relocates the superseded state-4→state-5 top-section blocks verbatim to `STATE_HISTORY.md` (ADR-0035 / §4.1 inv. 9). State-5 is read-only — no build/test gate re-ran (the state-4 §7.5 gate already proved green at `fde99b984`; AUTHORITATIVE Linux CI `27510477930` SUCCESS per ADR-0049). The review surfaced no contract-forcing finding → **NO new ADR** (ADR-0065 stays UNFIRED). **DECISIONS.md ledger head: ADR-0064** (count 65; next available **ADR-0065**). ADR-0014 in force; ADR-0028 open. No production change in THIS commit; no `unsafe`; no new ADR. ROADMAP rows `25.2` + parent `25` stay as-is (both advance to `done` at the state-6 close — the NEXT session). Per §5.1 this session EXITS after the STATE advance; the NEXT session runs `25.2`'s state-6 deterministic close-out.

> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases + the active phase's prior sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated

2026-06-15 (phase-25.2 **state-5 code review COMPLETE — STATE advance** — ran `superpowers:requesting-code-review` over the full `25.2` impl [base `41ae9a12d` = `ecc674a9d~1` → HEAD `132d32cec`], dispatching two INDEPENDENT read-only review subagents SERIALLY [`feedback_serial_subagent_dispatch`]: correctness/scope + tests/quality. **Verdict: Ready to merge YES — 0 Critical / 0 Important / 4 Minor** [all non-blocking — documented non-goals + doc/coverage polish]. Both confirmed: strict-`>` u64-safe comparison against the REAL buffered body; 17-byte 413 with content-type/length via the synth decorators [not the filter]; required-`u32` all fatal/accept cases tested; per-route disable/lower/empty-fallback NOT swallowed by the catch-all; HCM `.min(64 KiB)` behavior-preserving; NO new stats; no `unsafe`; tests assert real distinct behavior + the `0033` differential has no false-green path. `REVIEW.md` authored [APPROVED] + `## State-5 code review` PROGRESS entry. State-5 is read-only — no gate re-ran [state-4 §7.5 green at `fde99b984`; AUTHORITATIVE Linux CI `27510477930` SUCCESS per ADR-0049]. Advanced `STATE.md` to `25.2` lifecycle state-6-next [next: the state-6 deterministic close-out, no skill — BOOTSTRAP §6.1 step 4] and relocated the superseded state-4→state-5 top-section blocks verbatim to `STATE_HISTORY.md` [ADR-0035]. No `unsafe`; no new ADR [no contract-forcing finding → ADR-0065 UNFIRED]. ROADMAP rows `25.2` + parent `25` left as-is [both advance to `done` at the state-6 close — the NEXT session]. The 4 Minor carry-forwards [BEHAVIOR_CONTRACT 413-row phrasing; `==` unit + differential boundary probes; the full-body-buffering streaming residual] roll into phase-26 planning. Ledger head **ADR-0064** [count 65; next available **ADR-0065**]. ADR-0014 in force; ADR-0028 open. Per §5.1 the `25.2` state-6 deterministic close-out is the NEXT session.)

> Historical `## Last updated` notes — every superseded last-updated note (all closed phases + the active phase's prior sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: one state per session; do not chain states. State-6 close-out commits touch ROADMAP.md + STATE.md only and carry no code changes; the next session writes PLAN.md for the next active phase per `superpowers:writing-plans`.
- The reviewer's R2 disposition decision (option (a) retroactive split of 05.1 vs option (b) free-standing post-05.1 sub-phase) was settled at the 05.1 state-6 commit in favor of option (b); 05.4 is the chosen sibling sub-phase. Future-reviewers reading STATE.md should understand that 05.1 is structurally closed at the preamble landing; 05.4 is a SIBLING under parent-05, not a child of 05.1; and the execution order ran 05.1 → 05.4 → 05.2 → 05.3, with 05.3 the closing sub-phase that flips parent-05 to `done`.

> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Phase-25 state-1 brainstorm

- **Pick:** phase `25` = `envoy.filters.http.buffer`, selected at this brainstorm from `MISSION.md`'s §9 HTTP-filters family (the richest remaining vein). The easy header-only HTTP-filter vein is EXHAUSTED — all 7 shipped filters (header_mutation/local_ratelimit/rbac/fault/jwt/cors/csrf) are header-only decode-side decisions and the framework is headers-only (no streaming `decode_data` hook). Buffer is the lightest remaining body-dependent filter (no external service / engine / gzip dep / timing — a pure function of method + body length + policy → deterministic, locally-verifiable, byte-exact differential) AND the THIRD per-route `typed_per_filter_config` consumer (after cors+csrf). Rejected: the LB family (larger/riskier first cut + a new distinguishable-backend harness), the network-filters opener (low leverage), the heavier HTTP filters (external service / engine / non-deterministic), an H2-only buffer (near-cosmetic — exploits H2's incidental pre-buffering, builds no infra), and a pure H1-forwarding phase with buffer deferred (ships a foundation with no consumer). See ADR-0062.

- **Critical recon finding (SPEC §0):** H1 does NOT forward request bodies upstream today — the router forwards an always-empty body (`crates/envoy-http1/src/hcm.rs:356`) and drains+discards the downstream Content-Length body (`:678-697`), a pre-existing gap since phase 04.3; whereas H2 already fully buffers+forwards (`crates/envoy-http2/src/hcm.rs:437-448`). So buffer is NOT a pure plug-in: Part A (now `25.1`) closes the H1 forwarding gap (a regression-equivalence foundation slice), Part B (now `25.2`) adds the filter. The §6.1 split FIRED at the state-2 PLAN-write (ADR-0064).

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035. (The `### Phase-25 state-1 brainstorm` subsection above REMAINS while parent `25` is `in-progress`.)
