# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase

**id:** `25.2` — `25.2-http-filter-buffer` (Part B of parent phase `25` `envoy.filters.http.buffer` — the `BufferFilter` + `Buffer` chain config + `BufferPerRoute` per-route override + fixture `0033` + parent-25 close; the ADR-0064 split sibling of the now-closed `25.1`), lifecycle **state-4-complete / state-5-next** (`SPEC.md` + `PLAN.md` + `PROGRESS.md` present [PROGRESS records all 7 task entries T1–T7 + the `## State-4 verification` evidence]; `REVIEW.md` ABSENT → the next skill is the state-5 code review). Sub-phase **`25.1` is CLOSED** (state-6-complete). ROADMAP rows `00`–`24` + `25.1` are all `done`; parent row `25` is `in-progress` (sub-phases `25.1, 25.2`; flips `done` only when `25.2` closes); row `25.2` stays `planned` (the §5 state machine — not the ROADMAP status — tracks transitions; ROADMAP rows `25.2` AND parent `25` both advance to `done` at the `25.2` state-6 close per BOOTSTRAP §6.1 step 4).
**slug:** `25.2-http-filter-buffer`
**directory:** `docs/envoy-rust/phases/25.2-http-filter-buffer/` — carries **`SPEC.md` + `PLAN.md` + `PROGRESS.md`** (PROGRESS records the 7 state-3 task entries T1–T7 + the `## State-4 verification` entry; `REVIEW.md` absent — authored at state-5, the NEXT session). The now-closed sibling `docs/envoy-rust/phases/25.1-h1-request-body-forwarding/` carries all 4 artifacts (`REVIEW.md` APPROVED); the parent dir `docs/envoy-rust/phases/25-http-filter-buffer/` carries the parent `SPEC.md`.

**status:** **SUB-PHASE 25.2 (`25.2-http-filter-buffer`) STATE-4 §7.5 VERIFICATION GATE COMPLETE — all six §7.5 gates GREEN at THIS commit** (lifecycle state-4-complete / state-5-next). This session ran `superpowers:verification-before-completion` over the full `25.2` implementation at HEAD `fde99b984`. **Evidence:** full local Docker differential + h2spec (`cargo test -p differential -p h2spec-conformance` → **173 passed / 0 failed**; all **33** Docker-gated fixtures `0001`–`0033` green simultaneously against `envoyproxy/envoy:v1.33.0` incl. the new `0033-http-filter-buffer`; **`h2spec_pass_rate_gate ok`**); fresh `cargo build -p envoy-config -p envoy-filter -p envoy-http1` (isolated) + `cargo build --workspace --all-targets` + `cargo clippy --workspace --all-targets --all-features -- -D warnings` + `cargo fmt --all -- --check` + `cargo deny check` all clean; `cargo +nightly fuzz run parse_bootstrap` → 815,824 runs / 61s / 0 crash (exercises the new T7 `seed-buffer.yaml`); the remaining workspace crates' unit tests green (0 failures through `envoy-access-log`/`envoy-admin`/`envoy-bin`). **AUTHORITATIVE (ADR-0049): Linux CI run `27510477930` on HEAD `fde99b984` = SUCCESS** — the CI workflow runs the COMPLETE §7.5 gate (`cargo fmt --check` + `cargo clippy -D warnings` + `cargo build --workspace --all-targets` + `cargo test --workspace` ["includes differential harness → Docker" + all `envoy-bin` `*_in_process` tests + h2spec] + `cargo deny check` + the fuzz job); the local runs corroborate. Verification surfaced NO divergence forcing a contract change (`0033` matches Envoy byte-for-byte per ADR-0063) → **NO new ADR** (ADR-0065 stays UNFIRED). **DECISIONS.md ledger head: ADR-0064** (count 65; next available **ADR-0065**). ADR-0014 in force; ADR-0028 open. No `unsafe`. Per §5.1 (one state per session) this session EXITS after the STATE advance; the NEXT session runs `25.2`'s state-5 `superpowers:requesting-code-review` to author `REVIEW.md`.

> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases + the active phase's prior sub-state pointers, incl. the phase-25 state-1 brainstorm pointer) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill

Per `BOOTSTRAP_PROMPT.md` §5 state 5 + `SKILL_ROUTING.md`: with `25.2`'s state-4 §7.5 verification gate GREEN (PROGRESS `## State-4 verification` — all 33 Docker fixtures + h2spec + workspace/deny/fuzz green; authoritative Linux CI `27510477930` on `fde99b984` SUCCESS) and `REVIEW.md` still ABSENT → §5 **state 5**, the next session runs **`superpowers:requesting-code-review`** over the full `25.2` implementation, authoring `docs/envoy-rust/phases/25.2-http-filter-buffer/REVIEW.md`. The review surface (git range from the `25.1` close to HEAD): `crates/envoy-config` `Buffer`/`BufferPerRoute` config schema (T1) + `crates/envoy-filter/src/buffer.rs` `BufferFilter` runtime (T2) + `crates/envoy-filter/src/instance.rs` wiring (T3) + `crates/envoy-http1/src/hcm.rs` body-allocation bound (T4) + the `tests/differential` harness body support (T5) + fixture `0033` (T6) + the BEHAVIOR_CONTRACT 413 row + `parse_bootstrap` fuzz seed (T7). Dispatch independent read-only review subagents (correctness/scope + tests/quality), synthesize `REVIEW.md`. Per §5.2: if 0 Critical / 0 blocking-Important → advance to the state-6 deterministic close-out (flip ROADMAP rows `25.2` AND parent `25` to `done`); else re-enter state-3. Per §5.1 the state-5 review is the NEXT session's single state.

> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases + the active phase's prior sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit

**Phase-25.2 state-4 §7.5 verification gate — STATE advance (THIS commit):** the docs/STATE commit advancing `25.2` from state-4 to **state-4-complete / state-5-next** after the §7.5 gate ran green. This session ran `superpowers:verification-before-completion`: full local Docker differential + h2spec (`cargo test -p differential -p h2spec-conformance` → 173 passed / 0 failed; all 33 fixtures incl. `0033` green; `h2spec_pass_rate_gate ok`), fresh isolated + workspace builds + clippy `-D warnings` + fmt `--check` + `cargo deny check` all clean, `cargo +nightly fuzz run parse_bootstrap` 815,824 runs / 0 crash, and the remaining workspace crate tests 0 failures — corroborating the AUTHORITATIVE Linux CI run `27510477930` on HEAD `fde99b984` (SUCCESS; the full §7.5 gate incl. `cargo test --workspace` → Docker differential + `cargo deny` + fuzz, per ADR-0049). The `## State-4 verification` evidence was committed at `a36d6f838`. THIS commit advances `STATE.md` to `25.2` state-5-next (next skill `superpowers:requesting-code-review`) and relocates the superseded state-4 top-section blocks verbatim to `STATE_HISTORY.md` (ADR-0035 / §4.1 inv. 9). **DECISIONS.md ledger head: ADR-0064** (count 65; next available **ADR-0065**, UNFIRED — verification surfaced no decision). ADR-0014 in force; ADR-0028 open. No production change in THIS commit; no `unsafe`; no new ADR. ROADMAP rows `25.2` + parent `25` stay as-is (advance to `done` at the state-6 close). Per §5.1 this session EXITS after the STATE advance; the NEXT session runs `25.2`'s state-5 code review.

> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases + the active phase's prior sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated

2026-06-14 (phase-25.2 **state-4 §7.5 verification gate COMPLETE — STATE advance** — ran `superpowers:verification-before-completion` over the full `25.2` impl at HEAD `fde99b984`: full local Docker differential + h2spec [`cargo test -p differential -p h2spec-conformance` → 173 passed / 0 failed; all 33 fixtures `0001`–`0033` green incl. the new `0033`; `h2spec_pass_rate_gate ok`]; fresh `cargo build --workspace --all-targets` + `cargo clippy --workspace --all-targets --all-features -- -D warnings` + `cargo fmt --all -- --check` + `cargo deny check` all clean; `cargo +nightly fuzz run parse_bootstrap` 815,824 runs / 0 crash [exercises the new `seed-buffer.yaml`]; remaining workspace crate tests 0 failures. AUTHORITATIVE per ADR-0049: Linux CI run `27510477930` on `fde99b984` = SUCCESS [full §7.5 incl. `cargo test --workspace` → Docker differential + `cargo deny` + fuzz]. State-4 verification evidence committed `a36d6f838`. Advanced `STATE.md` to `25.2` lifecycle state-5-next [next skill `superpowers:requesting-code-review`] and relocated the superseded state-4 top-section blocks verbatim to `STATE_HISTORY.md` [ADR-0035]. No `unsafe`; no new ADR [no divergence → ADR-0065 UNFIRED]. ROADMAP rows `25.2` + parent `25` left as-is [advance to `done` at the state-6 close]. Ledger head **ADR-0064** [count 65; next available **ADR-0065**]. ADR-0014 in force; ADR-0028 open. Per §5.1 the `25.2` state-5 code review is the NEXT session.)

> Historical `## Last updated` notes — every superseded last-updated note (all closed phases + the active phase's prior sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: one state per session; do not chain states. State-6 close-out commits touch ROADMAP.md + STATE.md only and carry no code changes; the next session writes PLAN.md for the next active phase per `superpowers:writing-plans`.
- The reviewer's R2 disposition decision (option (a) retroactive split of 05.1 vs option (b) free-standing post-05.1 sub-phase) was settled at the 05.1 state-6 commit in favor of option (b); 05.4 is the chosen sibling sub-phase. Future-reviewers reading STATE.md should understand that 05.1 is structurally closed at the preamble landing; 05.4 is a SIBLING under parent-05, not a child of 05.1; and the execution order ran 05.1 → 05.4 → 05.2 → 05.3, with 05.3 the closing sub-phase that flips parent-05 to `done`.

> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Phase-25 state-1 brainstorm

- **Pick:** phase `25` = `envoy.filters.http.buffer`, selected at this brainstorm from `MISSION.md`'s §9 HTTP-filters family (the richest remaining vein). The easy header-only HTTP-filter vein is EXHAUSTED — all 7 shipped filters (header_mutation/local_ratelimit/rbac/fault/jwt/cors/csrf) are header-only decode-side decisions and the framework is headers-only (no streaming `decode_data` hook). Buffer is the lightest remaining body-dependent filter (no external service / engine / gzip dep / timing — a pure function of method + body length + policy → deterministic, locally-verifiable, byte-exact differential) AND the THIRD per-route `typed_per_filter_config` consumer (after cors+csrf). Rejected: the LB family (larger/riskier first cut + a new distinguishable-backend harness), the network-filters opener (low leverage), the heavier HTTP filters (external service / engine / non-deterministic), an H2-only buffer (near-cosmetic — exploits H2's incidental pre-buffering, builds no infra), and a pure H1-forwarding phase with buffer deferred (ships a foundation with no consumer). See ADR-0062.

- **Critical recon finding (SPEC §0):** H1 does NOT forward request bodies upstream today — the router forwards an always-empty body (`crates/envoy-http1/src/hcm.rs:356`) and drains+discards the downstream Content-Length body (`:678-697`), a pre-existing gap since phase 04.3; whereas H2 already fully buffers+forwards (`crates/envoy-http2/src/hcm.rs:437-448`). So buffer is NOT a pure plug-in: Part A (now `25.1`) closes the H1 forwarding gap (a regression-equivalence foundation slice), Part B (now `25.2`) adds the filter. The §6.1 split FIRED at the state-2 PLAN-write (ADR-0064).

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035. (The `### Phase-25 state-1 brainstorm` subsection above REMAINS while parent `25` is `in-progress`.)
