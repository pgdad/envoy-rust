# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase


**id:** **75.2** — `75.2-headermatcher-absence-accesslog` (ROADMAP row `75.2`, **`in-progress`** — flipped `planned` → `in-progress` at the state-2 PLAN-write and DELIBERATELY LEFT `in-progress` here, since a state-3 implementation does not close a row), the SECOND and FINAL sub-phase of parent **75** (`75-headermatcher-absence-parity`, cross-cutting correctness: `HeaderMatcher` ABSENCE-SEMANTICS parity with `envoyproxy/envoy:v1.33.0`; ROADMAP row `75`, **`in-progress`**, `sub-phases` = `75.1, 75.2`), created by the §6.1 SPLIT of parent 75 (ADR-0157). **Sibling `75.1` is `done`** and its engine fix is LANDED and is the PREMISE of everything 75.2 does. **depends-on `75.1` — satisfied.** **Parent row `75` flips `done` only when `75.2` is `done`** — that flip belongs to `75.2`'s own state-6 close-out, NOT to this session and NOT to state-4 or state-5.
**slug:** `75.2-headermatcher-absence-accesslog` (parent `75-headermatcher-absence-parity`; closed sibling `75.1-headermatcher-absence-engine-route`).
**directory:** `docs/envoy-rust/phases/75.2-headermatcher-absence-accesslog/` — now holds **`SPEC.md`, `PLAN.md` AND `PROGRESS.md`** (the `PROGRESS.md` was authored at THIS session). **`PLAN.md` + `PROGRESS.md` present with NO `REVIEW.md`, all 9 tasks implemented and committed, is the §5 state-4 detection — this file is authoritative.** The next session runs **`superpowers:verification-before-completion`** over the §7.5 gate that `PLAN.md`'s final section spells out. The CLOSED sibling `docs/envoy-rust/phases/75.1-headermatcher-absence-engine-route/` holds the full `SPEC.md` + `PLAN.md` + `PROGRESS.md` + `REVIEW.md` set — expected, NOT corruption (§6.2), and all four are FROZEN. The PARENT `docs/envoy-rust/phases/75-headermatcher-absence-parity/SPEC.md` also REMAINS ON DISK as the parent record and is likewise FROZEN; parent 75 has NO `PLAN.md` and never will (§6.2 step 1 ABANDONS the oversize plan).

**status:** **SUB-PHASE 75.2 §5 STATE-3 IMPLEMENTATION COMPLETE — ALL 9 `PLAN.md` TASKS LANDED IN 9 COMMITS; FIXTURES `0084` + `0085` GREEN AND PROVEN LOAD-BEARING BY MUTATION; ADR-0162 FIRED; STATE ADVANCED TO §5 state-4.** This session executed the §5 state-3 implementation and nothing else. Cold-started fully per `BOOTSTRAP_PROMPT.md` §1 on `HEAD == 1bf256aa01766f9c6e47132867c2e9e1083031f5`, `git status --porcelain` clean, branch `main`, `git fetch origin --prune` showing `origin/main` at the SAME SHA at session start AND re-checked immediately before committing. CI on that SHA re-confirmed on the FULL 40-char SHA (run `30199193179`, both jobs `success` at FULL step counts **15** and **13**, no runner-starvation signature). **STATE DETECTION: `PLAN.md` existed AND no `PROGRESS.md` — §5 state 3 exactly.** **WHAT LANDED:** the two backend-free byte-exact ACCESS-LOG differential fixtures **`0084`** (the **D1** witness — `exact_match: "v"` + `invert_match: true`, three probes, absent → DROPPED) and **`0085`** (the **D2** witness — a plain NON-inverted `present_match: false`, two probes, `x-a: v` → DROPPED), each with its ~63-line entrypoint; a new **`### Phase 75`** block in `BEHAVIOR_CONTRACT.md` (§A the rule, §B the four-cell polarity matrix, §C the nine-sink MEASURED access-log matrix, §D the mode-scoping guard, §E Trap A, §F Trap B, §G the fixtures, §H the driver constraint) inserted as a **PURE INSERTION** (`git diff --numstat` = `140 0`); the CF-72-2 `**§D` record EXTENDED to three members and CF-75-1 BANKED as `**§G`; **M74-31 CONSUMED** at all FOUR live causal sites (comments/prose only); and the 75.1 review's **M-1 / M-2 / M-3 / N-1 / N-2** closed. **THE TDD RED WAS A MUTATION CHECK AND IT FIRED A REAL PLAN CORRECTION (ADR-0162): `PLAN.md` Task 1 Step 8's specified `0084` mutation — hoisting the `(_, None)` arm — leaves `0084` GREEN**, because `return false` short-circuits BEFORE the XOR, so D1 is untouched; what it actually breaks is **P1**, turning FOUR in-process assertions RED (`649 passed` → `645 passed; 4 failed`). The TRUE D1 revert is `(_, None) => return false` → `(_, None) => false`, which reds `0084` on the line-count assertion (`envoy_rust=2, envoy=1`). The `0085` mutation as specified was CORRECT. Both fixtures are load-bearing; an UNMUTATED control was green before and after in the same scratch worktree. **NO `crates/` BEHAVIOR CHANGED** — the only two `crates/` edits are comment-only, proven by a diff filtered of `///`/`//` printing EMPTY. Sub-phase 75.1 was NOT re-opened, its gate NOT re-run, its APPROVED verdict NOT re-graded; no `75.1/` or parent `75/` artifact was touched. **The §7.5 gate was NOT run as this session's state — that is state-4**, and no full `cargo test --workspace` sweep or `cargo deny check` was run. Ledger head is now **ADR-0162**, next **ADR-0163**. **No `stop` file** (**102** of 104 ROADMAP rows are `done`; `75` and `75.2` remain `in-progress`; the `ROADMAP.md` `## Feature Families` at `ROADMAP.md:58` remain largely unbuilt).

_Historical (the SUB-PHASE 75.2 §5 STATE-2 PLAN-WRITE Active-phase `**id:**` + `**directory:**` + `**status:**` blocks + that block's own `_Historical_` pointer line — superseded by this SUB-PHASE 75.2 §5 STATE-3 IMPLEMENTATION; relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this session per ADR-0035. The `**slug:**` line is UNCHANGED and is carried forward in place.)_
> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases incl. the phase-51 state-3/4/5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill


**The next session runs the §5 STATE-4 VERIFICATION FOR SUB-PHASE 75.2** (`superpowers:verification-before-completion` — per the `SKILL_ROUTING.md` state machine: implementation is COMPLETE (all 9 `PLAN.md` tasks landed, `PROGRESS.md` written) and NOT yet verified, which is state 4 exactly). **It runs the §7.5 phase-done gate that `PLAN.md`'s final section spells out verbatim, and quotes every command output into `PROGRESS.md`.** It is a SEPARATE session by §5.1 and ADR-0127. **It must NOT** write `REVIEW.md` (that is state-5), re-open or re-verify 75.1, re-grade 75.1's APPROVED review, edit the FROZEN `75/SPEC.md` or any `75.1/` artifact, flip ROADMAP row `75.2` or parent row `75`, or re-implement — a state-4 session VERIFIES; if the gate REDs on a genuine defect it re-enters at state 3 (§5.2), not at 4.

**WHAT 75.2's STATE-4 VERIFICATION INHERITS.** **STATE-4 IS SOLO-SERIAL** — the cargo lock makes concurrency pointless and the §7.5 adjudication is one indivisible judgment. **THE GATE, from `PLAN.md`'s "The §7.5 phase-done gate" section:** (a) the two NEW fixtures green — `cargo test -p differential --test headermatcher_absence_accesslog` and `--test headermatcher_absence_accesslog_present_polarity`, **asserting on the `1 passed` count, never the exit code**; (b) all **83** pre-existing fixtures still green via `cargo test --workspace --no-fail-fast` with the FULL output redirected to a FILE (**never `tail`**), watching `0078`/`0079`/`0080`/`0081`/`0082` (the access-log filter family) and `0083` in particular — `0081` and `access_log_metadata_filter` were EDITED this session (comments only) and `0081`'s entrypoint was re-run green (`1 passed`); (c) conformance unchanged, `known-failures.txt` stays **21** lines and is NEVER trimmed; (d) **NO new fuzz target — nothing new to run** (**63** seeds, **5** targets, both unchanged, re-confirmed); (e) `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check`, `cargo test --workspace`, **`cargo deny check`**; (f) `REVIEW.md` approved is state-5, not state-4. **ALREADY RUN GREEN AT STATE-3 (re-run them; do not merely inherit):** both new fixtures (`1 passed` each), `access_log_metadata_filter` (`1 passed`), `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check`, and `cargo test -p envoy-config --lib` (**649 passed**, the same count as before this session's comment-only edits). **NOT YET RUN AT ALL: the full `cargo test --workspace --no-fail-fast` sweep and `cargo deny check`** — those two are the real work of state-4, and gate **(b)** is the dominant risk. **ADR-0162 is the state-3 finding the verifier should read first:** the two engine mutations are DISTINCT and hit DIFFERENT cells, so a future re-test must pick the matching one or it will misread a GREEN as a vacuous fixture. **ONE OBSERVATION HANDED FORWARD, deliberately NOT fixed:** the phase-72 `**§C` block in `BEHAVIOR_CONTRACT.md` (~`:2404-2406`) still says the arm-hoist mutation "turns three in-process guards RED" where this session MEASURED **four**; it is outside `PLAN.md`'s Task 8 finding list and was left alone rather than widened into — the state-5 review should weigh it.


**Standing traps** (enduring). **75.2-STATE-3-SPECIFIC (added at the implementation).** **THE TWO ENGINE MUTATIONS ARE DISTINCT AND HIT DIFFERENT CELLS — do not substitute one for the other. Read ADR-0162 and `BEHAVIOR_CONTRACT.md` Phase 75 §D before re-testing either access-log fixture.** In short: hoisting `(_, None)` to the TOP of the `match` breaks **P1** and leaves **D1** CORRECT (`return false` short-circuits before the XOR), turning FOUR in-process assertions RED while `0084`/`0085` stay GREEN; reverting **D1** instead needs `(_, None) => return false` → `(_, None) => false`, which REDs `0084` with `envoy_rust=2, envoy=1`. **Corollary: the arm ORDER is guarded ONLY in-process — no differential fixture can catch a regression in it.** **A mutation RED saying `not accept-ready within 10s: Connection refused` NEVER REACHED AN ASSERTION and is NOT evidence** (the `wait_accept_ready` port-reuse flake; this exact false RED fired at 75.2's state-3) — re-run, and always run the UNMUTATED control from the SAME worktree. **`PLAN.md` CENSUS RECIPES CAN BE UNSOUND — re-derive, don't trust:** the D-3.8 sweep `grep -rc '#!\[forbid(unsafe_code)\]' $(git ls-files 'crates/*/src/{lib,main}.rs') | grep -c ':1'` expects **17** but globs only `crates/` (**14** roots) when the workspace has **22** members, AND undercounts to **13** because `crates/envoy-listener/src/lib.rs` contains the string TWICE. Enumerate members from the ROOT `Cargo.toml`: **22 of 22** roots carry it exactly once — **D-3.8 HOLDS**. **RE-DERIVE EVERY `BEHAVIOR_CONTRACT.md` SITE BY TEXT ANCHOR, NEVER by a line number — this bit TWICE MID-SESSION**: the M74-31 site moved `2657` → `2709` and M-3's `2545` → `2597`, both pushed by this session's OWN earlier tasks. The file is now **3503** lines. **Access-log FIXTURE-AUTHORING rules are now permanently recorded elsewhere and are NOT needed to verify 75.2** — `%REQ(NAME)%` is allow-list gated so a gating header can never be echoed into the line (`REQ_ALLOW_LIST`, `crates/envoy-accesslog/src/command_operator.rs`); the per-side recipe is MEASURED 7/7 across `0078`-`0082` + `0084`/`0085` and written up in those READMEs (`codec_type: HTTP1` on BOTH sides is NOT a divergence, ADR-0158 C3); and ONE log file per side is a driver constraint banked as `BEHAVIOR_CONTRACT.md` Phase 75 §H, which is why one rule with two polarities needs a sibling PAIR. **Do NOT add `on_header_missing` to fixtures `0081` OR `0082`** — envoy-rust requires a `value` there, which would make the key RESOLVE and silently vacate the witness while the fixture stayed GREEN (ADR-0155 PV-6); their occurrences are `#` COMMENTS documenting the deliberate omission. **`0081` has THREE probes and expects TWO logged lines, byte-distinct so ORDER is pinned** — do not touch a probe, an `expect_logged` value or the probe ORDER. **THE 75.1 ENGINE IS LANDED AND IS THE PREMISE OF 75.2 — do NOT re-derive, revert or "simplify" it.** `HeaderMatcher::matches` (`crates/envoy-config/src/matcher.rs`) is an exhaustive tuple `match (&self.mode, value)` whose `(_, None) => return false` arm sits **AFTER** the `PresentMatch` arm and **BEFORE** every value arm, closed by an XOR with `invert_match`. Rule: `present_match(want)` → `(present == want) XOR invert_match`; every other mode → `false` on an absent header with `invert_match` NOT applied; an EMPTY header VALUE counts as PRESENT. **75.2 CHANGED NO `crates/` BEHAVIOR** — its only two `crates/` edits are comment-only. **CF-72-1 is CONSUMED. M74-31 is CONSUMED** (four sites, comments/prose only; `0081/expectations.yaml:20` is DESCRIPTIVE and was left alone, and the "FIVE" figure at `74/REVIEW.md:1269` is append-only and must NOT be "fixed"). **The 75.1 review's M-1/M-2/M-3/N-1/N-2 are CLOSED**; **M-5/N-3 need no fix and N-4 is record-only** (D-3.5). **CF-72-2 and CF-75-1 are BANKED, NOT FIXED** (`BEHAVIOR_CONTRACT.md` phase-72 `**§D` three members + `**§G`); owner is a future `HeaderMatcher` wire-shape-parity phase. **CF-75-2 is OPEN and OUT OF SCOPE:** upstream COMMA-JOINS duplicate header values before value matching while envoy-rust matches only the FIRST occurrence. The PRESENCE axis is PARITY, so it is NOT a regression and none of 75.1's rule is affected; it spans all SIX value modes across all FIVE consumers and needs its OWN measured phase — do NOT "fix" it opportunistically. **NEVER weaken a fixture; NEVER trim `known-failures.txt`** (**21** lines — this host scores h2spec 3.5/2 as PASS, so trimming on local evidence would break CI). **Do NOT add `on_header_missing` to fixtures `0081` OR `0082`** — envoy-rust requires a `value` there, which would make the key RESOLVE and silently vacate the witness while the fixture stayed GREEN (ADR-0155 PV-6); their occurrences are `#` COMMENTS documenting the deliberate omission. **`0081` has THREE probes and expects TWO logged lines, byte-distinct so ORDER is pinned** — do not touch a probe, an `expect_logged` value or the probe ORDER. **TWO CONFLATION TRAPS — do NOT unify them; both are now recorded in full as `BEHAVIOR_CONTRACT.md` Phase 75 §E and §F.** `HeaderMatcher.present_match` vs `ValueMatcher.present_match` (RBAC / access-log metadata, fixture `0044`), where `present_match: false` NEVER matches — a DIFFERENT and CORRECT rule that must NOT be "fixed" to match; since 75.1 the two agree in THREE of four cells and differ in exactly ONE (ABSENT × `want=false`). And `HeaderMatcher.invert_match` vs `MetadataMatcher.invert` (CF-74-1) — MEASURED accepted-but-INERT upstream, so it stays boot-fatal here; do NOT "implement" it. **CF-74-6:** the wrapped `match_if_key_not_found: { value: <bool> }` spelling is accepted AND honored upstream but BOOT-FATAL here — a documented REJECT-direction gap; do NOT "fix" it (bare-only, ADR-0063 / ADR-0133). **The ADR-0150 seam must keep holding:** `envoy-accesslog` has ZERO workspace deps and MUST NOT depend on `envoy-config`; matchers cross as injected **trait objects** (`Arc<dyn MetadataMatch>`, `Arc<dyn HeaderMatch>`); `LogFilter` has NO `Eq`/`PartialEq` — do not add either. **The documented host-flake set is CI-AUTHORITATIVE, never a regression — the 15 named tests plus the `TcpCloseBackend` IPv6 set and the `192.168.65.2` bridge-IP family are enumerated VERBATIM in `PLAN.md`'s "The §7.5 phase-done gate" section, which the state-4 session reads anyway.** The IPv6 and bridge-IP families fail DETERMINISTICALLY in isolation — **that determinism IS the environmental signature**; the startup-race family passes in isolation and its MEMBERSHIP CHANGES run to run, so **diff the failing SET across runs.** Adjudicate with `--no-fail-fast` + a FULL-output redirect, **NEVER `tail`** (memory `never-pipe-verification-runs-through-tail`; a bare `cargo test --workspace` aborts at the first failing BINARY); isolation re-runs name the target binary and `0 passed; N filtered out` is **NOT** a pass (memory `cargo-test-p-name-false-green-filtered-out`), while `error: no test target named …` exits 101 exactly like a real RED — **read the failure TEXT, always.** A MASS differential RED (`client error (Connect)`, dozens at once) is the Docker daemon down (memory `docker-desktop-down-after-reboot-kvm-acl`: `sudo setfacl -m u:esa:rw /dev/kvm && systemctl --user restart docker-desktop`). **`cargo build -p envoy-bin` before ANY local differential** (memory `differential-harness-uses-debug-envoy-bin`); a ~1-3 s green on a backend-free fixture with a warm image is NORMAL, not a skip. A clippy/build green finishing in ~1 s off a handful of `Checking` lines is PARTIALLY CACHED — `touch` the changed files and re-run; **`cargo clippy` prints `Checking`, NOT `Compiling`** (memories `mutation-check-needs-forced-rebuild`, `clippy-prints-checking-not-compiling`). Any tree-MUTATING probe belongs in its OWN scratch `git worktree`, reset to `main` first (memories `mutation-checks-collide-with-parallel-subagents`, `worktree-subagents-get-stale-base`). `cargo fuzz` runs from the CRATE dir and takes a **DIRECTORY, not a file**; a NEW seed needs a `!`-un-ignore line (memory `fuzz-corpus-seed-gitignored-by-default`). `cargo deny check` can red on a fresh unrelated RustSec advisory — patch-bump the dep (memory `cargo-deny-reds-on-unrelated-advisory`). Do NOT revert landed `00`-`74` work NOR sub-phase `75.1` NOR the parallel workstream's `envoy-http2` header-list bound; never add `rbac` to `is_terminal_network_filter`; never reject a top-level `filters: []` sink list; preserve ADR-0016/0124/0131/0133/0136/0137/0139/0140-0143/0144-0149/**0150**-**0162**; **ADR-0028 is NOT lifted.** The phase-73 `< 2` rejection on `and_filter`/`or_filter { filters }` is a composition-arm PGV rule, NOT the top-level `filters: []` sink-list rule — do NOT "relax" it. Do NOT disturb the **38** existing access-log fixtures. Any ROADMAP row edit preserves 6 cells + escapes `\|`; rows `36`/`38`/`39`/`52`/`54`/`66`/`70` are already malformed — do NOT "fix" them (append-only), and a naive `awk -F'|' $5` sweep FALSE-REPORTS `66`/`70` as not-done (splitting on `' | '` WITH the spaces is robust and yields the correct 104/102 census). **`DECISIONS.md` is NOT chronological AND is too large to Read whole** (chunk by `grep -n '^## ADR-NNNN'` + offset/limit; memory `decisions-md-not-chronological`; head **ADR-0162**, next **ADR-0163**). `ROADMAP.md` / `BEHAVIOR_CONTRACT.md` (**3503** lines) / `STATE_HISTORY.md` also exceed the single-read limit — chunk them. **envoy-bin writes `ConfigError` to STDOUT** and takes only `-c <path>`. `STATE.md` top-section blocks are single 3000-13000 char lines — script-edit + a 0×-grep afterwards (memory `state-md-blocks-are-single-lines-edit-hazard`); `STATE_HISTORY.md` archive headers must be matched by EXACT equality (memory `state-history-archive-headers-need-exact-match`) and BOTH files backed up first; ADR-0035 relocation checks are DELTA-based, never "no duplicates" (memory `state6-relocation-check-must-be-delta-based`) — allowlist the unchanged `**slug:**` line, this enduring Standing-traps line and the `### Doctrine reminders` bullet, all superseded IN PLACE (and per **ADR-0160** rewritable only after relocating the superseded text verbatim). **Beware VACUOUS greps** — a glob over an unresolved path returns `0 hits` that LOOKS clean; assert the number of files scanned. And the opposite: a grep can legitimately return >0 because a record QUOTES the defect it fixes — **adjudicate by LINE and by FILE, never by COUNT** (this fired twice this session: the `§C` heading count went 8 → 9 because 75.2's OWN new block adds a `**§C`, and the D-3.8 recipe above). **Recon traps on this host (all memory-backed — read them before hand-rolling any probe):** bind mounts are STALE-CACHED so every config revision needs a FRESH FILENAME (`docker-bind-mounts-stale-cached-on-this-host`, which also covers `--volumes-from` not reaching a stopped container's `/tmp` — use `docker cp`); port-map with `docker -p`, NOT `--network host` (`state0-recon-docker-needs-port-mapping`); a backend must be a SIBLING container on a shared network (`state0-recon-backend-sibling-container`); upstream Envoy will NOT create an access-log directory and its flush is TIMER-driven (~10 s), so a hand-rolled probe needs a graceful `docker stop -t 15` — the harness handles both itself; and YAML 1.1 parses an unquoted `cluster: y` as boolean `true`, which upstream rejects. **A `pkill -f <pattern>` whose pattern also matches the invoking shell's own command line kills that shell** — kill by PID or use `pgrep -x`. Cold-start reads `STATE.md` FIRST (disk-authoritative). Confirm CI with the **FULL 40-char SHA** (memory `gh-run-list-commit-needs-full-sha`) and check the STEP COUNTS, not merely the conclusion; a job `cancelled` with `runner_name:""` + `steps:0` never got a runner — rerun the same SHA (memory `ci-run-cancelled-with-no-runner-is-starvation`). `next-prompt.txt` is gitignored — refresh on disk, do NOT `git add` it. **CONCURRENCY:** `.claude/worktrees/agent-*` worktrees (FOUR observed this session) and a long-running `envoyproxy/envoy:v1.33.0` container belong to a PARALLEL workstream — LEAVE THEM ALONE (remove only your OWN artifacts, memory `concurrent-loop-sessions-race-on-phase-pick`) and EXCLUDE them from any repo-wide `find`/census/grep (they duplicate every `fuzz_targets` dir and inflate counts — enumerate workspace members from the root `Cargo.toml`). Re-check `git fetch origin --prune` at session start AND again immediately before committing.
_Historical (the SUB-PHASE 75.2 §5 STATE-2 PLAN-WRITE `## Next expected skill` pointer + its `WHAT 75.2's STATE-3 IMPLEMENTATION INHERITS` block + that block's own `_Historical_` pointer line — superseded by this SUB-PHASE 75.2 §5 STATE-3 IMPLEMENTATION; relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this session per ADR-0035. The Standing-traps block is ENDURING and is carried forward IN PLACE, but at this implementation it was superseded — its full prior text (12 652 characters) relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) under the `## Next expected skill` archive header FIRST, then rewritten per **ADR-0160**: the spent 75.2-state-2 PLAN-write narration removed (the four §6.2 drift anchors, now CONSUMED and in any case twice-stale; the `0078`-not-`0083` stencil rule, now spent because both fixtures are written; the two-fixtures-not-one derivation, now BANKED as `BEHAVIOR_CONTRACT.md` Phase 75 §H), every still-live trap retained, and the state-3 findings added (the two DISTINCT mutations; the false-RED startup flake; the unsound D-3.8 census recipe). **NET RESULT: 12 652 → 12 471 characters — this rewrite SHRANK the line by 181 characters.** That is a modest net reduction, not a dramatic one, and it is stated as measured rather than rounded up: the state-3 findings that had to be ADDED (the two DISTINCT mutations, the false-RED startup flake, the unsound D-3.8 census recipe) very nearly cancelled out the spent PLAN-write text that was REMOVED (the four §6.2 drift anchors; the `0078`-not-`0083` stencil rule; the two-fixtures derivation; the fixture-authoring recipe block; the verbatim host-flake enumeration, which is duplicated in `PLAN.md`'s §7.5 section the state-4 session reads anyway). The reduction was only achieved by replacing consumed measurements with POINTERS to where they are now permanently recorded — ADR-0162, `BEHAVIOR_CONTRACT.md` Phase 75, the fixtures' READMEs and the named memories.)_
> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases incl. the phase-37 state-1..5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit


**Sub-phase 75.2 §5 STATE-3 IMPLEMENTATION — all 9 `PLAN.md` tasks landed; fixtures `0084`+`0085` green and proven load-bearing; ADR-0162 fired; NO `crates/` behavior changed:** NINE commits on top of the state-2 PLAN-write head `1bf256aa01766f9c6e47132867c2e9e1083031f5` (CI `success` on that SHA, run `30199193179`, both jobs at FULL step counts **15** and **13**). **Files changed:** TEN created — `tests/fixtures/0084-headermatcher-absence-accesslog/{envoy.yaml,envoy-rust.yaml,expectations.yaml,README.md}`, `tests/fixtures/0085-headermatcher-absence-accesslog-present-polarity/{same four}`, and the two entrypoints `tests/differential/tests/headermatcher_absence_accesslog{,_present_polarity}.rs` — plus SEVEN modified: `docs/envoy-rust/BEHAVIOR_CONTRACT.md` (Tasks 5/6/7/8), `tests/differential/tests/access_log_metadata_filter.rs` (`//!` only), `tests/fixtures/0081-accesslog-metadata-filter/{expectations.yaml,README.md}` (comments/prose only), `crates/envoy-config/src/{bootstrap.rs,matcher.rs}` (**doc/test comments only — proven by a diff filtered of `///`/`//` printing EMPTY**), and `docs/envoy-rust/DECISIONS.md` (**ADR-0162** appended as a PURE INSERTION, `git diff --numstat` = `17 0`, so no landed ADR was edited), plus the NEW `PROGRESS.md`, `STATE.md` and `STATE_HISTORY.md`. **NO `ci.yml` step, fuzz target, corpus seed or `known-failures.txt` line was touched; ROADMAP row `75.2` was NOT flipped and parent row `75` was NOT flipped** — a state-3 implementation closes no row. **WHY AN ADR FIRED — the mutation check found a REAL error in `PLAN.md`:** Task 1 Step 8's specified `0084` mutation (hoisting the `(_, None)` arm) leaves `0084` **GREEN**, because `return false` short-circuits BEFORE the closing XOR so divergence D1 is untouched; what it actually breaks is **P1**, turning FOUR in-process assertions RED (`649 passed` → `645 passed; 4 failed`) while every value-mode assertion and BOTH fixtures stay green — so the arm ORDER is guarded ONLY in-process. The TRUE D1 revert is `(_, None) => return false` → `(_, None) => false`, which REDs `0084` on the line-count assertion (`envoy_rust=2, envoy=1`). Task 3 Step 7's `0085` mutation was CORRECT as written and REDs identically. **Its first run also produced a FALSE RED** — `not accept-ready within 10s: Connection refused`, the `wait_accept_ready` startup flake, which never reached an assertion — caught by reading the failure TEXT and re-running. **Also corrected:** the §D guard count THREE → **FOUR** on a fresh measurement (agreeing with the STATE.md trap, correcting ADR-0159's PRE-measurement, which is NOT edited); §G placed after `**§F` rather than the plan's "immediately after §D" so the letters read `§A…§G` in order; and `PLAN.md`'s D-3.8 census recipe shown unsound (**22 of 22** crate roots carry `#![forbid(unsafe_code)]` — D-3.8 HOLDS; the recipe globbed 14 roots and undercounted to 13). **Net LoC: ~998 against the plan's ~760 projection (+31%), still ~33% under the ~1500 §6.1 gate**, so the no-split verdict holds. **The §7.5 gate was NOT run — that is state-4**; no full `cargo test --workspace` sweep and no `cargo deny check`. Next: **sub-phase 75.2's §5 state-4 verification** (a SEPARATE session).

_Historical (the SUB-PHASE 75.2 §5 STATE-2 PLAN-WRITE Last-commit block + that session's own `_Historical_` pointer line — superseded by this SUB-PHASE 75.2 §5 STATE-3 IMPLEMENTATION Last-commit block; relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this session per ADR-0035.)_
> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases incl. the phase-37 state-1..5 sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated


2026-07-26 (sub-phase 75.2 **§5 STATE-3 IMPLEMENTATION COMPLETE — all 9 `PLAN.md` tasks landed in NINE commits; fixtures `0084` + `0085` GREEN and PROVEN LOAD-BEARING by mutation; ONE new ADR fired, ADR-0162**. **STATE DETECTION: `PLAN.md` existed AND no `PROGRESS.md` — §5 state 3 exactly**, and `superpowers:executing-plans` was the routed skill. Confirmed `git status --porcelain` clean, branch `main`, `HEAD` = `1bf256aa01766f9c6e47132867c2e9e1083031f5` with `origin/main` at the SAME SHA at session start and re-checked immediately before committing; CI on that SHA re-confirmed GREEN on the FULL 40-char SHA (run `30199193179`, both jobs `success` at FULL step counts **15** and **13** — the step counts were checked, not merely the conclusion). Cold-read `BOOTSTRAP_PROMPT.md`, `MISSION.md`, `STATE.md`, `SKILL_ROUTING.md`, `ENVOY_TARGET.md`, the 75.2 `SPEC.md` and the 75.2 `PLAN.md` IN FULL, plus the ROADMAP `75`/`75.1`/`75.2` rows and the `DECISIONS.md` ledger head by targeted read (`DECISIONS.md` / `ROADMAP.md` / `BEHAVIOR_CONTRACT.md` / `STATE_HISTORY.md` all exceed the single-read limit). **A PRE-FLIGHT ANCHOR SWEEP RE-DERIVED EVERY `BEHAVIOR_CONTRACT.md` SITE BEFORE ANY EDIT** and all of ADR-0161's corrected numbers HELD — then TWO of them drifted AGAIN mid-session, moved by this session's own Tasks 5/6 (M74-31's site `2657` → `2709`; M-3's `2545` → `2597`), which is exactly why every task re-derived immediately before editing. **THE MUTATION CHECK WAS THE SUBSTANCE OF THE SESSION AND FIRED ADR-0162:** the `0084` mutation `PLAN.md` specifies leaves `0084` GREEN (it breaks P1, not D1, because `return false` short-circuits before the XOR); the true D1 revert is dropping that `return`, which REDs `0084` on the line-count assertion (`envoy_rust=2, envoy=1`). The `0085` mutation was correct as written. Both fixtures are load-bearing, with an UNMUTATED control green BEFORE and AFTER in the same scratch worktree and `Compiling envoy-config` proving a forced rebuild on every run. **A false RED (`not accept-ready … Connection refused`) was correctly rejected as a startup flake rather than banked as evidence.** **ONE PROCEDURAL DEVIATION, recorded:** the two mutations ran in ONE scratch worktree SEQUENTIALLY rather than the two `PLAN.md` names — the guarantee the plan states is isolation from the MAIN tree, which one worktree satisfies, while two would have paid a second full cold build for no added guarantee; the worktree was restored pristine, a post-mutation control re-confirmed all three green, and ONLY that worktree was removed (the FOUR `.claude/worktrees/agent-*` of the parallel workstream were left untouched). **THREE SMALLER PLAN CORRECTIONS:** the §D guard count THREE → FOUR (fresh measurement; ADR-0159 NOT edited, append-only); §G placed after `**§F` so the phase-72 letters read `§A…§G` in order; and the D-3.8 census recipe shown unsound in two ways (**22 of 22** crate roots carry the attribute — the invariant HOLDS, only the recipe was wrong). Censuses re-verified on disk at completion: fixture dirs **85**, differential test files **85**, ROADMAP **104** rows (**102** `done`, 2 `in-progress` — `75` and `75.2`, NEITHER flipped), `known-failures.txt` **21** lines, **63** corpus seeds and **5** fuzz targets both unchanged, `BEHAVIOR_CONTRACT.md` now **3503** lines. **Net LoC ~998 vs the ~760 projection (+31%), ~33% under the ~1500 gate.** The four superseded top-section blocks were relocated verbatim per ADR-0035 with both files backed up first, the archive headers matched by EXACT equality, and the check run **DELTA-based**; the enduring `**Standing traps**` line was relocated verbatim FIRST and only then rewritten per **ADR-0160**, **shrinking 12 652 → 12 471 characters** — a genuine but MODEST net reduction of 181 characters, stated as measured rather than rounded, since the added state-3 findings very nearly cancelled the removed PLAN-write text. **Do NOT chain into state-4** (a SEPARATE session per §5.1 / ADR-0127); **no `stop` file** — the mission is NOT complete. Ledger head **ADR-0162**, next **ADR-0163**. Live carry-forwards: **CF-75-2**, **CF-72-2** and **CF-75-1** [both BANKED by Task 6, not fixed], **M71-6** [DECLINED at the PLAN-write, still open], CF-74-1/2/3/4/6, CF-73-1, N73-R2, M73-R1/M73-R2, M71-3, M71-7/8, M70-R4/R9, M69-A..I, CF-69-1/2/3/5, M68-1, M-1, CF-67-3/5/6/7, M74-3..M74-14, M74-16, M74-17/18/20/21/22/26/27/28/29, M74-30 and M74-32..M74-39, the older Minors, and the HTTP-filters-family (1)-(4). **M74-31 and the 75.1 review's M-1/M-2/M-3 + N-1/N-2 are now CLOSED.** ONE observation handed to state-5, deliberately not fixed: the phase-72 `**§C` block still says "three in-process guards" where this session measured FOUR.)

_Historical (the SUB-PHASE 75.2 §5 STATE-2 PLAN-WRITE Last-updated note + that session's own `_Historical_` pointer line — superseded by this SUB-PHASE 75.2 §5 STATE-3 IMPLEMENTATION Last-updated note; relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this session per ADR-0035.)_
> Historical `## Last updated` notes — every superseded last-updated note (all closed phases incl. the phase-37 state-1..5 sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsection for fully-closed phase 64 (the `### Phase-64 state-5 code-review` narrative — phase 64 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-64 state-6 close-out when ROADMAP row `64` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 63 (the `### Phase-63 state-5 code-review` narrative — phase 63 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-63 state-6 close-out when ROADMAP row `63` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 61 (the `### Phase-61 state-1 brainstorm` / `### Phase-61 state-2 PLAN-write` / `### Phase-61 state-3 implementation` / `### Phase-61 state-4 verification` / `### Phase-61 state-5 code-review` narratives, relocated at the phase-61 state-6 close-out when ROADMAP row `61` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 58 (the `### Phase-58 state-1 brainstorm` / `### Phase-58 state-2 PLAN-write` / `### Phase-58 state-3 implementation` / `### Phase-58 state-4 verification` / `### Phase-58 state-5 code-review` narratives, relocated at the phase-58 state-6 close-out when ROADMAP row `58` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 57 (the `### Phase-57 state-1 brainstorm` / `### Phase-57 state-2 PLAN-write` / `### Phase-57 state-3 implementation` / `### Phase-57 state-4 verification` / `### Phase-57 state-5 code-review` narratives, relocated at the phase-57 state-6 close-out when ROADMAP row `57` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 42 (the `### Phase-42 state-5 code-review (active phase)` narrative — phase 42 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-42 state-6 close-out when ROADMAP row `42` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 43 (the `### Phase-43 state-5 code-review (active phase)` narrative — phase 43 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-43 state-6 close-out when ROADMAP row `43` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 44 (the `### Phase-44 state-5 code-review (active phase)` narrative — phase 44 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-44 state-6 close-out when ROADMAP row `44` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 45 (the `### Phase-45 state-5 code-review (active phase)` narrative — phase 45 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-45 state-6 close-out when ROADMAP row `45` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 46 (the `### Phase-46 state-5 code-review (active phase)` narrative — phase 46 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-46 state-6 close-out when ROADMAP row `46` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 47 (the `### Phase-47 state-5 code-review (active phase)` narrative — phase 47 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-47 state-6 close-out when ROADMAP row `47` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 48 (the `### Phase-48 state-5 code-review (active phase)` narrative — phase 48 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-48 state-6 close-out when ROADMAP row `48` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 49 (the `### Phase-49 state-5 code-review (active phase)` narrative — phase 49 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-49 state-6 close-out when ROADMAP row `49` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 50 (the `### Phase-50 state-3 implementation (active phase)` narrative — phase 50 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-50 state-6 close-out when ROADMAP row `50` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 51 (the `### Phase-51 state-3 implementation (active phase)` narrative — phase 51 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-51 state-6 close-out when ROADMAP row `51` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 53 (the `### Phase-53 state-1 brainstorm` narrative — phase 53 kept a single active-phase Notes subsection at "state-1 brainstorm" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-1 subsection remained in `## Notes` at close — relocated at the phase-53 state-6 close-out when ROADMAP row `53` flipped `done`, together with the inline superseded state-3 narrative) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 54 (the `### Phase-54 state-1 brainstorm` / `### Phase-54 state-2 PLAN-write` / `### Phase-54 state-3 implementation` / `### Phase-54 state-4 verification` / `### Phase-54 state-5 code-review` narratives, relocated at the phase-54 state-6 close-out when ROADMAP row `54` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 55 (the `### Phase-55 state-5 code-review (active phase)` narrative — phase 55 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state individually superseded in place and relocated to STATE_HISTORY.md at its own transition], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-55 state-6 close-out when ROADMAP row `55` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 56 (the `### Phase-56 state-5 code-review (active phase)` narrative — phase 56 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state individually superseded in place and relocated to STATE_HISTORY.md at its own transition], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-56 state-6 close-out when ROADMAP row `56` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 66 (the `### Phase-66 state-5 code-review (active phase)` narrative — phase 66 used a rename-in-place active-phase Notes subsection across its state-0/1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-66 state-6 close-out when ROADMAP row `66` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed sub-phase 67.1 (the `### Phase-67.1 §5 state-5 code-review` narrative — sub-phase 67.1 used a rename-in-place active-phase Notes subsection across its state-2..5 + §5.2 state-3 re-entry arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-67.1 state-6 close-out when ROADMAP row `67.1` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed sub-phase 67.2 (the `### Phase-67.2 §5 state-5 re-review` narrative — sub-phase 67.2 used a rename-in-place active-phase Notes subsection across its state-2..5 + §5.2 state-3 re-entry + state-4-re-verification + state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review subsection remained in `## Notes` at close — relocated at the phase-67.2 state-6 close-out when ROADMAP row `67.2` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
> Historical Notes subsection for fully-closed phase 68 (the `### Phase-68 §5 state-5 code-review` narrative — phase 68 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-68 §5 state-6 close-out when ROADMAP row `68` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 69 (the `### Phase-69 §5 state-5 re-review` narrative — phase 69 used a rename-in-place active-phase Notes subsection across its state-3 → §5.2 state-3 re-entry → state-4 re-verification → state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review subsection remained in `## Notes` at close — relocated at the phase-69 §5 state-6 close-out when ROADMAP row `69` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 70 (the `### Phase-70 §5 state-5 re-review (2nd)` narrative — phase 70 used a rename-in-place active-phase Notes subsection across its state-1..5 + two-§5.2-re-entry + two-state-4-re-verification + two-state-5-re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review-(2nd) subsection remained in `## Notes` at close — relocated at the phase-70 §5 state-6 close-out when ROADMAP row `70` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 71 (the `### Phase-71 §5 state-5 code-review` narrative — phase 71 used a rename-in-place active-phase Notes subsection across its state-0/1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-71 §5 state-6 close-out when ROADMAP row `71` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 72 (the `### Phase-72 §5 state-5 re-review` narrative — phase 72 used a rename-in-place active-phase Notes subsection across its state-1..5 + §5.2 state-3 re-entry + state-4 re-verification + state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 re-review subsection remained in `## Notes` at close — relocated at the phase-72 §5 state-6 close-out when ROADMAP row `72` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 73 (the `### Phase-73 §5 state-5 code-review` narrative — phase 73 used a rename-in-place active-phase Notes subsection across its state-0/1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-73 §5 state-6 close-out when ROADMAP row `73` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 74 (the `### Phase-74 §5 state-5 re-review (2nd)` narrative — phase 74 used a rename-in-place active-phase Notes subsection across its state-0/1..5 + TWO §5.2 state-3 re-entry + TWO state-4 re-verification + TWO state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review-(2nd) subsection remained in `## Notes` at close — relocated at the phase-74 §5 state-6 close-out when ROADMAP row `74` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed sub-phase 75.1 (the `### Phase-75.1 §5 state-5 code-review` narrative — sub-phase 75.1 used a rename-in-place active-phase Notes subsection across its state-2..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the sub-phase-75.1 §5 state-6 close-out when ROADMAP row `75.1` flipped `done`; parent row `75` deliberately stays `in-progress` until sibling `75.2` is `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: **one state per session; do not chain states.** **PARENT PHASE 75 WAS SPLIT (§6.1, ADR-0157); SUB-PHASE `75.1` IS `done`; SUB-PHASE `75.2` IS `in-progress`** — its §5 state-3 implementation landed at this commit. **THE NEXT UNIT OF PROGRESS IS SUB-PHASE `75.2` AT §5 STATE-4, THE §7.5 VERIFICATION GATE** (`superpowers:verification-before-completion`): `PLAN.md` AND `PROGRESS.md` exist, all 9 tasks are implemented and committed, and no `REVIEW.md` exists — which is state 4 exactly. **State-4 is SOLO-SERIAL.** `PLAN.md`'s final section spells out the gate and the gate-(b) adjudication discipline verbatim; the two things NOT yet run at all are the full `cargo test --workspace --no-fail-fast` sweep and `cargo deny check`. ROADMAP parent row `75` is `in-progress` with `sub-phases` = `75.1, 75.2`; row `75.1` is `done`; row `75.2` is `in-progress`. **Parent row `75` flips `done` only at 75.2's state-6 close-out**, when all sub-phases are done — not at state-4 and not at state-5. ADR-0156 + ADR-0157 (the split) + ADR-0158 + ADR-0159 + ADR-0160 + ADR-0161 + **ADR-0162** (the state-3 mutation-procedure correction) are all FIRED; ledger head **ADR-0162**, next **ADR-0163** — append it only for a genuinely NEW decision, and never re-create a landed ADR. **Parent 75 has NO `PLAN.md` and never will** — §6.2 step 1 abandons the oversize plan; do not "finish" it, and do not edit the FROZEN parent `75/SPEC.md`. **Every `75.1/` artifact is FROZEN** — do not edit, re-grade or "correct" them; 75.1's M-1/M-2/M-3 + N-1/N-2 are now CLOSED by 75.2 Task 8, and M-5/N-3/N-4 need no fix at all (D-3.5). **The mission is NOT complete** — **102 of 104** ROADMAP rows are `done` and TWO are `in-progress` (`75`, `75.2`), and the `ROADMAP.md` `## Feature Families` (`ROADMAP.md:58`) remain largely unbuilt (network-filter payload codecs, `sni_cluster`, non-deterministic LB, HTTP/3 + QUIC, gRPC bridge/transcoding, observability SINKS [gRPC ALS, OTLP], runtime/RTDS, hot-restart, WASM host), so **no `stop` file**. Live carry-forwards: **CF-75-2** (upstream comma-joins duplicate header values before value matching; envoy-rust matches only the FIRST occurrence — MEASURED, PRE-EXISTING, needs its own phase), **CF-72-2** and **CF-75-1** (both BANKED into `BEHAVIOR_CONTRACT.md` by 75.2 Task 6 as the phase-72 `**§D`/`**§G` records, NOT fixed; owner is a future `HeaderMatcher` wire-shape-parity phase), **M71-6** (the H2 access-log-filter differential — DECLINED at the 75.2 PLAN-write with reasons in ADR-0161, still open), CF-74-1/2/3/4/6, CF-73-1, N73-R2, M73-R1/M73-R2, M71-3, M71-7/8, M70-R4/R9, M69-A..I, CF-69-1/2/3/5, M68-1, M-1, CF-67-3/5/6/7, M74-3..M74-14, M74-16, M74-17/18/20/21/22/26/27/28/29, M74-30 and M74-32..M74-39, the older Minors + the HTTP-filters-family (1)-(4). **CF-72-1 is CONSUMED** (at the 75.1 close-out); **M74-31 is CONSUMED** (75.2 Task 7, four sites).
- **On the §5.1 rule and its ONE recorded exception (read `ADR-0127`).** The phase-66 state-6 close-out was run in the SAME session as its state-5 code-review, at **explicit human instruction**. That was a one-off, human-authorized override; `BOOTSTRAP_PROMPT.md`, `MISSION.md` and `SKILL_ROUTING.md` were **not modified**, and **§5.1 remains binding on every autonomous session.** ADR-0127 records why the 5→6 transition alone was safe to chain (the close-out changes no code, re-runs no gate, and consumes only disk facts) and why **0→1, 1→2, 2→3, 3→4 and 4→5 must never be chained** — each carries a genuine self-review hazard, since the context that wrote an artifact would be grading it. Per memory `closeout-and-pick-are-separate-sessions`, the next-phase pick is STILL a separate session. Do not read the git history as licence to chain.

> Historical Notes subsection for fully-closed sub-phase 67.3 (the `### Phase-67.3 §5 state-5 re-review` narrative — sub-phase 67.3 used a rename-in-place active-phase Notes subsection across its state-2..5 + §5.2 state-3 re-entry + state-4-re-verification + state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review subsection remained in `## Notes` at close — relocated at the phase-67.3 §5 state-6 close-out when ROADMAP row `67.3` [and parent row `67`] flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 65 (the `### Phase-65 state-5 code-review` narrative — phase 65 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-65 state-6 close-out when ROADMAP row `65` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsection for fully-closed phase 52 (the `### Phase-52 state-5 code-review (active phase)` narrative — phase 52 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2/3/4 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-52 state-6 close-out when ROADMAP row `52` flipped `done`, together with the standing 05.1/05.4 future-reviewer note it carried) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 41 (the `### Phase-41 state-5 code-review (active phase)` narrative — phase 41 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the §6.2-recon PIVOT from `%REQ_WITHOUT_QUERY%` to `%ROUTE_NAME%` landed at state-2; the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-41 state-6 close-out when ROADMAP row `41` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 40 (the `### Phase-40 state-5 code-review (active phase)` narrative — phase 40 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-40 state-6 close-out when ROADMAP row `40` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 39 (the `### Phase-39 state-5 code-review (active phase)` narrative — phase 39 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-39 state-6 close-out when ROADMAP row `39` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
> Historical Notes subsection for fully-closed phase 38 (the `### Phase-38 state-2 PLAN-write (active phase)` narrative — phase 38 used a single active-phase Notes subsection for its whole arc; the state-3/4/5 sub-state narratives were superseded in the four-top-section archive blocks per-session, so only this state-2 subsection remained in `## Notes` at close — relocated at the phase-38 state-6 close-out when ROADMAP row `38` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) and for parent phase 25 (the `### Phase-25 state-1 brainstorm` pick + recon-finding narrative, relocated at the 25.2 state-6 close-out when parent `25` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 26 (the `### Phase-26 state-1 brainstorm` pivot/rejected-alternatives/key-scoping narrative, relocated at the phase-26 state-6 close-out when row `26` flipped to `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 27 (the `### Phase-27 state-1 brainstorm` / `### Phase-27 state-2 PLAN-write` / `### Phase-27 state-4 verification` narratives + the now-consumed `### Phase-27 carry-forwards` [M26-1..M26-8] block, relocated at the phase-27 state-6 close-out when row `27` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 28 (the `### Phase-28 state-1 brainstorm` / `### Phase-28 state-2 PLAN-write` / `### Phase-28 state-3 implementation + state-4 verification` / `### Phase-28 state-5 code review` narratives + the now-consumed `### Phase-28 carry-forwards` [M27-1..M27-3] block, relocated at the phase-28 state-6 close-out when row `28` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 29 (the now-consumed `### Phase-29 carry-forwards` [M28-1..M28-3] block + the `### Phase-29 state-1 brainstorm` / `### Phase-29 state-2 PLAN-write` / `### Phase-29 state-3 implementation` / `### Phase-29 state-4 verification` / `### Phase-29 state-5 code review` narratives, relocated at the phase-29 state-6 close-out when row `29` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 30 (the `### Phase-30 carry-forwards` [M29-1/M29-2, which fed phase 30 but were NOT consumed — they continue as phase-31 carry-forwards] block + the `### Phase-30 state-1 brainstorm` / `### Phase-30 state-2 PLAN-write` / `### Phase-30 state-3 implementation` / `### Phase-30 state-4 verification` / `### Phase-30 state-5 code review` narratives, relocated at the phase-30 state-6 close-out when row `30` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 31 (the `### Phase-31 carry-forwards` [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 — open Minors from the phase-30 REVIEW.md that fed phase 31 but were NOT consumed; they continue as carry-forwards for the next phase that touches the differential hash-sweep driver / the config parser] block + the `### Phase-31 state-1 brainstorm` / `### Phase-31 state-2 PLAN-write` / `### Phase-31 state-3 implementation` / `### Phase-31 state-4 verification` / `### Phase-31 state-5 code review` narratives, relocated at the phase-31 state-6 close-out when row `31` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 32 (the `### Phase-32 carry-forwards` block — the open Minors that fed phase 32 but were NOT consumed: the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3; they continue as carry-forwards for the future phase that touches their surface — re-listed in `## Next expected skill` above alongside the 6 new phase-32 REVIEW.md Minors M32-1…M32-6), relocated at the phase-32 state-6 close-out when row `32` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phases 33+34 (the `### Phase-33 carry-forwards` block — the active-phase carry-forwards Notes that lived through phases 33 and 34: the now-CONSUMED 6 phase-32 REVIEW.md Minors M32-1…M32-6 [folded + landed at the phase-33 state-3] + the "Other still-live carry-forwards" list [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3 + the HTTP-filters-family (1)-(4) — those still-live ones re-listed in `## Next expected skill` above alongside the new phase-34 Minors M34-1/M34-2/M34-3 + the phase-33 M33-1/M33-2]), relocated at the phase-34 state-6 close-out when row `34` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 35 (the `### Phase-35 state-1 brainstorm` / `### Phase-35 state-2 PLAN-write` / `### Phase-35 state-3 implementation` / `### Phase-35 state-4 verification` / `### Phase-35 state-5 code-review` narratives, relocated at the phase-35 state-6 close-out when ROADMAP row `35` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 36 (the `### Phase-36 state-5 code-review` narrative — phase 36 used a rename-in-place Notes discipline, so the state-1..4 sub-state narratives were superseded in place each session and only the four top-section blocks were relocated per-session — plus the now-CLOSED detailed M35-1 carry-forward bullet [CONSUMED by phase-36 F2], relocated at the phase-36 state-6 close-out when ROADMAP row `36` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 37 (the `### Phase-37 state-3 implementation` active-phase narrative, relocated at the phase-37 state-6 close-out when ROADMAP row `37` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### HTTP-filters-family carry-forwards (from the `25.2` REVIEW.md - NOT yet consumed; weigh whenever the HTTP-filters family is re-entered)

> These were never obligations on the xDS phase 26; they remain live for whenever an HTTP-filters-family phase resumes.

- **(1) [non-goal - architectural]** Over-limit request bodies are FULLY buffered before the 413 rejection (no streaming watermark). Documented deferred non-goal; differentially byte-identical to Envoy for the bounded fixture sizes. Revisit only if a streaming `decode_data` watermark path is ever planned.
- **(2) [doc precision]** The BEHAVIOR_CONTRACT 413-row "verified byte-exact against v1.33.0" phrasing - fixture `0033` is H1-only; the H2 over-limit path is covered by the in-process synth-decorator backstop, NOT differentially. Consider narrowing the phrasing if an H2 over-limit fixture is ever added.
- **(3) [coverage]** No standalone `== effective route limit` unit assertion (the boundary is exercised only via the over/under probes).
- **(4) [coverage]** No differential at-limit (`==`) probe in `0033` (within-limit `<` and over-limit `>` are both covered; the exact boundary is not differentially probed).
- _(2)-(4) are cheap polish, (1) is architectural and only relevant to a future streaming phase._
