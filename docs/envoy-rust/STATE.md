# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase


**id:** **66-network-filter-direct-response** (ROADMAP row `66`, `in-progress`) — the **Network-filters family OPENER**, the first row that family has ever had. Phase 66 implements `envoy.filters.network.direct_response`, differentially witnesses it **byte-exact** via a NEW fixture `0071` driven by a NEW `Driver::TcpDirectResponse` (the harness's first read-to-EOF raw-TCP driver), and closes the project's network-filter **terminal-validation** gap. Pick + scope locked by **ADR-0123**; the §6.2 V-3 reconciliation locked by **ADR-0124**. **STATE-4 VERIFICATION COMPLETE this session** — the full §7.5 (a)-(f) gate was run and **PASSES**; `PROGRESS.md` now carries every command's output verbatim. The state-5 code-review is the next session — see `## Next expected skill`.
**slug:** network-filter-direct-response
**directory:** `docs/envoy-rust/phases/66-network-filter-direct-response/` (`SPEC.md` + `PLAN.md` + `PROGRESS.md` present, `PROGRESS.md` now carrying the quoted §7.5 gate output; `REVIEW.md` absent — the §5 state-5 detection rule).

**status:** **PHASE 66 STATE-4 VERIFICATION COMPLETE — the §7.5 (a)-(f) gate PASSES; the state-5 code-review is next.** This session ran the §5 state-4 verification via `superpowers:verification-before-completion`. Cold-started fully per `BOOTSTRAP_PROMPT.md` §1; confirmed `git status --porcelain` clean, branch `main`, `HEAD` = `origin/main` = `36db7e3` (the phase-66 state-3 close-out, unmoved — `git fetch origin --prune` showed `0 0` ahead/behind and no sibling had written `REVIEW.md`), and confirmed the §5 state-4 detection rule. **NO production code changed this session — the state-4 verification is read-only over the tree.** **Gate verdict: (a) PASS, (b) PASS, (c) PASS, (d) PASS, (e) PASS; (f) is state-5.** **CI run `29026523684` at `36db7e3d8bf7cc749d39fcb1c3acfa47ba1689fc` is GREEN on attempt 1** (both jobs real runners, 15 + 12 steps): **147/147 test binaries `ok`, 1828 passed, 0 failed, ZERO `panicked at` / `test result: FAILED` / `error: test failed` across 28 758 log lines.** Locally: `cargo build --workspace --all-targets` exit 0 (0 warnings), `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0 (0 warnings), `cargo fmt --all -- --check` exit 0 (0 bytes), `cargo deny check` exit 0 (`advisories ok, bans ok, licenses ok, sources ok`; its five `license-not-encountered` lines are pre-existing unmatched *allowances* in `deny.toml`, warnings not violations). **`cargo test --workspace` exits 101 on THIS HOST ONLY, and the bare form aborted after 209 lines / 4 `test result:` lines** — the ADR-0125 §(3) / ADR-0126 early-abort trap, live: `envoy-config`'s 548 tests, h2spec and fixture `0071` never ran. Re-run with `--no-fail-fast` **twice**, the failing SETs diffed, and every member re-run **in isolation** (memory `local-red-set-varies-run-to-run`): an **invariant core of exactly 5** — `access_log_rf_upstream_reset` (`0061`), `access_log_rcd_upstream_reset` (`0062`), `access_log_h2_uc_upstream_reset` (`0069`), `access_log_h2_rcd_upstream_reset` (`0070`), `admin_config_dump_server_info` — fails in BOTH runs AND deterministically in isolation (⇒ environmental, load-independent), while a **varying tail of 5** (`http_filter_fault`, `lb_ring_hash`, `upstream_circuit_breaker`, `upstream_connection_pooling_and_per_class_counters`, `xds_file_based_cds`) fails only under full-workspace parallel load and **passes in isolation** (⇒ parallel-load flake, memory `differential-fixtures-flake-under-parallel-load`). **The core's cause was MEASURED from the failure text, not assumed:** the four close-backend fixtures show **upstream Envoy — the reference proxy — reporting `UF` / `immediate_connect_error:_Network_is_unreachable` against an IPv6 `remote_address:[fdc4:f303:9324::254]`** where envoy-rust correctly reports `UC` (memory `tcpclosebackend-ipv6-unreachable-host-flake`); `admin_config_dump_server_info` diverges on `/clusters` with `backend::192.168.65.2:<port>::*` stats present only on the Envoy side (memory `differential-host-bridge-ip-192-168-65-2`). Phase 66 cannot have changed how the reference Envoy container routes IPv6. **The arithmetic closes and is what makes the adjudication sound: local run A = 1823 passed + 5 failed = 1828; CI = 1828 passed + 0 failed = 1828, across the SAME 147 binaries** — the only delta is those five, each explicitly `ok` on CI. **Gate (a):** fixture `0071` green **four times** (run A, run B, isolation, CI). Every phase-66 surface green, including the ADR-0124 drain mutation check `post_eof_client_write_is_accepted_not_reset ... ok`, all four terminal-validation tests, the driver parse test, and the ADR-0126-fixed `upstream_h2_connection_pooling ... ok`. **Gate (c) is recorded HONESTLY: h2spec did NOT run locally** — `which h2spec` → not found, and `h2spec_runner.rs:20-31` `eprintln!`-skips (returning `ok`, in 0.00s) when the binary is absent, so a local `ok` is a **skip, not a pass**; it was deliberately NOT installed (this host scores invalid-preface 3.5/2 as PASS and would mis-adjudicate — memory `h2spec-3-5-2-preface-host-sensitive`). CI provisions it (`ci.yml:43-49`) and reports `test h2spec_pass_rate_gate ... ok`; `known-failures.txt` is **untouched** (21 lines, `git diff` over `tests/conformance/` empty) and phase 66 changed **no H2 codec/framing code**. **Gate (d) RECORDED EXPLICITLY, not skipped silently:** no new fuzz target (ADR-0123 §2.3 — `direct_response` parses nothing); `git diff --name-only 5e3afb9..HEAD -- '*fuzz_targets*' '.github/workflows/ci.yml'` is **empty**, so `ci.yml` needs no new step; the corpus seed is proven tracked via `git ls-files`; a fresh short-budget `cargo +nightly fuzz run parse_bootstrap -- -max_total_time=30` → **`Done 29063 runs`**, exit 0, no crash; CI's `fuzz` job ran all four targets clean. **No fixture was weakened; `known-failures.txt` was not trimmed; the ADR-0124 drain was not re-litigated; the `echo` `typed_config` asymmetry was not "fixed".** No escalation to `superpowers:systematic-debugging` was needed — every local RED resolved to an already-documented environmental cause under the prescribed 2×-run + isolation adjudication, and none re-failed deterministically on CI. `#![forbid(unsafe_code)]` holds (D-3.8). The **§6.1 split gate does not apply** at state-4. **Carry-forward disposition: phase 66 CONSUMES none**; CF-66-1, CF-66-2 and **M66-2** stay OPEN; every pre-existing Minor stays LIVE and NONE blocks. **DECISIONS.md ledger head: ADR-0126** (unchanged — **no ADR was written this session**; none was needed, the gate ran to a verdict without an ambiguity); next-available **ADR-0127**, unreserved. ADR-0123 REFINED by ADR-0124, not superseded; ADR-0014 in force; ADR-0028 open; ADR-0049 governs config-validity. This session executed the §5 state-3 implementation via `superpowers:executing-plans` with `superpowers:test-driven-development` on every task (doctrine D-3.1: tests first, no exceptions). Cold-started fully per `BOOTSTRAP_PROMPT.md` §1; confirmed `git status --porcelain` clean, branch `main`, `HEAD` = `origin/main` = `5e3afb9` (the phase-66 state-2 PLAN-write, unmoved), and confirmed the §5 state-3 detection rule (`SPEC.md` + `PLAN.md` present, `PROGRESS.md` absent). `git fetch origin --prune` + `git ls-tree -r origin/main` confirmed **no sibling loop session had started the implementation** — the `*direct_response*` paths already on `origin/main` (`crates/envoy-bin/tests/http{1,2}_direct_response.rs`, `tests/differential/tests/http{1,2}_direct_response.rs`, `fuzz/corpus/parse_bootstrap/hcm_direct_response_happy.yaml`) are the pre-existing HCM **route-level** `direct_response` action from phase 04, a DIFFERENT feature that merely shares the name. **All 9 PLAN tasks landed, each red-then-green:** (1) `DIRECT_RESPONSE_FILTER` const + `DirectResponseConfig {response: Option<DataSourceInline>}` + `TypedConfig::DirectResponse`; (2) the `validate()` arm; (3) the project's FIRST network-filter **terminal validation** — `ConfigError::NetworkFilterNotTerminal {name, position, chain_len}` + an `is_terminal_network_filter` per-name predicate (NOT a `len <= 1` check) + an **immutable pre-pass placed BEFORE** the existing mutating `for filter in &mut chain.filters` loop, which both dodges the HCM arm's `as_mut()` borrow and reproduces Envoy's error precedence; (4) `crates/envoy-bin/src/direct_response.rs` — an `echo.rs`-shaped standalone accept loop doing `write_all` → `flush` → `shutdown` (FIN) → **drain the read half to EOF** → drop, bounded by the existing `DRAIN_TIMEOUT` so **no new timeout knob** is added; (5) the `main.rs` dispatch arm + a 3-test in-process backstop; (6) `Driver::TcpDirectResponse` + `drive_tcp_direct_response`, the harness's first read-to-EOF raw-TCP driver; (7) fixture `0071` + its differential test; (8) the fuzz corpus seed; (9) the `BEHAVIOR_CONTRACT.md` items. **The MANDATORY PLAN Task-4 Step-5 MUTATION CHECK was performed and it BITES:** with the drain loop commented out, `post_eof_client_write_is_accepted_not_reset` FAILED with `Os {code: 32, kind: BrokenPipe}`; the loop was restored and the suite re-run green. ADR-0124 is therefore **pinned by a test, not merely asserted** — which matters because fixture `0071` structurally CANNOT catch a missing drain (its driver never writes after EOF). No escalation to `superpowers:systematic-debugging` was needed. **Fixture `0071` is GREEN LOCALLY** against the pinned `envoyproxy/envoy:v1.33.0` (`1 passed`, 8.84s) — a deterministic, timing-free, byte-exact cross-proxy witness, both sides carrying the IDENTICAL `typed_config` (no ADR-0014 shim). **SPEC §0 R-0.8 (terminal-rule safety) was re-confirmed EMPIRICALLY beyond the SPEC's fixture-only scan** by running the `envoy-admin` (97 passed) + `envoy-cluster` (160 passed) lib suites and the `tls_sni` / `xds_file_based_lds` / `xds_file_based_rds` `envoy-bin` targets (1 / 6 / 8 passed): **zero regressions**; `cargo test -p envoy-config` → **548 passed, 0 failed**. **Fuzz: NO new target** (ADR-0123 §2.3) — a corpus seed landed with its explicit `!`-un-ignore line and was **PROVEN tracked** via `git ls-files`; `cargo +nightly fuzz run parse_bootstrap -- -max_total_time=30` → `Done 9209 runs`, no crash. **§7.5 gate (d) is satisfied by that pre-existing target and the state-4 session must RECORD that explicitly rather than silently skip it.** A pre-push `cargo fmt --all -- --check` caught rustfmt drift in the `envoy-config` re-export list and it was fixed at state-3 rather than left to red CI (commit `414f72a`). One transient accepted and recorded: at Task 4's commit boundary clippy reported 3 `dead_code` errors (nothing calls `serve` until Task 5's dispatch arm) — structural to the PLAN's own task ordering; clippy returned to 0 errors/0 warnings at Task 5. **The §7.5 gate was deliberately NOT run** (§5.1: one state per session). The **§6.1 split gate was re-evaluated mid-execution and does NOT fire** (no task's sub-steps exceeded ~10 items). `#![forbid(unsafe_code)]` holds (D-3.8). **ADR-0125 + ADR-0126 FIRED** — the state-3 CI adjudication (the implementation itself matched the PLAN; ADR-0125 covers the out-of-plan diagnostic added to the untouched `upstream_h2_connection_pooling` test, ADR-0126 its root cause and fix: a nested `cargo run` was compiling inside that test's 30s readiness budget). **Carry-forward disposition: phase 66 CONSUMES none**; CF-66-1 + CF-66-2 stay OPEN; every pre-existing Minor stays LIVE and NONE blocks. **DECISIONS.md ledger head: ADR-0126** (ADR-0125 = the state-3 CI adjudication; ADR-0126 = the BLOCK-66-1 root-cause + fix; next-available **ADR-0127**, unreserved). ADR-0123 is REFINED, not superseded. ADR-0014 in force; ADR-0028 open; ADR-0049 governs config-validity.

> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases incl. the phase-51 state-3/4/5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill


Per `BOOTSTRAP_PROMPT.md` §5 state 5 + `SKILL_ROUTING.md`: phase 66 (`66-network-filter-direct-response`) is **VERIFIED** (`PLAN.md` + `PROGRESS.md` present, `PROGRESS.md` carries the quoted §7.5 (a)-(f) gate output) and **NOT yet reviewed** (`REVIEW.md` absent) → the next session runs the **§5 state-5 code-review** via **`superpowers:requesting-code-review`**, output `REVIEW.md`. Per §5.1, ONE state per session — do NOT chain into the state-6 close-out. **If `REVIEW.md` raises issues, re-enter at state 3 (NOT state 4)** — §5.2's deliberate asymmetry: you are resuming implementation + TDD, not merely re-verifying.

**The §7.5 gate is DONE and PASSED — do NOT re-run it.** Its evidence is quoted verbatim in `PROGRESS.md` §"§5 state-4 VERIFICATION". Summary: (a) fixture `0071` green ×4 (local run A, run B, isolation, CI); (b) all pre-existing fixtures green on CI (147/147 binaries `ok`); (c) h2spec green on CI (**skipped locally** — see below); (d) no new fuzz target (ADR-0123 §2.3), `parse_bootstrap` clean (`Done 29063 runs`), corpus seed proven tracked, `ci.yml` needs no new step — **explicitly recorded, not silently skipped**; (e) build/clippy/fmt/deny all exit 0 locally and `cargo test --workspace` is green on CI. **(f) `REVIEW.md` approved is THIS coming session's job.**

**What the state-5 code-review should focus on** (the diff is `5e3afb9..HEAD`, 20 files):
- `crates/envoy-config/`: `DIRECT_RESPONSE_FILTER`, `DirectResponseConfig { response: Option<DataSourceInline> }`, `TypedConfig::DirectResponse`, the `validate()` arm, and the project's FIRST network-filter **terminal validation** (`ConfigError::NetworkFilterNotTerminal { name, position, chain_len }` + an `is_terminal_network_filter` per-name predicate + an **immutable pre-pass placed BEFORE** the existing mutating `for filter in &mut chain.filters` loop — the ordering dodges the HCM arm's `as_mut()` borrow AND reproduces Envoy's error precedence).
- `crates/envoy-bin/src/direct_response.rs`: an `echo.rs`-shaped standalone accept loop — `write_all` → `flush` → `shutdown()` (FIN) → **drain the read half to EOF** → drop, bounded by the existing `DRAIN_TIMEOUT` (no new timeout knob).
- `tests/differential/src/lib.rs`: `Driver::TcpDirectResponse` + `drive_tcp_direct_response`, the harness's **first read-to-EOF raw-TCP driver**.
- `crates/envoy-bin/tests/upstream_h2_connection_pooling.rs`: the ADR-0125 bounded diagnostics + the ADR-0126 pre-build fix. **Out-of-PLAN by design, recorded in both ADRs, not silent.**

**FIVE THINGS THE CODE-REVIEW MUST NOT GET WRONG:**
- **(1) Do NOT re-litigate the drain — it is settled, pinned, and CI-green.** ADR-0124 requires `direct_response_once` to `write_all` → `flush` → `shutdown()` (FIN) → **read-and-discard until EOF** → drop. The mandatory mutation check was performed at state-3 and **it BITES**: with the drain loop commented out, `post_eof_client_write_is_accepted_not_reset` FAILS with `Os { code: 32, kind: BrokenPipe }`. Fixture `0071` **CANNOT** catch a missing drain (its driver never writes after EOF), so that in-process test is the only guard. **Do not weaken or delete it.**
- **(2) Do NOT "fix" the `echo` `typed_config` asymmetry.** Upstream Envoy REQUIRES `typed_config` on `envoy.filters.network.echo`; envoy-rust forbids it (`UnexpectedTypedConfig`). Fixture `0001`'s two sides differ accordingly — the pre-existing ADR-0014 YAML shim, not a bug. Both sides of fixture `0071` are identical.
- **(3) `envoy_config::DirectResponse` already existed** — it is the HCM **route-level** `direct_response` action (phase 04), a DIFFERENT feature that merely shares the name. The phase-66 network-filter type is `DirectResponseConfig`; `TypedConfig::DirectResponse` is an enum *variant* in a different namespace. `BEHAVIOR_CONTRACT.md`'s `## Network filters` section opens with a do-not-conflate banner. Same applies to the pre-existing `crates/envoy-bin/tests/http{1,2}_direct_response.rs` and `tests/differential/tests/http{1,2}_direct_response.rs`.
- **(4) `cargo build -p envoy-bin` before ANY local differential run** (memory `differential-harness-uses-debug-envoy-bin`) — the harness executes `target/debug/envoy-bin`, and this phase added a NEW config key AND a NEW filter name, so a stale debug binary REDs with `unsupported network filter` / `unknown field`.
- **(5) Never weaken a fixture; never trim `tests/conformance/h2spec/known-failures.txt`** (memory `h2spec-3-5-2-preface-host-sensitive`). Never pipe a verification run through `tail` (memory `never-pipe-verification-runs-through-tail`).

**BLOCK-66-1 IS CLEARED — do not re-open it.** It was root-caused (ADR-0126, **measured not inferred**) and fixed at state-3: `spawn_backend` ran `cargo run --quiet --manifest-path <helper>`, a SINGLE-PACKAGE selection whose resolver-v2 feature unification differs from `cargo build --workspace --all-targets`'s, so 7 build units compiled **inside** the test's 30s backend-readiness budget while `--quiet` suppressed both `Compiling …` and `Blocking waiting for file lock`. CI confirmed it quantitatively: `Finished dev profile … in 33.63s` immediately before `test upstream_h2_connection_pooling ... ok` — **33.63s of compiling against a 30s budget**, the ~3.6s overshoot that produced the old 30.35/30.35/30.37s timeouts. **Do not re-add `--quiet`; do not remove the pre-build; do not widen the budget.** *Log-reading trap:* CI interleaves ANSI escapes between `Compiling` and the crate name, so `grep "Compiling h2 "` silently returns nothing — grep the crate name alone.

**KNOWN LOCAL-RED EXPECTATIONS — measured again this session; CI IS AUTHORITATIVE** (memory `envoy-rust-state4-ci-first-execution`). `cargo test --workspace` exits **101 on this host** and, in its bare form, **aborts at the first failing test binary** (209 lines; `envoy-config`'s 548 tests, h2spec and fixture `0071` never run). Use `--no-fail-fast` to see the real SET. The state-4 session ran it twice and re-ran every failing member in isolation:
- **Invariant core (5), fails in both runs AND deterministically in isolation ⇒ environmental:** `access_log_rf_upstream_reset` (`0061`), `access_log_rcd_upstream_reset` (`0062`), `access_log_h2_uc_upstream_reset` (`0069`), `access_log_h2_rcd_upstream_reset` (`0070`), `admin_config_dump_server_info`. The four close-backend ones show **upstream Envoy itself** reporting `UF` / `immediate_connect_error:_Network_is_unreachable` at `remote_address:[fdc4:f303:9324::254]` where envoy-rust correctly reports `UC` (memory `tcpclosebackend-ipv6-unreachable-host-flake`); the admin one diverges on `backend::192.168.65.2:<port>::*` `/clusters` stats (memory `differential-host-bridge-ip-192-168-65-2`).
- **Varying tail (5), fails only under parallel load and PASSES in isolation ⇒ flake:** `http_filter_fault`, `lb_ring_hash`, `upstream_circuit_breaker`, `upstream_connection_pooling_and_per_class_counters`, `xds_file_based_cds` (memory `differential-fixtures-flake-under-parallel-load`).
- **The arithmetic closes:** local run A = 1823 passed + 5 failed = **1828**; CI = **1828** passed + 0 failed, across the same 147 binaries. All five core REDs are `ok` on CI.

CI itself carries documented startup-race flakes (`xds-eds-hot-reload-admin-ready-startup-flake`, `eds-fatal-startup-test-port-reuse-flake`, `rds-no-rds-is-inert-startup-flake`) → `gh run rerun <id> --failed` → green; escalate to `superpowers:systematic-debugging` ONLY if a rerun re-fails the SAME test deterministically. **`upstream_h2_connection_pooling` is NO LONGER in that list** — it was never a flake (ADR-0126), and rerunning provably could not fix it because `Swatinem/rust-cache` saves only on job success. Confirm CI with the FULL 40-char SHA — `gh run list --commit <short-sha>` silently returns `[]` (memory `gh-run-list-commit-needs-full-sha`). `cargo deny check` can red on a freshly-published advisory against an existing dep — patch-bump it, don't treat it as a phase regression (memory `cargo-deny-reds-on-unrelated-advisory`).

**h2spec is a SKIP, not a pass, on this host.** `which h2spec` → not found, and `tests/conformance/h2spec/tests/h2spec_runner.rs:20-31` `eprintln!`-skips (returning `ok`, in 0.00s) when the binary is absent. **Do not quote a local `ok` as evidence, and do not install it locally** — this host scores invalid-preface 3.5/2 as PASS while CI fails it, so a locally-"fixed" `known-failures.txt` breaks CI. CI provisions the binary at `.github/workflows/ci.yml:43-49`.

**Open carry-forward Minors (fold into the phase that next touches each surface; NONE blocks):** **M64-2** (the `crates/envoy-http2/src/hcm.rs:236` stale `synth_h2_502` mention) + **M64-3** (idle-probe-connection inefficiency in `Http2CloseBackend`/`Http2EchoBackend`) + **M57-1** (the `content-length` header omission on `synth_h2_no_healthy_upstream()`) + **M55-1** (the `hcm.rs:948`/`:7221` "M50-C deferred" stale comments in `crates/envoy-http1/src/hcm.rs`) + **M53-2** (BEHAVIOR_CONTRACT `:36/:289/:296/:387` "reset 503" needs an "(H1)" qualifier) + **M53-3** + **M48-2** + **M42-1** + the `DC`/retry-budget-overflow slices of **M45-2** + the phase-58 candidate carry-forward + **M40-1** + **M39-1/M39-2** + **M38-1/M38-2** + **CF-39-1** + M37-* + M36-* + M34-* + M33-* + the empty-`metadata_match` doc-comment + M29-*/M30-* + the phase-31 cosmetics + the HTTP-filters-family (1)-(4). **Opened by ADR-0123 §2.2:** **CF-66-1** (`inline_bytes`/`filename` `DataSource` arms unsupported, loudly rejected) + **CF-66-2** (no generic network-filter chain iteration protocol). **Phase 66 CONSUMES none of these.** **Pre-existing ROADMAP cosmetic defect (do NOT "fix"):** rows `36`/`38`/`39`/`52`/`54` carry unescaped `|` characters inside their landed `summary` cells; ROADMAP is append-only and D-3.5 preserves landed summaries verbatim (memory `roadmap-rows-need-escaped-pipes`).

**Mission is NOT complete** — the rest of the Network filters family (redis, mongo, kafka_broker, thrift, zookeeper, sni_cluster, rbac network) plus HTTP filters, load balancing, upstream robustness, HTTP/3+QUIC, gRPC, xDS/dynamic config, observability, runtime/hot-restart, and the WASM host remain unbuilt. **Do NOT create a `stop` file.**

> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases incl. the phase-37 state-1..5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit

**Phase-66 state-4 verification — the §7.5 (a)-(f) gate PASSES; the five local REDs are adjudicated environmental, and the arithmetic closes:** the §5 state-4 verification (`superpowers:verification-before-completion`). Cold-started clean (`git status --porcelain` empty; branch `main`; `HEAD` = `origin/main` = `36db7e3`, the phase-66 state-3 close-out, unmoved; `git fetch origin --prune` → `0 0` ahead/behind, no sibling had written `REVIEW.md`). **NO production code changed — the verification is read-only over the tree.** Docker 28.1.1 up; the local `envoyproxy/envoy:v1.33.0` digest matches the `ENVOY_TARGET.md` pin byte-for-byte (D-3.7). `cargo build -p envoy-bin` was run FIRST (the harness executes `target/debug/envoy-bin`). **Gate (e):** `cargo build --workspace --all-targets` exit 0 / 0 warnings; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0 / 0 warnings; `cargo fmt --all -- --check` exit 0 / 0 bytes; `cargo deny check` exit 0 (`advisories ok, bans ok, licenses ok, sources ok`). `cargo test --workspace` exits **101 on this host only**, and its BARE form aborted after 209 lines — the ADR-0125 §(3) early-abort trap, live. Re-run `--no-fail-fast` **twice** and every failing member re-run **in isolation**: an **invariant core of 5** (`0061`/`0062`/`0069`/`0070` + `admin_config_dump_server_info`) fails in both runs AND deterministically in isolation ⇒ **environmental** — the four close-backend ones show **upstream Envoy** reporting `UF` / `Network_is_unreachable` at IPv6 `[fdc4:f303:9324::254]` where envoy-rust correctly reports `UC`; the admin one diverges on `backend::192.168.65.2:<port>::*` `/clusters` stats. A **varying tail of 5** (`http_filter_fault`, `lb_ring_hash`, `upstream_circuit_breaker`, `upstream_connection_pooling_and_per_class_counters`, `xds_file_based_cds`) fails only under parallel load and **passes in isolation** ⇒ flake. **The arithmetic closes: local run A = 1823 passed + 5 failed = 1828; CI = 1828 passed + 0 failed, same 147 binaries** — every core RED is `ok` on CI. **CI run `29026523684` at `36db7e3d8bf7cc749d39fcb1c3acfa47ba1689fc` is GREEN on attempt 1**, 147/147 binaries `ok`, zero `panicked at` across 28 758 lines. **Gate (a):** fixture `0071` green ×4; the ADR-0124 drain mutation check, all four terminal-validation tests, the driver parse test, and the ADR-0126-fixed `upstream_h2_connection_pooling` all `ok`. **Gate (c) recorded honestly:** h2spec **skipped locally** (binary absent → `eprintln!`-skip in 0.00s; a local `ok` is NOT a pass) and green on CI; `known-failures.txt` untouched (21 lines); no H2 codec/framing change. **Gate (d) recorded explicitly:** no new fuzz target (ADR-0123 §2.3), `ci.yml` unchanged, corpus seed proven tracked via `git ls-files`, fresh `cargo +nightly fuzz run parse_bootstrap -- -max_total_time=30` → `Done 29063 runs`, no crash. **Gate (f) is state-5.** **THIS commit = the `PROGRESS.md` state-4 verification section + the ROADMAP row-66 `summary` state-4 append + STATE + STATE_HISTORY** — no src/fixture/test change. (`next-prompt.txt` refreshed on disk but **gitignored** at `.gitignore:9`, so NOT in this commit.) No fixture weakened, `known-failures.txt` not trimmed, the drain not re-litigated. `#![forbid(unsafe_code)]` holds. **CONSUMES no carry-forward; CF-66-1 + CF-66-2 + M66-2 stay OPEN.** **DECISIONS.md ledger head: ADR-0126 (unchanged — no ADR was written this session);** next-available **ADR-0127**, unreserved. The next session is the §5 state-5 code-review (`superpowers:requesting-code-review` → `REVIEW.md`).

> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases incl. the phase-37 state-1..5 sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated

2026-07-09 (phase-66 **state-4 VERIFICATION COMPLETE; the §7.5 (a)-(f) gate PASSES; awaiting the state-5 code-review**. Ran via `superpowers:verification-before-completion` against `HEAD` = `origin/main` = `36db7e3`, working tree clean, no sibling ahead. **NO production code changed — the verification is read-only over the tree; no ADR was written [none was needed].** Docker 28.1.1 up; the local `envoyproxy/envoy:v1.33.0` digest matches the `ENVOY_TARGET.md` pin byte-for-byte [D-3.7]; `cargo build -p envoy-bin` run FIRST [the harness executes `target/debug/envoy-bin`]. **Gate (e):** `cargo build --workspace --all-targets` exit 0 / 0 warnings; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0 / 0 warnings; `cargo fmt --all -- --check` exit 0 / 0 bytes; `cargo deny check` exit 0 [`advisories ok, bans ok, licenses ok, sources ok`; its five `license-not-encountered` lines are pre-existing unmatched *allowances*, warnings not violations]. **`cargo test --workspace` exits 101 on THIS HOST ONLY**, and the BARE form aborted after 209 lines / 4 `test result:` lines — the ADR-0125 §(3) / ADR-0126 early-abort trap, live: `envoy-config`'s 548 tests, h2spec and fixture `0071` never ran. Re-run `--no-fail-fast` **twice**, SETs diffed, every failing member re-run **in isolation** [memory `local-red-set-varies-run-to-run`]: an **invariant core of exactly 5** — `access_log_rf_upstream_reset` [`0061`], `access_log_rcd_upstream_reset` [`0062`], `access_log_h2_uc_upstream_reset` [`0069`], `access_log_h2_rcd_upstream_reset` [`0070`], `admin_config_dump_server_info` — fails in BOTH runs AND deterministically in isolation ⇒ **environmental, load-independent**; a **varying tail of 5** [`http_filter_fault`, `lb_ring_hash`, `upstream_circuit_breaker`, `upstream_connection_pooling_and_per_class_counters`, `xds_file_based_cds`] fails only under full-workspace parallel load and **passes in isolation** ⇒ parallel-load flake [memory `differential-fixtures-flake-under-parallel-load`]. **The core's cause was MEASURED from the failure text, not assumed:** the four close-backend fixtures show **upstream Envoy — the reference proxy — reporting `UF` / `immediate_connect_error:_Network_is_unreachable` at `remote_address:[fdc4:f303:9324::254]`** where envoy-rust correctly reports `UC` [memory `tcpclosebackend-ipv6-unreachable-host-flake`]; `admin_config_dump_server_info` diverges on `/clusters` with `backend::192.168.65.2:<port>::*` stats present only on the Envoy side [memory `differential-host-bridge-ip-192-168-65-2`]. Phase 66 cannot have changed how the reference Envoy container routes IPv6. **The arithmetic closes, which is what makes the adjudication sound: local run A = 1823 passed + 5 failed = 1828; CI = 1828 passed + 0 failed, across the SAME 147 binaries** — every core RED is explicitly `ok` on CI. **CI run `29026523684` at `36db7e3d8bf7cc749d39fcb1c3acfa47ba1689fc` is GREEN on attempt 1** [both jobs real runners, 15 + 12 steps]: 147/147 binaries `ok`, **zero** `panicked at` / `test result: FAILED` / `error: test failed` across 28 758 log lines. **Gate (a):** fixture `0071` green **four times** [run A, run B, isolation, CI]; every phase-66 surface green, including the ADR-0124 drain mutation check `post_eof_client_write_is_accepted_not_reset ... ok`, all four terminal-validation tests, `parses_tcp_direct_response_driver`, and the ADR-0126-fixed `upstream_h2_connection_pooling ... ok`. **Gate (b):** CI 147/147 `ok`; terminal validation touches no existing config [SPEC §0 R-0.8]. **Gate (c) recorded HONESTLY: h2spec did NOT run locally** — `which h2spec` → not found and `h2spec_runner.rs:20-31` `eprintln!`-skips [returning `ok`, in 0.00s] when the binary is absent, so a local `ok` is a **skip, not a pass**; deliberately NOT installed [this host scores invalid-preface 3.5/2 as PASS and would mis-adjudicate — memory `h2spec-3-5-2-preface-host-sensitive`]. CI provisions it [`ci.yml:43-49`] → `test h2spec_pass_rate_gate ... ok`; `known-failures.txt` **untouched** [21 lines]; **no H2 codec/framing change** this phase. **Gate (d) RECORDED EXPLICITLY, not skipped silently:** no new fuzz target [ADR-0123 §2.3 — `direct_response` parses nothing]; `git diff --name-only 5e3afb9..HEAD -- '*fuzz_targets*' '.github/workflows/ci.yml'` is **empty**, so `ci.yml` needs no new step [memory `new-fuzz-target-needs-a-ci-yml-step` does not bite]; the corpus seed is proven tracked via `git ls-files` [memory `fuzz-corpus-seed-gitignored-by-default`]; fresh `cargo +nightly fuzz run parse_bootstrap -- -max_total_time=30` → **`Done 29063 runs`**, exit 0, no crash; CI's `fuzz` job ran all four targets clean. **Gate (f) is state-5.** **No fixture was weakened; `known-failures.txt` was not trimmed; the ADR-0124 drain was not re-litigated; the `echo` `typed_config` asymmetry was not "fixed".** No escalation to `superpowers:systematic-debugging` was needed — every local RED resolved to an already-documented environmental cause, and none re-failed deterministically on CI. `#![forbid(unsafe_code)]` holds. CONSUMES no carry-forward; **CF-66-1** + **CF-66-2** + **M66-2** stay OPEN. **DECISIONS.md ledger head: ADR-0126, unchanged** [next-available **ADR-0127**, unreserved]. ADR-0123 REFINED by ADR-0124, not superseded; ADR-0014 in force; ADR-0028 open; ADR-0049 governs config-validity. The next session is the state-5 code-review [`superpowers:requesting-code-review` → `REVIEW.md`].)

> Historical `## Last updated` notes — every superseded last-updated note (all closed phases incl. the phase-37 state-1..5 sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsection for fully-closed phase 64 (the `### Phase-64 state-5 code-review` narrative — phase 64 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-64 state-6 close-out when ROADMAP row `64` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 63 (the `### Phase-63 state-5 code-review` narrative — phase 63 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-63 state-6 close-out when ROADMAP row `63` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 61 (the `### Phase-61 state-1 brainstorm` / `### Phase-61 state-2 PLAN-write` / `### Phase-61 state-3 implementation` / `### Phase-61 state-4 verification` / `### Phase-61 state-5 code-review` narratives, relocated at the phase-61 state-6 close-out when ROADMAP row `61` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 58 (the `### Phase-58 state-1 brainstorm` / `### Phase-58 state-2 PLAN-write` / `### Phase-58 state-3 implementation` / `### Phase-58 state-4 verification` / `### Phase-58 state-5 code-review` narratives, relocated at the phase-58 state-6 close-out when ROADMAP row `58` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 57 (the `### Phase-57 state-1 brainstorm` / `### Phase-57 state-2 PLAN-write` / `### Phase-57 state-3 implementation` / `### Phase-57 state-4 verification` / `### Phase-57 state-5 code-review` narratives, relocated at the phase-57 state-6 close-out when ROADMAP row `57` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 42 (the `### Phase-42 state-5 code-review (active phase)` narrative — phase 42 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-42 state-6 close-out when ROADMAP row `42` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 43 (the `### Phase-43 state-5 code-review (active phase)` narrative — phase 43 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-43 state-6 close-out when ROADMAP row `43` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 44 (the `### Phase-44 state-5 code-review (active phase)` narrative — phase 44 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-44 state-6 close-out when ROADMAP row `44` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 45 (the `### Phase-45 state-5 code-review (active phase)` narrative — phase 45 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-45 state-6 close-out when ROADMAP row `45` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 46 (the `### Phase-46 state-5 code-review (active phase)` narrative — phase 46 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-46 state-6 close-out when ROADMAP row `46` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 47 (the `### Phase-47 state-5 code-review (active phase)` narrative — phase 47 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-47 state-6 close-out when ROADMAP row `47` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 48 (the `### Phase-48 state-5 code-review (active phase)` narrative — phase 48 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-48 state-6 close-out when ROADMAP row `48` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 49 (the `### Phase-49 state-5 code-review (active phase)` narrative — phase 49 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-49 state-6 close-out when ROADMAP row `49` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 50 (the `### Phase-50 state-3 implementation (active phase)` narrative — phase 50 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-50 state-6 close-out when ROADMAP row `50` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 51 (the `### Phase-51 state-3 implementation (active phase)` narrative — phase 51 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-51 state-6 close-out when ROADMAP row `51` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 53 (the `### Phase-53 state-1 brainstorm` narrative — phase 53 kept a single active-phase Notes subsection at "state-1 brainstorm" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-1 subsection remained in `## Notes` at close — relocated at the phase-53 state-6 close-out when ROADMAP row `53` flipped `done`, together with the inline superseded state-3 narrative) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 54 (the `### Phase-54 state-1 brainstorm` / `### Phase-54 state-2 PLAN-write` / `### Phase-54 state-3 implementation` / `### Phase-54 state-4 verification` / `### Phase-54 state-5 code-review` narratives, relocated at the phase-54 state-6 close-out when ROADMAP row `54` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 55 (the `### Phase-55 state-5 code-review (active phase)` narrative — phase 55 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state individually superseded in place and relocated to STATE_HISTORY.md at its own transition], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-55 state-6 close-out when ROADMAP row `55` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 56 (the `### Phase-56 state-5 code-review (active phase)` narrative — phase 56 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state individually superseded in place and relocated to STATE_HISTORY.md at its own transition], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-56 state-6 close-out when ROADMAP row `56` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Phase-66 state-4 verification (active phase)

- **The §7.5 gate is DONE and it PASSES. Do NOT re-run it** — every command's output is quoted verbatim in `PROGRESS.md` §"§5 state-4 VERIFICATION". (a) fixture `0071` green ×4; (b) CI 147/147 binaries `ok`; (c) h2spec green on CI; (d) no new fuzz target, `parse_bootstrap` clean, seed tracked — **explicitly recorded**; (e) build/clippy/fmt/deny exit 0 locally, `cargo test --workspace` green on CI. **(f) `REVIEW.md` is the NEXT session.**
- **`cargo test --workspace` exits 101 on THIS HOST, and its bare form ABORTS at the first failing binary** (209 lines; `envoy-config`'s 548 tests, h2spec and fixture `0071` never run). Always add `--no-fail-fast` locally. The failing set separates cleanly and was measured, not assumed: an **invariant core of 5** (`0061`/`0062`/`0069`/`0070` + `admin_config_dump_server_info`) fails in both runs AND deterministically in isolation ⇒ environmental; a **varying tail of 5** (`http_filter_fault`, `lb_ring_hash`, `upstream_circuit_breaker`, `upstream_connection_pooling_and_per_class_counters`, `xds_file_based_cds`) passes in isolation ⇒ parallel-load flake. **The arithmetic closes: local 1823 passed + 5 failed = 1828 = CI's 1828 passed + 0 failed, same 147 binaries.** All five core REDs are `ok` on CI. The four close-backend ones show **upstream Envoy itself** reporting `UF` / `Network_is_unreachable` at IPv6 `[fdc4:f303:9324::254]`; phase 66 cannot have changed the reference container's routing.
- **h2spec is a SKIP, not a pass, on this host.** `which h2spec` → not found; `h2spec_runner.rs:20-31` `eprintln!`-skips (returning `ok` in 0.00s) when absent. **Never quote a local `ok` as evidence, and never install it locally** — this host scores invalid-preface 3.5/2 as PASS while CI fails it, so a locally-"fixed" `known-failures.txt` breaks CI.
- **The drain is PINNED (ADR-0124) and CI-green. Do not re-litigate it.** `direct_response_once` does `write_all` → `flush` → `shutdown()` [FIN] → **read-and-discard until EOF** → drop. The mandatory mutation check was performed at state-3: commenting out the drain loop makes `post_eof_client_write_is_accepted_not_reset` fail with `Os {code: 32, kind: BrokenPipe}`. Fixture `0071` CANNOT catch this (its driver never writes after EOF) — the in-process test is the only guard. **Do not remove it.**
- **`envoy_config::DirectResponse` already existed** — it is the HCM **route-level** `direct_response` action (phase 04). The phase-66 network-filter type is deliberately `DirectResponseConfig`; `TypedConfig::DirectResponse` is an enum *variant* in a different namespace. `BEHAVIOR_CONTRACT.md`'s `## Network filters` section opens with a do-not-conflate banner because every other `direct_response` row in that document refers to the route-level action.
- **Terminal validation is an immutable PRE-PASS**, placed before the existing `for filter in &mut chain.filters` loop (which borrows mutably — the HCM arm calls `as_mut()`). That ordering also reproduces upstream Envoy's error precedence: a chain of `[direct_response, echo]` reports the TERMINAL error even when the trailing filter is itself malformed. R-0.8 safety was re-confirmed EMPIRICALLY.
- **`cargo build -p envoy-bin` before ANY local differential run** (memory `differential-harness-uses-debug-envoy-bin`) — the harness executes `target/debug/envoy-bin`, and this phase adds a NEW config key AND a NEW filter name, so a stale binary REDs with `unsupported network filter` / `unknown field`.
- **Do NOT "fix" the `echo` `typed_config` asymmetry** (upstream requires it; envoy-rust forbids it — the pre-existing ADR-0014 YAML shim behind fixture `0001`'s two-sided divergence). `direct_response` needs no shim: both sides of fixture `0071` are identical.
- **BLOCK-66-1 is CLEARED (ADR-0126).** Do not re-add `--quiet` to `spawn_backend`, do not remove the pre-build, do not widen the 30s readiness budget. *Log-reading trap:* CI interleaves ANSI escapes between `Compiling` and the crate name — grep the crate name alone.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: one state per session; do not chain states. Phase 66 (`66-network-filter-direct-response`) is the ACTIVE phase — ROADMAP row `66` is `in-progress`. The state-4 verification completed THIS session and the §7.5 gate PASSES. `SPEC.md`, `PLAN.md` and `PROGRESS.md` all exist and `REVIEW.md` does not, so the next session is the §5 state-5 code-review (`superpowers:requesting-code-review` → `REVIEW.md`) — it must NOT chain into the state-6 close-out. **Per §5.2, if `REVIEW.md` raises issues, re-enter at state 3, NOT state 4.** Row `66` flips to `done` only at state-6. ROADMAP rows `00`-`58` + `61` + `63` + `64` + `65` are `done` (`59`/`60`/`62` are intentional gaps with no rows).

> Historical Notes subsection for fully-closed phase 65 (the `### Phase-65 state-5 code-review` narrative — phase 65 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-65 state-6 close-out when ROADMAP row `65` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsection for fully-closed phase 52 (the `### Phase-52 state-5 code-review (active phase)` narrative — phase 52 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2/3/4 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-52 state-6 close-out when ROADMAP row `52` flipped `done`, together with the standing 05.1/05.4 future-reviewer note it carried) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 41 (the `### Phase-41 state-5 code-review (active phase)` narrative — phase 41 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the §6.2-recon PIVOT from `%REQ_WITHOUT_QUERY%` to `%ROUTE_NAME%` landed at state-2; the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-41 state-6 close-out when ROADMAP row `41` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 40 (the `### Phase-40 state-5 code-review (active phase)` narrative — phase 40 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-40 state-6 close-out when ROADMAP row `40` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 39 (the `### Phase-39 state-5 code-review (active phase)` narrative — phase 39 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-39 state-6 close-out when ROADMAP row `39` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
> Historical Notes subsection for fully-closed phase 38 (the `### Phase-38 state-2 PLAN-write (active phase)` narrative — phase 38 used a single active-phase Notes subsection for its whole arc; the state-3/4/5 sub-state narratives were superseded in the four-top-section archive blocks per-session, so only this state-2 subsection remained in `## Notes` at close — relocated at the phase-38 state-6 close-out when ROADMAP row `38` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) and for parent phase 25 (the `### Phase-25 state-1 brainstorm` pick + recon-finding narrative, relocated at the 25.2 state-6 close-out when parent `25` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 26 (the `### Phase-26 state-1 brainstorm` pivot/rejected-alternatives/key-scoping narrative, relocated at the phase-26 state-6 close-out when row `26` flipped to `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 27 (the `### Phase-27 state-1 brainstorm` / `### Phase-27 state-2 PLAN-write` / `### Phase-27 state-4 verification` narratives + the now-consumed `### Phase-27 carry-forwards` [M26-1..M26-8] block, relocated at the phase-27 state-6 close-out when row `27` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 28 (the `### Phase-28 state-1 brainstorm` / `### Phase-28 state-2 PLAN-write` / `### Phase-28 state-3 implementation + state-4 verification` / `### Phase-28 state-5 code review` narratives + the now-consumed `### Phase-28 carry-forwards` [M27-1..M27-3] block, relocated at the phase-28 state-6 close-out when row `28` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 29 (the now-consumed `### Phase-29 carry-forwards` [M28-1..M28-3] block + the `### Phase-29 state-1 brainstorm` / `### Phase-29 state-2 PLAN-write` / `### Phase-29 state-3 implementation` / `### Phase-29 state-4 verification` / `### Phase-29 state-5 code review` narratives, relocated at the phase-29 state-6 close-out when row `29` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 30 (the `### Phase-30 carry-forwards` [M29-1/M29-2, which fed phase 30 but were NOT consumed — they continue as phase-31 carry-forwards] block + the `### Phase-30 state-1 brainstorm` / `### Phase-30 state-2 PLAN-write` / `### Phase-30 state-3 implementation` / `### Phase-30 state-4 verification` / `### Phase-30 state-5 code review` narratives, relocated at the phase-30 state-6 close-out when row `30` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 31 (the `### Phase-31 carry-forwards` [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 — open Minors from the phase-30 REVIEW.md that fed phase 31 but were NOT consumed; they continue as carry-forwards for the next phase that touches the differential hash-sweep driver / the config parser] block + the `### Phase-31 state-1 brainstorm` / `### Phase-31 state-2 PLAN-write` / `### Phase-31 state-3 implementation` / `### Phase-31 state-4 verification` / `### Phase-31 state-5 code review` narratives, relocated at the phase-31 state-6 close-out when row `31` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 32 (the `### Phase-32 carry-forwards` block — the open Minors that fed phase 32 but were NOT consumed: the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3; they continue as carry-forwards for the future phase that touches their surface — re-listed in `## Next expected skill` above alongside the 6 new phase-32 REVIEW.md Minors M32-1…M32-6), relocated at the phase-32 state-6 close-out when row `32` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phases 33+34 (the `### Phase-33 carry-forwards` block — the active-phase carry-forwards Notes that lived through phases 33 and 34: the now-CONSUMED 6 phase-32 REVIEW.md Minors M32-1…M32-6 [folded + landed at the phase-33 state-3] + the "Other still-live carry-forwards" list [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3 + the HTTP-filters-family (1)-(4) — those still-live ones re-listed in `## Next expected skill` above alongside the new phase-34 Minors M34-1/M34-2/M34-3 + the phase-33 M33-1/M33-2]), relocated at the phase-34 state-6 close-out when row `34` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 35 (the `### Phase-35 state-1 brainstorm` / `### Phase-35 state-2 PLAN-write` / `### Phase-35 state-3 implementation` / `### Phase-35 state-4 verification` / `### Phase-35 state-5 code-review` narratives, relocated at the phase-35 state-6 close-out when ROADMAP row `35` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 36 (the `### Phase-36 state-5 code-review` narrative — phase 36 used a rename-in-place Notes discipline, so the state-1..4 sub-state narratives were superseded in place each session and only the four top-section blocks were relocated per-session — plus the now-CLOSED detailed M35-1 carry-forward bullet [CONSUMED by phase-36 F2], relocated at the phase-36 state-6 close-out when ROADMAP row `36` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 37 (the `### Phase-37 state-3 implementation` active-phase narrative, relocated at the phase-37 state-6 close-out when ROADMAP row `37` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### HTTP-filters-family carry-forwards (from the `25.2` REVIEW.md - NOT yet consumed; weigh whenever the HTTP-filters family is re-entered)

> These were never obligations on the xDS phase 26; they remain live for whenever an HTTP-filters-family phase resumes.

- **(1) [non-goal - architectural]** Over-limit request bodies are FULLY buffered before the 413 rejection (no streaming watermark). Documented deferred non-goal; differentially byte-identical to Envoy for the bounded fixture sizes. Revisit only if a streaming `decode_data` watermark path is ever planned.
- **(2) [doc precision]** The BEHAVIOR_CONTRACT 413-row "verified byte-exact against v1.33.0" phrasing - fixture `0033` is H1-only; the H2 over-limit path is covered by the in-process synth-decorator backstop, NOT differentially. Consider narrowing the phrasing if an H2 over-limit fixture is ever added.
- **(3) [coverage]** No standalone `== effective route limit` unit assertion (the boundary is exercised only via the over/under probes).
- **(4) [coverage]** No differential at-limit (`==`) probe in `0033` (within-limit `<` and over-limit `>` are both covered; the exact boundary is not differentially probed).
- _(2)-(4) are cheap polish, (1) is architectural and only relevant to a future streaming phase._
