# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase


**id:** **44 — `44-accesslog-upstream-host` — STATE-3 IMPLEMENTATION COMPLETE; STATE-4 §7.5 VERIFICATION GATE NEXT.** Both PLAN tasks landed (FIXTURE-ONLY — NO `src/` change): T1 = fixture `0052-accesslog-upstream-host` + the differential test `access_log_upstream_host.rs` (commit `9100feb`); T2 = the BEHAVIOR_CONTRACT `%UPSTREAM_HOST%` row update + fuzz-seed SKIP (already present) (commit `b8d6ea8`). The `0052` differential ran GREEN locally (cross-proxy `uh:"<ip>:<port>"` byte-identical). The next session is the **state-4 §7.5 verification gate** (`superpowers:verification-before-completion`) on the AUTHORITATIVE Linux CI — see `## Next expected skill`.
**slug:** `44-accesslog-upstream-host`
**directory:** `docs/envoy-rust/phases/44-accesslog-upstream-host/` (`SPEC.md` + `PLAN.md` authored; `PROGRESS.md` written at state-4 with the gate-quote)

**status:** **PHASE 44 (`44-accesslog-upstream-host`) — STATE-3 IMPLEMENTATION COMPLETE; STATE-4 §7.5 VERIFICATION GATE NEXT.** This session (the §5 state-3 implementation, routed via `superpowers:subagent-driven-development` + `test-driven-development`) executed `PLAN.md`'s 2 TDD tasks via a fresh implementer subagent (controller-reviewed), FIXTURE-ONLY — **NO `src/` change** (the §6.2 recon held: envoy-rust renders `%UPSTREAM_HOST%` via `SocketAddr::to_string()` = `<ip>:<port>` = Envoy's format). **T1 (commit `9100feb`):** fixture `0052-accesslog-upstream-host` (paired `envoy.yaml`/`envoy-rust.yaml`/`expectations.yaml`/`README.md`) — a STATIC `{{BACKEND_IP}}` single-endpoint `{{HTTP1_BACKEND_PORT}}` cluster (from `0036`, NOT `0051`'s STRICT_DNS, NOT `0036`'s `_1_`/`_2_` markers) + a `json_format` logging `%UPSTREAM_HOST%` (key `uh`) plus the deterministic anchors `%UPSTREAM_CLUSTER%`/`%RESPONSE_CODE_DETAILS%`/`%REQ(:METHOD)%`/`%PROTOCOL%`; the `http1_access_log_byte_exact` driver (cross-proxy-EQUALITY, NO static literal) + the differential test `tests/differential/tests/access_log_upstream_host.rs` (a structural clone of `access_log_upstream_cluster.rs`). **T2 (commit `b8d6ea8`):** the BEHAVIOR_CONTRACT `%UPSTREAM_HOST%` row (`:1029`) updated → DIFFERENTIALLY WITNESSED byte-exact by `0052`; the fuzz seed SKIPPED (`json_format_logger.yaml:27` already carries `%UPSTREAM_HOST%`). **Local evidence (state-3):** `cargo build --workspace` clean; `cargo build -p envoy-bin` + `-p differential --tests` clean; the `0052` differential ran GREEN in isolation (both proxies emitted byte-identical `{"method":"GET","proto":"HTTP/1.1","rcd":"via_upstream","uc":"backend","uh":"192.168.1.75:41353"}` — the shared host-LAN-IP `<ip>:<port>`). **NO new `Op` / `AccessLogRecord` field / crate / dependency / fuzz-target / `ConfigError` variant; NO `src/` change** → fixtures `0001`-`0051` stay byte-identical. The `### Phase-44 state-3 implementation` Notes subsection (below) carries the active-phase detail. **§6.1 split did NOT fire** (2 tasks; **ADR-0102 reserved-but-UNFIRED**). `#![forbid(unsafe_code)]` holds. **Per "State-4 = CI's first real execution":** the implementer did NOT run `cargo fmt`, so this state-3's first CI is EXPECTED red-at-fmt — the `style: cargo fmt` fix + the full Docker differential land at the state-4 gate. **Open carry-forward Minors (NONE blocks):** **M42-1** (ADVANCED by phase 43) + M39-1/M39-2 + M38-2/M38-1 + CF-39-1 + M40-1 + M37-2/M37-1 + M36-* + M34-* + M33-* + older. **DECISIONS.md ledger head: ADR-0101** (count 101; next-available **ADR-0102**, reserved for the phase-44 §6.1 split which did NOT fire). ADR-0014 in force; ADR-0028 open; ADR-0049 governs config-validity. **ROADMAP row `44` `in-progress`; rows `00`-`43` `done`.** The next session is the state-4 §7.5 verification gate — see `## Next expected skill`.

> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases incl. the phase-37 state-1..5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill

Per `BOOTSTRAP_PROMPT.md` §5 state 4 + `SKILL_ROUTING.md`: phase 44 is IMPLEMENTED (state-3 COMPLETE; T1 fixture `0052` + the differential test, T2 BEHAVIOR_CONTRACT + fuzz-SKIP — commits `9100feb`/`b8d6ea8`) → the next session is the **state-4 §7.5 verification gate** (`superpowers:verification-before-completion`): run the §7.5 acceptance battery on the AUTHORITATIVE Linux CI and quote the GREEN evidence into `PROGRESS.md`. **§7.5 gate items:** (a) fixture `0052` green (cross-proxy-equal `%UPSTREAM_HOST%` line) + (b) all `0001`-`0051` green simultaneously (NO operator/field change → byte-identical) + (c) h2spec ≥95% + (d) `parse_bootstrap`/`accesslog_format_parse` fuzz clean + (e) build/clippy/**fmt**/test/deny clean + (f) `REVIEW.md` deferred to state-5. **EXPECTED first-CI red-at-fmt** (per "State-4 = CI's first real execution" — the state-3 implementer did NOT `cargo fmt`): the gate's FIRST action is `cargo fmt` + a `style: cargo fmt` commit, THEN re-run CI to green. The full Docker differential (incl. `0052`) first runs at this CI gate — budget CI-iteration debugging; host bridge-IP/parallel-load false-REDs are CI-authoritative-green. Do ONE state per session (§5.1): STOP after the §7.5 GREEN evidence is quoted into `PROGRESS.md` + the STATE advance; state-5 code-review is the SESSION AFTER.

**Open carry-forward Minors (fold into the phase that next touches each surface; NONE blocks):** **M42-1** (ADVANCED by phase 43; the failure-path detail vocabulary still owed) + **M39-1/M39-2** + **M38-2/M38-1** + **CF-39-1** + **M40-1** (adjacent to the access-log surface) + M37-2/M37-1 + M36-1/M36-2/M36-3 + M34-1/M34-2/M34-3 + M33-1/M33-2 + the empty-`metadata_match` doc-comment + M29-1/M29-2 + M30-1/M30-2 + the phase-31 cosmetics + the HTTP-filters-family (1)-(4).

**Phase 43 is fully closed + CI-GREEN — do NOT re-verify.** Phases 42/41/40/39/38/37/36/35/34 likewise closed + CI-GREEN. Phase 44 is IMPLEMENTED (state-3 COMPLETE — fixture `0052` + the differential test + BEHAVIOR_CONTRACT, FIXTURE-ONLY); the next session is its state-4 §7.5 verification gate (`superpowers:verification-before-completion`) on the AUTHORITATIVE Linux CI — EXPECT first-CI red-at-fmt (fix with `style: cargo fmt`); §5.1 one state per session.

> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases incl. the phase-37 state-1..5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit

**Phase-44 state-3 implementation — fixture `0052` + the differential test + BEHAVIOR_CONTRACT (FIXTURE-ONLY) + STATE advance, push + confirm CI (THESE commits):** the state-3 implementation (`BOOTSTRAP_PROMPT.md` §5 state 3, routed via `superpowers:subagent-driven-development` + `test-driven-development`). Executed `PLAN.md`'s 2 TDD tasks via a fresh implementer subagent (controller-reviewed the fixture + test + cluster shape), FIXTURE-ONLY — **NO `src/` change**. **T1 (`9100feb`):** created `tests/fixtures/0052-accesslog-upstream-host/{envoy.yaml,envoy-rust.yaml,expectations.yaml,README.md}` — a STATIC `{{BACKEND_IP}}` single-endpoint `{{HTTP1_BACKEND_PORT}}` cluster (from `0036`; NOT `0051`'s STRICT_DNS; NOT `0036`'s `_1_`/`_2_` markers) + a `json_format` logging `%UPSTREAM_HOST%` (`uh`) plus the deterministic anchors `%UPSTREAM_CLUSTER%`/`%RESPONSE_CODE_DETAILS%`/`%REQ(:METHOD)%`/`%PROTOCOL%`; the `http1_access_log_byte_exact` driver (cross-proxy-EQUALITY, NO static literal — `%UPSTREAM_HOST%` is the DYNAMIC-but-SHARED `<host-LAN-IP>:<port>`) + the differential test `tests/differential/tests/access_log_upstream_host.rs` (a structural clone of `access_log_upstream_cluster.rs`). **T2 (`b8d6ea8`):** updated the BEHAVIOR_CONTRACT `%UPSTREAM_HOST%` row (`:1029`) → DIFFERENTIALLY WITNESSED byte-exact by `0052`; SKIPPED the fuzz seed (`json_format_logger.yaml:27` already carries `%UPSTREAM_HOST%`). **Local evidence:** `cargo build --workspace`/`-p envoy-bin`/`-p differential --tests` clean; the `0052` differential ran GREEN in isolation (both proxies emitted byte-identical `{"method":"GET","proto":"HTTP/1.1","rcd":"via_upstream","uc":"backend","uh":"192.168.1.75:41353"}`). **THIS STATE-advance commit** (STATE + STATE_HISTORY). NO new crate/dependency/fuzz-target; NO new `Op`/record-field/`ConfigError` variant (witnesses an existing operator); NO `src/` change → `0001`-`0051` stay byte-identical. **§6.1 split did NOT fire** (2 tasks); **ADR-0102 reserved-but-UNFIRED**. `#![forbid(unsafe_code)]` holds. **EXPECTED first-CI red-at-fmt** (the implementer did NOT `cargo fmt`) — fixed at the state-4 gate. Advanced STATE to phase-44 state-3-complete/state-4-next (the state-2 four top-section narratives RELOCATED to STATE_HISTORY.md per ADR-0035; the active-phase Notes subsection renamed `### Phase-44 state-2 PLAN-write` → `### Phase-44 state-3 implementation` in place). **DECISIONS.md ledger head: ADR-0101** (count 101; next-available **ADR-0102**). ADR-0014 in force; ADR-0028 open. The next session is the state-4 §7.5 verification gate (`superpowers:verification-before-completion`).

> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases incl. the phase-37 state-1..5 sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated

2026-06-27 (phase-44 **STATE-3 IMPLEMENTATION COMPLETE — STATE-4 §7.5 VERIFICATION GATE NEXT**. Executed `PLAN.md`'s 2 TDD tasks via a fresh implementer subagent [controller-reviewed], FIXTURE-ONLY — NO `src/` change [the §6.2 recon held: `SocketAddr::to_string()` = `<ip>:<port>` = Envoy's format]. **T1 [`9100feb`]:** fixture `0052-accesslog-upstream-host` [STATIC `{{BACKEND_IP}}` single-endpoint `{{HTTP1_BACKEND_PORT}}` cluster from `0036` — NOT `0051`'s STRICT_DNS, NOT `0036`'s `_1_`/`_2_` markers — + a `json_format` logging `%UPSTREAM_HOST%` [`uh`] + the anchors `%UPSTREAM_CLUSTER%`/`%RESPONSE_CODE_DETAILS%`/`%REQ(:METHOD)%`/`%PROTOCOL%`; the `http1_access_log_byte_exact` driver, cross-proxy-EQUALITY] + the differential test `tests/differential/tests/access_log_upstream_host.rs`. **T2 [`b8d6ea8`]:** BEHAVIOR_CONTRACT `%UPSTREAM_HOST%` row [`:1029`] → DIFFERENTIALLY WITNESSED by `0052`; fuzz seed SKIPPED [`json_format_logger.yaml:27` already carries it]. **Local evidence:** workspace/envoy-bin/differential-tests build clean; the `0052` differential ran GREEN in isolation [byte-identical `…"uh":"192.168.1.75:41353"` cross-proxy]. Advanced STATE to phase-44 state-3-complete [the state-2 four top-section narratives relocated to STATE_HISTORY.md per ADR-0035; the active-phase Notes subsection renamed `### Phase-44 state-2 PLAN-write` → `### Phase-44 state-3 implementation` in place]. NO new `Op`/record-field [witnesses an existing operator]; NO `src/` change → `0001`-`0051` byte-identical. **§6.1 split did NOT fire** [2 tasks; **ADR-0102 reserved-but-UNFIRED**]. **EXPECTED first-CI red-at-fmt** [implementer did not `cargo fmt`] — fixed at the state-4 gate. Carry-forwards: **M42-1** [ADVANCED by phase 43] + M39-1/M39-2 + M38-1/M38-2 + CF-39-1 + M40-1 + older stay live. **ROADMAP row `44` `in-progress`; rows `00`-`43` `done`.** `#![forbid(unsafe_code)]` holds. **DECISIONS.md ledger head: ADR-0101** [count 101; next-available ADR-0102]. ADR-0014 in force; ADR-0028 open. The next session is the state-4 §7.5 verification gate [`superpowers:verification-before-completion`].)

> Historical `## Last updated` notes — every superseded last-updated note (all closed phases incl. the phase-37 state-1..5 sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 42 (the `### Phase-42 state-5 code-review (active phase)` narrative — phase 42 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-42 state-6 close-out when ROADMAP row `42` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 43 (the `### Phase-43 state-5 code-review (active phase)` narrative — phase 43 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-43 state-6 close-out when ROADMAP row `43` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Phase-44 state-3 implementation (active phase)

**Pick (ADR-0101):** differentially WITNESS the `%UPSTREAM_HOST%` access-log command operator — the resolved upstream endpoint `<ip>:<port>` — BYTE-EXACT on a real upstream, the gap fixture `0051` deliberately excluded. A DIFFERENTIAL-COVERAGE phase: `%UPSTREAM_HOST%` is implemented since phase 06 (`AccessLogRecord.upstream_host: Option<String>` set at the HCM proxy-success arm `hcm.rs:994` → `endpoint.to_string()`) but has NEVER been verified against upstream Envoy.

**STATE-3 IMPLEMENTATION DONE (this session) — FIXTURE-ONLY, NO `src/` change.** Both PLAN tasks landed via a fresh implementer subagent (controller-reviewed): **T1 (`9100feb`)** = fixture `0052-accesslog-upstream-host` (STATIC `{{BACKEND_IP}}` single-endpoint `{{HTTP1_BACKEND_PORT}}` cluster + a `json_format` logging `%UPSTREAM_HOST%`/`uh` + the `%UPSTREAM_CLUSTER%`/`%RESPONSE_CODE_DETAILS%`/`%REQ(:METHOD)%`/`%PROTOCOL%` anchors; the `http1_access_log_byte_exact` cross-proxy-equality driver) + the differential test `tests/differential/tests/access_log_upstream_host.rs`; **T2 (`b8d6ea8`)** = the BEHAVIOR_CONTRACT `%UPSTREAM_HOST%` row (`:1029`) → DIFFERENTIALLY WITNESSED by `0052`, fuzz seed SKIPPED (already present). Local: workspace/envoy-bin/differential-tests build clean; the `0052` differential ran GREEN in isolation — both proxies emitted byte-identical `{"method":"GET","proto":"HTTP/1.1","rcd":"via_upstream","uc":"backend","uh":"192.168.1.75:41353"}` (the shared host-LAN-IP `<ip>:<port>`). The §6.2 recon HELD (no byte-mismatch). NEXT = the state-4 §7.5 gate on the AUTHORITATIVE CI (EXPECT first-CI red-at-fmt → `style: cargo fmt`; the full Docker differential incl. `0052` first runs there).

**THE state-2 §6.2 format-match recon — FIXTURE-ONLY (no `src/` change):** envoy-rust renders `%UPSTREAM_HOST%` via `Some(endpoint.to_string())` where `endpoint: std::net::SocketAddr` (`hcm.rs:391` field; `hcm.rs:994` set-site). `SocketAddr::to_string()` (the `Display` impl) emits `<ip>:<port>` — IPv4 UNBRACKETED (`127.0.0.1:13099`), only IPv6 gets `[..]:port` brackets — byte-IDENTICAL to Envoy's `%UPSTREAM_HOST%` `<ip>:<port>`. This is ALREADY PROVEN in-repo: the LB fixtures `0036`-`0038` hash this SAME `SocketAddr` Display byte-exact against Envoy's `address()->asString()` (BEHAVIOR_CONTRACT). The `0052` differential uses an IPv4 `{{BACKEND_IP}}` → no IPv6-bracket edge case. **⇒ NO `%UPSTREAM_HOST%` format fix; the phase is the fixture + docs only.**

**The feasibility basis (code-VERIFIED at state-1; spec-review confirmed on 3 legs):** (1) `run_fixture` (`tests/differential/src/lib.rs:3011-3013`) resolves `{{BACKEND_IP}}` to ONE shared host-LAN-IP via `discover_host_lan_ip()` for BOTH sides (GLOBAL, before the `Driver` dispatch — not driver-specific) + `{{HTTP1_BACKEND_PORT}}` to the backend's actual port (identical both sides) → both proxies dial the IDENTICAL `<host-LAN-IP>:<port>` → render the IDENTICAL `%UPSTREAM_HOST%`. (2) The `http1_access_log_byte_exact` driver asserts cross-proxy EQUALITY (`assert_access_log_lines_byte_identical`, `access_log.rs:305`), NOT against a static literal — so the DYNAMIC-but-SHARED address passes. (3) The LB fixtures `0036`/`0037`/`0038` are the `{{BACKEND_IP}}` STATIC-cluster precedent.

**PLAN.md (2 TDD tasks, plan-reviewed → APPROVED):** **T1** — fixture `0052-accesslog-upstream-host` (paired `envoy.yaml`/`envoy-rust.yaml`/`expectations.yaml`/`README.md`): a STATIC `{{BACKEND_IP}}` cluster from `0036`/`0037` with a SINGLE `{{HTTP1_BACKEND_PORT}}` endpoint (NOT `0051`'s STRICT_DNS, NOT `0036`'s `_1_`/`_2_` two-endpoint markers — folded plan-review M2) + the access-log `json_format` with `%UPSTREAM_HOST%` (+ `%UPSTREAM_CLUSTER%`/`%RESPONSE_CODE_DETAILS%` anchors) + the `kind: http1_access_log_byte_exact` driver from `0051` (cross-proxy-equality, NO static literal) + the differential test `tests/differential/tests/access_log_upstream_host.rs`. **T2** — the BEHAVIOR_CONTRACT `%UPSTREAM_HOST%` row update (~:1029) + (the `%UPSTREAM_HOST%` fuzz seed ALREADY EXISTS in `json_format_logger.yaml` → **SKIP** — folded plan-review M1) + the local gate. **NO new `Op`/record-field/crate/dep/fuzz-target/`ConfigError`** (witnesses an existing operator). Load-bearing invariant: NO operator/field change → all `0001`-`0051` stay byte-identical.

**Rejected:** M42-1's `%RESPONSE_CODE_DETAILS%` failure-path vocabulary (stronger but needs a failure-injection fixture + code — a bigger phase); `%REQUEST_HEADERS_BYTES%` (formula-match rabbit hole); `%ACCESS_LOG_TYPE%` (near-constant/weak); CF-39-1/`sort_properties` (empty-differential risk); gRPC ALS/OTLP/tracing/tap (heavy infra).

**Next (state-4 §7.5 verification gate, `superpowers:verification-before-completion`):** run the §7.5 acceptance battery on the AUTHORITATIVE Linux CI + quote the GREEN evidence into `PROGRESS.md`: (a) `0052` green + (b) `0001`-`0051` green simultaneously + (c) h2spec ≥95% + (d) `parse_bootstrap`/`accesslog_format_parse` fuzz clean + (e) build/clippy/fmt/test/deny clean. **First CI is EXPECTED red-at-fmt** (the implementer did not `cargo fmt`) — the gate's first action is `cargo fmt` + a `style: cargo fmt` commit, then re-run to green. The full Docker differential (incl. `0052`) first runs at this CI gate — budget CI-iteration; host bridge-IP/parallel-load false-REDs are CI-authoritative. The state-5 code-review is the SESSION AFTER. **ADR-0102 reserved-but-UNFIRED** (§6.1 split did NOT fire — 2 tasks).

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: one state per session; do not chain states. Phase 44 is IMPLEMENTED (ROADMAP row `44` `in-progress`; state-3 COMPLETE — fixture `0052` + the differential test + BEHAVIOR_CONTRACT, FIXTURE-ONLY); the next session is the state-4 §7.5 verification gate (`superpowers:verification-before-completion`) on the AUTHORITATIVE Linux CI — run the §7.5 battery, quote GREEN into `PROGRESS.md`, advance STATE; EXPECT first-CI red-at-fmt (fix with `style: cargo fmt`); do NOT also start the state-5 code-review that session.
- The reviewer's R2 disposition decision (option (a) retroactive split of 05.1 vs option (b) free-standing post-05.1 sub-phase) was settled at the 05.1 state-6 commit in favor of option (b); 05.4 is the chosen sibling sub-phase. Future-reviewers reading STATE.md should understand that 05.1 is structurally closed at the preamble landing; 05.4 is a SIBLING under parent-05, not a child of 05.1; and the execution order ran 05.1 → 05.4 → 05.2 → 05.3, with 05.3 the closing sub-phase that flips parent-05 to `done`.

> Historical Notes subsection for fully-closed phase 41 (the `### Phase-41 state-5 code-review (active phase)` narrative — phase 41 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the §6.2-recon PIVOT from `%REQ_WITHOUT_QUERY%` to `%ROUTE_NAME%` landed at state-2; the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-41 state-6 close-out when ROADMAP row `41` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 40 (the `### Phase-40 state-5 code-review (active phase)` narrative — phase 40 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-40 state-6 close-out when ROADMAP row `40` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 39 (the `### Phase-39 state-5 code-review (active phase)` narrative — phase 39 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-39 state-6 close-out when ROADMAP row `39` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
> Historical Notes subsection for fully-closed phase 38 (the `### Phase-38 state-2 PLAN-write (active phase)` narrative — phase 38 used a single active-phase Notes subsection for its whole arc; the state-3/4/5 sub-state narratives were superseded in the four-top-section archive blocks per-session, so only this state-2 subsection remained in `## Notes` at close — relocated at the phase-38 state-6 close-out when ROADMAP row `38` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) and for parent phase 25 (the `### Phase-25 state-1 brainstorm` pick + recon-finding narrative, relocated at the 25.2 state-6 close-out when parent `25` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 26 (the `### Phase-26 state-1 brainstorm` pivot/rejected-alternatives/key-scoping narrative, relocated at the phase-26 state-6 close-out when row `26` flipped to `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 27 (the `### Phase-27 state-1 brainstorm` / `### Phase-27 state-2 PLAN-write` / `### Phase-27 state-4 verification` narratives + the now-consumed `### Phase-27 carry-forwards` [M26-1..M26-8] block, relocated at the phase-27 state-6 close-out when row `27` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 28 (the `### Phase-28 state-1 brainstorm` / `### Phase-28 state-2 PLAN-write` / `### Phase-28 state-3 implementation + state-4 verification` / `### Phase-28 state-5 code review` narratives + the now-consumed `### Phase-28 carry-forwards` [M27-1..M27-3] block, relocated at the phase-28 state-6 close-out when row `28` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 29 (the now-consumed `### Phase-29 carry-forwards` [M28-1..M28-3] block + the `### Phase-29 state-1 brainstorm` / `### Phase-29 state-2 PLAN-write` / `### Phase-29 state-3 implementation` / `### Phase-29 state-4 verification` / `### Phase-29 state-5 code review` narratives, relocated at the phase-29 state-6 close-out when row `29` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 30 (the `### Phase-30 carry-forwards` [M29-1/M29-2, which fed phase 30 but were NOT consumed — they continue as phase-31 carry-forwards] block + the `### Phase-30 state-1 brainstorm` / `### Phase-30 state-2 PLAN-write` / `### Phase-30 state-3 implementation` / `### Phase-30 state-4 verification` / `### Phase-30 state-5 code review` narratives, relocated at the phase-30 state-6 close-out when row `30` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 31 (the `### Phase-31 carry-forwards` [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 — open Minors from the phase-30 REVIEW.md that fed phase 31 but were NOT consumed; they continue as carry-forwards for the next phase that touches the differential hash-sweep driver / the config parser] block + the `### Phase-31 state-1 brainstorm` / `### Phase-31 state-2 PLAN-write` / `### Phase-31 state-3 implementation` / `### Phase-31 state-4 verification` / `### Phase-31 state-5 code review` narratives, relocated at the phase-31 state-6 close-out when row `31` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 32 (the `### Phase-32 carry-forwards` block — the open Minors that fed phase 32 but were NOT consumed: the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3; they continue as carry-forwards for the future phase that touches their surface — re-listed in `## Next expected skill` above alongside the 6 new phase-32 REVIEW.md Minors M32-1…M32-6), relocated at the phase-32 state-6 close-out when row `32` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phases 33+34 (the `### Phase-33 carry-forwards` block — the active-phase carry-forwards Notes that lived through phases 33 and 34: the now-CONSUMED 6 phase-32 REVIEW.md Minors M32-1…M32-6 [folded + landed at the phase-33 state-3] + the "Other still-live carry-forwards" list [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3 + the HTTP-filters-family (1)-(4) — those still-live ones re-listed in `## Next expected skill` above alongside the new phase-34 Minors M34-1/M34-2/M34-3 + the phase-33 M33-1/M33-2]), relocated at the phase-34 state-6 close-out when row `34` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 35 (the `### Phase-35 state-1 brainstorm` / `### Phase-35 state-2 PLAN-write` / `### Phase-35 state-3 implementation` / `### Phase-35 state-4 verification` / `### Phase-35 state-5 code-review` narratives, relocated at the phase-35 state-6 close-out when ROADMAP row `35` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 36 (the `### Phase-36 state-5 code-review` narrative — phase 36 used a rename-in-place Notes discipline, so the state-1..4 sub-state narratives were superseded in place each session and only the four top-section blocks were relocated per-session — plus the now-CLOSED detailed M35-1 carry-forward bullet [CONSUMED by phase-36 F2], relocated at the phase-36 state-6 close-out when ROADMAP row `36` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 37 (the `### Phase-37 state-3 implementation` active-phase narrative, relocated at the phase-37 state-6 close-out when ROADMAP row `37` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### HTTP-filters-family carry-forwards (from the `25.2` REVIEW.md - NOT yet consumed; weigh whenever the HTTP-filters family is re-entered)

> These were never obligations on the xDS phase 26; they remain live for whenever an HTTP-filters-family phase resumes.

- **(1) [non-goal - architectural]** Over-limit request bodies are FULLY buffered before the 413 rejection (no streaming watermark). Documented deferred non-goal; differentially byte-identical to Envoy for the bounded fixture sizes. Revisit only if a streaming `decode_data` watermark path is ever planned.
- **(2) [doc precision]** The BEHAVIOR_CONTRACT 413-row "verified byte-exact against v1.33.0" phrasing - fixture `0033` is H1-only; the H2 over-limit path is covered by the in-process synth-decorator backstop, NOT differentially. Consider narrowing the phrasing if an H2 over-limit fixture is ever added.
- **(3) [coverage]** No standalone `== effective route limit` unit assertion (the boundary is exercised only via the over/under probes).
- **(4) [coverage]** No differential at-limit (`==`) probe in `0033` (within-limit `<` and over-limit `>` are both covered; the exact boundary is not differentially probed).
- _(2)-(4) are cheap polish, (1) is architectural and only relevant to a future streaming phase._
