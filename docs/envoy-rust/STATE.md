# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase

**id:** `25.2` — `25.2-http-filter-buffer` (Part B of parent phase `25` `envoy.filters.http.buffer` — the `BufferFilter` + `Buffer` chain config + `BufferPerRoute` per-route override + fixture `0033` + parent-25 close; the ADR-0064 split sibling of the now-closed `25.1`), lifecycle **state-3-complete / state-4-next** (`SPEC.md` + `PLAN.md` + `PROGRESS.md` present [all 7 PLAN tasks T1–T7 recorded green]; `REVIEW.md` ABSENT → the next skill is the state-4 §7.5 verification gate). Sub-phase **`25.1` is CLOSED** (state-6-complete). ROADMAP rows `00`–`24` + `25.1` are all `done`; parent row `25` is `in-progress` (sub-phases `25.1, 25.2`; flips `done` only when `25.2` closes); row `25.2` stays `planned` (the §5 state machine — not the ROADMAP status — tracks transitions; ROADMAP rows `25.2` AND parent `25` both advance to `done` at the `25.2` state-6 close per BOOTSTRAP §6.1 step 4).
**slug:** `25.2-http-filter-buffer`
**directory:** `docs/envoy-rust/phases/25.2-http-filter-buffer/` — now carries **`SPEC.md` + `PLAN.md` + `PROGRESS.md`** (`PROGRESS.md` records all 7 state-3 task entries T1–T7; `REVIEW.md` absent — authored at state-5). The now-closed sibling `docs/envoy-rust/phases/25.1-h1-request-body-forwarding/` carries all 4 artifacts (`REVIEW.md` APPROVED); the parent dir `docs/envoy-rust/phases/25-http-filter-buffer/` carries the parent `SPEC.md`.

**status:** **SUB-PHASE 25.2 (`25.2-http-filter-buffer`) STATE-3 IMPLEMENTATION COMPLETE — all 7 PLAN tasks T1–T7 landed green; FINAL task T7 + STATE advance at THIS commit** (lifecycle state-3-complete / state-4-next). This session ran `superpowers:subagent-driven-development` for the FINAL task **T7** (BEHAVIOR_CONTRACT 413 local-reply row + a `parse_bootstrap` `seed-buffer.yaml` fuzz corpus seed [no new fuzz target] + the full state-3 §7.5 workspace gate), completing the 7-task PLAN (T1 config schema `ecc674a9d`; T2+T3 `BufferFilter` runtime + wiring `f84b13066`/`ad2c9a859`; T4 H1 body-allocation bound + tests `d7ea6f0fd`; T5 differential harness body support `61229f844`; T6 fixture `0033` + acceptance test `446b32d0b`; T7 production `b624b8288` / PROGRESS `aafb3305d`). **T7 ran the DEFERRED clippy** (skipped per-task across T1–T6 per `project_state3_arc_skips_clippy`): the phase-25.2 code was already clippy-clean — the only lint was a one-line doc-comment continuation in the T6 test file. **Full state-3 gate GREEN:** `cargo fmt --all -- --check`, isolated-crate build (`-p envoy-config -p envoy-filter -p envoy-http1`), `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and `cargo test --workspace` all clean (the 3 differential process-spawn tests that flaked under workspace-run contention confirmed **137/137 passing standalone** — `project_workspace_test_nested_cargo_backstop_flake` / `project_flaky_access_log_fixture_0012`). Two-stage review (spec-compliance ✅ + code-quality ✅, one Minor heading-colon applied). **DECISIONS.md ledger head: ADR-0064** (count 65; next available **ADR-0065**, UNFIRED — T1–T7 surfaced no new decision beyond the locked ADR-0062/0063/0064). ADR-0014 in force; ADR-0028 open. No `unsafe`. Per §5.1 (one state per session) this session EXITS after the FINAL-task T7 commit + STATE advance; the NEXT session runs `25.2`'s state-4 §7.5 verification gate via `superpowers:verification-before-completion` (re-confirm the workspace gate + `cargo deny check` + `cargo fuzz run parse_bootstrap` short-budget + the Docker **33**-fixture differential LOCALLY per `feedback_state4_runs_docker_differential`; AUTHORITATIVE Linux CI anchor per ADR-0049).

> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases + the active phase's prior sub-state pointers, incl. the phase-25 state-1 brainstorm pointer) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill

Per `BOOTSTRAP_PROMPT.md` §5 state 4 + `SKILL_ROUTING.md`: with `25.2` now carrying `SPEC.md` + `PLAN.md` + `PROGRESS.md` (all 7 PLAN tasks T1–T7 recorded green) and `REVIEW.md` ABSENT → §5 **state 4** (state-3 implementation COMPLETE), the next session runs **`superpowers:verification-before-completion`** to execute the state-4 §7.5 verification gate over the full `25.2` implementation. The gate RE-CONFIRMS the workspace build/clippy/fmt/test (already green at T7) and ADDS: `cargo deny check`; `cargo fuzz run parse_bootstrap` short-budget (now exercising the new `seed-buffer.yaml` corpus seed); and the **Docker 33-fixture differential LOCALLY** (`feedback_state4_runs_docker_differential` — Docker restored on this Mac; pre-build `cargo test -p differential -p h2spec-conformance --no-run` first; mind `project_local_differential_startup_stall` [~50min local, patient polling] + the `project_flaky_access_log_fixture_0012` re-run-clears-it family). The AUTHORITATIVE acceptance anchor is Linux CI per ADR-0049 (all 33 Docker-gated fixtures `0001`–`0033` green simultaneously + h2spec ≥95%). Per §5.1 the state-4 gate is the NEXT session's single state — record a `## State-4 verification` entry in `PROGRESS.md`; on green, state-5 runs `superpowers:requesting-code-review`, then the state-6 close-out flips ROADMAP rows `25.2` AND parent `25` to `done`.

> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases + the active phase's prior sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit

**Phase-25.2 state-3 implementation — FINAL task T7 + STATE advance (THIS commit):** the docs/STATE commit advancing `25.2` from lifecycle state-3 to **state-3-complete / state-4-next** after the last of the 7 PLAN tasks landed. T7 (production `b624b8288`: BEHAVIOR_CONTRACT.md 413 local-reply row [413 `Payload Too Large` / 17B no-newline / hex / content-type+length via the synth decorators / NO buffer stats / strict `>`] + `crates/envoy-config/fuzz/corpus/parse_bootstrap/seed-buffer.yaml` [chain `Buffer` + both `BufferPerRoute` oneof arms; no new fuzz target] + the `.gitignore` whitelist + a one-line clippy doc fix; PROGRESS `aafb3305d`) completed the 7-task PLAN (T1 `ecc674a9d`; T2/T3 `f84b13066`/`ad2c9a859`; T4 `d7ea6f0fd`; T5 `61229f844`; T6 `446b32d0b`). The full state-3 §7.5 workspace gate is GREEN (fmt + isolated build + workspace build + clippy `-D warnings` + `cargo test --workspace` — the 3 differential contention flakes confirmed 137/137 standalone); T7's deferred-clippy run found the phase-25.2 code already clean. Two-stage review ✅✅. THIS commit advances `STATE.md` to `25.2` state-4-next (next skill `superpowers:verification-before-completion`) and relocates the superseded `25.2` state-3 top-section blocks verbatim to `STATE_HISTORY.md` (ADR-0035 / §4.1 inv. 9). **DECISIONS.md ledger head: ADR-0064** (count 65; next available **ADR-0065**, UNFIRED). ADR-0014 in force; ADR-0028 open. No production change in THIS commit (T7's code was committed at `b624b8288`/`aafb3305d`); no `unsafe`; no new ADR. ROADMAP rows `25.2` + parent `25` stay as-is (advance to `done` at the state-6 close). Per §5.1 this session EXITS after the STATE advance; the NEXT session runs `25.2`'s state-4 verification gate.

> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases + the active phase's prior sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated

2026-06-14 (phase-25.2 **state-3 implementation COMPLETE — FINAL task T7 + STATE advance** — executed the last of the 7 PLAN tasks via `superpowers:subagent-driven-development`: T7 [BEHAVIOR_CONTRACT 413 local-reply row + `parse_bootstrap` `seed-buffer.yaml` fuzz corpus seed (no new fuzz target) + `.gitignore` whitelist + a one-line clippy doc fix; production `b624b8288`, PROGRESS `aafb3305d`], completing T1–T7. T7 ran the DEFERRED clippy [skipped per-task T1–T6 per `project_state3_arc_skips_clippy`] — phase-25.2 code already clean. Full state-3 §7.5 gate GREEN: fmt + isolated-crate build + `cargo build --workspace --all-targets` + `cargo clippy --workspace --all-targets --all-features -- -D warnings` + `cargo test --workspace` [3 differential process-spawn contention flakes confirmed 137/137 standalone per `project_workspace_test_nested_cargo_backstop_flake` / `project_flaky_access_log_fixture_0012`]. Two-stage review ✅✅ [one Minor heading-colon applied]. Advanced `STATE.md` to `25.2` lifecycle state-4-next [next skill `superpowers:verification-before-completion`] and relocated the superseded `25.2` state-3 top-section blocks verbatim to `STATE_HISTORY.md` [ADR-0035]. No `unsafe`; no new ADR [T1–T7 surfaced no decision beyond ADR-0062/0063/0064 → ADR-0065 UNFIRED]. ROADMAP rows `25.2` + parent `25` left as-is [advance to `done` at the state-6 close]. Ledger head **ADR-0064** [count 65; next available **ADR-0065**]. ADR-0014 in force; ADR-0028 open. Per §5.1 the `25.2` state-4 §7.5 verification gate is the NEXT session.)

> Historical `## Last updated` notes — every superseded last-updated note (all closed phases + the active phase's prior sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: one state per session; do not chain states. State-6 close-out commits touch ROADMAP.md + STATE.md only and carry no code changes; the next session writes PLAN.md for the next active phase per `superpowers:writing-plans`.
- The reviewer's R2 disposition decision (option (a) retroactive split of 05.1 vs option (b) free-standing post-05.1 sub-phase) was settled at the 05.1 state-6 commit in favor of option (b); 05.4 is the chosen sibling sub-phase. Future-reviewers reading STATE.md should understand that 05.1 is structurally closed at the preamble landing; 05.4 is a SIBLING under parent-05, not a child of 05.1; and the execution order ran 05.1 → 05.4 → 05.2 → 05.3, with 05.3 the closing sub-phase that flips parent-05 to `done`.

> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Phase-25 state-1 brainstorm

- **Pick:** phase `25` = `envoy.filters.http.buffer`, selected at this brainstorm from `MISSION.md`'s §9 HTTP-filters family (the richest remaining vein). The easy header-only HTTP-filter vein is EXHAUSTED — all 7 shipped filters (header_mutation/local_ratelimit/rbac/fault/jwt/cors/csrf) are header-only decode-side decisions and the framework is headers-only (no streaming `decode_data` hook). Buffer is the lightest remaining body-dependent filter (no external service / engine / gzip dep / timing — a pure function of method + body length + policy → deterministic, locally-verifiable, byte-exact differential) AND the THIRD per-route `typed_per_filter_config` consumer (after cors+csrf). Rejected: the LB family (larger/riskier first cut + a new distinguishable-backend harness), the network-filters opener (low leverage), the heavier HTTP filters (external service / engine / non-deterministic), an H2-only buffer (near-cosmetic — exploits H2's incidental pre-buffering, builds no infra), and a pure H1-forwarding phase with buffer deferred (ships a foundation with no consumer). See ADR-0062.

- **Critical recon finding (SPEC §0):** H1 does NOT forward request bodies upstream today — the router forwards an always-empty body (`crates/envoy-http1/src/hcm.rs:356`) and drains+discards the downstream Content-Length body (`:678-697`), a pre-existing gap since phase 04.3; whereas H2 already fully buffers+forwards (`crates/envoy-http2/src/hcm.rs:437-448`). So buffer is NOT a pure plug-in: Part A (now `25.1`) closes the H1 forwarding gap (a regression-equivalence foundation slice), Part B (now `25.2`) adds the filter. The §6.1 split FIRED at the state-2 PLAN-write (ADR-0064).

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035. (The `### Phase-25 state-1 brainstorm` subsection above REMAINS while parent `25` is `in-progress`.)
