# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase

**id:** `23` — `23-http-filter-cors` (the HTTP-filters family's FIFTH concrete phase — `envoy.filters.http.cors`; the FIRST per-route `typed_per_filter_config` infrastructure in the project), lifecycle **state-1-complete / state-2-next**. ROADMAP rows `00`–`22` are all `done`; row `23` is `planned` (it flips to `in-progress` at the next session's state-2 PLAN-write per invariant 4.1.3).
**slug:** `23-http-filter-cors`
**directory:** `docs/envoy-rust/phases/23-http-filter-cors/` — carries **`SPEC.md` only** (the state-1 brainstorm output; `PLAN.md` + `PROGRESS.md` + `REVIEW.md` do NOT exist yet — the state-2 PLAN-write writes `PLAN.md` next session). The four most-recently-closed phase directories — 22 `22-http-filter-jwt-authn`, 21 `21-xds-file-based-eds`, 20, 19 — each remain CLOSED carrying all 4 artifacts.

**status:** **PHASE 23 (`23-http-filter-cors`, the `cors` HTTP filter + per-route `typed_per_filter_config` infrastructure) STATE-1 NEW-PHASE BRAINSTORM COMPLETE at THIS commit** (lifecycle state-1-complete / state-2-next). This commit is the **phase-23 state-1 NEW-PHASE brainstorm** per `BOOTSTRAP_PROMPT.md` §5 state 0→1 + `SKILL_ROUTING.md` state 0/1 + the new-feature-family-phase brainstorm precedents (phase-12/13/15/16/17/18/19/20/21/22: a single state-0/1 brainstorm collapses BOTH states — adds the ROADMAP row AND creates the phase directory + `SPEC.md` + a scoping ADR in ONE docs-only commit). It (a) picks the next phase — **`23 — envoy.filters.http.cors`** (the HTTP-filters family, the richest remaining §9 vein; `cors` chosen to deliberately take on the per-route `typed_per_filter_config` infrastructure ADR-0055 deferred at phase 22 as "the natural cors close site" — the single highest-leverage deferred unlock in the filter family, gating cors AND csrf AND buffer AND per-route overrides of the four existing filters; chosen over the jwt_authn requirement-combinators follow-on [lower leverage — extends one filter], `compression` [gzip dep + non-byte-exact bodies], the load-balancing family [distribution/hash-exact testing + distinguishable-backend harness], xDS file-watching [deferred a SIXTH time on the three stacked ADR-0050 risks], the network-filters family, file-based SDS [low value], and the still-blocked gRPC family); (b) adds ROADMAP row `23` (`status: planned`) beneath the "HTTP filters family" §9 heading (the FIFTH concrete row there, after 09/10/11/22); (c) creates `docs/envoy-rust/phases/23-http-filter-cors/` + writes `SPEC.md`; (d) lands **ADR-0057** (the scoping ADR — the pick + the per-route-config-infrastructure decision + the minimum-viable scope + the alternatives-rejected analysis; see the `### Phase-23 state-1 brainstorm` Notes subsection); (e) advances this STATE Active phase from "AWAITING NEXT PLANNING" to `23` state-1-complete / state-2-next. **Three load-bearing scoping findings (ADR-0057; SPEC §0; read-only reconnaissance at HEAD `bd3cfd644`):** (1) **CORS is genuinely per-route in modern Envoy** — the filter-chain `Cors` entry is near-empty; the actual `CorsPolicy` is attached per-route via `typed_per_filter_config`, which does NOT exist in envoy-config (`Route` `bootstrap.rs:1152-1158`, `VirtualHost` `:1141-1149`, `RouteConfiguration` `:1124-1137` carry zero per-filter config; grep-empty); (2) **the HCM runs the filter pipeline BEFORE it resolves the matched route** (H1 `pipeline.decode_headers` at `crates/envoy-http1/src/hcm.rs:612` vs `build_response` route matching at `:1180-1202`; H2 at `crates/envoy-http2/src/hcm.rs:465-486`) — so phase 23 must resolve the route up-front and thread it + its per-filter config into the pipeline; (3) **that route-early-resolution change is regression-safe** — no existing filter reads the route (Router terminus decode no-op `crates/envoy-filter/src/router.rs:28-34`; `build_response` reuses the resolved route), so all 30 existing fixtures stay green (the 05.1/07.1/12.1/14.1 foundation-slice property). **Scope (ADR-0057; SPEC §0/§3):** a `typed_per_filter_config` map on `Route` + an `@type`-tagged `PerFilterConfig` enum (only the `Cors` variant registered/tested this phase) + a `CorsPolicy` schema (`allow_origin_string_match` reusing 04.x `StringMatcher` + `allow_methods`/`allow_headers`/`expose_headers`/`max_age`/`allow_credentials`); the HCM route-early-resolution + per-route-config threading on BOTH H1 + H2 (inert for every non-CORS filter → regression-equivalence); `CorsFilter` (seventh `HttpFilterInstance` variant; decode-side CORS-preflight short-circuit [`OPTIONS`+`Origin`+`Access-Control-Request-Method` → 200 + `access-control-*` via `Decision::StopAndSend`, decorated by the existing H1/phase-11-H2 filter-synth helpers] + encode-side actual-request response decoration [the phase-07.2 header_mutation push pattern] — the first genuinely encode-active feature filter); conditional `cors.{origin_valid,origin_invalid}` stats (namespace §6.2-verified); fixture `0031-http-filter-cors` (H1; 4 deterministic header-only probes — preflight-allowed / GET-allowed / GET-disallowed / GET-no-origin; zero timing/crypto); an in-process backstop; a new `parse_bootstrap` fuzz seed (NO new fuzz target — CORS reuses `StringMatcher`, no new parser crate); BEHAVIOR_CONTRACT cors rows. DEFER per ADR-0057 / SPEC §4: `filter_enabled`/`shadow_enabled` runtime gating, `allow_private_network_access`, vhost-level config + the route>vhost most-specific-config cascade, `typed_per_filter_config` for filters OTHER than cors, the deprecated inline `route.cors` field, gRPC-Web `bypass_cors_preflight`. **§6.1 split-gate: projected single-phase (~1100–1500 LoC / ~10–13 tasks — at the upper edge of the ~1500-LoC gate, the D2 cross-cutting HCM change [H1 + H2] the swing factor); the split valve is held in clear reserve as `23.1` [the per-route `typed_per_filter_config` schema + the HCM route-early-resolution + threading — a foundation slice with no new fixture, proven via regression-equivalence on all 30 existing fixtures, regression-safe per finding 3] / `23.2` [the `CorsFilter` + the `HttpFilterInstance` variant + stats + fixture 0031 + backstop + close]; the PLAN-writer decides AFTER the §6.2 `aws-lc-rs`-free verification materializes the D2 blast radius; the split ADR would be ADR-0059.** **DECISIONS.md ledger head: ADR-0057** (count 58; next available **ADR-0058**, reserved for the §6.2 reconciliation [most-likely trigger: the preflight status/headers or the allow-origin echo-vs-`*`]; **ADR-0059** reserved for the split). **ADR-0014 REMAINS IN FORCE** (the YAML-native shim — CORS does not engage the xDS protos); **ADR-0028 REMAINS OPEN** — phase 23 does not engage it. **THIS COMMIT touches exactly 5 docs files** (the four-file brainstorm set + STATE_HISTORY for the ADR-0035 relocations): (1) **CREATE** `docs/envoy-rust/phases/23-http-filter-cors/SPEC.md`; (2) **MODIFY** `docs/envoy-rust/ROADMAP.md` (add row `23` `status: planned` beneath the "HTTP filters family" heading — invariant 4.1.2: a new row enters `planned`; no flip of any existing row); (3) **MODIFY** `docs/envoy-rust/DECISIONS.md` (append **ADR-0057**, the scoping ADR); (4) **MODIFY** `docs/envoy-rust/STATE.md` (this file — top-pointer advance + `### Phase-23 state-1 brainstorm` Notes subsection appended; the prior "AWAITING NEXT PLANNING" blocks RELOCATED to STATE_HISTORY.md); (5) **MODIFY** `docs/envoy-rust/STATE_HISTORY.md` (the ADR-0035 / §4.1 invariant-9 relocations of the superseded phase-22-close-out top-section blocks). No production code / test / fixture / PLAN / PROGRESS / REVIEW / BEHAVIOR_CONTRACT / Cargo change; no `unsafe`. The brainstorm commit is docs-only so the CI run is vacuous-green. Per `BOOTSTRAP_PROMPT.md` §5.1 (one state per session) this brainstorm session EXITS after this commit; the NEXT session writes `PLAN.md` (state 2 — `superpowers:writing-plans` scoped to the phase-23 SPEC, with the §6.2 empirical verification against `envoyproxy/envoy:v1.33.0` — which RUNS LOCALLY for this filter phase).

> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases + the active phase's prior sub-state pointers, incl. the phase-22 state-1→state-5 pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill

Per `BOOTSTRAP_PROMPT.md` §5 state 2 + `SKILL_ROUTING.md` (state 2 — SPEC.md exists, PLAN.md does NOT → `superpowers:writing-plans` → PLAN.md → GATE: split if > ~25 tasks or > ~1500 LoC): the next session runs the **phase-23 state-2 standalone PLAN-write** (`superpowers:writing-plans`) over `docs/envoy-rust/phases/23-http-filter-cors/SPEC.md`, following the 06.2→22 standalone-PLAN cadence (ONE pre-Task-1 docs-only commit: `PLAN.md` + `PROGRESS.md` skeleton + Task-1 preamble + ROADMAP row-23 `planned → in-progress` flip + STATE advance + any §6.2 reconciliation ADR). **Concretely the PLAN-write must:** (1) run the **SPEC §6.2 empirical verification LOCALLY** against `envoyproxy/envoy:v1.33.0` (digest `sha256:56da5afd…`; CORS has no virtiofs/inotify dependency — the phase-22 §6.2-local methodology) — resolve the **Envoy CORS config shape FIRST** (the filter-chain `Cors` `@type`/fields + the per-route `CorsPolicy` `@type`/fields attached via `typed_per_filter_config`; gates the D1 schema), then capture byte-exact the **preflight response** (status 200-vs-204 + the `access-control-*` header names/values/formatting + the allow-origin echo-vs-`*` + the body/`content-length`), the **actual-request decoration**, the **disallowed/no-origin dispositions**, the **`cors` stat namespace**, and the **policy-for-absent-filter disposition** — land inline **ADR-0058** if any diverges materially from the SPEC projection (the phase-10 ADR-0034 / phase-22 ADR-0056 precedent); (2) run the **PLAN-time SPEC-correction pass** (read-only Explore + controller grep at HEAD — confirm the SPEC §0/§3 anchors: `Route`/`HttpFilterTypedConfig`/`StringMatcher`/`HttpFilterInstance`/`build_response`/`decorate_filter_synth_response{,_h2}` line cites); (3) **evaluate the §6.1 split gate** — the D2 cross-cutting HCM change (H1 + H2) is the swing factor; recommended single-phase, split reserved as 23.1 (per-route-config schema + HCM route-early-resolution+threading, regression-safe foundation slice) / 23.2 (CorsFilter + variant + stats + fixture 0031 + close), the split ADR ADR-0059; (4) write `PLAN.md` + the `PROGRESS.md` skeleton + the Task-1 preamble (clippy PER TASK per `project_state3_arc_skips_clippy`; the touched-crate standalone builds at state-4 per `project_isolated_crate_build_blindspot`; pre-build `tests/helpers/*` per `project_flaky_access_log_fixture_0012`; **a workspace-wide `cargo build` + `cargo test --workspace` at every D2/D5 config-variant + HCM-threading task** — the phase-22 LESSON that a config-enum variant breaks an exhaustive match masked by per-crate testing); (5) flip ROADMAP row `23` `planned → in-progress`; (6) advance this STATE to state-2-complete / state-3-next. Per §5.1 (one state per session) the PLAN-write lands its single docs-only commit and exits; the state-3 execution arc (`superpowers:subagent-driven-development`, SERIAL dispatch) is a LATER session.

> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases + the active phase's prior sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit

**Phase-23 state-1 NEW-PHASE brainstorm commit (THIS commit):** `phase 23: state-1 brainstorm — http-filter-cors SPEC.md (HTTP-filter-family fifth phase; first per-route typed_per_filter_config infra) [ADR-0057]`. Docs-only (CREATE `docs/envoy-rust/phases/23-http-filter-cors/SPEC.md` + MODIFY `docs/envoy-rust/ROADMAP.md` [add row `23` `status: planned`] + `docs/envoy-rust/DECISIONS.md` [append **ADR-0057**] + `docs/envoy-rust/STATE.md` [this file] + `docs/envoy-rust/STATE_HISTORY.md` [the ADR-0035 relocations of the superseded phase-22-close-out top-section blocks]). The **phase-23 state-1 NEW-PHASE brainstorm** (`superpowers:brainstorming`; the state-0/1-collapsing cadence of phases 12-22): picks + scopes **`23 — envoy.filters.http.cors`** (the HTTP-filters family's fifth concrete phase; the FIRST per-route `typed_per_filter_config` infrastructure — the gating unlock for cors AND csrf/buffer + per-route overrides of the existing filters), locks the minimum-viable scope by ADR-0057, and records the three §0 reconnaissance findings (per-route config absent; the HCM runs the pipeline before route resolution; the route-early-resolution change is regression-safe). Next phase id `23`; **DECISIONS.md ledger head ADR-0057 (count 58); ADR-0058 reserved for the §6.2 reconciliation; ADR-0059 reserved for the split.** ADR-0014 in force; ADR-0028 open. No code/test/fixture/Cargo/BEHAVIOR_CONTRACT change; no `unsafe`. Docs-only → the CI run at this push is vacuous-green (the project's differential evidence remains the phase-22 state-4 CI anchor `27179560746` at code-HEAD `04b9e97c4`). Per §5.1 the next session is the **phase-23 state-2 standalone PLAN-write** (`superpowers:writing-plans`; see `## Next expected skill`).

> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases + the active phase's prior sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated

2026-06-09 (phase-23 **state-1 NEW-PHASE brainstorm COMPLETE** — `superpowers:brainstorming`. Picked + scoped **`23 — envoy.filters.http.cors`** (the HTTP-filters family's fifth concrete phase after 09/10/11/22) TOGETHER WITH the per-route `typed_per_filter_config` infrastructure it is the first consumer of — the single highest-leverage deferred unlock in the filter family (gating cors AND csrf/buffer + per-route overrides of fault/rbac/local_ratelimit), the "natural cors close site" ADR-0055 deferred at phase 22. Chosen over the jwt_authn requirement-combinators follow-on [lower leverage], `compression` [gzip dep + non-byte-exact bodies], the load-balancing family [distribution/hash-exact testing + distinguishable-backend harness], xDS file-watching [deferred a sixth time on the three stacked ADR-0050 risks], the network-filters family, file-based SDS, and the still-blocked gRPC family. Read-only reconnaissance established the three §0 findings: per-route config is absent (`Route` `bootstrap.rs:1152-1158` et al., grep-empty); the HCM runs the filter pipeline BEFORE route resolution (`crates/envoy-http1/src/hcm.rs:612` vs `:1180-1202`); and the route-early-resolution change is regression-safe (no existing filter reads the route → all 30 fixtures stay green, the foundation-slice property). Landed `SPEC.md` (D1 per-route `typed_per_filter_config` schema + `PerFilterConfig`/`CorsPolicy` + D2 the HCM route-early-resolution+threading [H1+H2] + D4 `CorsFilter` [decode preflight short-circuit + encode actual-request decoration, the first encode-active feature filter] + D7 `cors.{origin_valid,origin_invalid}` stats + fixture `0031-http-filter-cors` [4 deterministic header-only probes]) + ADR-0057 (the scoping ADR) + ROADMAP row `23` (`planned`) + this STATE advance + the STATE_HISTORY relocations. **§6.1: projected single-phase (~1100–1500 LoC / ~10–13 tasks; the D2 cross-cutting HCM change the swing factor); split reserved as 23.1 [per-route-config schema + HCM threading, regression-safe foundation slice] / 23.2 [CorsFilter + variant + fixture + close], split ADR ADR-0059.** **DECISIONS.md ledger head: ADR-0057** (count 58; next available **ADR-0058**, reserved for the §6.2 reconciliation; **ADR-0059** reserved for the split). ADR-0014 in force; ADR-0028 open. THIS COMMIT is docs-only [CREATE `SPEC.md` + MODIFY `ROADMAP.md` + `DECISIONS.md` + `STATE.md` + `STATE_HISTORY.md`]; no code/test/fixture/Cargo/BEHAVIOR_CONTRACT change; no `unsafe`. Docs-only → CI vacuous-green (the project's differential evidence remains the phase-22 state-4 CI anchor `27179560746` at code-HEAD `04b9e97c4`). Per §5.1 the phase-23 state-2 PLAN-write is the NEXT session.)

> Historical `## Last updated` notes — every superseded last-updated note (all closed phases + the active phase's prior sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: one state per session; do not chain states. State-6 close-out commits (this commit) touch ROADMAP.md + STATE.md only and carry no code changes; the next session writes PLAN.md for the next active phase per `superpowers:writing-plans`.
- The reviewer's R2 disposition decision (option (a) retroactive split of 05.1 vs option (b) free-standing post-05.1 sub-phase) was settled at the 05.1 state-6 commit in favor of option (b); 05.4 is the chosen sibling sub-phase. Future-reviewers reading STATE.md should understand that 05.1 is structurally closed at the preamble landing; 05.4 is a SIBLING under parent-05, not a child of 05.1; and the execution order ran 05.1 → 05.4 → 05.2 → 05.3, with 05.3 the closing sub-phase that flips parent-05 to `done`.

> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Phase-23 state-1 brainstorm

The next-NEW-PHASE brainstorm (`superpowers:brainstorming`) at HEAD `bd3cfd644` (phase-22 state-6 close-out; ROADMAP rows `00`–`22` all `done`). Picked + scoped phase `23` on the merits across all §9 feature-families.

**Family pick — HTTP filters (the richest remaining §9 vein, ≈13 filters undone).** After 09 `local_ratelimit` / 10 `rbac` / 11 `fault` / 22 `jwt_authn`, the HTTP-filters family remains the deepest open vein. The decisive consideration: the per-route `typed_per_filter_config` infrastructure — deferred at phase 22 (ADR-0055 named `cors` "the natural close site") — is the single highest-leverage unlock in the family, gating `cors` AND `csrf` AND `buffer` AND per-route overrides of all four existing filters. Building it now (with `cors` as the testable first consumer) is strictly higher ROI than extending one filter.

**Filter pick — `cors`.** Fully differential-testable with zero timing/crypto sensitivity (CORS is a pure function of request method + `Origin` + the policy — header-only, no body modification, no clock), and the canonical first consumer of the per-route-config infra. The §6.3 anti-pattern forbids landing the infra without a tested consumer; `cors` is exactly that consumer.

**Three §0 reconnaissance findings (a read-only `Explore` subagent + controller cross-check at HEAD):** (1) **CORS is genuinely per-route in modern Envoy** — the filter-chain `Cors` entry is near-empty; the `CorsPolicy` is attached per-route via `typed_per_filter_config`, which does NOT exist in envoy-config (`Route` `bootstrap.rs:1152-1158`, `VirtualHost` `:1141-1149`, `RouteConfiguration` `:1124-1137` carry zero per-filter config; grep-empty workspace-wide). (2) **The HCM runs the filter pipeline BEFORE it resolves the matched route** (H1 `pipeline.decode_headers` at `crates/envoy-http1/src/hcm.rs:612` vs `build_response` route matching at `:1180-1202`; H2 at `crates/envoy-http2/src/hcm.rs:465-486`) — so a per-route-config filter cannot see its route at decode time today; phase 23 must resolve the route up-front and thread it + its per-filter config into the pipeline. (3) **That route-early-resolution change is regression-safe** — no existing filter reads the route (Router terminus decode no-op `crates/envoy-filter/src/router.rs:28-34`; `build_response` reuses the resolved route for identical dispatch), so all 30 existing fixtures stay green (the 05.1/07.1/12.1/14.1 foundation-slice property). This is the cross-cutting lift ADR-0055 warned about when deferring cors — real, but bounded and regression-safe.

**Alternatives rejected (full analysis in ADR-0057):** the jwt_authn requirement-combinators follow-on (lower leverage — extends one filter); `compression` (gzip foundations-grant + non-byte-exact zlib bodies); the load-balancing family (distribution/hash-exact testing + the missing distinguishable-backend harness); xDS file-watching (deferred a SIXTH time on the three stacked ADR-0050 risks — mutability refactor + timing + Linux-CI-only §6.2); the network-filters family (large stateful protocol parsers, or trivial low-value filters); file-based SDS (low value); the still-blocked gRPC family (H2 trailers).

**Scope locked by ADR-0057 (minimum-viable; SPEC §3/§4).** Delivers: D1 the per-route `typed_per_filter_config` schema (`Route` field + `@type`-tagged `PerFilterConfig` enum [only `Cors` registered] + `CorsPolicy` reusing 04.x `StringMatcher`); D2 the HCM route-early-resolution + threading (H1 + H2, inert for non-CORS filters); D4 `CorsFilter` (decode preflight short-circuit + encode actual-request decoration — the first encode-active feature filter); D7 conditional `cors.{origin_valid,origin_invalid}` stats; fixture `0031-http-filter-cors` (H1; 4 deterministic probes); a new `parse_bootstrap` fuzz seed (no new fuzz target); an in-process backstop; BEHAVIOR_CONTRACT cors rows. Defers: `filter_enabled`/`shadow_enabled` runtime gating, `allow_private_network_access`, vhost-level config + the route>vhost cascade, `typed_per_filter_config` for filters other than cors, the deprecated inline `route.cors` field, gRPC-Web `bypass_cors_preflight`.

**§6.1 split-gate.** Projected single-phase (~1100–1500 LoC / ~10–13 tasks — at the upper edge of the ~1500-LoC gate, the D2 cross-cutting HCM change [H1 + H2] the swing factor). The split valve is held in clear reserve as **23.1** (the per-route-config schema + the HCM route-early-resolution + threading — a foundation slice with no new fixture, proven via regression-equivalence on all 30 existing fixtures, regression-safe per finding 3) / **23.2** (the `CorsFilter` + the `HttpFilterInstance` variant + stats + fixture 0031 + backstop + parent-23 close). The PLAN-writer decides AFTER the §6.2 verification materializes the D2 blast radius; the split ADR would be ADR-0059.

**ADR posture.** **ADR-0057** lands at THIS brainstorm commit (the scoping + per-route-config-infrastructure decision). **ADR-0058** is reserved for the state-2 §6.2 empirical-verification reconciliation (most-likely trigger: the preflight status/headers or the allow-origin echo-vs-`*`). **ADR-0059** is reserved for the §6.1 split. DECISIONS.md ledger head ADR-0057 (count 58; next available ADR-0058). ADR-0014 in force; ADR-0028 open.
