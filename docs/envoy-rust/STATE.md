# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase

**id:** `36` — `36-rbac-matcher-value-enrichment` (**Observability / HTTP-filters family: RBAC matcher-value enrichment** — the `present_match` `ValueMatcher` variant on the phase-35 RBAC `metadata` condition [F1] + `safe_regex` `StringMatcher` compilation on the RBAC path [header + metadata values], closing carry-forward M35-1 [F2] + fixture `0044`). Lifecycle **state-2 PLAN-WRITE COMPLETE / state-3-next** (`SPEC.md` + `PLAN.md` present; NO `PROGRESS.md` yet). The next session runs the state-3 implementation (`superpowers:subagent-driven-development`) — see `## Next expected skill`.
**slug:** `36-rbac-matcher-value-enrichment`
**directory:** `docs/envoy-rust/phases/36-rbac-matcher-value-enrichment/` (`SPEC.md` + `PLAN.md` present; `PROGRESS.md`/`REVIEW.md` not yet)

**status:** **PHASE 36 (`36-rbac-matcher-value-enrichment`) — STATE-2 PLAN-WRITE COMPLETE.** This session (the §5 state-2 PLAN-write, `superpowers:writing-plans`) ran the **§6.2 LOCAL empirical reconnaissance** against live `envoyproxy/envoy:v1.33.0` (an H1 `direct_response` listener + a `[header_to_metadata, rbac, router]` chain; 9 RBAC `rules:` variants — runtime `curl` probes for `present_match: true`/`false`, a `metadata` `string_match.safe_regex` AND a `Permission::Header` `safe_regex_match`, anchoring-boundary probes; `--mode validate` for the non-string `ValueMatcher` oneof keys + a malformed `safe_regex`; `/config_dump` round-trip; bytes via `od -c`; all containers torn down), fired **ADR-0088** (the §6.2 reconciliation), authored `PLAN.md` (7 TDD tasks, plan-reviewed by a fresh subagent → ✅ Approved, advisory Task-5 recommendations folded), confirmed the **§6.1 split does NOT fire** (~7 tasks / ~400–800 LoC), and advanced STATE `36` state-1-complete/state-2-next → state-2-complete/state-3-next (relocating the state-1 four top-section narratives to STATE_HISTORY.md per ADR-0035). **§6.2 RESULT — STRONG differential target FIRES** (both F1 `present_match` and F2 `safe_regex` are byte-identical cross-proxy), with **TWO MATERIAL divergences** recorded by ADR-0088: (i) the `present_match` semantics are **`match = present && want`** — `present_match: false` NEVER matches — NOT the SPEC's projected `present == want` and NOT the `HeaderMatcherMode::PresentMatch` `want ? present : true` precedent (the spec-review I-1 foot-gun, resolved empirically); (ii) Envoy `safe_regex` is **RE2 FULL match (anchored)** whereas envoy-rust's `StringMatcher::matches` SafeRegex uses `regex::is_match` = PARTIAL — a pre-existing cross-cutting gap masked by anchored fixtures → fixture `0044` LOCKS an ANCHORED pattern (`^(prod|staging)$`) so partial==full, and the unanchored gap is recorded as **NEW carry-forward M36-1** (deferred, OUT of phase-36 scope). The headline confirmations: `present_match: true`/`false` BOTH accepted + round-trip verbatim; `true` ALLOWS any present `X-Tier` → `200`+`ok\n` / DENIES absent+present-empty → `403`+`RBAC: access denied`(19B); present-but-empty → metadata UNSET → absent (ADR-0084); the `safe_regex` `metadata`-value AND `Permission::Header` paths byte-identical; `null_match`/`bool_match`/`double_match`/`or_match`/`list_match` accepted by Envoy → envoy-rust stays STRICTER boot-fatal; a malformed RBAC `safe_regex` BOOT-FATAL (ADR-0049). **Implementation surface (locked in PLAN §A):** F1 — a `PresentMatch(bool)` arm on the `ValueMatcher` enum (`bootstrap.rs`) + `Deserialize`/`Serialize`/`KEYS` + a presence-aware `eval_metadata` / `ValueMatcher::matches_resolved` (`present && want`); F2 — public `HeaderMatcher`/`ValueMatcher::compile_safe_regexes` helpers + **fallible** `lower_permission`/`lower_principal` compiling RBAC `SafeRegex` at lowering time (closing M35-1's latent panic), threaded through the already-fallible `build_from_config` so a bad regex is BOOT-fatal; NO new `ConfigError`/`FilterError` variant. Fixture `0044` (F1 present/absent + F2 regex-match/miss probe pairs) + an in-process backstop (incl. the header-`safe_regex` panic-regression guard + `present_match: false` never-matches) + 2 `parse_bootstrap` seeds + a BEHAVIOR_CONTRACT "Phase 36" extension. **Two existing in-code tests repurposed:** `rbac_metadata_rejects_present_match_value` FLIPPED to ACCEPT; `rbac_metadata_value_safe_regex_is_parse_accepted` updated (M35-1 limitation closed). **DECISIONS.md ledger head: ADR-0088** (count 88; next-available **ADR-0089**, reserved/unused for the split). ADR-0014 in force; ADR-0028 open; ADR-0049 governs config-validity. **ROADMAP rows `00`-`35` `done`; `36` `in-progress`.** The next session runs the state-3 implementation — see `## Next expected skill`.

> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases + the active phase's prior sub-state pointers, incl. the phase-25 state-1 brainstorm pointer) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill

Per `BOOTSTRAP_PROMPT.md` §5 state 3 + `SKILL_ROUTING.md`: phase `36` (`36-rbac-matcher-value-enrichment`) has a `SPEC.md` AND a `PLAN.md` (implementation incomplete — no `PROGRESS.md`) → the next session runs the **state-3 implementation** (`superpowers:subagent-driven-development`, TDD per `superpowers:test-driven-development` on every task; append to `PROGRESS.md` per task). Execute `PLAN.md`'s **7 TDD tasks** in order (the §A facts are empirically locked by ADR-0088 — do NOT re-derive): T1 F1 config `present_match` `ValueMatcher` variant + `matches_resolved` (flip `rbac_metadata_rejects_present_match_value`; T1 touches BOTH `bootstrap.rs` + `matcher.rs` to stay exhaustive) / T2 F1 presence-aware `eval_metadata` (`present && want`) / T3 F2 public `compile_safe_regexes` helpers (repurpose `rbac_metadata_value_safe_regex_is_parse_accepted`) / T4 F2 fallible RBAC lowering compiling `SafeRegex` (closes M35-1; the header-`safe_regex` panic-regression guard MUST fail on the pre-fix tree) / T5 fixture `0044` differential (F1 present/absent + F2 regex-match/miss, ANCHORED `^(prod|staging)$`) / T6 `parse_bootstrap` seeds (NO new target) / T7 BEHAVIOR_CONTRACT + regression sweep. **Sequencing:** the natural per-commit fold is T1+T2 (F1) and T3+T4 (F2). **REBUILD `envoy-bin` before the T5 differential** (stale-binary `unknown filter`/`unknown field` — the phase-33/34 lesson). **PUSH per-session from state-3 onward + confirm CI green** (do NOT accumulate unpushed code commits — the phase-33 lesson). The §7.5 phase-done gate is the state-4 concern (after state-3). The SPEC (scope locked by **ADR-0087**) + the PLAN (§A locked by **ADR-0088**) were authored at state-1/state-2; the plan was reviewed by a fresh subagent (✅ Approved, Task-5 advisory recommendations folded).

**Open carry-forward Minors (fold into the phase that next touches each surface; NONE blocks):**
- **M35-1** — **SCHEDULED CONSUMED by phase 36 (F2):** the SafeRegex-in-RBAC-values latent panic — a `safe_regex` inside an RBAC `metadata`/`header` matcher `value` is config-load-accepted but NOT compiled (the RBAC validation path is an immutable borrow; only the route-config walk compiles header SafeRegex) → a runtime `StringMatcher::matches`/header match on a SafeRegex value PANICS (`matcher.rs:90` `.expect(...)`). Phase 36 F2 compiles it at `rbac.rs` lowering time (both `header` + `metadata`) + repurposes the in-code test `rbac_metadata_value_safe_regex_is_parse_accepted`; the PLAN's T4 fully specifies the fix (fallible RBAC lowering compiles `SafeRegex` + the header-`safe_regex` panic-regression guard); marked CONSUMED at the phase-36 close.
- **M36-1** (NEW, phase-36 §6.2) — envoy-rust's `StringMatcher::matches` SafeRegex uses `regex::is_match` (PARTIAL/substring) but Envoy `safe_regex` is RE2 FULL match (anchored): UNANCHORED patterns diverge (e.g. `is_match("prod|staging", "staging-2")` is `true` but Envoy denies). PRE-EXISTING (phase 04.2), cross-cutting (the route-config header SafeRegex shares the path), masked by anchored fixtures. Phase 36 stays clear by LOCKING an anchored `^(prod|staging)$` pattern (partial==full); a proper full-match fix (`matcher.rs` SafeRegex anchoring) is DEFERRED to its own future phase. DISTINCT from M35-1.
- **M34-1** (NEW) — no unit test locks the same-namespace/key last-write-wins overwrite in `crates/envoy-filter/src/header_to_metadata.rs` (`multi_rule_composes` only covers distinct namespaces). Cheap test addition.
- **M34-2** (NEW) — the A2 default-namespace (`envoy.filters.http.header_to_metadata`) is exercised only at the config-parse layer (`header_to_metadata_default_namespace_is_filter_name`), not at the filter-execution layer. A filter-execution unit test from a defaulted `metadata_namespace` would close the loop.
- **M34-3** (NEW) — cosmetics: the T5 redundant function-scope `use tempfile::tempdir` (`crates/envoy-http1/src/hcm.rs`); the BEHAVIOR_CONTRACT `A-missing`-vs-`A5` heading-numbering quirk; the fixture README `generate_request_id … load-bearing` overstatement.
- **M33-1** — unnecessary `.clone()` at `crates/envoy-http1/src/hcm.rs:1211` (the H1 record-build `dynamic_metadata` local is single-use/last-use → could move, as the H2 path does). Fold when the H1 HCM record-build is next touched. **M33-2** (doc-pointer line drift in `command_operator.rs`/`record.rs`) likewise — doc-only.
- the empty-`metadata_match`→fallback doc-comment (`crates/envoy-cluster/src/subset.rs`); M29-1/M29-2 + M30-1; M30-2 (`Cluster.lb_policy` serde-default); the phase-31 M-2/M-3; the HTTP-filters-family (1)-(4) buffer carry-forwards. (The 6 M32 carry-forwards are CONSUMED.)

**Phase 36 is at state-2 (docs-only: PLAN + ADR-0088 + STATE advance) — NO code yet, nothing to verify (the §7.5 gate is the state-4 concern after the state-3 implementation). Phases 35 (`28125206968` @ `d1cf8a3`, state-5 REVIEW.md approved `28131626377` @ `e8e463f`), 34 (`28062068794` @ `7c19803`) and 33 (`28021495767` @ `72fe40d`) are all closed + CI-GREEN — do NOT re-verify.** Per the phase-33 lesson, the state-2 PLAN-write docs commit MAY stay UNPUSHED (the push happens when code lands at state-3) OR push for green-on-main discipline. The phase-36 §6.2 reconnaissance ran LOCALLY against `envoyproxy/envoy:v1.33.0` this session (all containers torn down). The local Docker-differential false-REDs (`admin_config_dump_server_info` bridge-IP `192.168.65.2`; `http_filter_fault_fixture` H2-preface parallel-load timing — both PASS in isolation) are documented host-only, NOT regressions; CI is authoritative.

> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases + the active phase's prior sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit

**Phase-36 state-2 PLAN-write — §6.2 reconnaissance + ADR-0088 + PLAN.md + STATE advance (THIS commit):** the §5 state-2 PLAN-write (`superpowers:writing-plans`). Ran the **§6.2 LOCAL empirical reconnaissance** against live `envoyproxy/envoy:v1.33.0` (H1 `direct_response` + `[header_to_metadata, rbac, router]`; runtime + `--mode validate` + `/config_dump` probes; bytes via `od -c`; containers torn down), fired **ADR-0088** (the §6.2 reconciliation — TWO MATERIAL divergences: the `present_match` `match = present && want` semantics [`false` never matches]; Envoy `safe_regex` RE2-full-match vs envoy-rust `is_match`-partial → fixture LOCKS an anchored pattern + NEW carry-forward M36-1), authored `docs/envoy-rust/phases/36-rbac-matcher-value-enrichment/PLAN.md` (7 TDD tasks, plan-reviewed by a fresh subagent → ✅ Approved, Task-5 advisory recommendations folded), confirmed the **§6.1 split does NOT fire** (~7 tasks / ~400–800 LoC; ADR-0089 reserved/unused), and advanced STATE `36` state-1-complete/state-2-next → state-2-complete/state-3-next (relocating the phase-36 state-1 four top-section narratives to STATE_HISTORY.md per ADR-0035 / §4.1 inv. 9; renaming the `### Phase-36 state-1 brainstorm` Notes subsection to `### Phase-36 state-2 PLAN-write`). **THIS docs-only commit** (PLAN + DECISIONS + STATE + STATE_HISTORY). NO code change; `#![forbid(unsafe_code)]` unaffected. **DECISIONS.md ledger head: ADR-0088** (count 88; next-available **ADR-0089**). ADR-0014 in force; ADR-0028 open. **ROADMAP rows `00`-`35` `done`; `36` `in-progress`.** The next session is the state-3 implementation (`superpowers:subagent-driven-development`), which executes the 7 PLAN tasks.

> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases + the active phase's prior sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated

2026-06-25 (phase-36 **STATE-2 PLAN-WRITE COMPLETE**. Ran the **§6.2 LOCAL empirical reconnaissance** against live `envoyproxy/envoy:v1.33.0` [H1 `direct_response` + `[header_to_metadata, rbac, router]`; runtime `curl` probes for `present_match: true`/`false` + a `metadata`/`header` `safe_regex` + anchoring-boundary values; `--mode validate` for the non-string `ValueMatcher` keys + a malformed regex; `/config_dump` round-trip; `od -c`; containers torn down], fired **ADR-0088** [§6.2 reconciliation — TWO MATERIAL divergences: (i) `present_match` semantics `match = present && want` (`false` NEVER matches — NOT `present == want`, NOT the header-matcher precedent); (ii) Envoy `safe_regex` RE2-FULL-match vs envoy-rust `is_match`-PARTIAL → fixture `0044` LOCKS an ANCHORED pattern `^(prod|staging)$` so partial==full + NEW carry-forward **M36-1** (unanchored partial-vs-full, deferred, cross-cutting); plus confirmations: present-empty→absent; `safe_regex` metadata+header byte-identical; non-string `ValueMatcher` keys stay boot-fatal (stricter); malformed RBAC regex boot-fatal], authored `PLAN.md` [7 TDD tasks — F1 `present_match` variant + `matches_resolved` + presence-aware `eval_metadata`; F2 public `compile_safe_regexes` + fallible `lower_*` compiling RBAC `SafeRegex` (closes M35-1) + boot-fatal bad regex; fixture `0044`; seeds; BEHAVIOR_CONTRACT], plan-reviewed → ✅ Approved [Task-5 advisory recommendations folded], confirmed §6.1 split NOT fired [~7 tasks; ADR-0089 reserved/unused], advanced STATE [relocating the state-1 four top-section narratives to STATE_HISTORY.md per ADR-0035]. **M35-1 scheduled CONSUMED by F2** [the PLAN's T4 fully specifies the fix]; **M36-1 NEW** [unanchored SafeRegex partial-vs-full]. Older carry-forwards [M34-1/M34-2/M34-3 + M33-1/M33-2 + the empty-`metadata_match` doc-comment + M29-1/M29-2 + M30-1/M30-2 + the phase-31 cosmetics + the HTTP-filters-family (1)-(4)] remain live. **ROADMAP rows `00`-`35` `done`; `36` `in-progress`.** Docs-only; NO code change. **DECISIONS.md ledger head: ADR-0088** [count 88; next-available ADR-0089]. ADR-0014 in force; ADR-0028 open. The next session is the state-3 implementation [`superpowers:subagent-driven-development`].)

> Historical `## Last updated` notes — every superseded last-updated note (all closed phases + the active phase's prior sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: one state per session; do not chain states. State-6 close-out commits touch ROADMAP.md + STATE.md only and carry no code changes; the next session writes PLAN.md for the next active phase per `superpowers:writing-plans`.
- The reviewer's R2 disposition decision (option (a) retroactive split of 05.1 vs option (b) free-standing post-05.1 sub-phase) was settled at the 05.1 state-6 commit in favor of option (b); 05.4 is the chosen sibling sub-phase. Future-reviewers reading STATE.md should understand that 05.1 is structurally closed at the preamble landing; 05.4 is a SIBLING under parent-05, not a child of 05.1; and the execution order ran 05.1 → 05.4 → 05.2 → 05.3, with 05.3 the closing sub-phase that flips parent-05 to `done`.

### Phase-36 state-2 PLAN-write (active phase)

- **What landed:** the §5 state-2 PLAN-write (`superpowers:writing-plans`). Ran the §6.2 LOCAL empirical reconnaissance against live `envoyproxy/envoy:v1.33.0`, fired ADR-0088 (the §6.2 reconciliation), authored `PLAN.md` (7 TDD tasks), plan-reviewed by a fresh subagent (✅ Approved; Task-5 advisory recommendations folded), confirmed the §6.1 split does NOT fire (~7 tasks / ~400–800 LoC; ADR-0089 reserved/unused).
- **§6.2 locked facts (ADR-0088 §A; do NOT re-derive):** (A1) F1 `present_match` semantics = **`match = present && want`** — `present_match: false` NEVER matches (DIVERGES from the SPEC's `present == want` AND the `HeaderMatcherMode::PresentMatch` `want ? present : true` precedent — the spec-review I-1 foot-gun, resolved empirically); (A2) present-but-empty `X-Tier` → metadata UNSET → absent → `present_match: true` denies; `200`+`ok\n` / `403`+`RBAC: access denied`(19B) byte-exact; (A3) `safe_regex` on `metadata` value AND `Permission::Header` byte-identical; (A3b) Envoy `safe_regex` = RE2 FULL match (anchored), envoy-rust `is_match` = PARTIAL → fixture LOCKS `^(prod|staging)$` (partial==full); (A4) malformed RBAC regex BOOT-fatal via fallible lowering; (A5) non-string `ValueMatcher` keys stay boot-fatal (stricter than Envoy).
- **Implementation surface (PLAN §A / 7 tasks):** F1 — `PresentMatch(bool)` `ValueMatcher` arm + `matches_resolved` (presence-aware) + `eval_metadata` restructure; F2 — public `HeaderMatcher`/`ValueMatcher::compile_safe_regexes` + fallible `lower_permission`/`lower_principal` compiling RBAC `SafeRegex` at lowering (closes M35-1's panic) threaded through `build_from_config` (boot-fatal bad regex); fixture `0044` + backstop (incl. header-`safe_regex` panic-regression guard) + 2 `parse_bootstrap` seeds + BEHAVIOR_CONTRACT. Two existing in-code tests repurposed (`rbac_metadata_rejects_present_match_value` flipped to accept; `rbac_metadata_value_safe_regex_is_parse_accepted` updated).
- **Carry-forwards:** M35-1 scheduled CONSUMED by F2 (PLAN T4); **M36-1 NEW** (unanchored SafeRegex partial-vs-full, deferred, cross-cutting). No new `HttpFilterInstance` variant / crate / dependency / fuzz-target / `ConfigError`/`FilterError` variant.
- **Next:** state-3 implementation (`superpowers:subagent-driven-development`) executes the 7 PLAN tasks; the §7.5 gate is the state-4 concern.

> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) and for parent phase 25 (the `### Phase-25 state-1 brainstorm` pick + recon-finding narrative, relocated at the 25.2 state-6 close-out when parent `25` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 26 (the `### Phase-26 state-1 brainstorm` pivot/rejected-alternatives/key-scoping narrative, relocated at the phase-26 state-6 close-out when row `26` flipped to `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 27 (the `### Phase-27 state-1 brainstorm` / `### Phase-27 state-2 PLAN-write` / `### Phase-27 state-4 verification` narratives + the now-consumed `### Phase-27 carry-forwards` [M26-1..M26-8] block, relocated at the phase-27 state-6 close-out when row `27` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 28 (the `### Phase-28 state-1 brainstorm` / `### Phase-28 state-2 PLAN-write` / `### Phase-28 state-3 implementation + state-4 verification` / `### Phase-28 state-5 code review` narratives + the now-consumed `### Phase-28 carry-forwards` [M27-1..M27-3] block, relocated at the phase-28 state-6 close-out when row `28` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 29 (the now-consumed `### Phase-29 carry-forwards` [M28-1..M28-3] block + the `### Phase-29 state-1 brainstorm` / `### Phase-29 state-2 PLAN-write` / `### Phase-29 state-3 implementation` / `### Phase-29 state-4 verification` / `### Phase-29 state-5 code review` narratives, relocated at the phase-29 state-6 close-out when row `29` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 30 (the `### Phase-30 carry-forwards` [M29-1/M29-2, which fed phase 30 but were NOT consumed — they continue as phase-31 carry-forwards] block + the `### Phase-30 state-1 brainstorm` / `### Phase-30 state-2 PLAN-write` / `### Phase-30 state-3 implementation` / `### Phase-30 state-4 verification` / `### Phase-30 state-5 code review` narratives, relocated at the phase-30 state-6 close-out when row `30` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 31 (the `### Phase-31 carry-forwards` [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 — open Minors from the phase-30 REVIEW.md that fed phase 31 but were NOT consumed; they continue as carry-forwards for the next phase that touches the differential hash-sweep driver / the config parser] block + the `### Phase-31 state-1 brainstorm` / `### Phase-31 state-2 PLAN-write` / `### Phase-31 state-3 implementation` / `### Phase-31 state-4 verification` / `### Phase-31 state-5 code review` narratives, relocated at the phase-31 state-6 close-out when row `31` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 32 (the `### Phase-32 carry-forwards` block — the open Minors that fed phase 32 but were NOT consumed: the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3; they continue as carry-forwards for the future phase that touches their surface — re-listed in `## Next expected skill` above alongside the 6 new phase-32 REVIEW.md Minors M32-1…M32-6), relocated at the phase-32 state-6 close-out when row `32` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phases 33+34 (the `### Phase-33 carry-forwards` block — the active-phase carry-forwards Notes that lived through phases 33 and 34: the now-CONSUMED 6 phase-32 REVIEW.md Minors M32-1…M32-6 [folded + landed at the phase-33 state-3] + the "Other still-live carry-forwards" list [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3 + the HTTP-filters-family (1)-(4) — those still-live ones re-listed in `## Next expected skill` above alongside the new phase-34 Minors M34-1/M34-2/M34-3 + the phase-33 M33-1/M33-2]), relocated at the phase-34 state-6 close-out when row `34` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 35 (the `### Phase-35 state-1 brainstorm` / `### Phase-35 state-2 PLAN-write` / `### Phase-35 state-3 implementation` / `### Phase-35 state-4 verification` / `### Phase-35 state-5 code-review` narratives, relocated at the phase-35 state-6 close-out when ROADMAP row `35` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### HTTP-filters-family carry-forwards (from the `25.2` REVIEW.md - NOT yet consumed; weigh whenever the HTTP-filters family is re-entered)

> These were never obligations on the xDS phase 26; they remain live for whenever an HTTP-filters-family phase resumes.

- **(1) [non-goal - architectural]** Over-limit request bodies are FULLY buffered before the 413 rejection (no streaming watermark). Documented deferred non-goal; differentially byte-identical to Envoy for the bounded fixture sizes. Revisit only if a streaming `decode_data` watermark path is ever planned.
- **(2) [doc precision]** The BEHAVIOR_CONTRACT 413-row "verified byte-exact against v1.33.0" phrasing - fixture `0033` is H1-only; the H2 over-limit path is covered by the in-process synth-decorator backstop, NOT differentially. Consider narrowing the phrasing if an H2 over-limit fixture is ever added.
- **(3) [coverage]** No standalone `== effective route limit` unit assertion (the boundary is exercised only via the over/under probes).
- **(4) [coverage]** No differential at-limit (`==`) probe in `0033` (within-limit `<` and over-limit `>` are both covered; the exact boundary is not differentially probed).
- _(2)-(4) are cheap polish, (1) is architectural and only relevant to a future streaming phase._
