# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase

**id:** `25.1` — `25.1-h1-request-body-forwarding` (Part A of parent phase `25` `envoy.filters.http.buffer` — the H1 request-body-forwarding FOUNDATION SLICE; parent `25` was SPLIT into `25.1` + `25.2` per ADR-0064), lifecycle **state-4-complete / state-5-next**. ROADMAP rows `00`–`24` are all `done`; parent row `25` is `in-progress` (sub-phases `25.1, 25.2`); row `25.1` is `in-progress`; row `25.2` is `planned`.
**slug:** `25.1-h1-request-body-forwarding`
**directory:** `docs/envoy-rust/phases/25.1-h1-request-body-forwarding/` — carries **`SPEC.md` + `PLAN.md` + `PROGRESS.md`** (PROGRESS now records the state-3 close-out AND the `## State-4 verification` §7.5 evidence; `REVIEW.md` does NOT exist yet — it is the state-5 output, authored NEXT session). The parent dir `docs/envoy-rust/phases/25-http-filter-buffer/` carries the parent `SPEC.md`; the sibling `docs/envoy-rust/phases/25.2-http-filter-buffer/` carries `25.2`'s `SPEC.md` only (its `PLAN.md` is written AFTER `25.1` closes). The five most-recently-closed phase directories — 24 `24-http-filter-csrf`, 23 `23-http-filter-cors`, 22 `22-http-filter-jwt-authn`, 21 `21-xds-file-based-eds`, 20 — each remain CLOSED carrying all 4 artifacts.

**status:** **PHASE 25.1 (`25.1-h1-request-body-forwarding`) STATE-4 §7.5 PHASE-DONE VERIFICATION GATE COMPLETE — ALL GREEN at THIS commit** (lifecycle state-4-complete / state-5-next). This session ran `superpowers:verification-before-completion` over the full §7.5 gate at code-HEAD `ca023ce48` (the state-3 `hcm.rs` change landed in ancestor `20ce64010`). **Local gates GREEN:** `cargo fmt --all -- --check` (clean); `cargo clippy --workspace --all-targets --all-features -- -D warnings` (0 warnings); `cargo build --workspace --all-targets` + `cargo build -p envoy-http1` isolated (`project_isolated_crate_build_blindspot`); `cargo deny check` (`advisories ok, bans ok, licenses ok, sources ok`); `cargo test -p envoy-http1` (the changed crate, **110 passed; 0 failed** — incl. `h1_forwards_request_body_upstream` + the 4 Task-2 regression tests). **Differential GREEN — run LOCALLY per `feedback_state4_runs_docker_differential`** (Docker 28.0.4 + `envoyproxy/envoy:v1.33.0`; pre-built `--no-run` first so the Docker run raced no cargo build): `cargo test -p differential -p h2spec-conformance` (**exit 0, zero failures**; the differential unit suite 136/0/2; all 32 pre-existing Docker-gated fixtures `0001`–`0032` `1 passed; 0 failed` SIMULTANEOUSLY [the load-bearing regression-equivalence invariant — the relocated H1 body read is a body-wise no-op for every bodyless / `content-length: 0` fixture]; `h2spec_pass_rate_gate ... ok` ≥95%; `tcp_proxy_backend_*` passed — no readiness flake this run). `25.1` adds NO new fuzz target (it forwards client bytes); the existing `parse_bootstrap` + `jwt_parse` fuzzers ran GREEN on CI. **The local `cargo test -p envoy-bin` backstops emitted 11 passing blocks then hit the known nested-cargo backstop hang (`project_workspace_test_nested_cargo_backstop_flake`) — NOT a regression; envoy-bin is authoritatively GREEN under `cargo test --workspace` on Linux CI.** **AUTHORITATIVE Linux CI anchor (ADR-0049 Provenance): run `27476243121` at code-HEAD `ca023ce48`** — `ci.yml` runs the complete §7.5 gate (fmt + clippy + build + `cargo test --workspace` incl. the Docker differential + h2spec + deny + both fuzzers), `completed success`, both jobs green; the differential genuinely engaged Docker (every fixture integration test `1 passed; 0 failed`). **This FRESH anchor SUPERSEDES the stale `27457698815`** (code-HEAD `9b0e7b925`, predating the `25.1` change) as the differential evidence of record; the local differential corroborates, the CI run is authoritative. NO new fixture is added by `25.1`; the body-forwarding capability is verified by the in-process `h1_forwards_request_body_upstream` test and is DIFFERENTIALLY proven later by `25.2`'s fixture `0033`. **DECISIONS.md ledger head: ADR-0064** (count 65; next available **ADR-0065** — a state-4 VERIFICATION session opens no ADR, as projected; none was needed). ADR-0014 in force; ADR-0028 open. **THIS COMMIT touches docs only** (PROGRESS state-4 evidence + STATE advance + STATE_HISTORY relocation); NO production/test/fixture/Cargo change; no `unsafe`. Per §5.1 (one state per session) this session EXITS after this STATE advance; the NEXT session runs `25.1`'s state-5 code review (`superpowers:requesting-code-review`, producing `REVIEW.md`).

> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases + the active phase's prior sub-state pointers, incl. the phase-25 state-1 brainstorm pointer) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill

Per `BOOTSTRAP_PROMPT.md` §5 state 5 + `SKILL_ROUTING.md`: with `25.1`'s state-4 §7.5 verification gate GREEN (all 32 existing Docker-gated fixtures `0001`–`0032` green simultaneously — local + the AUTHORITATIVE Linux CI anchor `27476243121`; `cargo build`/`clippy`/`fmt`/`test`/`deny` clean; h2spec ≥95%; no new fuzz target), the next session runs **`superpowers:requesting-code-review`** over the `25.1` state-3 implementation (the `crates/envoy-http1/src/hcm.rs` body-read relocation + per-attempt `out_req.body = req.body.clone()` forward + discard-drain removal + the regression tests; commit chain `20ce64010`…`0c8b66512`), producing `REVIEW.md`. Per §5.2, if `REVIEW.md` surfaces a Critical or Important finding the phase re-enters state 3 (NOT 4); otherwise it advances to the state-6 deterministic close-out (flip ROADMAP row `25.1` → `done`, then `25.2` planning). Per §5.1 the state-5 code review is the NEXT session's single state.

> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases + the active phase's prior sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit

**Phase-25.1 state-4 verification — STATE-advance commit (THIS commit):** the bookkeeping commit recording the `25.1` state-4 §7.5 phase-done verification evidence and advancing STATE to state-4-complete / state-5-next. The full gate ran GREEN at code-HEAD `ca023ce48`: static gates + isolated-crate build + `cargo deny check` + the changed-crate `envoy-http1` 110/110 (all LOCAL); the Docker 32-fixture differential + h2spec run LOCALLY per `feedback_state4_runs_docker_differential` (`cargo test -p differential -p h2spec-conformance`, exit 0, zero failures, all 32 fixtures `0001`–`0032` green simultaneously, h2spec ≥95%); AUTHORITATIVE Linux CI anchor `27476243121` at the same code-HEAD (full `ci.yml` §7.5 gate green), superseding the stale `27457698815`. THIS commit appends the state-4 evidence to `25.1`'s `PROGRESS.md` (`## State-4 verification`), advances `STATE.md` (top-pointer → `25.1` state-4-complete / state-5-next + a `### Phase-25.1 state-4 verification` Notes subsection), and relocates the superseded state-3 top-section blocks verbatim to `STATE_HISTORY.md` (ADR-0035 / §4.1 inv. 9). Docs-only; NO production/test/fixture/Cargo change; no `unsafe`. **DECISIONS.md ledger head: ADR-0064** (count 65; next available **ADR-0065**). ADR-0014 in force; ADR-0028 open. Per §5.1 this session EXITS after this commit; the NEXT session runs `25.1`'s state-5 code review.

> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases + the active phase's prior sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated

2026-06-13 (phase-25.1 **state-4 §7.5 verification gate** — ran `superpowers:verification-before-completion` over the full phase-done gate at code-HEAD `ca023ce48`, ALL GREEN: `cargo fmt --all -- --check` clean / `cargo clippy --workspace --all-targets --all-features -- -D warnings` 0 warnings / `cargo build --workspace --all-targets` + isolated `cargo build -p envoy-http1` / `cargo deny check` ok / `cargo test -p envoy-http1` **110-0**; the Docker 32-fixture differential + h2spec run LOCALLY [`cargo test -p differential -p h2spec-conformance`, exit 0, zero failures, all 32 fixtures `0001`–`0032` green simultaneously, `h2spec_pass_rate_gate` ok]; no new fuzz target [`25.1` forwards client bytes]; the local `cargo test -p envoy-bin` backstop hang is the known nested-cargo flake [authoritatively green on CI]. AUTHORITATIVE Linux CI anchor **`27476243121`** at code-HEAD `ca023ce48` [full `ci.yml` §7.5 gate green], superseding the stale `27457698815`. Ledger head **ADR-0064** [count 65; next available **ADR-0065** — a verification session opens no ADR]. ADR-0014 in force; ADR-0028 open. Per §5.1 the `25.1` state-5 code review [`superpowers:requesting-code-review`, producing `REVIEW.md`] is the NEXT session.)

> Historical `## Last updated` notes — every superseded last-updated note (all closed phases + the active phase's prior sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: one state per session; do not chain states. State-6 close-out commits touch ROADMAP.md + STATE.md only and carry no code changes; the next session writes PLAN.md for the next active phase per `superpowers:writing-plans`.
- The reviewer's R2 disposition decision (option (a) retroactive split of 05.1 vs option (b) free-standing post-05.1 sub-phase) was settled at the 05.1 state-6 commit in favor of option (b); 05.4 is the chosen sibling sub-phase. Future-reviewers reading STATE.md should understand that 05.1 is structurally closed at the preamble landing; 05.4 is a SIBLING under parent-05, not a child of 05.1; and the execution order ran 05.1 → 05.4 → 05.2 → 05.3, with 05.3 the closing sub-phase that flips parent-05 to `done`.

> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Phase-25 state-1 brainstorm

- **Pick:** phase `25` = `envoy.filters.http.buffer`, selected at this brainstorm from `MISSION.md`'s §9 HTTP-filters family (the richest remaining vein). The easy header-only HTTP-filter vein is EXHAUSTED — all 7 shipped filters (header_mutation/local_ratelimit/rbac/fault/jwt/cors/csrf) are header-only decode-side decisions and the framework is headers-only (no streaming `decode_data` hook). Buffer is the lightest remaining body-dependent filter (no external service / engine / gzip dep / timing — a pure function of method + body length + policy → deterministic, locally-verifiable, byte-exact differential) AND the THIRD per-route `typed_per_filter_config` consumer (after cors+csrf). Rejected: the LB family (larger/riskier first cut + a new distinguishable-backend harness), the network-filters opener (low leverage), the heavier HTTP filters (external service / engine / non-deterministic), an H2-only buffer (near-cosmetic — exploits H2's incidental pre-buffering, builds no infra), and a pure H1-forwarding phase with buffer deferred (ships a foundation with no consumer). See ADR-0062.

- **Critical recon finding (SPEC §0):** H1 does NOT forward request bodies upstream today — the router forwards an always-empty body (`crates/envoy-http1/src/hcm.rs:356`) and drains+discards the downstream Content-Length body (`:678-697`), a pre-existing gap since phase 04.3; whereas H2 already fully buffers+forwards (`crates/envoy-http2/src/hcm.rs:437-448`). So buffer is NOT a pure plug-in: Part A (now `25.1`) closes the H1 forwarding gap (a regression-equivalence foundation slice), Part B (now `25.2`) adds the filter. The §6.1 split FIRED at the state-2 PLAN-write (ADR-0064).

### Phase-25.1 state-2 PLAN-write

- **§6.2 verification (ADR-0063) RESOLVED** the parent SPEC's wire-shape projections (ran LOCALLY against `envoyproxy/envoy:v1.33.0` via a `go-httpbin` sidecar upstream; transcript preserved in `phases/25.1-h1-request-body-forwarding/PROGRESS.md` Task-1 preamble): the 413 over-limit reply is `Payload Too Large` (17 bytes, no trailing newline) + `content-type: text/plain` via the existing decorate helpers; the reject is strictly `> max_request_bytes`; **Envoy emits NO buffer-scoped stats** (the over-limit 413 is reflected only in the generic HCM `downstream_rq_too_large`) → the SPEC §2.1 stats deliverable is DROPPED; the `Buffer { max_request_bytes }` + `BufferPerRoute` oneof `{ disabled, buffer }` shapes are confirmed; an absent-`buffer` per-route config is accepted-inert by Envoy but the generic `PerRouteConfigForAbsentFilter` validator is reused verbatim. Residual (deferred to the `25.2` PLAN-write): the absent/`0`/malformed `max_request_bytes` disposition was not probed.

- **§6.1 split (ADR-0064) FIRED in risk-isolation mode:** parent `25` SPLIT into `25.1` (Part A — H1 request-body forwarding; the always-active hot-path foundation slice; NO new fixture; all 32 existing fixtures green) + `25.2` (Part B — `BufferFilter` + `BufferPerRoute` + fixture `0033` + parent-25 close). The §6.2-refined estimate (~1050–1200 LoC / ~11 tasks) is UNDER the §6.1 numeric gate, but Part A's always-active nature (more regression-sensitive than the inert foundation slices) warrants the isolated green + reviewed gate. This PLAN-write landed `25.1`'s `PLAN.md` (3 tasks) per the split STOP condition; the `25.2` `PLAN.md` is authored AFTER `25.1` closes.

- **State-3 hand-off:** the next session runs `superpowers:subagent-driven-development` on `25.1`'s `PLAN.md` (Task 1: capturing-upstream test + body-read relocation + forward + remove discard-drain; Task 2: regression invariants; Task 3: BEHAVIOR_CONTRACT note + verification). SERIAL implementer dispatch (`feedback_serial_subagent_dispatch`).

### Phase-25.1 state-3 implementation

- **All 3 PLAN tasks landed** via SERIAL `superpowers:subagent-driven-development` (TDD; two-stage spec-then-quality review per task). Commit chain: `20ce64010` (T1 code) → `60ed197ea` (T1 PROGRESS) → `2fcf8fcee` (T2 code) → `6fcb96e44` (T2 PROGRESS) → `c98dafcf5` (rustfmt fixup) → `2b97667de` (T3 docs) → `0c8b66512` (T3 PROGRESS) → this STATE-advance commit.
- **The change (D1):** in `crates/envoy-http1/src/hcm.rs` the Content-Length request body is now read into a `Bytes` BEFORE the filter pipeline (populating `FilterRequest.body`) and forwarded upstream via `out_req.body = req.body.clone()` per attempt (replay-safe, ADR-0044), replacing the always-empty `Some(Bytes::new())` and the post-response discard-drain. Chunked/streaming bodies remain a non-goal (the 501 preserved); `body_len == 0` (every existing fixture) makes the new path a body-wise no-op → regression-equivalence holds.
- **Two PLAN-name adaptations (both justified, reviewed):** the recording test helper is `spawn_recording_upstream` (the PLAN's `spawn_capturing_upstream` collided with a pre-existing incompatible helper); a `drive_keep_alive` multi-request driver + a `spawn_fail_then_ok_recording_upstream` backend were added test-only (no prior equivalent).
- **State-3 gates ALL GREEN** (isolated-crate + workspace build/clippy/fmt/test). The only `cargo test --workspace` failures were `differential::backend::tcp_proxy_backend_{spawns_and_echoes,drop_terminates_child}` missing a 1 s accept-ready budget under the loaded concurrent run (the readiness-timeout flake family) — both pass 2/2 standalone. Clippy (skipped by per-task state-3 verification per `project_state3_arc_skips_clippy`) and fmt were run at Task 3; fmt needed a fixup of the Task 1-2 test additions (`c98dafcf5`).
- **State-4 hand-off:** the next session runs `superpowers:verification-before-completion` — the full §7.5 gate incl. the Docker 32-fixture differential LOCALLY (`feedback_state4_runs_docker_differential`; slow per `project_local_differential_startup_stall`); NO new fixture (all 32 existing green simultaneously is the acceptance signal).

### Phase-25.1 state-4 verification

- **Full §7.5 phase-done gate GREEN at code-HEAD `ca023ce48`** (the state-3 `hcm.rs` change is its ancestor `20ce64010`). Run via `superpowers:verification-before-completion`; evidence quoted in `phases/25.1-h1-request-body-forwarding/PROGRESS.md` `## State-4 verification`.
- **Local gates:** `cargo fmt --all -- --check` clean; `cargo clippy --workspace --all-targets --all-features -- -D warnings` 0 warnings; `cargo build --workspace --all-targets` + isolated `cargo build -p envoy-http1` ok; `cargo deny check` (`advisories/bans/licenses/sources ok`); `cargo test -p envoy-http1` **110 passed; 0 failed**.
- **Differential LOCALLY** (`feedback_state4_runs_docker_differential`; Docker 28.0.4 + `envoyproxy/envoy:v1.33.0`; pre-built `--no-run` first): `cargo test -p differential -p h2spec-conformance` → **exit 0, zero failures** — differential unit suite 136/0/2, all 32 fixtures `0001`–`0032` `1 passed; 0 failed` SIMULTANEOUSLY (the regression-equivalence invariant; the relocated body read is a no-op for every bodyless / CL:0 fixture), `h2spec_pass_rate_gate ... ok` (≥95%). No startup stall this run; `tcp_proxy_backend_*` passed (no flake).
- **AUTHORITATIVE Linux CI anchor: `27476243121`** at code-HEAD `ca023ce48` — full `ci.yml` §7.5 gate (fmt/clippy/build/`test --workspace` incl. Docker differential + h2spec / deny / 2 fuzzers), `completed success`. SUPERSEDES the stale `27457698815` (was at `9b0e7b925`, pre-`25.1`). The local differential corroborates; CI is authoritative (ADR-0049 Provenance).
- **Known-flake note:** the local `cargo test -p envoy-bin` backstops hung on a nested `cargo run` after 11 passing blocks (`project_workspace_test_nested_cargo_backstop_flake`) — NOT a regression; envoy-bin is green under `cargo test --workspace` on CI.
- **State-5 hand-off:** the next session runs `superpowers:requesting-code-review` over the `25.1` state-3 implementation, producing `REVIEW.md`. NO new ADR opened (verification records evidence, not decisions); ledger head stays ADR-0064.
