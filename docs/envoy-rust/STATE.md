# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase

**id:** `28` - `28-lb-ring-hash` (Load-balancing family OPENER: the **`RING_HASH` consistent-hashing load balancer** — a cluster `lb_policy: RING_HASH` builds a ketama-style hash ring over its endpoints; a request carrying a route-level header `hash_policy` routes to the endpoint the hashed header value maps to [same key → same backend]; chosen as the only DIFFERENTIABLE LB opener — consistent-hashing is deterministic so both proxies must select the SAME backend, unlike random/least_request — and its differential is LOCALLY observable, no reload trigger). Lifecycle **state-4 verification COMPLETE / state-5-next** (`SPEC.md` + `PLAN.md` + `PROGRESS.md` present; `REVIEW.md` absent -> the next step is the state-5 `superpowers:requesting-code-review`; the §7.5 gate (a)-(e) is GREEN at the AUTHORITATIVE Linux CI run `27837455306` @ `4b31144`). The FIRST concrete Load-balancing-family phase (the project shipped only `ROUND_ROBIN` since phase 02) and a NEW family after six consecutive xDS phases (18 CDS / 19 LDS / 20 RDS / 21 EDS / 26 RDS-hot-reload / 27 EDS-hot-reload). ROADMAP rows `00`-`27` are ALL `done`; row `28` is `in-progress` (flips `done` at the state-6 close-out).
**slug:** `28-lb-ring-hash`
**directory:** `docs/envoy-rust/phases/28-lb-ring-hash/` - carries **`SPEC.md` + `PLAN.md` + `PROGRESS.md`** (the state-1 brainstorm + state-2 PLAN-write + state-3 implementation + state-4 verification outputs; `REVIEW.md` not yet authored — the state-5 next step). `PLAN.md` = 10 tasks, ALL COMPLETE (Task 1 §6.2 recon DONE at PLAN-write; Tasks 2–9 the implementation arc [xxHash64 / config / route hash_policy / ring+dispatch / request-hash threading / fixture 0036 / backstop / BEHAVIOR_CONTRACT], each two-stage-reviewed; Task 10 the state-4 gate, GREEN); `PROGRESS.md` carries the §6.2 transcript + the oracle mapping + every per-task entry + the §7.5-gate entry. Implementation: `crates/envoy-cluster/src/xxhash.rs` (xxHash64 seed 0 from scratch), `crates/envoy-cluster/src/ring_hash.rs` (the HashRing build+lookup), the `pick()`/`pick_endpoint()` `Option<u64>` threading + the H1/H2 HCM hash-key extraction, `LbPolicy::RingHash`+`RingHashLbConfig`+route `hash_policy` config, fixture `0036-lb-ring-hash`.

**status:** **PHASE 28 (`28-lb-ring-hash`) state-4 verification COMPLETE / state-5-next.** The state-3 implementation arc (PLAN Tasks 2–9, TDD-per-task, `cargo clippy` per task, two-stage review [spec THEN quality] per task — all APPROVED, review-fold minors folded) LANDED on `main`, and the state-4 §7.5 verification gate (`superpowers:verification-before-completion`) is **GREEN at the AUTHORITATIVE Linux CI run `27837455306` @ HEAD `4b31144`** (BOTH jobs green: `build + test + lint` ran fmt + clippy + build + h2spec + the FULL `cargo test` workspace INCLUDING the Docker differential harness [fixture `0036-lb-ring-hash` cross-proxy RING_HASH witness + all pre-existing fixtures `0001`–`0035`] + `cargo deny check`; and `fuzz` ran `parse_bootstrap` [with the new RING_HASH+hash_policy seed] + `jwt_parse` clean). The shipped RING_HASH LB reproduces upstream Envoy v1.33.0's ring byte-for-byte (the ADR-0070 algorithm: xxHash64 seed 0 from scratch; `"{ip:port}_{i}"` ring keys with the load-bearing `_` separator; `min_ring_size/num_hosts` replicas; sorted ring; `bisect_left` wrap); a route header `hash_policy` keys it; empty-but-present header → HASHED (not fallback); XX_HASH-only (MURMUR_HASH_2 all-fatal). The `ROUND_ROBIN` path stays a no-op (key inert) → all 35 pre-existing fixtures green. The phase-27 carry-forwards M27-1 (`store_endpoints` → `pub(crate)`) + M27-2 (`pick()` slow-path `debug_assert_eq!`) were CONSUMED in Task 5; HC/OD + RING_HASH compose was DEFERRED as a SPEC §2.2 non-goal (recorded in BEHAVIOR_CONTRACT). `SPEC.md` + `PLAN.md` + `PROGRESS.md` present; **`REVIEW.md` ABSENT** → the next step is the state-5 `superpowers:requesting-code-review`. **§6.1 single-phase confirmed (ADR-0071 UNFIRED).** **DECISIONS.md ledger head: ADR-0070** (count 71; ADR-0071 reserved + UNFIRED). ADR-0014 in force; ADR-0028 open. No `unsafe`. Per §5.1 the NEXT session runs the phase-28 state-5 code review.

> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases + the active phase's prior sub-state pointers, incl. the phase-25 state-1 brainstorm pointer) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill

Per `BOOTSTRAP_PROMPT.md` §5 state 5 + `SKILL_ROUTING.md`: phase `28` has `SPEC.md` + `PLAN.md` + `PROGRESS.md` and the state-4 §7.5 gate is GREEN (Linux CI `27837455306` @ `4b31144`) but `REVIEW.md` is ABSENT -> the next session runs **`superpowers:requesting-code-review`** (state 5): a HOLISTIC phase review (each state-3 Task 2–9 was already two-stage-reviewed during execution, so state-5 is the cross-cutting system-level pass) over the full phase-28 production+test diff (`50a03e4`..`4b31144`), producing `REVIEW.md`. Focus areas: the xxHash64-from-scratch fidelity (canonical vectors + the ADR-0070 ring algorithm byte-exactness — the `_` separator / seed-0 / replica math / `bisect_left` wrap); the empty-vs-absent header distinction (the load-bearing ADR-0070 line in both HCMs); the `ring.is_some()` LB discriminator (sound for 2 policies; the Maglev-footgun guard comment); the round-robin no-op regression-equivalence; the fixture-0036 CI-portability mechanism (`discover_host_lan_ip`); the HC/OD+RING_HASH deferral. Per §5.2 an approved review with no Critical/Important does NOT re-enter state-3. Per §5.1 the state-5 review is the NEXT session.

> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases + the active phase's prior sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit

**Phase-28 state-3 implementation + state-4 verification gate (THIS chain):** ran `superpowers:subagent-driven-development` — dispatched PLAN Tasks 2–9 to fresh subagents SERIALLY (TDD-per-task, `cargo clippy` per task, one code + one PROGRESS commit per task, two-stage review [spec THEN quality] per task; review-fold minors folded), then `superpowers:verification-before-completion` for the Task-10 state-4 §7.5 gate. The chain `ad4fa42`…`4b31144` (Tasks 2–10 + per-task folds + the gate-evidence commit) is on `main`, pushed. **State-4 §7.5 gate GREEN at the AUTHORITATIVE Linux CI `27837455306` @ `4b31144`** (both jobs: full workspace test incl. the Docker differential [fixture 0036 + 0001–0035] + h2spec ≥95% + fuzz; local fmt/build/clippy/test/deny also clean). Shipped: xxHash64 seed-0 from scratch + the HashRing (build+lookup) + the `pick()`/`pick_endpoint()` `Option<u64>` threading + the H1/H2 HCM hash-key extraction + `LbPolicy::RingHash`/`RingHashLbConfig`/route `hash_policy` config + fixture `0036-lb-ring-hash` (cross-proxy RING_HASH differential, the STRONG target) + the in-process backstop + the BEHAVIOR_CONTRACT "LB selection" extension. M27-1+M27-2 consumed (Task 5); HC/OD+RING_HASH deferred (§2.2). The superseded phase-28 state-2 PLAN-write top-section blocks are relocated verbatim to `STATE_HISTORY.md` per ADR-0035 / §4.1 inv. 9; a `### Phase-28 state-3 implementation + state-4 verification` Notes subsection is added. **DECISIONS.md ledger head: ADR-0070** (count 71; ADR-0071 reserved + UNFIRED). ADR-0014 in force; ADR-0028 open. No `unsafe`. **NEXT = state-5 code review (`superpowers:requesting-code-review`).**

> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases + the active phase's prior sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated

2026-06-19 (phase-28 **state-3 implementation + state-4 verification COMPLETE — RING_HASH LB SHIPPED, §7.5 gate GREEN at AUTHORITATIVE Linux CI `27837455306` @ `4b31144`** - ran `superpowers:subagent-driven-development` [PLAN Tasks 2–9, TDD + per-task clippy + two-stage review, review-fold minors folded] then `superpowers:verification-before-completion` [Task-10 §7.5 gate]. Shipped Envoy's `RING_HASH` consistent-hashing LB byte-identical to v1.33.0 [ADR-0070: xxHash64 seed-0 from scratch; `"{ip:port}_{i}"` ring keys, load-bearing `_`; `min/num_hosts` replicas; sorted ring; `bisect_left` wrap; route header `hash_policy`; empty-present→hashed; XX_HASH-only]. Fixture `0036-lb-ring-hash` cross-proxy STRONG target GREEN [locally + on CI]; all 35 pre-existing fixtures green [round-robin no-op]; h2spec ≥95%; fuzz clean. M27-1+M27-2 consumed [Task 5]; HC/OD+RING_HASH deferred [§2.2, recorded in BEHAVIOR_CONTRACT]. Advanced STATE to state-5-next [next skill `superpowers:requesting-code-review`]; relocated the superseded phase-28 state-2 PLAN-write top-section blocks verbatim to `STATE_HISTORY.md` [ADR-0035]; added the `### Phase-28 state-3 implementation + state-4 verification` Notes subsection. Ledger head **ADR-0070** [count 71; ADR-0071 reserved]. ADR-0014 in force; ADR-0028 open. No `unsafe`. Per §5.1 the NEXT session runs the phase-28 state-5 code review.)

> Historical `## Last updated` notes — every superseded last-updated note (all closed phases + the active phase's prior sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: one state per session; do not chain states. State-6 close-out commits touch ROADMAP.md + STATE.md only and carry no code changes; the next session writes PLAN.md for the next active phase per `superpowers:writing-plans`.
- The reviewer's R2 disposition decision (option (a) retroactive split of 05.1 vs option (b) free-standing post-05.1 sub-phase) was settled at the 05.1 state-6 commit in favor of option (b); 05.4 is the chosen sibling sub-phase. Future-reviewers reading STATE.md should understand that 05.1 is structurally closed at the preamble landing; 05.4 is a SIBLING under parent-05, not a child of 05.1; and the execution order ran 05.1 → 05.4 → 05.2 → 05.3, with 05.3 the closing sub-phase that flips parent-05 to `done`.

> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) and for parent phase 25 (the `### Phase-25 state-1 brainstorm` pick + recon-finding narrative, relocated at the 25.2 state-6 close-out when parent `25` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 26 (the `### Phase-26 state-1 brainstorm` pivot/rejected-alternatives/key-scoping narrative, relocated at the phase-26 state-6 close-out when row `26` flipped to `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 27 (the `### Phase-27 state-1 brainstorm` / `### Phase-27 state-2 PLAN-write` / `### Phase-27 state-4 verification` narratives + the now-consumed `### Phase-27 carry-forwards` [M26-1..M26-8] block, relocated at the phase-27 state-6 close-out when row `27` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### HTTP-filters-family carry-forwards (from the `25.2` REVIEW.md - NOT yet consumed; weigh whenever the HTTP-filters family is re-entered)

> These were never obligations on the xDS phase 26; they remain live for whenever an HTTP-filters-family phase resumes.

- **(1) [non-goal - architectural]** Over-limit request bodies are FULLY buffered before the 413 rejection (no streaming watermark). Documented deferred non-goal; differentially byte-identical to Envoy for the bounded fixture sizes. Revisit only if a streaming `decode_data` watermark path is ever planned.
- **(2) [doc precision]** The BEHAVIOR_CONTRACT 413-row "verified byte-exact against v1.33.0" phrasing - fixture `0033` is H1-only; the H2 over-limit path is covered by the in-process synth-decorator backstop, NOT differentially. Consider narrowing the phrasing if an H2 over-limit fixture is ever added.
- **(3) [coverage]** No standalone `== effective route limit` unit assertion (the boundary is exercised only via the over/under probes).
- **(4) [coverage]** No differential at-limit (`==`) probe in `0033` (within-limit `<` and over-limit `>` are both covered; the exact boundary is not differentially probed).
- _(2)-(4) are cheap polish, (1) is architectural and only relevant to a future streaming phase._

### Phase-28 carry-forwards (from the phase-27 `REVIEW.md` M27-1..M27-3 - weigh at the phase-28 state-1 brainstorm / planning)

> All 3 are NON-BLOCKING Minors (REVIEW.md APPROVED 0C/0I); pure hardening nits, none an Envoy-equivalence divergence (the differential gate is green). The first two attach to the cluster endpoint-handle / LB selection code, so they are naturally weighed when the next cluster/LB or xDS hot-reload phase re-enters that code; the third is the long-deferred shared-test-support-crate extraction.

- **M27-1** Tighten `Cluster::store_endpoints` (`crates/envoy-cluster/src/cluster.rs`) from `pub` to `pub(crate)` to match the PLAN-of-record. It is effectively unreachable cross-crate today (no public API hands out an `&Cluster`/`Arc<Cluster>`; `into_inner` is `pub(crate)`), so this is NOT an actual write-surface leak — pure hardening. Resolve when next touched.
- **M27-2** Add a `pick()` slow-path `debug_assert_eq!(eps.len(), health.len())` (when `Some`) length-coupling (`crates/envoy-cluster/src/cluster.rs:344-355`). The slow path indexes `health[i]`/`ejection[i]` for `i in 0..eps.len()`, safe today by construction (plain clusters have `endpoint_health`/`outlier_detection` = `None`); the assert would turn a future regression (wiring a watcher onto an HC/OD cluster) into a loud test failure rather than a production index-panic. Defense-in-depth.
- **M27-3** The in-flight-isolation backstop uses a 400ms wall-clock sleep (`crates/envoy-bin/tests/xds_eds_hot_reload.rs:761`) — well-cushioned by a 2s slow-backend delay (very low flake risk) but the sole non-bounded timing assumption in the backstop. Note alongside the deferred **shared-test-support-crate extraction** (the M18-9/M26-6 item, now also the 3rd+ atomic-rename/`reserve_port` user — the reviewer reproduced a port-TOCTOU flake once under full parallel load): a `SO_REUSEADDR`-or-retry port helper + a bounded-wait replacement would remove both. The trigger is the THIRD file-reload driver (CDS/LDS/SDS hot-reload), which also triggers factoring the RDS/EDS reload-dispatch skeleton (the M26-track ~70-line duplication).

### Phase-28 state-1 brainstorm (pick + scope; the Load-balancing-family-opener rationale)

> Authored at the phase-28 state-1 brainstorm (`superpowers:brainstorming`). Records WHY phase 28 is the `RING_HASH` LB opener + the alternatives weighed; the full reasoning is **ADR-0069**. (Relocates to `STATE_HISTORY.md` per ADR-0035 / §4.1 inv. 9 at the phase-28 state-6 close-out, when row `28` flips `done`.)

- **The pick OPENS a new family (Load balancing), departing the six-phase xDS arc.** The project shipped only `ROUND_ROBIN` (phase 02); the LB family (`§9`: least_request/random/ring_hash/maglev/subset/locality-weighted/priority) is foundational — subset/locality/priority LB all layer onto a real policy-dispatch + a request-hash plumbing this phase builds. It was the phase-25 brainstorm's "larger/riskier — needs a NEW distinguishable-backend harness" deferral, which **phase 27 just retired** by building the two-backend harness seed.
- **`RING_HASH` is the only DIFFERENTIABLE LB opener.** Byte-exact selection equivalence is provable only for a DETERMINISTIC policy (consistent-hashing maps a request hash key → a fixed host); `random`/`least_request` (P2C-random) are non-deterministic → cannot be byte-exact-differentiated without a contract-relaxation ADR (defer). `ring_hash` (ketama ring) over `maglev` (heavier prime-permutation table) is the gentler opener. **And the differential is LOCALLY observable** (a normal request/response — no file-watch/reload trigger), retiring the Linux-CI-only-iteration tax that phases 26/27 paid (memories `host-docker-desktop-virtiofs-no-inotify` + `envoy-rust-state4-ci-first-execution`).
- **Rejected (ADR-0069):** **CDS/LDS hot-reload** — the named next xDS layers, but CDS needs a swappable `ClusterManager` (`{ clusters: HashMap<String, Arc<Cluster>> }` built-once-immutable today, `cluster.rs:745`) + per-cluster pool/HC/OD/budget lifecycle churn on add/remove + identity-preservation across the swap → heavy, split-prone, AND its reload-differential is CI-only (the EDS-over-CDS least-invasive reasoning, ADR-0067). Deferred-not-dropped — the phase-26/27 watcher+swap+warm-reject primitive waits for them. **maglev** (heavier); **random/least_request** (non-deterministic); **the gRPC/ADS transport** (still ADR-0014/H2-trailers-blocked); **the network-filters opener** (low leverage).
- **MVP scope (ADR-0069 / SPEC §2):** `LbPolicy::RingHash` + `RingHashLbConfig` (min/max ring size; XX_HASH only) + a route header `hash_policy` on `RouteAction_Route` + **xxHash64 (seed 0) written from scratch** (D-3.2; no hash crate exists/permitted) + the ring build + next-clockwise lookup + the request-hash key threaded through `pick_endpoint()` (public) / `pick()` (private) — the round-robin path a no-op (all 35 existing fixtures stay green) + fixture 0036 (one RING_HASH cluster, two distinguishable backends, a hash-key sweep asserting same-key→same-backend bilaterally) + a backstop + a BEHAVIOR_CONTRACT "LB selection" extension. **Deferred non-goals:** maglev; least_request/random; MURMUR_HASH_2; cookie/source-IP/query/filter-state hash sources + terminal/multi-policy/regex_rewrite; weighted-ring replicas; `use_hostname_for_hashing`; RING_HASH+EDS-hot-reload re-ring; subset/locality/priority LB; CDS/LDS hot-reload.
- **The core differential risk** (envoy-rust must replicate Envoy's EXACT ring [hash fn / ring-key string format / replica math] + header-hash to select the SAME backend) is de-risked by the **state-2 §6.2 LOCAL reconnaissance** (sweep keys against `envoyproxy/envoy:v1.33.0`, record key→backend mappings, reverse-engineer the ring), with a documented **same-key-stability fallback** equivalence (per-proxy consistency + distribution sanity, defined by ADR-0070) if exact cross-proxy identity proves intractable. **ADR-0070 reserved** for the §6.2 reconciliation, **ADR-0071 reserved** for the §6.1 split (PLAUSIBLE — 28.1 config-foundation / 28.2 ring+fixture). SPEC spec-reviewed → APPROVED-WITH-MINORS (0C/0I/4 doc Minor; M1 [public `pick_endpoint` / private `pick`] + M2 [H1→503/H2→502 no-host asymmetry] folded).

### Phase-28 state-2 PLAN-write (the §6.2-LOCKED ring algorithm + the design decisions the state-3 executor consumes)

> Authored at the phase-28 state-2 PLAN-write (`superpowers:writing-plans`). Records the §6.2 empirical findings (the EXACT ring algorithm) + the locked PLAN-time decisions; the full reconciliation is **ADR-0070**. (Relocates to `STATE_HISTORY.md` per ADR-0035 / §4.1 inv. 9 at the phase-28 state-6 close-out, when row `28` flips `done`.)

- **§6.2 ran AT the PLAN-write, LOCALLY** (ring_hash selection is observable on this Docker-Desktop host with NO file-watch/reload trigger — the phase-28 differentiating advantage). Against `envoyproxy/envoy:v1.33.0`: one `RING_HASH` `STATIC` cluster, two distinguishable `hashicorp/http-echo` backends (numeric IP on a docker network), a route header `hash_policy`. **The ring algorithm was CRACKED + EXACTLY VALIDATED — 36/36 keys reproduced by a from-scratch xxHash64 replica → STRONG differential target** (cross-proxy identical selection; the ADR-0069 same-key-stability fallback is NOT taken). Determinism + spread confirmed.
- **ADR-0070 FIRED — the LOCKED algorithm the state-3 executor MUST reproduce byte-for-byte:** hash = **xxHash64 seed 0** (vectors `xxh64("")=0xEF46DB3751D8E999`, `xxh64("abc")=0x44BC2CF5AD770999`); per-host ring key = **`"<ip:port>_<i>"`** (the `_` separator is load-bearing — other separators matched only 11–14/27) for `i` in `0..(minimum_ring_size/num_hosts)` (1024/2 = 512); sorted `(hash, host)` ring; request hash = `xxh64(header_value_bytes)`; lookup = first entry `≥ request_hash`, wrapping to index 0 (`bisect_left`). **Refinements:** an empty-but-present header value is HASHED (`xxh64("")`, deterministic) — NOT the random fallback (only an ABSENT hash key → random host); `hash_function: MURMUR_HASH_2` is a valid Envoy enum but is OUT of phase-28 scope (XX_HASH only → all-fatal config error); a bogus enum → proto parse-reject, `minimum_ring_size > maximum_ring_size` → semantic init-reject (both ADR-0049 all-fatal). The oracle key→backend mapping is pinned in `PROGRESS.md` (the Task-5 ring unit-test regression oracle).
- **PLAN = 10 tasks** (Task 1 §6.2 DONE; 2 xxHash64-from-scratch [+ canonical vectors] / 3 `LbPolicy::RingHash` + `RingHashLbConfig` + validators [XX_HASH-only narrowing; min>max reject] / 4 route header `hash_policy` / 5 ring build+lookup+`pick()` dispatch [folds the phase-27 M27-1 `store_endpoints` `pub(crate)` + M27-2 slow-path `debug_assert`] / 6 request-hash threading [BOTH `Cluster::pick` core + the `ClusterHandle::pick_endpoint` delegate; the 2 HCM call sites; the empty-vs-absent header MUST-HAVE test] / 7 fixture 0036 + the key-sweep differential driver [LOCALLY observable] / 8 backstop + `parse_bootstrap` fuzz seed / 9 BEHAVIOR_CONTRACT "LB selection" / 10 state-4 gate). The `ROUND_ROBIN` path is a no-op (the key is inert) → all 35 existing fixtures stay green (regression-equivalence). **§6.1 single-phase confirmed (~1000–1300 LoC / ~10 tasks; ADR-0071 UNFIRED).** PLAN plan-reviewed → APPROVED-WITH-MINORS (0C/2I/5 Minor; the 2 Importants — the two-type `pick`/`pick_endpoint` signature site + promoting the empty-vs-absent header assertion to a Task-6 must-have — and the concrete Minors folded). HC/OD+RING_HASH compose is a bounded Task-5 call (skip-retry if <~30 LoC, else a recorded §2.2 non-goal). xxHash64 is from scratch (D-3.2; no hashing crate).

### Phase-28 state-3 implementation + state-4 verification (what shipped + the state-5 reviewer's map)

> Authored at the phase-28 state-3 implementation arc + state-4 §7.5 gate. Records what landed + the per-task review outcomes; the state-5 holistic review consumes this. (Relocates to `STATE_HISTORY.md` per ADR-0035 / §4.1 inv. 9 at the phase-28 state-6 close-out, when row `28` flips `done`.)

- **Shipped (PLAN Tasks 2–9, all two-stage-reviewed + folded):** Task 2 xxHash64 seed-0 from scratch (`crates/envoy-cluster/src/xxhash.rs`, canonical vectors pinned); Task 3 `LbPolicy::RingHash`+`RingHashLbConfig`+`HashFunction`+validators (XX_HASH-only narrowing; `UnsupportedHashFunction`/`RingSizeInversion` all-fatal); Task 4 route `hash_policy` (header source; `UnsupportedHashPolicy` rejects non-header sources); Task 5 the `HashRing` build+lookup + `pick()` RingHash dispatch (the §6.2 oracle pinned as the regression guard; M27-1+M27-2 folded; `ring.is_some()` discriminator + a Maglev-footgun guard comment); Task 6 the `Option<u64>` request-hash threading through `pick()`+`pick_endpoint()` + the H1/H2 HCM extraction (the empty-vs-absent MUST-HAVE distinction — present-empty HASHED via `.map()`, absent → None); Task 7 fixture `0036-lb-ring-hash` (cross-proxy STRONG differential, GREEN locally + CI; the `{{BACKEND_IP}}`/`discover_host_lan_ip` shared-address mechanism — the ring is IP-string-sensitive, with a documented CI-portability risk); Task 8 the in-process backstop (the no-hash fallback / empty-is-hashed-vs-None-rotates / single-host edge cases the differential can't reach) + the `parse_bootstrap` fuzz seed; Task 9 the BEHAVIOR_CONTRACT "LB selection" extension.
- **State-4 §7.5 gate GREEN** at the AUTHORITATIVE Linux CI `27837455306` @ `4b31144` (both jobs; full workspace test incl. the Docker differential 0036 + 0001–0035 + h2spec + fuzz). The fixture-0036 CI-portability risk (the bridge→host-LAN-IP path) did NOT materialize — it ran green on Linux CI.
- **Phase-27 carry-forwards:** M27-1 (`Cluster::store_endpoints` → `pub(crate)`) + M27-2 (`pick()` slow-path `debug_assert_eq!` length-coupling) CONSUMED in Task 5. M27-3 (the 400ms backstop sleep + the shared-test-support-crate extraction) REMAINS deferred — still triggered by the THIRD file-reload driver (CDS/LDS/SDS hot-reload).
- **Carry-forward Minors for the state-5 review (non-blocking, from the per-task code-quality reviews):** Task 6's cosmetic minors (the extraction is config-shape-gated not LB-type-gated [wasted-but-harmless for non-ring clusters]; the `lookup` lifetime doc; the test-(a) None-arm over-specification; the TCP `None` degradation note). Task 8's spread/cursor test-comment nits (folded). All cosmetic; the state-5 holistic review re-triages.
