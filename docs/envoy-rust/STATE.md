# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase


**id:** **58** (`58-accesslog-h2-uo-overflow`) — witness the THIRD H2 `%RESPONSE_FLAGS%` value, `UO` (UpstreamOverflow), byte-exact on the H2 pool/circuit-breaker overflow 503 path. **STATE-4 VERIFICATION COMPLETE this session** — the full §7.5 gate is GREEN, both locally (modulo 5 documented, independently-reconfirmed host-only flakes) and on the authoritative CI run `28753634192` @ `ee0ae95`. The state-5 code-review (`superpowers:requesting-code-review`) is the next session — see `## Next expected skill`.
**slug:** `58-accesslog-h2-uo-overflow`
**directory:** `docs/envoy-rust/phases/58-accesslog-h2-uo-overflow/` — `SPEC.md` + `PLAN.md` + `PROGRESS.md` (incl. `## State-4 verification`) present; `REVIEW.md` NOT YET written (state-5 is the next session).

**status:** **PHASE 58 STATE-4 VERIFICATION COMPLETE — state-5 code-review is next.** This session executed the §5 state-4 verification via `superpowers:verification-before-completion`: (1) cold-started fully per `BOOTSTRAP_PROMPT.md` §1, confirmed `git status --porcelain` clean and `HEAD` at the phase-58 state-3 commit `ee0ae95` (concurrency guard) both before and after the local run — no sibling had advanced, `git fetch origin --prune` confirmed `origin/main` unmoved (only an unrelated new branch appeared); (2) ran the local §7.5 (a)-(e) subset: `cargo build --workspace --all-targets` clean; `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean; `cargo fmt --all -- --check` clean; rebuilt debug `envoy-bin` (memory `differential-harness-uses-debug-envoy-bin`); `cargo test --workspace` (run twice, once `--no-fail-fast`) surfaced 5 target-level failures, EVERY one independently re-confirmed passing in isolation and traced to already-documented host-only flake classes, none touching phase-58's changed files — `access_log_rcd_upstream_reset`/`access_log_rf_upstream_reset`/`admin_config_dump_server_info` (the `192.168.65.2` bridge-IP class, memory `differential-host-bridge-ip-192-168-65-2`, deterministic on this host) + `http_filter_buffer` (Docker-container-startup-under-parallel-load class, passed on standalone retry) + the `envoy-http2` h2-handshake test (memory `envoyrust-h2-handshake-test-host-flake`, passed standalone); fixture `0066`'s differential test and both new in-process backstops (`h2_pool_overflow_access_log_carries_uo_flag`, `h2_request_budget_overflow_access_log_carries_uo_flag`) passed cleanly in every run; `cargo deny check` clean (`advisories ok, bans ok, licenses ok, sources ok`); (3) confirmed CI is AUTHORITATIVE for the Docker differential (memory `envoy-rust-state4-ci-first-execution`) — run `28753634192` @ `ee0ae959e373841ff09bfeff16e7b35b06165547` was ALREADY green (pushed + confirmed at the end of the state-3 session), re-confirmed via `gh run view --json status,conclusion,headSha,jobs` (both jobs `success`, every step `success`) and by pulling the FULL 27645-line job log (`gh run view --log`) — zero occurrences of `FAILED` anywhere, `test access_log_h2_rf_overflow ... ok` present, `h2spec_pass_rate_gate ... ok`, `envoy-http2` lib 84 passed/0 failed/1 ignored, fuzz job `success`; (4) appended the `## State-4 verification` section to `PROGRESS.md` quoting every command's output plus the CI evidence; (5) advanced STATE (THIS update) — relocating the phase-58-state-3 four top-section narratives to `STATE_HISTORY.md` per ADR-0035, adding a NEW `### Phase-58 state-4 verification` Notes subsection (phase 58 uses the keep-all-substates convention — state-1/state-2/state-3 subsections all stay). **§7.5 gate (a)-(e) MET on CI, corroborated locally; (f) `REVIEW.md` is the next session's job.** No ADR fired — verification overturned no PLAN/SPEC fact; ADR-0115 remains the ledger head; ADR-0116 stays reserved-but-unfired. `#![forbid(unsafe_code)]` holds; NO status-code change anywhere; NO new `Op`/`AccessLogRecord` field/crate/dependency/`ConfigError` variant. **DECISIONS.md ledger head: ADR-0115** (unchanged this session; count 115; next-available **ADR-0116**). ADR-0014 in force; ADR-0028 open; ADR-0049 governs config-validity. The next session is the **§5 state-5 code-review** (`superpowers:requesting-code-review`).

> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases incl. the phase-51 state-3/4/5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill


Per `BOOTSTRAP_PROMPT.md` §5 state 5 + `SKILL_ROUTING.md`: phase 58 (`58-accesslog-h2-uo-overflow`) has `SPEC.md` + `PLAN.md` + `PROGRESS.md` (incl. `## State-4 verification`); the §7.5 gate is confirmed GREEN on CI (run `28753634192` @ `ee0ae95`) and corroborated locally. `REVIEW.md` NOT yet written. The next session runs the **§5 state-5 code-review** (`superpowers:requesting-code-review`): dispatch a FRESH reviewer subagent (no session history) against the phase-58 diff range, independently re-derive (not trust) every load-bearing claim in SPEC/PLAN/PROGRESS, author `REVIEW.md`. Do NOT run the state-6 close-out in the same session (§5.1 one state per session).

**IMPORTANT LESSON (carried from phases 41 through 58 — verify the pick EXISTS + is DETERMINISTIC):** this phase's state-0 recon (folded into SPEC.md's header) already ran the live-Envoy + envoy-rust-cross-check step for `UO`; the state-2 PLAN-write re-verified every SPEC §3 PLAN-VERIFY item against the live tree with NO drift found; state-3 implementation confirmed fixture `0066` green LOCALLY (unusual for a Docker differential fixture on this dev host — it needs no live backend/sibling-container, only a literal dead endpoint, so it isn't subject to the host's bridge-IP quirk); state-4 verification independently re-confirmed the full §7.5 gate GREEN both locally and on CI (run `28753634192` @ `ee0ae95`) — no drift, no regression found.

**Carried candidate menu (for the NEXT next-phase pick, after phase 58 closes):**
- **M56-1** — the remaining H2 `%RESPONSE_FLAGS%` values `URX`/`UF`/`UC` byte-exact on H2, one-at-a-time (now with `UH` consumed by phase 57 and `UO` scoped by phase 58) + the H2 failure-path `%RESPONSE_CODE_DETAILS%` strings beyond `route_not_found`/`no_healthy_upstream`/`{overflow}`.
- **M57-1** — a trivial cosmetic fix (the `content-length` header omission on `synth_h2_no_healthy_upstream()` vs. its sibling `synth_h2_overflow()`); fold into whichever phase next touches that helper — NOT a pick driver on its own.
- **The `DC` (DownstreamConnectionTermination) slice of M45-2** — timing-dependent, hard to drive deterministically; rejected at every prior consideration (ADR-0102 through ADR-0115).
- **M53-2's BEHAVIOR_CONTRACT "(H1)" qualifier** — a trivial doc fix (the "reset 503" wording reads global but the H2 reset path still synth-502).
- **A candidate future carry-forward slice from THIS phase's SPEC:** the H2 request-budget arm's OWN differential access-log witness (a `max_requests: 0` trigger, distinct from fixture `0066`'s pool-overflow trigger) — §B's in-process backstop (§F2) covers it at the unit level; a dedicated differential fixture is deferred, mirroring how H1's equivalent gap (M50-C) was deferred at phase 50 and only closed later, cheaply, at phase 55.
- **Another Observability access-log operator not yet witnessed** / **Observability family — gRPC ALS / OTLP / tracing / stats sinks / tap filter** (new sink/transport infra) / **other HTTP-filters-family / LB-family / xDS-family leaves** (e.g. CDS/LDS hot-reload, `least_request`/`random` LB).

**Open carry-forward Minors (fold into the phase that next touches each surface; NONE blocks):** **M57-1** (the `content-length` header omission on `synth_h2_no_healthy_upstream()` vs. its sibling `synth_h2_overflow()`, from the phase-57 REVIEW.md) + **M56-1** (the remaining H2 `%RESPONSE_FLAGS%` values `URX`/`UF`/`UC` — `UH` CONSUMED by phase 57, `UO` CONSUMED by phase 58 (implementation + CI-confirmation both complete) — + the H2 failure-path `%RESPONSE_CODE_DETAILS%` strings beyond `route_not_found`/`no_healthy_upstream`/`{overflow}`) + **M55-1** (the `hcm.rs:948`/`:7221` "M50-C deferred" stale comments IN `crates/envoy-http1/src/hcm.rs`; still live, unrelated) + **M53-2** (BEHAVIOR_CONTRACT `:36/:289/:296/:387` "reset 503" reads global but the H2 reset path still synth-502 — add an "(H1)" qualifier; left live) + **M53-3** (the un-reconned `retry_on:reset`+exhausted URX+UC body-wording mismatch — PRESERVED by the §A `!retry_limit_exceeded_for_log` guard, ALSO covered by an in-process backstop) + **M48-2** + **M42-1** + the `DC`/retry-budget-overflow slices of **M45-2** + **M40-1** + **M39-1/M39-2** + **M38-1/M38-2** + **CF-39-1** + M37-* + M36-* + M34-* + M33-* + the empty-`metadata_match` doc-comment + M29-*/M30-* + the phase-31 cosmetics + the HTTP-filters-family (1)-(4). **[M45-1 CONSUMED by phase 56; the `UH` slice of M56-1 CONSUMED by phase 57 — fully closed; the `UO` slice of M56-1 CONSUMED by phase 58 — implementation + state-4 CI-confirmation both now complete, state-5/6 remain; M50-C CONSUMED by phase 55; M54-1 CONSUMED by phase 55; M53-1 CONSUMED by phase 54; M52-1 by phase 53; M52-2 + the `UF` slice of M45-2 + M51-1 by phase 52; the `URX` slice by phase 51.]**

**Phase 58's implementation AND state-4 verification are both complete; the state-5 code-review is the next session — do NOT re-run the state-0/1 recon, re-litigate the pick, re-derive the PLAN-VERIFY items, re-implement any task, or re-run the state-4 verification gate (all done, see `## Last commit` + `PROGRESS.md`'s `## State-4 verification` section).** Phases 57/56/55/54/53/52/51/50/49/48/47/46/45/44/43/42/41/40/39/38/37/36/35/34 are closed + CI-GREEN; rows `00`-`57` ALL `done`; row `58` is `in-progress` (SPEC + PLAN + implementation + state-4 verification; state-5 code-review next).

> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases incl. the phase-37 state-1..5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit


**Phase-58 state-4 verification — full local §7.5 (a)-(e) gate + CI re-confirmation, append `## State-4 verification` to PROGRESS.md, advance STATE:** the §5 state-4 verification (`superpowers:verification-before-completion`, a SEPARATE session from the phase-58 state-3 implementation per §5.1). Cold-started fully; confirmed `git status --porcelain` clean and `HEAD` at the phase-58 state-3 commit `ee0ae95` before starting (concurrency guard) — no sibling had advanced; `git fetch origin --prune` confirmed `origin/main` unmoved. Ran `cargo build --workspace --all-targets` (clean), `cargo clippy --workspace --all-targets --all-features -- -D warnings` (clean), `cargo fmt --all -- --check` (clean), rebuilt debug `envoy-bin` (memory `differential-harness-uses-debug-envoy-bin`), `cargo test --workspace` (twice, once `--no-fail-fast`) — 5 target-level failures, ALL independently re-confirmed passing in isolation and traced to already-documented host-only flake classes (the `192.168.65.2` bridge-IP class on `access_log_rcd_upstream_reset`/`access_log_rf_upstream_reset`/`admin_config_dump_server_info`; a Docker-startup-race flake on `http_filter_buffer`, passed on retry; the `envoy-http2` h2-handshake test, passed standalone) — none touching phase-58's files; fixture `0066` and both new backstops passed cleanly throughout. `cargo deny check` clean. Confirmed CI run `28753634192` @ `ee0ae95` (already green from the state-3 session) via `gh run view --json` (both jobs `success`) and the full job log (`gh run view --log`, 27645 lines, zero `FAILED` occurrences, `access_log_h2_rf_overflow ... ok`, `h2spec_pass_rate_gate ... ok`, `envoy-http2` lib 84 passed/0 failed). Appended `## State-4 verification` to `PROGRESS.md` quoting all command outputs + the CI evidence. `#![forbid(unsafe_code)]` holds; NO status-code change; NO code-tree change this session (verification + docs only). No new ADR fired. **DECISIONS.md ledger head: ADR-0115** (unchanged; count 115; next-available **ADR-0116**). ADR-0014 in force; ADR-0028 open. The next session is the §5 state-5 code-review (`superpowers:requesting-code-review`).

> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases incl. the phase-37 state-1..5 sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated


2026-07-05 (phase-58 **STATE-4 VERIFICATION COMPLETE — the full §7.5 gate is GREEN, state-5 code-review is next.** Local build/clippy/fmt/test/deny all clean (modulo 5 documented, independently-reconfirmed host-only flakes, none touching phase-58's files); CI run `28753634192` @ `ee0ae95` independently re-confirmed green via the full job log (zero `FAILED`, fixture `0066` `ok`, h2spec gate `ok`, fuzz job `success`). `## State-4 verification` appended to `PROGRESS.md`. **NO status-code change; NO new `Op`/`AccessLogRecord` field/crate/dependency/`ConfigError` variant; no code-tree change this session.** `#![forbid(unsafe_code)]` holds. **DECISIONS.md ledger head: ADR-0115** [unchanged; count 115; next-available ADR-0116]. ADR-0014 in force; ADR-0028 open. The next session is the §5 state-5 code-review [`superpowers:requesting-code-review`].)

> Historical `## Last updated` notes — every superseded last-updated note (all closed phases incl. the phase-37 state-1..5 sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

### Phase-58 state-4 verification (active phase)

Executed via `superpowers:verification-before-completion`. Cold-started fully (`BOOTSTRAP_PROMPT.md` §1, `STATE.md`, `ROADMAP.md`, `DECISIONS.md` tail, `BEHAVIOR_CONTRACT.md`, `SKILL_ROUTING.md`, plus this phase's SPEC/PLAN/PROGRESS) before running the gate. `git status --porcelain` clean and `HEAD` at the phase-58 state-3 commit `ee0ae95` both before and after the local run (concurrency guard, memory `concurrent-loop-sessions-race-on-phase-pick`) — no sibling had advanced; `git fetch origin --prune` confirmed `origin/main` unmoved (only an unrelated new branch `perf/listener-so-reuseport` appeared).

**Local §7.5 (a)-(e) subset — all clean:** `cargo build --workspace --all-targets`; `cargo clippy --workspace --all-targets --all-features -- -D warnings`; `cargo fmt --all -- --check`; rebuilt debug `envoy-bin` (`cargo build -p envoy-bin`, memory `differential-harness-uses-debug-envoy-bin`); `cargo test --workspace` (run twice, once `--no-fail-fast`); `cargo deny check` (`advisories ok, bans ok, licenses ok, sources ok`).

**5 target-level test failures, every one independently re-confirmed as an already-documented host-only flake, none touching phase-58's changed files** (`crates/envoy-http2/src/hcm.rs`, fixture `0066`, its differential test, `BEHAVIOR_CONTRACT.md`): `access_log_rcd_upstream_reset` + `access_log_rf_upstream_reset` + `admin_config_dump_server_info` (the `192.168.65.2` Docker-bridge-IP class, memory `differential-host-bridge-ip-192-168-65-2` — re-run standalone, failed identically both times, deterministic on this host, not a parallel-load flake); `http_filter_buffer` (Docker-container-startup-under-parallel-load class, memory `differential-fixtures-flake-under-parallel-load` — failed once with "never became accept-ready", passed cleanly on standalone retry); the `envoy-http2` lib test `client::tests::send_request_maps_h2_handshake_failure_to_typed_error` (memory `envoyrust-h2-handshake-test-host-flake` — passed standalone). Fixture `0066`'s differential test (`access_log_h2_rf_overflow`) and both new in-process backstops (`h2_pool_overflow_access_log_carries_uo_flag`, `h2_request_budget_overflow_access_log_carries_uo_flag`) passed cleanly in every run, including a dedicated standalone additivity check alongside fixtures `0064`/`0065`.

**CI is authoritative for the Docker differential** (memory `envoy-rust-state4-ci-first-execution`): run `28753634192` @ `ee0ae959e373841ff09bfeff16e7b35b06165547` was already green from the state-3 session's end — re-confirmed via `gh run view --json status,conclusion,headSha,jobs` (both jobs `success`, every step `success`) and by pulling the full 27645-line job log (`gh run view --log`): zero occurrences of the literal `FAILED` anywhere; `test access_log_h2_rf_overflow ... ok` present; `test h2spec_pass_rate_gate ... ok` (unmoved, no H2 codec/framing change this phase); `envoy-http2` lib-test step `84 passed; 0 failed; 1 ignored` (includes both new backstops); the fuzz job (`parse_bootstrap`/`jwt_parse`/`cdn_loop_parse`/`accesslog_format_parse`) `success`. No new commit/re-trigger was needed since no regression was found locally — the existing green run is cited directly as the authoritative §7.5(a)-(d) evidence, per the phase-57 state-4 precedent.

Appended the full `## State-4 verification` section to `PROGRESS.md`, quoting all command outputs and the CI evidence, mirroring the phase-57 state-4 precedent's structure. **§7.5 gate (a)-(e) MET on CI, corroborated locally; (f) `REVIEW.md` is the state-5 session's job.** No ADR fired — verification overturned no PLAN/SPEC fact; ADR-0115 remains the ledger head; ADR-0116 stays reserved-but-unfired. `#![forbid(unsafe_code)]` holds; NO status-code change; NO code-tree change this session (verification + docs only).

**Next session:** the §5 state-5 code-review (`superpowers:requesting-code-review`) — dispatch a fresh reviewer subagent, author `REVIEW.md`. Per §5.1, one state per session — do not also run state-6 close-out in the same session.

### Phase-58 state-3 implementation

Executed via `superpowers:executing-plans` (direct execution, not `subagent-driven-development` — `PLAN.md`'s 6 tasks were already fully detailed with exact code diffs re-confirmed against the tree at state-2, so a fresh implementer subagent per task added no value). Session opened by pulling unrelated concurrent phases 59/60 (H1 hot-path alloc trims + vectored response write, both perf-focused, no ROADMAP rows of their own yet) from `origin/main` and confirming the workspace still builds/tests clean before starting phase-58 work.

**Tasks landed (RED commit → GREEN commit, per task):** Task 1 `a132f43`→`d3393dd` (§A: the caller-loop `attempt.outcome.is_none()` discriminator at `hcm.rs:691`-`703`, mirroring H1's `hcm.rs:1045`-`1052` exactly; §C: the three-arm `%RESPONSE_FLAGS%` derive gaining `Some("upstream_reset_before_response_started{overflow}") => "UO"`; §F1: in-process backstop `h2_pool_overflow_access_log_carries_uo_flag`, wiring a CONFIGURED `H2PoolManager` via a manual `HCM::new` spawn mirroring `h2_hcm_pool_reuses_upstream_conn_across_sequential_requests` since `spawn_h2_hcm` hard-codes `pool: None`). Task 2 `3ad55fb`→`b7fa13a` (§B: a direct tag on the pre-route request-budget `Rejected` arm, mirroring H1's `hcm.rs:951`-`952` exactly — this arm bypasses the retry loop entirely, so it's tagged directly rather than via §A's discriminator; §F2: in-process backstop `h2_request_budget_overflow_access_log_carries_uo_flag`, no pool needed since the budget gate fires before any pool contact). Task 3 `68ced77` (fixture `0066-accesslog-h2-rf-overflow`: 4 files, the H2 analogue of fixture `0058` combined with `0064`/`0065`'s H2C listener shape + an H2-upstream `typed_extension_protocol_options` cluster). Task 4 `130183d` (differential test `access_log_h2_rf_overflow.rs`, a structural clone of `access_log_h2_rf_no_healthy.rs`). Task 5 `a9c4c45` (BEHAVIOR_CONTRACT.md: the `%RESPONSE_FLAGS%` row's H2-witness sentence + the `%RESPONSE_CODE_DETAILS%` row's H2-witness sentence, both updated to record `UO`/`{overflow}` now witnessed on H2 via fixture `0066`). Task 6 `70f257e` (fmt-fix commit — `cargo fmt` reflowed two multi-line call sites in the new Task-1 backstop).

**Debug-binary staleness caught and fixed:** the first attempt to run fixture `0066`'s differential test locally FAILED with envoy-rust still emitting the OLD `via_upstream`/`-` output despite Task 1's source fix already landing — traced to a stale `target/debug/envoy-bin` (last built before the `hcm.rs` edits; the differential harness runs this binary as a subprocess, per memory `differential-harness-uses-debug-envoy-bin`). Rebuilt (`cargo build -p envoy-bin`) and the fixture immediately went GREEN — not a code regression, a harness-staleness trap worth flagging for future sessions touching `envoy-http2`/`envoy-http1` right before a local differential run.

**Verification sweep (Task 6):** `cargo clippy -p envoy-http2 -p differential --all-targets --all-features -- -D warnings` clean; `cargo fmt --all -- --check` needed one fix (two multi-line call-site reflows in the new backstop test), applied + committed. `cargo test --workspace` (run three times across the session, twice `--no-fail-fast`) surfaced these failures, ALL independently re-confirmed passing in isolation and traced to already-documented host-only flake classes (none touching phase-58's changed files): `access_log_rcd_upstream_reset` + `access_log_rf_upstream_reset` + `admin_config_dump_server_info` (the `192.168.65.2` Docker-bridge-IP class, memory `differential-host-bridge-ip-192-168-65-2`), the `envoy-http2` h2-handshake test (memory `envoyrust-h2-handshake-test-host-flake` — re-confirmed still passing, INCLUDING both new phase-58 backstops, when run in isolation), and a new-but-same-class Docker-container-startup-under-parallel-load flake on `upstream_retry` (`access_log_command_operators` also transiently hit this class mid-session, confirmed passing in isolation). Additivity re-grep (`for f in 0009 0010 0018 0021 0064 0065`) re-confirms only `0021` has ANY `circuit_breakers` hit (`max_connections: 4`, headroom only) — `0001`-`0065` stay byte-identical.

**Fixture `0066` run standalone locally** (unusual for a Docker differential fixture on this dev host — it needs no live backend/sibling-container, only a literal dead endpoint `127.0.0.1:1`, so it isn't subject to the host's bridge-IP quirk that REDs several other fixtures locally): GREEN, byte-exact `{"method":"GET","proto":"HTTP/2","rc":503,"rcd":"upstream_reset_before_response_started{overflow}","rf":"UO"}` on both proxies. Re-ran fixtures `0064`/`0065` alongside for additivity — both still green.

**Next session:** the §5 state-4 verification (`superpowers:verification-before-completion`) — push, watch/confirm the full §7.5 gate GREEN on CI, author `PROGRESS.md` quoting the CI evidence. Per §5.1, one state per session — do not also run state-5 code-review in the same session.

### Phase-58 state-2 PLAN-write

Re-verified all seven SPEC §3 PLAN-VERIFY items against the live tree at PLAN-write time — NO drift found: the four cited `crates/envoy-http2/src/hcm.rs` line ranges (`:407`-`417` the overflow-returning arm, `:613`-`636` the pre-route `BudgetAcquisition::Rejected` arm, `:691`-`703` the caller-loop discriminator site, `:949`-`963` the two-arm derive) are exact; a widened additivity re-grep across every H2-listener fixture (`0009`/`0010`/`0018`/`0021`/`0064`/`0065`) confirms only `0021` has ANY `circuit_breakers` config (`max_connections: 4`, headroom only — no pending/request cap) and `0018-http-filter-fault` has `clusters: []` (unreachable by definition) — neither reaches either new code path; the two H1 backstop names (`h1_pool_overflow_access_log_carries_uo_flag`, `crates/envoy-http1/src/hcm.rs:5625`; `h1_request_budget_overflow_access_log_carries_uo_flag`, `:7224`) and the H2 pool constructor (`mk_pool_pending`, `crates/envoy-http2/src/pool.rs:763`, used by `acquire_rejects_with_pending_overflow_when_max_pending_requests_zero`, `:1008`) all re-confirmed present and unchanged; fixture `0066` re-confirmed still next-free. `docs/envoy-rust/phases/58-accesslog-h2-uo-overflow/PLAN.md` authored with 6 tasks: **Task 1** (§A the caller-loop `attempt.outcome.is_none()` discriminator, mirroring H1's `hcm.rs:1045`-`1052` exactly; §C the three-arm `%RESPONSE_FLAGS%` derive gaining `Some("upstream_reset_before_response_started{overflow}") => "UO"`; §F1 an in-process backstop wiring a CONFIGURED `H2PoolManager` — the existing test `h2_hcm_pool_reuses_upstream_conn_across_sequential_requests`, `~hcm.rs:1870`-`1955`, already demonstrates the exact manual-spawn wiring needed since `spawn_h2_hcm` hard-codes `pool: None`); **Task 2** (§B a direct tag on the pre-route request-budget `Rejected` arm, mirroring H1's `hcm.rs:951`-`952` exactly; §F2 an in-process backstop needing NO pool — the request-budget gate fires before any pool contact); **Task 3** (fixture `0066-accesslog-h2-rf-overflow`, combining fixture `0058`'s H1 `UO` cluster shape with fixture `0064`/`0065`'s H2C listener + an H2-upstream `typed_extension_protocol_options` cluster, needed for envoy-rust's side to route through the H2 pool); **Task 4** (a thin differential test); **Task 5** (two exact BEHAVIOR_CONTRACT.md replace/with blocks for the `%RESPONSE_FLAGS%`/`%RESPONSE_CODE_DETAILS%` rows' H2-witness sentences); **Task 6** (clippy/fmt/test/additivity-recheck local sweep). §6.1 split decision: does NOT fire (~300-420 LoC / 6 tasks, well under the ~25-task/~1500-LoC gate). Ran the writing-plans self-review (spec coverage against SPEC §A-§H — all covered; placeholder scan — clean; type consistency — `Http1HCMConfig::from_config`/`HCMConfig::wrap`/`H2PoolManager::for_bootstrap` call shapes verified consistent across both new backstop tests) — no issues found, nothing to fix. No new ADR fired this session (ADR-0115 governs the whole phase; ADR-0116 stays reserved-but-unfired for the §6.1 split/§6.2 reconciliation, neither of which fired). The state-3 implementation (`superpowers:executing-plans` or `superpowers:subagent-driven-development`) is the next session — it executes `PLAN.md`'s 6 tasks in order, TDD per task, appending to `PROGRESS.md`.

### Phase-58 state-1 brainstorm

Picked **`UO`** (the THIRD H2 `%RESPONSE_FLAGS%` value, UpstreamOverflow) as phase 58, continuing the H2-cadence sequel to phase 57's `UH` (H1 cadence: `NR`→`UH`→`UO`, phases 48-50; H2 cadence: `NR`→`UH`→`UO`, phases 56-58). State-0 recon against live `envoyproxy/envoy:v1.33.0` (Docker `-p` port-mapping, ports 13000/13099) confirmed byte-stable `{"rc":503,"rcd":"upstream_reset_before_response_started{overflow}","rf":"UO"}` on an H2C-downstream listener routing to an H2-upstream `backend_cluster` (`typed_extension_protocol_options`/`http2_protocol_options`) with `circuit_breakers.thresholds.max_pending_requests: 0`; cross-checked `target/debug/envoy-bin` on the identical config — `503` (status ALREADY correct via the pre-existing `synth_h2_overflow()`) but `rcd:"via_upstream"`/`rf:"-"` (wrong), confirming a PURE two-site access-log gap — UNLIKE phases 50 and 57, no status-code correction is needed. `docs/envoy-rust/phases/58-accesslog-h2-uo-overflow/SPEC.md` scopes: §A a caller-loop overflow discriminator (`crates/envoy-http2/src/hcm.rs:691`-`703`, mirroring H1's `hcm.rs:1045`-`1052` exactly); §B tagging the pre-route request-budget `Rejected` arm (`hcm.rs:613`-`636`, mirroring H1's `hcm.rs:951`-`952`); §C a three-arm `%RESPONSE_FLAGS%` derive (`hcm.rs:959`-`963`); §D fixture `0066-accesslog-h2-rf-overflow`; §E a differential test; §F1/§F2 TWO in-process backstops (one per set-site — §F2 is REQUIRED, folded from a spec-review Important finding that the SPEC's first draft mis-attributed the request-budget arm's test coverage as deferrable when H1's own precedent never deferred it); §G BEHAVIOR_CONTRACT updates; §H no fuzz target. Spec-reviewed by a fresh `general-purpose` subagent (no session history) → **APPROVED WITH MINOR NOTES** (0 Critical / 1 Important / 1 Minor): the Important (the M50-C mis-attribution, folded via the REQUIRED §F2 backstop) and the Minor (the additivity-recheck fixture list omitted `0018-http-filter-fault`, harmless since `clusters: []`, folded by widening the list) are both resolved in the locked SPEC. **ADR-0115** locks the pick + scope (reclaiming the phase-57 lapsed reservation). ROADMAP row `58` lands `in-progress` (depends-on `06 15 32 42 45 46 47 48 49 50 56 57`). **§6.1 split projected NOT to fire** (~8-13 tasks / ~180-350 LoC). The state-2 PLAN-write (`superpowers:writing-plans`) is the next session — it re-confirms SPEC §3's PLAN-VERIFY items (all now resolved except line-number drift, the fixture-`0066` numbering, and the split decision) before authoring `PLAN.md`.

> Historical Notes subsections for fully-closed phase 57 (the `### Phase-57 state-1 brainstorm` / `### Phase-57 state-2 PLAN-write` / `### Phase-57 state-3 implementation` / `### Phase-57 state-4 verification` / `### Phase-57 state-5 code-review` narratives, relocated at the phase-57 state-6 close-out when ROADMAP row `57` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 42 (the `### Phase-42 state-5 code-review (active phase)` narrative — phase 42 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-42 state-6 close-out when ROADMAP row `42` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 43 (the `### Phase-43 state-5 code-review (active phase)` narrative — phase 43 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-43 state-6 close-out when ROADMAP row `43` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 44 (the `### Phase-44 state-5 code-review (active phase)` narrative — phase 44 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-44 state-6 close-out when ROADMAP row `44` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 45 (the `### Phase-45 state-5 code-review (active phase)` narrative — phase 45 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-45 state-6 close-out when ROADMAP row `45` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 46 (the `### Phase-46 state-5 code-review (active phase)` narrative — phase 46 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-46 state-6 close-out when ROADMAP row `46` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 47 (the `### Phase-47 state-5 code-review (active phase)` narrative — phase 47 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-47 state-6 close-out when ROADMAP row `47` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 48 (the `### Phase-48 state-5 code-review (active phase)` narrative — phase 48 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-48 state-6 close-out when ROADMAP row `48` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 49 (the `### Phase-49 state-5 code-review (active phase)` narrative — phase 49 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-49 state-6 close-out when ROADMAP row `49` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 50 (the `### Phase-50 state-3 implementation (active phase)` narrative — phase 50 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-50 state-6 close-out when ROADMAP row `50` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 51 (the `### Phase-51 state-3 implementation (active phase)` narrative — phase 51 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-51 state-6 close-out when ROADMAP row `51` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 53 (the `### Phase-53 state-1 brainstorm` narrative — phase 53 kept a single active-phase Notes subsection at "state-1 brainstorm" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-1 subsection remained in `## Notes` at close — relocated at the phase-53 state-6 close-out when ROADMAP row `53` flipped `done`, together with the inline superseded state-3 narrative) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 54 (the `### Phase-54 state-1 brainstorm` / `### Phase-54 state-2 PLAN-write` / `### Phase-54 state-3 implementation` / `### Phase-54 state-4 verification` / `### Phase-54 state-5 code-review` narratives, relocated at the phase-54 state-6 close-out when ROADMAP row `54` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 55 (the `### Phase-55 state-5 code-review (active phase)` narrative — phase 55 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state individually superseded in place and relocated to STATE_HISTORY.md at its own transition], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-55 state-6 close-out when ROADMAP row `55` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 56 (the `### Phase-56 state-5 code-review (active phase)` narrative — phase 56 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state individually superseded in place and relocated to STATE_HISTORY.md at its own transition], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-56 state-6 close-out when ROADMAP row `56` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: one state per session; do not chain states. Phase 57 (`57-accesslog-h2-uh-no-healthy`) is CLOSED (state-6 close-out THIS session) — ROADMAP rows `00`-`57` ALL `done`. STATE has NO active phase (awaiting next planning). The next session runs the §5 state-0 next-phase PICK (a fresh `superpowers:brainstorming`) — a SEPARATE session (memory `closeout-and-pick-are-separate-sessions`); do NOT pick the next phase in this session.

> Historical Notes subsection for fully-closed phase 52 (the `### Phase-52 state-5 code-review (active phase)` narrative — phase 52 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2/3/4 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-52 state-6 close-out when ROADMAP row `52` flipped `done`, together with the standing 05.1/05.4 future-reviewer note it carried) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 41 (the `### Phase-41 state-5 code-review (active phase)` narrative — phase 41 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the §6.2-recon PIVOT from `%REQ_WITHOUT_QUERY%` to `%ROUTE_NAME%` landed at state-2; the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-41 state-6 close-out when ROADMAP row `41` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 40 (the `### Phase-40 state-5 code-review (active phase)` narrative — phase 40 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-40 state-6 close-out when ROADMAP row `40` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 39 (the `### Phase-39 state-5 code-review (active phase)` narrative — phase 39 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-39 state-6 close-out when ROADMAP row `39` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
> Historical Notes subsection for fully-closed phase 38 (the `### Phase-38 state-2 PLAN-write (active phase)` narrative — phase 38 used a single active-phase Notes subsection for its whole arc; the state-3/4/5 sub-state narratives were superseded in the four-top-section archive blocks per-session, so only this state-2 subsection remained in `## Notes` at close — relocated at the phase-38 state-6 close-out when ROADMAP row `38` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) and for parent phase 25 (the `### Phase-25 state-1 brainstorm` pick + recon-finding narrative, relocated at the 25.2 state-6 close-out when parent `25` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 26 (the `### Phase-26 state-1 brainstorm` pivot/rejected-alternatives/key-scoping narrative, relocated at the phase-26 state-6 close-out when row `26` flipped to `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 27 (the `### Phase-27 state-1 brainstorm` / `### Phase-27 state-2 PLAN-write` / `### Phase-27 state-4 verification` narratives + the now-consumed `### Phase-27 carry-forwards` [M26-1..M26-8] block, relocated at the phase-27 state-6 close-out when row `27` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 28 (the `### Phase-28 state-1 brainstorm` / `### Phase-28 state-2 PLAN-write` / `### Phase-28 state-3 implementation + state-4 verification` / `### Phase-28 state-5 code review` narratives + the now-consumed `### Phase-28 carry-forwards` [M27-1..M27-3] block, relocated at the phase-28 state-6 close-out when row `28` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 29 (the now-consumed `### Phase-29 carry-forwards` [M28-1..M28-3] block + the `### Phase-29 state-1 brainstorm` / `### Phase-29 state-2 PLAN-write` / `### Phase-29 state-3 implementation` / `### Phase-29 state-4 verification` / `### Phase-29 state-5 code review` narratives, relocated at the phase-29 state-6 close-out when row `29` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 30 (the `### Phase-30 carry-forwards` [M29-1/M29-2, which fed phase 30 but were NOT consumed — they continue as phase-31 carry-forwards] block + the `### Phase-30 state-1 brainstorm` / `### Phase-30 state-2 PLAN-write` / `### Phase-30 state-3 implementation` / `### Phase-30 state-4 verification` / `### Phase-30 state-5 code review` narratives, relocated at the phase-30 state-6 close-out when row `30` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 31 (the `### Phase-31 carry-forwards` [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 — open Minors from the phase-30 REVIEW.md that fed phase 31 but were NOT consumed; they continue as carry-forwards for the next phase that touches the differential hash-sweep driver / the config parser] block + the `### Phase-31 state-1 brainstorm` / `### Phase-31 state-2 PLAN-write` / `### Phase-31 state-3 implementation` / `### Phase-31 state-4 verification` / `### Phase-31 state-5 code review` narratives, relocated at the phase-31 state-6 close-out when row `31` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 32 (the `### Phase-32 carry-forwards` block — the open Minors that fed phase 32 but were NOT consumed: the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3; they continue as carry-forwards for the future phase that touches their surface — re-listed in `## Next expected skill` above alongside the 6 new phase-32 REVIEW.md Minors M32-1…M32-6), relocated at the phase-32 state-6 close-out when row `32` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phases 33+34 (the `### Phase-33 carry-forwards` block — the active-phase carry-forwards Notes that lived through phases 33 and 34: the now-CONSUMED 6 phase-32 REVIEW.md Minors M32-1…M32-6 [folded + landed at the phase-33 state-3] + the "Other still-live carry-forwards" list [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3 + the HTTP-filters-family (1)-(4) — those still-live ones re-listed in `## Next expected skill` above alongside the new phase-34 Minors M34-1/M34-2/M34-3 + the phase-33 M33-1/M33-2]), relocated at the phase-34 state-6 close-out when row `34` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 35 (the `### Phase-35 state-1 brainstorm` / `### Phase-35 state-2 PLAN-write` / `### Phase-35 state-3 implementation` / `### Phase-35 state-4 verification` / `### Phase-35 state-5 code-review` narratives, relocated at the phase-35 state-6 close-out when ROADMAP row `35` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 36 (the `### Phase-36 state-5 code-review` narrative — phase 36 used a rename-in-place Notes discipline, so the state-1..4 sub-state narratives were superseded in place each session and only the four top-section blocks were relocated per-session — plus the now-CLOSED detailed M35-1 carry-forward bullet [CONSUMED by phase-36 F2], relocated at the phase-36 state-6 close-out when ROADMAP row `36` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 37 (the `### Phase-37 state-3 implementation` active-phase narrative, relocated at the phase-37 state-6 close-out when ROADMAP row `37` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### HTTP-filters-family carry-forwards (from the `25.2` REVIEW.md - NOT yet consumed; weigh whenever the HTTP-filters family is re-entered)

> These were never obligations on the xDS phase 26; they remain live for whenever an HTTP-filters-family phase resumes.

- **(1) [non-goal - architectural]** Over-limit request bodies are FULLY buffered before the 413 rejection (no streaming watermark). Documented deferred non-goal; differentially byte-identical to Envoy for the bounded fixture sizes. Revisit only if a streaming `decode_data` watermark path is ever planned.
- **(2) [doc precision]** The BEHAVIOR_CONTRACT 413-row "verified byte-exact against v1.33.0" phrasing - fixture `0033` is H1-only; the H2 over-limit path is covered by the in-process synth-decorator backstop, NOT differentially. Consider narrowing the phrasing if an H2 over-limit fixture is ever added.
- **(3) [coverage]** No standalone `== effective route limit` unit assertion (the boundary is exercised only via the over/under probes).
- **(4) [coverage]** No differential at-limit (`==`) probe in `0033` (within-limit `<` and over-limit `>` are both covered; the exact boundary is not differentially probed).
- _(2)-(4) are cheap polish, (1) is architectural and only relevant to a future streaming phase._
</content>
