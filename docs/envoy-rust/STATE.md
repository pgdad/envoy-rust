# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase

**id:** **PHASE 37 (`37-rbac-url-path-condition`) — STATE-1 BRAINSTORM COMPLETE; STATE-2 PLAN-WRITE NEXT.** ROADMAP rows `00`-`36` `done`; `37` `in-progress`. The active phase is `37-rbac-url-path-condition`; the next session runs its state-2 PLAN-write (`superpowers:writing-plans`) — see `## Next expected skill`.
**slug:** `37-rbac-url-path-condition`
**directory:** `docs/envoy-rust/phases/37-rbac-url-path-condition/` (`SPEC.md` present — state-1 brainstorm output, spec-reviewed by a fresh subagent → APPROVED; `PLAN.md` + `PROGRESS.md` + `REVIEW.md` not yet written)

**status:** **PHASE 37 (`37-rbac-url-path-condition`) — STATE-1 BRAINSTORM COMPLETE.** This session (the `BOOTSTRAP_PROMPT.md` §5 state-1 new-phase pick + brainstorm, `superpowers:brainstorming`) picked phase 37, authored its `SPEC.md`, fired **ADR-0089** (the pick + minimum-viable scope + rejected alternatives), appended ROADMAP row `37` (`in-progress`, depends-on `06 07 10`), and advanced STATE AWAITING-NEXT-PLANNING → `37` state-1-complete/state-2-next (relocating the post-phase-36 "awaiting next planning" four top-section narratives to STATE_HISTORY.md per ADR-0035 / §4.1 inv. 9). **Phase-37 pick:** WIDEN the RBAC condition-TYPE surface (after phases 35/36 deepened the matcher-VALUE axis) with the **`url_path` Permission/Principal condition** on the EXISTING phase-10 `envoy.filters.http.rbac` filter — Envoy's `type.matcher.v3.PathMatcher` (`url_path: { path: { <StringMatcher> } }`) matching the request path with the `?query` STRIPPED — with **NO new `HttpFilterInstance` variant, NO new infrastructure, NO metadata store, NO producer chain** (reuses the phase-10 RBAC engine + decision matrix + `403`/`RBAC: access denied` 19B local reply, the 04.x `StringMatcher`/`SafeRegex` + the `regex` foundation [ADR-0021], and the phase-36 compile-RBAC-SafeRegex-at-lowering fallible-lowering path UNCHANGED). **`url_path` chosen as the cheapest-strong next leaf** — the ONLY not-yet-shipped RBAC condition that reads a request attribute the filter ALREADY holds (`FilterRequest.path`, `crates/envoy-filter/src/types.rs:28-30`), so it needs ZERO new connection-context plumbing; the connection-context conditions (`destination_ip`/`destination_port`/`source_ip`/`direct_remote_ip`/`remote_ip`/`requested_server_name`-SNI) all need the socket address / TLS SNI threaded into the filter and DEFER. **Differential** (fixture `0045-http-rbac-url-path`): an H1 `direct_response` listener + a plain `[rbac, router]` chain (no producer); match path → `200`+body / miss → `403`+`RBAC: access denied` 19B / the query-strip probe `/allowed?x=1` → `200`+body (the load-bearing distinction — Envoy strips the query, envoy-rust's existing route matcher compares the raw request-target byte-for-byte, so this probe proves genuine path-extraction, not a header alias) — all byte-exact, DETERMINISTIC, LOCALLY observable (NOT Linux-CI-only). **Minimum-viable scope per ADR-0089** (defers the connection-context conditions; `url_path` path-normalization beyond query-strip [§6.2 verifies the exact path-extraction extent — the fixture locks already-normalized paths, recording any residual gap as a named carry-forward]; the unanchored-SafeRegex partial-vs-full gap [M36-1 — weighed, anchored-locked, NOT consumed]; the non-string `ValueMatcher` variants + multi-segment metadata `path` + `MetadataMatcher.invert`; `shadow_rules`; per-route `typed_per_filter_config`; the rest of the Observability family). **§6.1 split projected NOT to fire** (~350–700 LoC / ~6–9 tasks; **ADR-0091 reserved**). **ADR-0090 reserved** for the §6.2 reconciliation at the state-2 PLAN-write. Spec-reviewed by a fresh subagent → **APPROVED** (all load-bearing claims verified against the tree — the query-strip differential is genuinely distinguishable [`hcm.rs:1513` `path == p` on the raw request-target] and the `url_path`-over-IP/port/SNI justification is correct [`FilterRequest` carries no socket address/SNI]; 1 advisory folded — the differential narrative leans on the route-matcher byte-for-byte compare, not a `:path`-header alias). **DECISIONS.md ledger head: ADR-0089** (count 89; next-available **ADR-0090**, reserved for the §6.2 reconciliation). ADR-0014 in force; ADR-0028 open; ADR-0049 governs config-validity. **ROADMAP rows `00`-`36` `done`; `37` `in-progress`.** The next session runs the state-2 PLAN-write — see `## Next expected skill`.

> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases + the active phase's prior sub-state pointers, incl. the phase-25 state-1 brainstorm pointer) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill

Per `BOOTSTRAP_PROMPT.md` §5 state 2 + `SKILL_ROUTING.md`: phase `37` (`37-rbac-url-path-condition`) has `SPEC.md` (state-1 brainstorm output, spec-reviewed → APPROVED) but NO `PLAN.md` → the next session is the **state-2 PLAN-write** (`superpowers:writing-plans`). It must: run the §6.2 LOCAL empirical reconnaissance against live `envoyproxy/envoy:v1.33.0` (THE key risk — the EXACT path Envoy matches `url_path` against: query-strip ONLY vs also fragment-strip / percent-decode / path-normalization [`//`,`/./`,`/../`,case], swept via `/allowed?x=1`, `/allowed#frag`, `/al%6Cowed`, `/allowed/../allowed`, `//allowed`, `/allowed/` against a `url_path: { exact: "/allowed" }` policy; the `PathMatcher` wire shape + `Principal`-`url_path` acceptance + the `safe_regex` `url_path` verdicts [anchored portable pattern] + the config-validity dispositions); fire **ADR-0090** (reserved) with the empirically-locked §A facts inline (no `[§6.2-PENDING]`); author `PLAN.md` (~6–9 TDD tasks); GATE: if `PLAN.md` > ~25 tasks OR > ~1500 LoC → split into `37.1`/`37.2` (ADR-0091 reserved; projected NOT to fire) + update ROADMAP/STATE + stop. Do NOT start the state-3 implementation this session (§5.1 one state per session). **Carry the open carry-forward Minors below into the phase that next touches each surface** (NONE blocks; M36-1 is directly on the phase-37 SafeRegex surface — weighed, anchored-locked, NOT consumed).

**Open carry-forward Minors (fold into the phase that next touches each surface; NONE blocks):**
- **M36-1** (phase-36 §6.2) — envoy-rust's `StringMatcher::matches` SafeRegex uses `regex::is_match` (PARTIAL/substring) but Envoy `safe_regex` is RE2 FULL match (anchored): UNANCHORED patterns diverge. PRE-EXISTING (phase 04.2), cross-cutting (the route-config header SafeRegex shares the path), masked by anchored fixtures. **Directly on the phase-37 surface** (`url_path` `safe_regex` rides the same code path) — phase 37 stays clear by LOCKING anchored patterns (`^…$`, partial==full) in the fixture/backstop; a proper full-match fix (`matcher.rs` SafeRegex anchoring) is DEFERRED to its own future phase (NOT consumed by 37 — folding it would touch the cross-cutting route-config SafeRegex path).
- **M36-2** (phase-36 state-5 REVIEW.md) — `ValueMatcher::matches(&str)` (`crates/envoy-config/src/matcher.rs`) is production-dead (only test callers after phase-36 F1's `matches_resolved` routing); the "Kept for the value-present call sites" doc-comment is inaccurate. Fix: soften the comment or fold the caller. Doc/cleanup-only; non-blocking.
- **M36-3** (phase-36 state-5 REVIEW.md) — present-but-empty correctness is enforced one filter UPSTREAM: RBAC's `eval_metadata` (`crates/envoy-filter/src/rbac.rs`) treats a stored `Some("")` as present; correct only because `header_to_metadata` writes nothing for an empty header. A one-line `eval_metadata` comment would harden it. Doc-only; non-blocking.
- **M34-1** — no unit test locks the same-namespace/key last-write-wins overwrite in `crates/envoy-filter/src/header_to_metadata.rs` (`multi_rule_composes` covers only distinct namespaces). Cheap test addition. **M34-2** — the A2 default-namespace is exercised only at the config-parse layer, not the filter-execution layer. **M34-3** — cosmetics (T5 redundant `use tempfile::tempdir`; BEHAVIOR_CONTRACT `A-missing`-vs-`A5` heading; fixture README `generate_request_id … load-bearing` overstatement).
- **M33-1** — unnecessary `.clone()` at `crates/envoy-http1/src/hcm.rs:1211` (the H1 record-build `dynamic_metadata` local is last-use → could move, as the H2 path does). **M33-2** (doc-pointer line drift in `command_operator.rs`/`record.rs`) — doc-only.
- the empty-`metadata_match`→fallback doc-comment (`crates/envoy-cluster/src/subset.rs`); M29-1/M29-2 + M30-1; M30-2 (`Cluster.lb_policy` serde-default); the phase-31 M-2/M-3; the HTTP-filters-family (1)-(4) buffer carry-forwards. (M35-1 is CLOSED — CONSUMED by phase-36 F2; the 6 M32 carry-forwards are CONSUMED.)

**Phase 37 is at state-1-brainstorm-complete — SPEC.md authored + spec-reviewed (APPROVED); the state-2 PLAN-write is next (NO code, NO verification this session). Phases 36 (`28199106154` @ `19c3fe9`, state-5 REVIEW.md approved `28203230624` @ `3e1346f`), 35 (`28125206968` @ `d1cf8a3`), 34 (`28062068794` @ `7c19803`) and 33 (`28021495767` @ `72fe40d`) are all closed + CI-GREEN — do NOT re-verify.** The local Docker-differential / unit false-REDs (`admin_config_dump_server_info` bridge-IP `192.168.65.2`; the `envoy-http2` `send_request_maps_h2_handshake_failure_to_typed_error` host-flake; `upstream_active_health_check_fixture` parallel-load timing — all PASS in isolation / are host-only) are documented, NOT regressions; CI is authoritative.

> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases + the active phase's prior sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit

**Phase-37 state-1 brainstorm — pick phase 37 + author SPEC.md + fire ADR-0089 + append ROADMAP row `37` + advance STATE (THIS commit):** the `BOOTSTRAP_PROMPT.md` §5 state-1 new-phase pick + brainstorm (`superpowers:brainstorming`). Picked phase 37 (`37-rbac-url-path-condition`) — WIDEN the RBAC condition-TYPE surface with the `url_path` Permission/Principal condition (Envoy `type.matcher.v3.PathMatcher`, query-stripped path match) on the EXISTING phase-10 RBAC filter; NO new `HttpFilterInstance` variant / infrastructure / metadata store / producer chain (reuses the phase-10 RBAC engine, 04.x `StringMatcher`/`SafeRegex` + ADR-0021 `regex`, and the phase-36 SafeRegex-at-lowering fallible path). Authored `docs/envoy-rust/phases/37-rbac-url-path-condition/SPEC.md` (spec-reviewed by a fresh subagent → APPROVED, 1 advisory folded), fired **ADR-0089** (the pick + minimum-viable scope + rejected alternatives — the connection-context conditions, the non-string-Value/metadata-vein items, `json_format`, jwt_authn `payload_in_metadata`), appended ROADMAP row `37` (`in-progress`, depends-on `06 07 10`), and advanced STATE AWAITING-NEXT-PLANNING → `37` state-1-complete/state-2-next (relocating the post-phase-36 "awaiting next planning" four top-section narratives to STATE_HISTORY.md per ADR-0035 / §4.1 inv. 9). **THIS docs-only state-1 commit** (SPEC + DECISIONS + ROADMAP + STATE + STATE_HISTORY; NO code change). `#![forbid(unsafe_code)]` holds; no new crate/dependency/fuzz-target/`HttpFilterInstance` variant projected (a small new `ConfigError` variant for an invalid `PathMatcher` is a §3 PLAN-write call). **DECISIONS.md ledger head: ADR-0089** (count 89; next-available **ADR-0090**, reserved for the §6.2 reconciliation). ADR-0014 in force; ADR-0028 open. The next session runs the state-2 PLAN-write (`superpowers:writing-plans`).

> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases + the active phase's prior sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated

2026-06-25 (phase-37 **STATE-1 BRAINSTORM COMPLETE; STATE-2 PLAN-WRITE NEXT**. Picked phase 37 [`37-rbac-url-path-condition`] via `superpowers:brainstorming` — WIDEN the RBAC condition-TYPE surface with the `url_path` Permission/Principal condition [Envoy `type.matcher.v3.PathMatcher`: `url_path: { path: { <StringMatcher> } }`, matching the request path with the `?query` STRIPPED] on the EXISTING phase-10 `envoy.filters.http.rbac` filter; NO new `HttpFilterInstance` variant / infrastructure / metadata store / producer chain [reuses the phase-10 RBAC engine + decision matrix + `403`/`RBAC: access denied` 19B reply, the 04.x `StringMatcher`/`SafeRegex` + ADR-0021 `regex`, and the phase-36 compile-RBAC-SafeRegex-at-lowering fallible path UNCHANGED]. `url_path` chosen as the cheapest-strong leaf — the ONLY not-yet-shipped RBAC condition reading an attribute the filter already holds [`FilterRequest.path`], so ZERO new connection-context plumbing [the IP/port/SNI conditions defer]. Differential = fixture `0045` [match → 200 / miss → 403 / query-strip `/allowed?x=1` → 200, the load-bearing distinction], byte-exact + DETERMINISTIC + LOCALLY observable. Authored `SPEC.md` [spec-reviewed → APPROVED, 1 advisory folded], fired **ADR-0089** [pick + scope + rejected alternatives], appended ROADMAP row `37` [`in-progress`, depends-on `06 07 10`], advanced STATE [relocating the post-phase-36 "awaiting next planning" four top-section narratives to STATE_HISTORY.md per ADR-0035 / §4.1 inv. 9]. §6.1 split projected NOT to fire [~350–700 LoC / ~6–9 tasks; ADR-0091 reserved]; **ADR-0090 reserved** for the §6.2 reconciliation. M36-1 [unanchored SafeRegex partial-vs-full] is directly on this surface — weighed, anchored-locked, NOT consumed; M36-2/M36-3 + older carry-forwards remain live; M35-1 CLOSED [consumed by phase-36 F2]. **ROADMAP rows `00`-`36` `done`; `37` `in-progress`.** `#![forbid(unsafe_code)]` holds; NO code change. **DECISIONS.md ledger head: ADR-0089** [count 89; next-available ADR-0090]. ADR-0014 in force; ADR-0028 open. The next session runs the state-2 PLAN-write [`superpowers:writing-plans`].)

> Historical `## Last updated` notes — every superseded last-updated note (all closed phases + the active phase's prior sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: one state per session; do not chain states. State-6 close-out commits touch ROADMAP.md + STATE.md only and carry no code changes; the next session writes PLAN.md for the next active phase per `superpowers:writing-plans`.
- The reviewer's R2 disposition decision (option (a) retroactive split of 05.1 vs option (b) free-standing post-05.1 sub-phase) was settled at the 05.1 state-6 commit in favor of option (b); 05.4 is the chosen sibling sub-phase. Future-reviewers reading STATE.md should understand that 05.1 is structurally closed at the preamble landing; 05.4 is a SIBLING under parent-05, not a child of 05.1; and the execution order ran 05.1 → 05.4 → 05.2 → 05.3, with 05.3 the closing sub-phase that flips parent-05 to `done`.

> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) and for parent phase 25 (the `### Phase-25 state-1 brainstorm` pick + recon-finding narrative, relocated at the 25.2 state-6 close-out when parent `25` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 26 (the `### Phase-26 state-1 brainstorm` pivot/rejected-alternatives/key-scoping narrative, relocated at the phase-26 state-6 close-out when row `26` flipped to `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 27 (the `### Phase-27 state-1 brainstorm` / `### Phase-27 state-2 PLAN-write` / `### Phase-27 state-4 verification` narratives + the now-consumed `### Phase-27 carry-forwards` [M26-1..M26-8] block, relocated at the phase-27 state-6 close-out when row `27` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 28 (the `### Phase-28 state-1 brainstorm` / `### Phase-28 state-2 PLAN-write` / `### Phase-28 state-3 implementation + state-4 verification` / `### Phase-28 state-5 code review` narratives + the now-consumed `### Phase-28 carry-forwards` [M27-1..M27-3] block, relocated at the phase-28 state-6 close-out when row `28` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 29 (the now-consumed `### Phase-29 carry-forwards` [M28-1..M28-3] block + the `### Phase-29 state-1 brainstorm` / `### Phase-29 state-2 PLAN-write` / `### Phase-29 state-3 implementation` / `### Phase-29 state-4 verification` / `### Phase-29 state-5 code review` narratives, relocated at the phase-29 state-6 close-out when row `29` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 30 (the `### Phase-30 carry-forwards` [M29-1/M29-2, which fed phase 30 but were NOT consumed — they continue as phase-31 carry-forwards] block + the `### Phase-30 state-1 brainstorm` / `### Phase-30 state-2 PLAN-write` / `### Phase-30 state-3 implementation` / `### Phase-30 state-4 verification` / `### Phase-30 state-5 code review` narratives, relocated at the phase-30 state-6 close-out when row `30` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 31 (the `### Phase-31 carry-forwards` [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 — open Minors from the phase-30 REVIEW.md that fed phase 31 but were NOT consumed; they continue as carry-forwards for the next phase that touches the differential hash-sweep driver / the config parser] block + the `### Phase-31 state-1 brainstorm` / `### Phase-31 state-2 PLAN-write` / `### Phase-31 state-3 implementation` / `### Phase-31 state-4 verification` / `### Phase-31 state-5 code review` narratives, relocated at the phase-31 state-6 close-out when row `31` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 32 (the `### Phase-32 carry-forwards` block — the open Minors that fed phase 32 but were NOT consumed: the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3; they continue as carry-forwards for the future phase that touches their surface — re-listed in `## Next expected skill` above alongside the 6 new phase-32 REVIEW.md Minors M32-1…M32-6), relocated at the phase-32 state-6 close-out when row `32` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phases 33+34 (the `### Phase-33 carry-forwards` block — the active-phase carry-forwards Notes that lived through phases 33 and 34: the now-CONSUMED 6 phase-32 REVIEW.md Minors M32-1…M32-6 [folded + landed at the phase-33 state-3] + the "Other still-live carry-forwards" list [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3 + the HTTP-filters-family (1)-(4) — those still-live ones re-listed in `## Next expected skill` above alongside the new phase-34 Minors M34-1/M34-2/M34-3 + the phase-33 M33-1/M33-2]), relocated at the phase-34 state-6 close-out when row `34` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 35 (the `### Phase-35 state-1 brainstorm` / `### Phase-35 state-2 PLAN-write` / `### Phase-35 state-3 implementation` / `### Phase-35 state-4 verification` / `### Phase-35 state-5 code-review` narratives, relocated at the phase-35 state-6 close-out when ROADMAP row `35` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 36 (the `### Phase-36 state-5 code-review` narrative — phase 36 used a rename-in-place Notes discipline, so the state-1..4 sub-state narratives were superseded in place each session and only the four top-section blocks were relocated per-session — plus the now-CLOSED detailed M35-1 carry-forward bullet [CONSUMED by phase-36 F2], relocated at the phase-36 state-6 close-out when ROADMAP row `36` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Phase-37 state-1 brainstorm (active phase)

- **What ran:** the §5 state-1 new-phase pick + brainstorm (`superpowers:brainstorming`). Read the cold-start contract (MISSION/STATE/ROADMAP/DECISIONS/BEHAVIOR_CONTRACT/SKILL_ROUTING), grounded the pick in the real RBAC/StringMatcher/path code via a fresh Explore subagent, picked phase 37, authored `SPEC.md`, spec-reviewed it via a fresh subagent (→ APPROVED), fired ADR-0089, appended ROADMAP row `37`, and advanced STATE. Docs-only; NO code change (§5.1 one state per session — did NOT chain into the state-2 PLAN-write).
- **The pick (ADR-0089):** the `url_path` RBAC Permission/Principal condition on the existing phase-10 filter (Envoy `type.matcher.v3.PathMatcher`, query-stripped path match). Widens the condition-TYPE axis (phases 35/36 deepened the matcher-VALUE axis). The cheapest-strong leaf: the only not-yet-shipped RBAC condition reading an attribute the filter already holds (`req.path`) → ZERO new connection-context plumbing. The query-strip semantic is the load-bearing differential richness (genuinely distinguishable from a raw-request-target match — verified `hcm.rs:1513` does `path == p` on the query-bearing target).
- **Reuse (no new infrastructure):** the phase-10 RBAC engine + decision matrix + `403`/`RBAC: access denied` reply; the 04.x `StringMatcher`/`SafeRegex` + the `regex` foundation (ADR-0021); the phase-36 fallible compile-RBAC-SafeRegex-at-lowering path (so a `safe_regex` `url_path` value is boot-fatal on a bad pattern, not a first-request panic). NO metadata store / producer chain (unlike phases 35/36 — `url_path` is self-contained, fixture chain is plain `[rbac, router]`).
- **Carry-forwards:** M36-1 (unanchored SafeRegex partial-vs-full) is directly on this surface — weighed, anchored-locked in the fixture/backstop, NOT consumed (folding it touches the cross-cutting route-config SafeRegex path). M36-2/M36-3 + M34-*/M33-* + older carry-forwards remain live; M35-1 CLOSED (consumed by phase-36 F2).
- **Next:** the state-2 PLAN-write (`superpowers:writing-plans`) — runs the §6.2 LOCAL reconnaissance against `envoyproxy/envoy:v1.33.0` (THE key risk: the exact `url_path` path-extraction extent — query-strip only vs further normalization), fires ADR-0090 with the locked §A facts inline, and authors `PLAN.md` (~6–9 TDD tasks; GATE: split into `37.1`/`37.2` if > ~25 tasks / > ~1500 LoC — ADR-0091 reserved, projected NOT to fire). Do NOT chain into state-3.

### HTTP-filters-family carry-forwards (from the `25.2` REVIEW.md - NOT yet consumed; weigh whenever the HTTP-filters family is re-entered)

> These were never obligations on the xDS phase 26; they remain live for whenever an HTTP-filters-family phase resumes.

- **(1) [non-goal - architectural]** Over-limit request bodies are FULLY buffered before the 413 rejection (no streaming watermark). Documented deferred non-goal; differentially byte-identical to Envoy for the bounded fixture sizes. Revisit only if a streaming `decode_data` watermark path is ever planned.
- **(2) [doc precision]** The BEHAVIOR_CONTRACT 413-row "verified byte-exact against v1.33.0" phrasing - fixture `0033` is H1-only; the H2 over-limit path is covered by the in-process synth-decorator backstop, NOT differentially. Consider narrowing the phrasing if an H2 over-limit fixture is ever added.
- **(3) [coverage]** No standalone `== effective route limit` unit assertion (the boundary is exercised only via the over/under probes).
- **(4) [coverage]** No differential at-limit (`==`) probe in `0033` (within-limit `<` and over-limit `>` are both covered; the exact boundary is not differentially probed).
- _(2)-(4) are cheap polish, (1) is architectural and only relevant to a future streaming phase._
