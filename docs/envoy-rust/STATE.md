# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase


**id:** **76.1** — `76.1-redirect-config-surface` (the CONFIG SURFACE slice of the `Route.redirect` action: the `RedirectAction` schema, the five-value `RedirectResponseCode` enum, the THIRD `RouteAction` variant, the widened three-way `Route` action-cardinality check, the two intra-`RedirectAction` oneof validators, both `Serialize` arms, and a `parse_bootstrap` fuzz corpus seed — NO runtime behaviour and NO new fixture). **§5 STATE-3 IMPLEMENTATION IS COMPLETE AT THIS COMMIT** — all **8** `PLAN.md` tasks are landed with per-task commits, `PROGRESS.md` exists and quotes verbatim command output per task, and the tree is ready for the state-4 gate. **NO new ADR was required** (the split is settled by ADR-0169; every design choice is either MEASURED upstream behaviour or an explicit ADR-0169 DECISION), so the ledger head stays **ADR-0170** and **ADR-0171 is the next free number**. `ConfigError` went **123 → 125** variants; `fuzz/.gitignore` **66 → 67** lines with **63 → 64** `!` lines and **63 → 64** tracked seeds; `envoy-config --lib` **649 → 680** tests and `envoy-http1 --lib` **186 → 187**, i.e. **+32 tests, 0 failed**. Net `crates/`+`tests/` LoC is **774** (`added=793 deleted=19`) against `PLAN.md`'s ≈515 projection — a **+50% overshoot living almost entirely in the test half**, exactly where the plan flagged the risk; it does NOT re-open the §6.1 gate (774 is 52% of the ~1500 threshold at 8 tasks against ~25, and the mid-execution sub-step trigger never fired). ROADMAP row `76.1` stays `in-progress`; parent row `76` stays `in-progress`; row `76.2` stays `planned`. Census is **107** rows / **104** `done` / **2** `in-progress` / **1** `planned`. **The mission is NOT complete and no `stop` file exists** (ADR-0167 DECISION 2).
**slug:** `76.1-redirect-config-surface` — **implementation COMPLETE and UNVERIFIED, i.e. §5 state 4**; it flips to `done` only after the state-4 gate, an approved `REVIEW.md` and the state-6 close-out. Parent: `76-route-redirect-action` (still `in-progress`; it flips to `done` only after BOTH `76.1` and `76.2` are `done`). Sibling: `76.2-redirect-runtime-fixture` (`planned`, depends on `76.1`, MUST NOT start before `76.1` is `done`) — and it INHERITS a named obligation from this session: T-C9 (`build_response_redirect_is_not_implemented_placeholder`) pins the honest `synth_501` placeholder and `76.2` MUST deliberately flip it.
**directory:** `docs/envoy-rust/phases/76.1-redirect-config-surface/` — now holds **`SPEC.md` + `PLAN.md` + `PROGRESS.md`**, i.e. §5 **state 4**. `REVIEW.md` does not exist. The sibling `docs/envoy-rust/phases/76.2-redirect-runtime-fixture/` still holds **`SPEC.md` ONLY** and is **NOT the active unit**. The parent `docs/envoy-rust/phases/76-route-redirect-action/` holds its original `SPEC.md`, still **BYTE-UNCHANGED** (ADR-0169 DECISION 5) — and **it has no `PLAN.md` and never will** (§6.2 step 1). Both `76.1/SPEC.md` and `76.2/SPEC.md` are also **BYTE-UNCHANGED** by this session (ADR-0170). The three FROZEN predecessor directories are `phases/75-headermatcher-absence-parity/`, `75.1-headermatcher-absence-engine-route/` and `75.2-headermatcher-absence-accesslog/` — all CLOSED; do not re-open, re-grade or edit them.

**status:** **SUB-PHASE `76.1` §5 STATE-3 IMPLEMENTATION COMPLETE (THIS session). ALL 8 `PLAN.md` TASKS LANDED, TDD-FIRST, ONE COMMIT EACH.** This session ran `superpowers:executing-plans` scoped to `76.1` on `HEAD == cf5cf85d0a2c477b90636b74fd93f6d36038f890`, SERIALLY in the main session per `PLAN.md`'s Execution stance (seven of eight tasks edit one file; Tasks 3-5 are a strict compile-order chain). **WHAT IT TOUCHED — exactly the FIVE files `PLAN.md` permits, and nothing else:** `crates/envoy-config/src/bootstrap.rs` (`+655 −10`), `crates/envoy-config/src/lib.rs` (`+28 −8`), `crates/envoy-http1/src/hcm.rs` (`+72 −1`), `crates/envoy-config/fuzz/.gitignore` (`+1`), and the NEW seed `crates/envoy-config/fuzz/corpus/parse_bootstrap/route_redirect_action.yaml` (`+37`). **NO `tests/` file, NO fixture, NO `BEHAVIOR_CONTRACT.md` line, NO `ci.yml` edit, no new crate, no new dependency, no new fuzz target, and no landed ADR edited.** **THE COMPILER-FORCED FOUR `match` SITES WERE ALL HANDLED IN ONE INDIVISIBLE TASK 3** (`Serialize for Route`, the SEPARATE `Serialize for RouteAction`, `validate_hcm`'s `match &r.action`, and `build_response_in`'s `match &route.action`); `Route::serialize`'s `len` was deliberately left unchanged and the FIFTH dispatch at `bootstrap.rs:4053` was LEFT ALONE. The runtime arm is the **honest** `BuildOutcome::Synth(synth_501(close), None)` placeholder pinned by T-C9, which `76.2` deliberately flips. **THREE MUTATION CHECKS WERE RUN, EACH IN ITS OWN SCRATCH `git worktree` WITH AN UNMUTATED CONTROL, AND TWO OF THEM CORRECTED `PLAN.md` — this is the most important thing a reviewer should read.** (1) **PLAN.md Task 4 Step 5's mutation is MISAIMED**: dropping `"route"` from the visitor's `unknown_field` NAME LIST leaves `all_five_preexisting_route_keys_still_parse` **GREEN** (that `&[…]` is only error-message text and does not stop the `"route" =>` key arm from matching); the correctly-aimed mutation REMOVES THE KEY ARM and REDs it with a genuine assertion failure. (2) **PLAN.md Task 6 Mutation B produced a THIRD outcome the plan did not anticipate — BOTH tests stayed GREEN — because the assertion `ser.contains("redirect:")` is VACUOUS: the FIELD name `port_redirect:` ends with the substring `redirect:`**, so a mis-keyed `Serialize` arm satisfied it. Both round-trip assertions were STRENGTHENED to anchor the key at column 0 (`ser.lines().any(|l| l.starts_with("redirect:"))`, commit `68e39b1`), after which Mutation B yields exactly the predicted one-RED/one-GREEN — the empirical proof the two `Serialize` impls are separate. (3) Mutation A (a bogus `port_redirect > 65535` bound) REDs T-A2 on the nose. **TWO FURTHER COVERAGE ADDITIONS, neither weakening anything:** `all_five_preexisting_route_keys_still_parse` gained a fifth-key block (the plan's body exercised only FOUR keys, so it did not actually deliver T-C5; `typed_per_filter_config` cannot ride the `route_action_yaml` scaffold because the 23 D3 validator rejects a per-filter key absent from the HCM's `http_filters` set, so it is asserted at `Route` level), and the new fuzz seed was added to the `fuzz_corpus_seeds_parse_or_reject_cleanly` cohort walk as well as its own test (house precedent carries seeds in BOTH). **LOCAL GATE REHEARSAL (NOT the §7.5 gate):** `cargo fmt --all -- --check` exit 0 with an EMPTY output file; `cargo build --workspace --all-targets` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0 with ZERO warning/error lines — **but the FIRST clippy green had 0 `Checking` lines and was REJECTED as fully cached**, so it was re-run forced via `touch` and produced **13** `Checking` lines, the ADR-0150 seam witness figure with `envoy-accesslog` ABSENT (no 14th line: the seam holds); `envoy-config --lib` **680 passed / 0 failed**; `envoy-http1 --lib` **187 passed / 0 failed**; `envoy-bin` REBUILT (`Compiling envoy-bin` present) so state 4's differential does not run a stale binary against a NEW config key. **`cargo test --workspace` and `cargo deny check` were deliberately NOT run — they are state 4's job** (§5.1; ADR-0127). **Two `PLAN.md` citations had drifted and were re-anchored on TEXT:** the `hcm.rs` test-module import is at **`:2353`**, not `:2361`. The `lib.rs` `pub use` rustfmt REFLOW happened as warned (materialising as `+8 −8` rather than the plan's `+29 −9` framing — same event, shape depends on where the inserted line breaks); it is **rustfmt's, never hand-wrapped**, and `fmt --check` is clean.

_Historical (the sub-phase-76.1 §5 STATE-2 PLAN-WRITE Active-phase `**id:**` + `**slug:**` + `**directory:**` + `**status:**` blocks + that block's own `_Historical_` pointer line — superseded by THIS sub-phase-76.1 §5 STATE-3 IMPLEMENTATION, which advances `76.1` from state 3 to state 4; relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this commit per ADR-0035. **The `**slug:**` line is relocated at a within-sub-phase state advance too**, because its parent/sibling status prose is superseded, which is why the relocation set here is **14** lines, matching ADR-0168 DECISION 6, ADR-0169 and ADR-0170.)_
> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases incl. the phase-51 state-3/4/5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill


**The next session runs the §5 STATE-4 VERIFICATION** (`superpowers:verification-before-completion`, per the `SKILL_ROUTING.md` state machine) **for SUB-PHASE `76.1`**, whose `SPEC.md`, `PLAN.md` AND `PROGRESS.md` all exist and whose implementation is COMPLETE but UNVERIFIED — that is state 4's unambiguous detection rule (`REVIEW.md` does not exist). **It is scoped to `76.1`, NOT to the parent `76` and NOT to `76.2`.** **It is a SEPARATE session and THIS implementation deliberately did NOT chain into it** (§5.1; ADR-0127 — the context that wrote the code must not be the one that grades it): `cargo test --workspace`, `cargo deny check`, the differential suite and the conformance suites were all left UNRUN, and no `REVIEW.md` was opened. **RUN THE FULL §7.5 GATE and quote every command's output into `PROGRESS.md`.** `PROGRESS.md` §7 names the seven commands and the adjudication rules; its §4 records which gates are already MET, which are VACUOUS and which are still OPEN. **Adjudicate `cargo test --workspace` with `--no-fail-fast` and a FULL-output redirect, NEVER `tail`**; run it 2-3× and DIFF the failing SET; classify any RED by ADR-0164's four-part test (assertion never reached + passes in isolation + absent from some sweep + untouched by the phase's surface), **never by membership in a list**. **A clippy or build green with ZERO `Checking`/`Compiling` lines is CACHED and is NOT evidence — force it with `touch`** (this session hit exactly that: the first clippy pass reported 0 `Checking` lines). **`cargo deny check` can RED on a fresh unrelated RustSec advisory** — that is a patch-bump, not a phase regression. `envoy-bin` is already rebuilt, so the differential will not run a stale binary against the NEW `redirect` config key. **The arithmetic identity: `local passed + failed == CI passed`; the last derived CI total was `binaries: 162 passed=2105 failed=0` and THIS PHASE ADDS 32 TESTS, so both figures MUST GROW.**

**SUB-PHASE `76.1` SCOPE AS BUILT** (full detail in `PROGRESS.md`; the design record in `76.1/SPEC.md`; the task-by-task plan in `76.1/PLAN.md`). **THE 8 TASKS, ALL LANDED:** (1) `RedirectResponseCode` — the five-value SCREAMING_SNAKE wire enum + `status() -> u16` (301/302/303/307/308), CamelCase variants + `rename_all` + `#[default]`, clippy-clean with NO `#[allow(non_camel_case_types)]`; (2) `RedirectAction` — eight fields, `deny_unknown_fields`; (3) `RouteAction::Redirect` + BOTH `Serialize` arms + the `pub use` re-export + the `hcm.rs` test import + the honest `synth_501` placeholder (T-C9) — ONE indivisible task because the new variant breaks FOUR `match` sites at once; (4) the `Route` visitor — accumulator, `"redirect"` key arm, SIX-name unknown-field list, three-way cardinality, widened `expecting`; (5) the two oneof validators + two `ConfigError` variants (`RedirectPathRewriteConflict`, `RedirectSchemeRewriteConflict`, 123 → 125); (6) end-to-end accept direction + both `Serialize` round-trips; (7) the `parse_bootstrap` corpus seed + its `!`-un-ignore line; (8) the local gate rehearsal + `PROGRESS.md`. **THE `Option`s ARE LOAD-BEARING AND ARE NOW LANDED AS SUCH — DO NOT "SIMPLIFY" THEM.** Upstream's oneofs are exclusive on FIELD PRESENCE, not value, so `https_redirect` is `Option<bool>` and `path_redirect`/`prefix_rewrite`/`scheme_redirect` are `Option<String>`, and the validators test **`.is_some()`** — never truthiness, never `!s.is_empty()`, never `.unwrap_or(false)`. T-R8 (`https_redirect: false` + `scheme_redirect: "ftp"` REJECTS) paired with T-A5 (`https_redirect: false` ALONE ACCEPTS) pins it in both directions; T-R9 does the same for `path_redirect: ""`. **`port_redirect` carries NO range bound** — `0` and `70000` both ACCEPT and `70000` round-trips verbatim (Mutation A proves the pin is live). **THE THREE-WAY CARDINALITY WORDING IS DELIBERATE:** `neither is present` kept VERBATIM from the landed message, `both are present` → `more than one is present`, with a catch-all `_` holding the match at five arms; the three pre-existing tests over these strings assert substring containment of `direct_response`, `route` and `exactly one` only, so **all three stayed GREEN and NONE was edited**. **DIFFERENTIAL SURFACE = THE REJECT DIRECTION** (seven MEASURED upstream rejections J1-J7, now boot-fatal here) **plus regression-equivalence over the 85 existing fixtures**, which state 4 adjudicates. **NO new fixture: gate (a) is VACUOUSLY met and `PROGRESS.md` §4 SAYS SO EXPLICITLY** so a reviewer does not read the absence as an oversight. **NO new fuzz target, so gate (d) needs NO `ci.yml` edit** — it is met by the existing `parse_bootstrap` run with the new seed confirmed tracked via `git ls-files`. **NON-GOALS, each still deliberately boot-fatal:** ALL runtime behaviour (that is `76.2` — the pure `location`-builder, the dedicated `synth_redirect` which must NOT reuse the `content-type`-emitting `synth_with`, the `canonical_reason` 303/307/308 gap, the real dispatch arm, the `prefix_rewrite` `:path` mutation, fixture `0086`, the contract bank and the parent close); `regex_rewrite` inside `redirect` (boot-fatal via `deny_unknown_fields`, which is HOW J2 rejects here — a different mechanism, the same VERDICT); the `route:`-arm rewrite family; `internal_redirect_policy`; `non_forwarding_action`; `weighted_clusters`; `cluster_header`; route-level `timeout`; route/vhost/route-config-level header mutation; and every open carry-forward including **CF-76-1**.


**Standing traps** (enduring). **STATE: SUB-PHASE `76.1` IS THE ACTIVE UNIT — `76.1-redirect-config-surface`, at §5 STATE 4; the parent `76` was OPENED at the §5 state-0/1 pick (ADR-0168), SPLIT at its state-2 PLAN-write (ADR-0169) and is STILL `in-progress`. `76.1`'s directory holds `SPEC.md` + `PLAN.md` + `PROGRESS.md`; `REVIEW.md` DOES NOT EXIST and the NEXT session is the §5 STATE-4 VERIFICATION via `superpowers:verification-before-completion` running the FULL §7.5 gate, a SEPARATE session (§5.1; ADR-0127). ROADMAP census is 107 rows / 104 `done` / 2 `in-progress` / 1 `planned`. The mission is NOT complete; do NOT create a `stop` file.** **[NEW AT THE 76.1 STATE-3 IMPLEMENTATION — FIVE findings, every one a real failure or a real false-green caught by EXECUTING rather than reading.]** **(1) A PLAN-SPECIFIED MUTATION CAN NAME A DIFFERENT CELL FROM THE ONE THE TEST PINS, AND THEN "PASSES" HARMLESSLY.** `PLAN.md` Task 4 Step 5 says to drop `"route"` from the `Route` visitor's accepted key list to RED `all_five_preexisting_route_keys_still_parse`; MEASURED, the test stays **GREEN**, because that `&[…]` is only the `unknown_field` ERROR-MESSAGE text and removing a name from it does not stop the `"route" =>` KEY ARM from matching. The correctly-aimed mutation REMOVES THE KEY ARM. **A mutation that leaves the fixture green usually means the MUTATION is misaimed — but confirm which cell it names before believing either verdict.** **(2) A SUBSTRING ASSERTION ON A YAML KEY CAN BE SATISFIED BY A FIELD NAME THAT ENDS WITH IT.** `PLAN.md` Task 6 Mutation B expected one RED and one GREEN; MEASURED, **BOTH stayed GREEN** — a third outcome the plan did not anticipate — because the assertion was `ser.contains("redirect:")` and the FIELD name **`port_redirect:` ends with `redirect:`**. Anchor a key assertion to column 0 (`ser.lines().any(|l| l.starts_with("key:"))`). Fixed at `68e39b1`; both round-trip tests now carry a comment saying why, so nobody "simplifies" it back. **A mutation's THIRD possible outcome is that it exposes a weak ASSERTION rather than a wrong IMPL.** **(3) `cargo clippy` CAN RETURN exit 0 WITH ZERO `Checking` LINES — THAT GREEN IS FULLY CACHED AND IS NOT EVIDENCE.** It happened on the first Task-8 rehearsal pass. `touch` the changed files and re-run; the honest figure here is **13** `Checking` lines (clippy prints `Checking`, build/test print `Compiling`). **(4) A `PLAN.md` TEST BODY CAN UNDER-DELIVER ITS OWN STATED OBLIGATION.** Task 4's `all_five_preexisting_route_keys_still_parse` exercises only FOUR `Route` keys, so T-C5 (`typed_per_filter_config`) was not actually covered; it cannot ride the `route_action_yaml` scaffold because the 23 D3 validator rejects a per-filter key absent from the HCM's `http_filters` set, so assert it at `Route` level via a direct `serde_yaml::from_str::<Route>`. **READ WHAT A PLAN'S TEST ASSERTS, not what its NAME claims.** **(5) A NET-LoC PROJECTION CAN UNDERSHOOT BY 50% IN ITS TEST HALF.** Measured **774** net against a ≈515 projection; the implementation half was near-exact (the plan MEASURED it in a worktree) and the whole overshoot is in the house-style individually-named tests. It does not re-open §6.1 (774 = 52% of ~1500 at 8 tasks), but **do not treat a plan's test-half LoC figure as reliable.** **`DECISIONS.md` is NOT chronological AND is too large to Read whole** — **4038** lines but ~310 000 tokens (single-line ADR blocks), so a whole-file Read is REFUSED by the tool; chunk it by `grep -n '^## ADR-NNNN'` + offset/limit (memory `decisions-md-not-chronological`). It runs ascending `ADR-0001..0100`, then a NEWEST-FIRST block. **Ledger head is ADR-0170; next available ADR-0171 — state the head as MEASURED, never inherited** (re-derive with `grep -o '^## ADR-[0-9]\{4\}' … | sort -t- -k2 -n | tail -1`). Note `grep -c '^## ADR-'` returns **167** because it also counts the `## ADR-NNNN: <title>` TEMPLATE near line 10, and the numbers are NOT contiguous — **never derive the next free number from a count.** `ROADMAP.md` (**204** lines but ~95 000 tokens) / `BEHAVIOR_CONTRACT.md` (**3645** lines) / `STATE_HISTORY.md` all exceed the single-read limit — chunk them. **`STATE.md` itself is ~92 000 characters and a full Read is TRUNCATED at ~53 lines — page it with offset/limit.** **`STATE.md` top-section blocks are single multi-thousand-character lines.** An Edit whose `old_string` stops mid-line silently leaves the superseded tail glued on — span the FULL line or script it, then grep the old phrase (must be 0) (memory `state-md-blocks-are-single-lines-edit-hazard`). **MEASURE a line before editing it; never inherit a figure and never assume a range.** This Standing-traps line's MEASURED series is `da06059` **14653** → `1da0662` **16577** → `b369f9c` **20519** → `0c541d7` **23222** → `c02f31b`/`9bd18bd` **25930** → `361a005` **30754** → `533dceb` **31738** → `cf5cf85` **34110**, TRIMMED once at the 75.2 close-out under ADR-0160. **The `cf5cf85` line's OWN prose claimed 33948 — it MEASURED 34110. MEASURE it yourself every time.** **ADR-0035 relocation checks are DELTA-based AND PER-FILE:** a relocated line must go to **0** in `STATE.md` and **+1** in `STATE_HISTORY.md`; a COMBINED count is invariant by construction and FALSE-PASSES (memory `state6-relocation-check-must-be-delta-based`). Also assert §4.1(9)(d) directly (relocated multiset == `old STATE.md − new STATE.md`) and that 0 history lines were lost. **CAPTURE THE SUPERSEDED LINES FROM A PRE-EDIT BACKUP, BEFORE MUTATING ANYTHING** — capturing after rewriting archives the NEW text and fails the check (memory `adr0035-capture-superseded-lines-before-mutating`). **Back BOTH files up first**, and match `STATE_HISTORY.md` archive headers by **EXACT equality** — a prefix match hits per-phase variants and aborts AFTER `STATE.md` was rewritten (memory `state-history-archive-headers-need-exact-match`). Run the check against the FULL superseded set **INCLUDING the `### Doctrine reminders` §5.1 bullet, which is NOT exempt**; **the set is 14 lines at a PICK, at a SPLIT, at a CLOSE-OUT *and* at a within-sub-phase state advance alike** (this state-3 commit measured 14 again: id/slug/directory/status + the Active-phase `_Historical_` pointer, the next-skill pointer + scope block + Standing-traps + its `_Historical_` pointer, last-commit + its pointer, last-updated + its pointer, and the doctrine bullet). Per **ADR-0160** the enduring Standing-traps line and the doctrine bullet are rewritable only after relocating the superseded text verbatim. **Any ROADMAP row edit preserves all 6 cells and escapes `\|`.** Rows `36`/`38`/`39`/`52`/`54`/`66`/`70` already carry unescaped pipes — **do NOT "fix" them** (append-only). Only `66` and `70` actually MISCLASSIFY under a naive `awk -F'|' $5` (their stray pipe precedes the status column); splitting on `' | '` WITH the spaces is robust, **status is field 4, not 5**, and yields the correct census (memory `roadmap-rows-need-escaped-pipes`). A close-out flips the STATUS CELL ONLY (memory `roadmap-closeout-flips-status-cell-only`); **a state-3 commit flips NO status cell.** **`next-prompt.txt` is gitignored** (`.gitignore:13`) — refresh it on disk, do NOT `git add` it, and do not copy landed narratives that list it among a commit's contents. **The ubiquitous "MISSION.md §9 feature families" citation is FALSE** — `MISSION.md` has no §9; the 11 families live at `ROADMAP.md:58`. Verify there; never "fix" archived occurrences. **Adjudicate any sweep with `--no-fail-fast` + a FULL-output redirect, NEVER `tail`** (memory `never-pipe-verification-runs-through-tail`; a bare `cargo test --workspace` aborts at the first failing BINARY, and `cargo build | tail -5` drops the `Compiling <crate>` line and fakes a stale-binary FALSE PASS). Run it 2-3× and DIFF the failing SET. **`0 passed; N filtered out` is NOT a pass** and `error: no test target named …` exits 101 exactly like a real RED — assert on the `N passed` count, never the exit code (memory `cargo-test-p-name-false-green-filtered-out`); **read the failure TEXT, always.** **THE CI-TOTAL RECIPE IS NOT SELF-VALIDATING, AND ITS AWK FIELDS ARE 4 AND 6.** `grep -o 'test result: ok\. …'` discards `test result: FAILED.` lines before `awk` sees them, so its `failed=0` is true BY CONSTRUCTION — use `grep -oE 'test result: (ok|FAILED)\. …'` and assert the binary count separately. **`grep -o` strips the CI log's job/timestamp prefix, so the counts land at `$4`/`$6`; using `$5`/`$7` returns a clean-looking `binaries: 162 passed=0 failed=0`, a VACUOUS result — disbelieve a zero.** Last derived: `binaries: 162 passed=2105 failed=0`; **this phase ADDS 32 tests, so both figures MUST GROW.** **The same blindness bit a SECOND recipe** — `PROGRESS.md`'s `[a-z]+` cannot match `FAILED` (carried-forward finding M-4); use `(ok|FAILED|ignored)`. **THE LOCAL HOST-FLAKE SET IS A STABLE CORE OF FIVE PLUS AN *OPEN-ENDED* STARTUP-RACE TAIL WHOSE SIZE CARRIES NO SIGNAL (ADR-0164).** **CORE (fails DETERMINISTICALLY in isolation — that determinism IS the environmental signature):** the four `access_log_*_upstream_reset` binaries (`TcpCloseBackend` IPv6-unreachable) and `admin_config_dump_server_info` (the `192.168.65.2` bridge-IP family). **TAIL: every member PASSES in isolation — the OPPOSITE signature.** Measured membership `1 / 0 / 2`, then `1 / 15 / 0`, union **SIXTEEN**; the core stayed at exactly five and was the exact INTERSECTION of all three sweeps. **Adjudicate any RED by ADR-0164's four-part test** (assertion never reached + passes in isolation + absent from some sweep + untouched by the phase's surface), **never by membership in a list**. **A WHOLE-SWEEP STARTUP WAVE CAN PREEMPT A CORE MEMBER'S NORMAL ASSERTION — classify by ISOLATION, never by failure TEXT.** The arithmetic identity to re-check every sweep: `local passed + failed == CI passed`. **RE-DERIVE EVERY INHERITED CENSUS — FIFTEEN have now been caught wrong or incomplete.** Re-confirmed at this state-3 implementation: **85** fixture dirs and **85** differential test files (unchanged — this sub-phase adds none), **107** ROADMAP rows, **21**-line `known-failures.txt`, **5** fuzz targets across FIVE crates, `fuzz/.gitignore` **67** lines / **64** `!` lines / **64** tracked seeds, **125** `ConfigError` variants, **3** `HEADER_ALLOW_LIST` entries, `bootstrap.rs` at **21 042** lines. **NEWLY REFUTED THIS SESSION: the `hcm.rs` test-module import is at `:2353`, not the `:2361` `PLAN.md` cites.** **Treat every number you inherit as suspect anyway.** **Beware VACUOUS greps.** A fixture census against `tests/differential/tests/fixtures/` returns a clean-looking **0** (fixtures live at `tests/fixtures/`); a fuzz-target census against `crates/envoy-config/fuzz/fuzz_targets/` returns **1** (the five span FIVE crates); the CI-total awk field error returns `passed=0`. **A case-INSENSITIVE grep for `response_code` in `bootstrap.rs` returns 11 hits that are ALL the access-log literal `%RESPONSE_CODE%`.** **And the opposite:** a grep can legitimately return >0 because a record QUOTES the thing it supersedes. **Adjudicate by LINE and by FILE, never by COUNT.** **DERIVE fixture→test-binary maps FROM THE TREE, never from the fixture name** (`grep -rl "tests/fixtures/00NN" tests/differential/tests/*.rs`): `0079`/`0080` are `access_log_and_filter`/`access_log_or_filter`, which a name-guessed grep misses. **Likewise derive a failing test's BINARY from its panic line.** **33 test-binary NAMES are duplicated between `tests/differential/tests/` and `crates/envoy-bin/tests/` — always pass `-p differential` (or the right `-p`), never a bare `--test <name>`.** **A SUBAGENT FINDING IS A CLAIM, NOT A RESULT — RE-VERIFY EVERY ONE ON DISK.** Prefer MEASURING a hand-derivation over quoting it. **A `_Historical_`/"see below" POINTER MUST POINT AT SOMETHING ON DISK** (D-3.4). **Mutation-check hygiene:** any tree-MUTATING probe belongs in its OWN scratch `git worktree` created `--detach` at HEAD (memories `mutation-checks-collide-with-parallel-subagents`, `worktree-subagents-get-stale-base` — a parallel agent's `git checkout` can silently revert an in-place mutation; FOUR sibling `.claude/worktrees/agent-*` worktrees were live this session); commit the task CLEAN first and mutate a worktree at that commit, so the main tree is never mutated; re-grep the mutation as STILL PRESENT after the run; grep `Compiling <crate>` to prove a forced rebuild (**`cargo clippy` prints `Checking`, NOT `Compiling`**); and ALWAYS run an UNMUTATED CONTROL from the SAME worktree — a RED that never reached an assertion is NOT evidence. **`git worktree remove` while the shell's cwd is inside it leaves the shell with no cwd and the NEXT command dies with `fatal: Unable to read current working directory` — benign, but re-verify the removal from the repo root.** **Remove only YOUR OWN worktree.** **PLAN.md example code trips clippy** — the plan's literal Rust repeatedly fails the plan's OWN `clippy -D warnings` and `cargo fmt --check` and can cite helper fns that don't exist; catch it in the state-3 pre-flight. (This plan was PRE-FLIGHTED by execution and its Rust transcribed CLEAN — the pre-flight method works and is worth repeating.) **A §5.2 re-entry usually pins ALREADY-correct code**, so the new test passes immediately — honour TDD's RED with a mutation check and record THAT as the RED evidence. **Mechanical fan-out scripts corrupt doc comments** — grep a commit's `+` lines for `///`. **THE 75.1 ENGINE IS LANDED AND IS THE PREMISE OF EVERYTHING ON THIS SURFACE — do NOT re-derive, revert or "simplify" it.** `HeaderMatcher::matches` (`crates/envoy-config/src/matcher.rs`) is an exhaustive tuple `match (&self.mode, value)` whose `(_, None) => return false` arm sits **AFTER** the `PresentMatch` arm and **BEFORE** every value arm, closed by an XOR with `invert_match`. The rule in full is `BEHAVIOR_CONTRACT.md` Phase 75 §A; `present_match: false` means **"the header must be ABSENT"**. **`BEHAVIOR_CONTRACT.md` §D IS CORRECTED — do NOT "restore" the old wording and do NOT re-raise I-1.** The arm ORDER is guarded in-process AND cross-proxy. **FOUR ENGINE MUTATIONS ARE ON RECORD AND THEY HIT DIFFERENT CELLS — never substitute one for another.** **TWO CONFLATION TRAPS — do NOT unify them** (`BEHAVIOR_CONTRACT.md` Phase 75 §E and §F): **`HeaderMatcher.present_match`** vs **`ValueMatcher.present_match`** (RBAC / access-log metadata, fixture `0044`), where `present_match: false` NEVER matches; and **`HeaderMatcher.invert_match`** vs **`MetadataMatcher.invert`** (CF-74-1) — MEASURED accepted-but-INERT upstream, so it stays boot-fatal here. **ACCESS-LOG FIXTURE-AUTHORING RULE, standing: EVERY probe in an `http1_access_log_byte_exact` fixture must carry a DISTINCT `path:`** (`BEHAVIOR_CONTRACT.md` Phase 75 §G, **whose rule binds EXISTING fixtures too**). **DO NOT collapse `0084`/`0085` back to a shared path.** **`0078`/`0079`/`0080` STILL carry that blindness (CF-75-4) — recorded, do NOT fix it opportunistically** (§6.3). **`%REQ(<GATING-HEADER>)%` IS BOOT-FATAL**; **`%REQ(:PATH)%` IS allow-listed.** **The byte-exact access-log driver takes ONE log file per side.** **Never weaken a fixture; never trim `known-failures.txt`** (21 lines — this host scores h2spec 3.5/2 as PASS where CI does not). **Do NOT add `on_header_missing` to fixtures `0081` OR `0082`** (ADR-0155 PV-6). **`0081` has THREE probes and expects TWO logged lines, byte-distinct so ORDER is pinned.** Do NOT disturb the **38** existing access-log fixtures. **THE h2spec GATE IS *NOT* VACUOUS IN CI — SETTLED (ADR-0163), do NOT re-raise it.** Locally it self-skips silently, so a local green needs `--nocapture`. **CF-75-3:** `.github/workflows/ci.yml:67` runs `cargo test --workspace` with NO `--no-fail-fast`, and the file has ZERO `continue-on-error` and ZERO `if:`, so a RED at or before the Docker pre-pull erases the whole test step and a RED test also loses `cargo deny check`; **owner is its OWN phase — do NOT widen into it.** **CF-75-6 IS OPEN and ACTIVELY FLAKY IN CI** — the `envoy-bin` fatal-startup EPHEMERAL-PORT-REUSE family: FOUR files (`crates/envoy-bin/tests/xds_file_based_{cds,eds,lds,rds}.rs`), **17** `assert_fatal_startup` call sites, plus 5 further same-class sites; root cause is the reserve-then-drop `reserve_port()` at `crates/envoy-bin/tests/common/mod.rs:22-29`, so any parallel bind flips a dead process's freed port to `Ok`. **IF IT REDS YOUR COMMIT: read the failure TEXT, measure the executable diff, and RERUN THE SAME SHA — do NOT weaken a test and do NOT trim `known-failures.txt`.** **CF-75-2 is OPEN and OUT OF SCOPE:** upstream COMMA-JOINS duplicate header values (single comma, NO following space, OWS-trimmed, seen by every value mode, preceding the numeric parse, `cookie` not special-cased) while envoy-rust matches only the FIRST occurrence; the fix is SINGLE-SITE at `crates/envoy-config/src/matcher.rs:40-43` with H2 pre-collapsing `host` at `crates/envoy-http2/src/request.rs:65-74` — do NOT "fix" it here. **It is recorded in THIS FILE, *NOT* in `BEHAVIOR_CONTRACT.md`.** **CF-72-2 and CF-75-1 are BANKED, NOT fixed.** **CF-74-6:** the wrapped `match_if_key_not_found: { value: <bool> }` spelling is accepted AND honored upstream but BOOT-FATAL here — a documented REJECT-direction gap; **do NOT "fix" it.** **CF-76-1 IS OPEN and OUT OF SCOPE FOR BOTH SUB-PHASES:** upstream STRIPS the query before route path matching — `match: { path: "/exact" }` MATCHES `/exact?q=1` (201) and `/exact?` (201) but NOT `/exact%3Fq=1` (404) — while envoy-rust compares the RAW target byte-for-byte (`route_matches` does `path == p` at `crates/envoy-http1/src/hcm.rs`, and `codec.rs:26-28` says "byte-for-byte (no normalization)"; H2 inherits it). SILENT, PRE-EXISTING, on the most-exercised surface in the project, pinned by NO fixture. **Do NOT fix it opportunistically** (§6.3); `76.2` stays clean of it by using `prefix:`-matched routes for query-bearing probes. **The ADR-0150 seam must keep holding:** `envoy-accesslog` has ZERO workspace deps (only `tokio`, `bytes`, `tracing`, `thiserror`) and MUST NOT depend on `envoy-config`; matchers cross as injected **trait objects**; `LogFilter` has NO `Eq`/`PartialEq`. **The `13 Checking` figure is the seam's witness — a 14th line would mean the seam had been BREACHED** (re-measured 13 this session on a forced envoy-config-touching re-run, with `envoy-accesslog` ABSENT from the list: the seam holds). **Do NOT revert landed work** — phases `00`-`74`, NOR sub-phases `75.1`/`75.2`, NOR the parallel workstream's `envoy-http2` header-list bound. Never add `rbac` to `is_terminal_network_filter`; never reject a top-level `filters: []` sink list; preserve ADR-0016 + ADR-0124 + ADR-0131 + ADR-0133 + ADR-0136/0137 + ADR-0139 + ADR-0140-0143 + ADR-0144-0149 + ADR-0150 through **ADR-0170**. **ADR-0028 is NOT lifted.** The phase-73 `< 2` rejection on `and_filter`/`or_filter { filters }` is a composition-arm PGV rule, **NOT** the top-level `filters: []` sink-list rule. **PHASE-76 SEMANTICS, all MEASURED against `envoyproxy/envoy:v1.33.0` and now half-landed.** **THE `RedirectAction` ONEOFS ARE EXCLUSIVE ON FIELD PRESENCE, NOT VALUE** — `https_redirect: false` + `scheme_redirect: "ftp"` REJECTS, `https_redirect: false` ALONE ACCEPTS, `path_redirect: ""` + `prefix_rewrite` REJECTS. The landed model is `Option<bool>` / `Option<String>` with `.is_some()` validators; **a `#[serde(default)] bool` would lose presence and mint a NEW divergence — T-R8 is the test that catches it.** **`port_redirect` has NO PGV bound** — `0` and `70000` both ACCEPT and `70000` renders verbatim; do NOT add `1..=65535`. **SEVEN MEASURED REJECTIONS J1-J7** (`path_redirect`+`prefix_rewrite`; `path_redirect`+`regex_rewrite`; `redirect`+`route`; `redirect`+`direct_response`; `scheme_redirect`+`https_redirect`; unknown `response_code` name; NUMERIC `response_code`), all now boot-fatal here. **Error TEXT is NOT part of the equivalence contract** — match the VERDICT only. **FOR `76.2`:** **`location` is NOT on the `HEADER_ALLOW_LIST`** (exactly THREE entries — `server`, `date`, `x-envoy-upstream-service-time` — at `tests/differential/src/lib.rs:1173-1181`), and `diff_headers` compares every non-allow-listed name value-exact; that is WHY fixture `0086` needs no new harness machinery, and **NEVER add `location` to that list** or the entire witness is silently vacated. **A REDIRECT RESPONSE HAS NO `content-type`**, so the redirect arm MUST NOT reuse the shared `synth_with` (`crates/envoy-http1/src/hcm.rs`), which always emits it. **`canonical_reason` (`crates/envoy-http1/src/response.rs`) IS MISSING 303/307/308** — they fall through to `_ => "OK"`, so a 303 would emit `HTTP/1.1 303 OK`, and **the differential fixture CANNOT catch it** (the harness parses the status CODE only) — it needs an IN-PROCESS pin. **THE AUTHORITY IN `location` COMES FROM THE `Host` HEADER, NOT THE SOCKET**; `host_redirect` DROPS the original port, a scheme-only change PRESERVES it, and `port_redirect` overrides both WITHOUT normalising a redundant `:443`. **The query is PRESERVED by default even when `path_redirect` replaces the path wholesale**; `strip_query: true` drops it. **`%RESPONSE_CODE_DETAILS%` for a redirect is `direct_response`** — the string envoy-rust ALREADY emits, so add NO new detail string; **`%RESPONSE_FLAGS%` is `-`.** **`prefix_rewrite` MUTATES the logged `:path` while `path_redirect` does NOT.** **H2 has NO route resolver of its own**, so ONE dispatch arm serves both codecs — and a bug there hits both. **`Http1ProbeList` ABORTS AT THE FIRST FAILING PROBE**, so one red run names ONE probe; cite a second fixture rather than claiming two cells from one run. **PREFIX-OVERLAP SILENTLY SHADOWS A PROBE** (a recon cell was lost when `prefix: "/scheme"` preceded `prefix: "/schemehost"`), so every probe in `0086` must select a DIFFERENT route AND carry a DISTINCT `path:`. **T-C9 (`build_response_redirect_is_not_implemented_placeholder`, `crates/envoy-http1/src/hcm.rs`) PINS THE HONEST `synth_501` PLACEHOLDER AND `76.2` MUST DELIBERATELY FLIP IT** — it is a named test precisely so the replacement is visible rather than silent. **This host's Docker BIND MOUNTS are STALE-CACHED** — use a FRESH FILENAME/DIRECTORY for every config revision (memory `docker-bind-mounts-stale-cached-on-this-host`). **`--volumes-from` does NOT retrieve a stopped container's `/tmp`** — use `docker cp`. **Port-map with `docker -p`, NOT `--network host`.** **A backend-reachability probe needs the backend as a SIBLING container on a shared docker network** (`host.docker.internal` is IPv6-only). **This host has virtiofs and no inotify**, so bind-mount file-watch/reload tests are native-CI-authoritative. **Backend-routing fixtures go RED locally** because this host routes the backend via `192.168.65.2`, not the allow-listed `192.168.65.254`/`172.17.0.1` — CI is authoritative. **A `redirect` fixture is backend-free and therefore FULLY verifiable locally — a deliberate property of this phase's pick.** **Upstream Envoy will NOT create an access-log directory**, and flush is TIMER-driven (~10 s). **YAML 1.1:** an unquoted `cluster: y` under `node:` parses as boolean `true`. **`--mode validate` probes wire-shape networking-free** but CANNOT answer runtime matching semantics or whether a field is INERT — **it is the cheapest recon tool you have.** **`cargo build -p envoy-bin` before ANY local differential** (the harness runs `target/debug/envoy-bin`, not release — and `76.1` ADDED a config key, so a stale binary REDs every fixture with a bogus `unknown field 'redirect'`; this session already rebuilt it). **A MASS differential RED** (`client error (Connect)`, dozens at once) = the Docker daemon down (`sudo setfacl -m u:esa:rw /dev/kvm && systemctl --user restart docker-desktop`) — **but a mass wave of `never became accept-ready` with the daemon UP is CONTENTION.** **A ~1-3 s green on a backend-free fixture is NORMAL**, not a silent skip. **`cargo fuzz` runs from the CRATE dir, not the repo root, and takes a DIRECTORY, not a file**; a NEW fuzz target is NOT auto-discovered (this phase adds none, so gate (d) needs no `ci.yml` edit); a NEW corpus seed needs an explicit `!`-un-ignore line or it is silently untracked (**verify via `git ls-files`** — this session did: 64 tracked). **`cargo deny check` can red on a fresh unrelated RustSec advisory** — patch-bump the dep, not a phase regression; its `license-not-encountered` warnings are unmatched ALLOWANCES in `deny.toml`, NOT violations. **The `cdn_loop_parse` fuzz target runs with an EMPTY seed corpus (CF-75-5)** — libFuzzer prints only FOUR `INFO: seed corpus` lines for FIVE targets; owned by its OWN phase. **envoy-bin writes `ConfigError` to STDOUT** and takes only `-c <path>`. **A `pkill -f <pattern>` whose pattern also matches the invoking shell's own command line kills that shell** — kill by PID or use `pgrep -x`. **Confirm CI with the FULL 40-char SHA** — `--commit <short-sha>` silently returns `[]` (memory `gh-run-list-commit-needs-full-sha`) — and check the **STEP COUNTS (15 for `build + test + lint`, 13 for `fuzz`)**, not merely the conclusion. A job `cancelled` with `runner_name:""` + `steps:0` never got a runner → rerun the SAME SHA; `cancelled` with FULL step counts is your own follow-up push cancelling the older run and is benign. **Do not push twice in quick succession.** CI has no `paths-ignore`, so docs-only pushes DO build. **THREE `gh`-LOG TRAPS:** `gh` NEEDS ITS CWD INSIDE THE REPO (after a `cd` to a scratch dir it fails with `failed to determine base repo` and writes a ~120-byte file, and the CI-total census over that returns a clean-looking `passed= failed=` — assert `wc -c` is in the hundreds of KB first); **`gh run view --log` RETURNS ONLY ONE JOB**, so a fuzz-only failure is INVISIBLE (enumerate via `gh api repos/pgdad/envoy-rust/actions/runs/<run>/jobs`); and **a loose `warning|error` grep over a CI log is ALL FALSE POSITIVES** (17 hits, 11 of them test NAMES under `error::tests::…`). **A GitHub outage can persist >1h on this host** (`No route to host`) — NOT an auth error. **A NAIVE `awk` FAMILY-HEADING CENSUS UNDER-REPORTS — it returned 1 where the truth is 4.** An `awk` whose first rule matches `/^### .* family/` then calls `next` never reaches a later `/^### /` rule. MEASURED TRUE ANSWER: **four** family headings carry ZERO rows — `### HTTP/3 + QUIC family` (`ROADMAP.md:122`), `### gRPC family` (`:126`), `### Runtime + hot restart family` (`:183`), `### WASM host family` (`:185`). **CONCURRENCY — A PARALLEL WORKSTREAM IS ACTIVE.** FOUR `.claude/worktrees/agent-*` git worktrees plus a long-running container on `envoyproxy/envoy:v1.33.0` and a sibling's testcontainers reaper. **LEAVE THEM ALONE**, and **EXCLUDE those worktrees from any repo-wide `find`/census/grep** — they duplicate every `fuzz_targets` dir and will inflate counts; prefer `git ls-files`, or enumerate workspace members from the root `Cargo.toml`. **Re-check `git fetch origin --prune` at session start AND again immediately before your commit** — a clean cold-start `git status` can go dirty mid-session from a sibling; defer to the further-along session and remove only your own artifacts (memory `concurrent-loop-sessions-race-on-phase-pick`). **A SESSION WITHOUT A COMMIT ADVANCES NOTHING** — scratch outputs are session-scoped and unrecoverable; if cut short, label numbers PREDICTIONS, not results. Cold-start reads `STATE.md` FIRST (disk-authoritative). **One state per session; do not chain** (§5.1), and the state-6 close-out and the next-phase state-0/1 PICK are ALWAYS separate sessions. **A REVIEWER MUST NOT FIX WHAT IT GRADES, and the context that wrote an artifact must not grade it** (§5.1; **ADR-0127**, which also records the ONE human-authorized 5→6 chaining exception at phase 66 and why 0→1, 1→2, 2→3, 3→4 and 4→5 must NEVER be chained) — **do not read the git history as licence to chain.** Parallelism is permitted strictly INSIDE one state (READ-ONLY review dimensions; the main session is the SOLE writer of the state ledger and makes every decisive measurement itself), never ACROSS states, and a state-6 close-out is ONE INDIVISIBLE UNIT, SOLO.
_Historical (the sub-phase-76.1 §5 STATE-2 PLAN-WRITE `## Next expected skill` pointer + the superseded `**SUB-PHASE `76.1` SCOPE AT A GLANCE**` block + the superseded `**Standing traps**` line + that block's own `_Historical_` pointer line — superseded by THIS sub-phase-76.1 §5 STATE-3 IMPLEMENTATION; relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this commit per ADR-0035. Per ADR-0160 the enduring Standing-traps line is rewritable only after relocating its superseded text verbatim, and since ADR-0160 landed at `3f0ec89` EVERY state commit archives it. The superseded line MEASURED **34110** characters.)_
> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases incl. the phase-37 state-1..5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit


**Sub-phase 76.1 §5 STATE-3 IMPLEMENTATION — all 8 `PLAN.md` tasks landed TDD-first with per-task commits; three mutation checks run, TWO of which corrected the plan; `PROGRESS.md` written with verbatim output.** NINE commits: `3e8dd80` (task 1, `RedirectResponseCode`), `20dd682` (task 2, `RedirectAction`), `fea479b` (task 3, the indivisible four-`match`-site task + the honest `synth_501` placeholder), `27c8a05` (task 4, the `Route` visitor), `68dd907` (task 5, the two oneof validators, `ConfigError` 123 → 125), `c8002da` (task 6, the accept direction + both `Serialize` round-trips), `68e39b1` (task 6 cont., anchoring both `Serialize` key assertions to column 0 after Mutation B exposed a VACUOUS `contains("redirect:")` check that `port_redirect:` satisfies), `c5e1024` (task 7, the corpus seed + its `!`-un-ignore line), and THIS state-3 commit (task 8: `PROGRESS.md` + the `STATE.md` advance). FIVE `crates/` files touched and nothing else: `bootstrap.rs` `+655 −10`, `lib.rs` `+28 −8`, `hcm.rs` `+72 −1`, `fuzz/.gitignore` `+1`, and the new 37-line corpus seed — net **774** `crates/`+`tests/` LoC. **NO `tests/` file, NO fixture, NO `BEHAVIOR_CONTRACT.md` line, NO `ci.yml` edit, NO new ADR** (head stays ADR-0170, next free ADR-0171), and all three `SPEC.md` files (`76`, `76.1`, `76.2`) left BYTE-UNCHANGED. ROADMAP row `76.1` stays `in-progress` (a state-3 commit flips no status cell). `STATE.md` + `STATE_HISTORY.md` carry the ADR-0035 relocation, **14** lines on a PER-FILE delta check. Predecessor `HEAD` was `cf5cf85d0a2c477b90636b74fd93f6d36038f890`.

_Historical (the sub-phase-76.1 §5 STATE-2 PLAN-WRITE Last-commit block + that session's own `_Historical_` pointer line — superseded by THIS sub-phase-76.1 §5 STATE-3 IMPLEMENTATION; relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this commit per ADR-0035.)_
> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases incl. the phase-37 state-1..5 sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated


2026-07-29 (sub-phase-76.1 **§5 STATE-3 IMPLEMENTATION COMPLETE — ALL 8 `PLAN.md` TASKS LANDED TDD-FIRST, ONE COMMIT EACH, AND THE TREE IS READY FOR THE STATE-4 GATE.** `ConfigError` **123 → 125**; `fuzz/.gitignore` **66 → 67** lines / **63 → 64** `!` lines / **63 → 64** tracked seeds; `envoy-config --lib` **649 → 680** and `envoy-http1 --lib` **186 → 187**, i.e. **+32 tests, 0 failed**; net **774** `crates/`+`tests/` LoC against the ≈515 projection (a +50% overshoot in the test half that does NOT re-open the §6.1 gate). Local rehearsal: `fmt --check` exit 0 (empty output), `build --workspace --all-targets` exit 0, `clippy --workspace --all-targets --all-features -D warnings` exit 0 with ZERO warnings on a FORCED **13**-`Checking`-line re-run (the first pass reported 0 `Checking` lines and was rejected as cached), `envoy-bin` rebuilt. **`cargo test --workspace` and `cargo deny check` were deliberately NOT run — state 4 owns them.** THREE mutation checks, each in its own scratch worktree with an unmutated control: `PLAN.md` Task 4 Step 5's mutation is **MISAIMED** (it edits the error-message name list, not the key arm, and leaves the test GREEN) and Task 6 Mutation B revealed a **VACUOUS assertion** (`port_redirect:` contains the substring `redirect:`), both now corrected. NO new ADR was required; the head stays **ADR-0170** and **ADR-0171** is next free. The active unit is still **`76.1`**, now at §5 **state 4**. ROADMAP census: **107** rows / **104** `done` / **2** `in-progress` / **1** `planned`. The mission is NOT complete and NO `stop` file was created.)

_Historical (the sub-phase-76.1 §5 STATE-2 PLAN-WRITE Last-updated note + that session's own `_Historical_` pointer line — superseded by THIS sub-phase-76.1 §5 STATE-3 IMPLEMENTATION; relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this commit per ADR-0035.)_
> Historical `## Last updated` notes — every superseded last-updated note (all closed phases incl. the phase-37 state-1..5 sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsection for fully-closed phase 64 (the `### Phase-64 state-5 code-review` narrative — phase 64 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-64 state-6 close-out when ROADMAP row `64` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 63 (the `### Phase-63 state-5 code-review` narrative — phase 63 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-63 state-6 close-out when ROADMAP row `63` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 61 (the `### Phase-61 state-1 brainstorm` / `### Phase-61 state-2 PLAN-write` / `### Phase-61 state-3 implementation` / `### Phase-61 state-4 verification` / `### Phase-61 state-5 code-review` narratives, relocated at the phase-61 state-6 close-out when ROADMAP row `61` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 58 (the `### Phase-58 state-1 brainstorm` / `### Phase-58 state-2 PLAN-write` / `### Phase-58 state-3 implementation` / `### Phase-58 state-4 verification` / `### Phase-58 state-5 code-review` narratives, relocated at the phase-58 state-6 close-out when ROADMAP row `58` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 57 (the `### Phase-57 state-1 brainstorm` / `### Phase-57 state-2 PLAN-write` / `### Phase-57 state-3 implementation` / `### Phase-57 state-4 verification` / `### Phase-57 state-5 code-review` narratives, relocated at the phase-57 state-6 close-out when ROADMAP row `57` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 42 (the `### Phase-42 state-5 code-review (active phase)` narrative — phase 42 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-42 state-6 close-out when ROADMAP row `42` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 43 (the `### Phase-43 state-5 code-review (active phase)` narrative — phase 43 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-43 state-6 close-out when ROADMAP row `43` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 44 (the `### Phase-44 state-5 code-review (active phase)` narrative — phase 44 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-44 state-6 close-out when ROADMAP row `44` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 45 (the `### Phase-45 state-5 code-review (active phase)` narrative — phase 45 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-45 state-6 close-out when ROADMAP row `45` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 46 (the `### Phase-46 state-5 code-review (active phase)` narrative — phase 46 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-46 state-6 close-out when ROADMAP row `46` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 47 (the `### Phase-47 state-5 code-review (active phase)` narrative — phase 47 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-47 state-6 close-out when ROADMAP row `47` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 48 (the `### Phase-48 state-5 code-review (active phase)` narrative — phase 48 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-48 state-6 close-out when ROADMAP row `48` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 49 (the `### Phase-49 state-5 code-review (active phase)` narrative — phase 49 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-49 state-6 close-out when ROADMAP row `49` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 50 (the `### Phase-50 state-3 implementation (active phase)` narrative — phase 50 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-50 state-6 close-out when ROADMAP row `50` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 51 (the `### Phase-51 state-3 implementation (active phase)` narrative — phase 51 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-51 state-6 close-out when ROADMAP row `51` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 53 (the `### Phase-53 state-1 brainstorm` narrative — phase 53 kept a single active-phase Notes subsection at "state-1 brainstorm" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-1 subsection remained in `## Notes` at close — relocated at the phase-53 state-6 close-out when ROADMAP row `53` flipped `done`, together with the inline superseded state-3 narrative) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 54 (the `### Phase-54 state-1 brainstorm` / `### Phase-54 state-2 PLAN-write` / `### Phase-54 state-3 implementation` / `### Phase-54 state-4 verification` / `### Phase-54 state-5 code-review` narratives, relocated at the phase-54 state-6 close-out when ROADMAP row `54` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 55 (the `### Phase-55 state-5 code-review (active phase)` narrative — phase 55 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state individually superseded in place and relocated to STATE_HISTORY.md at its own transition], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-55 state-6 close-out when ROADMAP row `55` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 56 (the `### Phase-56 state-5 code-review (active phase)` narrative — phase 56 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state individually superseded in place and relocated to STATE_HISTORY.md at its own transition], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-56 state-6 close-out when ROADMAP row `56` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 66 (the `### Phase-66 state-5 code-review (active phase)` narrative — phase 66 used a rename-in-place active-phase Notes subsection across its state-0/1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-66 state-6 close-out when ROADMAP row `66` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed sub-phase 67.1 (the `### Phase-67.1 §5 state-5 code-review` narrative — sub-phase 67.1 used a rename-in-place active-phase Notes subsection across its state-2..5 + §5.2 state-3 re-entry arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-67.1 state-6 close-out when ROADMAP row `67.1` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed sub-phase 67.2 (the `### Phase-67.2 §5 state-5 re-review` narrative — sub-phase 67.2 used a rename-in-place active-phase Notes subsection across its state-2..5 + §5.2 state-3 re-entry + state-4-re-verification + state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review subsection remained in `## Notes` at close — relocated at the phase-67.2 state-6 close-out when ROADMAP row `67.2` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
> Historical Notes subsection for fully-closed phase 68 (the `### Phase-68 §5 state-5 code-review` narrative — phase 68 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-68 §5 state-6 close-out when ROADMAP row `68` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 69 (the `### Phase-69 §5 state-5 re-review` narrative — phase 69 used a rename-in-place active-phase Notes subsection across its state-3 → §5.2 state-3 re-entry → state-4 re-verification → state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review subsection remained in `## Notes` at close — relocated at the phase-69 §5 state-6 close-out when ROADMAP row `69` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 70 (the `### Phase-70 §5 state-5 re-review (2nd)` narrative — phase 70 used a rename-in-place active-phase Notes subsection across its state-1..5 + two-§5.2-re-entry + two-state-4-re-verification + two-state-5-re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review-(2nd) subsection remained in `## Notes` at close — relocated at the phase-70 §5 state-6 close-out when ROADMAP row `70` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 71 (the `### Phase-71 §5 state-5 code-review` narrative — phase 71 used a rename-in-place active-phase Notes subsection across its state-0/1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-71 §5 state-6 close-out when ROADMAP row `71` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 72 (the `### Phase-72 §5 state-5 re-review` narrative — phase 72 used a rename-in-place active-phase Notes subsection across its state-1..5 + §5.2 state-3 re-entry + state-4 re-verification + state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 re-review subsection remained in `## Notes` at close — relocated at the phase-72 §5 state-6 close-out when ROADMAP row `72` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 73 (the `### Phase-73 §5 state-5 code-review` narrative — phase 73 used a rename-in-place active-phase Notes subsection across its state-0/1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-73 §5 state-6 close-out when ROADMAP row `73` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 74 (the `### Phase-74 §5 state-5 re-review (2nd)` narrative — phase 74 used a rename-in-place active-phase Notes subsection across its state-0/1..5 + TWO §5.2 state-3 re-entry + TWO state-4 re-verification + TWO state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review-(2nd) subsection remained in `## Notes` at close — relocated at the phase-74 §5 state-6 close-out when ROADMAP row `74` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed sub-phase 75.1 (the `### Phase-75.1 §5 state-5 code-review` narrative — sub-phase 75.1 used a rename-in-place active-phase Notes subsection across its state-2..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the sub-phase-75.1 §5 state-6 close-out when ROADMAP row `75.1` flipped `done`; parent row `75` deliberately stays `in-progress` until sibling `75.2` is `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Phase-76 §5 state-0/1 brainstorm (active phase)

- **Pick:** the `Route.redirect` action (`envoy.config.route.v3.RedirectAction`) — the THIRD `Route.action` oneof arm, which envoy-rust WHOLLY LACKS (`RouteAction` is a two-variant enum at `crates/envoy-config/src/bootstrap.rs:2178`; the `Route` visitor accepts five keys at `:2483-2494`), so a `redirect:` route upstream SERVES is boot-fatal here. Pick + scope: **ADR-0168**. **ADR-0169 RESERVED-UNFIRED** for a `76.1`/`76.2` split.
- **The recon drove the pick, and it also produced two findings that outlived it.** `CF-76-1` (NEW): upstream STRIPS the query before route path matching while envoy-rust matches the RAW target — silent, pre-existing, on the most-exercised surface in the project, pinned by no fixture; NOT fixed here, owner is its own phase. And `CF-75-2`'s join rule, which its own record flagged as UNMEASURED, is now measured (single comma, no space, OWS-trimmed, seen by every value mode, precedes the numeric parse, `cookie` not special-cased) — the divergence stays OPEN and out of scope.
- **Four measured properties, not intuitions, decided it.** Backend-free (zero clusters — so locally verifiable on a host where backend-routing fixtures go RED); zero new harness machinery (`location` is not allow-listed, so `Http1ProbeList` compares it value-exact for free); one dispatch seam that serves BOTH codecs (H2 calls H1's resolver); and 15 deterministic runtime cells + 7 reject-direction rules.
- **The elegant fit:** `%RESPONSE_CODE_DETAILS%` for a redirect is measured as `direct_response` — the string envoy-rust ALREADY emits from its `DirectResponse` arm — so the new arm reuses the existing constant and adds no access-log surface at all.
- **Scope line = the §6.1 LoC gate.** `regex_rewrite` inside `redirect` is a NON-GOAL (measured working upstream, but it needs substitution machinery) and stays boot-fatal, the house posture already used for `hash_policy`/`retry_policy`/`domains`. Projected ~1340 net LoC / ~16-20 tasks: under both thresholds but within ~11% of the LoC gate, so the PLAN-write must re-derive it.
- **Honest gaps.** `SPEC.md` §7 records SEVEN explicitly NOT-MEASURED items; the fixture-critical ones (302/308 reason phrases, the full response-header set) must be measured at the PLAN-write. One recon probe was silently shadowed by prefix overlap and had to be re-measured — prefix overlap is a live hazard for fixture `0086`.
- **Next:** the §5 **state-2 PLAN-write** (a SEPARATE session per §5.1) — re-verify this SPEC's citations on disk, measure the two open cells, re-derive the estimate, then author `PLAN.md`.

### Phase-76 §5 state-2 SPLIT — sub-phase `76.1` is the active unit

- **The §6.1 LoC gate FIRED and `ADR-0169` consumed its RESERVED slot.** Phase `76` is split into `76.1-redirect-config-surface` and `76.2-redirect-runtime-fixture`. **No `PLAN.md` was written for phase `76` and none ever will be** — §6.2 step 1 says *stop, do not continue writing the oversize plan*. Do not read the parent's missing `PLAN.md` as an unexpected state under §5.1 Step E.
- **The estimate was RE-DERIVED and CALIBRATED, not inherited.** ≈1530 net `crates/`+`tests/` LoC at ≈20-24 tasks, against a ~1500 gate, benchmarked on the measured net LoC of seven recent phases (68→950, 69→1540, 70→1372, 73→873, 74→1981, 75.1→1413, 75.2→897). The parent SPEC had projected ~1340.
- **It rose because measurement added four obligations the parent SPEC lacked:** no `content-type` on a redirect response (so `synth_with` is unusable for it); `canonical_reason` missing 303/307/308 (and the fixture cannot catch it); the oneofs being exclusive on field PRESENCE not value (so `Option<bool>`, not `bool`); and the `prefix_rewrite` `:path` mutation needing `&mut Request` across 8 call sites.
- **Both parent-SPEC §7 cells this session owed are MEASURED:** `FOUND` → `302 Found`, `PERMANENT_REDIRECT` → `308 Permanent Redirect`, and the full redirect header set.
- **The parent `76/SPEC.md` is BYTE-UNCHANGED** (ADR-0169 DECISION 5) — it is the measured record of the state-0/1 recon. The two sub-phase SPECs are self-contained per D-3.4, and they state the parent's two corrections rather than editing them in.
- **Next:** the §5 **state-2 PLAN-write for `76.1`** (a SEPARATE session per §5.1) — re-verify `76.1/SPEC.md`'s citations on disk, write TDD-ordered tasks for T-R1..T-R10 / T-A1..T-A7 / T-C1..T-C9, and pre-flight the PLAN's own literal Rust against `cargo fmt --check` + `clippy -D warnings`.

### Sub-phase 76.1 §5 state-2 PLAN-write (active phase)

- **`PLAN.md` is written: 8 TDD-ordered tasks, ≈515 net LoC re-derived, and the §6.1 gate did NOT fire.** `ADR-0170` records the §6.2 empirical reconciliation. `76.1` moves from §5 state 2 to state 3; the next session is the state-3 implementation via `superpowers:executing-plans`, a SEPARATE session.
- **The estimate's implementation half is MEASURED, not projected.** Applying the plan's own literal Rust in a scratch `git worktree` gave **net 129 LoC** (`bootstrap.rs` 101 + `lib.rs` 20 + `hcm.rs` 8); only the test half (≈348) plus the fuzz seed (38) is projected. Calibration was re-derived rather than inherited — **74→1981, 75.1→1413, 75.2→897** all reproduced exactly.
- **The design was PRE-VALIDATED semantically, not just syntactically.** In that worktree: `cargo fmt --all -- --check` clean, `clippy --workspace --all-targets --all-features -- -D warnings` ZERO warnings (155 `Checking` lines, forced), `envoy-config` **653 passed / 0 failed**, `envoy-http1` **187 passed / 0 failed**, and **all 21 MEASURED verdict cells correct** (J1-J7 + T-R8/T-R9/T-R10 reject; A1-A4/A6 + `https_redirect: false` alone + all five `response_code` names accept; `port_redirect: 70000` verbatim).
- **Three pre-flight findings the `SPEC.md` does not carry, each a real failure:** `lib.rs:14` is an EXPLICIT non-glob `pub use` re-export list (a missed edit site → `E0432`); inserting into it makes rustfmt REFLOW the block's whole tail (`+29 −9`, and the first pre-flight pass genuinely failed `fmt --check`); and `hcm.rs:2361`'s test-module import is a THIRD edit site (`E0422`).
- **One inherited figure REFUTED:** `crates/envoy-config/src/bootstrap.rs` is **20 397** lines, not the "~14 400" both the SPEC §0 and the handoff assert. Every other SPEC citation verified TRUE on disk, with four PARTIALs on RANGE only; the SPEC's two corrections of the parent SPEC were themselves CONFIRMED.
- **Next:** the §5 **state-3 implementation for `76.1`** (a SEPARATE session per §5.1) — `superpowers:executing-plans`, NOT subagent-driven (one file, a compile-order chain), TDD per task, appending to `PROGRESS.md`, and honouring TDD's RED with the plan's specified mutation checks for the two tasks that pin already-correct code.

### Sub-phase 76.1 §5 state-3 implementation (active phase)

- **All 8 `PLAN.md` tasks are landed, TDD-first, one commit each; `PROGRESS.md` quotes verbatim command output per task.** `76.1` moves from §5 state 3 to state 4; the next session is the state-4 verification running the FULL §7.5 gate, a SEPARATE session. **No new ADR was required** — the head stays ADR-0170, next free ADR-0171.
- **THREE MUTATION CHECKS, AND TWO OF THEM CORRECTED `PLAN.md`.** Each ran in its own scratch `git worktree` with an UNMUTATED CONTROL, was reverted, and was re-grepped as gone; the main tree was never mutated. (a) **Task 4 Step 5's mutation is MISAIMED** — dropping `"route"` from the visitor's `unknown_field` NAME LIST leaves the test GREEN, because that `&[…]` is only error-message text; the correctly-aimed mutation removes the `"route" =>` KEY ARM and REDs it with a genuine assertion failure. (b) **Task 6 Mutation B produced a THIRD outcome the plan did not anticipate — BOTH tests GREEN** — because `ser.contains("redirect:")` is satisfied by the FIELD name `port_redirect:`. Both round-trip assertions were strengthened to anchor the key at column 0 (`68e39b1`), after which Mutation B gives exactly the predicted one-RED/one-GREEN. (c) Mutation A (a bogus `port_redirect > 65535` bound) REDs T-A2 on the nose.
- **The plan's pre-flight paid off: its literal Rust transcribed clean.** Every code block compiled, `fmt`-checked and clippy-checked as written, and every helper it named existed. The two divergences were both citation drift (`hcm.rs` import at `:2353`, not `:2361`) and the rustfmt `pub use` reflow materialising as `+8 −8` rather than `+29 −9` — the same event.
- **Two coverage additions, neither weakening anything.** `all_five_preexisting_route_keys_still_parse` gained a fifth-key block (the plan's body exercised only FOUR keys, so T-C5 was not actually delivered; `typed_per_filter_config` cannot ride the `route_action_yaml` scaffold because the 23 D3 validator rejects a per-filter key absent from the HCM's `http_filters` set, so it is asserted at `Route` level), and the new fuzz seed was added to the `fuzz_corpus_seeds_parse_or_reject_cleanly` cohort walk as well as its own dedicated test.
- **The first clippy green of the Task-8 rehearsal had ZERO `Checking` lines and was rejected as fully cached.** Forced via `touch`, it produced **13** `Checking` lines with `envoy-accesslog` ABSENT — the ADR-0150 seam witness. `envoy-bin` was rebuilt so state 4's differential does not run a stale binary against the NEW `redirect` config key.
- **Net 774 `crates/`+`tests/` LoC against `PLAN.md`'s ≈515 projection** — a +50% overshoot living almost entirely in the test half, exactly where the plan flagged the risk. It does NOT re-open the §6.1 gate (52% of ~1500 at 8 tasks against ~25) and the mid-execution sub-step trigger never fired.
- **Next:** the §5 **state-4 verification for `76.1`** (a SEPARATE session per §5.1) — `superpowers:verification-before-completion`, the full §7.5 gate, quoting every command's output into `PROGRESS.md`. `PROGRESS.md` §4 records which gates are already MET, which are VACUOUS ((a), no new fixture) and which are still OPEN (`cargo test --workspace`, `cargo deny check`, the differential suite, conformance).

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: **one state per session; do not chain states.** **PARENT PHASE `76` WAS SPLIT (§6.1, ADR-0169) AND IS STILL `in-progress`; THE ACTIVE UNIT IS SUB-PHASE `76.1` (`76.1-redirect-config-surface`), NOW AT §5 STATE 4** — its directory holds `SPEC.md` + `PLAN.md` + `PROGRESS.md` but no `REVIEW.md`, so the next session runs `superpowers:verification-before-completion` scoped to **`76.1`**, NOT to the parent `76` and NOT to `76.2`. **The active unit is always the lowest-numbered sub-phase that is not `done`.** **NO `PLAN.md` EXISTS OR WILL EVER EXIST FOR PHASE `76` ITSELF** — §6.2 step 1 stops the oversize plan, so the parent's state-2 output was the SPLIT, not a plan; do not read the parent's missing `PLAN.md` as an unexpected state under §1 Step E. **`76.2` MUST NOT START until `76.1` is `done`.** **The state-3 implementation and the state-4 verification are ALWAYS separate sessions** (ADR-0127: the context that wrote an artifact must not be the one that extends or grades it), and so are 4→5 and the state-6 close-out.

- **On the §5.1 rule and its ONE recorded exception (read `ADR-0127`).** The phase-66 state-6 close-out was run in the SAME session as its state-5 code-review, at **explicit human instruction**. That was a one-off, human-authorized override; `BOOTSTRAP_PROMPT.md`, `MISSION.md` and `SKILL_ROUTING.md` were **not modified**, and **§5.1 remains binding on every autonomous session.** ADR-0127 records why the 5→6 transition alone was safe to chain (the close-out changes no code, re-runs no gate, and consumes only disk facts) and why **0→1, 1→2, 2→3, 3→4 and 4→5 must never be chained** — each carries a genuine self-review hazard, since the context that wrote an artifact would be grading it. Per memory `closeout-and-pick-are-separate-sessions`, the next-phase pick is STILL a separate session. Do not read the git history as licence to chain.

> Historical Notes subsection for fully-closed sub-phase 67.3 (the `### Phase-67.3 §5 state-5 re-review` narrative — sub-phase 67.3 used a rename-in-place active-phase Notes subsection across its state-2..5 + §5.2 state-3 re-entry + state-4-re-verification + state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review subsection remained in `## Notes` at close — relocated at the phase-67.3 §5 state-6 close-out when ROADMAP row `67.3` [and parent row `67`] flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 65 (the `### Phase-65 state-5 code-review` narrative — phase 65 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-65 state-6 close-out when ROADMAP row `65` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsection for fully-closed phase 52 (the `### Phase-52 state-5 code-review (active phase)` narrative — phase 52 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2/3/4 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-52 state-6 close-out when ROADMAP row `52` flipped `done`, together with the standing 05.1/05.4 future-reviewer note it carried) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 41 (the `### Phase-41 state-5 code-review (active phase)` narrative — phase 41 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the §6.2-recon PIVOT from `%REQ_WITHOUT_QUERY%` to `%ROUTE_NAME%` landed at state-2; the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-41 state-6 close-out when ROADMAP row `41` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 40 (the `### Phase-40 state-5 code-review (active phase)` narrative — phase 40 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-40 state-6 close-out when ROADMAP row `40` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 39 (the `### Phase-39 state-5 code-review (active phase)` narrative — phase 39 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-39 state-6 close-out when ROADMAP row `39` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
> Historical Notes subsection for fully-closed phase 38 (the `### Phase-38 state-2 PLAN-write (active phase)` narrative — phase 38 used a single active-phase Notes subsection for its whole arc; the state-3/4/5 sub-state narratives were superseded in the four-top-section archive blocks per-session, so only this state-2 subsection remained in `## Notes` at close — relocated at the phase-38 state-6 close-out when ROADMAP row `38` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) and for parent phase 25 (the `### Phase-25 state-1 brainstorm` pick + recon-finding narrative, relocated at the 25.2 state-6 close-out when parent `25` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 26 (the `### Phase-26 state-1 brainstorm` pivot/rejected-alternatives/key-scoping narrative, relocated at the phase-26 state-6 close-out when row `26` flipped to `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 27 (the `### Phase-27 state-1 brainstorm` / `### Phase-27 state-2 PLAN-write` / `### Phase-27 state-4 verification` narratives + the now-consumed `### Phase-27 carry-forwards` [M26-1..M26-8] block, relocated at the phase-27 state-6 close-out when row `27` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 28 (the `### Phase-28 state-1 brainstorm` / `### Phase-28 state-2 PLAN-write` / `### Phase-28 state-3 implementation + state-4 verification` / `### Phase-28 state-5 code review` narratives + the now-consumed `### Phase-28 carry-forwards` [M27-1..M27-3] block, relocated at the phase-28 state-6 close-out when row `28` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 29 (the now-consumed `### Phase-29 carry-forwards` [M28-1..M28-3] block + the `### Phase-29 state-1 brainstorm` / `### Phase-29 state-2 PLAN-write` / `### Phase-29 state-3 implementation` / `### Phase-29 state-4 verification` / `### Phase-29 state-5 code review` narratives, relocated at the phase-29 state-6 close-out when row `29` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 30 (the `### Phase-30 carry-forwards` [M29-1/M29-2, which fed phase 30 but were NOT consumed — they continue as phase-31 carry-forwards] block + the `### Phase-30 state-1 brainstorm` / `### Phase-30 state-2 PLAN-write` / `### Phase-30 state-3 implementation` / `### Phase-30 state-4 verification` / `### Phase-30 state-5 code review` narratives, relocated at the phase-30 state-6 close-out when row `30` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 31 (the `### Phase-31 carry-forwards` [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 — open Minors from the phase-30 REVIEW.md that fed phase 31 but were NOT consumed; they continue as carry-forwards for the next phase that touches the differential hash-sweep driver / the config parser] block + the `### Phase-31 state-1 brainstorm` / `### Phase-31 state-2 PLAN-write` / `### Phase-31 state-3 implementation` / `### Phase-31 state-4 verification` / `### Phase-31 state-5 code review` narratives, relocated at the phase-31 state-6 close-out when row `31` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 32 (the `### Phase-32 carry-forwards` block — the open Minors that fed phase 32 but were NOT consumed: the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3; they continue as carry-forwards for the future phase that touches their surface — re-listed in `## Next expected skill` above alongside the 6 new phase-32 REVIEW.md Minors M32-1…M32-6), relocated at the phase-32 state-6 close-out when row `32` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phases 33+34 (the `### Phase-33 carry-forwards` block — the active-phase carry-forwards Notes that lived through phases 33 and 34: the now-CONSUMED 6 phase-32 REVIEW.md Minors M32-1…M32-6 [folded + landed at the phase-33 state-3] + the "Other still-live carry-forwards" list [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3 + the HTTP-filters-family (1)-(4) — those still-live ones re-listed in `## Next expected skill` above alongside the new phase-34 Minors M34-1/M34-2/M34-3 + the phase-33 M33-1/M33-2]), relocated at the phase-34 state-6 close-out when row `34` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 35 (the `### Phase-35 state-1 brainstorm` / `### Phase-35 state-2 PLAN-write` / `### Phase-35 state-3 implementation` / `### Phase-35 state-4 verification` / `### Phase-35 state-5 code-review` narratives, relocated at the phase-35 state-6 close-out when ROADMAP row `35` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 36 (the `### Phase-36 state-5 code-review` narrative — phase 36 used a rename-in-place Notes discipline, so the state-1..4 sub-state narratives were superseded in place each session and only the four top-section blocks were relocated per-session — plus the now-CLOSED detailed M35-1 carry-forward bullet [CONSUMED by phase-36 F2], relocated at the phase-36 state-6 close-out when ROADMAP row `36` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 37 (the `### Phase-37 state-3 implementation` active-phase narrative, relocated at the phase-37 state-6 close-out when ROADMAP row `37` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### HTTP-filters-family carry-forwards (from the `25.2` REVIEW.md - NOT yet consumed; weigh whenever the HTTP-filters family is re-entered)

> These were never obligations on the xDS phase 26; they remain live for whenever an HTTP-filters-family phase resumes.

- **(1) [non-goal - architectural]** Over-limit request bodies are FULLY buffered before the 413 rejection (no streaming watermark). Documented deferred non-goal; differentially byte-identical to Envoy for the bounded fixture sizes. Revisit only if a streaming `decode_data` watermark path is ever planned.
- **(2) [doc precision]** The BEHAVIOR_CONTRACT 413-row "verified byte-exact against v1.33.0" phrasing - fixture `0033` is H1-only; the H2 over-limit path is covered by the in-process synth-decorator backstop, NOT differentially. Consider narrowing the phrasing if an H2 over-limit fixture is ever added.
- **(3) [coverage]** No standalone `== effective route limit` unit assertion (the boundary is exercised only via the over/under probes).
- **(4) [coverage]** No differential at-limit (`==`) probe in `0033` (within-limit `<` and over-limit `>` are both covered; the exact boundary is not differentially probed).
- _(2)-(4) are cheap polish, (1) is architectural and only relevant to a future streaming phase._
