# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase


**id:** **75.2** — `75.2-headermatcher-absence-accesslog` (ROADMAP row `75.2`, **`in-progress`** — DELIBERATELY LEFT `in-progress` here, since a §5.2 state-3 RE-ENTRY does not close a row), the SECOND and FINAL sub-phase of parent **75** (`75-headermatcher-absence-parity`, cross-cutting correctness: `HeaderMatcher` ABSENCE-SEMANTICS parity with `envoyproxy/envoy:v1.33.0`; ROADMAP row `75`, **`in-progress`**, `sub-phases` = `75.1, 75.2`), created by the §6.1 SPLIT of parent 75 (ADR-0157). **Sibling `75.1` is `done`** and its engine fix is LANDED and is the PREMISE of everything 75.2 does. **depends-on `75.1` — satisfied.** **Parent row `75` flips `done` only when `75.2` is `done`.** That flip belongs to `75.2`'s eventual state-6 close-out, which now sits behind a **§5 state-4 RE-VERIFICATION → §5 state-5 RE-REVIEW** arc, because the §5.2 state-3 RE-ENTRY that discharged the RE-REVIEW's charter has just landed. **Gates (a)-(e) are LANDED GREEN but were measured BEFORE this re-entry; gate (f) is REFUSED, not merely open.**
**slug:** `75.2-headermatcher-absence-accesslog` (parent `75-headermatcher-absence-parity`; closed sibling `75.1-headermatcher-absence-engine-route`).
**directory:** `docs/envoy-rust/phases/75.2-headermatcher-absence-accesslog/` — holds **`SPEC.md` (521), `PLAN.md` (1631), `PROGRESS.md` (**3037**, EXTENDED AT THIS SESSION with its `# §5.2 state-3 RE-ENTRY (SECOND) — closing the `REVIEW.md` §5 state-5 RE-REVIEW charter` section — 2547 → 3037, **+490 insertions / 0 deletions**, append-only VERIFIED by re-diffing the first 2547 lines against a pre-append backup) AND `REVIEW.md` (1568)**. **`REVIEW.md` CARRIES TWO VERDICTS AND BOTH CHARTERS ARE NOW DISCHARGED — it is HISTORY, not a live charter.** The `# §5 state-5 RE-REVIEW — the phase's CURRENT verdict` section at line 935 (charter §R9) was closed by `PROGRESS.md`'s `# §5.2 state-3 RE-ENTRY (SECOND)` section at line **2549**; the earlier `# Sub-phase 75.2 … §5 state-5 CODE-REVIEW` at line 1 was closed by the FIRST `# §5.2 state-3 RE-ENTRY` at `PROGRESS.md:1317`. `PROGRESS.md` now carries FOUR gate/implementation records — `# §5 state-4` (line 740, measured at `406c379`, SUPERSEDED but left as landed per D-3.5), `# §5.2 state-3 RE-ENTRY` (1317), `# §5 state-4 RE-VERIFICATION` (1756 — the gate that covers the PRE-re-entry head) and `# §5.2 state-3 RE-ENTRY (SECOND)` (2549 — THIS session). The CLOSED sibling `docs/envoy-rust/phases/75.1-headermatcher-absence-engine-route/` holds the full `SPEC.md` + `PLAN.md` + `PROGRESS.md` + `REVIEW.md` set — expected, NOT corruption (§6.2), and all four are FROZEN. The PARENT `docs/envoy-rust/phases/75-headermatcher-absence-parity/SPEC.md` also REMAINS ON DISK as the parent record and is likewise FROZEN; parent 75 has NO `PLAN.md` and never will (§6.2 step 1 ABANDONS the oversize plan).

**status:** **SUB-PHASE 75.2 §5.2 STATE-3 RE-ENTRY (SECOND) COMPLETE — every `REVIEW.md` §R9 finding is CLOSED or its NO-ACTION disposition RECORDED. GATE (f) IS STILL REFUSED; the next session is the §5 STATE-4 RE-VERIFICATION.** This session IMPLEMENTED (§5.2 step 3, NOT step 4) and graded nothing. Cold-started fully per `BOOTSTRAP_PROMPT.md` §1 on `HEAD == b369f9c5de87e442ae90c6d1c8916ff6979dc8c9`, `git status --porcelain` clean, branch `main`, `git fetch origin --prune` showing `origin/main` at the SAME SHA at session start AND re-checked before committing. CI on that FULL 40-char SHA re-confirmed: run **`30302697031`**, `completed`/`success`, both jobs at FULL step counts **15** and **13**. **STATE DETECTION: `SPEC.md`, `PLAN.md`, `PROGRESS.md` AND `REVIEW.md` all existed; `REVIEW.md`'s CURRENT verdict (the `# §5 state-5 RE-REVIEW` section at line 935) was CHANGES-REQUESTED and NO `PROGRESS.md` section yet closed it — so the review was a LIVE CHARTER and the phase re-entered at §5.2 step 3.** Skills: `superpowers:executing-plans` + `superpowers:test-driven-development`. **THE TDD RED WAS RE-MEASURED, NOT TRANSCRIBED** (§R9 item 6 requires exactly this): mutation H — the arm-ORDER hoist — was re-applied in this session's OWN `--detach` scratch worktree with a private `CARGO_TARGET_DIR`, with the UNMUTATED CONTROL RUN FIRST (`649 passed; 0 failed` in-process, all three fixtures `1 passed; 0 filtered out`), `Compiling envoy-config` + `Compiling envoy-bin` confirmed on the post-mutation build, the mutation RE-GREPPED as still present after the runs, and the control RE-RUN from the same worktree after restoring. **RESULT — the RE-REVIEW's refutation REPRODUCES: in-process `645 passed; 4 failed`; `0084` GREEN (`1 passed`); `0085` RED (`envoy-rust emitted 0 access-log lines but 1 were expected to be logged; lines: []`); `0083` RED (`fixture passes: probe p07-absent-keeps-GUARD` / byte-exact body mismatch `p07=MATCH` vs `p07=NOMATCH`).** Every RED quotes a REAL assertion — none is a `not accept-ready` startup artifact. **WHAT LANDED: I-1 CLOSED** — `BEHAVIOR_CONTRACT.md` §D rewritten to the measured behavior (site re-derived BY TEXT ANCHOR, never by the line numbers `REVIEW.md` quotes), with a three-row measured table, the TRUE conclusion (**the arm ORDER is guarded in-process AND cross-proxy**), and a `> **History**` blockquote preserving the superseded sentence so the correction cannot be silently re-inherited; **I-2 CLOSED** — the comment-only claim RE-PROVEN over the TRUE base `3f0ec89..HEAD` (the `///`-only filter prints **6**, the widened `'^[+-]\s*(///|//!|//)'` prints EMPTY, and the comment-stripped WHOLE-FILE compare reports both `crates/` files byte-identical: **ZERO executable lines changed across the whole sub-phase**); **M-1, M-2, M-3, M-6, M-7 CLOSED** by measurement (two `crates/` FILES not three; the caught-wrong-census count reconciled to **NINE**; only ROADMAP rows **66** and **70** misclassify, naive `done=100` not 95; the ADR-0035 delta block APPENDED so `PROGRESS.md:1720`'s "below" pointer now resolves on disk — 13/13 at delta +1, 0 history lines lost; the traps-line range verified still fixed and RE-MEASURED at **20 519** characters); **CF-75-4 and CF-75-5 RECORDED**, and `BEHAVIOR_CONTRACT.md` §G's closing sentence rewritten so the distinct-`path:` rule binds EXISTING fixtures too, naming `0078`/`0079`/`0080`; **N-4 and N-6 NO-ACTION** (landed append-only artifacts), N-1/N-2/N-3/N-5/N-7 dispositioned. **NO ADR WAS FIRED** — every decision executed was already ADR-0165's; ledger head stays **ADR-0165**, next **ADR-0166**. **NO `crates/` file, fixture, entrypoint, `PLAN.md`, `SPEC.md`, ROADMAP row, landed ADR, `75.1/` artifact or frozen `75/SPEC.md` was touched** — the whole footprint is `docs/envoy-rust/`. The §7.5 gate was NOT re-run.

_Historical (the SUB-PHASE 75.2 §5 STATE-5 RE-REVIEW Active-phase `**id:**` + `**directory:**` + `**status:**` blocks + that block's own `_Historical_` pointer line — superseded by this SUB-PHASE 75.2 §5.2 STATE-3 RE-ENTRY (SECOND); relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this session per ADR-0035. The `**slug:**` line is UNCHANGED and is carried forward in place — it is the ONLY allowlisted carry-in-place.)_
> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases incl. the phase-51 state-3/4/5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill


**The next session runs the §5 STATE-4 RE-VERIFICATION FOR SUB-PHASE 75.2** (`superpowers:verification-before-completion`) — it re-runs the WHOLE §7.5 phase-done gate over the §5.2 state-3 RE-ENTRY's result and quotes every command output VERBATIM into `PROGRESS.md`. **It is state 4, NOT a second re-entry:** the `REVIEW.md` §R9 charter is DISCHARGED (see `PROGRESS.md:2549`), so implementation is complete and unverified — exactly §5 state 4. It is a SEPARATE session by §5.1 and ADR-0127 — the context that WROTE the fixes must not gate them. **It must NOT** grade the work (that is the state-5 RE-REVIEW that follows it), re-open or re-grade sub-phase 75.1, edit the FROZEN `75/SPEC.md` or any `75.1/` artifact, edit a landed ADR, flip ROADMAP row `75.2` or parent row `75` (both belong to the state-6 close-out), widen 75.2 into CF-75-2/CF-75-3/CF-75-4/CF-75-5, chain into the review or the close-out, or create a `stop` file.

**WHAT 75.2's §5 STATE-4 RE-VERIFICATION INHERITS.** **The §5.2 state-3 RE-ENTRY is COMPLETE and its record is `PROGRESS.md`'s `# §5.2 state-3 RE-ENTRY (SECOND)` section (line 2549; the document is 3037 lines).** Every `REVIEW.md` §R9 finding is CLOSED (I-1, I-2, M-1, M-2, M-3, M-6, M-7) or its NO-ACTION disposition RECORDED (N-4, N-6; N-1/N-2/N-3/N-5/N-7 dispositioned as optional polish), and CF-75-4 + CF-75-5 are banked. **THE RE-ENTRY CHANGED ONLY `docs/envoy-rust/` — no `crates/` file, no fixture, no entrypoint.** That is the single most useful fact for the gate: **the previous gate's (a)-(e) results at `PROGRESS.md:1756` were measured on a tree whose CODE AND FIXTURES ARE BYTE-IDENTICAL to this one**, so any RED is either a host flake or a docs-only regression (e.g. a broken markdown table), never a behavioural one. **STILL RE-RUN THE GATE IN FULL — do NOT reason from that identity to a skipped run** (D-3.6; §7.5 gates (a)-(e) are not satisfiable by argument). **Gate (f) stays REFUSED** until a LATER state-5 RE-REVIEW APPROVES. **Adjudication guidance:** the local host-flake set is a STABLE CORE OF FIVE plus a VARYING startup-race TAIL (ADR-0164) — run the sweep 2-3× with `--no-fail-fast` and a FULL-output redirect (**NEVER `tail`**), DIFF the failing SET between runs, adjudicate every RED by ADR-0164's four-part test rather than by membership in `PLAN.md:1600`'s list, and re-check the identity `local passed + failed == CI passed` (**2105**). **`cargo build -p envoy-bin` before ANY local differential.** Quote every output VERBATIM. After the gate lands the sequence is **state-5 RE-REVIEW → state-6 CLOSE-OUT**, each its own session, with BOTH row `75.2` and parent row `75` flipping `done` only at the close-out.


**Standing traps** (enduring). **75.2-RE-ENTRY-SPECIFIC (updated at the §5.2 state-3 RE-ENTRY that discharged the §5 state-5 RE-REVIEW charter; gates (a)-(e) are LANDED GREEN, **GATE (f) IS STILL REFUSED**, and the NEXT session is the §5 STATE-4 RE-VERIFICATION).** **`BEHAVIOR_CONTRACT.md` §D IS NOW CORRECTED ON DISK — the blocking finding I-1 is CLOSED; do NOT "restore" the old wording.** §D of the Phase 75 block USED to say the arm-ORDER hoist leaves **both** `0084`/`0085` GREEN and concluded the arm order is *"guarded ONLY in-process, so the differential fixtures cannot catch a regression in it"*. That was REFUTED by measurement at the RE-REVIEW and RE-MEASURED INDEPENDENTLY at the re-entry: **`0085` goes RED (`envoy-rust emitted 0 access-log lines but 1 were expected to be logged; lines: []`) and `0083` goes RED (`fixture passes: probe p07-absent-keeps-GUARD` / `p07=MATCH` vs `p07=NOMATCH`); only `0084` stays GREEN, because it uses `exact_match`, a value mode the hoist does not change.** §D now states the TRUE conclusion — **the arm ORDER is guarded in-process AND cross-proxy** — carries a three-row measured table, and keeps a `> **History**` blockquote QUOTING the superseded sentence so a grep for the old text returns 2 hits that are CORRECT (adjudicate by LINE, never by COUNT). **The same false text sits in append-only ADR-0162 and is corrected FORWARD by ADR-0165 — never edit it.** **RE-MEASURED at the re-entry and NOT in the RE-REVIEW's account: a single `0083` run witnesses `p07` ONLY**, because the `http1_probe_list` driver aborts at the FIRST failing probe; p12 is the same `present_match: false` × absent cell as `0085` probe 2 and is independently witnessed there. **THE §7.5 GATE IS RE-RUN AND LANDED — do NOT re-run it, and do NOT treat the earlier `# §5 state-4 — the §7.5 phase-done gate` section as current.** The gate that covers HEAD is `PROGRESS.md`'s `# §5 state-4 RE-VERIFICATION — the §7.5 phase-done gate, re-run in full` section, whose every output is quoted VERBATIM; the earlier state-4 section was measured at `406c379` and is superseded but LEFT AS LANDED (append-only, D-3.5). **Gate (f) is STILL REFUSED**, not merely open: the §5 state-5 RE-REVIEW returned CHANGES-REQUESTED (ADR-0165) and its charter has now been DISCHARGED by the §5.2 state-3 RE-ENTRY, so the next session is the **§5 STATE-4 RE-VERIFICATION**, then a state-5 RE-REVIEW; only a RE-REVIEW that APPROVES can close gate (f). Each is a SEPARATE session (§5.1; ADR-0127). **ACCESS-LOG FIXTURE-AUTHORING RULE, now standing: EVERY probe in an `http1_access_log_byte_exact` fixture must carry a DISTINCT `path:`** (route at `match: { prefix: "/" }`). That driver asserts only a per-side line COUNT plus whole-line cross-proxy equality, so identical rendered lines cannot attribute the survivor to a probe and any regression that MOVES the keep between probes passes GREEN — MEASURED on `0084`/`0085`, both of which were blind that way until the re-entry. Rationale, evidence and the free-at-runtime `%REQ(:PATH)%` mechanics: `BEHAVIOR_CONTRACT.md` Phase 75 §G. **DO NOT collapse `0084`/`0085` back to a shared path**, and do not touch a probe, an `expect_logged` value or the probe ORDER. **THE LOCAL HOST-FLAKE SET IS A STABLE CORE OF FIVE PLUS A VARYING STARTUP-RACE TAIL — the long-repeated "exactly FIVE tests RED on this host" figure is INCOMPLETE (ADR-0164).** **STABLE CORE (present in ALL THREE state-4 sweeps, and failing DETERMINISTICALLY in isolation — that determinism IS the environmental signature, NOT a regression):** the four `access_log_*_upstream_reset` binaries (`TcpCloseBackend` IPv6-unreachable: upstream `rf: UF` from `immediate_connect_error:_Network_is_unreachable` at `[fdc4:f303:9324::254]`, envoy-rust `rf: UC`) and `admin_config_dump_server_info` (the `192.168.65.2` bridge-IP family; `envoy-rust-only: []`). **FLOATING TAIL (membership varied 1 / 0 / 2 across the three state-4 sweeps, and every member PASSES in isolation — the OPPOSITE signature):** `http1_router_upstream_fixture`, `xds_file_based_rds_fixture` (both `fixture passes: upstream Envoy never became accept-ready` / `not accept-ready within 10s: Connection refused` — a RED that NEVER REACHED AN ASSERTION) and `client::tests::send_request_maps_h2_handshake_failure_to_typed_error` (`expected H2ClientHandshake, got Ok(ClientStream …)`). **The first two are NOT named in `PLAN.md:1600`'s flake list** — do not let that route you to a §5.2 re-entry; adjudicate each RED by the ADR-0164 four-part test (assertion never reached + passes in isolation + absent from some sweep + untouched by the phase's surface), never by membership in an enumerated list. Failure TEXT for ALL EIGHT is quoted in `PROGRESS.md`'s state-4 RE-VERIFICATION section, and all eight PASS in CI on the same SHA. **Run the sweep 2-3× and DIFF the failing SET** — with one run the tail is invisible and the core looks like the whole set. **The arithmetic identity to re-check every sweep: `local passed + failed == CI passed`** — measured `2099 + 6`, `2100 + 5` and `2098 + 7`, all `= 2105`, against a CI total of **2105** re-derived from the run log; the SUM is invariant even when the split moves. **THE h2spec GATE IS *NOT* VACUOUS IN CI — SETTLED, do NOT re-raise it** (ADR-0163; now CF-75-3, which wants its OWN phase). Locally it self-skips silently and `cargo test` swallows the notice, so a local `3 passed` proves nothing — run it with `--nocapture`, or report the criterion `PLAN.md` actually states, **"unchanged"** (`known-failures.txt` byte-identical, no H2/codec/conformance code touched). The genuinely real adjacent gap: `ci.yml` runs `cargo test --workspace` with NO `--no-fail-fast`, so on a RED CI run conformance status is UNKNOWN, not green. **Never trim `known-failures.txt` (21 lines) on local evidence** — this host scores h2spec 3.5/2 as PASS where CI does not. **FOUR ENGINE MUTATIONS ARE NOW ON RECORD AND THEY HIT DIFFERENT CELLS — never substitute one for another; read ADR-0162 and `BEHAVIOR_CONTRACT.md` Phase 75 §D first.** (a) Hoisting `(_, None)` to the TOP: FOUR in-process RED and `0084` GREEN are both CONFIRMED, **but "both fixtures GREEN" is REFUTED (ADR-0165) and §D is now CORRECTED ON DISK — `0085` AND `0083` go RED**, so the arm ORDER is guarded in-process AND cross-proxy. RE-MEASURED at the re-entry with an unmutated control from the same worktree: control `649 passed; 0 failed` + all three fixtures `1 passed`; mutated `645 passed; 4 failed`, `0084` `1 passed`, `0085` and `0083` RED. It breaks **P1** *and* the `present_match: false` × absent cell; **D1** is left correct. (b) `(_, None) => return false` → `(_, None) => false` is the true **D1** revert and REDs `0084` with `envoy_rust=2, envoy=1`. (c) `PresentMatch` → `if *want_present { v.is_some() } else { true }` is the pre-75.1 **D2** revert and REDs `0085`. (d) The polarity swap (`==` → `!=`) and the XOR drop USED to leave both fixtures GREEN and now RED them (`envoy="STATUS=200 PATH=/absent" envoy-rust="STATUS=200 PATH=/present"` on `0085`; `envoy="STATUS=200 PATH=/valmiss" envoy-rust="STATUS=200 PATH=/valmatch"` on `0084`) — that is the FIRST re-entry's TDD evidence, INDEPENDENTLY REPRODUCED character-for-character at the RE-REVIEW, and it must STAY red. **A mutation RED saying `not accept-ready within 10s: Connection refused` NEVER REACHED AN ASSERTION and is NOT evidence** (the `wait_accept_ready` port-reuse flake) — re-run, read the failure TEXT, and always run the UNMUTATED control from the SAME worktree. **`PLAN.md` CENSUS RECIPES CAN BE UNSOUND — re-derive, don't trust.** Enumerate workspace members from the ROOT `Cargo.toml`, never a `crates/*/src/{lib,main}.rs` glob: **22 of 22** roots carry `#![forbid(unsafe_code)]` — **D-3.8 HOLDS**. **RE-DERIVE EVERY `BEHAVIOR_CONTRACT.md` SITE BY TEXT ANCHOR, NEVER by a line number — this bit TWICE MID-SESSION at state-3.** The file is now **3645** lines (3599 before the second re-entry, 3560 before the first). The re-entry located §D by anchoring on `^\*\*§D The guard` / `^\*\*§E TRAP A` rather than on any quoted number. **RE-DERIVE EVERY INHERITED CENSUS — NINE have now been caught wrong or incomplete:** the `3503` `BEHAVIOR_CONTRACT.md` line count (state-4), the Standing-traps character count (state-5), the M74-31 "FIVE sites" figure (the PLAN-write), the state-5 record's own "7 Minor / 889-line `REVIEW.md`" (the re-entry — both were written before finding M-8 was appended; the landed file is **931** lines with **8** Minors), the "**5 targets / 63 seeds**" fuzz pairing (state-4 RE-VERIFICATION — **63** is `parse_bootstrap`'s seed count ALONE against a corpus total of **75**: 63 + 8 `accesslog_format_parse` + 3 `jwt_parse` + 1 `grpc_health_decode` + **ZERO** for `cdn_loop_parse`), the "exactly FIVE host-flake REDs" set (ADR-0164, above), the "five caught-wrong censuses" figure ITSELF (RE-REVIEW finding M-2 — `PROGRESS.md` said five where `STATE.md`, landed in the SAME commit, said six; reconciled to **NINE** at the re-entry), the "three `crates/` FILES" figure (M-1 — it is three EDITS across **TWO** files) and the "seven ROADMAP rows misclassify" figure (M-3 — only **two** do). **Treat every number you inherit as suspect — the Standing-traps line itself measured **14653** characters before this session's rewrite, which DID match its inherited figure; the point is to MEASURE, not to assume either way.** **DERIVE fixture→test-binary maps FROM THE TREE, never from the fixture name** (`grep -rl "tests/fixtures/00NN" tests/differential/tests/*.rs`): `0079`/`0080` are `access_log_and_filter`/`access_log_or_filter`, which a name-guessed grep misses and reports as a clean-looking `0 hits`. **Do NOT add `on_header_missing` to fixtures `0081` OR `0082`** — envoy-rust requires a `value` there, which would make the key RESOLVE and silently vacate the witness while the fixture stayed GREEN (ADR-0155 PV-6); their occurrences are `#` COMMENTS documenting the deliberate omission. **`0081` has THREE probes and expects TWO logged lines, byte-distinct so ORDER is pinned** — do not touch a probe, an `expect_logged` value or the probe ORDER. **THE 75.1 ENGINE IS LANDED AND IS THE PREMISE OF 75.2 — do NOT re-derive, revert or "simplify" it.** `HeaderMatcher::matches` (`crates/envoy-config/src/matcher.rs`) is an exhaustive tuple `match (&self.mode, value)` whose `(_, None) => return false` arm sits **AFTER** the `PresentMatch` arm and **BEFORE** every value arm, closed by an XOR with `invert_match`; the rule in full is `BEHAVIOR_CONTRACT.md` Phase 75 §A. **75.2 CHANGED NO `crates/` BEHAVIOR** — THREE edits across **TWO** files (`bootstrap.rs`, `matcher.rs`; `command_operator.rs` was NEVER edited — RE-REVIEW finding M-1), all comment-only, **RE-PROVEN over the FULL phase span by comment-stripped whole-file compare: ZERO executable lines changed.** **State the BASE: it is `3f0ec89` (75.1's landing), NOT `29055a5`** — and the inherited `grep -vE '^[+-]\s*///'` filter strips DOC comments ONLY, so over the true span it prints SIX non-empty `//` lines and FALSE-FAILS. Widen it to `'^[+-]\s*(///|//!|//)'` or use the whole-file compare (RE-REVIEW finding I-2, **CLOSED at the re-entry, which RE-MEASURED both**: the `///`-only filter prints **6** over `3f0ec89..HEAD`, the widened one prints EMPTY, and the whole-file compare reports both files byte-identical. The filter errs CONSERVATIVELY — it can false-FAIL, never false-PASS, so no green was ever faked). **Access-log FIXTURE-AUTHORING rules live in the `0078`-`0085` READMEs and `BEHAVIOR_CONTRACT.md` Phase 75 §G/§H** (`%REQ(NAME)%` allow-list gating; the per-side recipe MEASURED 7/7; `codec_type: HTTP1` on BOTH sides is NOT a divergence, ADR-0158 C3; ONE log file per side, which is why one rule with two polarities needs a sibling PAIR). **CF-72-1, M74-31 and the 75.1 review's M-1/M-2/M-3/N-1/N-2 are all CLOSED**; the append-only "FIVE" figure at `74/REVIEW.md:1269` and `ROADMAP.md` row 75.2's refuted five-site figure must NOT be "fixed" (append-only; the refutation lives in ADR-0161 — a recorded decision, review finding M-7). **CF-75-2 is recorded in THIS FILE, *NOT* in `BEHAVIOR_CONTRACT.md`** (`grep -c "CF-75-2"` there = **0**, here = **5**); both new fixture READMEs said otherwise and were CORRECTED at the re-entry. **CF-72-2 and CF-75-1 are BANKED, NOT FIXED** (`BEHAVIOR_CONTRACT.md` phase-72 `**§D` three members + `**§G`); owner is a future `HeaderMatcher` wire-shape-parity phase. **CF-75-2 is OPEN and OUT OF SCOPE:** upstream COMMA-JOINS duplicate header values before value matching while envoy-rust matches only the FIRST occurrence. The PRESENCE axis is PARITY, so it is NOT a regression and none of 75.1's rule is affected; it spans all SIX value modes across all FIVE consumers and needs its OWN measured phase — do NOT "fix" it opportunistically. **NEVER weaken a fixture.** **TWO CONFLATION TRAPS — do NOT unify them; both are recorded in full as `BEHAVIOR_CONTRACT.md` Phase 75 §E and §F.** `HeaderMatcher.present_match` vs `ValueMatcher.present_match` (RBAC / access-log metadata, fixture `0044`), where `present_match: false` NEVER matches — a DIFFERENT and CORRECT rule that must NOT be "fixed" to match; since 75.1 the two agree in THREE of four cells and differ in exactly ONE (ABSENT × `want=false`). And `HeaderMatcher.invert_match` vs `MetadataMatcher.invert` (CF-74-1) — MEASURED accepted-but-INERT upstream, so it stays boot-fatal here; do NOT "implement" it. **CF-74-6:** the wrapped `match_if_key_not_found: { value: <bool> }` spelling is accepted AND honored upstream but BOOT-FATAL here — a documented REJECT-direction gap; do NOT "fix" it (bare-only, ADR-0063 / ADR-0133). **The ADR-0150 seam must keep holding:** `envoy-accesslog` has ZERO workspace deps and MUST NOT depend on `envoy-config`; matchers cross as injected **trait objects** (`Arc<dyn MetadataMatch>`, `Arc<dyn HeaderMatch>`); `LogFilter` has NO `Eq`/`PartialEq` — do not add either. Adjudicate any sweep with `--no-fail-fast` + a FULL-output redirect, **NEVER `tail`** (memory `never-pipe-verification-runs-through-tail`; a bare `cargo test --workspace` aborts at the first failing BINARY); run it 2-3× and DIFF the failing SET; isolation re-runs name the target binary and `0 passed; N filtered out` is **NOT** a pass (memory `cargo-test-p-name-false-green-filtered-out`), while `error: no test target named …` exits 101 exactly like a real RED — **read the failure TEXT, always.** A MASS differential RED (`client error (Connect)`, dozens at once) is the Docker daemon down (memory `docker-desktop-down-after-reboot-kvm-acl`: `sudo setfacl -m u:esa:rw /dev/kvm && systemctl --user restart docker-desktop`). **`cargo build -p envoy-bin` before ANY local differential** (memory `differential-harness-uses-debug-envoy-bin`); a ~1-3 s green on a backend-free fixture with a warm image is NORMAL, not a skip. A clippy/build green finishing in ~1 s is PARTIALLY CACHED — `touch` the changed files and re-run; **`cargo clippy` prints `Checking`, NOT `Compiling`** (memories `mutation-check-needs-forced-rebuild`, `clippy-prints-checking-not-compiling`), and a cargo BUILD green can be audited by re-running it and confirming the second run is fully cached. Any tree-MUTATING probe belongs in its OWN scratch `git worktree`, created `--detach` at HEAD (memories `mutation-checks-collide-with-parallel-subagents`, `worktree-subagents-get-stale-base`); remove only your OWN worktree. `cargo fuzz` runs from the CRATE dir and takes a **DIRECTORY, not a file**; a NEW seed needs a `!`-un-ignore line (memory `fuzz-corpus-seed-gitignored-by-default`). `cargo deny check` can red on a fresh unrelated RustSec advisory — patch-bump the dep (memory `cargo-deny-reds-on-unrelated-advisory`); its `license-not-encountered` warnings are unmatched ALLOWANCES in `deny.toml`, NOT violations. Do NOT revert landed `00`-`74` work NOR sub-phase `75.1` NOR the parallel workstream's `envoy-http2` header-list bound; never add `rbac` to `is_terminal_network_filter`; never reject a top-level `filters: []` sink list; preserve ADR-0016/0124/0131/0133/0136/0137/0139/0140-0143/0144-0149/**0150**-**0164**; **ADR-0028 is NOT lifted.** The phase-73 `< 2` rejection on `and_filter`/`or_filter { filters }` is a composition-arm PGV rule, NOT the top-level `filters: []` sink-list rule — do NOT "relax" it. Do NOT disturb the **38** existing access-log fixtures. Any ROADMAP row edit preserves 6 cells + escapes `\|`; rows `36`/`38`/`39`/`52`/`54`/`66`/`70` are already malformed — do NOT "fix" them (append-only) — but **only rows `66` and `70` actually MISCLASSIFY under a naive `awk -F'|' $5`** (in the other five the stray pipes sit AFTER the status column), so the naive sweep yields `done=100`, not 95 (RE-REVIEW finding M-3) (splitting on `' | '` WITH the spaces is robust, with status in field **4**, and yields the correct 104/102 census). **`DECISIONS.md` is NOT chronological AND is too large to Read whole** (chunk by `grep -n '^## ADR-NNNN'` + offset/limit; memory `decisions-md-not-chronological`; head **ADR-0165**, next **ADR-0166** — the re-entry fired NO ADR, because every decision it executed was already ADR-0165's). `ROADMAP.md` / `BEHAVIOR_CONTRACT.md` (**3645** lines after the re-entry's §D/§G corrections, up from 3599) / `STATE_HISTORY.md` / this phase's `PLAN.md` (**1631**), `PROGRESS.md` (**3037**, up from 2547) and `REVIEW.md` (**1568**) all exceed the single-read limit — chunk them. **`REVIEW.md` CARRIES TWO VERDICTS AND BOTH CHARTERS ARE NOW DISCHARGED** — the first review's §10 by the FIRST `# §5.2 state-3 RE-ENTRY` section (`PROGRESS.md:1317`), and the `# §5 state-5 RE-REVIEW`'s §R9 (`REVIEW.md:935`) by the SECOND, `# §5.2 state-3 RE-ENTRY (SECOND)` (`PROGRESS.md:2549`). Read the PROGRESS sections, not the review, for what is closed. **envoy-bin writes `ConfigError` to STDOUT** and takes only `-c <path>`. `STATE.md` top-section blocks are single multi-thousand-character lines (the Standing-traps line alone MEASURED 20 519 characters before this re-entry's rewrite — MEASURE before editing, never assume or inherit a range; RE-REVIEW finding M-7, verified still fixed) — script-edit + a 0×-grep afterwards (memory `state-md-blocks-are-single-lines-edit-hazard`); `STATE_HISTORY.md` archive headers must be matched by EXACT equality (memory `state-history-archive-headers-need-exact-match`, since per-phase variants share the prefix) and BOTH files backed up first; ADR-0035 relocation checks are DELTA-based, never "no duplicates" (memory `state6-relocation-check-must-be-delta-based`) — allowlist the unchanged `**slug:**` line only, and run the check against the FULL superseded set **INCLUDING the `### Doctrine reminders` §5.1 bullet, which is NOT exempt** (the state-4 session rewrote it without relocating its prior text; review finding M-8, repaired at state-5 and honoured again here). Per **ADR-0160** the enduring Standing-traps line is rewritable only after relocating the superseded text verbatim. **Beware VACUOUS greps** — a glob over an unresolved path returns `0 hits` that LOOKS clean; assert the number of files scanned. And the opposite: a grep can legitimately return >0 because a record QUOTES the defect it fixes — **adjudicate by LINE and by FILE, never by COUNT.** **Recon traps on this host, all memory-backed — read them BEFORE hand-rolling any probe:** `docker-bind-mounts-stale-cached-on-this-host` (fresh FILENAME per revision; `docker cp`, not `--volumes-from`), `state0-recon-docker-needs-port-mapping` (`docker -p`, never `--network host`), `state0-recon-backend-sibling-container`. Upstream Envoy will NOT create an access-log dir and flushes on a ~10 s TIMER (the harness handles both); YAML 1.1 parses an unquoted `cluster: y` as `true`, which upstream rejects. **A `pkill -f <pattern>` whose pattern also matches the invoking shell's own command line kills that shell** — kill by PID or use `pgrep -x`. Cold-start reads `STATE.md` FIRST (disk-authoritative). Confirm CI with the **FULL 40-char SHA** (memory `gh-run-list-commit-needs-full-sha`) and check the STEP COUNTS, not merely the conclusion; a job `cancelled` with `runner_name:""` + `steps:0` never got a runner — rerun the same SHA (memory `ci-run-cancelled-with-no-runner-is-starvation`). `next-prompt.txt` is gitignored — refresh on disk, do NOT `git add` it. **CONCURRENCY:** `.claude/worktrees/agent-*` worktrees and a long-running `envoyproxy/envoy:v1.33.0` container (observed Up 2 days at state-4 and still up at the re-entry) belong to a PARALLEL workstream — LEAVE THEM ALONE (remove only your OWN artifacts, memory `concurrent-loop-sessions-race-on-phase-pick`) and EXCLUDE them from any repo-wide `find`/census/grep — prefer `git ls-files` or the root `Cargo.toml`. Re-check `git fetch origin --prune` at session start AND again immediately before committing. **RE-REVIEW ADDITIONS (ADR-0165).** **A REVIEWER MUST NOT FIX WHAT IT GRADES** (§5.1; ADR-0127) — the §D correction belongs to the §5.2 re-entry, not to the review that found it; a reviewer that edits its subject destroys the isolation the whole state machine rests on. **THE CI-TOTAL RECIPE IS NOT SELF-VALIDATING:** `grep -o 'test result: ok\. …'` discards `test result: FAILED.` lines before `awk` sees them, so its `failed=0` is true BY CONSTRUCTION and the binary count would silently fall below 162 rather than flag anything — use `grep -oE 'test result: (ok|FAILED)\. …'` and assert the count separately (M-4). **THREE SIBLING ACCESS-LOG FIXTURES STILL CARRY THE I-1 BLINDNESS — `0078`, `0079`, `0080`** (every probe shares `path: /x` under a `STATUS=… PATH=…` format, so a regression that MOVES the keep passes GREEN); all three exercise `header_filter`, and `0078` is the stencil `0084` was copied from. **`0081`/`0082`/`0040` are NOT blind** — they carry a second discriminator (`M=%DYNAMIC_METADATA%` rendering `-`/`1`/`2`; `ua=`/`xff=`) — a subagent claim CORRECTED DOWNWARD on disk. Banked as **CF-75-4**. **§G NO LONGER waives existing fixtures** — the re-entry rewrote its closing sentence to bind EVERY `http1_access_log_byte_exact` fixture, with the full family census and `0078`/`0079`/`0080` NAMED as outstanding. Do NOT fix them opportunistically (§6.3); the owner is whoever next touches the access-log fixture family. **`cdn_loop_parse` HAS a `ci.yml` step but ZERO tracked corpus seeds**, so it fuzzes from an empty corpus (the other four have 63 / 8 / 3 / 1 of **75** tracked files) — banked as **CF-75-5**, NOT a 75.2 defect. **A SUBAGENT FINDING IS A CLAIM, NOT A RESULT** — two of this review's four dimensions returned figures that were WRONG or WEAKER than stated and were corrected by re-verifying on disk; re-verify every one, and prefer MEASURING a hand-derivation over quoting it. **A `_Historical_`/"see below" POINTER MUST POINT AT SOMETHING ON DISK** — `PROGRESS.md:1720`'s "the ADR-0035 delta check below" had no below; the numbers lived only in the `da06059` commit message (M-6). **CLOSED at the re-entry by APPENDING the block, so the forward pointer now resolves on disk**: 13 relocated `STATE.md` lines, 13/13 at delta +1, 0 history lines lost, INCLUDING the `### Doctrine reminders` §5.1 bullet. Disk is the only memory (D-3.4).
_Historical (the SUB-PHASE 75.2 §5 STATE-5 RE-REVIEW `## Next expected skill` pointer + its `WHAT 75.2's §5.2 STATE-3 RE-ENTRY INHERITS` block + that block's own `_Historical_` pointer line + the `### Doctrine reminders` §5.1 bullet — superseded by this SUB-PHASE 75.2 §5.2 STATE-3 RE-ENTRY (SECOND); all relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this session per ADR-0035, and the ADR-0035 delta check was run against that FULL superseded set INCLUDING the doctrine bullet, which is NOT exempt (first-review finding M-8). The Standing-traps block is ENDURING and is carried forward IN PLACE, but at this session it was superseded — its full prior text (**20519** characters, MEASURED here, not inherited) was relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) under the `## Next expected skill` archive header FIRST, then rewritten per **ADR-0160** by eleven surgical substring substitutions, each of which the rewriting script asserted matched EXACTLY ONCE before touching anything: clause (a)'s "§D is REFUTED pending correction" framing is now "§D is CORRECTED ON DISK", the gate-(f) clause now routes to the state-4 RE-VERIFICATION, the caught-wrong-census count goes SIX → **NINE**, the I-2 recipe carries its re-measured evidence, the mutation-(a) clause records the re-entry's own control-and-mutation numbers, §G's "future fixtures only" waiver is retired, and the M-6 "below" pointer is marked CLOSED. The rewritten line measures **23222** characters — a MEASURED delta of **+2703**, not a rounded one.)_
> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases incl. the phase-37 state-1..5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit


**Sub-phase 75.2 §5.2 STATE-3 RE-ENTRY (SECOND) — the `REVIEW.md` §R9 charter DISCHARGED; NO ADR fired:** ONE commit on top of the state-5 RE-REVIEW head `b369f9c5de87e442ae90c6d1c8916ff6979dc8c9` (CI `success` on that SHA, run **`30302697031`**, both jobs at FULL step counts **15** and **13**). **Files changed: FOUR, and ALL FOUR are under `docs/envoy-rust/` — NOT ONE is code, a fixture, a test entrypoint or a ROADMAP row.** `docs/envoy-rust/BEHAVIOR_CONTRACT.md` (the Phase 75 block's §D rewritten to the MEASURED behavior — finding I-1, the blocking one — plus §G's closing sentence widened to bind EXISTING fixtures, naming `0078`/`0079`/`0080` as CF-75-4; 3599 → **3645**), `docs/envoy-rust/phases/75.2-headermatcher-absence-accesslog/PROGRESS.md` (the APPENDED `# §5.2 state-3 RE-ENTRY (SECOND)` section; 2547 → **3037**, +490 insertions / 0 deletions, append-only VERIFIED by re-diffing the first 2547 lines against a backup), and `docs/envoy-rust/STATE.md` / `docs/envoy-rust/STATE_HISTORY.md` (the ADR-0035 relocation of the four top-section blocks PLUS the `### Doctrine reminders` §5.1 bullet, delta-checked). **NO `crates/` code, NO fixture, NO probe, NO `expect_logged` value, NO probe ORDER, NO test entrypoint, NO `PLAN.md`/`SPEC.md`/`REVIEW.md`, NO ROADMAP row, NO `ci.yml`, NO fuzz target, NO corpus seed, NO `known-failures.txt` line, NO landed ADR, NO `75.1/` artifact and NO frozen `75/SPEC.md` was touched.** **WHY NO ADR FIRED:** every decision this session executed is already a landed decision of record in **ADR-0165** — the §D correction (its DECISION 1, which states the exact true sentence to write), the comment-only recipe's base and filter (DECISION 3), CF-75-5 (DECISION 4), CF-75-4 and the §G scope change (DECISION 5), and the NO-ACTION posture on landed append-only artifacts (its Consequences). A §5.2 re-entry EXECUTES the review's decisions; it does not make new ones, and D-3.5 authorizes a new ADR only for a genuinely NEW decision. Ledger head stays **ADR-0165**, next **ADR-0166**. Next: **sub-phase 75.2's §5 state-4 RE-VERIFICATION** (a SEPARATE session).

_Historical (the SUB-PHASE 75.2 §5 STATE-5 RE-REVIEW Last-commit block + that session's own `_Historical_` pointer line — superseded by this SUB-PHASE 75.2 §5.2 STATE-3 RE-ENTRY (SECOND); relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this session per ADR-0035.)_
> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases incl. the phase-37 state-1..5 sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated


2026-07-28 (sub-phase 75.2 **§5.2 STATE-3 RE-ENTRY (SECOND) COMPLETE — every `REVIEW.md` §R9 finding CLOSED or its NO-ACTION disposition RECORDED; GATE (f) STILL REFUSED; NO ADR fired**. **STATE DETECTION: all four phase artifacts present; `REVIEW.md`'s CURRENT verdict — the `# §5 state-5 RE-REVIEW` section at line 935 — was CHANGES-REQUESTED and no `PROGRESS.md` section yet closed it, so it was a LIVE CHARTER: §5.2 step 3, NOT step 4.** **The TDD RED was RE-MEASURED rather than transcribed**, in this session's own `--detach` scratch worktree with a private `CARGO_TARGET_DIR` and the UNMUTATED CONTROL RUN FIRST: mutation H (the arm-ORDER hoist) gives in-process `645 passed; 4 failed`, `0084` GREEN, `0085` RED (`0 access-log lines but 1 were expected`), `0083` RED (`p07-absent-keeps-GUARD`, `p07=MATCH` vs `p07=NOMATCH`) — the RE-REVIEW's refutation REPRODUCES, and the restored control returns `649 passed; 0 failed` with all three fixtures `1 passed`. **I-1 CLOSED:** `BEHAVIOR_CONTRACT.md` §D now states the TRUE conclusion — **the arm ORDER is guarded in-process AND cross-proxy** — with a three-row measured table and a `> **History**` blockquote so the correction cannot be silently re-inherited; the site was re-derived BY TEXT ANCHOR. **I-2 CLOSED:** re-proven over the TRUE base `3f0ec89..HEAD` — the `///`-only filter prints 6, the widened filter prints EMPTY, and the comment-stripped whole-file compare reports both `crates/` files byte-identical (**ZERO executable lines changed across the sub-phase**). **M-1/M-2/M-3/M-6/M-7 CLOSED by measurement** (TWO `crates/` files not three; caught-wrong censuses reconciled to **NINE**; only ROADMAP rows **66**/**70** misclassify, naive `done=100`; the ADR-0035 delta block APPENDED so `PROGRESS.md:1720`'s "below" now resolves — 13/13 at delta +1, 0 history lines lost; the traps-line range verified still fixed). **CF-75-4 and CF-75-5 RECORDED** and §G widened to bind EXISTING fixtures. **N-4/N-6 NO-ACTION** — landed append-only artifacts. One NEW measured refinement the RE-REVIEW did not state: **a single `0083` run witnesses `p07` ONLY**, because the `http1_probe_list` driver aborts at the FIRST failing probe. Next: **sub-phase 75.2's §5 state-4 RE-VERIFICATION** (a SEPARATE session per §5.1 / ADR-0127); **no `stop` file** — the mission is NOT complete. **CF-75-3 and CF-75-2 remain open and OUT OF SCOPE.**)

_Historical (the SUB-PHASE 75.2 §5 STATE-5 RE-REVIEW Last-updated note + that session's own `_Historical_` pointer line — superseded by this SUB-PHASE 75.2 §5.2 STATE-3 RE-ENTRY (SECOND); relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this session per ADR-0035.)_
> Historical `## Last updated` notes — every superseded last-updated note (all closed phases incl. the phase-37 state-1..5 sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsection for fully-closed phase 64 (the `### Phase-64 state-5 code-review` narrative — phase 64 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-64 state-6 close-out when ROADMAP row `64` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 63 (the `### Phase-63 state-5 code-review` narrative — phase 63 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-63 state-6 close-out when ROADMAP row `63` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 61 (the `### Phase-61 state-1 brainstorm` / `### Phase-61 state-2 PLAN-write` / `### Phase-61 state-3 implementation` / `### Phase-61 state-4 verification` / `### Phase-61 state-5 code-review` narratives, relocated at the phase-61 state-6 close-out when ROADMAP row `61` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 58 (the `### Phase-58 state-1 brainstorm` / `### Phase-58 state-2 PLAN-write` / `### Phase-58 state-3 implementation` / `### Phase-58 state-4 verification` / `### Phase-58 state-5 code-review` narratives, relocated at the phase-58 state-6 close-out when ROADMAP row `58` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 57 (the `### Phase-57 state-1 brainstorm` / `### Phase-57 state-2 PLAN-write` / `### Phase-57 state-3 implementation` / `### Phase-57 state-4 verification` / `### Phase-57 state-5 code-review` narratives, relocated at the phase-57 state-6 close-out when ROADMAP row `57` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 42 (the `### Phase-42 state-5 code-review (active phase)` narrative — phase 42 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-42 state-6 close-out when ROADMAP row `42` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 43 (the `### Phase-43 state-5 code-review (active phase)` narrative — phase 43 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-43 state-6 close-out when ROADMAP row `43` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 44 (the `### Phase-44 state-5 code-review (active phase)` narrative — phase 44 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-44 state-6 close-out when ROADMAP row `44` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 45 (the `### Phase-45 state-5 code-review (active phase)` narrative — phase 45 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-45 state-6 close-out when ROADMAP row `45` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 46 (the `### Phase-46 state-5 code-review (active phase)` narrative — phase 46 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-46 state-6 close-out when ROADMAP row `46` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 47 (the `### Phase-47 state-5 code-review (active phase)` narrative — phase 47 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-47 state-6 close-out when ROADMAP row `47` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 48 (the `### Phase-48 state-5 code-review (active phase)` narrative — phase 48 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-48 state-6 close-out when ROADMAP row `48` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 49 (the `### Phase-49 state-5 code-review (active phase)` narrative — phase 49 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-49 state-6 close-out when ROADMAP row `49` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 50 (the `### Phase-50 state-3 implementation (active phase)` narrative — phase 50 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-50 state-6 close-out when ROADMAP row `50` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 51 (the `### Phase-51 state-3 implementation (active phase)` narrative — phase 51 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-51 state-6 close-out when ROADMAP row `51` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 53 (the `### Phase-53 state-1 brainstorm` narrative — phase 53 kept a single active-phase Notes subsection at "state-1 brainstorm" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-1 subsection remained in `## Notes` at close — relocated at the phase-53 state-6 close-out when ROADMAP row `53` flipped `done`, together with the inline superseded state-3 narrative) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 54 (the `### Phase-54 state-1 brainstorm` / `### Phase-54 state-2 PLAN-write` / `### Phase-54 state-3 implementation` / `### Phase-54 state-4 verification` / `### Phase-54 state-5 code-review` narratives, relocated at the phase-54 state-6 close-out when ROADMAP row `54` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 55 (the `### Phase-55 state-5 code-review (active phase)` narrative — phase 55 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state individually superseded in place and relocated to STATE_HISTORY.md at its own transition], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-55 state-6 close-out when ROADMAP row `55` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 56 (the `### Phase-56 state-5 code-review (active phase)` narrative — phase 56 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state individually superseded in place and relocated to STATE_HISTORY.md at its own transition], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-56 state-6 close-out when ROADMAP row `56` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 66 (the `### Phase-66 state-5 code-review (active phase)` narrative — phase 66 used a rename-in-place active-phase Notes subsection across its state-0/1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-66 state-6 close-out when ROADMAP row `66` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed sub-phase 67.1 (the `### Phase-67.1 §5 state-5 code-review` narrative — sub-phase 67.1 used a rename-in-place active-phase Notes subsection across its state-2..5 + §5.2 state-3 re-entry arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-67.1 state-6 close-out when ROADMAP row `67.1` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed sub-phase 67.2 (the `### Phase-67.2 §5 state-5 re-review` narrative — sub-phase 67.2 used a rename-in-place active-phase Notes subsection across its state-2..5 + §5.2 state-3 re-entry + state-4-re-verification + state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review subsection remained in `## Notes` at close — relocated at the phase-67.2 state-6 close-out when ROADMAP row `67.2` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
> Historical Notes subsection for fully-closed phase 68 (the `### Phase-68 §5 state-5 code-review` narrative — phase 68 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-68 §5 state-6 close-out when ROADMAP row `68` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 69 (the `### Phase-69 §5 state-5 re-review` narrative — phase 69 used a rename-in-place active-phase Notes subsection across its state-3 → §5.2 state-3 re-entry → state-4 re-verification → state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review subsection remained in `## Notes` at close — relocated at the phase-69 §5 state-6 close-out when ROADMAP row `69` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 70 (the `### Phase-70 §5 state-5 re-review (2nd)` narrative — phase 70 used a rename-in-place active-phase Notes subsection across its state-1..5 + two-§5.2-re-entry + two-state-4-re-verification + two-state-5-re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review-(2nd) subsection remained in `## Notes` at close — relocated at the phase-70 §5 state-6 close-out when ROADMAP row `70` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 71 (the `### Phase-71 §5 state-5 code-review` narrative — phase 71 used a rename-in-place active-phase Notes subsection across its state-0/1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-71 §5 state-6 close-out when ROADMAP row `71` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 72 (the `### Phase-72 §5 state-5 re-review` narrative — phase 72 used a rename-in-place active-phase Notes subsection across its state-1..5 + §5.2 state-3 re-entry + state-4 re-verification + state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 re-review subsection remained in `## Notes` at close — relocated at the phase-72 §5 state-6 close-out when ROADMAP row `72` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 73 (the `### Phase-73 §5 state-5 code-review` narrative — phase 73 used a rename-in-place active-phase Notes subsection across its state-0/1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-73 §5 state-6 close-out when ROADMAP row `73` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 74 (the `### Phase-74 §5 state-5 re-review (2nd)` narrative — phase 74 used a rename-in-place active-phase Notes subsection across its state-0/1..5 + TWO §5.2 state-3 re-entry + TWO state-4 re-verification + TWO state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review-(2nd) subsection remained in `## Notes` at close — relocated at the phase-74 §5 state-6 close-out when ROADMAP row `74` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed sub-phase 75.1 (the `### Phase-75.1 §5 state-5 code-review` narrative — sub-phase 75.1 used a rename-in-place active-phase Notes subsection across its state-2..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the sub-phase-75.1 §5 state-6 close-out when ROADMAP row `75.1` flipped `done`; parent row `75` deliberately stays `in-progress` until sibling `75.2` is `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: **one state per session; do not chain states.** **PARENT PHASE 75 WAS SPLIT (§6.1, ADR-0157); SUB-PHASE `75.1` IS `done`; SUB-PHASE `75.2` IS `in-progress`** — its state-3 implementation, its state-4 verification, its state-5 code review, its **§5.2 state-3 RE-ENTRY**, its **§5 state-4 RE-VERIFICATION**, its **§5 state-5 RE-REVIEW** and now its **SECOND §5.2 state-3 RE-ENTRY** have each run as their OWN session. **Gates (a)-(e) are LANDED GREEN but were measured BEFORE this re-entry; gate (f) is REFUSED.** The remaining arc is **§5 state-4 RE-VERIFICATION → state-5 RE-REVIEW → state-6 CLOSE-OUT**, each its own session, and only the close-out flips BOTH row `75.2` AND parent row `75` to `done`. **A §5.2 re-entry re-enters at step 3, NOT step 4** — that asymmetry is the whole point of §5.2, and this sub-phase has now exercised it TWICE. **ADR-0164** records that a gate-(b) RED outside `PLAN.md`'s enumerated flake list is NOT automatically a regression NOR automatically a flake: adjudicate it by the four-part test (assertion never reached + passes in isolation + absent from some sweep + untouched by the phase's surface), and record the call — the state-5 RE-REVIEW CONFIRMED that adjudication independently and noted that leg (iv) is the load-bearing one. **ADR-0165** records that a MEASURED claim in the CANONICAL `BEHAVIOR_CONTRACT.md` was refuted by re-measurement, and that a reviewer must NOT fix what it grades (§5.1) — the correction belonged to the re-entry, which has now made it. **M74-31 is CONSUMED** (75.2 Task 7, four sites); **the first review's I-1..I-4, M-1..M-6, N-1/N-2/N-7 are CONSUMED** and its **M-7/M-8/N-3/N-4/N-5/N-6 are recorded NO-ACTION decisions**; **the RE-REVIEW's I-1/I-2 + M-1..M-3 + M-6/M-7 are now ALSO CONSUMED**, its N-4/N-6 are recorded NO-ACTION, and **CF-75-4 / CF-75-5 are banked**.

- **On the §5.1 rule and its ONE recorded exception (read `ADR-0127`).** The phase-66 state-6 close-out was run in the SAME session as its state-5 code-review, at **explicit human instruction**. That was a one-off, human-authorized override; `BOOTSTRAP_PROMPT.md`, `MISSION.md` and `SKILL_ROUTING.md` were **not modified**, and **§5.1 remains binding on every autonomous session.** ADR-0127 records why the 5→6 transition alone was safe to chain (the close-out changes no code, re-runs no gate, and consumes only disk facts) and why **0→1, 1→2, 2→3, 3→4 and 4→5 must never be chained** — each carries a genuine self-review hazard, since the context that wrote an artifact would be grading it. Per memory `closeout-and-pick-are-separate-sessions`, the next-phase pick is STILL a separate session. Do not read the git history as licence to chain.

> Historical Notes subsection for fully-closed sub-phase 67.3 (the `### Phase-67.3 §5 state-5 re-review` narrative — sub-phase 67.3 used a rename-in-place active-phase Notes subsection across its state-2..5 + §5.2 state-3 re-entry + state-4-re-verification + state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review subsection remained in `## Notes` at close — relocated at the phase-67.3 §5 state-6 close-out when ROADMAP row `67.3` [and parent row `67`] flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 65 (the `### Phase-65 state-5 code-review` narrative — phase 65 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-65 state-6 close-out when ROADMAP row `65` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsection for fully-closed phase 52 (the `### Phase-52 state-5 code-review (active phase)` narrative — phase 52 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2/3/4 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-52 state-6 close-out when ROADMAP row `52` flipped `done`, together with the standing 05.1/05.4 future-reviewer note it carried) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 41 (the `### Phase-41 state-5 code-review (active phase)` narrative — phase 41 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the §6.2-recon PIVOT from `%REQ_WITHOUT_QUERY%` to `%ROUTE_NAME%` landed at state-2; the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-41 state-6 close-out when ROADMAP row `41` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 40 (the `### Phase-40 state-5 code-review (active phase)` narrative — phase 40 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-40 state-6 close-out when ROADMAP row `40` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 39 (the `### Phase-39 state-5 code-review (active phase)` narrative — phase 39 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-39 state-6 close-out when ROADMAP row `39` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
> Historical Notes subsection for fully-closed phase 38 (the `### Phase-38 state-2 PLAN-write (active phase)` narrative — phase 38 used a single active-phase Notes subsection for its whole arc; the state-3/4/5 sub-state narratives were superseded in the four-top-section archive blocks per-session, so only this state-2 subsection remained in `## Notes` at close — relocated at the phase-38 state-6 close-out when ROADMAP row `38` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) and for parent phase 25 (the `### Phase-25 state-1 brainstorm` pick + recon-finding narrative, relocated at the 25.2 state-6 close-out when parent `25` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 26 (the `### Phase-26 state-1 brainstorm` pivot/rejected-alternatives/key-scoping narrative, relocated at the phase-26 state-6 close-out when row `26` flipped to `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 27 (the `### Phase-27 state-1 brainstorm` / `### Phase-27 state-2 PLAN-write` / `### Phase-27 state-4 verification` narratives + the now-consumed `### Phase-27 carry-forwards` [M26-1..M26-8] block, relocated at the phase-27 state-6 close-out when row `27` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 28 (the `### Phase-28 state-1 brainstorm` / `### Phase-28 state-2 PLAN-write` / `### Phase-28 state-3 implementation + state-4 verification` / `### Phase-28 state-5 code review` narratives + the now-consumed `### Phase-28 carry-forwards` [M27-1..M27-3] block, relocated at the phase-28 state-6 close-out when row `28` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 29 (the now-consumed `### Phase-29 carry-forwards` [M28-1..M28-3] block + the `### Phase-29 state-1 brainstorm` / `### Phase-29 state-2 PLAN-write` / `### Phase-29 state-3 implementation` / `### Phase-29 state-4 verification` / `### Phase-29 state-5 code review` narratives, relocated at the phase-29 state-6 close-out when row `29` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 30 (the `### Phase-30 carry-forwards` [M29-1/M29-2, which fed phase 30 but were NOT consumed — they continue as phase-31 carry-forwards] block + the `### Phase-30 state-1 brainstorm` / `### Phase-30 state-2 PLAN-write` / `### Phase-30 state-3 implementation` / `### Phase-30 state-4 verification` / `### Phase-30 state-5 code review` narratives, relocated at the phase-30 state-6 close-out when row `30` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 31 (the `### Phase-31 carry-forwards` [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 — open Minors from the phase-30 REVIEW.md that fed phase 31 but were NOT consumed; they continue as carry-forwards for the next phase that touches the differential hash-sweep driver / the config parser] block + the `### Phase-31 state-1 brainstorm` / `### Phase-31 state-2 PLAN-write` / `### Phase-31 state-3 implementation` / `### Phase-31 state-4 verification` / `### Phase-31 state-5 code review` narratives, relocated at the phase-31 state-6 close-out when row `31` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 32 (the `### Phase-32 carry-forwards` block — the open Minors that fed phase 32 but were NOT consumed: the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3; they continue as carry-forwards for the future phase that touches their surface — re-listed in `## Next expected skill` above alongside the 6 new phase-32 REVIEW.md Minors M32-1…M32-6), relocated at the phase-32 state-6 close-out when row `32` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phases 33+34 (the `### Phase-33 carry-forwards` block — the active-phase carry-forwards Notes that lived through phases 33 and 34: the now-CONSUMED 6 phase-32 REVIEW.md Minors M32-1…M32-6 [folded + landed at the phase-33 state-3] + the "Other still-live carry-forwards" list [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3 + the HTTP-filters-family (1)-(4) — those still-live ones re-listed in `## Next expected skill` above alongside the new phase-34 Minors M34-1/M34-2/M34-3 + the phase-33 M33-1/M33-2]), relocated at the phase-34 state-6 close-out when row `34` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 35 (the `### Phase-35 state-1 brainstorm` / `### Phase-35 state-2 PLAN-write` / `### Phase-35 state-3 implementation` / `### Phase-35 state-4 verification` / `### Phase-35 state-5 code-review` narratives, relocated at the phase-35 state-6 close-out when ROADMAP row `35` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 36 (the `### Phase-36 state-5 code-review` narrative — phase 36 used a rename-in-place Notes discipline, so the state-1..4 sub-state narratives were superseded in place each session and only the four top-section blocks were relocated per-session — plus the now-CLOSED detailed M35-1 carry-forward bullet [CONSUMED by phase-36 F2], relocated at the phase-36 state-6 close-out when ROADMAP row `36` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 37 (the `### Phase-37 state-3 implementation` active-phase narrative, relocated at the phase-37 state-6 close-out when ROADMAP row `37` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### HTTP-filters-family carry-forwards (from the `25.2` REVIEW.md - NOT yet consumed; weigh whenever the HTTP-filters family is re-entered)

> These were never obligations on the xDS phase 26; they remain live for whenever an HTTP-filters-family phase resumes.

- **(1) [non-goal - architectural]** Over-limit request bodies are FULLY buffered before the 413 rejection (no streaming watermark). Documented deferred non-goal; differentially byte-identical to Envoy for the bounded fixture sizes. Revisit only if a streaming `decode_data` watermark path is ever planned.
- **(2) [doc precision]** The BEHAVIOR_CONTRACT 413-row "verified byte-exact against v1.33.0" phrasing - fixture `0033` is H1-only; the H2 over-limit path is covered by the in-process synth-decorator backstop, NOT differentially. Consider narrowing the phrasing if an H2 over-limit fixture is ever added.
- **(3) [coverage]** No standalone `== effective route limit` unit assertion (the boundary is exercised only via the over/under probes).
- **(4) [coverage]** No differential at-limit (`==`) probe in `0033` (within-limit `<` and over-limit `>` are both covered; the exact boundary is not differentially probed).
- _(2)-(4) are cheap polish, (1) is architectural and only relevant to a future streaming phase._
