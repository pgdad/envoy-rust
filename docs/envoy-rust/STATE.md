# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase

**id:** `24` — `24-http-filter-csrf` (the HTTP-filters family's SIXTH concrete phase — `envoy.filters.http.csrf`; the SECOND consumer of the phase-23 per-route `typed_per_filter_config` infrastructure), lifecycle **state-5-complete / state-6-next**. ROADMAP rows `00`–`23` are all `done`; row `24` remains **`in-progress`** (it flips to `done` at the state-6 deterministic close-out NEXT session, now that `REVIEW.md` is APPROVED).
**slug:** `24-http-filter-csrf`
**directory:** `docs/envoy-rust/phases/24-http-filter-csrf/` — now carries **all four artifacts: `SPEC.md` + `PLAN.md` + `PROGRESS.md` + `REVIEW.md`** (the state-5 code review landed `REVIEW.md` at THIS commit — verdict APPROVED, 0 Critical / 0 Important / 4 Minor). The five most-recently-closed phase directories — 23 `23-http-filter-cors`, 22 `22-http-filter-jwt-authn`, 21 `21-xds-file-based-eds`, 20, 19 — each remain CLOSED carrying all 4 artifacts.

**status:** **PHASE 24 (`24-http-filter-csrf`) STATE-5 CODE REVIEW COMPLETE — `REVIEW.md` APPROVED at THIS commit** (lifecycle state-5-complete / state-6-next). This commit is the **phase-24 state-5 code review** (`superpowers:requesting-code-review`) over the state-3 implementation arc (`9d5f7ebc9..e7441ab00` — the 6 task code commits `f97440f9`…`e7441ab00`): three read-only review subagents dispatched **SERIALLY** (`feedback_serial_subagent_dispatch`) over concern-clusters — **A** the envoy-config surface (`CsrfPolicy`/`RuntimeFractionalPercent` schema, `PerFilterConfig::Csrf` + `HttpFilterTypedConfig::Csrf` variants, `validate_csrf_config` + the two new `ConfigError`s, the SC1/SC2 exhaustive-consumer fixes, the L7 absent-filter route-walk ordering); **B** the centerpiece `CsrfFilter` runtime (L3 `host_and_port` origin compute + L6 chain-base/route-replace + L5 mutually-exclusive stats + L2 modify set + L4 byte-exact 403) + the `instance.rs` wiring, scrutinized adversarially; **C** the coverage surface (fixture `0032` + `Http1Method::Post` + differential wrapper, the `parse_bootstrap` fuzz seed, the in-process backstop, the BEHAVIOR_CONTRACT rows). **Verdict: APPROVED — 0 Critical / 0 Important / 4 Minor** (M24-1 no explicit `numerator:0` accept test; M24-2 `host_and_port` over-trims at `?`/`#` vs Envoy's `/`-only `hostAndPort` — latent/untested/browser-unreachable; M24-3 duplicate-`Origin` first-wins untested; M24-4 `README.md:54` matcher-stripping prose). All three clusters CLEAN/MINOR-ONLY; all six named focus items PASS (the L6 chain-base/route-replace model incl. per-request leak-safety; the L3 origin compute; the L5 stat mutual-exclusivity; the validator 0%-AND-100% acceptance via the explicit both-endpoints check; the L7 route-walk ordering; the fixture-0032 content-length workaround legitimacy). The **eighth consecutive clean state-5** (after 17/18/19/20/21/22/23). The controller independently re-verified the two load-bearing findings (the `csrf.rs:190` `?`/`#` over-trim; the `README.md:54` prose) against disk before accepting. Per `BOOTSTRAP_PROMPT.md` §5.2 the re-enter-state-3 trigger is a Critical or Important finding — there are none, so the phase lands APPROVED with the 4 Minors as M-track follow-ups (the phase-21/22/23 precedent), NOT a state-3 re-entry. **THIS commit is docs-only** (CREATE `REVIEW.md`; MODIFY `STATE.md` + `STATE_HISTORY.md`): no code/test/fixture change, no new ADR — **DECISIONS.md ledger head stays ADR-0061** (count 62; **ADR-0062** reserved-but-unfired). ADR-0014 in force; ADR-0028 open. It advances this STATE → state-5-complete / state-6-next and relocates the superseded state-4 top-section narratives verbatim to `STATE_HISTORY.md` (ADR-0035 / §4.1 inv. 9). The §7.5 gate remains GREEN at code-HEAD `9b0e7b925` (the AUTHORITATIVE Linux CI anchor `27457698815`); the review is docs-only so there is NO re-CI obligation. Per §5.1 (one state per session) this session EXITS after the STATE advance; the NEXT session runs the **phase-24 state-6 deterministic close-out** (flip ROADMAP row `24` `in-progress → done`; STATE → AWAITING NEXT PLANNING).

> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases + the active phase's prior sub-state pointers, incl. the phase-24 state-1 brainstorm pointer) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill

Per `BOOTSTRAP_PROMPT.md` §5 state 6 + `SKILL_ROUTING.md` (state 5 complete — `REVIEW.md` exists and is APPROVED): the next session runs the **phase-24 state-6 deterministic close-out**. `REVIEW.md` is APPROVED (0 Critical / 0 Important / 4 non-gating Minor — M24-1…M24-4; the §5.2 re-enter-state-3 trigger is NOT met). The close-out is a doc-only commit (no code change): flip **ROADMAP row `24` `in-progress → done`** (parent-row schema invariant 4.1.2 — rows `00`–`24` then ALL `done`), advance **STATE → phase-24 CLOSED / AWAITING NEXT PLANNING** (rewrite `## Active phase` + `## Next expected skill` to point at the next-NEW-PHASE brainstorm — `superpowers:brainstorming` — which is the SESSION AFTER the close-out, per §5.1), and relocate the phase-24 active-phase top-section narratives (state-1…state-5) to `STATE_HISTORY.md` (ADR-0035, now that the phase closes). The §7.5 gate is GREEN (state-4 evidence in `PROGRESS.md` Task 7; the AUTHORITATIVE Linux CI anchor is run `27457698815` at code-HEAD `9b0e7b925`). The 4 Minor findings (M24-1…M24-4) are M-track follow-ups recorded in `REVIEW.md` — NOT close-out blockers. Per §5.1 (one state per session) the state-6 session advances ONE state.

> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases + the active phase's prior sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit

**Phase-24 state-5 code-review-complete + STATE-advance commit (THIS commit):** advances STATE `24` state-4-complete/state-5-next → state-5-complete/state-6-next and relocates the superseded state-4 top-section narratives verbatim to `STATE_HISTORY.md` (ADR-0035 / §4.1 inv. 9). Wrote `docs/envoy-rust/phases/24-http-filter-csrf/REVIEW.md` (`superpowers:requesting-code-review`): three read-only review subagents dispatched SERIALLY over concern-clusters (A envoy-config schema/variants/validator + exhaustive-consumer fixes; B the `CsrfFilter` runtime centerpiece — L3 origin compute + L6 chain-base/route-replace + L5 stats + L4 403 — + `instance.rs` wiring, adversarially; C fixture `0032`/fuzz-seed/backstop/BEHAVIOR_CONTRACT coverage). **Verdict APPROVED — 0 Critical / 0 Important / 4 Minor** (M24-1…M24-4), all three clusters CLEAN/MINOR-ONLY, all six named focus items PASS, the eighth consecutive clean state-5. No code change (REVIEW.md + STATE.md + STATE_HISTORY.md only); no new ADR — **DECISIONS.md ledger head stays ADR-0061** (count 62; **ADR-0062** reserved-but-unfired). ADR-0014 in force; ADR-0028 open. The §7.5 gate remains GREEN at code-HEAD `9b0e7b925` (the AUTHORITATIVE Linux CI anchor `27457698815`); the review is docs-only so there is NO re-CI obligation. Per §5.1 the NEXT session is the **phase-24 state-6 deterministic close-out** (flip ROADMAP row `24` `in-progress → done`; STATE → AWAITING NEXT PLANNING; see `## Next expected skill`).

> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases + the active phase's prior sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated

2026-06-13 (phase-24 **state-5 code-review COMPLETE — `REVIEW.md` APPROVED** — `superpowers:requesting-code-review`. Reviewed the phase-24 implementation arc (`9d5f7ebc9..e7441ab00`) via three read-only review subagents dispatched SERIALLY over concern-clusters [A envoy-config `CsrfPolicy`/`RuntimeFractionalPercent` schema + `PerFilterConfig::Csrf`/`HttpFilterTypedConfig::Csrf` variants + `validate_csrf_config` + SC1/SC2 exhaustive-consumer fixes + L7 route-walk ordering; B the `CsrfFilter` runtime centerpiece — L3 `host_and_port` origin compute + L6 chain-base/route-replace + L5 mutually-exclusive stats + L2 modify set + L4 byte-exact 403 — + `instance.rs` wiring, adversarial; C fixture `0032`+`Http1Method::Post`+wrapper, the `parse_bootstrap` fuzz seed, the in-process backstop, the BEHAVIOR_CONTRACT rows]. **Verdict APPROVED — 0 Critical / 0 Important / 4 Minor** [M24-1 no explicit `numerator:0` accept test; M24-2 `host_and_port` over-trims at `?`/`#` vs Envoy's `/`-only `hostAndPort` — latent/untested/browser-unreachable, the most valuable M-track follow-up; M24-3 duplicate-`Origin` first-wins untested; M24-4 `README.md:54` matcher-stripping prose]; all three concern-clusters CLEAN/MINOR-ONLY; all six named focus items verified PASS [the L6 chain-base/route-replace + per-request leak-safety; the L3 origin compute; the L5 stat mutual-exclusivity; the validator 0%-AND-100% acceptance; the L7 route-walk ordering; the fixture-0032 content-length workaround legitimacy]; the eighth consecutive clean state-5 [after 17/18/19/20/21/22/23]. Per §5.2 no Critical/Important → no state-3 re-entry; the 4 Minors are M-track follow-ups. No code change (doc-only: CREATE `REVIEW.md`; MODIFY `STATE.md` + `STATE_HISTORY.md`); no new ADR; ledger head **ADR-0061** [count 62; **ADR-0062** reserved-but-unfired]. ADR-0014 in force; ADR-0028 open. The §7.5 gate remains GREEN at code-HEAD `9b0e7b925` (CI anchor `27457698815`); the review is docs-only so there is NO re-CI obligation. Per §5.1 the state-6 deterministic close-out [ROADMAP row `24` → `done`; STATE → AWAITING NEXT PLANNING] is the NEXT session.)

> Historical `## Last updated` notes — every superseded last-updated note (all closed phases + the active phase's prior sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: one state per session; do not chain states. State-6 close-out commits touch ROADMAP.md + STATE.md only and carry no code changes; the next session writes PLAN.md for the next active phase per `superpowers:writing-plans`.
- The reviewer's R2 disposition decision (option (a) retroactive split of 05.1 vs option (b) free-standing post-05.1 sub-phase) was settled at the 05.1 state-6 commit in favor of option (b); 05.4 is the chosen sibling sub-phase. Future-reviewers reading STATE.md should understand that 05.1 is structurally closed at the preamble landing; 05.4 is a SIBLING under parent-05, not a child of 05.1; and the execution order ran 05.1 → 05.4 → 05.2 → 05.3, with 05.3 the closing sub-phase that flips parent-05 to `done`.

### Phase-24 state-1 brainstorm

- **Phase pick + scope (ADR-0060).** Phase `24` = **`envoy.filters.http.csrf`**, the HTTP-filter-family's SIXTH concrete phase (after 09 local_ratelimit / 10 rbac / 11 fault / 22 jwt_authn / 23 cors) and the **SECOND consumer of the phase-23 per-route `typed_per_filter_config` infrastructure**. The pick is driven by the §6.3 anti-pattern: phase 23 landed the per-route-config infra but registered only the `Cors` variant; a second tested consumer is needed to prove it generalizes additively. `csrf` is the standout: deterministic, header-only, locally-verifiable, reuses existing infra, and — decisively — the cross-cutting HCM route-early-resolution risk that made phase 23 borderline is GONE (phase 23 landed `resolve_route` + the `apply_route_config` threading), so phase 24 is a near-pure additive plug-in (an enum arm + a filter module), the lightest filter phase since the framework completed.
- **Alternatives rejected at the brainstorm:** `buffer` (request-body-buffering machinery — higher risk; deferred to its own phase); per-route overrides of existing `fault`/`rbac`/`local_ratelimit` (lower new-surface value; the interesting route>vhost `mostSpecificPerFilterConfig` cascade was deferred at phase 23); `compression` (gzip foundations-ADR + non-byte-exact body comparison); `ext_authz`/`ext_proc` (external auth service / gRPC still H2-trailers-blocked); load-balancing family (non-deterministic distribution / byte-exact hash-ring reproduction / distinguishable-backend harness). The phase-23 M23-6/M23-7 connection-handling hardening was offered as a fold-in foundation slice and DECLINED — kept as M-track follow-ups to keep phase 24 single-purpose.
- **Minimum-viable scope (SPEC §3):** the second `PerFilterConfig::Csrf(CsrfPolicy)` variant + a near-empty filter-chain `Csrf` entry + a `CsrfPolicy` schema (`additional_origins: Vec<StringMatcher>` reusing 04.x + `filter_enabled` deterministic-0%/100% only per the phase-11 fault precedent); `CsrfFilter` (eighth `HttpFilterInstance` variant; first decode-side-only per-route filter — modify-method origin guard, `Origin`/`Referer` vs `Host` + `additional_origins`, 403 `StopAndSend` on mismatch/missing-source via the existing filter-synth helpers; per-route policy via the existing `apply_route_config` hook — NO HCM change); `http.<stat_prefix>.csrf.{request_valid,request_invalid,missing_source_origin}` stats; fixture `0032-http-filter-csrf` (H1, real upstream cluster per ADR-0058 L6, 5 deterministic probes); in-process backstop; a `parse_bootstrap` fuzz seed (no new target). The existing `PerRouteConfigForAbsentFilter` validator is reused verbatim. _(Superseded in part by the state-2 §6.2 findings — see ADR-0061 / the `### Phase-24 state-2 PLAN-write` subsection: the chain entry is NOT near-empty [required `filter_enabled`]; the origin comparison is scheme-stripped `host[:port]`.)_
- **Deferred (SPEC §4):** `filter_enabled` runtime fractional gating + `shadow_enabled` shadow mode; vhost-level config + the route>vhost cascade; `typed_per_filter_config` for filters OTHER than cors/csrf; streaming/trailers + gRPC-Web CSRF.
- **§6.1 split posture:** projected single-phase (~900–1300 LoC / ~8–11 tasks; lighter than phase 23). Split valve reserved as 24.1 (schema + wiring + validator) / 24.2 (CsrfFilter + stats + fixture + close) under ADR-0062 — very unlikely to fire.
- **§6.2 (deferred to the state-2 PLAN-write, runs LOCALLY):** resolve the config shape FIRST (gates D1 — the chain-vs-route `filter_enabled` requirement); then the modify-method set / source-target origin computation + `Referer` fallback / 403 failure-body bytes + `content-type` + missing-source disposition / stat namespace + semantics / `filter_enabled` disposition / the per-route-for-absent-filter reuse. Inline ADR-0061 lands if any item diverges (config shape + modify-method set are the most likely triggers).
- **ADR posture:** ledger head **ADR-0060** (this brainstorm; count 61). **ADR-0061** reserved for the §6.2 reconciliation; **ADR-0062** reserved for the split. ADR-0014 in force; ADR-0028 open.

### Phase-24 state-2 PLAN-write

- **§6.2 empirical verification (LOCAL Docker, `envoyproxy/envoy:v1.33.0`, digest `sha256:56da5afd…`, 2026-06-11) → ADR-0061.** Full probe transcript in `phases/24-http-filter-csrf/PROGRESS.md` Task-1 preamble. Of the 7 §6.2 items, THREE diverged materially (reconciled by ADR-0061): **L1 (config shape)** — Envoy uses ONE `…csrf.v3.CsrfPolicy` message at BOTH the filter-chain `http_filters[].typed_config` AND the per-route `typed_per_filter_config`, with `filter_enabled` REQUIRED at both (`--mode validate` → `CsrfPolicyValidationError.FilterEnabled: value is required`) — so `HttpFilterTypedConfig::Csrf(CsrfPolicy)` + `PerFilterConfig::Csrf(CsrfPolicy)` (same type, NOT a near-empty `CsrfConfig {}`) + a NEW `RuntimeFractionalPercent { default_value: FractionalPercent, runtime_key: Option<String> }`; **L3 (origin computation)** — source + target origins are reduced to scheme-stripped `host[:port]` (`Url::hostAndPort()`), NOT `scheme://host:port` (verified: `Origin: http://additional.example.com` matches `exact: "additional.example.com"` but NOT `exact: "http://additional.example.com:80"`); source = `Origin`, fallback `Referer` (Origin precedence confirmed); **L6 (chain-base/route-replace)** — the chain-level `CsrfPolicy` is an always-applied base (a route without an override is STILL guarded by it), a per-route `CsrfPolicy` REPLACES it wholesale; `filter_enabled` honored at deterministic 0%/100% (envoy-rust rejects non-deterministic `default_value` + a present `runtime_key` startup-fatal per ADR-0049). CONFIRMED: **L2** modify set `{POST,PUT,DELETE,PATCH}` (safe `{GET,HEAD,OPTIONS,TRACE}` pass, no stat); **L4** 403 `Invalid origin` (14 bytes, no newline, `content-type: text/plain`, `content-length: 14`, `server: envoy` — the H1 `decorate_filter_synth_response` auto-adds `content-type` for non-empty bodies, the rbac precedent); **L5** `http.<prefix>.csrf.{request_valid,request_invalid,missing_source_origin}` HCM-prefixed + MUTUALLY EXCLUSIVE (missing-source does NOT also tick request_invalid); **L7** the generic `PerRouteConfigForAbsentFilter` validator covers csrf verbatim (ADR-0058 L7 stricter-reject); **L8** fixture 0032 needs a real upstream (ADR-0058 L6).
- **PLAN-time SPEC corrections (SC1–SC8, read-only recon at HEAD `bb58319ea`).** Load-bearing: SC1 — `PerFilterConfig` is DERIVE-based (one-variant change) but adding `Csrf` breaks `cors.rs:118` + the `bootstrap.rs:12908` test (fixed in Task 1, gate `cargo build --workspace`); SC2 — `HttpFilterTypedConfig::Csrf` breaks the `instance.rs` build match + the `bootstrap.rs:2803` validator match (the chain variant lands in Task 3 with `CsrfFilter`); SC3 — `FractionalPercent` (+`selects_deterministic()`) exists, `RuntimeFractionalPercent` is NEW; SC5 — `header_ci` duplication reaches N=3 (duplicate, do not extract); SC7 — CSRF is decode-only but the `instance.rs` encode match is exhaustive → `CsrfFilter::encode_headers` returns the trivial `Continue` (rbac no-op precedent).
- **§6.1 split gate → SINGLE PHASE.** Refined estimate ~1000–1350 LoC / 7 tasks (no shared-HCM change — reused from phase 23); comfortably under both thresholds. **ADR-0062 (split) DOES NOT FIRE.** Task map: T1 D1 schema + `PerFilterConfig::Csrf`; T2 D2+D4 `CsrfFilter` (chain-base/route-replace, scheme-stripped origin, 403) + 3 stats + BEHAVIOR_CONTRACT; T3 D3 `HttpFilterTypedConfig::Csrf` + `HttpFilterInstance::Csrf` wiring + `validate_csrf_config`; T4 D6.1 fixture 0032 + `Http1Method::Post`; T5 D6.2 fuzz seed; T6 D6.3 backstop; T7 state-4 verification + STATE advance.
- **ADR posture:** ledger head **ADR-0061** (this PLAN-write; count 62). **ADR-0062** reserved-but-unfired for the §6.1 split. ADR-0014 in force; ADR-0028 open.

> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
