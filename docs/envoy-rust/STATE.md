# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase

**id:** `29` - `29-lb-maglev` (Load-balancing family: the **`MAGLEV` consistent-hashing load balancer** — the SECOND [and last] deterministic, byte-exact-differentiable LB policy after phase-28 `RING_HASH`; a cluster `lb_policy: MAGLEV` builds Google's Maglev prime-sized permutation table over its endpoints; a request carrying the SAME phase-28 route header `hash_policy` routes to the table slot the hashed value maps to [same key → same backend]; **the REQUEST side is IDENTICAL to RING_HASH** — only the table-build differs). Lifecycle **state-2 PLAN-write COMPLETE / state-3-next** (`SPEC.md` + `PLAN.md` + `PROGRESS.md` present; `REVIEW.md` absent -> the next step is the state-3 `superpowers:subagent-driven-development`; the §6.2 Maglev table algorithm is empirically CRACKED + VALIDATED [replica 80/80 at default `table_size`, 64/64 at M=17] against live Envoy and LOCKED by **ADR-0072** — STRONG differential target). The SECOND concrete Load-balancing-family phase. ROADMAP rows `00`-`28` are ALL `done`; row `29` is now `in-progress` (flips `done` at the state-6 close-out).
**slug:** `29-lb-maglev`
**directory:** `docs/envoy-rust/phases/29-lb-maglev/` - carries **`SPEC.md` + `PLAN.md` + `PROGRESS.md`** (the state-1 brainstorm + state-2 PLAN-write outputs; scope locked by ADR-0071, the §6.2-verified Maglev table algorithm locked by ADR-0072). `PLAN.md` = 8 tasks (Task 1 seeded xxHash64; 2 `LbPolicy::Maglev` + `MaglevLbConfig`; 3 table_size validation; 4 `maglev.rs` table build+lookup [the §A oracle]; 5 the M28-3 `hash_lb: Option<HashLb>` refactor; 6 fixture 0037 differential; 7 backstop; 8 fuzz seed + BEHAVIOR_CONTRACT); `PROGRESS.md` carries the §6.2-locked algorithm + the oracle key→host mapping. Reuse sources (phase 28): xxHash64 seed-0 (`crates/envoy-cluster/src/xxhash.rs` — Task 1 generalizes it to a seeded `xxh64_seed`), `ring_hash.rs` HashRing as the structural sibling for the new `maglev.rs`, the `pick()`/`pick_endpoint()` `Option<u64>` threading + the `ring: Option<HashRing>` discriminator (`crates/envoy-cluster/src/cluster.rs:209` — the M28-3 refactor target) + the Maglev-footgun guard comment (`:1375`), the `LbPolicy`/`RingHashLbConfig`/`HashFunction` + route `hash_policy` config (`crates/envoy-config/src/bootstrap.rs`), and fixture `0036-lb-ring-hash` + the key-sweep differential driver + `discover_host_lan_ip`/`{{BACKEND_IP}}` (`tests/differential/src/lib.rs`) as the fixture-0037 template.

**status:** **PHASE 29 (`29-lb-maglev`) OPEN at state-2 PLAN-write COMPLETE / state-3-next.** Ran `superpowers:writing-plans` — first the SPEC §6.2 empirical reconnaissance (LOCALLY, against `envoyproxy/envoy:v1.33.0` — maglev selection is observable with no reload trigger; delegated to a read-only subagent to preserve context budget), then authored `PLAN.md` + `PROGRESS.md`. **The §6.2 recon CRACKED + EXACTLY VALIDATED the Maglev table algorithm (a from-scratch replica reproduced the live-Envoy oracle 80/80 at the default `table_size`, 64/64 at M=17) → ADR-0072 FIRED, STRONG differential target confirmed.** The locked algorithm (`maglev.rs` MUST reproduce it byte-for-byte): host key = the `ip:port` string (NO `_i` suffix); `offset = xxHash64(key, seed 0) % M`; `skip = xxHash64(key, seed 1) % (M-1) + 1` (**seed 1** is the cracked unknown); `permutation[j] = (offset + j·skip) % M`; populate by the round-robin claim loop in config order (earlier host wins contention); lookup = `table[xxHash64(header_value, seed 0) % M]` (the SAME request hash as RING_HASH); `table_size` default 65537 / prime / max 5000011. Config dispositions: non-prime + over-max `table_size` → startup-fatal; `maglev_lb_config` on a non-MAGLEV cluster → accept-and-ignore (Envoy parity + the phase-28 ring precedent — this CORRECTS the SPEC §2.1 fatal projection). No portable LB stat. **PLAN = 8 tasks** (1 seeded xxHash64 / 2 `LbPolicy::Maglev` + `MaglevLbConfig` / 3 table_size validation [prime/over-max, MAGLEV-gated] / 4 `maglev.rs` build+lookup [the §A oracle is the gate] / 5 the M28-3 `ring: Option<HashRing>` → `hash_lb: Option<HashLb>` refactor [RING_HASH + round-robin paths byte-identical] / 6 fixture 0037 differential [STRONG, `{{BACKEND_IP}}` shared-IP] / 7 backstop / 8 `parse_bootstrap` fuzz seed + BEHAVIOR_CONTRACT MAGLEV row [M28-1 folded]). All 36 existing fixtures stay green (regression-equivalence). **§6.1 single-phase confirmed (~450–550 LoC / 8 tasks; ADR-0073 UNFIRED).** **DECISIONS.md ledger head: ADR-0072** (count 73; **ADR-0073 reserved + UNFIRED** for the §6.1 split). ADR-0014 in force; ADR-0028 open. No `unsafe`. Per §5.1 the NEXT session runs the phase-29 state-3 implementation (`superpowers:subagent-driven-development`, Task 1 first).

> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases + the active phase's prior sub-state pointers, incl. the phase-25 state-1 brainstorm pointer) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill

Per `BOOTSTRAP_PROMPT.md` §5 state 3 + `SKILL_ROUTING.md`: phase `29` has `SPEC.md` + `PLAN.md` + `PROGRESS.md` but no `REVIEW.md`, and the implementation is incomplete -> the next session runs **`superpowers:subagent-driven-development`** (state 3), executing `PLAN.md`'s 8 tasks TDD-per-task (`superpowers:test-driven-development`) with a two-stage review per task (`superpowers:requesting-code-review`), appending to `PROGRESS.md` on each completion. Start at **Task 1** (seeded xxHash64 `xxh64_seed(data, seed)` — seed 0 byte-identical to the existing `xxh64`, seed 1 for the maglev `skip`), then Task 2→8 in order. The §6.2 algorithm is already LOCKED in `PLAN.md §A` (no further Docker recon needed); Task 4's pinned oracle (the §A key→host table) is the correctness gate; Task 5 (the M28-3 refactor) is load-bearing — keep BOTH the round-robin AND the RING_HASH fixture-0036 paths byte-identical. The state-4 §7.5 gate (the FIRST real CI execution — fmt + clippy + the Docker differential incl. fixture 0037) is the state AFTER state-3. Per §5.1 the state-3 implementation is the NEXT session's single state.

> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases + the active phase's prior sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit

**Phase-29 state-2 PLAN-write — `PLAN.md` + `PROGRESS.md` authored, ADR-0072 FIRED (THIS commit):** the `superpowers:writing-plans` commit that runs the SPEC §6.2 LOCAL empirical reconnaissance (delegated to a read-only subagent — stood up `envoyproxy/envoy:v1.33.0` with one `MAGLEV` cluster + two distinguishable backends + a header `hash_policy`, swept ≥64 keys, CRACKED + EXACTLY VALIDATED the Maglev table 80/80 at default `table_size` + 64/64 at M=17) and authors `PLAN.md` (8 tasks, the §6.2-locked algorithm inline in §A + the regression oracle) + `PROGRESS.md`. Fired **ADR-0072** (the §6.2 reconciliation — the STRONG differential target; host key `ip:port`; `offset`=seed-0 / `skip`=seed-1 derivation; config-order populate; the config dispositions incl. the accept-and-ignore correction of the SPEC's fatal projection; ledger head -> **ADR-0072**, count 73), flipped ROADMAP row `29` `planned` -> `in-progress`, relocated the superseded phase-29 state-1 top-section blocks verbatim to `STATE_HISTORY.md` (ADR-0035 / §4.1 inv. 9), and recorded the `### Phase-29 state-2 PLAN-write` Notes subsection. **§6.1 split did NOT fire — ADR-0073 reserved + UNFIRED** (8 tasks / ~450–550 LoC, well under the gate). NO production/test change (docs-only — PLAN + PROGRESS + DECISIONS + ROADMAP + STATE + STATE_HISTORY). ADR-0014 in force; ADR-0028 open. No `unsafe`. The commit title carries `[ADR-0072]`. Per §5.1 the NEXT session runs the phase-29 state-3 implementation (`superpowers:subagent-driven-development`, Task 1 first).

> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases + the active phase's prior sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated

2026-06-19 (phase-29 **state-2 PLAN-write — `PLAN.md` authored, ADR-0072 FIRED** - `superpowers:writing-plans`. Ran the SPEC §6.2 LOCAL reconnaissance [delegated to a read-only subagent; `envoyproxy/envoy:v1.33.0`, one `MAGLEV` cluster + two distinguishable backends + a header `hash_policy`] which CRACKED + EXACTLY VALIDATED the Maglev table [replica 80/80 at default `table_size`, 64/64 at M=17] → **ADR-0072 FIRED** [§6.2 reconciliation; STRONG differential target; host key `ip:port`; `offset`=seed-0 / `skip`=seed-1; config-order populate; ledger head -> ADR-0072, count 73]. Authored `PLAN.md` [8 tasks, the §A locked algorithm + oracle inline] + `PROGRESS.md`, flipped ROADMAP row `29` `planned` -> `in-progress`, relocated the superseded phase-29 state-1 top-section blocks verbatim to `STATE_HISTORY.md` [ADR-0035 / §4.1 inv. 9], recorded the `### Phase-29 state-2 PLAN-write` Notes subsection. PLAN = seeded xxHash64 / `LbPolicy::Maglev`+`MaglevLbConfig` / table_size validation / `maglev.rs` build+lookup / the M28-3 `hash_lb` refactor / fixture 0037 / backstop / fuzz seed + BEHAVIOR_CONTRACT; all 36 existing fixtures stay green. **§6.1 split did NOT fire — ADR-0073 UNFIRED** [~450–550 LoC / 8 tasks]. NO production/test change [docs-only]. ADR-0014 in force; ADR-0028 open. No `unsafe`. Per §5.1 the phase-29 state-3 implementation is the NEXT session.)

> Historical `## Last updated` notes — every superseded last-updated note (all closed phases + the active phase's prior sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: one state per session; do not chain states. State-6 close-out commits touch ROADMAP.md + STATE.md only and carry no code changes; the next session writes PLAN.md for the next active phase per `superpowers:writing-plans`.
- The reviewer's R2 disposition decision (option (a) retroactive split of 05.1 vs option (b) free-standing post-05.1 sub-phase) was settled at the 05.1 state-6 commit in favor of option (b); 05.4 is the chosen sibling sub-phase. Future-reviewers reading STATE.md should understand that 05.1 is structurally closed at the preamble landing; 05.4 is a SIBLING under parent-05, not a child of 05.1; and the execution order ran 05.1 → 05.4 → 05.2 → 05.3, with 05.3 the closing sub-phase that flips parent-05 to `done`.

> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) and for parent phase 25 (the `### Phase-25 state-1 brainstorm` pick + recon-finding narrative, relocated at the 25.2 state-6 close-out when parent `25` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 26 (the `### Phase-26 state-1 brainstorm` pivot/rejected-alternatives/key-scoping narrative, relocated at the phase-26 state-6 close-out when row `26` flipped to `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 27 (the `### Phase-27 state-1 brainstorm` / `### Phase-27 state-2 PLAN-write` / `### Phase-27 state-4 verification` narratives + the now-consumed `### Phase-27 carry-forwards` [M26-1..M26-8] block, relocated at the phase-27 state-6 close-out when row `27` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 28 (the `### Phase-28 state-1 brainstorm` / `### Phase-28 state-2 PLAN-write` / `### Phase-28 state-3 implementation + state-4 verification` / `### Phase-28 state-5 code review` narratives + the now-consumed `### Phase-28 carry-forwards` [M27-1..M27-3] block, relocated at the phase-28 state-6 close-out when row `28` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### HTTP-filters-family carry-forwards (from the `25.2` REVIEW.md - NOT yet consumed; weigh whenever the HTTP-filters family is re-entered)

> These were never obligations on the xDS phase 26; they remain live for whenever an HTTP-filters-family phase resumes.

- **(1) [non-goal - architectural]** Over-limit request bodies are FULLY buffered before the 413 rejection (no streaming watermark). Documented deferred non-goal; differentially byte-identical to Envoy for the bounded fixture sizes. Revisit only if a streaming `decode_data` watermark path is ever planned.
- **(2) [doc precision]** The BEHAVIOR_CONTRACT 413-row "verified byte-exact against v1.33.0" phrasing - fixture `0033` is H1-only; the H2 over-limit path is covered by the in-process synth-decorator backstop, NOT differentially. Consider narrowing the phrasing if an H2 over-limit fixture is ever added.
- **(3) [coverage]** No standalone `== effective route limit` unit assertion (the boundary is exercised only via the over/under probes).
- **(4) [coverage]** No differential at-limit (`==`) probe in `0033` (within-limit `<` and over-limit `>` are both covered; the exact boundary is not differentially probed).
- _(2)-(4) are cheap polish, (1) is architectural and only relevant to a future streaming phase._

### Phase-29 carry-forwards (from the phase-28 `REVIEW.md` M28-1..M28-3 - weigh at the phase-29 state-1 brainstorm / planning)

> All 3 are NON-BLOCKING Minors (REVIEW.md APPROVED 0C/0I); none an Envoy-equivalence divergence (the differential gate is green). All three attach to the RING_HASH LB code, so they are naturally weighed when the next LB-family phase (maglev / least_request / subset / locality / priority) or a RING_HASH+EDS-reload phase re-enters that code.

- **M28-1** `maximum_ring_size` is parse-validated + stored but never consulted in the ring build (`crates/envoy-cluster/src/ring_hash.rs`) — only `minimum_ring_size` governs the replica count (`min_ring_size / num_hosts`). Upstream Envoy's ketama build can scale replicas UP toward `maximum_ring_size` for small host counts; envoy-rust's build is `minimum_ring_size`-governed. This does NOT affect the validated 2-host/1024 oracle (512 replicas/host, far under 8M) — which is why 36/36 holds — but a future fixture with a host count where `min/num_hosts` collides with Envoy's max-scaling could silently diverge. **Fix:** no code change for phase-28's validated scope — add one sentence to the `ring_hash.rs` module doc + BEHAVIOR_CONTRACT making explicit that `maximum_ring_size` is parse-validation-only and the build is `minimum_ring_size`-governed. Land opportunistically when the ring code is next touched. _(Reviewer recommendation: a future fixture with an odd host count [e.g. 3 hosts, non-divisible `minimum_ring_size`] would empirically bound this divergence against live Envoy.)_
- **M28-2** the hash-key-absent fallback is the round-robin CURSOR path (`crates/envoy-cluster/src/cluster.rs` `pick` with `None` key), where Envoy's documented fallback is a RANDOM host — a real behavioral divergence. It is acceptable + correctly handled in phase 28: BEHAVIOR_CONTRACT records it as non-deterministic-in-Envoy / NOT differentially asserted, and no fixture exercises it. The backstop's `None → host0 then host1` rotation is a characterization of envoy-rust's internal behavior, NOT an Envoy-matched fidelity contract. Note for whenever a hash-policy-absent path is ever differentially scoped.
- **M28-3** the defense-in-depth `host_index < total` guard in the ring dispatch (`crates/envoy-cluster/src/cluster.rs` ring arm) is dead under the current invariant — the ring is built once from the bootstrap set and never rebuilt on reload (RING_HASH+EDS is out of scope), so `host_index` always indexes `eps` validly. The guard is correct, harmless, and commented; it silently falls through to the cursor path rather than erroring if it ever tripped. Honor it (convert the `ring.is_some()` dispatch to an explicit `lb_policy` discriminant + rebuild the ring on endpoint-set swaps) when MAGLEV or RING_HASH+EDS-reload lands — both already flagged in-code by the Maglev-footgun guard comment.

### Phase-29 state-1 brainstorm (pick + scope; the Maglev / maximal-reuse rationale)

> Authored at the phase-29 state-1 brainstorm (`superpowers:brainstorming`). Records WHY phase 29 is `MAGLEV` + the alternatives weighed; the full reasoning is **ADR-0071**. (Relocates to `STATE_HISTORY.md` per ADR-0035 / §4.1 inv. 9 at the phase-29 state-6 close-out, when row `29` flips `done`.)

- **The pick continues the Load-balancing family with `MAGLEV`** — the SECOND (and last) deterministic, byte-exact-differentiable LB policy after phase-28 `RING_HASH`, explicitly named in the phase-28 SPEC §2.2 as "the LB family's next LB-opener candidate." Among the LB family only the consistent-hashing policies are deterministic (a request hash key → a fixed host); `random`/`least_request` (P2C-random) are non-deterministic → not byte-exact-differentiable without a contract-relaxation ADR (deferred).
- **Maglev is the maximal-reuse next step.** Its REQUEST side is IDENTICAL to RING_HASH (the same route `hash_policy` header → xxHash64 → `Option<u64>` key threaded through `pick()`), so the entire phase-28 request-hash plumbing + the `LbPolicy`/`MaglevLbConfig` config pattern + xxHash64-from-scratch + the two-distinguishable-backend fixture harness (fixture 0036 + `discover_host_lan_ip`/`{{BACKEND_IP}}`) are reused; only the TABLE-BUILD differs (Google's prime-sized permutation table replaces the ketama ring). And like RING_HASH the differential is a NORMAL request/response → LOCALLY observable (no reload trigger), no Linux-CI-only tax.
- **It is the natural M28-3 consumer.** Phase 28 used `ring.is_some()` as the "is RING_HASH" discriminator with a Maglev-footgun guard comment warning a SECOND consistent-hash policy must make the discriminator explicit. MAGLEV is that policy → this phase replaces `ring.is_some()` with an explicit hash-LB dispatch (a load-bearing in-scope refactor; both the round-robin AND the RING_HASH fixture-0036 paths stay behavior-identical).
- **Rejected (ADR-0071):** `least_request`/`random` (non-deterministic); CDS/LDS hot-reload (the deferred xDS layers — heavy swappable-ClusterManager + per-cluster lifecycle churn, split-prone, and CI-only differential; deferred again, not abandoned — the phase-26/27 watcher+swap primitive waits); subset/locality/priority LB (later, layered on the policy-dispatch); the gRPC/ADS transport (still ADR-0014/H2-trailers-blocked).
- **MVP scope (ADR-0071 / SPEC §2):** `LbPolicy::Maglev` + `MaglevLbConfig` (`table_size` default 65537, prime, max 5000011) + validators + a new `maglev.rs` (the permutation-table build + lookup, reusing xxHash64) + the M28-3 discriminator refactor + the route `hash_policy`/request-hash plumbing reused as-is + fixture 0037 (clone of 0036's two-backend shape) + backstop + a `parse_bootstrap` fuzz seed + a BEHAVIOR_CONTRACT "LB selection" extension (M28-1 folded). **Deferred non-goals:** least_request/random; MURMUR_HASH_2; non-header hash sources + terminal/multi-policy/regex_rewrite; weighted Maglev; MAGLEV+EDS-hot-reload re-table; MAGLEV+HC/OD differential; subset/locality/priority LB; CDS/LDS hot-reload.
- **The core differential risk** (envoy-rust must replicate Envoy's EXACT Maglev table + the already-matched header-hash to select the SAME backend) is de-risked by the **state-2 §6.2 LOCAL reconnaissance** (sweep keys against `envoyproxy/envoy:v1.33.0`, reverse-engineer the table-build hash inputs / `offset`-`skip` derivation / prime-`table_size` handling / populate order), with a documented same-key-stability fallback if exact cross-proxy identity proves intractable. **ADR-0072 reserved** (§6.2 reconciliation), **ADR-0073 reserved** (§6.1 split, projected NOT to fire — ~700–1000 LoC / ~8 tasks, smaller than phase 28 since the plumbing already exists). SPEC spec-reviewed → APPROVED (0C/0I/3 doc Minor; the M28-3-naming Minor folded).

### Phase-29 state-2 PLAN-write (the §6.2-locked Maglev table + the 8-task plan)

> Authored at the phase-29 state-2 PLAN-write (`superpowers:writing-plans`). Records the §6.2 reconnaissance result + the PLAN shape; the full algorithm lock is **ADR-0072** and `PLAN.md §A`. (Relocates to `STATE_HISTORY.md` per ADR-0035 / §4.1 inv. 9 at the phase-29 state-6 close-out, when row `29` flips `done`.)

- **The §6.2 reconnaissance CRACKED Envoy's exact Maglev table** (run LOCALLY against `envoyproxy/envoy:v1.33.0`; delegated to a read-only subagent to preserve context budget). A from-scratch replica reproduced the live-Envoy key→backend oracle **80/80** at the default `table_size` (65537) and **64/64** at M=17 → the **STRONG** differential target (cross-proxy byte-identical selection, as phase 28) FIRES; the same-key-stability fallback is NOT needed.
- **The locked algorithm (`maglev.rs` reproduces it byte-for-byte; ADR-0072):** host key = the host `ip:port` string (NO `_i` replica suffix — unlike the ring); `offset = xxHash64(key, seed 0) % M`; `skip = xxHash64(key, seed 1) % (M-1) + 1` (**seed 1** was the central cracked unknown — the high/low-32-bit and same-seed hypotheses scored ~chance); `permutation[j] = (offset + j·skip) % M`; populate `table[0..M]` by the round-robin claim loop in config/endpoint-list order (earlier host wins contention); lookup = `table[xxHash64(header_value, seed 0) % M]` (the SAME request hash as RING_HASH). `table_size` default 65537, prime, max 5000011.
- **Config dispositions (§6.2-verified, ADR-0072):** non-prime `table_size` → startup-fatal (`MaglevTableSizeNotPrime`); over-max → startup-fatal (`MaglevTableSizeTooLarge`); `maglev_lb_config` on a non-MAGLEV cluster → **accept-and-ignore** (validation GATED to MAGLEV clusters), matching upstream Envoy AND the phase-28 `ring_hash_lb_config`-on-non-RING_HASH precedent — this **CORRECTS the SPEC §2.1 fatal projection** toward parity. No portable LB stat (only generic `cluster.<name>.lb_*`). Header-absent fallback = the cursor path (M28-2); empty-but-present value is hashed.
- **PLAN = 8 tasks** (1 seeded xxHash64 `xxh64_seed` / 2 `LbPolicy::Maglev` + `MaglevLbConfig { table_size }` / 3 table_size validation [prime + over-max, MAGLEV-gated, + `is_prime`] / 4 `maglev.rs` table build + lookup [the §A pinned oracle is the correctness gate] / 5 the **M28-3 discriminator refactor** [`ring: Option<HashRing>` → `hash_lb: Option<HashLb>` with `HashLb { Ring, Maglev }`; both the round-robin AND the RING_HASH fixture-0036 paths byte-identical] / 6 fixture `0037-lb-maglev` differential [STRONG; clone of 0036; the `{{BACKEND_IP}}` shared-IP is load-bearing per the `consistent-hash-lb-differential-needs-identical-endpoint-strings` memory] / 7 in-process backstop [determinism / spread / fallback / single-host / the M28-3 regression witness] / 8 `parse_bootstrap` maglev fuzz seed + BEHAVIOR_CONTRACT "LB selection" MAGLEV row [M28-1 folded]). All 36 existing fixtures stay green (regression-equivalence).
- **§6.1 split did NOT fire — ADR-0073 reserved + UNFIRED** (~450–550 LoC / 8 tasks, well under the ~1500-LoC / ~25-task gate — xxHash64, the route-`hash_policy` request plumbing, the `LbPolicy`/`*LbConfig` config pattern, and the two-backend fixture harness all pre-exist). The xxHash64 generalization to a seeded `xxh64_seed` (seed 0 byte-identical) is Task 1; the M28-3 carry-forward is consumed by Task 5; M28-1 folds into Task 8; M28-2 is reconfirmed (header-absent → cursor) and backstop-covered.
