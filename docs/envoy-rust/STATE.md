# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase

**id:** **PHASE 37 (`37-rbac-url-path-condition`) — STATE-2 PLAN-WRITE COMPLETE; STATE-3 IMPLEMENTATION NEXT.** ROADMAP rows `00`-`36` `done`; `37` `in-progress`. The active phase is `37-rbac-url-path-condition`; the next session runs its state-3 implementation (`superpowers:executing-plans` / `subagent-driven-development`) — see `## Next expected skill`.
**slug:** `37-rbac-url-path-condition`
**directory:** `docs/envoy-rust/phases/37-rbac-url-path-condition/` (`SPEC.md` + `PLAN.md` present — `PLAN.md` is the state-2 output [7 TDD tasks], plan-reviewed by a fresh subagent → ✅ APPROVED; `PROGRESS.md` + `REVIEW.md` not yet written)

**status:** **PHASE 37 (`37-rbac-url-path-condition`) — STATE-2 PLAN-WRITE COMPLETE.** This session (the `BOOTSTRAP_PROMPT.md` §5 state-2 PLAN-write, `superpowers:writing-plans`) ran the **§6.2 LOCAL empirical reconnaissance** against live `envoyproxy/envoy:v1.33.0` (the ENVOY_TARGET pin; an H1 `direct_response` listener + a `[rbac, router]` chain; raw-socket path probes so the exact request-target reaches Envoy; `/config_dump` round-trip; isolated-container config-validity boots; all containers torn down), fired **ADR-0090** (the §6.2 reconciliation), authored `PLAN.md` (7 TDD tasks, plan-reviewed by a fresh subagent → ✅ APPROVED), confirmed the **§6.1 split does NOT fire** (~7 tasks / ~350–550 LoC; **ADR-0091 stays UNFIRED**), and advanced STATE `37` state-1-complete/state-2-next → state-2-complete/state-3-next (relocating the phase-37 state-1 four top-section narratives to STATE_HISTORY.md per ADR-0035 / §4.1 inv. 9; renaming the `### Phase-37 state-1 brainstorm` Notes subsection to `### Phase-37 state-2 PLAN-write`). **§6.2 RESULT — the SPEC projection CONFIRMED across the board; STRONG differential target FIRES.** The headline findings (ADR-0090 §A-§D): (i) **THE key risk resolved** — Envoy matches `url_path` against the request-target with **everything from the first `?` removed, and NOTHING else** (query-strip ONLY; the sweep `/al%6Cowed`→DENY [no percent-decode], `/allowed/../allowed`→DENY [no dot-segment norm], `//allowed`→DENY [no slash-merge], `/allowed/`→DENY [trailing slash significant], `/ALLOWED`→DENY [case-sensitive] — EXACTLY the MVP projection); (ii) `url_path` is accepted under BOTH `permissions[]` AND `principals[]` and `/config_dump` round-trips the inline form verbatim; (iii) `safe_regex` is RE2 FULL-match against the query-stripped path (anchored `^/allowed/[0-9]+$` → `/allowed/42`→ALLOW, `/allowed`→DENY); (iv) all four config-validity cases (empty `PathMatcher`, empty `StringMatcher`, unknown sub-key, malformed regex) are **boot-fatal on BOTH proxies** — so **NO new `ConfigError` variant** is needed (every case maps to an existing serde-parse error or the lowering `FilterError::InvalidConfig`); (v) NEW carry-forward **M37-1** — `/allowed#frag` is rejected at the H1 codec as a **400** before `url_path` matching (a separate codec surface, OUT of phase-37). **Implementation surface (locked in PLAN, ADR-0090 §Decision):** a thin DERIVED `PathMatcher { path: StringMatcher }` `#[serde(deny_unknown_fields)]` struct; `Permission::UrlPath`/`Principal::UrlPath` variants (+ `Deserialize`/`Serialize`/`KEYS` arms + `UrlPath(_) => Ok(())` validator leaf arms); `RuntimePermission::UrlPath(StringMatcher)`/`RuntimePrincipal::UrlPath(StringMatcher)` holding the inner `StringMatcher` directly + an `eval` arm calling `strip_query(&req.path)` then `sm.matches`; a `strip_query` helper (`path.split('?').next()`); the two `lower` arms reusing `StringMatcher::compile_safe_regex()` (the phase-36 fallible-lowering path) so a `safe_regex` `url_path` value is BOOT-fatal on a bad pattern; fixture `0045-http-rbac-url-path` (3 path-varying probes: match/miss/query-strip) + a NEW differential test wrapper `tests/differential/tests/rbac_url_path.rs` (there is NO manifest/glob — each fixture needs a hand-written `#[tokio::test]`); an in-process backstop; a `parse_bootstrap` seed (NO new fuzz target); a BEHAVIOR_CONTRACT subsection. **NO new `HttpFilterInstance` variant / crate / dependency / fuzz-target / `ConfigError` variant.** **DECISIONS.md ledger head: ADR-0090** (count 90; next-available **ADR-0091**, RESERVED for the §6.1 split which ADR-0090 confirms does NOT fire). ADR-0014 in force; ADR-0028 open; ADR-0049 governs config-validity. **ROADMAP rows `00`-`36` `done`; `37` `in-progress`.** The next session runs the state-3 implementation — see `## Next expected skill`.

> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases + the active phase's prior sub-state pointers, incl. the phase-25 state-1 brainstorm pointer) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill

Per `BOOTSTRAP_PROMPT.md` §5 state 3 + `SKILL_ROUTING.md`: phase `37` (`37-rbac-url-path-condition`) has a `SPEC.md` AND a `PLAN.md` (implementation incomplete — no `PROGRESS.md`) → the next session runs the **state-3 implementation** (`superpowers:executing-plans` or `superpowers:subagent-driven-development`, TDD per `superpowers:test-driven-development` on every task; append to `PROGRESS.md` per task). Execute `PLAN.md`'s **7 TDD tasks** in order (the §A-§D facts are empirically locked by **ADR-0090** — do NOT re-derive): T1 `PathMatcher` config struct + export / T2 `Permission::UrlPath` end-to-end (config variant + validator arm + `RuntimePermission::UrlPath` + eval arm + fallible lower arm + the `strip_query` helper) / T3 `Principal::UrlPath` end-to-end (symmetric) / T4 backstop (StringMatcher modes + `and_rules`/`or_rules`/`not_rule` composition + DENY-inversion + anchored `safe_regex`) / T5 config-validity boot-fatal backstop / T6 fixture `0045` + the NEW `tests/differential/tests/rbac_url_path.rs` wrapper / T7 `parse_bootstrap` seed + BEHAVIOR_CONTRACT + final §7.5 gate. **Sequencing notes (in PLAN):** T2/T3 are each end-to-end (config + filter) so every commit stays workspace-green (an enum variant breaks the exhaustive matches in both crates); the round-trip test uses **JSON** not YAML (serde_yaml emits `!url_path` tag form the hand-rolled visitor rejects — precedent `rbac_metadata_permission_json_round_trips`); the differential verify command is `cargo test -p differential rbac_url_path` (NOT `0045` — test names are non-numeric → a `0045` filter is a false green); **REBUILD `envoy-bin` before the T6 differential** (stale-binary `unknown field` — the differential-harness-uses-debug-binary lesson); the new fuzz seed needs an explicit `!`-un-ignore line in `crates/envoy-config/fuzz/.gitignore` (verify `git ls-files`). **PUSH per-session from state-3 onward + confirm CI green** (do NOT accumulate unpushed code commits — the phase-33 lesson). The §7.5 phase-done gate is the state-4 concern (after state-3). Do NOT advance to state-4 this session (§5.1 one state per session). **Carry the open carry-forward Minors below into the phase that next touches each surface** (NONE blocks).

**Open carry-forward Minors (fold into the phase that next touches each surface; NONE blocks):**
- **M37-1** (phase-37 §6.2 / ADR-0090 R1) — `/allowed#frag` (a literal `#fragment` in the H1 request-target) is rejected by Envoy at the H1 codec as a **400 Bad Request** BEFORE `url_path` matching — a codec-level request-target-validation surface, NOT a `url_path` verdict. OUT of phase-37 scope (the fixture is locked to query-strip-only probes). envoy-rust's H1-codec handling of a literal `#` in the request line is UNVERIFIED against Envoy; fold whenever the H1 request-line validation surface is next touched.
- **M36-1** (phase-36 §6.2) — envoy-rust's `StringMatcher::matches` SafeRegex uses `regex::is_match` (PARTIAL/substring) but Envoy `safe_regex` is RE2 FULL match (anchored): UNANCHORED patterns diverge. PRE-EXISTING (phase 04.2), cross-cutting (the route-config header SafeRegex shares the path), masked by anchored fixtures. **Phase-37 anchored-LOCKED** (`url_path` `safe_regex` rides the same code path; the fixture/backstop use `^…$` so partial==full) — NOT consumed by 37 (folding the proper `matcher.rs` SafeRegex-anchoring fix would touch the cross-cutting route-config SafeRegex path); DEFERRED to its own future phase.
- **M36-2** (phase-36 state-5 REVIEW.md) — `ValueMatcher::matches(&str)` (`crates/envoy-config/src/matcher.rs`) is production-dead (only test callers after phase-36 F1's `matches_resolved` routing); the "Kept for the value-present call sites" doc-comment is inaccurate. Fix: soften the comment or fold the caller. Doc/cleanup-only; non-blocking.
- **M36-3** (phase-36 state-5 REVIEW.md) — present-but-empty correctness is enforced one filter UPSTREAM: RBAC's `eval_metadata` (`crates/envoy-filter/src/rbac.rs`) treats a stored `Some("")` as present; correct only because `header_to_metadata` writes nothing for an empty header. A one-line `eval_metadata` comment would harden it. Doc-only; non-blocking.
- **M34-1** — no unit test locks the same-namespace/key last-write-wins overwrite in `crates/envoy-filter/src/header_to_metadata.rs` (`multi_rule_composes` covers only distinct namespaces). Cheap test addition. **M34-2** — the A2 default-namespace is exercised only at the config-parse layer, not the filter-execution layer. **M34-3** — cosmetics (T5 redundant `use tempfile::tempdir`; BEHAVIOR_CONTRACT `A-missing`-vs-`A5` heading; fixture README `generate_request_id … load-bearing` overstatement).
- **M33-1** — unnecessary `.clone()` at `crates/envoy-http1/src/hcm.rs:1211` (the H1 record-build `dynamic_metadata` local is last-use → could move, as the H2 path does). **M33-2** (doc-pointer line drift in `command_operator.rs`/`record.rs`) — doc-only.
- the empty-`metadata_match`→fallback doc-comment (`crates/envoy-cluster/src/subset.rs`); M29-1/M29-2 + M30-1; M30-2 (`Cluster.lb_policy` serde-default); the phase-31 M-2/M-3; the HTTP-filters-family (1)-(4) buffer carry-forwards. (M35-1 is CLOSED — CONSUMED by phase-36 F2; the 6 M32 carry-forwards are CONSUMED.)

**Phase 37 is at state-2-PLAN-write-complete — `PLAN.md` authored (7 TDD tasks) + plan-reviewed (✅ APPROVED) + ADR-0090 fired (§6.2 ground truth); the state-3 implementation is next. This state-2 session is docs-only (PLAN + ADR-0090 + STATE + STATE_HISTORY + ROADMAP) — NO code yet, nothing to verify (the §7.5 gate is the state-4 concern after state-3). Phases 36 (`28199106154` @ `19c3fe9`, state-5 REVIEW.md approved `28203230624` @ `3e1346f`), 35 (`28125206968` @ `d1cf8a3`), 34 (`28062068794` @ `7c19803`) and 33 (`28021495767` @ `72fe40d`) are all closed + CI-GREEN — do NOT re-verify.** The §6.2 reconnaissance ran LOCALLY against `envoyproxy/envoy:v1.33.0` this session (all containers torn down). The local Docker-differential / unit false-REDs (`admin_config_dump_server_info` bridge-IP `192.168.65.2`; the `envoy-http2` `send_request_maps_h2_handshake_failure_to_typed_error` host-flake; `upstream_active_health_check_fixture` parallel-load timing — all PASS in isolation / are host-only) are documented, NOT regressions; CI is authoritative.

> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases + the active phase's prior sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit

**Phase-37 state-2 PLAN-write — §6.2 reconnaissance + ADR-0090 + PLAN.md + STATE advance (THIS commit):** the `BOOTSTRAP_PROMPT.md` §5 state-2 PLAN-write (`superpowers:writing-plans`). Ran the **§6.2 LOCAL empirical reconnaissance** against live `envoyproxy/envoy:v1.33.0` (H1 `direct_response` listener + `[rbac, router]`; raw-socket path probes; `/config_dump` round-trip; isolated-container config-validity boots; containers torn down), fired **ADR-0090** (the §6.2 reconciliation — the SPEC projection CONFIRMED across the board: the path-extraction is **query-strip ONLY** (no percent-decode / dot-segment / slash-merge / case norm); `url_path` accepted under BOTH `permissions[]` + `principals[]` + round-trips verbatim; `safe_regex` RE2-FULL-match; all four config-validity cases boot-fatal-ON-BOTH → **NO new `ConfigError` variant**; new carry-forward **M37-1** = `#fragment` codec-400), authored `docs/envoy-rust/phases/37-rbac-url-path-condition/PLAN.md` (7 TDD tasks, plan-reviewed by a fresh subagent → ✅ APPROVED after a JSON-round-trip + differential-test-wrapper fix), confirmed the **§6.1 split does NOT fire** (~7 tasks / ~350–550 LoC; **ADR-0091 stays UNFIRED**), and advanced STATE `37` state-1-complete/state-2-next → state-2-complete/state-3-next (relocating the phase-37 state-1 four top-section narratives to STATE_HISTORY.md per ADR-0035 / §4.1 inv. 9; renaming the `### Phase-37 state-1 brainstorm` Notes subsection to `### Phase-37 state-2 PLAN-write`). **THIS docs-only commit** (PLAN + DECISIONS + ROADMAP + STATE + STATE_HISTORY; NO code change). `#![forbid(unsafe_code)]` unaffected; no new crate/dependency/fuzz-target/`HttpFilterInstance` variant/`ConfigError` variant. **DECISIONS.md ledger head: ADR-0090** (count 90; next-available **ADR-0091**, reserved for the §6.1 split which ADR-0090 confirms does NOT fire). ADR-0014 in force; ADR-0028 open. The next session runs the state-3 implementation (`superpowers:executing-plans` / `subagent-driven-development`).

> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases + the active phase's prior sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated

2026-06-25 (phase-37 **STATE-2 PLAN-WRITE COMPLETE**. Ran the **§6.2 LOCAL empirical reconnaissance** against live `envoyproxy/envoy:v1.33.0` [H1 `direct_response` + `[rbac, router]`; raw-socket path probes so the exact request-target reaches Envoy; `/config_dump` round-trip; isolated-container config-validity boots; containers torn down], fired **ADR-0090** [§6.2 reconciliation — the SPEC projection CONFIRMED across the board: (i) THE key risk resolved — Envoy matches `url_path` against the request-target with everything from the first `?` removed and NOTHING else (query-strip ONLY — `/al%6Cowed`/`/allowed/../allowed`/`//allowed`/`/allowed/`/`/ALLOWED` all DENY; no percent-decode / dot-segment / slash-merge / case norm); (ii) `url_path` accepted under BOTH `permissions[]` + `principals[]`, `/config_dump` round-trips verbatim; (iii) `safe_regex` RE2-FULL-match against the stripped path; (iv) empty/path-less `PathMatcher` + unknown sub-key + malformed regex all boot-fatal ON BOTH proxies → **NO new `ConfigError` variant**; (v) NEW carry-forward **M37-1** = `/allowed#frag` → Envoy 400 at the H1 codec before `url_path`], authored `PLAN.md` [7 TDD tasks — T1 `PathMatcher` struct / T2 `Permission::UrlPath` end-to-end + `strip_query` / T3 `Principal::UrlPath` / T4 modes+composition+DENY+anchored-`safe_regex` backstop / T5 config-validity boot-fatal / T6 fixture `0045` + the NEW `rbac_url_path.rs` differential wrapper / T7 fuzz seed + BEHAVIOR_CONTRACT + §7.5 gate], plan-reviewed by a fresh subagent → ✅ APPROVED [after a JSON-round-trip fix (serde_yaml emits `!url_path` tag form the hand-rolled visitor rejects) + a differential-test-wrapper fix (no manifest/glob — each fixture needs a hand-written `#[tokio::test]`)], confirmed §6.1 split NOT fired [~7 tasks / ~350–550 LoC; **ADR-0091 UNFIRED**], advanced STATE [relocating the phase-37 state-1 four top-section narratives to STATE_HISTORY.md per ADR-0035; renaming the `### Phase-37 state-1 brainstorm` Notes subsection to `### Phase-37 state-2 PLAN-write`]. **M37-1 NEW** [`#fragment` codec-400]; **M36-1** [unanchored SafeRegex partial-vs-full] anchored-LOCKED in the fixture/backstop, NOT consumed; M36-2/M36-3 + older carry-forwards remain live; M35-1 CLOSED [consumed by phase-36 F2]. **ROADMAP rows `00`-`36` `done`; `37` `in-progress`.** Docs-only; NO code change; `#![forbid(unsafe_code)]` unaffected. **DECISIONS.md ledger head: ADR-0090** [count 90; next-available ADR-0091]. ADR-0014 in force; ADR-0028 open. The next session runs the state-3 implementation [`superpowers:executing-plans` / `subagent-driven-development`].)

> Historical `## Last updated` notes — every superseded last-updated note (all closed phases + the active phase's prior sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: one state per session; do not chain states. State-6 close-out commits touch ROADMAP.md + STATE.md only and carry no code changes; the next session writes PLAN.md for the next active phase per `superpowers:writing-plans`.
- The reviewer's R2 disposition decision (option (a) retroactive split of 05.1 vs option (b) free-standing post-05.1 sub-phase) was settled at the 05.1 state-6 commit in favor of option (b); 05.4 is the chosen sibling sub-phase. Future-reviewers reading STATE.md should understand that 05.1 is structurally closed at the preamble landing; 05.4 is a SIBLING under parent-05, not a child of 05.1; and the execution order ran 05.1 → 05.4 → 05.2 → 05.3, with 05.3 the closing sub-phase that flips parent-05 to `done`.

> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) and for parent phase 25 (the `### Phase-25 state-1 brainstorm` pick + recon-finding narrative, relocated at the 25.2 state-6 close-out when parent `25` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 26 (the `### Phase-26 state-1 brainstorm` pivot/rejected-alternatives/key-scoping narrative, relocated at the phase-26 state-6 close-out when row `26` flipped to `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 27 (the `### Phase-27 state-1 brainstorm` / `### Phase-27 state-2 PLAN-write` / `### Phase-27 state-4 verification` narratives + the now-consumed `### Phase-27 carry-forwards` [M26-1..M26-8] block, relocated at the phase-27 state-6 close-out when row `27` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 28 (the `### Phase-28 state-1 brainstorm` / `### Phase-28 state-2 PLAN-write` / `### Phase-28 state-3 implementation + state-4 verification` / `### Phase-28 state-5 code review` narratives + the now-consumed `### Phase-28 carry-forwards` [M27-1..M27-3] block, relocated at the phase-28 state-6 close-out when row `28` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 29 (the now-consumed `### Phase-29 carry-forwards` [M28-1..M28-3] block + the `### Phase-29 state-1 brainstorm` / `### Phase-29 state-2 PLAN-write` / `### Phase-29 state-3 implementation` / `### Phase-29 state-4 verification` / `### Phase-29 state-5 code review` narratives, relocated at the phase-29 state-6 close-out when row `29` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 30 (the `### Phase-30 carry-forwards` [M29-1/M29-2, which fed phase 30 but were NOT consumed — they continue as phase-31 carry-forwards] block + the `### Phase-30 state-1 brainstorm` / `### Phase-30 state-2 PLAN-write` / `### Phase-30 state-3 implementation` / `### Phase-30 state-4 verification` / `### Phase-30 state-5 code review` narratives, relocated at the phase-30 state-6 close-out when row `30` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 31 (the `### Phase-31 carry-forwards` [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 — open Minors from the phase-30 REVIEW.md that fed phase 31 but were NOT consumed; they continue as carry-forwards for the next phase that touches the differential hash-sweep driver / the config parser] block + the `### Phase-31 state-1 brainstorm` / `### Phase-31 state-2 PLAN-write` / `### Phase-31 state-3 implementation` / `### Phase-31 state-4 verification` / `### Phase-31 state-5 code review` narratives, relocated at the phase-31 state-6 close-out when row `31` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 32 (the `### Phase-32 carry-forwards` block — the open Minors that fed phase 32 but were NOT consumed: the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3; they continue as carry-forwards for the future phase that touches their surface — re-listed in `## Next expected skill` above alongside the 6 new phase-32 REVIEW.md Minors M32-1…M32-6), relocated at the phase-32 state-6 close-out when row `32` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phases 33+34 (the `### Phase-33 carry-forwards` block — the active-phase carry-forwards Notes that lived through phases 33 and 34: the now-CONSUMED 6 phase-32 REVIEW.md Minors M32-1…M32-6 [folded + landed at the phase-33 state-3] + the "Other still-live carry-forwards" list [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3 + the HTTP-filters-family (1)-(4) — those still-live ones re-listed in `## Next expected skill` above alongside the new phase-34 Minors M34-1/M34-2/M34-3 + the phase-33 M33-1/M33-2]), relocated at the phase-34 state-6 close-out when row `34` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 35 (the `### Phase-35 state-1 brainstorm` / `### Phase-35 state-2 PLAN-write` / `### Phase-35 state-3 implementation` / `### Phase-35 state-4 verification` / `### Phase-35 state-5 code-review` narratives, relocated at the phase-35 state-6 close-out when ROADMAP row `35` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 36 (the `### Phase-36 state-5 code-review` narrative — phase 36 used a rename-in-place Notes discipline, so the state-1..4 sub-state narratives were superseded in place each session and only the four top-section blocks were relocated per-session — plus the now-CLOSED detailed M35-1 carry-forward bullet [CONSUMED by phase-36 F2], relocated at the phase-36 state-6 close-out when ROADMAP row `36` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Phase-37 state-2 PLAN-write (active phase)

- **What ran:** the §5 state-2 PLAN-write (`superpowers:writing-plans`). Re-read the cold-start contract + the phase-37 SPEC + ADR-0089/0090-reserved, re-verified the code grounding (RBAC enums/visitors/validators, `StringMatcher`/`compile_safe_regex`, `FilterRequest.path`, the 0043 fixture + `http1_probe_list` driver), ran the §6.2 LOCAL reconnaissance against live `envoyproxy/envoy:v1.33.0`, fired ADR-0090, authored + plan-reviewed `PLAN.md` (→ ✅ APPROVED), applied the §6.1 gate (no split), and advanced STATE. Docs-only; NO code change (§5.1 one state per session — did NOT chain into the state-3 implementation).
- **§6.2 ground truth (ADR-0090 §A-§D):** THE key risk resolved — Envoy matches `url_path` against the request-target with **everything from the first `?` removed and NOTHING else** (query-strip ONLY; the sweep proves no percent-decode [`/al%6Cowed`→DENY], no dot-segment norm [`/allowed/../allowed`→DENY], no slash-merge [`//allowed`→DENY], trailing-slash significant [`/allowed/`→DENY], case-sensitive [`/ALLOWED`→DENY] — EXACTLY the MVP projection). `url_path` accepted under BOTH `permissions[]` + `principals[]`; `/config_dump` round-trips verbatim. `safe_regex` is RE2 FULL-match against the stripped path (anchored `^/allowed/[0-9]+$` → `/allowed/42`→ALLOW, `/allowed`→DENY). All four config-validity cases (empty `PathMatcher`, empty `StringMatcher`, unknown sub-key, malformed regex) are **boot-fatal on BOTH proxies** → **NO new `ConfigError` variant** (every case maps to an existing serde-parse error or the lowering `FilterError::InvalidConfig`). NEW carry-forward **M37-1**: `/allowed#frag` → Envoy 400 at the H1 codec before `url_path` (a codec surface, OUT of scope).
- **Implementation surface (PLAN, 7 TDD tasks):** a thin DERIVED `PathMatcher { path: StringMatcher }` `#[serde(deny_unknown_fields)]` struct (no hand-rolled visitor); `Permission::UrlPath`/`Principal::UrlPath` variants (+ `Deserialize`/`Serialize`/`KEYS` + `UrlPath(_) => Ok(())` validator leaf arms); `RuntimePermission::UrlPath(StringMatcher)`/`RuntimePrincipal::UrlPath(StringMatcher)` holding the inner `StringMatcher` directly; an eval arm calling `strip_query(&req.path)` then `sm.matches`; a `strip_query` helper; the two `lower` arms reusing `StringMatcher::compile_safe_regex()` (phase-36 fallible lowering); fixture `0045` (match/miss/query-strip) + a NEW `tests/differential/tests/rbac_url_path.rs` wrapper; an in-process backstop; a `parse_bootstrap` seed (NO new target); a BEHAVIOR_CONTRACT subsection. NO new `HttpFilterInstance` variant / crate / dependency / fuzz-target / `ConfigError` variant.
- **Plan-review fixes (fresh subagent → ✅ APPROVED):** two CRITICAL caught + fixed — (C1) the round-trip test must use **JSON** not YAML (serde_yaml serializes the externally-tagged enum as `!url_path` tag form the hand-rolled visitor rejects on reparse — precedent `rbac_metadata_permission_json_round_trips`); (C2) fixture 0045 needs an explicit hand-written `#[tokio::test]` wrapper (there is NO manifest/glob, and `cargo test -p differential 0045` matches zero tests = false green → use `rbac_url_path`). Plus two MINORs (export alphabetical slot; `and_rules`/`or_rules` composition coverage).
- **Carry-forwards:** M37-1 NEW (`#fragment` codec-400). M36-1 (unanchored SafeRegex partial-vs-full) anchored-LOCKED in the fixture/backstop, NOT consumed (folding it touches the cross-cutting route-config SafeRegex path). M36-2/M36-3 + M34-*/M33-* + older carry-forwards remain live; M35-1 CLOSED (consumed by phase-36 F2).
- **Next:** the state-3 implementation (`superpowers:executing-plans` / `subagent-driven-development`) — execute `PLAN.md`'s 7 TDD tasks in order (the §A-§D facts are locked by ADR-0090 — do NOT re-derive), TDD per task, append to `PROGRESS.md`, PUSH per-session + confirm CI green. Do NOT chain into state-4.

### HTTP-filters-family carry-forwards (from the `25.2` REVIEW.md - NOT yet consumed; weigh whenever the HTTP-filters family is re-entered)

> These were never obligations on the xDS phase 26; they remain live for whenever an HTTP-filters-family phase resumes.

- **(1) [non-goal - architectural]** Over-limit request bodies are FULLY buffered before the 413 rejection (no streaming watermark). Documented deferred non-goal; differentially byte-identical to Envoy for the bounded fixture sizes. Revisit only if a streaming `decode_data` watermark path is ever planned.
- **(2) [doc precision]** The BEHAVIOR_CONTRACT 413-row "verified byte-exact against v1.33.0" phrasing - fixture `0033` is H1-only; the H2 over-limit path is covered by the in-process synth-decorator backstop, NOT differentially. Consider narrowing the phrasing if an H2 over-limit fixture is ever added.
- **(3) [coverage]** No standalone `== effective route limit` unit assertion (the boundary is exercised only via the over/under probes).
- **(4) [coverage]** No differential at-limit (`==`) probe in `0033` (within-limit `<` and over-limit `>` are both covered; the exact boundary is not differentially probed).
- _(2)-(4) are cheap polish, (1) is architectural and only relevant to a future streaming phase._
