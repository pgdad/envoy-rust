# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase


**id:** **75.1** — `75.1-headermatcher-absence-engine-route`, the FIRST sub-phase of parent **75** (`75-headermatcher-absence-parity`, cross-cutting correctness: `HeaderMatcher` ABSENCE-SEMANTICS parity with `envoyproxy/envoy:v1.33.0`), created by the §6.1 SPLIT of parent 75 (ADR-0157). **THIS session completed 75.1's §5 STATE-4 VERIFICATION — the §7.5 phase-done gate RAN IN FULL and every command output is quoted verbatim into `PROGRESS.md` under a state-4 heading.** **THE GATE IS GREEN.** `cargo build --workspace --all-targets` exit 0 / 0 warnings; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0 with ZERO warnings, **re-proved under a FORCED re-check** (the first run finished in 1.04 s off only 8 `Checking` lines, i.e. partially cached, so the eight changed files were `touch`ed and clippy re-run → **14** `Checking` lines covering EVERY crate this sub-phase touched); `cargo fmt --all -- --check` exit 0; `cargo build -p envoy-bin` exit 0 BEFORE any differential; `cargo deny check` = `advisories ok, bans ok, licenses ok, sources ok`; **`cargo test --workspace --no-fail-fast` run TWICE, both times `2096 passed; 7 failed; 9 ignored` across 160 binaries**; and the h2spec conformance gate `h2spec_pass_rate_gate ... ok` in BOTH runs with `known-failures.txt` still **21** lines, NEVER trimmed. **ALL SEVEN REDs ADJUDICATED AS HOST-ENVIRONMENTAL, NONE ATTRIBUTABLE TO 75.1** — see `## Next expected skill`. **Fixture `0083` = `1 passed` in BOTH runs** (gate (a)), and **the ENTIRE `SPEC.md` §10 watch-list is green in BOTH runs** — `0007`, `0017`, `0018`, `0078`, `0081`, `0082` plus the in-process backstops — which is gate (b), the phase's DOMINANT risk, CONFIRMING PV-9's prediction that the complete break set was four in-process assertions in one file, all amended by Task 2, with ZERO fixture risk. **§7.4 gate (d) is VACUOUS BY DESIGN and was CONFIRMED rather than assumed:** 0 new fuzz targets (5 total), `ci.yml` untouched (0 commits since `2856976`), corpus seeds still **63**. **`DECISIONS.md` is UNTOUCHED by this session** — no genuinely new decision arose at the gate, so ledger head stays **ADR-0159**, next available **ADR-0160**. **NO `ROADMAP.md` edit** (row `75.1` flips `done` at state-6, not here). **THE NEXT STATE IS 75.1's §5 state-5 CODE REVIEW, a SEPARATE session** — `PROGRESS.md` now carries the state-4 evidence and `REVIEW.md` does not exist, which is state 5 exactly. ROADMAP parent row `75` stays `in-progress` with `sub-phases` = `75.1, 75.2`; row `75.1` stays `in-progress`; row `75.2` stays `planned`.
**slug:** `75.1-headermatcher-absence-engine-route` (parent `75-headermatcher-absence-parity`; sibling `75.2-headermatcher-absence-accesslog`).
**directory:** `docs/envoy-rust/phases/75.1-headermatcher-absence-engine-route/` — holds `SPEC.md`, `PLAN.md` and `PROGRESS.md` (the last now carrying BOTH the state-3 implementation log AND this session's state-4 `# §5 STATE-4 VERIFICATION — the §7.5 phase-done gate` section). **`REVIEW.md` does NOT exist and is written at state-5, the NEXT session.** The sibling `docs/envoy-rust/phases/75.2-headermatcher-absence-accesslog/` holds `SPEC.md` ONLY and must NOT be worked until 75.1 is `done`. The PARENT `docs/envoy-rust/phases/75-headermatcher-absence-parity/SPEC.md` REMAINS ON DISK as the parent record and is a FROZEN artifact — it is NOT edited, and parent 75 has NO `PLAN.md` and never will (§6.2 step 1 ABANDONS the oversize plan).

**status:** **SUB-PHASE 75.1 §5 STATE-4 VERIFICATION COMPLETE (THIS session); 75.1 AWAITS ITS §5 state-5 CODE REVIEW.** Cold-started fully per `BOOTSTRAP_PROMPT.md` §1 on `HEAD == ee865747f1e3fd891e53ec2aa8f98a16bb74b7eb` with `git status --porcelain` clean, branch `main`, and `git fetch origin --prune` showing `origin/main` at the SAME SHA, so no sibling had advanced. CI on that SHA re-confirmed on the FULL 40-char SHA (run `30173334248`, both jobs `success` at FULL step counts **15** and **13**, no runner-starvation signature) — which is LOAD-BEARING for the adjudication, since it means every local RED below passed on CI hardware. **THE SEVEN LOCAL REDs SPLIT INTO EXACTLY TWO DOCUMENTED FAMILIES, and the failing SET was proved UNSTABLE between the two full runs — which is itself the diagnosis** (memory `local-red-set-varies-run-to-run`: a deterministic environmental CORE plus a varying parallel-load TAIL). **FAMILY 1 — fails DETERMINISTICALLY in isolation, the ENVIRONMENTAL signature (5, identical in both runs):** the four upstream-reset fixtures (`access_log_rf_upstream_reset`, `access_log_rcd_upstream_reset`, `access_log_h2_uc_upstream_reset`, `access_log_h2_rcd_upstream_reset`) are the `TcpCloseBackend` IPv6-unreachable family — **real Envoy is the side that is wrong**, resolving the close backend to `[fdc4:f303:9324::254]` and logging `immediate_connect_error: Network_is_unreachable` / `rf: UF` instead of ever reaching it, while envoy-rust logs the CORRECT `UC` / `connection_termination`; and `admin_config_dump_server_info` is the `192.168.65.2` bridge-IP family (envoy-only `backend::192.168.65.2:32965::*` lines with `hostname::host.docker.internal`). **FAMILY 2 — PASSES in isolation at `1 passed`, a parallel-load startup flake (4 across the two runs, MEMBERSHIP CHANGED between them):** run 1 had `upstream_active_health_check_fixture` + `upstream_circuit_breaker_budgets_fixture`; run 2 had `network_filter_direct_response_fixture` + `network_filter_rbac_allow_fixture`. All four share ONE failure text — `upstream Envoy never became accept-ready … Connection refused (os error 111)` — a **STARTUP** failure that never reached a semantic assertion. **THE TWO UNDOCUMENTED MEMBERS WERE NOT ASSUMED AWAY:** `0019-upstream-active-health-check` and `0025-upstream-circuit-breaker-retry-budget` were checked STRUCTURALLY and carry **ZERO** header-matcher constructs across their config files, so they cannot reach `HeaderMatcher::matches` at all. **TWO METHOD TRAPS WERE CAUGHT RATHER THAN BELIEVED:** (1) the first blast-radius grep used a shell glob that DID NOT RESOLVE and returned a vacuously clean `0 hits` — the fixture directories were then resolved from the test entrypoints instead; and (2) an isolation re-run exited **101** on `cargo test -p differential --test network_filter_rbac`, which was NOT a test failure but `error: no test target named … in 'differential' package` (that target lives in `envoy-bin`; the fixture's real binary is `network_filter_rbac_allow`, which is `1 passed`) — an exit code alone would have recorded a phantom RED. Every isolation re-run named its target binary and was adjudicated on the `N passed` COUNT, never on the exit code. **NO code, fixture, expectation or `known-failures.txt` line was changed by this session** — a verification session measures, it does not repair. **No `stop` file** (the `ROADMAP.md` `## Feature Families` at `ROADMAP.md:58` remain largely unbuilt).

_Historical (the SUB-PHASE 75.1 §5 STATE-3 IMPLEMENTATION Active-phase `**id:**` + `**slug:**` + `**directory:**` + `**status:**` blocks + that block's own `_Historical_` pointer line — superseded by this SUB-PHASE 75.1 §5 STATE-4 VERIFICATION; relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this session per ADR-0035.)_
> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases incl. the phase-51 state-3/4/5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill


**The next session runs the §5 STATE-5 CODE REVIEW FOR SUB-PHASE 75.1** (`superpowers:requesting-code-review` — per the `SKILL_ROUTING.md` state machine: the implementation is complete, `PROGRESS.md` exists AND now carries the state-4 gate evidence, and `REVIEW.md` does not exist, which is state 5 exactly). **It writes `REVIEW.md`.** It is a SEPARATE session by §5.1 and ADR-0127 — **the context that RAN the gate must not be the one that reviews it**, which is precisely why this session did NOT write `REVIEW.md`. **It must NOT re-run the whole §7.5 gate** (it is green and quoted in `PROGRESS.md`; re-measure only what a specific review question actually needs), **must NOT re-implement, must NOT edit `DECISIONS.md`** (ADR-0159 landed at state-2 and BOTH state-3 and state-4 left it untouched — its last commit is still `78c37a3`; append an ADR-0160 only if a genuinely NEW decision arises), **must NOT start 75.2**, and **must NOT chain into state-6**. Per memory `state5-must-probe-untested-compositions`, a green gate proves the code does what its tests ASK, not that the tests ask the right question — the review should fan out READ-ONLY review dimensions, then make any decisive measurement ITSELF in a scratch worktree, and RE-VERIFY every subagent finding on disk before adopting it. If the review finds issues, §5.2 re-entry is at **state 3**, not state 4.

**WHAT STATE-5 INHERITS — the gate is GREEN and the evidence is banked.** **`PROGRESS.md` now carries a full `# §5 STATE-4 VERIFICATION` section** with every command output quoted verbatim, a per-failure adjudication table, and a §7.5 verdict table. Headline numbers, so the reviewer need not re-derive them: `cargo test --workspace --no-fail-fast` run **TWICE**, both `2096 passed; 7 failed; 9 ignored` across **160** binaries; build/clippy/fmt/`cargo build -p envoy-bin`/`cargo deny check` all exit **0** (clippy with ZERO warnings, re-proved under a FORCED re-check yielding **14** `Checking` lines after `touch`ing the eight changed files — the first run was partially cached at 1.04 s / 8 lines); `h2spec_pass_rate_gate ... ok` in both runs; the 9 `ignored` are pre-existing `#[ignore]`d benchmarks, not new skips. **Gate (a):** fixture `0083` = `1 passed` in BOTH runs. **Gate (b) — the phase's DOMINANT risk — PASSED:** the entire `SPEC.md` §10 watch-list (`0007`, `0017`, `0018`, `0078`, `0081`, `0082` + the in-process backstops) is green in BOTH runs, CONFIRMING PV-9. **Gate (d) is VACUOUS BY DESIGN and was CONFIRMED on disk:** 0 new fuzz targets, `ci.yml` untouched, 63 corpus seeds. **The 7 REDs are NOT open questions** — they resolve to two documented host families (see `## Active phase`), the failing SET was proved to CHANGE between the two runs, the deterministic five ARE the environmental signature, the other four pass in isolation at `1 passed`, and the two members not previously documented were shown to carry ZERO header-matcher constructs in their configs. **A reviewer should NOT re-open them as regressions**; if the review wants to challenge that, challenge the ADJUDICATION METHOD, not the counts.


**Standing traps** (enduring). **NEW HOST-FLAKE FAMILY MEMBER (observed at the 75.1 state-2 PLAN-write):** `plaintext_rbac_before_tcp_proxy_delivers_banner_to_a_byteless_client` (`crates/envoy-bin/tests/network_filter_rbac.rs:856`) false-REDs on CI with `admin up: Os { code: 111, kind: ConnectionRefused }` — the SAME port-reuse startup-race signature as `no_rds_is_inert`, `happy_reload_flips_endpoint_and_ticks_counters` and `happy_path_dynamic_cluster_serves_and_reports`. It was hit on a **DOCS-ONLY** commit that changed zero code (proof it is not a regression), it never reached an RBAC assertion, and `gh run rerun --failed` went GREEN with both jobs at full step counts. **Add it to the CI-authoritative flake set; do NOT debug it as a phase regression.** Cold-start reads `STATE.md` FIRST (disk-authoritative). Confirm CI with the **FULL 40-char SHA** (memory `gh-run-list-commit-needs-full-sha`); a job `cancelled` with `runner_name:""` + `steps:0` never got a runner — rerun the same SHA (memory `ci-run-cancelled-with-no-runner-is-starvation`). `next-prompt.txt` is gitignored — refresh on disk, do NOT `git add` it. The documented host-flake set is CI-authoritative (adjudicate with `--no-fail-fast` + full-output redirect — never `tail`; isolation re-runs name the target binary, `0 passed` is NOT a pass; a MASS differential RED (`client error (Connect)`) is the Docker daemon down — memory `docker-desktop-down-after-reboot-kvm-acl`). `cargo build -p envoy-bin` before ANY local differential (memory `differential-harness-uses-debug-envoy-bin`); `cargo fuzz` from the CRATE dir — and note it takes a **DIRECTORY, not a file** (passing a seed file errors `The required directory … does not exist`); a NEW fuzz corpus seed needs a `!`-un-ignore line (memory `fuzz-corpus-seed-gitignored-by-default`, verified with `git ls-files` — now **63** tracked seeds). Do NOT revert landed `67`-`74` work NOR the parallel workstream's `envoy-http2` header-list bound; never add `rbac` to `is_terminal_network_filter`; never reject `filters: []`; preserve ADR-0016/0124/0131/0133/0136/0137/0139/0140-0143/0144-0149/**0150**/**0151**/**0152**/**0153**/**0154**/**0155**; **ADR-0028 NOT lifted.** Do NOT disturb the **36** access-log fixtures (the 34 prior incl. `0076`-`0080`, plus the new `0081`/`0082`) and do NOT relax the phase-73 composition-arm `< 2` rule. Never weaken a fixture; **never trim `known-failures.txt`** (memory `h2spec-3-5-2-preface-host-sensitive`). Any ROADMAP row edit preserves 6 cells + escapes `\|`; rows `36`/`38`/`39`/`52`/`54` are already malformed — do NOT "fix" them (append-only). `DECISIONS.md` is NOT chronological (memory `decisions-md-not-chronological`; ledger head **ADR-0155**). **envoy-bin writes `ConfigError` to STDOUT.** `#![forbid(unsafe_code)]` holds at every crate root. `STATE.md` top-section blocks are single 3000-8000 char lines — script-edit + 0×-grep (memory `state-md-blocks-are-single-lines-edit-hazard`); ADR-0035 relocation checks are DELTA-based, never "no duplicates" (memory `state6-relocation-check-must-be-delta-based`). `cargo deny` can red on a fresh unrelated RustSec advisory (patch-bump the dep — memory `cargo-deny-reds-on-unrelated-advisory`). **The ADR-0150 seam held through this phase:** `envoy-accesslog` still has ZERO workspace deps; the new `MetadataMatch` matcher is an injected **trait object** exactly like `LogFilter::Header`'s `Arc<dyn HeaderMatch>`; `LogFilter` still has NO `Eq`/`PartialEq`. CF-72-1 is MODE-SCOPED (memory `envoy-headermatcher-invert-absent-is-mode-dependent`): value-matcher+invert+absent DIVERGES, `present_match`+invert+absent is PARITY — a future fixer MUST preserve the `present_match` KEEP. **Phase-74-specific:** `metadata_filter`'s `matcher.invert` is a DIFFERENT field on a DIFFERENT message — MEASURED accepted-but-INERT upstream, so it stays boot-fatal here (CF-74-1); do NOT "implement" it. **Fixture `0082` must keep OMITTING `on_header_missing`** (ADR-0155 PV-6) — envoy-rust requires a `value` on it, which would make the key RESOLVE and silently vacate the key-not-found witness. **The SAME trap now applies to `0081`**, which since the §5.2 re-entry carries a THIRD probe sending NO `x-a`: adding `on_header_missing` there would make that key resolve and vacate the `match_if_key_not_found` default-`true` witness. **`0081` now has THREE probes and expects TWO logged lines** (`STATUS=200 PATH=/x M=-` then `STATUS=200 PATH=/x M=1`, byte-distinct so ORDER is pinned, kept-LAST preserved by placing the new probe SECOND) — a stale `target/debug/envoy-bin` will mis-report it, so `cargo build -p envoy-bin` before ANY local differential. **CF-74-6:** the wrapped `match_if_key_not_found: { value: <bool> }` spelling is accepted+honored upstream and BOOT-FATAL here — a documented REJECT-direction load-parity gap pinned by `metadata_filter_deserialize_round_trip_and_defaults`; do NOT "fix" it by making `Option<bool>` wrapper-accepting (that would lose the absent-vs-explicit-`false` distinction `unwrap_or(true)` depends on). **PHASE-75-SPECIFIC (added at this pick).** **The fix MUST be MODE-SCOPED** — `present_match` + `invert_match` is measured PARITY in both cells, so a naive uniform "absent ⇒ DROP" BREAKS it and mints a NEW divergence; the guard tests are `matcher.rs:425` and `matcher.rs:463`, and the latter's own doc comment instructs the fixer to preserve it. **Honor TDD's RED with a mutation check** that breaks the mode-scoping and watches the P1 guard go RED — run it in a **scratch `git worktree`**, never in the main tree with parallel subagents active (memory `mutation-checks-collide-with-parallel-subagents`), and grep the run for `Compiling` before believing a green (memory `mutation-check-needs-forced-rebuild`; note `clippy` prints `Checking`, not `Compiling`). **TWO CONFLATION TRAPS, do NOT unify them:** `HeaderMatcher.present_match` (this phase) vs `ValueMatcher.present_match` (RBAC/metadata — `bootstrap.rs:1704` and `BEHAVIOR_CONTRACT.md:1863-1885` record that `present_match: false` NEVER matches there, a DIFFERENT and CORRECT rule); and `HeaderMatcher.invert_match` (this phase) vs `MetadataMatcher.invert` (CF-74-1 — accepted-but-INERT upstream, boot-fatal here, must NOT be "implemented"). **Recon traps banked this session:** this host's Docker bind mounts are **STALE-CACHED** — after editing a config in a bind-mounted directory the container kept reading the PREVIOUS contents (verified with `docker run --entrypoint sed`), so `--mode validate` reported a PGV error for a constraint the on-disk file no longer violated; **use a FRESH FILENAME for every config revision**. `--volumes-from` does NOT retrieve a stopped container's `/tmp` — use `docker cp`. envoy-rust requires an explicit `codec_type` on the HCM where upstream defaults it. A parallel workstream's container (`quizzical_goldstine`, image `envoyproxy/envoy:v1.33.0`) was running and was LEFT ALONE; only this session's own `er-recon-*` containers were removed. **PHASE-75.1/75.2-SPECIFIC (added at the §6.1 SPLIT).** **Parent 75 has NO `PLAN.md` and never will** — §6.2 step 1 abandons the oversize plan; do not "finish" it. **The parent `75-headermatcher-absence-parity/SPEC.md` is a FROZEN artifact** — the three corrections ADR-0158 measured against it (C1 the third divergence-encoding test, C2 the stale `BEHAVIOR_CONTRACT.md:1878-1880` cross-reference, C3 the `codec_type` non-divergence) are carried by that ADR and by the sub-phase SPECs; do NOT edit it, and do NOT re-derive the split. **75.2 MUST NOT START until 75.1 is `done`** — both its fixtures assert POST-fix behavior and would be RED on a pre-75.1 tree. **The byte-exact access-log driver takes exactly ONE log file per side** (`AccessLogPaths`, `tests/differential/src/lib.rs:1088-1093`, `deny_unknown_fields`; only the envoy-side parent dir is bind-mounted at `lib.rs:4019`; max sinks in any of the 82 fixtures = 1) — a multi-sink access-log fixture is INFEASIBLE without a driver change, which is why 75.2 has TWO fixtures and why the next ids are **0083**, **0084**, **0085**. **The amend list is THREE tests, not two** (`matcher.rs:342` in addition to `:432` and `:489`). **Correct exactly TWO `matcher.rs:51` citations** (`BEHAVIOR_CONTRACT.md:2369` and the IN-SOURCE `matcher.rs:439`) out of the 26 in the repo — the five in `DECISIONS.md` (`:2479`/`:2546`/`:2555`/`:2624`/`:2631`, plus its already-stale `:373`/`:397`) are APPEND-ONLY, as are the historical phase docs and `STATE_HISTORY.md`. **Upstream Envoy will not CREATE an access-log directory** — a `path:` under a nonexistent dir is boot-fatal (`unable to open file … No such file or directory`); the differential harness creates and `chmod 0o777`s both parent dirs itself (`lib.rs:3991-4014`), so this bites only hand-rolled probes. **YAML 1.1 booleans:** an unquoted `cluster: y` under `node:` parses as boolean `true` and upstream's JSON-proto path then rejects the whole bootstrap (`@ node.cluster: string, … unexpected character 't'`) while envoy-rust's serde accepts it — quote scalar node fields in hand-rolled probe configs. **A `pkill -f <pattern>` whose pattern also matches the invoking shell's own command line kills that shell** — kill by PID or use `pgrep -x`. **Concurrency:** a parallel workstream was running containers named `p75f-s2` / `p75s-arm1a`..`arm4` on image `envoyproxy/envoy:contrib-v1.37.2` — **NOT this project's `v1.33.0` pin**, so they are not authoritative for anything here; they and `quizzical_goldstine` were LEFT ALONE, and only this session's own `er-pv*` containers were removed. **75.1-STATE-2-SPECIFIC (added at the PLAN-write).** **`75.1/PLAN.md` now exists — state-3 EXECUTES it, it does not re-plan.** **ADR-0159 has ALREADY LANDED; `DECISIONS.md` must be UNTOUCHED by state-3** (ledger head ADR-0159, next ADR-0160). **The `DECISIONS.md` `matcher.rs:51` do-not-touch LINE NUMBERS DRIFT every time an ADR is appended** — the SPEC's seven cited lines were already wrong when written and shifted twice more since. **Verify that append-only set by FILE, never by line number, and never by total COUNT** (26 → 32 → higher as this sub-phase's own artifacts quote the defect they fix). Exactly TWO sites are corrected: `crates/envoy-config/src/matcher.rs:439` (the sole `.rs` hit) and `BEHAVIOR_CONTRACT.md:2369`. **The engine shape is DECIDED (ADR-0159)** — an exhaustive tuple `match (&self.mode, value)` whose `(_, None) => return false` arm sits AFTER `PresentMatch` and BEFORE every value arm; do NOT "simplify" the arm order, and do NOT reintroduce an early-return split or an `unreachable!()` arm. **The state-3 mutation is PRE-VALIDATED**: hoisting `(_, None)` above `PresentMatch` turns THREE guards RED (`matcher.rs:463`, `:425`, `:348`), verified against an unmutated control in a scratch worktree. **`validate_header_matcher` never inspects `invert_match`** and its `PresentMatch` arm is a no-op, so the fix needs NO validator change and adds NO config surface. **Fixture `0083`'s `p07-absent-keeps-GUARD` probe is LOAD-BEARING** — a naive uniform absent-DROP passes every other probe in the fixture and fails only that one. **`0083`'s two empty-value probes are the ONE wire-shape unknown** (`curl -H "x-a;"` sent `x-a:`; the harness sends `x-a: `): probe both proxies directly first; dropping ONLY those two probes is pre-authorised if they disagree, and nothing else may be weakened. **The `node:` block in `0083`'s `envoy-rust.yaml` is envoy-rust-ONLY**, so the unquoted YAML-1.1 `cluster: y` boolean trap does NOT apply there (it bites hand-rolled configs sent to UPSTREAM); match the proven `0007` house form and do not "fix" it. **`codec_type: HTTP1` goes on BOTH sides** and is NOT a per-side divergence (ADR-0158 C3). **75.1-STATE-3-SPECIFIC (added at the implementation).** **The engine fix has LANDED** — `HeaderMatcher::matches` is now an exhaustive tuple `match (&self.mode, value)` whose `(_, None) => return false` arm sits AFTER the `PresentMatch` arm and BEFORE every value arm. **Do NOT "simplify" that arm order**: hoisting `(_, None)` to the top is the naive uniform absent-DROP, and it turns FOUR in-process tests RED (`matcher.rs` `:463`, `:425`, `:348` + the absence matrix) while leaving every value-mode assertion green. **CF-72-1 is CONSUMED** by this landing. **Fixture `0083` exists (corpus 83)** and its `p07-absent-keeps-GUARD` probe is LOAD-BEARING — a naive uniform absent-DROP passes every other probe in that fixture and fails only that one. **The empty-value wire-shape question is CLOSED:** `x-a: ` (harness) and `x-a:` (`curl -H "x-a;"`) were BOTH driven at BOTH proxies and all four cells agree, so both empty-value probes stand at full strength — do not "simplify" them away. **`PLAN.md`'s literal Rust is NOT fmt-clean** (five sites in tasks 5/7/8/9) and its `envoy_http1::codec::Request::test(..)` constructor DOES NOT EXIST — a future plan-executor should expect to `cargo fmt --all` and to mirror the sibling test's struct literal. **`cargo test -p envoy-config --lib matcher` selects 60 tests, not 59** — the filter also catches 22 `bootstrap::tests::*matcher*` tests. **A ~1s differential green is NORMAL for `0083`** (backend-free, warm image); it is not evidence of a skip — verified by polling `docker ps` mid-run and by a negative control that fails against upstream's actual bytes. **75.1-STATE-4-SPECIFIC (added at the verification gate).** **The §7.5 gate is GREEN on `ee865747f1e3fd891e53ec2aa8f98a16bb74b7eb`** and every output is quoted in `PROGRESS.md` — do NOT re-run it wholesale at state-5. **TWO NEW PARALLEL-LOAD FLAKE MEMBERS, both CI-authoritative, both proved by isolation `1 passed`:** `upstream_active_health_check_fixture` (fixture `0019`) and `upstream_circuit_breaker_budgets_fixture` (fixture `0025`) joined the `upstream Envoy never became accept-ready … Connection refused (os error 111)` startup-race family, alongside the already-documented `network_filter_direct_response_fixture` and `network_filter_rbac_allow_fixture` — **the membership of that tail CHANGES run to run, so a stable-looking failing set is not evidence of anything; diff the SET across runs.** Both new members carry **ZERO** header-matcher constructs in their configs, so they are structurally outside any `HeaderMatcher` phase's blast radius. **THE FOUR upstream-reset REDs ARE UPSTREAM ENVOY'S FAULT ON THIS HOST, NOT ENVOY-RUST'S** — real Envoy resolves the close backend to IPv6 `[fdc4:f303:9324::254]`, fails `Network_is_unreachable` and logs `rf: UF`, while envoy-rust logs the CORRECT `UC`/`connection_termination`; do NOT "fix" envoy-rust toward `UF`. **A `cargo test -p <pkg> --test <name>` that exits 101 may be an ARGUMENT error, not a failure** — `error: no test target named … in '<pkg>' package` exits 101 exactly like a real RED (this bit the `network_filter_rbac` isolation attempt; the fixture's binary is `network_filter_rbac_allow`, and it lives in `differential` while a SAME-NAMED target lives in `envoy-bin`). **A clippy green finishing in ~1 s off a handful of `Checking` lines is PARTIALLY CACHED** — `touch` the phase's changed files and re-run before believing it (this run went 8 → **14** `Checking` lines after the touch).
_Historical (the SUB-PHASE 75.1 §5 STATE-3 IMPLEMENTATION `## Next expected skill` pointer + the superseded `WHAT STATE-4 INHERITS` risk-posture block + that block's own `_Historical_` pointer line — superseded by this SUB-PHASE 75.1 §5 STATE-4 VERIFICATION; relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this session per ADR-0035. The Standing-traps block is ENDURING and is carried forward IN PLACE with the 75.1 state-4 additions APPENDED, not relocated.)_
> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases incl. the phase-37 state-1..5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit


**Sub-phase 75.1 §5 STATE-4 VERIFICATION — the §7.5 phase-done gate, run in full and quoted verbatim.** **NO code, fixture, config, expectation, `known-failures.txt` line, ADR or ROADMAP row was changed by this session** — a verification session MEASURES, it does not repair. The ONLY file modified is `docs/envoy-rust/phases/75.1-headermatcher-absence-engine-route/PROGRESS.md`, which gains a `# §5 STATE-4 VERIFICATION — the §7.5 phase-done gate` section (~300 lines) carrying: the STEP-0.5 CI re-confirmation on the full 40-char SHA; the build/clippy/fmt/`envoy-bin`/`deny` outputs incl. the FORCED clippy re-check; BOTH `cargo test --workspace --no-fail-fast` runs at `2096 passed; 7 failed; 9 ignored` across 160 binaries; the run-1-vs-run-2-vs-isolation adjudication table for all 9 distinct failures; the gate (a)/(b)/(c)/(d) evidence; the census re-verification; and the §7.5 verdict table. Plus this `STATE.md` advance and its ADR-0035 relocation into `STATE_HISTORY.md`. **`DECISIONS.md` UNTOUCHED** (its last commit remains the state-2 PLAN-write `78c37a3`); **NO `ROADMAP.md` edit**; **NO `ci.yml` edit**; **NO new fuzz target and NO new corpus seed**; **`known-failures.txt` still 21 lines, NEVER trimmed**. Censuses re-verified on disk and all UNCHANGED by this session: fixture dirs **83**, corpus seeds **63**, fuzz targets **5**, `#![forbid(unsafe_code)]` at every crate root. Carry-forward state is UNCHANGED — **CF-72-1 stays CONSUMED** by the state-3 landing (a gate does not move carry-forwards), **M74-31** still travels with 75.2. Next: **75.1's §5 state-5 code review** (a SEPARATE session).

_Historical (the SUB-PHASE 75.1 §5 STATE-3 IMPLEMENTATION Last-commit block + that session's own `_Historical_` pointer line — superseded by this SUB-PHASE 75.1 §5 STATE-4 VERIFICATION Last-commit block; relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this session per ADR-0035.)_
> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases incl. the phase-37 state-1..5 sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated


2026-07-25 (sub-phase 75.1 **§5 STATE-4 VERIFICATION COMPLETE — the §7.5 phase-done gate RAN IN FULL and is GREEN; every command output quoted verbatim into `PROGRESS.md`; `DECISIONS.md` UNTOUCHED**. Confirmed `git status --porcelain` clean, branch `main`, `HEAD` = `ee865747f1e3fd891e53ec2aa8f98a16bb74b7eb` with `origin/main` at the SAME SHA at session start and re-checked before committing; CI on that SHA re-confirmed GREEN on the FULL 40-char SHA (run `30173334248`, both jobs `success` at FULL step counts 15 and 13). Cold-read `BOOTSTRAP_PROMPT.md`, `STATE.md`, the 75.1 `SPEC.md` and `PROGRESS.md` IN FULL, plus `ENVOY_TARGET.md`, the ROADMAP rows `75`/`75.1`/`75.2` and the `PLAN.md` task structure by targeted read. **Ran the gate SOLO-SERIAL** — no subagents: the cargo lock makes concurrency pointless and the §7.5 adjudication is one indivisible judgment. **The gate is green on all six parts:** build 0 / clippy 0 warnings / fmt 0 / `envoy-bin` 0 / `deny` `advisories ok, bans ok, licenses ok, sources ok` / `cargo test --workspace --no-fail-fast` **run TWICE at `2096 passed; 7 failed; 9 ignored`** / `h2spec_pass_rate_gate ... ok` / fixture `0083` `1 passed` in both runs / the whole gate-(b) watch-list green in both runs / gate (d) vacuous-by-design and CONFIRMED on disk. **THREE FALSE-SIGNAL TRAPS WERE CAUGHT RATHER THAN BELIEVED:** (a) a 1.04 s clippy green off 8 `Checking` lines was PARTIALLY CACHED — `touch`ing the eight changed files and re-running gave **14** `Checking` lines covering every touched crate (memories `mutation-check-needs-forced-rebuild` + `clippy-prints-checking-not-compiling`); (b) the first blast-radius grep used a shell glob that did not resolve and returned a VACUOUSLY clean `0 hits` — redone by resolving the fixture dirs from the test entrypoints; (c) an isolation re-run exiting **101** was an `error: no test target named …` ARGUMENT error, not a RED (memory `cargo-test-p-name-false-green-filtered-out`, in its mirror form — read the TEXT, assert on the `N passed` COUNT). **All 7 REDs adjudicated as host-environmental with the reasoning written down**, split into the deterministic-in-isolation environmental core (4 upstream-reset IPv6-unreachable + 1 bridge-IP `admin_config_dump_server_info`) and the parallel-load startup-race tail whose MEMBERSHIP CHANGED between the two runs (all 4 pass in isolation at `1 passed`); the two members not previously documented were shown to carry ZERO header-matcher constructs. **Do NOT chain into 75.1's state-5 code review** (a SEPARATE session per §5.1 / ADR-0127); **no `stop` file**. Ledger head **ADR-0159**, next **ADR-0160**. Live carry-forwards UNCHANGED from state-3: **CF-72-2** [extended], **CF-75-1**, CF-74-1/2/3/4/6 [CF-74-5 CLOSED], CF-73-1, N73-R2, M73-R1/M73-R2, M71-3, M71-6/7/8, M70-R4/R9, M69-A..I, CF-69-1/2/3/5, M68-1, M-1, CF-67-3/5/6/7, M74-3..M74-14, M74-16, M74-17/18/20/21/22/26/27/28/29, M74-30..M74-39, the older Minors + the HTTP-filters-family (1)-(4). **CF-72-1 stays CONSUMED**; **M74-31 travels with 75.2**.)

_Historical (the SUB-PHASE 75.1 §5 STATE-3 IMPLEMENTATION Last-updated note + that session's own `_Historical_` pointer line — superseded by this SUB-PHASE 75.1 §5 STATE-4 VERIFICATION Last-updated note; relocated verbatim to [STATE_HISTORY.md](STATE_HISTORY.md) at this session per ADR-0035.)_
> Historical `## Last updated` notes — every superseded last-updated note (all closed phases incl. the phase-37 state-1..5 sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsection for fully-closed phase 64 (the `### Phase-64 state-5 code-review` narrative — phase 64 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-64 state-6 close-out when ROADMAP row `64` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 63 (the `### Phase-63 state-5 code-review` narrative — phase 63 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-63 state-6 close-out when ROADMAP row `63` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 61 (the `### Phase-61 state-1 brainstorm` / `### Phase-61 state-2 PLAN-write` / `### Phase-61 state-3 implementation` / `### Phase-61 state-4 verification` / `### Phase-61 state-5 code-review` narratives, relocated at the phase-61 state-6 close-out when ROADMAP row `61` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 58 (the `### Phase-58 state-1 brainstorm` / `### Phase-58 state-2 PLAN-write` / `### Phase-58 state-3 implementation` / `### Phase-58 state-4 verification` / `### Phase-58 state-5 code-review` narratives, relocated at the phase-58 state-6 close-out when ROADMAP row `58` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 57 (the `### Phase-57 state-1 brainstorm` / `### Phase-57 state-2 PLAN-write` / `### Phase-57 state-3 implementation` / `### Phase-57 state-4 verification` / `### Phase-57 state-5 code-review` narratives, relocated at the phase-57 state-6 close-out when ROADMAP row `57` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 42 (the `### Phase-42 state-5 code-review (active phase)` narrative — phase 42 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-42 state-6 close-out when ROADMAP row `42` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 43 (the `### Phase-43 state-5 code-review (active phase)` narrative — phase 43 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-43 state-6 close-out when ROADMAP row `43` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 44 (the `### Phase-44 state-5 code-review (active phase)` narrative — phase 44 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-44 state-6 close-out when ROADMAP row `44` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 45 (the `### Phase-45 state-5 code-review (active phase)` narrative — phase 45 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-45 state-6 close-out when ROADMAP row `45` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 46 (the `### Phase-46 state-5 code-review (active phase)` narrative — phase 46 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-46 state-6 close-out when ROADMAP row `46` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 47 (the `### Phase-47 state-5 code-review (active phase)` narrative — phase 47 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-47 state-6 close-out when ROADMAP row `47` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 48 (the `### Phase-48 state-5 code-review (active phase)` narrative — phase 48 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-48 state-6 close-out when ROADMAP row `48` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 49 (the `### Phase-49 state-5 code-review (active phase)` narrative — phase 49 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-49 state-6 close-out when ROADMAP row `49` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 50 (the `### Phase-50 state-3 implementation (active phase)` narrative — phase 50 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-50 state-6 close-out when ROADMAP row `50` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 51 (the `### Phase-51 state-3 implementation (active phase)` narrative — phase 51 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-51 state-6 close-out when ROADMAP row `51` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 53 (the `### Phase-53 state-1 brainstorm` narrative — phase 53 kept a single active-phase Notes subsection at "state-1 brainstorm" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-1 subsection remained in `## Notes` at close — relocated at the phase-53 state-6 close-out when ROADMAP row `53` flipped `done`, together with the inline superseded state-3 narrative) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 54 (the `### Phase-54 state-1 brainstorm` / `### Phase-54 state-2 PLAN-write` / `### Phase-54 state-3 implementation` / `### Phase-54 state-4 verification` / `### Phase-54 state-5 code-review` narratives, relocated at the phase-54 state-6 close-out when ROADMAP row `54` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 55 (the `### Phase-55 state-5 code-review (active phase)` narrative — phase 55 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state individually superseded in place and relocated to STATE_HISTORY.md at its own transition], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-55 state-6 close-out when ROADMAP row `55` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 56 (the `### Phase-56 state-5 code-review (active phase)` narrative — phase 56 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state individually superseded in place and relocated to STATE_HISTORY.md at its own transition], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-56 state-6 close-out when ROADMAP row `56` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 66 (the `### Phase-66 state-5 code-review (active phase)` narrative — phase 66 used a rename-in-place active-phase Notes subsection across its state-0/1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-66 state-6 close-out when ROADMAP row `66` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed sub-phase 67.1 (the `### Phase-67.1 §5 state-5 code-review` narrative — sub-phase 67.1 used a rename-in-place active-phase Notes subsection across its state-2..5 + §5.2 state-3 re-entry arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-67.1 state-6 close-out when ROADMAP row `67.1` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed sub-phase 67.2 (the `### Phase-67.2 §5 state-5 re-review` narrative — sub-phase 67.2 used a rename-in-place active-phase Notes subsection across its state-2..5 + §5.2 state-3 re-entry + state-4-re-verification + state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review subsection remained in `## Notes` at close — relocated at the phase-67.2 state-6 close-out when ROADMAP row `67.2` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
> Historical Notes subsection for fully-closed phase 68 (the `### Phase-68 §5 state-5 code-review` narrative — phase 68 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-68 §5 state-6 close-out when ROADMAP row `68` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 69 (the `### Phase-69 §5 state-5 re-review` narrative — phase 69 used a rename-in-place active-phase Notes subsection across its state-3 → §5.2 state-3 re-entry → state-4 re-verification → state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review subsection remained in `## Notes` at close — relocated at the phase-69 §5 state-6 close-out when ROADMAP row `69` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 70 (the `### Phase-70 §5 state-5 re-review (2nd)` narrative — phase 70 used a rename-in-place active-phase Notes subsection across its state-1..5 + two-§5.2-re-entry + two-state-4-re-verification + two-state-5-re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review-(2nd) subsection remained in `## Notes` at close — relocated at the phase-70 §5 state-6 close-out when ROADMAP row `70` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 71 (the `### Phase-71 §5 state-5 code-review` narrative — phase 71 used a rename-in-place active-phase Notes subsection across its state-0/1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-71 §5 state-6 close-out when ROADMAP row `71` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 72 (the `### Phase-72 §5 state-5 re-review` narrative — phase 72 used a rename-in-place active-phase Notes subsection across its state-1..5 + §5.2 state-3 re-entry + state-4 re-verification + state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 re-review subsection remained in `## Notes` at close — relocated at the phase-72 §5 state-6 close-out when ROADMAP row `72` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 73 (the `### Phase-73 §5 state-5 code-review` narrative — phase 73 used a rename-in-place active-phase Notes subsection across its state-0/1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-73 §5 state-6 close-out when ROADMAP row `73` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 74 (the `### Phase-74 §5 state-5 re-review (2nd)` narrative — phase 74 used a rename-in-place active-phase Notes subsection across its state-0/1..5 + TWO §5.2 state-3 re-entry + TWO state-4 re-verification + TWO state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review-(2nd) subsection remained in `## Notes` at close — relocated at the phase-74 §5 state-6 close-out when ROADMAP row `74` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Phase-75.1 §5 state-4 verification (active sub-phase 75.1)

- **The §7.5 phase-done gate RAN IN FULL and is GREEN**, with every command output quoted verbatim into `PROGRESS.md` under a `# §5 STATE-4 VERIFICATION` heading. `cargo build --workspace --all-targets` exit 0 / 0 warnings; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0 / ZERO warnings; `cargo fmt --all -- --check` exit 0; `cargo build -p envoy-bin` exit 0 BEFORE any differential; `cargo deny check` = `advisories ok, bans ok, licenses ok, sources ok`; `cargo test --workspace --no-fail-fast` **run TWICE**, both `2096 passed; 7 failed; 9 ignored` across **160** binaries; `h2spec_pass_rate_gate ... ok` in both runs with `known-failures.txt` untrimmed at **21** lines.
- **Gate (b) — the phase's DOMINANT risk — PASSED, confirming PV-9.** The entire `SPEC.md` §10 watch-list is green in BOTH runs (`0007`, `0017`, `0018`, `0078`, `0081`, `0082`, plus `http_filter_rbac_in_process_backstop`, `http_filter_fault_in_process_backstop`, `http1_direct_response_round_trip`). Gate (a): fixture `0083` = `1 passed` in both runs. Gate (d) is vacuous by design and was CONFIRMED on disk (0 new fuzz targets, `ci.yml` untouched, 63 seeds), not inherited from the plan.
- **All 7 REDs adjudicated as host-environmental; NONE attributable to 75.1.** The decisive move was running the suite TWICE and diffing the failing SET: **it changed**. A deterministic core of 5 fails identically in both runs AND in isolation — the four upstream-reset fixtures (real Envoy resolving the close backend to IPv6 `[fdc4:f303:9324::254]`, logging `Network_is_unreachable`/`rf: UF` while **envoy-rust logs the CORRECT `UC`**) and `admin_config_dump_server_info` (the `192.168.65.2` bridge-IP family). A tail of 4 CHANGED membership between runs and every member passes in isolation at `1 passed`, all sharing the startup text `upstream Envoy never became accept-ready … Connection refused (os error 111)` — never a semantic assertion.
- **The two tail members that were NOT in the documented flake set were checked, not assumed.** `0019-upstream-active-health-check` and `0025-upstream-circuit-breaker-retry-budget` carry **ZERO** header-matcher constructs across their config files, so they cannot reach `HeaderMatcher::matches` at all. CI on this exact SHA being GREEN is the independent cross-check: every local RED passed on CI hardware.
- **THREE false-signal traps were caught rather than believed.** (a) A clippy green finishing in **1.04 s** off only 8 `Checking` lines was PARTIALLY CACHED — `touch`ing the eight changed files and re-running gave **14** `Checking` lines covering every crate this sub-phase touched (and the token to grep for clippy is `Checking`, never `Compiling`). (b) The first blast-radius grep used a shell glob that DID NOT RESOLVE, returning a vacuously clean `0 hits`; it was redone by resolving the fixture directories from the test entrypoints. (c) An isolation re-run exiting **101** was an `error: no test target named 'network_filter_rbac' in 'differential' package` ARGUMENT error, not a RED — the fixture's real binary is `network_filter_rbac_allow` (a same-named target exists in `envoy-bin`), and it is `1 passed`.
- **NOTHING was repaired, weakened or trimmed.** No code, fixture, config, expectation, `known-failures.txt` line, ADR or ROADMAP row changed — a verification session measures. `DECISIONS.md` stays at ledger head **ADR-0159** with its last commit still the state-2 `78c37a3`.
- **Next:** **75.1's §5 state-5 CODE REVIEW** (a SEPARATE session per §5.1 / ADR-0127 — the context that RAN the gate must not grade it, which is why this session did NOT write `REVIEW.md`). It should NOT re-run the whole gate; per memory `state5-must-probe-untested-compositions` a green gate proves the code does what its tests ASK, not that the tests ask the right question. §5.2 re-entry, if any, is at state **3**.

### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: **one state per session; do not chain states.** **PARENT PHASE 75 WAS SPLIT (§6.1, ADR-0157); the ACTIVE SUB-PHASE IS 75.1** (`75.1-headermatcher-absence-engine-route`; ROADMAP parent row `75` is `in-progress` with `sub-phases` = `75.1, 75.2`, row `75.1` is `in-progress`, row `75.2` is `planned` and depends-on `75.1`). ADR-0156 + **ADR-0157** (the split) + **ADR-0158** (the parent's §6.2 reconciliation) + **ADR-0159** (75.1's own §6.2 reconciliation, fired at the PLAN-write) are all FIRED; ledger head **ADR-0159**, next **ADR-0160**. **`DECISIONS.md` was left UNTOUCHED by state-3 and must stay so at state-4 — do NOT re-create ADR-0159; append ADR-0160 only for a genuinely NEW decision.** **Parent 75 has NO `PLAN.md` and never will** — §6.2 step 1 abandons the oversize plan; do not "finish" it, and do not edit the FROZEN parent `75/SPEC.md`. `75.1/SPEC.md`, `75.1/PLAN.md` **and `75.1/PROGRESS.md`** all exist and `REVIEW.md` does not, so the next session is **75.1's §5 state-4 VERIFICATION** (`superpowers:verification-before-completion`) — see `## Next expected skill` and the SUB-PHASE 75.1 SCOPE block above. The implementation is DONE (all 13 tasks landed; the engine fix, all five consumer pins, fixture `0083` and the §C rewrite are committed); state-4 RUNS THE §7.5 GATE and must NOT re-implement, must NOT re-plan, must NOT re-adjudicate the split (settled on a re-derived ~1910 LoC; 75.1 itself re-derives to ~1210 LoC / ~12-14 tasks, comfortably inside the gate) nor re-measure PV-1..PV-10 from scratch (re-measured at the split and banked in ADR-0158 + `75.1/SPEC.md` with file:line citations that were accurate to the line). **Do NOT chain into state-5** (the code review is a separate session — ADR-0127: the context that wrote the code must not grade it). **Do NOT start 75.2 until 75.1 is `done`** — both of 75.2's fixtures assert POST-fix behavior and would be RED on a pre-75.1 tree. This sub-phase changes a SHARED matching engine read by FIVE subsystems, so the dominant §7.5 risk is gate **(b)** (pre-existing fixtures and tests), not gate (a) — though PV-9 enumerated the complete break set as FOUR in-process assertions across THREE tests in ONE file, with ZERO fixture and ZERO fuzz-corpus risk. The fix MUST be mode-scoped: `present_match` + `invert_match` is MEASURED PARITY and a naive uniform absent-DROP would break it. **The amend list is THREE tests, not two** (`matcher.rs:342` in addition to `:432` and `:489` — ADR-0158 C1). Live carry-forwards: **CF-72-2** (extended with the `contains_match` member), the NEW **CF-75-1**, CF-74-1/2/3/4/6 (**CF-74-5 CLOSED**), CF-73-1, N73-R2, M73-R1/M73-R2, M71-3, M71-6/7/8, M70-R4/R9, M69-A..I, CF-69-1/2/3/5, M68-1, M-1, CF-67-3/5/6/7, M74-3..M74-14, M74-16, M74-17/18/20/21/22/26/27/28/29, M74-30..M74-39, the older Minors + the HTTP-filters-family (1)-(4); **CF-72-1 is CONSUMED by the 75.1 state-3 landing** and **M74-31 by 75.2**, but neither is consumed until its sub-phase lands. **The mission is NOT complete** — the ROADMAP.md `## Feature Families` families (`ROADMAP.md:58`) remain largely unbuilt, so **no `stop` file**.
- **On the §5.1 rule and its ONE recorded exception (read `ADR-0127`).** The phase-66 state-6 close-out was run in the SAME session as its state-5 code-review, at **explicit human instruction**. That was a one-off, human-authorized override; `BOOTSTRAP_PROMPT.md`, `MISSION.md` and `SKILL_ROUTING.md` were **not modified**, and **§5.1 remains binding on every autonomous session.** ADR-0127 records why the 5→6 transition alone was safe to chain (the close-out changes no code, re-runs no gate, and consumes only disk facts) and why **0→1, 1→2, 2→3, 3→4 and 4→5 must never be chained** — each carries a genuine self-review hazard, since the context that wrote an artifact would be grading it. Per memory `closeout-and-pick-are-separate-sessions`, the next-phase pick is STILL a separate session. Do not read the git history as licence to chain.

> Historical Notes subsection for fully-closed sub-phase 67.3 (the `### Phase-67.3 §5 state-5 re-review` narrative — sub-phase 67.3 used a rename-in-place active-phase Notes subsection across its state-2..5 + §5.2 state-3 re-entry + state-4-re-verification + state-5 re-review arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5-re-review subsection remained in `## Notes` at close — relocated at the phase-67.3 §5 state-6 close-out when ROADMAP row `67.3` [and parent row `67`] flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 65 (the `### Phase-65 state-5 code-review` narrative — phase 65 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [each prior sub-state's own STATE.md wording superseded in place per-session, its four top-section blocks relocated at that session's own transition], so only this state-5 subsection remained in `## Notes` at close — relocated at the phase-65 state-6 close-out when ROADMAP row `65` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsection for fully-closed phase 52 (the `### Phase-52 state-5 code-review (active phase)` narrative — phase 52 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2/3/4 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-52 state-6 close-out when ROADMAP row `52` flipped `done`, together with the standing 05.1/05.4 future-reviewer note it carried) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 41 (the `### Phase-41 state-5 code-review (active phase)` narrative — phase 41 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the §6.2-recon PIVOT from `%REQ_WITHOUT_QUERY%` to `%ROUTE_NAME%` landed at state-2; the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-41 state-6 close-out when ROADMAP row `41` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 40 (the `### Phase-40 state-5 code-review (active phase)` narrative — phase 40 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-40 state-6 close-out when ROADMAP row `40` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 39 (the `### Phase-39 state-5 code-review (active phase)` narrative — phase 39 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-39 state-6 close-out when ROADMAP row `39` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
> Historical Notes subsection for fully-closed phase 38 (the `### Phase-38 state-2 PLAN-write (active phase)` narrative — phase 38 used a single active-phase Notes subsection for its whole arc; the state-3/4/5 sub-state narratives were superseded in the four-top-section archive blocks per-session, so only this state-2 subsection remained in `## Notes` at close — relocated at the phase-38 state-6 close-out when ROADMAP row `38` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) and for parent phase 25 (the `### Phase-25 state-1 brainstorm` pick + recon-finding narrative, relocated at the 25.2 state-6 close-out when parent `25` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 26 (the `### Phase-26 state-1 brainstorm` pivot/rejected-alternatives/key-scoping narrative, relocated at the phase-26 state-6 close-out when row `26` flipped to `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 27 (the `### Phase-27 state-1 brainstorm` / `### Phase-27 state-2 PLAN-write` / `### Phase-27 state-4 verification` narratives + the now-consumed `### Phase-27 carry-forwards` [M26-1..M26-8] block, relocated at the phase-27 state-6 close-out when row `27` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 28 (the `### Phase-28 state-1 brainstorm` / `### Phase-28 state-2 PLAN-write` / `### Phase-28 state-3 implementation + state-4 verification` / `### Phase-28 state-5 code review` narratives + the now-consumed `### Phase-28 carry-forwards` [M27-1..M27-3] block, relocated at the phase-28 state-6 close-out when row `28` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 29 (the now-consumed `### Phase-29 carry-forwards` [M28-1..M28-3] block + the `### Phase-29 state-1 brainstorm` / `### Phase-29 state-2 PLAN-write` / `### Phase-29 state-3 implementation` / `### Phase-29 state-4 verification` / `### Phase-29 state-5 code review` narratives, relocated at the phase-29 state-6 close-out when row `29` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 30 (the `### Phase-30 carry-forwards` [M29-1/M29-2, which fed phase 30 but were NOT consumed — they continue as phase-31 carry-forwards] block + the `### Phase-30 state-1 brainstorm` / `### Phase-30 state-2 PLAN-write` / `### Phase-30 state-3 implementation` / `### Phase-30 state-4 verification` / `### Phase-30 state-5 code review` narratives, relocated at the phase-30 state-6 close-out when row `30` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 31 (the `### Phase-31 carry-forwards` [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 — open Minors from the phase-30 REVIEW.md that fed phase 31 but were NOT consumed; they continue as carry-forwards for the next phase that touches the differential hash-sweep driver / the config parser] block + the `### Phase-31 state-1 brainstorm` / `### Phase-31 state-2 PLAN-write` / `### Phase-31 state-3 implementation` / `### Phase-31 state-4 verification` / `### Phase-31 state-5 code review` narratives, relocated at the phase-31 state-6 close-out when row `31` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 32 (the `### Phase-32 carry-forwards` block — the open Minors that fed phase 32 but were NOT consumed: the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3; they continue as carry-forwards for the future phase that touches their surface — re-listed in `## Next expected skill` above alongside the 6 new phase-32 REVIEW.md Minors M32-1…M32-6), relocated at the phase-32 state-6 close-out when row `32` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phases 33+34 (the `### Phase-33 carry-forwards` block — the active-phase carry-forwards Notes that lived through phases 33 and 34: the now-CONSUMED 6 phase-32 REVIEW.md Minors M32-1…M32-6 [folded + landed at the phase-33 state-3] + the "Other still-live carry-forwards" list [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3 + the HTTP-filters-family (1)-(4) — those still-live ones re-listed in `## Next expected skill` above alongside the new phase-34 Minors M34-1/M34-2/M34-3 + the phase-33 M33-1/M33-2]), relocated at the phase-34 state-6 close-out when row `34` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 35 (the `### Phase-35 state-1 brainstorm` / `### Phase-35 state-2 PLAN-write` / `### Phase-35 state-3 implementation` / `### Phase-35 state-4 verification` / `### Phase-35 state-5 code-review` narratives, relocated at the phase-35 state-6 close-out when ROADMAP row `35` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 36 (the `### Phase-36 state-5 code-review` narrative — phase 36 used a rename-in-place Notes discipline, so the state-1..4 sub-state narratives were superseded in place each session and only the four top-section blocks were relocated per-session — plus the now-CLOSED detailed M35-1 carry-forward bullet [CONSUMED by phase-36 F2], relocated at the phase-36 state-6 close-out when ROADMAP row `36` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 37 (the `### Phase-37 state-3 implementation` active-phase narrative, relocated at the phase-37 state-6 close-out when ROADMAP row `37` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### HTTP-filters-family carry-forwards (from the `25.2` REVIEW.md - NOT yet consumed; weigh whenever the HTTP-filters family is re-entered)

> These were never obligations on the xDS phase 26; they remain live for whenever an HTTP-filters-family phase resumes.

- **(1) [non-goal - architectural]** Over-limit request bodies are FULLY buffered before the 413 rejection (no streaming watermark). Documented deferred non-goal; differentially byte-identical to Envoy for the bounded fixture sizes. Revisit only if a streaming `decode_data` watermark path is ever planned.
- **(2) [doc precision]** The BEHAVIOR_CONTRACT 413-row "verified byte-exact against v1.33.0" phrasing - fixture `0033` is H1-only; the H2 over-limit path is covered by the in-process synth-decorator backstop, NOT differentially. Consider narrowing the phrasing if an H2 over-limit fixture is ever added.
- **(3) [coverage]** No standalone `== effective route limit` unit assertion (the boundary is exercised only via the over/under probes).
- **(4) [coverage]** No differential at-limit (`==`) probe in `0033` (within-limit `<` and over-limit `>` are both covered; the exact boundary is not differentially probed).
- _(2)-(4) are cheap polish, (1) is architectural and only relevant to a future streaming phase._
