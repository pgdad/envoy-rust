# envoy-rust Project State

> This file is the single source of truth for "what next." Cold-start reads it
> first after `MISSION.md`. It names the active phase directory and the
> next expected skill invocation. Any session mutating project state must end
> by updating this file.

## Active phase


**id:** **`54-accesslog-rcd-upstream-reset`** — PHASE 54 OPEN (state-4 verification COMPLETE — the §7.5 gate is GREEN on CI run `28481385288` @ `352c0c0`; the §5 state-5 code-review is NEXT). The EIGHTEENTH Observability access-log row + the SEVENTH differentially-witnessed `%RESPONSE_CODE_DETAILS%` value (the FIRST deterministic upstream-reset rcd): `upstream_reset_before_response_started{connection_termination}` is now SET on the pure-reset path (`hcm.rs:~1200`, guarded `!retry_limit_exceeded_for_log`) + the `UC` `%RESPONSE_FLAGS%` derivation MIGRATED from the phase-53 `reset_for_log` boolean to the now-unique rcd-match (`hcm.rs:1376`, the phase-50 `{overflow} => "UO"` precedent) — the redundant boolean RETIRED — and fixture `0062` is differentially GREEN on CI (byte-exact `{"rc":503,"rcd":"upstream_reset_before_response_started{connection_termination}","rf":"UC"}` on both proxies). **CONSUMED carry-forward M53-1 — CI-CONFIRMED** (fixture 0062 green on CI run `28481385288`); NO new `Op`/`AccessLogRecord` field/crate/dependency/fuzz-target/`ConfigError` variant/test-harness code. Scope locked by **ADR-0111**; the §6.1 split did NOT fire (**ADR-0112 stays reserved-but-UNFIRED**); §6.2 recon overturned NO §A-§G fact (**ADR-0113 stays reserved**). The next session is the **§5 state-5 code-review** (`superpowers:requesting-code-review`).
**slug:** `54-accesslog-rcd-upstream-reset`
**directory:** `docs/envoy-rust/phases/54-accesslog-rcd-upstream-reset/` (SPEC.md present + ✅ APPROVED; PLAN.md present + plan-reviewed ✅ APPROVED; PROGRESS.md present, extended THIS session with the state-4 verification record; REVIEW.md not yet created)

**status:** **PHASE 54 STATE-4 VERIFICATION COMPLETE (THIS session) — STATE-5 CODE-REVIEW NEXT.** Ran the §5 state-4 verification (`superpowers:verification-before-completion`) — evidence before claims. **Local §7.5 (a)-(e) subset:** rebuilt the DEBUG `envoy-bin` (memory `differential-harness-uses-debug-envoy-bin`), then `cargo build --workspace --all-targets` / `cargo clippy --workspace --all-targets --all-features -- -D warnings` / `cargo fmt --all -- --check` all clean (EXIT 0); `cargo test --workspace --no-fail-fast` surfaced exactly 4 LOCAL-RED failures, ALL documented pre-existing host-environment artifacts, NONE a regression from this phase's `hcm.rs` change: (1) `access_log_rcd_upstream_reset` (fixture `0062`, new) + (2) `access_log_rf_upstream_reset` (fixture `0061`, phase-53) — both the SAME host-bridge `UF`-vs-`UC` artifact (memory `differential-host-bridge-ip-192-168-65-2` — the upstream Envoy *container* can't reach the sibling accept-then-close backend over this host's Docker bridge, sees a connect-failure, while envoy-rust reaches it directly and sees the genuine reset); (3) `admin_config_dump_server_info` — the documented `192.168.65.2` bridge-IP cluster-stat artifact; (4) `xds_file_based_eds_fixture` — a Docker container-startup race under parallel `cargo test` load (the `differential-fixtures-flake-under-parallel-load` class). Every other test binary reported `test result: ok`. `cargo deny check` clean (advisories/bans/licenses/sources ok — same benign license-not-encountered warnings as every prior phase). **CI is AUTHORITATIVE for the Docker differential** (memory `envoy-rust-state4-ci-first-execution`): origin `main` already carried the state-3 commits from the prior session's push, and Linux CI run **`28481385288`** @ **`352c0c0`** had ALREADY completed `success` on both jobs (`build + test + lint`, `fuzz`) — no new push was needed. Pulled the full job log (`gh run view --log`) and confirmed directly: clippy clean, fmt clean, **fixture `0062` GREEN** (`test access_log_rcd_upstream_reset ... ok`, byte-identical `{"rc":503,"rcd":"upstream_reset_before_response_started{connection_termination}","rf":"UC"}` on both proxies — the host-bridge artifact does NOT occur on native Linux CI), fixture `0061` GREEN, `admin_config_dump_server_info` GREEN, `xds_file_based_eds_fixture` GREEN (confirming all 4 local failures were host artifacts, not regressions), **133 `test result: ok` / 0 `test result: FAILED`** across the whole workspace (one more green than phase 53's 132 — the net-new `0062`), `h2spec_pass_rate_gate ... ok` (no HTTP/2 codec change this phase, gate unmoved), `cargo deny check` clean, fuzz job `success` (existing 4 targets, 0 crashes, no new fuzz target this phase). Appended the full verification record to `docs/envoy-rust/phases/54-accesslog-rcd-upstream-reset/PROGRESS.md` (`## State-4 verification` section, quoting all command outputs per the phase-53 precedent format). **§7.5 gate (a)-(e) MET on CI; (f) REVIEW.md is the next session's job.** **CONSUMES M53-1 — NOW CI-CONFIRMED** (no longer pending); NO new carry-forward. M53-2 + M53-3 (PRESERVED by the §A guard, ALSO covered by an in-process backstop) + M50-C + M48-2 + M42-1 + M45-1 + the `DC`/retry-budget-overflow slices of M45-2 + older stay live; NONE blocks. **No ADR fired** — verification overturned no PLAN/SPEC fact (**ADR-0112**/**ADR-0113** stay reserved-but-UNFIRED). `#![forbid(unsafe_code)]` holds; NO new `Op`/`AccessLogRecord` field/crate/dependency/fuzz-target/`ConfigError` variant/test-harness code. **DECISIONS.md ledger head: ADR-0111** (unchanged this session). ADR-0014 in force; ADR-0028 open; ADR-0049 governs config-validity. Per §5.1, STOP after state-4 — the next session is the **§5 state-5 code-review** (`superpowers:requesting-code-review`).

> Historical `## Active phase` status narratives — every superseded `**status:**` paragraph (all closed phases incl. the phase-51 state-3/4/5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Next expected skill


Per `BOOTSTRAP_PROMPT.md` §5 state 5 + `SKILL_ROUTING.md`: phase 54 (`54-accesslog-rcd-upstream-reset`) has `PLAN.md` Tasks 1-5 implemented RED→GREEN→commit + `PROGRESS.md` authored + the state-4 verification COMPLETE this session (§7.5 gate (a)-(e) GREEN on CI run `28481385288` @ `352c0c0` — fixture `0062` + ALL `0001`-`0061` simultaneously green, 133/133 `test result: ok`, h2spec unmoved, deny check clean, fuzz job green). The next session is the **state-5 code-review** (`superpowers:requesting-code-review`): dispatch a fresh `superpowers:code-reviewer` subagent (no prior session history) to review the ENTIRE phase-54 diff (the 5 task commits `e12825e`..`c5a3a77` + the state-3 STATE-advance commit + THIS state-4 STATE-advance commit) against `SPEC.md` + `PLAN.md` + `PROGRESS.md` + `BEHAVIOR_CONTRACT.md` §E + the project coding standards, and write `REVIEW.md`. If issues surface, re-enter at **state-3** (NOT state-4) per §5.2. Do NOT chain past state-5 to the state-6 close-out — that is a separate session (memory `closeout-and-pick-are-separate-sessions`).

**State-4 verification DONE (THIS session):** local §7.5 (a)-(e) subset clean except 4 documented host-artifact LOCAL-REDs (fixtures `0061`/`0062` host-bridge UF/UC mismatch, `admin_config_dump_server_info` bridge-IP mismatch, `xds_file_based_eds_fixture` Docker-startup race — all pre-existing flake classes, none a regression); CI run `28481385288` @ `352c0c0` confirms ALL of these GREEN on native Linux (133 `test result: ok`, 0 FAILED), plus clippy/fmt/deny/h2spec/fuzz all clean. Full verification record appended to `PROGRESS.md`.

**Open carry-forward Minors (fold into the phase that next touches each surface; NONE blocks):** **M53-2** (BEHAVIOR_CONTRACT `:36/:289/:296/:387` "reset 503" reads global but the H2 reset path still synth-502 — add an "(H1)" qualifier; the §E edit touches the rcd/flag rows but M53-2 concerns the H2 status path, left live) + **M53-3** (the un-reconned `retry_on:reset`+exhausted URX+UC body-wording mismatch — PRESERVED by the §A `!retry_limit_exceeded_for_log` guard, NOW also covered by an in-process backstop) + **M50-C** (request-budget `max_requests` overflow UO/rcd) + **M48-2** + **M42-1** + **M45-1** (H2 failure-path details + an H2 access-log differential driver) + the `DC`/retry-budget-overflow slices of **M45-2** + **M40-1** + **M39-1/M39-2** + **M38-1/M38-2** + **CF-39-1** + M37-* + M36-* + M34-* + M33-* + the empty-`metadata_match` doc-comment + M29-*/M30-* + the phase-31 cosmetics + the HTTP-filters-family (1)-(4). **[M53-1 CONSUMED by phase 54 — CI-CONFIRMED at state-4 (run `28481385288`); M52-1 by phase 53; M52-2 + the `UF` slice of M45-2 + M51-1 by phase 52; the `URX` slice by phase 51.]**

> Historical `## Next expected skill` narratives — every superseded next-skill pointer (all closed phases incl. the phase-37 state-1..5 sub-state pointers) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

## Last commit


**Phase-54 state-4 verification — run the full §7.5 gate, confirm CI GREEN, append the verification record + advance STATE, push + confirm CI (THIS commit):** the §5 state-4 verification (`superpowers:verification-before-completion`). Rebuilt the DEBUG `envoy-bin`, ran `cargo build --workspace --all-targets` / `cargo clippy --workspace --all-targets --all-features -- -D warnings` / `cargo fmt --all -- --check` (all clean) / `cargo test --workspace --no-fail-fast` (4 documented host-artifact LOCAL-REDs, no regression) / `cargo deny check` (clean) locally, then pulled the authoritative Linux CI run **`28481385288`** @ **`352c0c0`** (already `success` on both jobs from the prior session's push — no new push needed) and confirmed via the full job log: fixture `0062` GREEN byte-exact, fixture `0061` + `admin_config_dump_server_info` + `xds_file_based_eds_fixture` all GREEN (confirming the 4 local failures were host artifacts), 133/133 `test result: ok`, h2spec unmoved, deny check clean, fuzz job green. Appended the verification record to `PROGRESS.md`. **THIS commit** = `PROGRESS.md` + `STATE.md` + `STATE_HISTORY.md` + `next-prompt.txt` (docs-only, advancing to state-5). **CONSUMES M53-1 — NOW CI-CONFIRMED; NO new carry-forward.** **No ADR fired** — verification overturned no fact; ADR-0112 + ADR-0113 stay reserved-but-UNFIRED. `#![forbid(unsafe_code)]` holds; NO new `Op`/`AccessLogRecord` field/crate/dependency/fuzz-target/`ConfigError` variant/test-harness code. **DECISIONS.md ledger head: ADR-0111** (unchanged). ADR-0014 in force; ADR-0028 open. The next session is the **§5 state-5 code-review** (`superpowers:requesting-code-review`).

> Historical `## Last commit` narratives — every superseded last-commit block (all closed phases incl. the phase-37 state-1..5 sub-state commits) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Last updated


2026-07-01 (phase-54 **STATE-4 VERIFICATION COMPLETE — STATE-5 CODE-REVIEW NEXT.** Ran the §5 state-4 verification (`superpowers:verification-before-completion`). Local §7.5 (a)-(e) subset clean except 4 documented host-artifact LOCAL-REDs — fixtures `0061`/`0062` (host-bridge UF/UC mismatch), `admin_config_dump_server_info` (bridge-IP mismatch), `xds_file_based_eds_fixture` (Docker-startup race) — all pre-existing flake classes, none a regression from this phase's `hcm.rs` change. Authoritative Linux CI run `28481385288` @ `352c0c0` (already `success` on both jobs from the prior session's push) confirmed via the full job log: fixture `0062` GREEN byte-exact (`{"rc":503,"rcd":"upstream_reset_before_response_started{connection_termination}","rf":"UC"}` on both proxies), fixture `0061` + `admin_config_dump_server_info` + `xds_file_based_eds_fixture` all GREEN (confirming the local failures were host artifacts), 133/133 `test result: ok`, h2spec unmoved, deny check clean, fuzz job green. Appended the full verification record to `PROGRESS.md`. `#![forbid(unsafe_code)]` holds. CONSUMES M53-1 — NOW CI-CONFIRMED; NO new carry-forward. **No ADR fired** — ADR-0112 + ADR-0113 stay reserved-but-UNFIRED. **DECISIONS.md ledger head: ADR-0111** (unchanged; count 111). ADR-0014 in force; ADR-0028 open. The next session is the **§5 state-5 code-review** (`superpowers:requesting-code-review`).)

> Historical `## Last updated` notes — every superseded last-updated note (all closed phases incl. the phase-37 state-1..5 sub-state notes) — are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


## Notes

> Historical Notes subsections for fully-closed phases 00-07 (ADR-numbering notes, per-phase rollovers, ADR ledgers, and the earlier-phase-carryforward + phase-00-deferral snapshots) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 42 (the `### Phase-42 state-5 code-review (active phase)` narrative — phase 42 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-42 state-6 close-out when ROADMAP row `42` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 43 (the `### Phase-43 state-5 code-review (active phase)` narrative — phase 43 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-43 state-6 close-out when ROADMAP row `43` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 44 (the `### Phase-44 state-5 code-review (active phase)` narrative — phase 44 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-44 state-6 close-out when ROADMAP row `44` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 45 (the `### Phase-45 state-5 code-review (active phase)` narrative — phase 45 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-45 state-6 close-out when ROADMAP row `45` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 46 (the `### Phase-46 state-5 code-review (active phase)` narrative — phase 46 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-46 state-6 close-out when ROADMAP row `46` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 47 (the `### Phase-47 state-5 code-review (active phase)` narrative — phase 47 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-47 state-6 close-out when ROADMAP row `47` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 48 (the `### Phase-48 state-5 code-review (active phase)` narrative — phase 48 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-48 state-6 close-out when ROADMAP row `48` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 49 (the `### Phase-49 state-5 code-review (active phase)` narrative — phase 49 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-49 state-6 close-out when ROADMAP row `49` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 50 (the `### Phase-50 state-3 implementation (active phase)` narrative — phase 50 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-50 state-6 close-out when ROADMAP row `50` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 51 (the `### Phase-51 state-3 implementation (active phase)` narrative — phase 51 kept a single active-phase Notes subsection at "state-3 implementation" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-3 subsection remained in `## Notes` at close — relocated at the phase-51 state-6 close-out when ROADMAP row `51` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 53 (the `### Phase-53 state-1 brainstorm` narrative — phase 53 kept a single active-phase Notes subsection at "state-1 brainstorm" across its state-3/4/5 arc [the sub-state narratives superseded in the four top-section blocks per-session; the active-phase subsection was NOT renamed-in-place], so only this state-1 subsection remained in `## Notes` at close — relocated at the phase-53 state-6 close-out when ROADMAP row `53` flipped `done`, together with the inline superseded state-3 narrative) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### Phase-54 state-1 brainstorm

- **Pick:** the deterministic upstream-reset `%RESPONSE_CODE_DETAILS%` string `upstream_reset_before_response_started{connection_termination}` (CONSUMES M53-1). The SEVENTH witnessed rcd value + the FIRST deterministic upstream-reset rcd. Chosen over the SEVENTH `%RESPONSE_FLAGS%` value `DC` (timing-dependent downstream disconnect — hard to drive deterministically), M50-C's `max_requests` UO (re-witnesses a seen flag), the M53-2/M45-1 H2-driver harness phase (heavier), `%REQUEST_HEADERS_BYTES%` (recon-uncertain byte accounting), and gRPC ALS/OTLP/tracing/stats-sinks/tap (new infra) — per the §6 cheapest-strong-differential discipline.
- **state-0 recon [make-or-break gate — PASSED]:** live `envoyproxy/envoy:v1.33.0` (digest `sha256:56da5afd…`, host Docker `-p`; the accept-then-close backend run as a SIBLING container on a shared docker bridge — REQUIRED on this dev host, memory `differential-host-bridge-ip-192-168-65-2` + `state0-recon-backend-sibling-container`), `{rc,rcd,rf}` json_format, `GET /` → `{"rc":503,"rcd":"upstream_reset_before_response_started{connection_termination}","rf":"UC"}`, byte-stable across 3 probes + a container restart. The brace content `connection_termination` is a FIXED reset-reason enum (NOT OS text — CONTRAST the connect-failure rcd's `{remote_connection_failure|delayed_connect_error:_Connection_refused}` the same arm rendered, M45-2), structurally identical to the phase-50 `{overflow}` rcd (fixture 0058) → DETERMINISTIC + witnessable.
- **Scope (ADR-0111):** §A set the reset rcd on the pure-reset path (`hcm.rs:~1200`, guarded `!retry_limit_exceeded_for_log` — preserves M53-3; replay-safe per ADR-0044) + §B migrate `UC` to the rcd-match (`:1373`, the `{overflow}=>UO` precedent) + DELETE the redundant `reset_for_log` boolean + §C fixture `0062` (clone of 0061 + `rcd:"%RESPONSE_CODE_DETAILS%"`, reusing the `{{CLOSE_BACKEND_PORT}}` harness) + §D a thin `run_fixture` differential test + §E BEHAVIOR_CONTRACT updates (rcd row + the `UC` flag-derivation INVERSION + the stale `:1343`→`:1366` anchor) + §F the `reset_for_log` sweep + §G an in-process backstop (the M53-3 negative case REQUIRED). NO new `Op`/field/crate/dependency/fuzz-target/`ConfigError` variant/test-harness code. H1-only.
- **Load-bearing invariant:** all `0001`-`0061` byte-identical — the §A rcd-set fires on no existing logged path, and the §B `UC`-via-rcd migration is output-equivalent to the `UC`-via-boolean derive on the reset path (fixture 0061's `rf:"UC"` unchanged — spec-review CONFIRMED). **0062 is a backend-spawning fixture → expect LOCAL-RED on this dev host, GREEN on CI — CI AUTHORITATIVE** (the phase-53/0061 precedent).
- **SPEC spec-reviewed** by a fresh subagent → ✅ APPROVED (0 Critical / 0 Important / 3 Minor: M1 anchor refresh, M2 contract-clause inversion, M3 backstop negative-case REQUIRED — all folded into §E/§G). §6.1 split projected NOT to fire (**ADR-0112 reserved-but-unfired**); §6.2 reconciliation **ADR-0113 reserved**.

### Phase-54 state-2 PLAN-write

- **PLAN-VERIFY (SPEC §3, all confirmed against the live `crates/envoy-http1/src/hcm.rs` — no §A-§G fact overturned → ADR-0113 NOT fired):** §3.1 the §A set-site is the post-loop reconciliation region `:1200` (where phase 53 set `reset_for_log`); it runs AFTER the in-loop per-attempt `via_upstream` write `:1055-1062` (reset = `endpoint:Some`+`outcome:Some(Reset)` → the `else "via_upstream"` arm) and BEFORE the record-build derive `:1366` → the rcd-set OVERRIDES `via_upstream`. The `!retry_limit_exceeded_for_log` guard reads the boolean set at `:1181`. §3.1 §B: 0061 logs `{rc,rf}`-only → the rcd-set does not change its line; the flag still renders `UC` (now via the rcd-match) → byte-identical. §3.2: the ONLY `{{CLOSE_BACKEND_PORT}}` pure-reset fixture is 0061; rcd-logging fixtures 0050-0059 route to non-reset paths → §A fires on no existing fixture. §3.4: `reset_for_log` refs only at `:873`/`:1200`/`:1358`/`:1362`/`:1370` + the backstop test doc-comment — phase-53-local, no other consumer. §3.5: §G positive = extend `h1_upstream_reset_access_log_carries_uc_flag` (`:7528`); the M53-3 negative case is drivable via `retry_on:"reset"` (→ `is_retriable(_,Reset)=true`, `bootstrap.rs:1970`/`:1998`).
- **PLAN form:** 5 TDD tasks, ~50-90 LoC `crates/` change → §6.1 split did NOT fire (**ADR-0112 reserved-but-UNFIRED**). TDD ordering: Task 1 writes §A UNGUARDED (keeps all existing tests green — the URX rcd test `:7373` is `retry_on:"5xx"` whose final outcome is a Response, not a Reset, so unguarded §A does not fire there); Task 2 adds the `!retry_limit_exceeded_for_log` guard via a genuine red→green on the new M53-3 negative test.
- **Plan-reviewed** by a fresh `superpowers:code-reviewer` subagent → ✅ APPROVED (0 Critical / 0 Important / 4 Minor, all anticipatory/self-corrected: the braced rcd-match arm fmt-width [folded into Task 1 Step 4], the `:1366` post-change anchor drift [PLAN self-corrects], the `%RESPONSE_CODE_DETAILS%` default-absent tail [PLAN says verify current text], the URX test citation [assertion line vs fn header]).

### Phase-54 state-3 implementation

- **Execution model:** `superpowers:subagent-driven-development` — one fresh implementer subagent per `PLAN.md` task, a dedicated task-reviewer subagent per task (spec-compliance + code-quality verdicts), and a progress ledger (`.superpowers/sdd/progress.md`, git-ignored scratch) tracking task completion. All 5 task reviews ✅ **Approved** — 0 Critical / 0 Important across every task; only cosmetic/process Minors surfaced (a stale in-comment line-number reference inherited from the PLAN brief text, trivial test-boilerplate duplication matching existing file convention, a collapsed cross-reference comment, brief-wording tension over the archival-doc exception) — none required rework.
- **Task 1** (`e12825e`): §A set `response_code_details_for_log = Some("upstream_reset_before_response_started{connection_termination}")` on the pure-reset final-outcome path, UNGUARDED (Task 2 adds the guard); §B added the braced `Some("upstream_reset_before_response_started{connection_termination}") => { "UC" }` rcd-match arm (single-line form would exceed 100 cols — kept braced per `cargo fmt`) + deleted the `else if reset_for_log { "UC" }` branch + the `reset_for_log` declaration; extended the positive backstop `h1_upstream_reset_access_log_carries_uc_flag`. RED (pre-change `{"rc":503,"rcd":"via_upstream","rf":"UC"}`) → GREEN; full `envoy-http1` suite 150/150.
- **Task 2** (`1aa024f`): added `&& !retry_limit_exceeded_for_log` to the §A condition; new backstop `h1_retry_exhausted_reset_keeps_via_upstream_rcd_and_urx_flag` (`retry_on:"reset"`, `num_retries:1`, an always-resetting backend) — genuine RED (unguarded §A wrongly set the deterministic rcd even retry-exhausted) → GREEN (`{"rc":503,"rcd":"via_upstream","rf":"URX"}`); suite 151/151.
- **Task 3** (`c222ab4`): fixture `0062-accesslog-rcd-upstream-reset` — a structural clone of `0061` (verified near-byte-exact by the task reviewer's direct `diff`) + `rcd:"%RESPONSE_CODE_DETAILS%"` inserted between `rc`/`rf`; the thin differential test `access_log_rcd_upstream_reset.rs`. `cargo test -p differential --test access_log_rcd_upstream_reset --no-run` compiles + discovers cleanly. NOT run to completion locally (backend-spawning → expected LOCAL-RED, memory `differential-host-bridge-ip-192-168-65-2`); CI-authoritative at state-4.
- **Task 4** (`9764c5b`): §E BEHAVIOR_CONTRACT — **(M2)** the `UC` per-flag clause fully INVERTED (no coexistence of the old/new claims — verified by the task reviewer); **(M1)** the stale `hcm.rs:1343` anchor refreshed to the re-derived-current `:1376` (NOT the brief's suggested `:1366` — Tasks 1-2's edits shifted the derive-site line further; the task reviewer independently re-confirmed `:1376` via `grep -n "response_flags: if retry_limit_exceeded_for_log"`); the `%RESPONSE_CODE_DETAILS%` row extended with the new set-site + fixture-0062 rationale.
- **Task 5** (`c5a3a77`): the residual `reset_for_log` sweep — retargeted 5 LIVE-prose occurrences (`hcm.rs` ×2, `BEHAVIOR_CONTRACT.md` ×2, fixture-0062's README ×2, the differential test's doc-comment ×1) that Tasks 1/4 had introduced naming the retired identifier in "was retired" footnotes; deliberately left ARCHIVAL material untouched (`ROADMAP.md`, `STATE_HISTORY.md`, `DECISIONS.md`, phase-53's own `{SPEC,PLAN,REVIEW,PROGRESS}.md`, this phase's own `SPEC.md`/`PLAN.md`, `STATE.md`, fixture `0061`'s README) — the task reviewer independently re-derived this archival/live boundary via `git log`/`grep` and confirmed it correct (fixture READMEs and closed-phase docs are never revisited by later phases in this codebase). Local `cargo build`/`clippy -D warnings`/`fmt --check`/`test -p envoy-http1` (151/151)/`test -p envoy-accesslog` (98/98) all clean.
- **PROGRESS.md authored** (`docs/envoy-rust/phases/54-accesslog-rcd-upstream-reset/PROGRESS.md`) — the per-task RED→GREEN execution log, quoting each task's TDD evidence + task-review verdict.
- **NOT run this session** (the state-4 gate): the Docker differential (`0062` + the full `0001`-`0061` regression set on CI), `cargo test --workspace`, `cargo deny check`, h2spec. The final whole-branch code review (per `superpowers:subagent-driven-development`'s process — and per the project's own §5 state-5) is ALSO deferred — it runs at state-5, AFTER state-4 verification, not this session.

### Phase-54 state-4 verification

- **Execution model:** `superpowers:verification-before-completion` — evidence before claims. Rebuilt the DEBUG `envoy-bin` first (memory `differential-harness-uses-debug-envoy-bin`).
- **Local §7.5 (a)-(e) subset:** `cargo build --workspace --all-targets` / `cargo clippy --workspace --all-targets --all-features -- -D warnings` / `cargo fmt --all -- --check` all clean (EXIT 0). `cargo test --workspace` (default fail-fast) halted at the first differential failure; re-ran with `--no-fail-fast` for the complete local picture: exactly 4 failures, ALL documented pre-existing host-environment artifacts, NONE a regression from this phase's `hcm.rs` change — (1) `access_log_rcd_upstream_reset` (fixture `0062`, new) and (2) `access_log_rf_upstream_reset` (fixture `0061`, phase-53) both show the SAME host-bridge `UF`-vs-`UC` mismatch (memory `differential-host-bridge-ip-192-168-65-2` — the upstream Envoy *container* can't reach the sibling accept-then-close backend over this host's Docker bridge and sees a connect-failure, while envoy-rust reaches it directly and sees the genuine post-connect reset); (3) `admin_config_dump_server_info` shows the documented `192.168.65.2` bridge-IP cluster-stat artifact; (4) `xds_file_based_eds_fixture` shows a Docker container-startup race under parallel `cargo test` load (the `differential-fixtures-flake-under-parallel-load` class). Every other test binary reported `test result: ok`. `cargo deny check` clean (advisories/bans/licenses/sources ok — the same benign license-not-encountered warnings as every prior phase).
- **CI is AUTHORITATIVE for the Docker differential** (memory `envoy-rust-state4-ci-first-execution`): `git fetch origin main` showed origin already carried the phase-54 state-3 commits from the prior session's push (`e12825e`..`352c0c0`) — no new push was needed this session. `gh run list` showed Linux CI run **`28481385288`** @ **`352c0c0`** already `completed`/`success` on both jobs (`build + test + lint`, `fuzz`). Pulled the FULL job log (`gh run view 28481385288 --log`) and confirmed directly, not just from the job-summary conclusion: clippy ran clean, `cargo fmt --all -- --check` ran clean, **fixture `0062` GREEN** (`test access_log_rcd_upstream_reset ... ok` — both proxies emit the byte-identical `{"rc":503,"rcd":"upstream_reset_before_response_started{connection_termination}","rf":"UC"}`; the host-bridge artifact does NOT occur on native Linux CI, both proxies reach the sibling backend), fixture `0061` GREEN, `admin_config_dump_server_info` GREEN, `xds_file_based_eds_fixture` GREEN (cross-confirming all 4 local failures were host artifacts, not regressions), **133 `test result: ok` / 0 `test result: FAILED`** across the whole workspace (one more green than phase 53's 132 — the net-new `0062`), `h2spec_pass_rate_gate ... ok` (no HTTP/2 codec change this phase — gate unmoved), `cargo deny check` clean on CI too, and the `fuzz` job `success` (the existing 4 targets — `parse_bootstrap`/`jwt_parse`/`cdn_loop_parse`/`accesslog_format_parse` — 0 crashes; no new fuzz target this phase, `ci.yml` unchanged).
- **Verification record appended** to `docs/envoy-rust/phases/54-accesslog-rcd-upstream-reset/PROGRESS.md` (`## State-4 verification` section) quoting every command's output, mirroring the phase-53 state-4 record's format/structure.
- **Disposition:** §7.5 gate (a)-(e) MET on CI (the authoritative environment); (f) `REVIEW.md` is the next session's job (state-5 code-review). No ADR fired — verification overturned no PLAN/SPEC fact (ADR-0112/ADR-0113 stay reserved-but-UNFIRED). No re-implementation needed. **CONSUMES M53-1 — now CI-CONFIRMED** (no longer "pending"). Per §5.1, STOP here — advance STATE to the §5 state-5 code-review, do not chain into it this session.


### Doctrine reminders

- Any deviation from the state machine requires `superpowers:systematic-debugging` before proceeding — see §1 Step E of `BOOTSTRAP_PROMPT.md`.
- Consult `docs/envoy-rust/SKILL_ROUTING.md` for the full phase lifecycle state machine.
- `BOOTSTRAP_PROMPT.md` §5.1: one state per session; do not chain states. Phase 54 (`54-accesslog-rcd-upstream-reset`) is OPEN — the §7.5 gate is GREEN on CI (run `28481385288` @ `352c0c0`), ROADMAP row `54` `in-progress`. The next session is the **§5 state-5 code-review** (`superpowers:requesting-code-review`): dispatch a fresh reviewer subagent, write `REVIEW.md`, then STOP (do NOT chain to the state-6 close-out).

> Historical Notes subsection for fully-closed phase 52 (the `### Phase-52 state-5 code-review (active phase)` narrative — phase 52 used a rename-in-place active-phase Notes subsection across its state-3/4/5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session; state-1/2/3/4 detail relocated earlier], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-52 state-6 close-out when ROADMAP row `52` flipped `done`, together with the standing 05.1/05.4 future-reviewer note it carried) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 41 (the `### Phase-41 state-5 code-review (active phase)` narrative — phase 41 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the §6.2-recon PIVOT from `%REQ_WITHOUT_QUERY%` to `%ROUTE_NAME%` landed at state-2; the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-41 state-6 close-out when ROADMAP row `41` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 40 (the `### Phase-40 state-5 code-review (active phase)` narrative — phase 40 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-40 state-6 close-out when ROADMAP row `40` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 39 (the `### Phase-39 state-5 code-review (active phase)` narrative — phase 39 used a rename-in-place active-phase Notes subsection across its state-1..5 arc [the sub-state narratives superseded in place each session; the four top-section blocks relocated per-session], so only the state-5 subsection remained in `## Notes` at close — relocated at the phase-39 state-6 close-out when ROADMAP row `39` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.
> Historical Notes subsection for fully-closed phase 38 (the `### Phase-38 state-2 PLAN-write (active phase)` narrative — phase 38 used a single active-phase Notes subsection for its whole arc; the state-3/4/5 sub-state narratives were superseded in the four-top-section archive blocks per-session, so only this state-2 subsection remained in `## Notes` at close — relocated at the phase-38 state-6 close-out when ROADMAP row `38` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.


> Historical Notes subsections for fully-closed phases 05.4 / 08 / 09 / 10 (brainstorm, split, PLAN-write, execution-arc, rollovers, and ADR-ledger narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phases 11–21 (brainstorm / split / PLAN-write / execution-arc + verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 22 (brainstorm / PLAN-write / execution-arc + state-4 verification / code-review / rollovers narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 23 (state-1 brainstorm / state-2 PLAN-write / state-3 execution arc / state-4 verification gate / state-5 code-review narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 24 (state-1 brainstorm / state-2 PLAN-write narratives) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed sub-phase 25.1 (state-2 PLAN-write / state-3 implementation / state-4 verification / state-5 code-review narratives) and for parent phase 25 (the `### Phase-25 state-1 brainstorm` pick + recon-finding narrative, relocated at the 25.2 state-6 close-out when parent `25` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 26 (the `### Phase-26 state-1 brainstorm` pivot/rejected-alternatives/key-scoping narrative, relocated at the phase-26 state-6 close-out when row `26` flipped to `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 27 (the `### Phase-27 state-1 brainstorm` / `### Phase-27 state-2 PLAN-write` / `### Phase-27 state-4 verification` narratives + the now-consumed `### Phase-27 carry-forwards` [M26-1..M26-8] block, relocated at the phase-27 state-6 close-out when row `27` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 28 (the `### Phase-28 state-1 brainstorm` / `### Phase-28 state-2 PLAN-write` / `### Phase-28 state-3 implementation + state-4 verification` / `### Phase-28 state-5 code review` narratives + the now-consumed `### Phase-28 carry-forwards` [M27-1..M27-3] block, relocated at the phase-28 state-6 close-out when row `28` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 29 (the now-consumed `### Phase-29 carry-forwards` [M28-1..M28-3] block + the `### Phase-29 state-1 brainstorm` / `### Phase-29 state-2 PLAN-write` / `### Phase-29 state-3 implementation` / `### Phase-29 state-4 verification` / `### Phase-29 state-5 code review` narratives, relocated at the phase-29 state-6 close-out when row `29` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 30 (the `### Phase-30 carry-forwards` [M29-1/M29-2, which fed phase 30 but were NOT consumed — they continue as phase-31 carry-forwards] block + the `### Phase-30 state-1 brainstorm` / `### Phase-30 state-2 PLAN-write` / `### Phase-30 state-3 implementation` / `### Phase-30 state-4 verification` / `### Phase-30 state-5 code review` narratives, relocated at the phase-30 state-6 close-out when row `30` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 31 (the `### Phase-31 carry-forwards` [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 — open Minors from the phase-30 REVIEW.md that fed phase 31 but were NOT consumed; they continue as carry-forwards for the next phase that touches the differential hash-sweep driver / the config parser] block + the `### Phase-31 state-1 brainstorm` / `### Phase-31 state-2 PLAN-write` / `### Phase-31 state-3 implementation` / `### Phase-31 state-4 verification` / `### Phase-31 state-5 code review` narratives, relocated at the phase-31 state-6 close-out when row `31` flipped to `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 32 (the `### Phase-32 carry-forwards` block — the open Minors that fed phase 32 but were NOT consumed: the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3; they continue as carry-forwards for the future phase that touches their surface — re-listed in `## Next expected skill` above alongside the 6 new phase-32 REVIEW.md Minors M32-1…M32-6), relocated at the phase-32 state-6 close-out when row `32` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phases 33+34 (the `### Phase-33 carry-forwards` block — the active-phase carry-forwards Notes that lived through phases 33 and 34: the now-CONSUMED 6 phase-32 REVIEW.md Minors M32-1…M32-6 [folded + landed at the phase-33 state-3] + the "Other still-live carry-forwards" list [the empty-`metadata_match`→fallback doc-comment + M29-1/M29-2 + M30-1 + M30-2 + the phase-31 cosmetic Minors M-2/M-3 + the HTTP-filters-family (1)-(4) — those still-live ones re-listed in `## Next expected skill` above alongside the new phase-34 Minors M34-1/M34-2/M34-3 + the phase-33 M33-1/M33-2]), relocated at the phase-34 state-6 close-out when row `34` flipped `done`, is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsections for fully-closed phase 35 (the `### Phase-35 state-1 brainstorm` / `### Phase-35 state-2 PLAN-write` / `### Phase-35 state-3 implementation` / `### Phase-35 state-4 verification` / `### Phase-35 state-5 code-review` narratives, relocated at the phase-35 state-6 close-out when ROADMAP row `35` flipped `done`) are preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 36 (the `### Phase-36 state-5 code-review` narrative — phase 36 used a rename-in-place Notes discipline, so the state-1..4 sub-state narratives were superseded in place each session and only the four top-section blocks were relocated per-session — plus the now-CLOSED detailed M35-1 carry-forward bullet [CONSUMED by phase-36 F2], relocated at the phase-36 state-6 close-out when ROADMAP row `36` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

> Historical Notes subsection for fully-closed phase 37 (the `### Phase-37 state-3 implementation` active-phase narrative, relocated at the phase-37 state-6 close-out when ROADMAP row `37` flipped `done`) is preserved verbatim in [STATE_HISTORY.md](STATE_HISTORY.md) per ADR-0035.

### HTTP-filters-family carry-forwards (from the `25.2` REVIEW.md - NOT yet consumed; weigh whenever the HTTP-filters family is re-entered)

> These were never obligations on the xDS phase 26; they remain live for whenever an HTTP-filters-family phase resumes.

- **(1) [non-goal - architectural]** Over-limit request bodies are FULLY buffered before the 413 rejection (no streaming watermark). Documented deferred non-goal; differentially byte-identical to Envoy for the bounded fixture sizes. Revisit only if a streaming `decode_data` watermark path is ever planned.
- **(2) [doc precision]** The BEHAVIOR_CONTRACT 413-row "verified byte-exact against v1.33.0" phrasing - fixture `0033` is H1-only; the H2 over-limit path is covered by the in-process synth-decorator backstop, NOT differentially. Consider narrowing the phrasing if an H2 over-limit fixture is ever added.
- **(3) [coverage]** No standalone `== effective route limit` unit assertion (the boundary is exercised only via the over/under probes).
- **(4) [coverage]** No differential at-limit (`==`) probe in `0033` (within-limit `<` and over-limit `>` are both covered; the exact boundary is not differentially probed).
- _(2)-(4) are cheap polish, (1) is architectural and only relevant to a future streaming phase._
