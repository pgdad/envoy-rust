# Phase 65 — `65-accesslog-h2-rcd-upstream-reset` — SPEC

**Pick (ADR-0122):** differentially WITNESS the **deterministic H2 upstream-reset `%RESPONSE_CODE_DETAILS%` string — `upstream_reset_before_response_started{connection_termination}`** — BYTE-EXACT on the **H2 upstream-disconnect-before-headers 503** path (the IDENTICAL trigger phase 64 built: an H2-upstream `STRICT_DNS`+`V4_ONLY` cluster whose single endpoint is the SPAWNED `Http2CloseBackend` that completes a genuine H2 handshake, accepts the request stream, then drops it WITHOUT responding — an implicit stream reset; NO `retry_policy`, NO `circuit_breakers`), by (i) **setting `response_code_details_for_log_h2 = Some("upstream_reset_before_response_started{connection_termination}")` on the pure-reset final-outcome path** (today that path's rcd is the shared `via_upstream` — set in-loop at `crates/envoy-http2/src/hcm.rs:757`, the `endpoint:Some`/`outcome:Some(Reset)` arm — overridden post-loop at `hcm.rs:896`, the exact site where phase 64 set `reset_for_log_h2`), guarded `!retry_limit_exceeded_for_log_h2`, and (ii) **migrating the H2 `UC` `%RESPONSE_FLAGS%` derivation from the `reset_for_log_h2` boolean to the now-unique rcd string** (`hcm.rs:1101` rcd-match gains `Some("upstream_reset_before_response_started{connection_termination}") => "UC"`, EXACTLY mirroring the existing `{overflow} => "UO"` arm) — which makes the phase-64 `reset_for_log_h2` boolean **redundant and removable** (it was introduced ONLY as a placeholder because the H2 reset rcd was indistinguishably `via_upstream`; making the rcd unique completes the intent the phase-64 SPEC §4 / BEHAVIOR_CONTRACT earmarked as **M64-1**). This phase **CONSUMES carry-forward M64-1**. It is the H2 analogue of phase 54 (which did EXACTLY this for the H1 side, consuming M53-1), completing the H1/H2 parity for the deterministic upstream-reset rcd. NO new `Op`/`AccessLogRecord` field/crate/dependency/`ConfigError` variant, and NO new test-harness code (the §C fixture reuses phase 64's `--close-before-response` + `Http2CloseBackend` + the `{{H2_CLOSE_BACKEND_PORT}}` launch arm verbatim).

> **State-0 recon — this session (code-read + inherited live-Envoy finding):**
>
> **Finding 1 (empirical target — inherited from phase 64 Finding 1, same pinned image, same path, re-cited).** Live `envoyproxy/envoy:v1.33.0` (digest `sha256:56da5afd…`, confirmed matching `ENVOY_TARGET.md`, sibling-container topology per memory `state0-recon-backend-sibling-container`) on this EXACT trigger (an H2C listener → `STRICT_DNS`+`V4_ONLY` H2-upstream cluster → a handshake-then-reset backend) emitted, byte-stable across 3 repeats AND a container restart: `{"method":"GET","proto":"HTTP/2","rc":503,"rcd":"upstream_reset_before_response_started{connection_termination}","rf":"UC"}`. The `rcd` is a single, clean, **DETERMINISTIC** string — the brace content `connection_termination` is a FIXED reset-reason enum, NOT OS-derived strerror/errno text (CONTRAST the connect-failure rcd, whose recon renders non-deterministic OS text — M45-2). This is structurally IDENTICAL to the phase-50/58 `{overflow}` rcd (`upstream_reset_before_response_started{<reason>}`) already witnessed byte-exact on BOTH H1 (fixture 0058) and H2 (fixture 0066), AND to the H1 `{connection_termination}` rcd witnessed byte-exact at phase 54 (fixture 0062) — so a fixed-enum reset-reason rcd is a PROVEN-deterministic surface. Fixture 0069 (phase 64) already logs `{method,proto,rc,rf}` on this path; this phase adds `rcd` and witnesses the string. **PLAN-VERIFY (§3.3) re-confirms the live disposition** per the standing discipline of not blindly trusting a prior session's recon — but the value is a fixed enum, already witnessed byte-exact on three sibling surfaces (H1 `{connection_termination}` @0062, H1/H2 `{overflow}` @0058/0066), so the empirical risk is minimal.
>
> **Finding 2 (code-read — envoy-rust's H2 reset rcd is WRONG today, and `UC` is boolean-derived).** The H2 reset path reaches the caller-loop `if let Some(endpoint)` block (`hcm.rs:745`-`768`): a reset has `endpoint:Some` (the connection was established) and `outcome:Some(Reset)` (NOT `None`), so the `attempt.outcome.is_none()` discriminator (added phase 58 for `{overflow}`) evaluates false → it sets `response_code_details_for_log_h2 = Some("via_upstream")`. Post-loop (`hcm.rs:889`-`897`), phase 64 set `reset_for_log_h2 = matches!(final_outcome_h2, Some(Reset))`, and the `%RESPONSE_FLAGS%` derive (`hcm.rs:1087`-`1107`) renders `UC` via the `else if reset_for_log_h2` branch (checked THIRD, after `URX`/`UF`), leaving the rcd as the shared `via_upstream`. So on this path envoy-rust TODAY emits `rcd:"via_upstream"` (WRONG — Envoy emits `upstream_reset_before_response_started{connection_termination}`) while rendering the correct `rf:"UC"` from the boolean. This phase corrects the rcd and re-derives `UC` from it — the EXACT shape phase 54 applied to the H1 side.
>
> **Finding 3 (the migration RETIRES the boolean, mirroring phase 54).** `reset_for_log_h2` has exactly 5 sites, all phase-64-local (confirmed via `grep -n "reset_for_log_h2"`): the decl (`hcm.rs:577`), the post-loop set (`:896`), the `finalize_h2_stream` call-site arg (`:944`), the `finalize_h2_stream` parameter (`:1000`), and the derive branch (`:1091`). Once the rcd is unique, the `UC`-via-boolean derive becomes redundant dead-weight — this phase migrates `UC` to rcd-derivation (the `UO`/`{overflow}` precedent) and REMOVES all 5 sites, a net simplification identical to phase 54's retirement of H1's `reset_for_log`. `connect_failure_for_log_h2` (its rcd carries OS text) and `retry_limit_exceeded_for_log_h2` (a real completing 503) KEEP their booleans — only `UC`'s placeholder boolean is retired, exactly as on H1.

## §1 — Why this pick

The candidate menu (`STATE.md`, left by the phase-64 close-out) weighed: **M64-1** (this pick — the H2-side deterministic `UC` rcd, explicitly deferred at phase 64's minimum-viable witness); **M64-2**/**M64-3** (cosmetic fold-ins); **M57-1** (a trivial `content-length` header fix, unrelated surface); **M53-2** (a trivial BEHAVIOR_CONTRACT doc-only qualifier); the `DC` downstream-disconnect flag (timing-dependent, rejected at every prior consideration through ADR-0121); and heavier family pivots (gRPC ALS/OTLP/tracing, CDS/LDS hot-reload, non-deterministic LB policies, the HTTP-filters-family carry-forwards). **M64-1 is the cheapest-strong-differential leaf** and the natural close of the H2 upstream-reset witness opened at phase 64:

- **It is the DIRECT H2 analogue of phase 54.** Phase 53 witnessed the H1 `UC` flag `{rc,rf}`-only (deferring the deterministic rcd as M53-1); phase 54 (ADR-0111) then consumed M53-1 by setting the deterministic reset rcd and migrating H1's `UC` onto it. Phase 64 (ADR-0121) did the phase-53 move for H2 (witnessed the H2 `UC` flag, deferred the H2 rcd as **M64-1**). This phase does the phase-54 move for H2 — the exact sequel, closing the last H1/H2 asymmetry in the upstream-reset witness set.
- **The trigger is already fully built — zero new harness.** Phase 64 (ADR-0121) built the H2-aware close-before-response upstream: the `http2-echo-server --close-before-response` mode, the `Http2CloseBackend` harness struct (`tests/differential/src/backend.rs:491`), and the marker-driven launch arm (`tests/differential/src/lib.rs:3344`-`3406` — `{{H2_CLOSE_BACKEND_PORT}}` auto-spawns `Http2CloseBackend`). Fixture 0070 reuses ALL of it via the same markers — so this phase is even cheaper than phase 64 (which had to BUILD the backend), exactly as phase 54 was cheaper than phase 53.
- **State-0 recon-CONFIRMED + structurally PROVEN.** The rcd `upstream_reset_before_response_started{connection_termination}` is byte-stable + DETERMINISTIC (fixed reset-reason enum, no OS text — inherited from phase 64 Finding 1, and independently witnessed byte-exact on the H1 sibling at phase 54 fixture 0062). The `upstream_reset_before_response_started{<fixed-enum>}` family is proven byte-exact on four sibling surfaces (H1 `{overflow}` @0058, H2 `{overflow}` @0066, H1 `{connection_termination}` @0062, and this path's own `rf`-only witness @0069).
- **Completing phase 64 simplifies the code.** Phase 64 introduced the `reset_for_log_h2` boolean ONLY because the H2 reset rcd was indistinguishably `via_upstream` — so the `UC` flag could not key on the rcd (UNLIKE `UO`, which keys on `{overflow}`). Making the rcd unique lets `UC` derive 1:1 from it (the `UO` precedent) and **removes** the boolean — a net simplification, not just an addition (identical to phase 54's H1 outcome). `URX` [61] and `UF` [63] KEEP their booleans: `URX`'s rcd is a real completing 503 (`via_upstream`), `UF`'s carries non-deterministic OS text — so those two H2 flags remain boolean-derived. Only `UC`'s placeholder boolean is retired.
- **The `DC` flag remains genuinely harder** (timing-dependent, rejected at every prior consideration through ADR-0121); nothing this session changes that assessment.

## §2 — Scope (minimum-viable, ADR-0122)

**Set the deterministic reset rcd on the pure-reset path + migrate the H2 `UC` flag onto it (retiring the redundant `reset_for_log_h2` boolean) + a NEW `{rc,rcd,rf,...}` fixture reusing the phase-64 close-before-response backend:**

- **§A — set the reset rcd on the pure-reset final-outcome path.** At the post-loop reconciliation site (`hcm.rs:896`, where phase 64 set `reset_for_log_h2 = matches!(final_outcome_h2, Some(AttemptOutcome::Reset))`), set `response_code_details_for_log_h2 = Some("upstream_reset_before_response_started{connection_termination}".to_owned())` when the FINAL attempt's `AttemptOutcome` is `Reset` **AND `!retry_limit_exceeded_for_log_h2`**. The `!retry_limit_exceeded_for_log_h2` guard preserves the H2 M53-3-equivalent edge: in the un-recon'd `retry_on:reset`-exhausted case (retry-limit-exceeded with a final reset), the rcd STAYS the shared `via_upstream` and the flag renders `URX` (the derive's `URX` branch is checked first) — exactly as today. The set OVERRIDES the `via_upstream` written in-loop at `hcm.rs:757` for the reset path; on a reset-retried-to-success (`final_outcome_h2 = Some(Response)`) the `matches!` fails → the rcd stays `via_upstream` → renders neither the new rcd nor `UC` (replay-safe, mirroring the H1 phase-54 §A guarantee). NO change to the synth status (already 503 since phase 64's `synth_h2_reset()`), the synth body (not differentially compared — §3.3), the response headers, `x-envoy-attempt-count`, the retry counters, or `upstream_rq_5xx`.
- **§B — derive `UC` from the now-unique rcd + retire the `reset_for_log_h2` boolean.** Today `hcm.rs:1087`-`1107` reads `if retry_limit_exceeded_for_log_h2 { "URX" } else if connect_failure_for_log_h2 { "UF" } else if reset_for_log_h2 { "UC" } else { match response_code_details_for_log_h2.as_deref() { Some("route_not_found") => "NR", Some("no_healthy_upstream") => "UH", Some("upstream_reset_before_response_started{overflow}") => "UO", _ => "-" } }`. This phase (i) ADDS `Some("upstream_reset_before_response_started{connection_termination}") => "UC"` to the rcd-match (mirroring the `{overflow} => "UO"` arm), (ii) DELETES the `} else if reset_for_log_h2 { "UC" }` branch (`:1091`-`1099`, incl. its phase-64 comment), and (iii) DELETES the `reset_for_log_h2` declaration (`:577`), its post-loop set (`:896`, replaced by §A's rcd-set at the same site) + the phase-64 comment block (`:889`-`897`), its `finalize_h2_stream` call-site arg (`:944`), and its `finalize_h2_stream` parameter + doc comment (`:997`-`1000`). Match-arm order among the four rcd strings is irrelevant (all distinct). Fixture 0069 (`{method,proto,rc,rf}`-only) stays **byte-identical**: the reset path's rcd is now `{connection_termination}` (set in §A) → the rcd-match yields `UC` → `rf:"UC"` exactly as before. The `URX`/`UF` booleans + the `NR`/`UH`/`UO` arms are unchanged. The borrow-before-move discipline (`.as_deref()` shared borrow ends before the owned `String` moves into `response_code_details:` at `:1131`) is unchanged.
- **§C — the fixture `0070-accesslog-h2-rcd-upstream-reset`** (paired `envoy.yaml`/`envoy-rust.yaml`/`expectations.yaml`/`README.md`): a structural clone of fixture `0069` — an H2C listener (`codec_type: HTTP2`, `http2_protocol_options: {}`) → a single route `/` to a `STRICT_DNS`+`V4_ONLY` H2-upstream cluster (`typed_extension_protocol_options`→`explicit_http_config.http2_protocol_options: {}`) whose single endpoint is the **same `Http2CloseBackend`** (the `{{BACKEND_HOST}}` + `{{H2_CLOSE_BACKEND_PORT}}` markers, which auto-spawn `Http2CloseBackend` via the existing `lib.rs:3344`-`3406` launch arm — NO new harness code), NO `circuit_breakers`, NO `retry_policy`. The ONLY change vs 0069: the HCM `json_format` adds `rcd: "%RESPONSE_CODE_DETAILS%"` → logs `{rc, rf, method, proto, rcd}`. The two side configs differ ONLY by the documented per-side deltas 0069 already uses (bind address, admin block, the `{{BACKEND_HOST}}` per-side value — `host.docker.internal` reference-side, `127.0.0.1` subject-side). ONE probe: `GET /`, `expected_status: 503`, cross-proxy-equal whole-line `{"rc":503,"rf":"UC","method":"GET","proto":"HTTP/2","rcd":"upstream_reset_before_response_started{connection_termination}"}` (exact key order = 0069's order + `rcd` appended; PLAN §3.7 confirms the emitted key order matches on both sides).
- **§D — the differential test** `tests/differential/tests/access_log_h2_rcd_upstream_reset.rs`: a thin `differential::run_fixture(&dir)` wrapper (a structural clone of `access_log_h2_uc_upstream_reset.rs` → `0069`), driven by the EXISTING `Driver::Http2AccessLogByteExact` with a `GET /` probe + `expected_status: 503`. The marker-driven backend launch is already centralized in `run_fixture` (`{{H2_CLOSE_BACKEND_PORT}}` → `Http2CloseBackend`); the test body spawns nothing. Per the phase-53/54/64 precedent, expect this fixture to be **LOCAL-RED on this dev host** (a backend-spawning fixture → subject to the host-bridge-IP differential flake, memory `differential-host-bridge-ip-192-168-65-2`) and **GREEN on CI** — CI is authoritative.
- **§E — BEHAVIOR_CONTRACT updates.** (1) The `%RESPONSE_CODE_DETAILS%` row: add the H2 reset rcd witness `upstream_reset_before_response_started{connection_termination}` set on the H2 pure-reset path (fixture `0070`), noting its brace content is a FIXED reset-reason enum (like `{overflow}`), so byte-exact deterministic — this is the H2 sibling of the H1 witness at fixture 0062. (2) The `%RESPONSE_FLAGS%` row: update the H2 `UC` per-flag equivalence — H2 `UC` is now **derived 1:1 from `%RESPONSE_CODE_DETAILS%` = `upstream_reset_before_response_started{connection_termination}`** (the `UO`/`{overflow}` pattern, matching H1's own `UC`), NOT from the (now-removed) `reset_for_log_h2` boolean; the phase-64 boolean discriminator was RETIRED; cite fixture `0070`. Update carry-forward **M64-1** to CONSUMED. The witnessed-rcd-derived H2 flag set is now `{NR, UH, UO, UC}` + `{URX, UF}` (boolean-derived) — full parity with H1's own post-phase-54 derivation split. **(mirror phase-54 spec-review M2)** the existing H2-`UC` clause explicitly reads "derived from a `reset_for_log_h2` boolean … NOT 1:1 from rcd" — that ENTIRE clause MUST be INVERTED, not appended-to, so the row is not left internally contradictory. **(mirror phase-54 spec-review M1)** while editing, refresh any stale H2-derive-site anchor in the touched clauses (`hcm.rs:1091`/derive region) to the post-change line if cheap.
- **§F — update the affected unit tests + comments (the `reset_for_log_h2` sweep).** Re-grep `crates/envoy-http2/src/` for every reference to `reset_for_log_h2` and the phase-64 comments describing the `UC`-via-boolean derivation (`:889`-`897`, `:997`-`1000`, `:1091`-`1099`); remove/retarget them to the rcd-derivation. The phase-64 in-process backstop `h2_upstream_reset_access_log_carries_uc_flag` (`hcm.rs:4963`) STAYS green (UC still renders, now via the rcd) — §G extends it. PLAN produces the exhaustive list (§3.4). Leave BACKWARD-LOOKING historical narrative comments (e.g. phase-57/63/64-authored "as of phase NN" prose) verbatim per the D-3.4/D-3.5 no-retroactive-rewrite convention; correct only ACTIVE-state prose describing today's mechanism.
- **§G — in-process backstop.** EXTEND (or sibling) the phase-64 `h2_upstream_reset_access_log_carries_uc_flag` backstop (`hcm.rs:4963`) to ALSO assert the pure-reset final outcome yields `response_code_details = Some("upstream_reset_before_response_started{connection_termination}")` (not just `response_flags = "UC"`). **(mirror phase-54 spec-review M3 — REQUIRED, not optional)** the H2 M53-3-equivalent negative case (retry-limit-exceeded-reset → rcd stays `via_upstream`, flag `URX`) MUST be asserted in the backstop: it is the single most error-prone line in §A (the `!retry_limit_exceeded_for_log_h2` guard) and the differential `0070` cannot exercise the retry-exhausted-reset path. PLAN decides the minimal viable form (§3.5) — extend-in-place vs a new sibling test — either is acceptable; the differential `0070` is authoritative for the pure-reset path, the backstop guards the in-process derivation + the negative edge.
- **§H — no new fuzz target.** `%RESPONSE_CODE_DETAILS%`/`%RESPONSE_FLAGS%` are existing operators already covered by `accesslog_format_parse` / `parse_bootstrap`; this phase adds NO new operator/grammar, only a new VALUE on an existing field. SKIP (no new fuzz target, `ci.yml` unchanged), matching the phase-54 precedent.

**Load-bearing additivity invariant:** all `0001`-`0069` stay byte-identical. The §A rcd-set fires ONLY on the pure-reset final-outcome path (`endpoint:Some` + `outcome:Some(Reset)`, `!retry_limit_exceeded`), which NO existing fixture `0001`-`0069` that logs `%RESPONSE_CODE_DETAILS%` drives (re-grep §3.2 — the ONLY reset fixture, 0069, logs no rcd; every other rcd-logging fixture drives a non-reset disposition), and the §B `UC`-via-rcd migration is output-equivalent to the `UC`-via-boolean derive on the reset path (fixture 0069's `rf:"UC"` unchanged — §3.1). PLAN (§3.2) re-greps every existing fixture that logs rcd OR rf to confirm none newly diverges.

## §3 — PLAN-VERIFY items (state-2 §6.2)

1. **The §A set-site + the §B migration output-equivalence (PLAN finalizes + re-greps).** Confirm `hcm.rs:896` (the post-loop site where phase 64 set `reset_for_log_h2`) is the correct site to set the rcd, that it runs AFTER the in-loop `via_upstream` set at `:757` and BEFORE the record-build derive at `:1087`, and that the set OVERRIDES `via_upstream` for the reset path. Confirm the `!retry_limit_exceeded_for_log_h2` guard reproduces today's behavior for BOTH the pure-reset case (rcd→`{connection_termination}`, flag `UC`) AND the retry-exhausted-reset case (rcd stays `via_upstream`, flag `URX`). Confirm fixture 0069's `rf:"UC"` is byte-identical post-migration.
2. **Additive byte-preservation (re-derived against the §A/§B edits).** Re-grep ALL differential fixtures that log `%RESPONSE_CODE_DETAILS%` AND those that log `%RESPONSE_FLAGS%` (incl. the H2 set `0064`-`0069`); confirm NONE drives a pure-reset final outcome that would newly set the rcd or render a changed flag; confirm `0001`-`0069` byte-identical post-change.
3. **Re-confirm the live-Envoy disposition** on this exact H2 trigger (inherited from phase 64 Finding 1; the value is a fixed enum, but re-verify per the standing PLAN-VERIFY discipline of not blindly trusting a prior session's recon) — `rcd:"upstream_reset_before_response_started{connection_termination}"`, `rf:"UC"`, status `503`, deterministic.
4. **Affected `reset_for_log_h2` reference + comment sweep (§F) — PLAN produces the exhaustive list.** Re-grep `crates/envoy-http2/src/` for every `reset_for_log_h2` reference (the 5 sites: decl `:577`, post-loop set `:896`, call-site arg `:944`, parameter `:1000`, derive branch `:1091`) + the phase-64 comments; retarget each to the rcd-derivation. Confirm no OTHER consumer exists (it is phase-64-local).
5. **§G backstop form (PLAN decides).** Confirm whether the phase-64 `h2_upstream_reset_access_log_carries_uc_flag` backstop can be extended in-place to assert the rcd `{connection_termination}` + the retry-exhausted-reset negative case, or whether a new sibling test is cleaner. Keep proportionate — but the negative case is REQUIRED (§G).
6. **Decide the §6.1 split** — count actual tasks/LoC once scoped in detail; projected NOT to fire (see §6).
7. **Fixture naming/number + json key order** — confirm `0070` is still the next-free fixture number at PLAN-write time (standing concurrency guard), and that the emitted `json_format` key order is identical on both proxies (append `rcd` to 0069's key set).

## §4 — Rejected / deferred

- **Amending fixture 0069 to add `rcd` (vs a NEW fixture 0070)** — rejected to keep 0069 a clean `{method,proto,rc,rf}`-only byte-identical regression witness AND to follow the one-fixture-per-witness convention of the rcd/flag sub-family (the H1 sibling phase 54 likewise added a NEW fixture 0062 rather than amending 0061). 0070 reuses 0069's entire backend harness via the markers, so the marginal cost is one fixture dir + a thin test wrapper.
- **Keeping the `reset_for_log_h2` boolean (purely-additive: set the rcd, leave the flag derive)** — rejected: once the rcd is unique, the boolean is redundant dead-weight (a reviewer Minor); migrating `UC` to rcd-derivation (the `UO` precedent, and phase 54's own H1 choice) is the cleaner completion, and is output-equivalent on fixture 0069 (§3.1). The `!retry_limit_exceeded_for_log_h2` guard keeps the retry-exhausted-reset edge unchanged.
- **The retry-exhausted-reset combination** (both `retry_limit_exceeded_for_log_h2` AND a final reset) — un-recon'd on live Envoy (mirrors H1's own un-recon'd M53-3 combination); the §A `!retry_limit_exceeded_for_log_h2` guard renders `URX` deterministically regardless, so envoy-rust's behavior is well-defined even though the EXACT Envoy disposition for this combination has not been empirically verified. Deferred, matching H1's standing disposition.
- **A hard RST (SO_LINGER-style abrupt reset) vs the graceful stream-reset this path uses** (`drop(send_response)`, an implicit `RST_STREAM`) — un-recon'd whether Envoy renders a DIFFERENT reset-reason brace on a hard RST; out of scope (the graceful implicit-reset path is the clean, deterministic, phase-64-verified `{connection_termination}`).
- **The `DC` downstream-disconnect flag** (M45-2 slice) — timing-dependent, hard to drive deterministically; rejected at every prior consideration (ADR-0102 through ADR-0121); no new information this session changes that.
- **M64-2 (stale `hcm.rs:236` comment) / M64-3 (idle-probe-connection inefficiency) / M57-1 (`content-length` header omission) / M53-2 (BEHAVIOR_CONTRACT "(H1)" qualifier)** — trivial standalone fixes, deliberately NOT bundled into this phase (this phase does not touch those surfaces); left as cheap future picks or fold-ins whenever a phase next touches each surface. (M64-2 lives in the SAME file this phase edits; PLAN §3.4 MAY opportunistically fold it in if the comment region is being touched anyway — otherwise it stays a live carry-forward.)
- **A family pivot to xDS CDS/LDS hot-reload or LB `least_request`/`random`** — both viable, both carry larger up-front design cost than this session's recon-narrowed fix; deferred to a future pick, not rejected outright.

## §5 — Acceptance (§7.5, re-run at state-4)

(a) fixture `0070` green (cross-proxy-equal status `503` + whole-line `{...,"rcd":"upstream_reset_before_response_started{connection_termination}",...}` on the H2 upstream-disconnect-before-headers path) + (b) all `0001`-`0069` green simultaneously (additive — the §2 load-bearing invariant, re-verified §3.2) + (c) h2spec ≥95% (NO H2 codec/framing change) + (d) no new fuzz target (SPEC §H) + (e) build/clippy/fmt/test/deny clean (including the §B boolean removal + the §F/§G test/comment updates) + (f) `REVIEW.md` approved. **Fixture `0070` is a backend-spawning fixture → expect LOCAL-RED on this dev host (the `differential-host-bridge-ip-192-168-65-2` flake, per the phase-53/54/64 precedent) and GREEN on CI — CI is AUTHORITATIVE.** `#![forbid(unsafe_code)]` holds; NO new crate/dependency/`Op`/`AccessLogRecord` field/`ConfigError` variant/test-harness code. The `crates/` changes are the §A rcd-set + the §B rcd-match arm/boolean-removal + the §F sweep; the test changes are the §C fixture (reusing phase 64's harness) + the §D differential test + the §G backstop; the doc changes are the §E BEHAVIOR_CONTRACT updates. **NO `%RESPONSE_FLAGS%` value change** (the witnessed H2 flag stays `UC`; only its DERIVATION moves from the boolean to the rcd). **If §3.2's re-grep finds an existing fixture would newly diverge, or §3.3's re-verification overturns the rcd**, a §6.2 reconciliation ADR fires and §A-§G are re-scoped — PLAN scopes that contingency if it triggers.

## §6 — Process

- **§6.1 split — projected NOT to fire.** LIGHTER than phase 64 (which had to BUILD the H2-aware backend) — this phase reuses that backend verbatim and is a pure rcd-set + flag-migration + boolean-removal + fixture + thin test + backstop-extension + BEHAVIOR_CONTRACT edit. Projected ~6-9 tasks / ~60-140 LoC — comparable to phase 54 (H1's own rcd phase, ~5-8 tasks/~50-120 LoC) plus a marginally larger fixture (the H2C-listener + H2-upstream-cluster shape vs H1's plain listener) — well under the ~25-task/~1500-LoC gate. PLAN-write (§3.6) makes the final call.
- **§6.2 reconciliation — NOT expected to fire.** The state-0 recon inherits phase 64's byte-exact live-Envoy finding (same pinned image, same path) AND the rcd is structurally proven deterministic on four sibling surfaces (H1 `{connection_termination}` @0062, H1/H2 `{overflow}` @0058/0066, this path's `rf`-only witness @0069). **ADR-0123 reserved** for the state-2 PLAN-write §6.2 reconciliation — lands inline ONLY if the PLAN recon overturns a §A-§G fact (e.g. the §B migration is NOT output-equivalent on 0069, the §A rcd-set does not override `via_upstream` at `:896`, or the `!retry_limit_exceeded_for_log_h2` guard fails to reproduce the retry-exhausted-reset edge). Reclaimed by the next NEW phase pick if it lapses (the standing lapsed-reservation convention).
- **Carry-forward consumption:** this phase CONSUMES **M64-1** (witness the deterministic H2 `UC` rcd `upstream_reset_before_response_started{connection_termination}`). NO new carry-forward (the §B migration RETIRES the `reset_for_log_h2` boolean — a simplification). M64-2 + M64-3 + M57-1 + M55-1 + M53-2 + M53-3 + M48-2 + M42-1 + the `DC`/retry-budget-overflow slices of M45-2 + the phase-58 candidate carry-forward (H2 request-budget arm's own differential fixture) + M40-1 + M39-1/M39-2 + M38-1/M38-2 + CF-39-1 + the HTTP-filters-family (1)-(4) + older stay live; NONE blocks.
- Pick + §A-§H ground-truth locked by **ADR-0122** (reclaims the number reserved-but-UNFIRED for the phase-64 §6.1 split that did NOT fire — the lapsed-reservation convention; confirmed via a fresh `grep -oE '^## ADR-[0-9]+' DECISIONS.md` re-scan this session: highest fired entry `0121`; no sibling draft claims `0122`).
- **Phase number `65`** — confirmed via `ls docs/envoy-rust/phases/` this session: `64` is the highest ROADMAP-rowed phase; the sibling H1-perf-work directories `59`/`60`/`61-perf`/`62` remain disjoint, no-ROADMAP-row, other-sessions' concern.

_Scope locked by ADR-0122. The state-2 PLAN-write (`superpowers:writing-plans`) re-confirms §3's PLAN-VERIFY items (the §A set-site + guard, the §B migration form + 0069 output-equivalence, the additivity re-grep, the live-Envoy re-confirm, the `reset_for_log_h2` sweep, the §G backstop form incl. the required negative case, the §6.1 split decision, the fixture-`0070` numbering + json key order) and authors `PLAN.md`. The state-3 implementation is the session after._
