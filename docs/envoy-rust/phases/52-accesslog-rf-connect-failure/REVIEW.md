# Phase 52 — `52-accesslog-rf-connect-failure` — REVIEW

**State-5 code-review** (`superpowers:requesting-code-review` → a FRESH `superpowers:code-reviewer` subagent, independent of the implementation session's context). **Verdict: ✅ APPROVED — Ready to merge: YES.**

- **Range reviewed:** `git diff d831393..0a5eb12` (base = phase-52 state-2 PLAN commit `d831393`; code starts at `2d42490`). Implementation commits: `2d42490` (T1 §A 502→503 on the three connect-failure arms + 3 warns + 3 tests), `4d6774e` (T2 §A `connect_failure_for_log` discriminator + §B `"UF"` derive branch + §F in-process backstop), `4ecbea9` (T3 fixture `0060`), `a4ea26d` (T4 differential test `access_log_rf_connect_failure.rs`), `0453c1e` (T5 BEHAVIOR_CONTRACT §E + M51-1 anchor fold), `badee9f` (T6 §G stale-502 comment/doc sweep). The later commits `b847726`/`d10fdc4`/`0a5eb12` are PROGRESS.md/STATE/STATE_HISTORY housekeeping — correctly NON-load-bearing for the code surface (CI run `28401920739` was green on the implementation HEAD `d10fdc4`).
- **Reviewed against:** `SPEC.md` (§2 §A-§G, §3 PLAN-VERIFY, §5 acceptance), `PLAN.md` (T1-T8), `PROGRESS.md`, `BEHAVIOR_CONTRACT.md` §E, and project doctrine (additive/byte-identical `0001`-`0059` invariant, `#![forbid(unsafe_code)]`, NO new `Op`/`AccessLogRecord` field/crate/dependency/fuzz-target/`ConfigError` variant, discriminator soundness, NO rcd change, borrow discipline, test-not-mock).

## Outcome

| Severity | Count | Blocking? |
|----------|-------|-----------|
| Critical | 0 | — |
| Important | 0 | — |
| Minor | 2 | No (1 doc-accuracy one-word fix outside §G scope → new carry-forward M52-2; 1 informational un-witnessed edge → M45-2 retry-budget slice) |

**No code changes required this phase.** The reviewer independently verified every load-bearing claim against the tree (not on trust); the requester independently re-verified the `final_outcome` capture, the derive ordering, and the 502→503 sweep completeness before recording this verdict.

## Strengths (reviewer-confirmed, independently re-verified against the tree)

- **The `final_outcome` capture is sound on every loop-exit path (invariant #2 / SPEC §3.1).** `hcm.rs:1082` (`final_outcome = attempt.outcome;`) sits at the top of the loop body, after `run_attempt` returns and before the retry decision. The loop has exactly one `break` (`:1131`) and two `continue` paths (`:1106`, `:1118`); every path through the body assigns `final_outcome` first, so on loop exit it always holds the breaking (final) attempt's outcome. No break/early-return bypasses the capture. Mirrors the established `final_retriable` pattern (`:1086`) verbatim — the SPEC-mandated mechanism (the loop `break` carries only `(response, upstream_response)`, NOT `attempt.outcome`, so the capture is load-bearing, not redundant).
- **`ConnectFailure` outcome is provably 1:1 with the connect-failure synth-503.** `hcm.rs:626-631` builds `AttemptResult { response, outcome: Some(AttemptOutcome::ConnectFailure), .. }` where `response` is the `synth_status(503, …)` from the three acquire arms. So `final_outcome == Some(ConnectFailure)` ⟺ the surfaced response IS the connect-failure synth-503. `connect_failure_for_log` can never be true when the final response is a success (`Response`), a reset (`Reset`/502), an overflow (`None`/503), or no-healthy-upstream (`None`/503). A connect-failure retried to success has `final_outcome = Some(Response)` → not flagged (SPEC §A(ii) retry-to-success exclusion holds).
- **502→503 sweep complete and correctly scoped (SPEC §A(i) / §G).** All three `AcquireOutcome::ConnectFailure` arms (`:501`/`:530`/`:547`), all three runtime `tracing::warn!` strings (`:499`/`:528`/`:545` → "returning 503"), and the rippled comments are flipped. `grep -nE '502|Bad Gateway'` over `hcm.rs` confirms the only surviving `502`/`Bad Gateway` are the reset/send-fail arm (`:615` warn + `:618` synth, `AttemptOutcome::Reset`) and the comments that CORRECTLY describe that arm as `reset synth-502`/`send-fail-502`. The deferred M52-1 boundary (§4) is respected exactly. The three flipped tests now assert `HTTP/1.1 503 Service Unavailable` (`:3312`, `:6798`, `:6840`).
- **Derive ordering correct (SPEC §B).** `hcm.rs:1343-1354`: `if retry_limit_exceeded_for_log { "URX" } else if connect_failure_for_log { "UF" } else { match rcd … }`. URX correctly precedes UF, so a (deferred, un-recon'd) retry-exhausted-connect-failure that set both booleans renders `URX` deterministically. The `else`-match arms (`NR`/`UH`/`UO`) are unreachable with `connect_failure_for_log` set (its rcd is `via_upstream` → `_ => "-"`), so fixtures `0056`-`0059` stay byte-identical.
- **`upstream_rq_5xx` non-interaction holds (SPEC §3.2).** The L5 gate (`hcm.rs:1143`) is `completing_upstream_response && final_response.status / 100 == 5` — a connect-failure synth sets `completing_upstream_response = false`, so the gate stays false at 503 exactly as at 502. The 502→503 change does NOT newly tick `upstream_rq_5xx`; the co-located `rq_5xx 0` test assertions correctly kept their values.
- **Tests exercise the real path, not mocks (invariant #5 / SPEC §F).** `h1_connect_failure_access_log_carries_uf_flag` and the three flipped assertions drive the real `serve` path against a `127.0.0.1:1` kernel-refused endpoint with a real `FileSink` + json_format, asserting the full line `{"rc":503,"rf":"UF"}\n`. Fail-first state recorded in PROGRESS (`{"rc":503,"rf":"-"}` pre-derive-branch). All four pass locally + on CI.
- **Fixture `0060` is a clean clone of the 0058 discipline (SPEC §C).** `diff envoy.yaml envoy-rust.yaml` → exactly the three documented deltas (admin line, `0.0.0.0` vs `127.0.0.1` bind, log path). No `circuit_breakers`/`retry_policy`, literal dead `127.0.0.1:1` endpoint, no backend spawned — genuinely forces a DIALED kernel-refused connect (the correct contrast with 0058's pre-connect pending-gate reject). `json_format` logs `{rc, rf}` only (rcd OMITTED — the non-deterministic transport-failure reason). The driver `access_log_rf_connect_failure.rs` needs no `needs_health_aware_backend`/`--per-path` wiring (the dead-endpoint pattern).
- **M51-1 anchor fold accurate (SPEC §E(3)).** All `hcm.rs:1225` occurrences gone; the contract now anchors the NR/UH/UO/URX/UF rules to `hcm.rs:1343` — the ACTUAL live derive line (PROGRESS correctly overrode the PLAN's stale `:1305` after the Task-2 comment block shifted the derive +38). The `upstream_rq_total`/`upstream_rq_5xx`/`downstream_rq_5xx` prose updated to synth-503 while preserving the "count unaffected" semantics.

## Minors (advisory — disposition per `superpowers:receiving-code-review`)

1. **`tests/fixtures/0019-upstream-active-health-check/envoy.yaml:18` — a stale contrast comment.** The line reads `…vs synth_status (502 connect-fail) at the connect-fail arm`; after this phase the connect-fail arm synthesizes **503**, so the parenthetical is now factually wrong. Harmless (0019 exercises the no-healthy-upstream 503 path, not connect-failure; the line is only a contrast reference) and outside the declared §G sweep scope (`crates/` + `BEHAVIOR_CONTRACT.md`). The 0060 README mentions of "502" are all correct "502→503 correction" context, NOT stale; the historical phase SPECs (`docs/.../phases/07`,`15`) that mention `connect-fail-502` are append-only point-in-time records and must NOT be retroactively edited. → **NO action this state (one-state-per-session; the verdict is APPROVED, this is a non-blocking one-word doc fix). Tracked as new carry-forward M52-2** (a trivial doc-hygiene fix — fold into the next phase touching the connect-failure path, or a close-out addendum).
2. **Informational — the un-witnessed connect-failure + retry-budget-overflow edge.** On the retry-budget-`Rejected` exit (`hcm.rs:1126-1128` → fall-through `break`), if the final attempt was a connect failure, `final_outcome == Some(ConnectFailure)` → `connect_failure_for_log` becomes true → the derive renders `UF`. The surfaced response there IS the connect-failure synth-503, so `UF` is arguably correct — but this combination is un-recon'd against real Envoy and is explicitly the deferred retry-budget-overflow slice of M45-2. Fixture `0060` has NO `circuit_breakers`, so this path is never exercised this phase. → **No action required; flagged for whoever picks up the retry-budget-overflow witness so the `UF`-vs-other disposition gets recon'd rather than silently inherited** (folds into the existing M45-2 retry-budget slice).

## Recommendations

- None blocking. The two Minors are tracking notes (M52-2 doc-hygiene; M45-2 retry-budget recon), not code defects.

## Assessment (verbatim verdict)

**Ready to merge? YES.** The 502→503 correction is complete and consistent (only the intentionally-deferred reset arm keeps 502), the `final_outcome`/`connect_failure_for_log` discriminator is sound on every loop-exit path and provably 1:1 with the connect-failure synth-503, the URX-before-UF ordering is correct, the `upstream_rq_5xx` non-interaction holds, and fixture `0060` + its driver genuinely witness `{"rc":503,"rf":"UF"}` with CI green on the code commit `d10fdc4`. Scope is exactly the SPEC/PLAN-locked shape — one `bool` local + a three-arm status-literal change + one derive branch + a fixture + a differential test + the §G sweep; `#![forbid(unsafe_code)]` holds; NO new `Op`/`AccessLogRecord` field/crate/dependency/fuzz-target/`ConfigError` variant. The only findings are a one-word stale comment in an unrelated fixture (M52-2, non-blocking) and an informational un-witnessed deferred edge (M45-2). No Critical, no Important issues.

---

_State-5 complete → phase 52 advances to **state-6 close-out** NEXT (the SESSION AFTER, per §5.1 + memory `closeout-and-pick-are-separate-sessions`): flip ROADMAP row `52` → `done`, relocate the active-phase Notes to `STATE_HISTORY.md`, STATE → awaiting-next-planning. Carry-forward consumption at close: phase 52 CONSUMES the connect-failure `UF` slice of M45-2 (leaving the downstream-termination `DC` + the retry-budget-overflow slices live) and FOLDS M51-1 (the `:1225`→`:1343` anchor reconciliation, done in §E); NEW carry-forward **M52-1** (the reset/disconnect `UC` flag + the reset 502→503 status) + **M52-2** (the `0019/envoy.yaml:18` connect-fail-502→503 contrast-comment doc fix); M50-C / M48-2 / M42-1 / M45-1 / the `DC`+retry-budget slices of M45-2 / older stay live._
