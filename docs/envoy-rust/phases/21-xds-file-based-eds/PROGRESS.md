# Phase 21 (`21-xds-file-based-eds`) — PROGRESS

> Running log, updated by the executor on each task completion (the 06.2 → 20 cadence).
> One entry per PLAN task; quote the verifying command output. The state-3 arc runs
> `cargo clippy --workspace --all-targets --all-features -- -D warnings` PER TASK
> (NOT deferred to state-4) per `project_state3_arc_skips_clippy`.

**PLAN:** `docs/envoy-rust/phases/21-xds-file-based-eds/PLAN.md`
**SPEC:** `docs/envoy-rust/phases/21-xds-file-based-eds/SPEC.md`
**Scope ADRs:** ADR-0053 (the xDS-family continuation pick [file-based EDS over CDS/LDS/RDS file watching (three stacked risks, ROI improved by deferral to its best yet) + the still-blocked gRPC family] + the four §0 findings [the config-source machinery + envelope parser reused, the new shape is `ClusterType::EDS` + `eds_cluster_config` on the cluster + `load_assignment`→`Option` — the FIRST CLUSTER-scoped dynamic resource; the merge-into-effective-`load_assignment` needs no cluster runtime refactor; EDS stats are PER-CLUSTER-scoped — extending the existing namespace, lighter than RDS's per-HCM topology; cluster warming is the EDS-specific disposition question + no pre-existing allow-listed Envoy-side EDS surface — the §6.2 stat verification is load-bearing] + the minimum-viable scope/deferral ledger). ADR-0054 (the §6.2 empirical-verification reconciliation — **FIRES**: item 5 [the `EndpointsConfigDump` shape + `configs[]` ordering] diverged materially; item 1 [numeric-IP constraint], item 2/4 [warm-and-503 vs all-fatal], item 6a [EDS+`load_assignment` accepted], item 10 [STATIC emits `update_*` at 0] all diverged; items 1-base/7/8/6b/6c MATCHED).

---

## State-2 PLAN-write (this commit)

- Performed the HEAVY SPEC §6.2 empirical verification against `envoyproxy/envoy:v1.33.0` (digest `sha256:56da5afd…`; Docker on **darwin — ran LOCALLY**, no virtiofs/inotify dependency for initial-load EDS; foreground general-purpose subagent; a `type: EDS` cluster `eds_backend` [`eds_cluster_config.eds_config.path_config_source.path` → a `ClusterLoadAssignment` file] + a host HTTP backend [reached via the Docker-Desktop host-gateway `192.168.65.254`] + admin `/stats`, `/stats?format=json`, `/config_dump`, `/config_dump?include_eds`, `/clusters`, `/ready` scrapes + the missing-file / malformed-content / missing-`ClusterLoadAssignment` / empty-`resources` / EDS-with-`load_assignment` / STATIC-with-`eds_cluster_config` / EDS-with-neither negative variants). Findings L1–L11 locked into PLAN.md "§6.2 empirical lock-ins" + ADR-0054. **Item 5 DIVERGES materially → ADR-0054 FIRES** (alongside the item 1 numeric-IP, item 2/4 warm-vs-fatal, item 6a, item 10 divergences).
  - **L1 (envelope / `eds_cluster_config`-on-cluster) MATCH + a NEW numeric-IP constraint:** both bare `resources:` + full `DiscoveryResponse` accepted; per-resource `@type: …endpoint.v3.ClusterLoadAssignment` (REQUIRED); cluster block `type: EDS` + `eds_cluster_config: { eds_config: { path_config_source: { path }, resource_api_version? }, service_name? }`; `resource_api_version` + `service_name` both OPTIONAL. **The endpoint `socket_address.address` MUST be a NUMERIC IP** (hostname → `update_rejected`) — EDS endpoints are resolved socket addresses (STATIC semantics).
  - **L2 (readiness / warming) MATCH:** EDS endpoints active before `/ready` 200; first request succeeds immediately, no warm-up window; `warming_state: 0`. envoy-rust mirrors via synchronous `load_dynamic_resources`.
  - **L3 (per-cluster stats) enumerated; subset locked:** the §2.1 subset is `cluster.<name>.update_{attempt,success,failure,empty}` (value-exact **1/1/0/0**) + the data-plane witness `cluster.<name>.upstream_rq_total` (1, unconditional). **`membership_healthy`/`membership_total` NOT asserted** — a verified envoy-rust narrowing (`membership_healthy` is health-check-gated at `cluster.rs:926` with an explicit `:2227` inertness test; `membership_total` does NOT exist in envoy-rust; fixture 0029 has no health checks; Envoy emits both → allow-listed envoy-only, NOT broadened). Envoy-only (NOT asserted): `update_no_rebuild`/`update_rejected`/`update_time`/`update_duration`/`membership_change`/`degraded`/`excluded`/`assignment_*`/`version`/`version_text`/`warming_state`.
  - **L4 (negative-path disposition) — the load-bearing answer: warm-and-503 on Envoy; ONLY missing-FILE-PATH fatal on both:** (a) missing file PATH = Envoy hard-exit at config-load; (b) malformed content = Envoy `update_failure:1`+503; (c) missing/mismatched `ClusterLoadAssignment` = Envoy `update_rejected:1`+503 (`Unexpected EDS cluster (expecting <name>): <other>`, process stays UP, `/ready`=LIVE, boot does NOT block on warming); (d) empty `resources:[]` = Envoy `update_empty:1`+`update_success:1`+503. **envoy-rust = ALL FATAL** (the ADR-0049 decision-2 posture extended to EDS) → `update_failure`/`update_empty`/`update_rejected` register at 0, structurally unreachable; negative paths backstop-only. Only (a) fatal on BOTH.
  - **L5 (EndpointsConfigDump shape + `configs[]` ordering) DIVERGES MATERIALLY — the ADR-0054 trigger:** (1) Envoy OMITS `EndpointsConfigDump` from the DEFAULT `/config_dump` — only `/config_dump?include_eds` surfaces it; (2) file-based EDS endpoints land under `static_endpoint_configs[]`, NOT `dynamic_endpoint_configs[]` (file-based EDS is "static" config-dump-wise; a STATIC cluster ALSO populates it under `?include_eds`); (3) shape `{ "@type": ".../EndpointsConfigDump", "static_endpoint_configs": [ { "endpoint_config": { "@type": ".../ClusterLoadAssignment", "cluster_name", "endpoints", "policy" } } ] }` (no `version_info`/`last_updated`); (4) `?include_eds` order Bootstrap[0]/Clusters[1]/**Endpoints[2]**/Listeners[3]/ScopedRoutes[4]/Routes[5]/Secrets[6]. **Reconciliation (ADR-0054):** envoy-rust emits `EndpointsConfigDump` (conditional on some cluster EDS) with `static_endpoint_configs[].endpoint_config`, pushed after Clusters/before Listeners, UNCONDITIONAL of `?include_eds` (a recorded narrowing); the admin STRIPS the query string (so `/config_dump?include_eds` routes to ConfigDump); the fixture-0029 probe scrapes `/config_dump?include_eds` and asserts via a per-side `JsonSubtreeRule` (Envoy `configs.2`, envoy-rust `configs.1` — 0029 has no `cds_config` → no ClustersConfigDump) — REUSING the ADR-0052 per-side path mechanism (NO new harness JSON code).
  - **L6 (consistency validation) — 6b/6c MATCH (hard-exit), 6a DIVERGES (Envoy accepts):** 6a EDS+inline `load_assignment` = Envoy ACCEPTS (ignores inline) → envoy-rust STRICTER reject (`LoadAssignmentOnEdsCluster`, recorded); 6b STATIC+`eds_cluster_config` = Envoy hard-exit (`eds_cluster_config set in a non-EDS cluster`) → envoy-rust `EdsConfigOnNonEdsCluster`; 6c EDS+neither = Envoy hard-exit (`cannot create an EDS cluster without an EDS config`) → envoy-rust `MissingEdsClusterConfig`. Plus a non-EDS cluster with no `load_assignment` → envoy-rust `MissingLoadAssignment`.
  - **L7 (wire shape) MATCH:** a GET to an EDS endpoint is byte-identical to a static-endpoint response (200 + echo body + `x-envoy-upstream-service-time` + `server: envoy` + allow-list; no EDS-specific header).
  - **L8 (`service_name` selection) MATCH:** `service_name` unset → file CLA `cluster_name` must equal the cluster name; `service_name: X` set → must equal `X`. The D3 key = `service_name.unwrap_or(cluster.name)`.
  - **L9 (file shareable-as-TEMPLATE + per-side NUMERIC IP) DIVERGES from projected plain-shareable:** a minimal CLA is accepted verbatim, BUT the endpoint address must be a NUMERIC IP that differs per side → the EDS file is a SHARED template (`eds.yaml`) rendered per-side via a NEW `{{EDS_BACKEND_IP}}` marker (upstream → the runtime-discovered numeric host-gateway IP; subject → `127.0.0.1`); the harness DISCOVERS the gateway IP (one-shot `getent` in the pinned Envoy image, gated to EDS fixtures). **The load-bearing harness reconciliation (D6).**
  - **L10 (stat conditionality) DIVERGES (STATIC emits `update_*` at 0) but PRE-EXISTING + already tolerated:** STATIC clusters emit `update_{attempt,success,failure,empty,no_rebuild}` at 0 (only `update_rejected` is EDS-exclusive); PRE-EXISTING Envoy behavior, the existing 28 fixtures already tolerate it. envoy-rust conditionally registers `update_*` ONLY for `cluster_type == Eds` → adds ZERO names to the existing fixtures.
  - **L11 (version) Envoy-only;** not asserted.
- Performed the PLAN-time SPEC-correction pass (read-only Explore subagent + controller re-verification by direct grep) against HEAD `f350d952f`. **All SPEC §0/§3 anchors CONFIRMED; zero drift.** Corrections recorded in PLAN.md "PLAN-time SPEC corrections": C16 (the validate-gate + EDS-pass-trigger must extend to `has_eds_cluster` — fixture 0029 is a static-but-EDS bootstrap with NO `dynamic_resources`/`rds`, so the phase-20 gate would skip the post-merge validate; the EDS pass + the re-validation must fire on any EDS cluster); C17 (**the D1 migration is SMALLER than feared — ~17 actual Rust `Cluster {…}` struct literals, NOT the ~151 the first survey conflated with YAML-in-string fixtures** [the 85 `load_assignment:` occurrences in `bootstrap.rs` are YAML strings → `serde` → `Some`, NO edit]; the real literals concentrate in `envoy-cluster/src/cluster.rs` + `envoy-admin/src/endpoint.rs` tests; comparable to phase-18's 26 / phase-20's 11); C18 (the `eds.rs` / dynamic-file-render-helper consolidations, now N=4, DEFERRED by deliberate risk-managed decision — write a new `eds.rs` mirroring `cds.rs`, render `{{EDS_PATH}}` as a fourth shared-template branch, to keep the three green parser modules + the green harness untouched); C19 (the EDS pass mutates `load_assignment` in-place → BootstrapConfigDump shows the populated `load_assignment` for a static EDS cluster, a known minor divergence vs Envoy, NOT asserted; the EndpointsConfigDump is the faithful resolved-endpoints surface). D1 migration enumerated: **~17 Rust `Cluster {…}` struct-literal construction sites** (each gains `eds_cluster_config: None` + `load_assignment: Some(...)`) + the production reads (`cluster.rs:735` endpoint build, `bootstrap.rs:2262/2265/2269` validator) + ~12 `.load_assignment` test reads in `bootstrap.rs` + the new `Eds` match arm at `cluster.rs:735-793`. The fuzz corpus is **31** seeds (→ 32 with `cluster_eds.yaml`).
- Evaluated the §6.1 split gate against the §6.2-refined surface (**~1150–1500 LoC / 11 tasks**; the per-side `JsonSubtreeRule` is REUSED from phase 20 [no new harness JSON code], the new harness cost is the gateway-IP discovery ~40 LoC, the D1 migration is ~17 sites not ~151) → **single un-split phase; ADR-0055 does NOT fire** (reserved-but-unconsumed).
- Flipped ROADMAP row `21` `planned → in-progress`. Advanced STATE.md to `21` state-2-complete / state-3-next.

## Task 1 — `envoy-config` schema (`ClusterType::Eds` + `eds_cluster_config` + `load_assignment`→Option + exactly-one-of validator + the Eds build arm + the D1 migration sweep)

**Preamble (read before starting):**
- **Goal:** Add `ClusterType::Eds` (the `rename_all` maps it to `EDS` — no per-variant rename); add the `EdsClusterConfig { eds_config: ConfigSource, service_name: Option<String> }` struct (`deny_unknown_fields`; `eds_config` reuses `ConfigSource`/`PathConfigSource` verbatim); change `Cluster.load_assignment` from `LoadAssignment` to `Option<LoadAssignment>` + add `eds_cluster_config: Option<EdsClusterConfig>`; add the exactly-one-of-and-consistent validation (in `validate_cluster`: `MissingEdsClusterConfig`/`MissingLoadAssignment`/`EdsConfigOnNonEdsCluster` + the `Option`-guarded endpoint checks with the EDS name-mismatch carve; PLUS the parse-time-only `LoadAssignmentOnEdsCluster` via a `check_endpoint_sources(&Bootstrap)` pass, mirroring phase-20's `check_route_sources`); add the new `Eds` match arm at `cluster.rs:735-793` (shares the `Static` numeric-parse arm — L1) + the `load_assignment` `Option`-unwrap; add the 7 new `ConfigError` variants; and FIX ALL ~17 `Cluster {…}` construction sites + the read sites in the same commit. TDD per the PLAN Task 1 test list (groups a–i).
- **§6.2 lock-ins that bind this task:** L1 (`EdsClusterConfig` shape = `eds_config` + optional `service_name`; the EDS endpoint is a NUMERIC IP — the `Eds` build arm parses like `Static`, NOT `StrictDns`); L6 (the four validator variants — 6a `LoadAssignmentOnEdsCluster` is parse-time-only + STRICTER than Envoy; 6b/6c match Envoy's hard-exit); the `api_config_source`/`ads`/`watched_directory` deferral stays rejected by `ConfigSource`'s + `EdsClusterConfig`'s `deny_unknown_fields`.
- **PLAN-time corrections that bind this task:** C8 (the `validate_cluster` name-mismatch check applies to INLINE/non-EDS clusters only — an EDS cluster's populated CLA `cluster_name` equals `service_name`, NOT the cluster name; re-checking against `cluster.name` post-merge would falsely reject); C17 (the migration is ~17 Rust literals — confirm by building; the 85 `bootstrap.rs` `load_assignment:` occurrences are YAML-in-string, NO edit); the `LoadAssignmentOnEdsCluster` parse-time placement (a `check_endpoint_sources` pass, NOT inside `validate_cluster`, to avoid false-positiving the post-merge loaded state where an EDS cluster is `(Eds, Some, Some)`).
- **Anchors (verified at HEAD `f350d952f`):** `ClusterType` `crates/envoy-config/src/bootstrap.rs:225-239` (`rename_all = "SCREAMING_SNAKE_CASE"`); `Cluster` `:174-223` (`load_assignment` `:181`); `ConfigSource`/`PathConfigSource` `:107-123` (reused by `EdsClusterConfig`); `LoadAssignment` hierarchy `:262-359`; `validate_cluster` `:2261-2278` (`LoadAssignmentNameMismatch` `:2262/2265`, `EmptyClusterEndpoints` `:2269`); the endpoint-build dispatch `crates/envoy-cluster/src/cluster.rs:730-793` (`load_assignment.endpoints` `:735`, the `Static`/`StrictDns` arms); `ConfigError` (`Cds*`/`Lds*`/`Rds*` variants) in `lib.rs`; the ~17 construction sites + the read sites enumerated in PLAN "PLAN-time SPEC corrections" C17 / the D1 carry-forward warning.
- **Carry-forward warning (D1 — the workspace stays RED until done):** `load_assignment` becoming `Option` + adding `eds_cluster_config` + adding `ClusterType::Eds` (the non-exhaustive `match cfg.cluster_type` at `cluster.rs:735`) breaks every `Cluster {…}` literal + read + the match across the workspace. Fix ALL ~17 construction sites (`load_assignment: Some(...), eds_cluster_config: None`) + the read sites + the new `Eds` match arm in THIS commit (the phase-18/19/20 struct-literal-sweep lesson). Run `cargo build --workspace --all-targets` before committing; do NOT split the sweep across commits.
- **Verification:** `cargo test -p envoy-config` (PASS; inline-`load_assignment` fixtures parse identically) + `cargo build --workspace --all-targets` + the 4 standalone-crate builds (per `project_isolated_crate_build_blindspot` — the `load_assignment`→`Option` change ripples through `envoy-cluster` most heavily) + `cargo clippy --workspace --all-targets --all-features -- -D warnings` (the D1 `Option`-unwrap sites are likely lint sites per SPEC §6.5) + `cargo fmt --all -- --check`.

**Completion (this commit — code `655ec3cf0`):**

- **What landed:**
  - `bootstrap.rs`: `ClusterType::Eds` (3rd variant; `rename_all = "SCREAMING_SNAKE_CASE"` → `EDS`, no per-variant rename); `EdsClusterConfig { eds_config: ConfigSource, service_name: Option<String> }` (`deny_unknown_fields`; `eds_config` reuses `ConfigSource`/`PathConfigSource` verbatim); `Cluster.load_assignment` → `Option<LoadAssignment>` + new `eds_cluster_config: Option<EdsClusterConfig>` (both `#[serde(default, skip_serializing_if = "Option::is_none")]`).
  - `validate_cluster`: the `(is_eds, la_some, eds_some)` cardinality match (`MissingEdsClusterConfig` / `MissingLoadAssignment` / `EdsConfigOnNonEdsCluster`) + `Option`-guarded endpoint checks (`let Some(la) = …else { return Ok(()) }` for the pre-merge EDS cluster) + the name-mismatch carve (`!is_eds && la.cluster_name != cluster.name` — C8).
  - New `check_endpoint_sources(&Bootstrap)` pass (mirrors phase-20's `check_route_sources`): rejects an inline `load_assignment` on a `type: EDS` cluster (`LoadAssignmentOnEdsCluster`, L6 6a — stricter than Envoy), wired into `parse_bootstrap` AFTER `check_route_sources`, BEFORE `validate()` — parse-time-only so it never false-positives the post-merge loaded `(Eds, Some, Some)` state.
  - `lib.rs`: the 7 new `ConfigError` variants (`MissingLoadAssignment`, `MissingEdsClusterConfig`, `EdsConfigOnNonEdsCluster`, `LoadAssignmentOnEdsCluster`, `EdsFileError`, `EdsParseError`, `EdsClusterLoadAssignmentNotFound`) with the exact PLAN `#[error(...)]` text.
  - `cluster.rs`: the `Eds` build arm shares the `Static` numeric-parse branch (`ClusterType::Static | ClusterType::Eds` — L1, NOT `StrictDns`); the `load_assignment` `Option`-unwrap via `.as_ref().expect("load_assignment populated post-load — §5.3 invariant")`.
- **D1 migration sweep — `Cluster {}` literal sites (file:count):** `crates/envoy-cluster/src/cluster.rs:3` (the only Rust `envoy_config::Cluster {…}` struct literals in the workspace — at the test sites ~`:1360` / `:1399` `mk_cluster` / `:3231` `mk_static_cluster`). **Plus 9 `.load_assignment.*` test reads in `bootstrap.rs`** adapted to `.load_assignment.as_ref().unwrap().*`. **Total: 3 literals + 9 reads + the 2 production reads (validator + endpoint-build) + the new `Eds` match arm.** SMALLER than the C17 ~17 estimate — confirmed empirically by `cargo build --workspace --all-targets`: every other crate (`envoy-http1`/`envoy-http2`/`envoy-health`/`envoy-tcp`/`envoy-admin`/`envoy-bin`) constructs clusters from YAML-in-string fixtures (serde → `Some`, NO Rust edit), exactly as C17 predicted for the `bootstrap.rs` 85-occurrence count.
- **Verification outputs:**
  - `cargo test -p envoy-config` → `test result: ok. 377 passed; 0 failed; 0 ignored` (includes the 9 new `eds_schema_*` group a–i tests). Doc-tests: `0 passed; 0 failed`.
  - `cargo test -p envoy-cluster` → `test result: ok. 88 passed; 0 failed; 0 ignored`.
  - `cargo build --workspace --all-targets` → green (exit 0; the D1 sweep restores the workspace build; the broken state was RED only until all literals + reads + the non-exhaustive match were fixed).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings` → clean (exit 0; the `Option`-unwrap `expect` site lints clean).
  - `cargo fmt --all` → applied (3 files reformatted).
  - 4 standalone builds: `cargo build -p envoy-config` ✓ · `-p envoy-cluster` ✓ · `-p envoy-http1` ✓ · `-p envoy-http2` ✓ (all `Finished` exit 0).
- **Scope discipline:** Inline STATIC/STRICT_DNS clusters parse + behave identically (regression-equivalence test (c) `eds_schema_inline_static_still_parses` green; the 28-fixture stat output unchanged — no new `update_*` names added in this task). Nothing from Tasks 2–11 touched (`eds.rs`, the merge pass, the stats, the config_dump entry, the harness all deferred).

## Tasks 2–11

Pending — see `PLAN.md` for the full per-task steps. The executor appends a Completion block to each on landing (one code commit + one PROGRESS commit per task, SERIAL dispatch per `feedback_serial_subagent_dispatch`, TDD + clippy PER TASK per `project_state3_arc_skips_clippy`):

- **Task 2** — EDS file parser `eds.rs` (mirror `cds.rs`; the consolidation DEFERRED per C18).
- **Task 3** — `load_dynamic_resources` EDS pass (cluster walk + name-select + populate + §5.7 ordering + the C16 validate-gate extension + re-run `check_endpoint_sources` over the merged set).
- **Task 4** — per-cluster `cluster.<name>.update_*` stats (register-and-set per L3; NO main.rs call site — the registration is inside `from_bootstrap`).
- **Task 5** — `/config_dump` `EndpointsConfigDump` (`static_endpoint_configs`) + the admin query-string strip.
- **Task 6** — harness `{{EDS_PATH}}` shared-template + `{{EDS_BACKEND_IP}}` per-side numeric marker + host-gateway-IP discovery (the genuinely-new harness primitive — verify on BOTH macOS Docker Desktop + Linux CI).
- **Task 7** — fixture `0029-xds-file-based-eds` + Docker-gated wrapper.
- **Task 8** — in-process backstop (happy + 6 negative + inertness).
- **Task 9** — fuzz seed `cluster_eds.yaml` (corpus 31 → 32).
- **Task 10** — BEHAVIOR_CONTRACT extensions (EDS stat rows + xDS EDS extension + EndpointsConfigDump row).
- **Task 11** — state-4 phase-done verification + STATE advance to state-5-next.

### Task 2 — Completion (this commit — code `527cd0573`)

- **What landed:**
  - New `crates/envoy-config/src/eds.rs` — the EDS file parser, mirroring `cds.rs`/`lds.rs`/`rds.rs` verbatim (the C18 decision: a NEW sibling module, NOT the deferred `xds_file.rs` N=4 consolidation). The `@type`-tagged single-variant enum `EdsResource::ClusterLoadAssignment` renamed `type.googleapis.com/envoy.config.endpoint.v3.ClusterLoadAssignment`; the per-resource payload is the existing `bootstrap::LoadAssignment` struct (envoy-rust's name for `ClusterLoadAssignment`), reused verbatim.
  - `pub fn parse_eds_file(path: &str, contents: &str) -> Result<Vec<LoadAssignment>, crate::ConfigError>` — `serde_yaml::from_str` + error mapping to the Task-1 `ConfigError::EdsParseError { path, message }`. NO validation here (the name-selection + per-cluster endpoint checks run at merge time in Task 3, against the merged state — like `parse_rds_file`, unlike `parse_cds_file`).
  - `version_info` handling matches `CdsFile`/`LdsFile`/`RdsFile` EXACTLY: the envelope has `#[serde(default)] resources` and NO `deny_unknown_fields`, so `version_info` (and `type_url`/`nonce`/…) is accept-and-ignore by omission — the existing idiom, NOT the PLAN draft's explicit `version_info: Option<String>` + `#[allow(dead_code)]` field (which the "match the existing modules exactly" instruction overrides; dropping it also keeps clippy clean with no `dead_code` allow).
  - `lib.rs`: `pub mod eds;` (beside `pub mod rds;`) + `pub use eds::parse_eds_file;` (beside `pub use rds::parse_rds_file;` — matching the cds/lds/rds re-export idiom).
- **TDD:** the 6 `eds_parse_*` tests (groups a–f) written FIRST and confirmed RED (compile error `cannot find function parse_eds_file`, 6 errors) BEFORE implementing, then GREEN: (a) bare `resources:` envelope, (b) full DiscoveryResponse + `version_info` (ignored), (c) multiple CLAs (returned in order — name-selection is Task 3), (d) non-ClusterLoadAssignment `@type` rejected, (e) malformed YAML → `EdsParseError` carrying the path, (f) missing `@type` → `EdsParseError` mentioning `@type`.
- **Verification outputs:**
  - `cargo test -p envoy-config eds_parse` → `test result: ok. 7 passed; 0 failed; 0 ignored` (the 6 `eds_parse_*` + the fuzz-corpus seed test matched by the filter).
  - `cargo test -p envoy-config` (full crate) → `test result: ok. 383 passed; 0 failed; 0 ignored` (377 from Task 1 + 6 new). Doc-tests: `0 passed; 0 failed`.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings` → clean (exit 0).
  - `cargo fmt --all` → applied (the `parse_eds_file` signature wrapped to multi-line; no other reformatting).

### Task 3 — Completion (this commit — code `517ad47bf`)

- **What landed** (all in `crates/envoy-config/src/lib.rs`, `load_dynamic_resources`):
  - **The EDS pass** — slotted AFTER the RDS pass and BEFORE the single post-merge `bootstrap::validate(bootstrap)?`. Walks every cluster across the EFFECTIVE set (static clusters chained with the CDS-merged `dynamic_clusters`); for each `cluster.cluster_type == ClusterType::Eds`: reads `eds_cluster_config.eds_config.path_config_source.path` via `std::fs::read_to_string` (→ `ConfigError::EdsFileError { path, source }` on io error), `eds::parse_eds_file`s it (→ `EdsParseError` on malformed), name-selects the `ClusterLoadAssignment` whose `cluster_name == service_name.unwrap_or(cluster.name)` (L8) via `position(...).map(|i| parsed.remove(i))` (→ `EdsClusterLoadAssignmentNotFound { name, path }` if none matches), and sets `cluster.load_assignment = Some(selected)` (§5.3 uniform downstream shape). `eds_cluster_config` is retained, not consumed.
  - **Split-borrow handling:** the two-field `&mut` borrow (`&mut bootstrap.static_resources.clusters`, `&mut bootstrap.dynamic_clusters`) is wrapped in an explicit `{ … }` block so it ends BEFORE the post-merge `validate(bootstrap)`. `had_eds_cluster` is a Copy `bool` captured inside the loop, so nothing into `bootstrap` is held past the block — mirrors the RDS pass's `had_rds_hcm` idiom (the RDS pass relies on the loop ending the borrow; the EDS pass needs the explicit block because the `validate` call follows in the same scope).
  - **The §5.7 validate-gate extension (C16):** the existing post-merge gate `dynamic_clusters.is_some() || dynamic_listeners.is_some() || had_rds_hcm` now also includes `|| had_eds_cluster`, so a purely-static EDS bootstrap (fixture 0029 — NO `dynamic_resources`/`rds`) still re-validates its now-EDS-populated `load_assignment` (e.g. empty endpoints → `EmptyClusterEndpoints`). Without this, the post-merge validate would be skipped and a malformed EDS load would WRONGLY succeed.
  - **Step 4 — `check_endpoint_sources` re-call over the merged set:** added a `bootstrap::check_endpoint_sources(bootstrap)?` call AFTER the CDS+LDS merge (right beside the existing `check_route_sources` re-call) and BEFORE the EDS pass. `check_endpoint_sources` already walks `all_clusters()` (static + dynamic-CDS), so a CDS-supplied `type: EDS` cluster carrying an inline `load_assignment` is rejected (`LoadAssignmentOnEdsCluster`) here — BEFORE the EDS pass populates `(Eds, Some, Some)`, which `validate_cluster` tolerates. (The Task-1 reviewer's note that the helper iterates `all_clusters()` is confirmed; the re-call is the load-bearing fix for the CDS-supplied case, proven by the new test.)
- **TDD:** the 9 `load_dynamic_eds_*` tests (in the `bootstrap.rs` test module, beside the CDS/LDS/RDS merge tests, using `tempfile`) written FIRST and confirmed RED (8 wrong-result + 1 compile-clean failure) BEFORE implementing, then GREEN: (a) `…pass_loads_and_populates` — EDS file CLA populates `load_assignment` (port 9001, `eds_cluster_config` retained); (b) `…service_name_selection` — `service_name: my_svc` selects the `my_svc` CLA (L8); (c) `…missing_file_is_fatal` → `EdsFileError`; (d) `…malformed_file_is_fatal` → `EdsParseError`; (e) `…cla_mismatch_is_fatal` → `EdsClusterLoadAssignmentNotFound { name: "eds_backend", .. }`; (f) `…empty_endpoints_is_fatal_post_merge` → `EmptyClusterEndpoints("eds_backend")`; (g) `…walks_static_and_cds_clusters` — CDS `dynamic_backend` (port 8124) untouched + static `eds_backend` (port 9004) populated (§5.7 composition-ready); (h) `…validate_gate_fires_static_only` — static-only EDS bootstrap with empty-endpoints CLA still rejected post-merge (proves the C16 gate extension); plus the Step-4 `…cds_eds_with_inline_load_assignment_rejected` — a CDS file whose cluster is `type: EDS` + inline `load_assignment` → `LoadAssignmentOnEdsCluster { cluster: "bad_eds" }`. (PLAN test (i) — EDS+inline LA rejected at PARSE — was already landed in Task 1 as `eds_schema_eds_inline_load_assignment_rejected`; not duplicated.)
- **Verification outputs:**
  - `cargo test -p envoy-config load_dynamic_eds` → `test result: ok. 9 passed; 0 failed; 0 ignored`.
  - `cargo test -p envoy-config` (full crate) → `test result: ok. 392 passed; 0 failed; 0 ignored` (383 from Task 2 + 9 new); doc-tests `0 passed; 0 failed`.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings` → clean (exit 0).
  - `cargo fmt --all` → applied (the `select_name` `.unwrap_or_else(...)` chain wrapped to multi-line; no other reformatting).
  - `cargo build --workspace --all-targets` → Finished (exit 0).
  - Standalone builds — `cargo build -p envoy-config` / `-p envoy-cluster` / `-p envoy-http1` / `-p envoy-http2` → all Finished (exit 0) (the `project_isolated_crate_build_blindspot` guard).

### Task 4 — Completion (this commit — code `b6ac083fa`)

- **What landed** (all in `crates/envoy-cluster/src/cluster.rs`, `from_bootstrap`, at the per-cluster stat registration site — placed right after the `membership_healthy`/`endpoint_health` block and before the `outlier_detection` block, where `cfg`/`registry`/`cfg.name` are in scope):

  ```rust
          if cfg.cluster_type == envoy_config::ClusterType::Eds {
              let mk = |suffix: &str| -> Result<Arc<envoy_stats::Counter>, ClusterError> {
                  registry
                      .register_counter(&format!("cluster.{}.{suffix}", cfg.name))
                      .map_err(|e| ClusterError::StatsRegistration {
                          cluster: cfg.name.clone(),
                          message: e.to_string(),
                      })
              };
              mk("update_attempt")?.add(1);
              mk("update_success")?.add(1);
              mk("update_failure")?; // registers at 0 (L4)
              mk("update_empty")?; // registers at 0 (L4)
          }
  ```

  - The conditional EDS `update_*` family — registered ONLY when `cfg.cluster_type == ClusterType::Eds` (the §5.2 conditional-registration discipline; STATIC/STRICT_DNS clusters add ZERO `update_*` names, so all 28 existing fixtures stay regression-equivalent). Register-and-set DIRECTLY per L3 (no handle threading): `update_attempt`/`update_success` `.add(1)`; `update_failure`/`update_empty` register at 0 (the all-fatal L4 posture makes non-zero structurally unreachable). The local `mk` closure mirrors the `cluster_manager.cds.*` template at `:1085-1092` EXACTLY (same `register_counter` → `ClusterError::StatsRegistration { cluster, message }` error map, same `Arc<envoy_stats::Counter>` return).
  - **L3 narrowing honored — NO membership touched:** registers ONLY the 4 `update_*` counters. `membership_healthy` stays health-check-gated at its existing `:937` site (untouched); `membership_total` is NOT added (it does not exist in envoy-rust). The data-plane witness `cluster.<name>.upstream_rq_total` already registers unconditionally (`:847`) — NOT re-registered.
  - **NO `main.rs` call-site change:** the per-cluster registration lives inside `from_bootstrap` (already called at startup) — unlike `register_rds_stats`, no new call site. Confirmed by `git status` showing ONLY `crates/envoy-cluster/src/cluster.rs` modified + a clean `cargo build --workspace --all-targets`.
- **TDD:** the 3 `eds_stats_*` tests (in the `cluster_manager.*` test module, reusing the existing `mk_bootstrap`/`cds_dynamic_resources`/`stat_value` helpers + a new `mk_eds_cluster` factory) written FIRST and confirmed RED (`eds_stats_registered_for_eds_cluster` failed `left: None, right: Some(1)`; the two inertness tests passed pre-implementation since nothing wrong was registered yet) BEFORE implementing, then GREEN: (a) `eds_stats_not_registered_for_non_eds_clusters` — an all-STATIC bootstrap WITH `cds_config` (the fixture-0026 inertness witness) registers NO `cluster.<name>.update_*` name; (b) `eds_stats_registered_for_eds_cluster` — a `type: EDS` cluster `eds_backend` (load_assignment pre-populated with a numeric IP, `eds_cluster_config` present) reads `update_attempt == 1`, `update_success == 1`, `update_failure == 0`, `update_empty == 0`; (c) `eds_stats_register_no_membership_gauges` — the same EDS cluster (no health checks) registers NEITHER `cluster.eds_backend.membership_total` (absent) NOR `cluster.eds_backend.membership_healthy` (HC-gated).
- **Verification outputs:**
  - `cargo test -p envoy-cluster eds_stats` → `test result: ok. 3 passed; 0 failed; 0 ignored` (RED→GREEN confirmed: the registration test went `None → Some(1)`).
  - `cargo test -p envoy-cluster` (full crate) → `test result: ok. 91 passed; 0 failed; 0 ignored` (88 prior + 3 new); doc-tests `0 passed; 0 failed`.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings` → clean (exit 0).
  - `cargo fmt --all` → applied (no reformatting of the new block beyond standard).
  - `cargo build --workspace --all-targets` → Finished (exit 0).
- **Note:** `EdsClusterConfig` is NOT re-exported at the `envoy_config` crate root (a Task-1 omission — only the struct + `ClusterType::Eds` are in the `pub use bootstrap::{…}` list), so the test factory references it via the fully-qualified `envoy_config::bootstrap::EdsClusterConfig` (the `bootstrap` module is `pub`). Out of scope for Task 4 to touch the re-export; flagged for Task-5+ if a root-level path becomes convenient.

### Task 5 — Completion (this commit — code `749bb5eb4`)

- **What landed:**
  - **The admin query-string strip** (`crates/envoy-admin/src/endpoint.rs`, `AdminEndpoint::from_path`): a single `let path = path.split('?').next().unwrap_or(path);` line BEFORE the `match path { … }`, so `/config_dump?include_eds` routes to `ConfigDump` (Envoy's admin does the same — surfaces the EndpointsConfigDump bilaterally per L5). No existing fixture uses a query string, so this is inert for every established endpoint (the bare-path arms are unchanged).
  - **The `ConfigDumpEntry::Endpoints` variant** (after `Clusters`): `#[serde(rename = "type.googleapis.com/envoy.admin.v3.EndpointsConfigDump")]` carrying `static_endpoint_configs: Vec<StaticEndpointConfigEntry<'a>>` with `#[serde(skip_serializing_if = "Vec::is_empty")]`. Supporting serialize structs (placed beside `TaggedCluster`/`CLUSTER_TYPE_URL`): `StaticEndpointConfigEntry { endpoint_config: ClusterLoadAssignmentBody }` and `ClusterLoadAssignmentBody { @type (type_url: &'static str), cluster_name: &'a str, endpoints: &'a Vec<envoy_config::LocalityLbEndpoints> }`, plus the `CLUSTER_LOAD_ASSIGNMENT_TYPE_URL = "type.googleapis.com/envoy.config.endpoint.v3.ClusterLoadAssignment"` const. Mirrors the existing `TaggedCluster`/`TaggedListener` idiom (borrowed `&'a` fields, no `Clone` cascade; the `@type` injected on the nested `endpoint_config` body; the `LoadAssignment` `endpoints` reused VERBATIM so the serialization is byte-identical to the inline-CLA shape). Emitted shape: `{ "@type": ".../EndpointsConfigDump", "static_endpoint_configs": [ { "endpoint_config": { "@type": ".../ClusterLoadAssignment", "cluster_name": "...", "endpoints": [...] } } ] }` — NO `version_info`/`last_updated` (L5).
  - **The conditional push** (`render_config_dump`, AFTER the `Clusters` block / BEFORE the `Listeners` block): collects `static_endpoint_configs` from `bootstrap.all_clusters()` (the SAME static+dynamic accessor the existing `Clusters` block's surroundings use) filtered to `cluster_type == ClusterType::Eds` and `load_assignment.is_some()`; `configs.push(ConfigDumpEntry::Endpoints { … })` ONLY when the vec is non-empty (conditional emission — fixtures 0014/0026/0027/0028, which carry NO EDS cluster, are untouched). With `cds_config` present it lands at `configs[2]` (Bootstrap[0]/Clusters[1]/Endpoints[2]); with no cds at `configs[1]`. envoy-rust emits it unconditional of `?include_eds` (a recorded L5 narrowing); the per-side index mismatch is reconciled in the harness (Tasks 6/7).
  - **`EdsClusterConfig` re-export ADDED** at the `envoy_config` crate root (`crates/envoy-config/src/lib.rs`, in the `pub use bootstrap::{…}` list — the trivial in-scope re-export the Task-4 note flagged). Not strictly needed by the Task-5 tests (they parse `Cluster` from YAML, not via struct literals), but it closes the Task-1 omission cleanly while the file was open; `cargo fmt` reflowed the import block.
- **TDD:** the 4 `endpoints_config_dump_tests::*` tests (new colocated test module, mirroring `clusters_config_dump_tests`/`listeners_config_dump_tests` helper shape) written FIRST and confirmed RED (3 failed: query-strip `dispatch` not matching, `EndpointsConfigDump entry present` panic, ordering `left: None right: Some(2)`; the inertness test passed pre-implementation since nothing was emitted yet) BEFORE implementing, then GREEN: (a) `query_string_strips_to_config_dump` — `dispatch("GET", "/config_dump?include_eds")` → `Endpoint(ConfigDump)`, bare `/config_dump` still resolves, `/nope?x=1` still 404s; (b) `eds_cluster_emits_endpoints_config_dump` — a `type: EDS` cluster `eds_backend` (load_assignment populated, numeric IP) → an entry whose `@type` ends `EndpointsConfigDump` with `static_endpoint_configs[0].endpoint_config.{@type==ClusterLoadAssignment, cluster_name==eds_backend, endpoints[0]…address==127.0.0.1}`, NO `version_info`/`last_updated`; (c) `non_eds_clusters_emit_no_endpoints_config_dump` — only STATIC+STRICT_DNS clusters ⇒ NO EndpointsConfigDump entry (single Bootstrap entry); (d) `endpoints_entry_orders_after_clusters` — cds+EDS ⇒ `configs[2]`, no-cds+EDS ⇒ `configs[1]`.
- **Verification outputs:**
  - `cargo test -p envoy-admin` → `test result: ok. 95 passed; 0 failed; 0 ignored` (91 prior + 4 new); the 4 existing config_dump / clusters / listeners config-dump test modules all stay green (regression-equivalence — the new entry only appears when some cluster is EDS).
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings` → clean (exit 0, 0 warnings).
  - `cargo fmt --all` → applied (reflowed the `pub use bootstrap::{…}` import block in `lib.rs` for the added `EdsClusterConfig`; no other reformatting).
  - `cargo build --workspace --all-targets` → Finished (exit 0).
