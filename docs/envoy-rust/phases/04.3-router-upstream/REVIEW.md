# Phase 04.3 REVIEW — Upstream HTTP/1.1 origination + router proxy arm + fixture 0008 + http1-echo-server helper + Cluster::name() close-out

- **Base:** `04163c5` (parent of 04.3's state-2 PLAN.md commit `c02eea7`; the 04.2 state-6 close-out — last commit before any 04.3 work).
- **Head:** `cb0949e` (state-4 phase-done gate verification — `phase 04.3: state-4 phase-done gate verification (task 17)`).
- **Files:** 29 changed (+7495 / -85). Net Rust delta is approximately +1900 LoC (matches SPEC §5's ~1490-LoC point estimate plus modest sibling-module factoring); the headline figure is inflated by the 4228-line `PLAN.md` and 330-line `PROGRESS.md`. New library surface lands in `crates/envoy-http1/src/client.rs` (551 LoC = ~290 impl + 260 tests, including the chunked-encoding response reader new in 04.3 + 8 unit tests) and `crates/envoy-http1/src/router.rs` (285 LoC = ~155 impl + 130 tests, `RouterError` + `HCM_EMITTED_HEADERS` + `X_ENVOY_UPSTREAM_SERVICE_TIME` const + `write_proxied_response` + 4 tests). Schema additions land in `crates/envoy-config/src/bootstrap.rs` (+367 / 0 net: `RouteAction` enum tagged-union with `DirectResponse` (04.1) + `Route(RouteAction_Route)` (04.3) variants, hand-rolled `impl<'de> Deserialize<'de> for Route` visitor mirroring 04.2's `HeaderMatcher` pattern, validator `RouteAction::Route` arm reusing 02.1-landed `ConfigError::UnknownCluster`, 5 parse-shape tests, 3 validator tests, 1 corpus-walk extension), and `crates/envoy-config/src/lib.rs` (+10 / -2 net: re-exports `RouteAction`, `RouteAction_Route`; generalized `UnknownCluster` Display string from "tcp_proxy filter references unknown cluster" → "unknown cluster"). HCM grows substantially in `crates/envoy-http1/src/hcm.rs` (+522 / -7 net: `HCMConfig.cluster_mgr: Arc<envoy_cluster::ClusterManager>` field + `from_config` signature lift + `BuildOutcome::{Synth, Proxy}` enum + `serve_connection` Proxy-arm dispatch with `pick_endpoint`/`Client::connect`/`send_request` + `tracing::warn!` cluster-attributed error logs + outgoing-request `Connection`+`Transfer-Encoding` strip, plus 8 new tests). `crates/envoy-cluster/src/cluster.rs` gains `pub fn Cluster::name(&self) -> &str` and `pub fn ClusterHandle::name(&self) -> &str`, with `#[allow(dead_code)]` removed from `Cluster.name`, plus 3 unit tests — closes the multi-phase carryforward originating in phase-02.1 REVIEW M1. `crates/envoy-http1/src/error.rs` gains 3 variants (`UpstreamConnect { addr, source }`, `MalformedResponseLine`, `MalformedChunkedFraming`) + 1 Display test. `crates/envoy-http1/src/codec.rs` gains `Request.body: Option<bytes::Bytes>` + `body_len_estimate` / `body_len_string` / `body_bytes` helpers. `crates/envoy-http1/src/headers.rs` gains `TRANSFER_ENCODING` const. New workspace member `tests/helpers/http1-echo-server/` (Cargo.toml + 385-LoC src/main.rs with argv parser + accept loop + deterministic echo body + 5 tests). Differential harness extensions in `tests/differential/src/backend.rs` (+183 LoC: `Http1EchoBackend` + `locate_http1_echo_server` + `Drop` + 3 tests) and `tests/differential/src/lib.rs` (+69 / -0 net: `HEADER_ALLOW_LIST` extended with `x-envoy-upstream-service-time`; `run_fixture` dispatch cascade extended with `{{HTTP1_BACKEND_PORT}}` arm; 1 cross-module dispatch test). New 20-LoC Docker-gated acceptance test `tests/differential/tests/http1_router_upstream.rs` and 245-LoC in-process backstop `crates/envoy-bin/tests/http1_router_upstream.rs`. Five-file fixture `tests/fixtures/0008-http1-router-upstream/`. New 33-line fuzz seed `crates/envoy-config/fuzz/corpus/parse_bootstrap/hcm_route_to_cluster.yaml` plus its `.gitignore` allow-list extension. `BEHAVIOR_CONTRACT.md` Header allow-list table gains the `x-envoy-upstream-service-time` row (in lockstep with the in-code constant). `crates/envoy-bin/src/main.rs` gains a 2-line wiring change at the HCM dispatch arm to thread `Arc::clone(&cluster_mgr)` into `HCMConfig::from_config`. **Cargo.lock** synced inline at Task 11 (workspace member addition) — no Cargo.lock changes accumulated through Tasks 13–16, no dedicated tail commit needed.
- **Reviewed:** 2026-05-01.
- **Verdict:** **Approved with M-track follow-ups** — state 5 complete. No blocking issues in the 04.3 surface itself. The Docker-gated `host.docker.internal`/`STATIC` regression flagged in PROGRESS Task 17 is **NOT a 04.3 regression** (it originates at phase-02.2's ADR-0015 landing and has been latent across five phases; fixture 0008 inherits the broken pattern); it is appropriately deferred to a dedicated post-04.3 fixture-hardening sub-phase rather than blocking close-out. The state-4 gate is green on first attempt (locally re-verified at HEAD `cb0949e` — see "State-4 gate evidence" below) and the in-phase review-fix discipline (Tasks 2/8/9/11/12/13/14/16) closed eight well-targeted issues before they propagated.

---

## 1. Summary

Phase 04.3 closes parent phase 04 (HTTP/1.1 data plane) by landing upstream origination, the router-filter "proxy to cluster" arm, a deterministic `http1-echo-server` helper crate, fixture 0008, and the multi-phase `Cluster::name()` carryforward that has been re-deferred from phase-02.1 through phase-04.2. The implementation reads as a careful continuation of the 04.1 + 04.2 posture rather than a re-architecture: every doctrinal axis, every public-surface boundary, and every harness invariant from earlier phases is preserved. The new `Client` module at `crates/envoy-http1/src/client.rs` (551 LoC) introduces the workspace's first use of `httparse::Response::parse` (the 04.1 codec uses `httparse::Request::parse`); the HCM router invocation site at `crates/envoy-http1/src/hcm.rs:189-288` extends from the 04.1 one-arm `match action` to a two-arm match (`DirectResponse` unchanged; new `Route` arm dispatches into `cluster_mgr.get → pick_endpoint → Client::connect → send_request → router::write_proxied_response`); the new `crates/envoy-http1/src/router.rs` factors `RouterError` + `HCM_EMITTED_HEADERS` + `write_proxied_response` into a sibling module per SPEC §6 signpost 19's "either inline or sibling is fine"; the helper crate `tests/helpers/http1-echo-server/` ships with `#![forbid(unsafe_code)]` and consumes `envoy-http1` for codec + response-writer reuse (so `httparse` stays sole-deps under `envoy-http1`); the differential harness gains `Http1EchoBackend` + `locate_http1_echo_server` + the `{{HTTP1_BACKEND_PORT}}` dispatch arm.

The work reads cleanly against doctrine on every axis I checked. **D-3.1 TDD discipline**: each implementation task in PROGRESS.md narrates tests landing alongside or RED-first ahead of impl. Per-task delta: Task 1 +5 (RouteAction parse-shape), Task 2 +3 (validator), Task 3 +0 (corpus walk just extends), Task 4 +1 (UpstreamConnect Display), Task 5 +2 (connect happy/refused), Task 6 +4 (send_request CL), Task 7 +2 (chunked reader), Task 8 +3 + review-fix +1 (router), Task 9 +5+3 (envoy-http1 HCM + envoy-cluster), Task 9 review-fix +1 (transfer-encoding strip), Task 11 +4 (argv), Task 12 +1 (round-trip), Task 13 +4 (harness backend + dispatch), Task 14 +1 (in-process backstop), Task 15 +1 (Docker-gated). Test-count delta from 04.2 close (275 + 1 ignored): **+39 passed, total 314 + 1 ignored**, locally re-verified (52 differential lib + 19 envoy-bin + 11 envoy-cluster + 139 envoy-config + 43 envoy-http1 + 6 envoy-listener + 11 envoy-tcp + 15 envoy-tls + 5 http1-echo-server + 8 tcp-echo-server + 5 tls-echo-server = 314; the 1 ignored is the pre-existing 02.2 TcpProxyBackend smoke). **D-3.2 permitted foundations**: zero new dep additions; the `envoy-cluster` path-dep on `envoy-http1` (carried since 04.1 as M3) is now structurally consumed via `HCMConfig.cluster_mgr: Arc<envoy_cluster::ClusterManager>` (`crates/envoy-http1/src/hcm.rs:31`). The new helper crate's deps are all pre-existing permitted foundations (`envoy-http1`, `bytes`, `tokio`, `anyhow`, `thiserror`, `tracing`, `tracing-subscriber`). **D-3.4 PROGRESS cold-readability**: PROGRESS.md is dense (330 lines) but every task entry carries Commit / Change / Verification / Deviation sections; deviations are itemized and named. Eight in-phase review-fix follow-up subsections (Task 2 review fix, Task 8 review fix, Task 9 review fix, Task 11 review fix, Task 12 review fix, Task 13 review fix, Task 14 review fix, Task 16 review fix) preserve the audit trail. **D-3.5 append-only ADRs**: `git diff 04163c5..cb0949e -- docs/envoy-rust/DECISIONS.md` is empty (ADR ledger head remains 21; no new ADRs anticipated by SPEC §7, and none surfaced during execution). **D-3.6 green-build first-attempt**: PROGRESS Task 17 reports the state-4 phase-done gate clean on first attempt; locally re-verified at HEAD `cb0949e` all five gate commands exit 0 with no fix-during-gate commits. **D-3.7 Envoy pin / D-3.9 toolchain pin**: both empty diffs over the review range. **D-3.8 unsafe-code forbid**: verified `#![forbid(unsafe_code)]` at line 1 of the new crate root `tests/helpers/http1-echo-server/src/main.rs:1`. The new modules `crates/envoy-http1/src/client.rs` and `crates/envoy-http1/src/router.rs` inherit the crate-root forbid via the existing `crates/envoy-http1/src/lib.rs:1` attribute (Rust's `#![forbid]` propagates to submodules). No other new crates were added.

SPEC §3 deliverables D1–D5 + the `Cluster::name()` close-out (D5 in 04.3-SPEC numbering) all land in the described shape, with several well-disclosed plan-time deviations. **D1** (per-connection `Client`) ships exactly the public surface SPEC §3 D1 enumerates: `Client::connect(SocketAddr, &str)` at `crates/envoy-http1/src/client.rs:33-46`; `ClientStream::send_request(Request) -> Response` at `client.rs:69-221`; chunked-encoding response reader at `client.rs:234-337` (handles hex `chunk-size` lines with optional `;ext` per RFC 7230 §4.1, fixed `chunk-size CRLF chunk-data CRLF` framing, zero-size chunk termination with optional trailer-skip, READ_TIMEOUT 30s on every read). 8 unit tests cover happy-connect, refused-port, request-bytes-on-wire (with case-insensitive Host de-dup), explicit-Host wins over connect-host (case-insensitive), CL response body, chunked response body, malformed response line, malformed chunk size. **D2** (router proxy arm) ships `RouteAction::Route(RouteAction_Route { cluster: String })` schema at `bootstrap.rs:325-348`, the validator extension reusing `ConfigError::UnknownCluster` at `bootstrap.rs:1131-1147`, the HCM two-arm match at `hcm.rs:370-376`, the `BuildOutcome` enum at `hcm.rs:311-314` separating Synth from Proxy, the `Proxy` arm dispatch at `hcm.rs:193-287` (including the 503/502 synth-status fallbacks for `pick_endpoint`-None and `Client::connect`-Err / `send_request`-Err, plus `tracing::warn!` log lines naming `cluster.name()` and `addr` per D5's per-cluster-attribution rationale), the new `RouterError` enum at `router.rs:14-41`, the `write_proxied_response` helper at `router.rs:75-154` applying the HCM_EMITTED_HEADERS overwrite + transfer-encoding strip + connection authoritative-set + x-envoy-upstream-service-time append. **D3** (`http1-echo-server` helper crate) ships sibling-shape with `tcp-echo-server` and `tls-echo-server` (argv parser with `--port`/`--help`/`--version`, accept loop with `JoinSet` + `tokio::select!` ctrl-c handling, drain-budget, exit codes 0/1/2). The deterministic echo body at `tests/helpers/http1-echo-server/src/main.rs:210-244` correctly alphabetically-sorts headers by lowercase name (the LOAD-BEARING property per SPEC §3 D3 for differential equivalence) and emits the worked-example body shape byte-exact (`method: ...\npath: ...\nheaders:\n  ...: ...\nbody: ...\n`). **D4** (Http1EchoBackend + fixture 0008 + harness wiring) ships the backend struct + `locate_http1_echo_server` + `container_host()` returning `"host.docker.internal"` per ADR-0015, with SIGKILL-on-Drop. Fixture 0008 ships all 5 files (envoy.yaml, envoy-rust.yaml, expectations.yaml, payload.bin, README.md). The `request_headers_to_remove` + `generate_request_id: false` divergence in `envoy.yaml` (vs. `envoy-rust.yaml` which carries neither) is a well-disclosed mechanical adaptation: Envoy v1.33 by default injects 6 request headers (x-forwarded-for, x-forwarded-proto, x-request-id, x-envoy-expected-rq-timeout-ms, x-envoy-internal, x-envoy-external-address) which would land in the helper's deterministic-echo body and break byte-equivalence; envoy-rust does not inject these in 04.3 (parent SPEC §4 deferral); stripping on the Envoy side neutralizes the divergence. The README.md ("Header-injection divergence" section) names the rationale and cross-references SPEC §3 D4's "per-side YAML divergence is acceptable" precedent. **D5** (the multi-phase `Cluster::name()` close-out) closes cleanly at Task 9 commit `3fdf960`: `pub fn Cluster::name(&self) -> &str` lands at `crates/envoy-cluster/src/cluster.rs:24-26` (note: visibility lifted from the SPEC's `pub(crate)` to `pub` because the consumers — RouterError construction sites in `envoy-http1` — live in a different crate, per the SPEC's explicit allowance); `ClusterHandle::name(&self) -> &str` lands at `cluster.rs:60-62` delegating to the inner `Cluster`; the field-level `#[allow(dead_code)]` on `Cluster.name` was removed (now genuinely consumed). The 3 unit tests `cluster_name_returns_configured_name`, `cluster_handle_exposes_name`, and `cluster_name_outlives_borrow_correctly` (`crates/envoy-cluster/src/cluster.rs:427-452`) pin the accessor's surface. The router-arm `tracing::warn!` log lines at `hcm.rs:208`, `hcm.rs:248`, `hcm.rs:265` use `cluster.name()` directly — proving the accessor is load-bearing per SPEC §3 D5's rationale.

SPEC §6 signposts are honored. **Signpost 1** (HCM router invocation site placement in `crates/envoy-http1/src/hcm.rs`): yes, `BuildOutcome` + Proxy-arm dispatch at `hcm.rs:189-288`. **Signpost 2** (`envoy-http1` is sole workspace dep on `httparse` for runtime use): the new `httparse::Response::parse` use site is inside `crates/envoy-http1/src/client.rs:127`; no other production crate calls `httparse::*` for runtime parsing. (Caveat: `crates/envoy-bin/Cargo.toml:20` and `tests/differential/Cargo.toml:14` carry `httparse = "1"` at HEAD — but both are pre-existing from before 04.3 and are well-known scoped uses: envoy-bin's admin endpoint at `crates/envoy-bin/src/admin.rs:173-174` plus its existing test scaffolding, and the differential harness's response-parsing in `drive_http1` / `drive_http_get` in `tests/differential/src/lib.rs:596-751`. These are documented in the 04.1 REVIEW §1 as "the documented 'admin code routes through envoy-http1 when admin is next touched' posture"; 04.3 does NOT regress this — the new use site is correctly inside envoy-http1.) **Signpost 5** (`Host:` header forwarding posture): the router-proxy arm at `hcm.rs:220-222` captures the downstream request's `Host:` via `find_header(&req.headers, headers::HOST).expect(...)` (the `.expect` is defense-in-depth — `build_response` rejects missing/empty Host with synth_400 before the Proxy outcome is ever constructed; Task 9 review-fix tightened this from `.unwrap_or("")` to the typed expect). **Signpost 6** (`x-envoy-upstream-service-time` measurement window): `start = Instant::now()` at `hcm.rs:243` immediately before `Client::connect`; `elapsed_ms = start.elapsed().as_millis()` at `hcm.rs:278` immediately after `send_request` returns the parsed response; the integer ms value is forwarded to `write_proxied_response` and emitted as the value of the `x-envoy-upstream-service-time` header. **Signpost 7** (header re-emit policy in `write_proxied_response`): the 5-step shape lands cleanly at `router.rs:84-145` — start with the upstream's status; for each upstream header, replace `server` and `date` with envoy-rust's values (or inject defaults if absent), drop `connection` and `transfer-encoding` authoritatively, pass other headers through with the name lowercased; append `x-envoy-upstream-service-time`; set `Connection:` per posture; forward the body. **Signpost 8** (alphabetic header sort in helper response is LOAD-BEARING): yes, `tests/helpers/http1-echo-server/src/main.rs:219-224` builds a `sorted_headers: Vec<(String, String)>` via `sort_by(|a, b| a.0.cmp(&b.0))` after lowercasing names. **Signpost 9** (helper is single-shot plaintext, no keep-alive): yes, `handle_connection` at `tests/helpers/http1-echo-server/src/main.rs:135-191` writes a `Connection: close` response and then `stream.shutdown()` and returns. **Signpost 14** (one-shot Client; no pooling): yes, `Client::connect` returns a `ClientStream` consumed by a single `send_request` then dropped; no `close()` method, no reuse contract. **Signpost 16** (`Cluster::name()` close-out): yes (per D5 above). **Signpost 17** (HCM_EMITTED_HEADERS as a const): yes, `router.rs:48` `pub const HCM_EMITTED_HEADERS: &[&str] = &["server", "date"];`. **Signpost 18** (Cargo.lock cadence): inline at Task 11 — the workspace-member addition forced a lock mutation at scaffold time; consistent with the 04.1 / 04.2 inline-at-scaffold pattern (M5 carryforward addressed once again by inheriting the inline cadence). **Signpost 19** (`x-envoy-upstream-service-time` allow-list addition in lockstep with the BEHAVIOR_CONTRACT.md edit): both edits land at the same Task 10 commit `cdd0218`; verified `BEHAVIOR_CONTRACT.md:45` row matches `tests/differential/src/lib.rs:189-193` `HEADER_ALLOW_LIST` constant entry exactly.

State-4 gate evidence is solid. PROGRESS Task 17 (lines ~256-308) reports gate-clean on first attempt; locally re-verified at HEAD `cb0949e` (2026-05-01):

- `cargo build --workspace --all-targets` → exit 0, `Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.09s`.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` → exit 0, `Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.11s`, no warnings.
- `cargo fmt --all -- --check` → exit 0, no output.
- `cargo test --workspace --lib --bins` → **314 passed, 0 failed, 1 ignored** across 11 result lines (52 + 19 + 11 + 139 + 43 + 6 + 11 + 15 + 5 + 8 + 5 = 314; the 1 ignored is differential's pre-existing 02.2 TcpProxyBackend smoke). Per-crate deltas vs 04.2 close (275): envoy-config 131 → 139 (+8), envoy-cluster 8 → 11 (+3), envoy-http1 24 → 43 (+19), differential 48 → 52 (+4), http1-echo-server 0 → 5 (NEW). Total +39 passed; matches PROGRESS Task 17 exactly.
- `git diff 04163c5..cb0949e -- docs/envoy-rust/{DECISIONS.md,ENVOY_TARGET.md,STATE.md,ROADMAP.md} rust-toolchain.toml` → empty (all five frozen-or-state-6-deferred artifacts unchanged at HEAD; STATE.md and ROADMAP.md flip in the state-6 phase-done commit, not state-4).

The Docker-gated `http1_router_upstream_fixture` and the pre-existing fixtures 0003/0004/0005/0006/0007 acceptance tests are skipped by `cargo test --workspace --lib --bins` (they live under `tests/differential/tests/` as `--test` binaries, not `--lib --bins`). PROGRESS Task 17's pre-existing-systemic-regression finding (lines ~310-322) on `host.docker.internal` + `STATIC` parse-rejection by Envoy v1.33 is a separate concern — see §3 Important / §4 below.

The executor's self-audit discipline is excellent. PROGRESS narrates ~30 deviations across 17 tasks, every one named and rationalized. Task 1 documents the `Clone` derive on `DataSource` (in addition to `DirectResponse`) needed to keep `RouteAction` cloneable, the `#[allow(non_camel_case_types)]` on `RouteAction_Route` for the deliberate Envoy schema mirror, and the validator dispatch update. Task 2 review-fix documents the well-disclosed generalization of the `UnknownCluster` Display string from "tcp_proxy filter references unknown cluster" to "unknown cluster" (lossy on the referrer but no longer misleading on HCM-only configs). Task 5 documents the `#[allow(dead_code)]` on `ClientStream`'s unread fields (suppressed until Task 6's send_request consumes them) and the `#[derive(Debug)]` add forced by `expect_err`'s `Debug` requirement. Task 6 documents the `#[allow(dead_code)]` on the three `Request` body helpers (consumed by send_request only, until Task 9's HCM Proxy arm calls them in production). Task 7 documents the clippy-driven removal of an unreachable `pos += 2` and the rustfmt reflow. Task 8 documents the well-justified lowercase-normalization on the pass-through branch (the test asserted lowercase egress; verbatim case would have failed). Task 8 review-fix documents the C1 critical fix (transfer-encoding strip, RFC 7230 §3.3.3 rule 3 violation) and the I1/I2 doc-comment + test cleanups. Task 9 documents the BuildOutcome::Proxy `cluster: String` shape (PLAN's anticipated fallback over the borrowed-`&'a RouteAction_Route` form) and the test fixture YAML pivot from `node:` to `admin:` runtime-presence anchor. Task 9 review-fix documents the Host invariant tightening from `.unwrap_or("")` to `.expect(...)` and the symmetric outgoing-request `Transfer-Encoding` strip. Task 11 review-fix preserves the WHY-comment on `ArgvError` and labels the `DRAIN_BUDGET` placeholder. Task 12 review-fix factors `handle_connection` out from a 6-level-deep inline closure to a named function and hoists file-scope `use` statements + adds `READ_TIMEOUT` const. Task 13 review-fix tightens render-substitution assert from two `contains` to `assert_eq!` and `contains` → `starts_with` for the status-line check. Task 14 documents the LoC overage (247 vs SPEC's "~120") attributable to mirroring the sibling `http1_direct_response.rs` boilerplate exactly. Task 14 review-fix trims the WHAT-narrating prefix on the `spawn_http1_upstream` doc-comment per D-3.7 minimalism. Task 15 documents 4 well-disclosed deviations from SPEC §3 D4's worked example: (1) `expectations.yaml`'s shape vs SPEC's `equivalence.response_headers` (the actual Rust struct rejects that key under `deny_unknown_fields`; the per-driver `expected_headers` field is what the harness reads), (2) `request_headers_to_remove` on Envoy side only, (3) `generate_request_id: false` on Envoy HCM only, (4) `payload.bin` is empty (Driver::Http1 constructs the request from driver fields rather than reading payload.bin). Task 16 audits the M-track close-outs and earlier-phase carryforwards. Task 16 review-fix corrects two factually-wrong items (M6 glob description; #14 missing destination phase). Every deviation is well-justified, documented in PROGRESS.md, and inline-commented at the affected sites. The 8 review-fix commits (Tasks 2, 8, 9, 11, 12, 13, 14, 16) are particularly clean — small, single-purpose, with PROGRESS.md follow-up subsections rather than overwrites of the original task entries.

---

## 2. Strengths

- **Doctrine conformance end-to-end.** `#![forbid(unsafe_code)]` at line 1 of the only new crate root `tests/helpers/http1-echo-server/src/main.rs:1`. The new `client.rs` and `router.rs` modules inherit the crate-root forbid via the existing `crates/envoy-http1/src/lib.rs:1` attribute. Zero new dep additions to the allow-list; the new helper crate's deps (`envoy-http1`, `bytes`, `tokio`, `anyhow`, `thiserror`, `tracing`, `tracing-subscriber`) are all pre-existing permitted foundations. Append-only ADR ledger preserved exactly (`git diff 04163c5..cb0949e -- docs/envoy-rust/DECISIONS.md` empty; ADR ledger head remains 21). Toolchain pin untouched. Envoy version pin untouched.

- **`Client::send_request` correctly handles every architectural seam SPEC §3 D1 names.** `crates/envoy-http1/src/client.rs:69-221`. Case-insensitive `Host` de-dup (`client.rs:79-87` — request's explicit Host wins over connect-time captured host); case-insensitive `Content-Length` de-dup (`client.rs:95-103`); CL framing on the request body via `body_len_estimate` + `body_bytes`; httparse `Response::parse` driven response-header read with 8 KiB cap (RESPONSE_HEADERS_CAP) enforced both on `Status::Partial` (line 195-200) and on `httparse::Error::TooManyHeaders` (line 213-217) — defense-in-depth against attackers crafting a 65-row response under the byte cap; exhaustive `httparse::Error` variant mapping (Token/Version/Status → MalformedResponseLine; HeaderName/HeaderValue/NewLine → MalformedHeader; TooManyHeaders → HeadersTooLarge); Transfer-Encoding/Content-Length detection (`client.rs:141-144` is case-insensitive on both name and value); 30s `READ_TIMEOUT` on every wire read.

- **Chunked-encoding response reader correctly handles the RFC 7230 §4.1 surface.** `crates/envoy-http1/src/client.rs:234-337`. (a) Hex `chunk-size` line read via `find_crlf` + `usize::from_str_radix(_, 16)`; non-hex fails with `MalformedChunkedFraming` (line 268); optional `;ext` chunk-extensions handled by `split(';').next()` (line 266). (b) Exactly `chunk_size` body bytes + 2 trailing CRLF read with explicit byte-equality check on the trailing CRLF (line 332-334) — defense against framing violations. (c) Zero-size chunk termination (line 272-318) with an optional trailer-skip path: if the next two bytes are not CRLF, read until empty CRLF line — defensive against the SPEC §4 trailer-deferral. (d) READ_TIMEOUT (30s) bounds slow-upstream attacks on every read (lines 252-258 / 285-291 / 299-309 / 322-329); read-zero-bytes-EOF in any of these paths surfaces as `MalformedChunkedFraming`. (e) The `_already` parameter is unused (the `pos` cursor inside the function does the work; the parameter is held for caller-site clarity) — clippy-clean as `_-prefixed` suppressed dead-code. `send_request_reads_chunked_response_body` (`client.rs:521-532`) and `send_request_returns_malformed_chunked_on_bad_size_line` (`client.rs:534-550`) pin the happy and bad-size paths.

- **HCM Route arm dispatches through `BuildOutcome` separation cleanly.** `crates/envoy-http1/src/hcm.rs:189-288`. The route-walker function `build_response` (lines 316-376) is pure (synchronous, takes `&HCMConfig + &Request + bool`, returns the enum) and decides Synth-vs-Proxy without performing any I/O. The async `serve_connection` is the I/O layer that dispatches the outcome. This separation is correct and testable: 5 of the 6 SPEC-named tests at `hcm.rs::tests` exercise both paths via the dispatch site rather than mocking `BuildOutcome` directly. The Proxy arm correctly: (a) validates the cluster is present (`.expect("validator ensures cluster present")` defense-in-depth at line 199-202); (b) handles the empty-cluster case via 503 + `tracing::warn!` (line 204-218); (c) handles connect-failure via 502 + tracing::warn naming `cluster.name()` and `addr` (line 244-260); (d) handles send-failure via 502 + tracing::warn (line 261-277); (e) measures elapsed_ms across the connect+send window; (f) writes via `router::write_proxied_response`. The `if close { return Ok(()) } continue;` posture (line 213-216, 255-258, 272-275) correctly handles both keep-alive (request another loop iteration) and close (terminate the connection).

- **Outgoing-request hop-by-hop header strip is RFC-compliant and symmetric with the response-side.** `crates/envoy-http1/src/hcm.rs:228-232`. The Task 9 review-fix extended the existing `Connection:` strip to include `Transfer-Encoding:` per RFC 7230 §3.3.3 (T-E + CL combo forbidden); the 04.3 outgoing body is forced to `Bytes::new()` (CL: 0; chunked-request-forwarding is a SPEC §4 non-goal), so without the strip the outgoing wire would carry both `transfer-encoding: chunked` and `content-length: 0` — RFC 7230 §3.3.3 rule 3 violation. The PROGRESS Task 9 review-fix entry explicitly cites this and notes the chunked-request 501-reject at `hcm.rs:153` already catches `T-E: chunked` from downstream, so the strip is "defensive symmetry against a future relaxation." `proxy_strips_transfer_encoding_from_outgoing_request` test at `hcm.rs::tests` is the regression guard (TDD-disciplined: RED before strip, GREEN after).

- **`write_proxied_response` correctly applies the 5-step shape SPEC §6 signpost 7 names.** `crates/envoy-http1/src/router.rs:75-154`. (1) Status forwards verbatim. (2) Per-header dispatch: HCM_EMITTED_HEADERS (`server`, `date`) overwrite with envoy-rust's values; `connection` and `transfer-encoding` strip authoritatively; `content-length` preserve as-passed-by-upstream; other headers pass-through with name lowercased per RFC 7230 §3.2 case-insensitivity (this lowercase-normalization is a Task 8 well-disclosed deviation — ensures byte-equal egress shape with the rest of `Http1Response::write_to`). (3) Inject defaults for HCM_EMITTED_HEADERS the upstream did not carry (lines 121-127). (4) Inject `content-length: <body.len()>` if upstream did not carry one (post-chunked-decode body has known length; line 129-135). (5) Append `x-envoy-upstream-service-time: <elapsed_ms>` (line 136-140). (6) Authoritative `Connection:` per posture (line 142-145). The 4 unit tests `proxied_response_appends_x_envoy_upstream_service_time`, `proxied_response_overwrites_server_and_date_headers`, `proxied_response_sets_connection_per_posture`, and `proxied_response_strips_upstream_transfer_encoding` (`router.rs:183-284`) pin all four properties.

- **`http1-echo-server`'s alphabetic header sort is LOAD-BEARING and tested.** `tests/helpers/http1-echo-server/src/main.rs:210-244`. The body builder lowercase-normalizes header names then sorts by `String::cmp` (line 219-224). Both Envoy and envoy-rust forward the SAME logical request to the SAME helper; Envoy's HTTP/1.1 codec may emit headers in a different order than envoy-rust's; sorting in the helper neutralizes this so the byte-exact body equality holds across both proxies' proxied responses. The helper's body shape is rigorous: `method:`, `path:`, `headers:` block (each header indented 2 spaces + lowercase name + ": " + verbatim value + `\n`), `body: <body bytes as UTF-8 if possible else `?` per byte> + \n`. The `accepts_and_echoes_request` test at `tests/helpers/http1-echo-server/src/main.rs:326-384` pins the body shape byte-exact for a `GET / HTTP/1.1\r\nHost: x.test\r\nContent-Length: 0\r\n\r\n` request, asserting the response ends with `method: GET\npath: /\nheaders:\n  content-length: 0\n  host: x.test\nbody: \n`.

- **`Cluster::name()` close-out is well-shaped and removes the field-level `#[allow(dead_code)]`.** `crates/envoy-cluster/src/cluster.rs:24-26`: `pub fn name(&self) -> &str` on `Cluster`; `cluster.rs:60-62`: `pub fn name(&self) -> &str` on `ClusterHandle` delegating to inner. The visibility is `pub` rather than the SPEC's preferred `pub(crate)` because the consumer crate (`envoy-http1`'s RouterError construction sites) is different from `envoy-cluster` — SPEC §3 D5 explicitly authorizes the `pub` lift in this case. `Cluster.name` field is now genuinely consumed (no `#[allow(dead_code)]` annotation; verified in source). The router-arm `tracing::warn!` log lines at `crates/envoy-http1/src/hcm.rs:208`, `:248`, `:265` use `cluster.name()` directly — proving the accessor is load-bearing per SPEC §3 D5's rationale (per-cluster log attribution materially helps debugging). The 3 unit tests `cluster_name_returns_configured_name`, `cluster_handle_exposes_name`, `cluster_name_outlives_borrow_correctly` (`cluster.rs:427-452`) pin the accessor's surface including a borrow-check regression guard. The carryforward chain that ran phase-02.1 → 02.2 → 03.1 → 03.2 → 04.1 → 04.2 → 04.3 ends here (#12 in PROGRESS Task 16).

- **`HEADER_ALLOW_LIST` lockstep with `BEHAVIOR_CONTRACT.md` is exact.** `tests/differential/src/lib.rs:189-193` carries `("server", AllowMode::NameRequired)`, `("date", AllowMode::NameRequired)`, `("x-envoy-upstream-service-time", AllowMode::NameRequired)` in that order. `docs/envoy-rust/BEHAVIOR_CONTRACT.md:43-45` carries the same three rows in the same order. Task 10 commit `cdd0218` lands both edits atomically per SPEC §6 signpost 19's lockstep discipline. ADR-0011's "phase 01 defers response-header equivalence to phase 04" continues to close cleanly through 04.3.

- **Hand-rolled `Deserialize` for `Route` mirrors the 04.2 `HeaderMatcher` visitor pattern.** `crates/envoy-config/src/bootstrap.rs:367-450`. The `Route` struct is parsed via a `MapAccess` visitor that peers through the `match`, `direct_response`, and `route` keys at the route-map level, enforces mutually-exclusive direct_response/route oneof at parse time (via the inline `mode is_some()` guard), and rejects unknown top-level keys. The 5 parse-shape tests (`parses_route_with_direct_response_action`, `parses_route_with_route_action`, `rejects_route_with_both_direct_response_and_route`, `rejects_route_with_neither_direct_response_nor_route`, `rejects_route_with_unknown_top_level_key`) pin all 5 paths. The 3 validator tests (`parses_route_with_cluster_action`, `rejects_hcm_route_with_unknown_cluster`, `rejects_hcm_route_with_empty_cluster_name`) pin the validator's `RouteAction::Route` arm and `ConfigError::UnknownCluster` reuse.

- **`UnknownCluster` Display string generalization is the right scope.** Task 2 review-fix commit `d31e1a8` changed `crates/envoy-config/src/lib.rs:52` from `"tcp_proxy filter references unknown cluster '{0}'"` to `"unknown cluster '{0}'"`. Was: precise about the referrer (true on 02.1) but a half-truth on HCM-only configs (the variant is now thrown from both `TcpProxyConfig.cluster` and `RouteAction_Route.cluster` validation sites). After: lossy on the referrer but no longer misleading. The PLAN explicitly disclaimed the schema-breaking alternative (carrying a referrer field on the variant); the minimum-scope fix is a 1-line change. No tests assert on the literal string (all use `matches!` on the variant), so no test ripples.

- **`Http1EchoBackend` mirrors phase-02.2's `TcpProxyBackend` and phase-03.2's `TlsEchoBackend` posture exactly.** `tests/differential/src/backend.rs:170-265`. Same `start_kill` + 50ms-poll/2s-deadline SIGKILL-on-Drop pattern; same `pub(crate) fn locate_<name>` locator helper; same `container_host()` returning `"host.docker.internal"` per ADR-0015; same `wait_accept_ready` budget. The 3 backend tests (`http1_echo_backend_spawns_and_echoes`, `http1_echo_backend_drop_terminates_child`, `locate_http1_echo_server_returns_existing_path`) follow the established sibling test cadence with `eprintln!`-skip on missing-binary paths. The Task 13 review-fix tightened the render-substitution assert from two `contains` to `assert_eq!` and from `contains` to `starts_with` on the status-line — strictly more rigorous than necessary for the test's purpose, well-justified by reviewer feedback.

- **In-process integration backstop at `crates/envoy-bin/tests/http1_router_upstream.rs` mirrors phase-04.1's `http1_direct_response.rs`.** 245 LoC, no Docker dependency. Spawns an in-process tokio HTTP/1.1 upstream on `127.0.0.1:0`, spawns `envoy-bin` as a subprocess via `CARGO_BIN_EXE_envoy-bin` against an HCM YAML config with `route: { cluster: backend }` pointed at the in-process upstream, opens a plaintext TCP connection, writes `GET / HTTP/1.1\r\nHost: envoy-rust.test\r\n\r\n`, parses the response with httparse, asserts status 200 + 6 required headers (case-insensitive: server, date, content-length, content-type, connection, x-envoy-upstream-service-time) + content-length: 5 + body bytes `b"hello"`. The test is GREEN on first run because the exercised wiring landed at Task 9; the Task 14 backstop is a regression guard for that wiring (sibling shape to `http1_direct_response.rs` for the 04.1 direct_response arm).

- **In-phase review-fix discipline closed eight well-targeted issues before propagation.** Tasks 2, 8, 9, 11, 12, 13, 14, 16 each landed exactly one review-fix commit + a paired progress-note. Task 2 fix `d31e1a8` generalized the UnknownCluster Display string. Task 8 fix `da00861` was the most substantive: added `headers::TRANSFER_ENCODING` const + the response-side strip (RFC 7230 §3.3.3 rule 3 compliance) + a regression test. Task 9 fix `d8b2360` tightened the Host invariant from `.unwrap_or("")` to `.expect(...)` and added the symmetric outgoing-side T-E strip + a regression test. Task 11 fix `e8c6e12` restored a WHY-comment block + cleaned up two narrating-WHAT comments. Task 12 fix `d5131fb` extracted `handle_connection` from a 6-level inline closure + hoisted file-scope `use` + added `READ_TIMEOUT` const + dropped narrating comments. Task 13 fix `4071e69` tightened the render-substitution assert and the status-line check. Task 14 fix `c8926b8` trimmed a WHAT-narrating prefix on the `spawn_http1_upstream` doc-comment per D-3.7 minimalism. Task 16 fix `e4957cc` corrected M6's factually-wrong glob description and #14's missing carry-forward destination. Every review-fix is small (1-3 file edits each), single-purpose, and follows the `phase 04.3: <crate> — task N review fix (<summary>)` commit-message form. PROGRESS subsections are ADDED rather than overwriting the original task entries — preserves the audit trail.

- **PROGRESS.md cadence is consistent.** Every task has a section; review-fix follow-ups added subsections rather than overwriting; state-4 section quotes verification outputs verbatim; the Docker-gated regression finding (lines ~310-322) is quoted in full and explicitly handed off to the state-5 reviewer for the verdict choice.

- **CI gate cleared on first attempt.** PROGRESS Task 17 reports build/clippy/fmt/test all clean with no fix-during-gate commits. Local re-verification on HEAD `cb0949e`: all four commands exit 0 on first attempt with the test count matching exactly (314 + 1 ignored). Matches phase-04.2 / 04.1 / 03.2 / 03.1 / 02.2 / 02.1 first-attempt-green pattern.

- **Commit-message hygiene matches the established precedent.** Task commits follow `phase 04.3: <crate> — <summary> (task N)`; progress-note commits are `phase 04.3: progress note (task N)`; review-fix commits use `phase 04.3: <crate> — task N review fix (<summary>)`. No `[ADR-NNNN]` annotations on individual task commits — correct, since no ADRs landed in 04.3 (per SPEC §7).

---

## 3. Issues

### Critical

None.

### Important

**I1. Docker-gated systemic regression on `host.docker.internal` + `STATIC` cluster type affects fixtures 0003/0004/0005/0006/0008.** *(NOT a 04.3 regression; SURFACED by 04.3.)*

PROGRESS Task 17 lines ~310-322 documents this systemic regression in full: upstream Envoy v1.33.0 rejects the rendered `address: host.docker.internal` under `type: STATIC` with:

```
[critical][main] [source/server/server.cc:416] error initializing config '/etc/envoy/envoy.yaml':
malformed IP address: host.docker.internal. Consider setting resolver_name or setting cluster type
to 'STRICT_DNS' or 'LOGICAL_DNS'
```

The regression originates at phase-02.2's ADR-0015 landing (where `host.docker.internal` was introduced as the BACKEND_HOST substitution for cross-container reachability via `host-gateway`). Subsequent phases 02.2 / 03.1 / 03.2 / 04.1 / 04.2 did not push to CI between phase-02.1 close (run `24913934580`) and 04.3 task 14 (run `25106213773`), so the regression has been latent across **five phases**. Envoy v1.33's tightened `socket_address.address` parse semantics expect either a literal IP (under `STATIC`) or DNS resolution opt-in (under `STRICT_DNS` / `LOGICAL_DNS`). Fixing requires a coordinated edit across `tests/fixtures/{0003,0004,0005,0006,0008}/envoy.yaml` to either (a) flip to `type: STRICT_DNS` (plus matching schema-level support for `STRICT_DNS` in `crates/envoy-config/src/bootstrap.rs::ClusterType` enum which is currently single-variant `Static` at lines 60-62; verified) or (b) add `resolver_name: envoy.dns.cares` on the upstream-side socket_address. Both options carry envoy-config schema implications because `envoy-rust.yaml` mirrors the cluster-type field (verified at fixture 0003 / 0008 envoy-rust.yaml).

**My verdict choice on this finding: (b) "Approved with M-track follow-ups" — defer to a dedicated post-04.3 fixture-hardening sub-phase.** Rationale:

1. **Cross-phase scope.** The fix touches 5 fixtures spanning phases 02.2, 03.1, 03.2, 04.3 and requires schema growth in envoy-config (a new `ClusterType::StrictDns` variant + the validator's accept path + at minimum the per-cluster DNS-resolution stub in `envoy-cluster` to handle the runtime resolution — though for `STATIC` reachability via `host-gateway` the actual DNS resolution is local and trivial, the schema-and-validator surface is non-zero). Pulling this scope into a state-3 loop-back inside 04.3 would be doctrinally questionable: 04.3's SPEC explicitly does NOT include schema growth for new cluster types as a deliverable, and forcing it in via a state-3 loop would inflate the in-phase scope past the original 17-task / ~1490-LoC budget.
2. **The regression is not a 04.3-specific defect.** Fixture 0008 inherits the broken pattern uniformly with the four pre-04.3 fixtures; fixing 0008 alone in 04.3 would leave 0003/0004/0005/0006 broken the same way. The right shape is a coordinated fix across all five.
3. **Local stable-toolchain gate is green.** All unit-test surface (313 passes, 1 ignored) is clean. The Docker-gated suite is gated on `DOCKER=1` in CI; the regression does not block local development or the unit-test gate.
4. **Phase 04.3's SPEC acceptance signal §1(a)-(f) is partially achievable without a fix:** (a) the new fixture 0008 IS green at the unit-test level (parse + validate + in-process backstop all pass; only the Docker-gated cross-proxy differential is red, and it's red for the same reason 0003-0006 are red); (b) is partially red (0003/0004/0005/0006 also Docker-gated red); (c)/(d)/(e) are all clean per the local gate; (f) is what this REVIEW is producing.
5. **Doctrine fit.** The acceptance signal in §1 of the 04.3 SPEC was authored before the regression was discovered (latent at SPEC time); it presumed the Docker-gated state was preserved across the latent window. That presumption broke; the right doctrinal response is to acknowledge the cross-phase scope and track via M-track rather than overload 04.3's planned scope.

The recommended forward work is a dedicated post-04.3 fixture-hardening sub-phase (or roll into phase-05 brainstorm scope) that (1) adds `ClusterType::StrictDns` to the envoy-config schema with the necessary validator + cluster-construction support; (2) coordinated-edits the 5 affected fixtures to use STRICT_DNS where `host.docker.internal` is the backend host literal; (3) re-pushes to CI to confirm green Docker-gated runs across all 5 fixtures + 0008. See §4 carryforward "C-1" below.

The reviewer notes that an alternative verdict — "Approved with fixes" with state-3 loop-back — is defensible if the reviewer prefers minimum-cross-phase-bleed: under that posture, 04.3 stays open until all 5 fixtures (including 0008) are green Docker-gated, with the schema growth landing as a 04.3 in-phase task. I rejected this option for the reasons above (cross-phase scope, original-budget inflation, doctrinal fit). The choice is mine and is recorded here; downstream phases inherit (b).

### Minor

**M3-correction. Task 16's claim that M3 closed at Task 5 is structurally inaccurate; the actual closure is at Task 9.** *(Awareness-only; PROGRESS is accurate at the verdict level but imprecise on the proximate cause.)*

PROGRESS Task 16 line ~224 reads: "M3 ... CLOSED in 04.3 Task 5 (commit `c033e20`) where `envoy-cluster` was confirmed pre-staged in `crates/envoy-http1/Cargo.toml` and first activated by `Client::connect`'s use site." Verified in source (`crates/envoy-http1/src/client.rs` grep for `envoy-cluster`/`envoy_cluster`): the Client module has **zero** references to `envoy_cluster::*`. `Client::connect` takes a raw `std::net::SocketAddr` and `&str` host string (`client.rs:33-46`); `ClientStream::send_request` consumes only `crate::*` types. The actual structural consumption of `envoy-cluster` from `envoy-http1` is via `HCMConfig.cluster_mgr: Arc<envoy_cluster::ClusterManager>` at `crates/envoy-http1/src/hcm.rs:31` (Task 9, commit `3fdf960`), and the actual call sites are `cluster_mgr.get(&cluster_name)` at `hcm.rs:201` and `cluster.name()` / `cluster.pick_endpoint()` at `hcm.rs:204-218`. Task 5's commit message and PROGRESS entry are correct that the path-dep is *active in the build graph* by Task 5 (the `envoy-cluster = { path = ... }` entry in `Cargo.toml` was already present pre-04.3), but the *consumer* of that dep landed at Task 9, not Task 5. The verdict ("M3 is closed") is accurate; the proximate cause statement is imprecise.

*Fix:* leave as-is for 04.3 (PROGRESS is on disk; the imprecision is in a closed-in-04.3 audit subsection, not in code or in a forward-binding contract). Track forward as M-track item M3-correction in §4 below; if a future phase audits the M3 closure, the right citation is Task 9, not Task 5.

**M-claim. Task 16's "M6 practically closed" hedge is honest; readers should understand `drive_http1` per-function unit test never landed.** *(Awareness-only; PROGRESS Task 16 names this directly.)*

PROGRESS Task 16 line ~227 reads: "M6 ... PRACTICALLY CLOSED in 04.3. Task 6 / Task 9 added Task 6's 4 client::tests::send_request_* tests + Task 9's 5 HCM tests ... A strict per-function isolation unit test for drive_http1 in tests/differential/src/lib.rs::tests was never added (fixture 0008's Docker-gated test is the end-to-end gate for the harness-side dispatch). Mark as closed with the awareness that the per-function in-isolation unit test remains absent." Verified: `grep -n "fn drive_http1\|drive_http1(" tests/differential/src/lib.rs` shows the helper is consumed by `Driver::Http1` and `Driver::Http1ProbeList` dispatch arms in `run_fixture` only; no per-function unit test exercising `drive_http1` against an in-process tokio echo backend exists in `tests::*`. The Task 13 cross-module test `run_fixture_dispatches_http1_backend_on_template_marker` is a substitution check, not a `drive_http1` exerciser.

The hedged closure is acceptable because: (a) the helper is now exercised end-to-end via fixture 0008's Docker-gated test (when the Docker-gated regression I1 is fixed); (b) the helper is also exercised end-to-end via the Task 14 in-process backstop `crates/envoy-bin/tests/http1_router_upstream.rs` (which doesn't call `drive_http1` directly but does exercise the same response-parsing shape inlined); (c) `drive_http1`'s body is short (~80 LoC) and is a thin wrapper around well-tested deps. The reviewer notes the Docker-gated regression I1 currently masks the end-to-end exercise.

*Fix:* leave as-is for 04.3. Track forward as M6 carryforward (no longer "carry forward to 04.3" since 04.3 ended; redirected to whichever phase first adds a third Driver::Http1 consumer or the fixture-hardening sub-phase from I1).

**M-payload. `payload.bin` for fixture 0008 is empty (0 bytes), not the literal request bytes the SPEC §3 D4 worked example shows.** *(Awareness-only; intentional and correctly matches the harness behavior.)*

PROGRESS Task 15 deviation 4 documents this: SPEC §3 D4 lines 435-441 show `inputs/payload.bin` containing `GET / HTTP/1.1\r\nHost: envoy-rust.test\r\nContent-Length: 0\r\n\r\n`. The actual `Driver::Http1` arm in `tests/differential/src/lib.rs` calls `drive_http1` which constructs the request from the driver fields (`method`, `path`, `host`) — it does not read `payload.bin`. The fixture mirrors sibling fixture 0007's empty `payload.bin` to keep the directory structure uniform; the file content is moot for `Driver::Http1`. PROGRESS notes this explicitly. This is a SPEC-vs-reality drift introduced before 04.3 (the parent SPEC §6 signpost 10 named the wire-bytes-on-disk approach, but `Driver::Http1` was implemented at 04.1 to construct from fields instead). Fixture 0007 already followed the empty-payload.bin convention; 0008 inherits.

*Fix:* leave as-is. Track forward only if a future phase adds a `Driver::Http1Raw` (or similar) that reads `payload.bin` directly; if so, fixture 0008 may need to be amended to populate the file.

**M-spec-equiv. `expectations.yaml`'s SPEC vs reality drift.** *(Awareness-only; intentional and correctly named in PROGRESS.)*

PROGRESS Task 15 deviation 1 documents this: SPEC §3 D4 lines 443-460 shows `equivalence: { response_status, response_body, response_headers }` (3 keys) — but the actual `Equivalence` Rust struct at `tests/differential/src/lib.rs:95` carries `#[serde(deny_unknown_fields)]` and only has `response_status` + `response_body` fields; `response_headers` lives on the per-driver `expected_headers` field instead (per the working shape established at fixture 0007). Fixture 0008's `expectations.yaml` correctly carries `expected_headers: set_equal_modulo_allow_list` at the driver level and only the 2 `equivalence` keys at the equivalence level. The SPEC drift was anticipated in PROGRESS and is well-disclosed.

*Fix:* leave as-is. SPEC §3 D4 is now slightly stale on this point but is closed at this commit; no follow-up needed.

**M-payload-divergence. `request_headers_to_remove` + `generate_request_id: false` on Envoy side only is intentional and load-bearing.** *(Awareness-only; well-disclosed in fixture README + PROGRESS.)*

PROGRESS Task 15 deviations 2 and 3 document this: Envoy v1.33's HCM by default injects 6 request headers on the upstream-bound side (x-forwarded-for, x-forwarded-proto, x-request-id, x-envoy-expected-rq-timeout-ms, x-envoy-internal, x-envoy-external-address), plus a UUID `x-request-id` per request. envoy-rust does not inject these in 04.3 (parent SPEC §4 line 570 deferral). Without stripping them on the Envoy side, they would land in the helper's deterministic-echo body (which alphabetically sorts received headers and emits them verbatim in the body) and break envoy ↔ envoy-rust byte-equivalence. The `envoy.yaml` carries `route_config.request_headers_to_remove` listing all 6 + `generate_request_id: false` at the HCM level; `envoy-rust.yaml` carries neither (envoy-rust's `RouteConfiguration` and `HttpConnectionManagerConfig` both use `#[serde(deny_unknown_fields)]` and would reject either). Field-set divergence is intentional; mirrors fixture 0005's `admin:` / bind-address divergence per SPEC §3 D4 line 433. The fixture README's "Header-injection divergence" section names the rationale.

*Fix:* leave as-is. The right long-term resolution is to extend envoy-rust to emit the same headers (parent SPEC §4 line 570 names this as a "default plan (b)" for any header that surfaces during fixture development; (a) — extend envoy-rust — lands as a follow-on if production-realism demands it). Track forward to a future phase that adds these headers to envoy-rust's HCM emission set.

**M-architectural-claim. The "envoy-http1 sole workspace dep on httparse" rule has carve-outs at envoy-bin's admin endpoint and the differential harness, both pre-existing 04.3.** *(Awareness-only; well-known and not a 04.3 regression.)*

`grep "httparse" Cargo.toml` across the workspace shows three deps: `crates/envoy-http1/Cargo.toml:12` (the canonical owner), `crates/envoy-bin/Cargo.toml:20` (admin endpoint at `crates/envoy-bin/src/admin.rs:173-191`), and `tests/differential/Cargo.toml:14` (response-parsing in `drive_http1` and `drive_http_get` at `tests/differential/src/lib.rs:596-751`). The 04.1 REVIEW §1 (line 27) names this explicitly: "envoy-bin and envoy-config consume `envoy-http1`'s public types; ... the existing httparse entry continues to back the admin endpoint per the documented 'admin code routes through envoy-http1 when admin is next touched' posture." 04.3's new `httparse::Response::parse` use site is correctly inside `envoy-http1` (`client.rs:127`), not in envoy-bin or differential. The rule is preserved on the 04.3 NEW USE SITE axis.

*Fix:* leave as-is. The pre-existing carve-outs are tracked from 04.1 and forward; eventual consolidation would route the differential harness's response parser through envoy-http1's `Client::send_request` instead of inlining httparse — but that's well outside 04.3's scope.

---

## 4. Forward work (M-track)

Numbered M-items and carryforwards for the closing-out commit reference. None block state 6.

**Items closed in 04.3 (per Task 16 audit + this REVIEW's verification):**

- **M3** (envoy-http1's forward-looking envoy-cluster dep): structurally closed via `HCMConfig.cluster_mgr` field at Task 9 commit `3fdf960`. Note PROGRESS Task 16's proximate-cause attribution to Task 5 is imprecise (see §3 M3-correction); the closure verdict is accurate.
- **M6** (drive_http1 per-function unit test): "practically closed" — Task 6/9/13 added end-to-end exercise; the strict per-function in-isolation unit test was never added. Hedged closure noted in §3 M-claim above.
- **M10** (PLAN.md late-landing precedent): closed cleanly. `git log --oneline -- docs/envoy-rust/phases/04.3-router-upstream/PLAN.md` shows a single commit `c02eea7` (2026-04-27 16:43) predating Task-1 commit `313707f` (2026-04-27 17:15) — the 04.3 planner committed PLAN.md as a dedicated standalone commit before any task commits, breaking the 04.1 → 04.2 inline-PLAN precedent. Verified at this REVIEW.
- **#12** (`Cluster::name()` accessor, originating phase-02.1 REVIEW M1): closed cleanly at Task 9 commit `3fdf960`. `pub fn Cluster::name(&self) -> &str` lands; field-level `#[allow(dead_code)]` removed; consumed by router-arm `tracing::warn!` log lines at `crates/envoy-http1/src/hcm.rs:208/248/265`. Verified at this REVIEW.

**Items carrying forward to phase 05+ / hardening / fixture-hardening sub-phase:**

1. **C-1 — Docker-gated `host.docker.internal`/`STATIC` regression on fixtures 0003/0004/0005/0006/0008.** *(Important; cross-phase systemic regression originating at phase-02.2 ADR-0015; surfaced by 04.3 Task 17 push cadence; reviewer's verdict choice is "defer to dedicated fixture-hardening sub-phase or phase 05+ scope".)* The fix requires (a) `ClusterType::StrictDns` schema growth in `crates/envoy-config/src/bootstrap.rs` (currently single-variant `Static` enum at lines 60-62); (b) validator accept path; (c) coordinated-edit of the 5 affected fixtures to use STRICT_DNS where `host.docker.internal` is the backend host literal; (d) re-push to CI to confirm green Docker-gated runs across all 5 fixtures including 0008. See §3 I1 above for the full rationale.

2. **M1 — `diff_headers` duplicate-header semantics** (carryforward from 04.1 REVIEW M1; not exercised in 04.3 because fixture 0008 has no duplicate-header response shape). Track forward into the first phase whose fixtures emit duplicate headers (`Set-Cookie`, `Vary`, etc.).

3. **M2 — Body-drain idle timeout returns `Ok(())` silently** (carryforward from 04.1 REVIEW M2; `crates/envoy-http1/src/hcm.rs:183` per current line numbers — the `_elapsed => return Ok(())` arm in the body-drain timeout). Not exercised in 04.3 because fixture 0008's deterministic-echo body is small and well-framed. Track forward to hardening pass.

4. **M3-correction — PROGRESS Task 16 imprecise on M3 closure attribution** (this REVIEW). PROGRESS says Task 5; actual closure is Task 9. No code or contract impact; awareness-only. Track forward only if a future audit cites M3's closure commit.

5. **M4 — `strip_port` IPv6-Host correctness** (carryforward from 04.1 REVIEW M4; not exercised in 04.3 because fixture 0008 uses a DNS-name Host). Track forward to hardening or a future IPv6-Host fixture.

6. **M5 — Cargo.lock sync cadence standardization** (carryforward from 04.1/04.2 REVIEW M5; 04.3 inherited the inline-at-scaffold cadence — Cargo.lock landed at Task 11 with the new `http1-echo-server` workspace member). Both 04.1, 04.2, and 04.3 used inline; the 03.x precedent had dedicated tail commits. ADR-0021's Consequences prose still contradicts the actual 04.x cadence (M9). Track forward: the next phase that adds a workspace member should pick consciously and either supersede ADR-0021 or document inline as the project's posture.

7. **M6 carryforward** (per §3 M-claim above). Strict per-function `drive_http1` unit test never landed; first end-to-end exercise via fixture 0008 is currently masked by I1's Docker-gated regression. Track forward to whichever phase first adds a third Driver::Http1 consumer OR the fixture-hardening sub-phase.

8. **M7 — `TlsAcceptingHandler.inner` generalization for HCM+TLS** (carryforward from 04.1 REVIEW M7; 04.3 introduces no TLS-bearing HCM fixtures per parent SPEC §4 line 568 deferral). Track forward to phase 05+'s brainstorm.

9. **M8 — `safe_regex_partial_eq` opaque-equality may surprise post-validate consumers** (carryforward from 04.2 REVIEW M8; not exercised in 04.3 because no consumer compares RouteConfiguration values post-validate). Track forward to first phase that does config-diff (e.g. xDS).

10. **M9 — ADR-0021's "dedicated state-4 commit" prose contradicts actual inline cadence** (carryforward from 04.2 REVIEW M9; 04.3 inherited the inline cadence per M5). Per D-3.5 ADRs are append-only. Track forward alongside M5; the next phase's planner picks consciously and either supersedes ADR-0021 or documents inline.

11. **M11 — `Http1Probe.extra_headers` duplicate semantics, coupled with M1** (carryforward from 04.2 REVIEW M11; not exercised in 04.3 because fixture 0008 uses `Driver::Http1` (single probe), not `Driver::Http1ProbeList`). Track forward alongside M1.

12. **M-payload-divergence — `request_headers_to_remove` + `generate_request_id: false` on Envoy side only is a per-side YAML divergence** (per §3 M-payload-divergence above). Right long-term resolution is to extend envoy-rust to emit `x-forwarded-for`/`x-forwarded-proto`/`x-request-id`/`x-envoy-*` headers per parent SPEC §4 line 570's "default plan (b)" / "follow-on (a)" decision tree. Track forward to a future phase that adds these headers to envoy-rust's HCM emission set.

13. **#13 — `x509-parser` deferred ADR** (carryforward from earlier phases; 04.3 introduces no mTLS or peer-cert-attribution headers per parent SPEC §4 line 568 deferral). Track forward to phase 05+ or whichever phase first needs structured cert introspection.

14. **#14 — `enable_half_close: true` flip-fixture** (carryforward from earlier phases; 04.3 introduces no asymmetric-close semantics; ADR-0016 posture unchanged). Track forward to phase 05+ or whichever phase first needs asymmetric-close semantics.

---

## 5. In-phase fixes (closed)

This section preserves the audit trail of issues caught and resolved in-phase by review-fix commits. The same cadence as phase-04.1 / 04.2 — review-fix commits are *additive* (not amends) and PROGRESS.md gains a follow-up subsection rather than overwriting the original task entry.

### Task 2 review fix — commit `d31e1a8` (followed by progress note `a278336`)

- **Issue:** `ConfigError::UnknownCluster`'s `#[error(...)]` Display string was `"tcp_proxy filter references unknown cluster '{0}'"` (02.1-landed) — a half-truth on HCM-only configs after Task 2 widened the throw sites to include the HCM `RouteAction::Route` arm.
- **Fix:** generalized to `"unknown cluster '{0}'"`. Lossy on the referrer but no longer misleading. Schema-breaking alternative (carrying a referrer field) was disclaimed in the PLAN.
- **Verification:** `cargo test -p envoy-config --lib` → 139 passed (unchanged); gate clean. No tests assert on the literal string.

### Task 8 review fix — commit `da00861` (followed by progress note `12d586b`)

- **Issue (1 critical + 2 important):** (C1) `client.rs`'s chunked reader decoded the body but left `Transfer-Encoding: chunked` in `upstream_response.headers`; `write_proxied_response` would emit both `transfer-encoding: chunked` and a synthesized `content-length: N` on the downstream wire, violating RFC 7230 §3.3.3 rule 3. (I1) Doc-comment in `write_proxied_response` did not mention the connection/transfer-encoding strip or the lowercase-normalization. (I2) `proxied_response_sets_connection_per_posture` test used a manual field-clone instead of `up.clone()`.
- **Fix:** added `pub const TRANSFER_ENCODING: &str = "transfer-encoding";` to `crates/envoy-http1/src/headers.rs`. Added a strip-arm in `write_proxied_response`'s header for-loop. Updated the doc-comment to name the strip and the lowercase-normalization. Switched the test to symmetric `up.clone()`. Added regression test `proxied_response_strips_upstream_transfer_encoding`.
- **Verification:** `cargo test -p envoy-http1 --lib router::tests` → 4 passed; `cargo test -p envoy-http1 --lib` → 37 passed (was 36, +1); gate clean.

### Task 9 review fix — commit `d8b2360` (followed by progress note `1a81aa2`)

- **Issue (2 important):** (I1) BuildOutcome::Proxy arm used `find_header(&req.headers, headers::HOST).unwrap_or("")` — papered over the upstream invariant that `build_response` rejects missing/empty Host with synth_400 before the Proxy outcome is constructed. (I2) Outgoing-request headers were not stripping `Transfer-Encoding:` symmetrically with the response-side strip; RFC 7230 §3.3.3 violation if a future relaxation of the chunked-request 501-reject leaks T-E to the outgoing wire.
- **Fix:** replaced `.unwrap_or("")` with `.expect("build_response rejected missing/empty Host before BuildOutcome::Proxy")`. Extended the outgoing `out_headers.retain(...)` strip to drop both `Connection:` and `Transfer-Encoding:`. Inline comment cites RFC 7230 §3.3.3 + the symmetry with `router::write_proxied_response`. Added regression test `proxy_strips_transfer_encoding_from_outgoing_request`.
- **Verification:** `cargo test -p envoy-http1 --lib hcm::tests::proxy_strips_transfer_encoding_from_outgoing_request` → RED before strip, GREEN after; `cargo test -p envoy-http1 --lib` → 43 passed (was 42, +1); gate clean.

### Task 11 review fix — commit `e8c6e12` (followed by progress note `c7f23f5`)

- **Issue (3 minor):** (M1) WHY-comment block above `enum ArgvError` was inadvertently dropped during the Task 11 paste-and-adapt. (M2) `// No --port arg → MissingFlag("--port").` comment in `argv_rejects_missing_port` narrated WHAT and added nothing. (M3) `// Task 12 lands the accept loop.` line did not label the deferred-runtime status or the `let _ = DRAIN_BUDGET;` purpose.
- **Fix:** restored the WHY-comment block mirroring the tls-echo-server sibling. Deleted the narrating-WHAT comment. Replaced the bare comment with `// Task 12 lands the accept loop; the no-op DRAIN_BUDGET keeps the const live until then.`
- **Verification:** comment-only changes; gate clean.

### Task 12 review fix — commit `d5131fb` (followed by progress note `c7f23f5`)

- **Issue (3 important + 1 minor):** (I1) Inline spawned closure in `run` was 80 LoC at 6-level depth — extracted to a named `handle_connection` function. (I2) File-scope `use` statements were duplicated inline; hoisted to module top alongside existing `std` use statements. (I3) Two inline `Duration::from_secs(5)` call sites lacked a named const; introduced `READ_TIMEOUT`. (M2) Two narrating-WHAT comments dropped per D-3.7.
- **Fix:** factored `handle_connection(stream: tokio::net::TcpStream)` extracting the read-parse-respond logic. Hoisted `tokio::io::{AsyncReadExt, AsyncWriteExt}`, `tokio::net::TcpListener`, `tokio::task::JoinSet`, `tokio::time::timeout` to module top. Added `const READ_TIMEOUT: Duration = Duration::from_secs(5);` near `DRAIN_BUDGET` with a WHY-comment. Dropped the narrating WHAT comments; preserved the SPEC-citing WHY comments.
- **Verification:** `cargo test -p http1-echo-server` → 5 passed (unchanged); gate clean.

### Task 13 review fix — commit `4071e69` (followed by progress note `48d4ba8`)

- **Issue (2 minor):** (M3) Render-substitution assert in `run_fixture_dispatches_http1_backend_on_template_marker` used two `contains` checks that would tolerate a partial port match. (M9) `http1_echo_backend_spawns_and_echoes` used `response.contains("HTTP/1.1 200 OK\r\n")` — weaker than `starts_with` (a buggy server prepending garbage would still pass).
- **Fix:** tightened the substitution assert from two `contains` to `assert_eq!(rendered, format!("endpoint: host.docker.internal:{port_str}"))`. Swapped `contains` for `starts_with` on the status-line check.
- **Verification:** `cargo test -p differential --lib` → 52 passed (unchanged); gate clean.

### Task 14 review fix — commit `c8926b8` (followed by progress note `f7a10ad`)

- **Issue (1 minor):** `spawn_http1_upstream` doc-comment opened with two narrating-WHAT lines redundant with the function name and body.
- **Fix:** trimmed the two narrating lines, retaining only the genuine WHY (deliberate non-replication of `http1-echo-server`'s deterministic-echo body shape, with the reason — Task 15's job in the differential harness).
- **Verification:** `cargo test -p envoy-bin --test http1_router_upstream` → 1 passed (unchanged); gate clean.

### Task 16 review fix — commit `e4957cc` (followed by progress note `e4957cc` — same commit per PROGRESS line ~249)

- **Issue (2 important):** (I1) M6 bullet's glob description `hcm::tests::route_walk_* (5 tests)` matched only 3 of the 5 names; the remaining 2 are `proxied_response_*`. (I2) #14 carry-forward bullet ended with bare `CARRY FORWARD.` with no destination phase, unlike every other carryforward bullet.
- **Fix:** corrected the glob description to "Task 6's 4 `client::tests::send_request_*` tests + Task 9's 5 HCM tests (3 `route_walk_*` + 2 `proxied_response_*`)". Added destination "phase 05+ or whichever phase first needs asymmetric-close semantics" to #14.
- **Verification:** doc-only change; gate clean.

---

## 6. Verdict

**Approved with M-track follow-ups.** State 5 complete. No blocking issues in the 04.3 surface itself.

The critical reviewer-of-state-5 finding from PROGRESS Task 17 — the systemic Docker-gated regression on `host.docker.internal` + `STATIC` cluster type affecting fixtures 0003/0004/0005/0006/0008 — is treated as a cross-phase forward-track item (C-1 in §4 above) rather than a state-3 loop-back, on the rationale enumerated in §3 I1: cross-phase scope spanning 5 fixtures and 4 phases, schema growth not in 04.3's planned deliverable set, original-budget-fit concerns, doctrinal alignment with M-track propagation, and the fact that fixture 0008 inherits the broken pattern uniformly with the four pre-04.3 fixtures (fixing 0008 alone is a half-measure). The unit-test gate is fully green and the in-process backstop test `crates/envoy-bin/tests/http1_router_upstream.rs` exercises the same router-proxy pipeline end-to-end without Docker dependency.

The M-track count for 04.3 close is comparable to 04.1 / 04.2: 4 closed-in-phase items (M3, M6, M10, #12) + 1 important new carryforward (C-1) + 11 unchanged carryforwards from earlier phases. The state-6 phase-done commit lands immediately; STATE.md and ROADMAP.md row flips happen at state-6 per the project lifecycle. Parent ROADMAP row 04 flips to `done` at the same commit per the SPEC §1 invariant ("the parent flips to `done` only after all sub-phases are `done`"; 04.1 + 04.2 are already `done` before 04.3 close).

The Docker-gated regression should propagate forward via STATE.md "Phase-04.3 rollovers" Notes section as item C-1 alongside the M-track items per phase doctrine. The reviewer recommends that the next session (which lands the state-6 phase-done commit) explicitly enumerates C-1 in the commit message body so the regression's cross-phase scope and deferred-resolution posture are durable in the git log.