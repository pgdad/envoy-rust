# Phase 04.3 Progress

## Task 1 — envoy-config: RouteAction enum + RouteAction_Route + Route restructure + 5 parse-shape tests (2026-04-27)

- Commit: 313707f
- Change: added RouteAction enum (DirectResponse + Route) and RouteAction_Route { cluster: String } struct in crates/envoy-config/src/bootstrap.rs; restructured Route from { r#match, direct_response } to { r#match, action: RouteAction } with hand-rolled impl<'de> Deserialize<'de> for Route handling the field-name oneof at the route map level (mirrors 04.2's HeaderMatcher visitor pattern). Updated crates/envoy-http1/src/hcm.rs's clone_route_config (added clone_route_action helper) + the synth_direct_response call site (placeholder Route(_) arm calls existing synth_501 until Task 9) + 13 test-fixture mechanical edits replacing `direct_response: DirectResponse { ... }` with `action: RouteAction::DirectResponse(DirectResponse { ... })`. Updated bootstrap.rs's validator to dispatch on the action variant (DirectResponse keeps 04.1's status-range + body-shape checks; Route(_) is a no-op until Task 2). Added Clone derive to DirectResponse and DataSource (required for RouteAction's Clone derive). Extended crates/envoy-config/src/lib.rs's pub use re-exports with RouteAction + RouteAction_Route.
- Tests added (5): parses_route_with_direct_response_action, parses_route_with_route_action, rejects_route_with_both_direct_response_and_route, rejects_route_with_neither_direct_response_nor_route, rejects_route_with_unknown_top_level_key.
- Verification: `cargo build --workspace --all-targets` -> clean; `cargo test -p envoy-config --lib` -> 136 passed (was 131, +5 new); `cargo test -p envoy-http1 --lib` -> 24 passed (unchanged count after 13 mechanical fixture edits); `cargo clippy --workspace --all-targets --all-features -- -D warnings` -> clean; `cargo fmt --all -- --check` -> clean.
- ADRs: none in 04.3 Task 1 (per SPEC §7). ADR ledger head: 21.
- Deviations from PLAN: minor — (1) needed to add `Clone` derive to `DataSource` (in addition to `DirectResponse` as PLAN noted) so that `RouteAction::DirectResponse(DirectResponse)` derives `Clone` cleanly. (2) added `#[allow(non_camel_case_types)]` on `RouteAction_Route` because Rust's default-deny lint flags the snake_case-ish struct name; the name itself is a deliberate Envoy schema mirror. (3) updated validator to match on `RouteAction` and updated the existing `parses_listener_with_hcm_direct_response` test's assertions (these were not separately enumerated in the PLAN but were obvious ripples of the Route restructure). (4) `synth_501` already existed in hcm.rs — the placeholder Route(_) arm reuses it without adding any new helper.

## Task 2 — envoy-config validator: walk RouteAction + reuse UnknownCluster + 3 validator tests (2026-04-27)

- Commit: 69fa095
- Change: extended validate_hcm signature with clusters: &[Cluster] parameter; in the per-route walk, the RouteAction::Route(ar) arm now checks ar.cluster against declared cluster names and emits ConfigError::UnknownCluster(ar.cluster.clone()) on miss (reuses the 02.1-landed variant per SPEC §3 D2 — no new error variant). Updated the single caller in validate's HCM_FILTER arm to pass &bootstrap.static_resources.clusters; the borrow pattern mirrors the existing TCP_PROXY_FILTER arm (shared borrow of clusters held alongside the &mut walk over listeners — split-field borrow accepted by the borrow checker without a fallback). The RouteAction::DirectResponse(dr) arm is unchanged (preserves 04.1 status-range + body-shape checks).
- Tests added (3): parses_route_with_cluster_action, rejects_hcm_route_with_unknown_cluster, rejects_hcm_route_with_empty_cluster_name.
- Verification: `cargo test -p envoy-config --lib` -> 139 passed (was 136, +3 new); `cargo test -p envoy-http1 --lib` -> 24 passed (unchanged); `cargo build --workspace --all-targets` -> clean; `cargo clippy --workspace --all-targets --all-features -- -D warnings` -> clean; `cargo fmt --all -- --check` -> clean; `cargo test --workspace --lib` -> all green (envoy-cluster 48, envoy-listener 8, envoy-config 139, envoy-http1 24, envoy-bin 6, envoy-tcp 11, envoy-tls 15).
- ADRs: none in 04.3 Task 2 (per SPEC §7). ADR ledger head: 21.
- Deviations from PLAN: none — used the default `&[Cluster]` shape; no fallback to `&[String]` needed.

### Task 2 review fix — generalize `ConfigError::UnknownCluster` Display string

- Commit: d31e1a8
- Change: in `crates/envoy-config/src/lib.rs:52` the `UnknownCluster` variant's `#[error(...)]` string was `"tcp_proxy filter references unknown cluster '{0}'"` (02.1-landed). After Task 2 widened the throw sites to include the HCM `RouteAction::Route` arm, that prefix became a half-truth on HCM-only configs. Generalized to `"unknown cluster '{0}'"` — lossy on the referrer, but no longer misleading. The PLAN explicitly disclaimed the schema-breaking alternative (carrying a referrer field on the variant); the tracing-warn alternative (PLAN §Architecture) was a forward-looking framing not carried into the Task 2 step body. This is the minimum-scope fix — 1-line change.
- Verification: `cargo test -p envoy-config --lib` -> 139 passed (unchanged); `cargo clippy --workspace --all-targets --all-features -- -D warnings` -> clean; `cargo fmt --all -- --check` -> clean. No tests assert on the literal Display string (all use `matches!` on the variant), so no test ripples.
- ADRs: none. ADR ledger head: 21.

## Task 3 — envoy-config fuzz corpus: hcm_route_to_cluster seed (2026-04-27)

- Commit: f78d7ab
- Change: created `crates/envoy-config/fuzz/corpus/parse_bootstrap/hcm_route_to_cluster.yaml` exercising the `RouteAction::Route { cluster: backend }` variant (HCM listener with a single virtual host route + a matching STATIC `backend` cluster, so the seed parses + validates through the Task-2 cluster-name check); appended `!corpus/parse_bootstrap/hcm_route_to_cluster.yaml` in alphabetical position in `crates/envoy-config/fuzz/.gitignore`; extended the parse-success allow-list in `crates/envoy-config/src/bootstrap.rs::tests::fuzz_corpus_seeds_parse_or_reject_cleanly` with the new seed (the test enumerates seeds via explicit `&[..]` slices, not `read_dir`, so the source change was required).
- Verification: `cargo test -p envoy-config --lib fuzz_corpus_seeds_parse_or_reject_cleanly` -> pass; `cargo test -p envoy-config --lib` -> 139 passed (unchanged count — the corpus-walk test just walks one more file); `cargo test -p envoy-http1 --lib` -> 24 passed (unchanged); `cargo build --workspace --all-targets` -> clean; `cargo clippy --workspace --all-targets --all-features -- -D warnings` -> clean; `cargo fmt --all -- --check` -> clean. cargo-fuzz run deferred to CI: `cargo-fuzz` is not installed in this environment (`cargo +nightly fuzz` -> `no such command: 'fuzz'`); per phase precedent (04.1, 04.2) the corpus-walk test is the sufficient local gate.
- ADRs: none. ADR ledger head: 21.
- Deviations from PLAN: none — placed the new seed in the parse-success block (PLAN Step 4 left this conditional on the test's shape; the test uses explicit slices and the seed's `cluster: backend` resolves successfully against the declared `backend` cluster, so parse-success is the correct bucket). Fuzz run deferred-to-CI as anticipated by the PLAN's Step-5 fallback.

## Task 4 — envoy-http1::error: 3 new Http1Error variants (2026-04-27)

- Commit: 75a611b
- Change: extended `Http1Error` in `crates/envoy-http1/src/error.rs` with three new variants — `UpstreamConnect { addr: SocketAddr, source: io::Error }` (TCP-connect failure to an upstream cluster endpoint, surfaces from `Client::connect`), `MalformedResponseLine` (httparse rejection of an upstream HTTP/1.1 status line, mirrors `MalformedRequestLine`'s posture for incoming requests), and `MalformedChunkedFraming` (RFC 7230 §4.1 violations from the chunked-encoding response reader). All three are stub-landed ahead of Tasks 5–7's `client.rs` (mirrors phase-04.2 Task 1's variants-first pattern). Added a new `#[cfg(test)] mod tests` block (none existed in error.rs prior) with one Display-shape smoke test on `UpstreamConnect` (the only variant whose `Display` string surfaces in `tracing::warn!` log lines, hence the operationally significant shape). No `#[allow(dead_code)]` needed — clippy under `-D warnings` accepted the unused pub-visible variants without complaint, consistent with the existing 04.1/04.2 enum carry pattern.
- Tests added (1): upstream_connect_display_includes_addr_and_source.
- Verification: `cargo test -p envoy-http1 --lib upstream_connect_display` -> RED before variant add (E0599 no variant `UpstreamConnect`), GREEN after; `cargo test -p envoy-http1 --lib` -> 25 passed (was 24, +1); `cargo test -p envoy-config --lib` -> 139 passed (unchanged); `cargo build --workspace --all-targets` -> clean; `cargo clippy --workspace --all-targets --all-features -- -D warnings` -> clean; `cargo fmt --all -- --check` -> clean.
- ADRs: none. ADR ledger head: 21.
- Deviations from PLAN: none.

## Task 5 — envoy-http1::client: skeleton + Client::connect + 2 connect tests (2026-04-27)

- Commit: c033e20
- Change: created crates/envoy-http1/src/client.rs with Client (unit struct), ClientStream { stream: TcpStream, host: String, buf: BytesMut }, and Client::connect (TCP-connect via tokio::net::TcpStream::connect + UpstreamConnect mapping). Extended crates/envoy-http1/src/lib.rs with `pub mod client;` (alphabetic position before `codec`) + `pub use client::{Client, ClientStream}`. Verified envoy-cluster path-dep is active in crates/envoy-http1/Cargo.toml (M3 carryforward — pre-staged in 04.1, consumed in 04.3 starting Task 9; no Cargo.toml edit needed).
- Tests added (2): connect_succeeds_against_in_process_acceptor, connect_returns_upstream_connect_on_refused_port. Both are #[tokio::test(flavor = "multi_thread")].
- TDD discipline: per the Task 5 spec's explicit allowance, used the one-shot paste-and-verify approach (the PLAN paste includes both impl + tests; the impl is a single TCP-connect call with a map_err). Tests verified passing via `cargo test -p envoy-http1 --lib client::tests` -> 2 passed.
- Verification: `cargo test -p envoy-http1 --lib` -> 27 passed (was 25, +2); `cargo test -p envoy-config --lib` -> 139 passed (unchanged); `cargo build --workspace --all-targets` -> clean; `cargo clippy --workspace --all-targets --all-features -- -D warnings` -> clean; `cargo fmt --all -- --check` -> clean.
- ADRs: none. ADR ledger head: 21.
- Deviations from PLAN: two minor mechanical deviations from the verbatim paste at PLAN lines 1380-1478, both forced by workspace gates. (1) Added `#[derive(Debug)]` on `ClientStream` — the `connect_returns_upstream_connect_on_refused_port` test's `Result<ClientStream, Http1Error>::expect_err(..)` call requires `T = ClientStream` to implement `Debug` (E0277 without it). Both inner field types (tokio::net::TcpStream, bytes::BytesMut) already derive Debug, so the derive is mechanical. (2) Added `#[allow(dead_code)]` on `ClientStream` — clippy under `-D warnings` flags the unread `stream` and `buf` fields (the `host` field IS read by the success-test assertion, but `stream` and `buf` are unread until Task 6's send_request lands). The PLAN's Step-5 fallback explicitly anticipated a similar suppression ("if cargo build complains ... suppress with a #[allow(unused_imports)] on client.rs"); the actual lint that fired was dead_code on fields, hence the slightly different attribute.

## Task 6 — envoy-http1::client: send_request CL path + 4 tests (2026-04-28)

- Commit: 17ca18a
- Change: implemented `ClientStream::send_request` in `crates/envoy-http1/src/client.rs` for the Content-Length response path (request serialization with case-insensitive Host de-dup against the connect-time host + case-insensitive Content-Length de-dup against the request's headers; httparse::Response::parse-driven response-headers loop with `RESPONSE_HEADERS_CAP = 8192` cap and 30s `READ_TIMEOUT`; CL-framed body drain using bytes already read past the headers + iterative reads to fill the remainder; httparse error mapping covering Token/Version/Status → `MalformedResponseLine`, HeaderName/HeaderValue/NewLine → `MalformedHeader`, TooManyHeaders → `HeadersTooLarge`). Chunked-response branch is stubbed to return `Http1Error::MalformedChunkedFraming` until Task 7 lands the reader. Removed the `#[allow(dead_code)]` from `ClientStream` (now that all three fields are read) and dropped the placeholder Task-6-marker comment inside `impl ClientStream`. Extended `crates/envoy-http1/src/codec.rs::Request` with `pub body: Option<bytes::Bytes>` (defaulted to `None` by `Http1Codec::parse_request`'s constructor — incoming-side never carries a body in this field; the router-proxy arm in Task 9 fills it for outgoing requests) plus three helper methods: `body_len_estimate(&self) -> usize` (pub, for `Content-Length:` synthesis), `body_len_string(&self) -> String` (pub(crate)), and `body_bytes(&self) -> Option<&[u8]>` (pub(crate)). The three helpers carry `#[allow(dead_code)]` because `send_request` is the only intra-crate consumer until Task 9's router-proxy arm wires up the production callsite; Task 9 will remove the allows.
- Tests added (4): `send_request_writes_serialized_request_bytes` (asserts request-line + injected Host + user-agent + injected `content-length: 0` + final CRLFCRLF on a `GET /` with the connect-time host as the Host source), `send_request_uses_request_host_when_provided` (request explicitly carries `Host: explicit.example`; asserts the explicit value wins, the connect-time host is dropped, and exactly one Host header appears), `send_request_reads_cl_response_body` (status 200 + 5-byte `hello` body), `send_request_returns_malformed_response_line_on_garbage` (`NOT AN HTTP RESPONSE` upstream → `Http1Error::MalformedResponseLine`). All four are `#[tokio::test(flavor = "multi_thread")]` and use a shared in-process `capturing_acceptor(response)` helper that binds 127.0.0.1:0, accepts one TCP, captures up to 8 KiB of request bytes via a 500ms-deadline read, writes the fixed response, and shuts down — sufficient for the deterministic single-shot fixtures in this task.
- Verification: `cargo test -p envoy-http1 --lib client::tests` -> 6 passed (Task 5's 2 + Task 6's 4); `cargo test -p envoy-http1 --lib` -> 31 passed (was 27, +4); `cargo test -p envoy-config --lib` -> 139 passed (unchanged); `cargo build --workspace --all-targets` -> clean; `cargo clippy --workspace --all-targets --all-features -- -D warnings` -> clean; `cargo fmt --all` -> applied a single rustfmt reflow on the test-helper `tokio::time::timeout(...)` call (a minor `let n =` line-break shape that rustfmt prefers to collapse onto the timeout call); the PLAN paste's manually-wrapped form did not match rustfmt's chosen layout. No semantic change.
- ADRs: none in 04.3 Task 6 (per SPEC §7). ADR ledger head: 21.
- Deviations from PLAN: three minor mechanical deviations. (1) Did NOT add `#[serde(skip)]` on the new `Request::body` field (the PLAN noted this as conditional — `Request` does not derive `Deserialize`, so the directive is unneeded; rustc + clippy under `-D warnings` accept the bare `pub body: Option<bytes::Bytes>` shape). (2) Added `#[allow(dead_code)]` on the three `Request` body helpers (`body_len_estimate` / `body_len_string` / `body_bytes`) — clippy under `-D warnings` flags them as unread until Task 9 (the only intra-crate consumer chain runs `send_request → body_*`, and `send_request` itself is currently exercised only by the new tests). The PLAN's Step-5 fallback anticipated similar suppression patterns. Task 9 will remove the allows when the router-proxy arm wires up the production callsite. (3) The rustfmt reflow on the test-helper closure (above) is a layout-only deviation from the PLAN's verbatim paste.
