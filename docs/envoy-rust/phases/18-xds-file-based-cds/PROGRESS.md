# Phase 18 (`18-xds-file-based-cds`) — PROGRESS

> Running log, updated by the executor on each task completion (the 06.2 → 17 cadence).
> One entry per PLAN task; quote the verifying command output. The state-3 arc runs
> `cargo clippy --workspace --all-targets --all-features -- -D warnings` PER TASK
> (NOT deferred to state-4) per `project_state3_arc_skips_clippy`.

**PLAN:** `docs/envoy-rust/phases/18-xds-file-based-cds/PLAN.md`
**SPEC:** `docs/envoy-rust/phases/18-xds-file-based-cds/SPEC.md`
**Scope ADRs:** ADR-0048 (the xDS-family pick + the three §0 findings [filesystem transport needs no protos; initial-load-only compatible with ClusterManager immutability; the Envoy-side surface already allow-listed] + the minimum-viable scope/deferral ledger); ADR-0049 (§6.2 reconciliation — the @type-required/extension-driven envelope [L1]; the all-CDS-load-errors-fatal posture vs Envoy's 3-way warn-and-serve split [L4]; the static-wins collision rule replacing the projected DuplicateClusterName reject [L9]; the node.id + validate_clusters:false bootstrap prerequisites [L12]).

---

## State-2 PLAN-write (this commit)

- Performed the HEAVY SPEC §6.2 empirical verification against `envoyproxy/envoy:v1.33.0` (digest `sha256:56da5afd…`; Docker; foreground general-purpose subagent; CDS-configured bootstrap + host backend + admin `/stats` + `/config_dump` scrapes; every routing claim cross-checked against backend access logs). Findings L1–L12 locked into PLAN.md "§6.2 empirical lock-ins". **The §0 core findings are CONFIRMED** (the bare `resources:` YAML envelope loads [L1]; readiness implies loaded [L2]; the stat tree + ClustersConfigDump exist as projected [L3/L5]; zero-static-clusters bootstraps are valid [L7]). Four material divergences/absences (**L1** @type required + extension-driven parser; **L4** the negative-path 3-way split [missing-path fatal / parse-error warn-and-serve / semantic-error warn-and-serve] vs the projected binary; **L9** static-WINS on name collision [the projected DuplicateClusterName reject is dropped]; **L12** the node.id/node.cluster + validate_clusters:false bootstrap prerequisites) → **ADR-0049 landed**.
- Performed the PLAN-time SPEC-correction pass (read-only Explore subagent + controller re-verification by direct grep) against HEAD `3acf7367b`. **One anchor drifted** (the fuzz corpus is 28 in the `.gitignore` allow-list / 27 in the test arrays — a pre-existing inconsistency [`cluster_http2_protocol_options.yaml` allow-listed but not in the SUCCESS array]; carried to the state-5 inventory; Task 9 adds the new seed to BOTH). **Four structural corrections** recorded in PLAN.md "PLAN-time SPEC corrections": the config load is SYNC (std::fs, no tokio dep on envoy-config — the fuzz target stays pure); AdminHandler needs NO signature widening (it already holds Arc<Bootstrap>); the cluster-reference checks live at TWO sites (+ a dependent `.expect`); the full downstream-consumer migration set is 7 sites (OutlierManager excluded — it iterates the ClusterManager).
- Evaluated the §6.1 split gate against the §6.2-refined surface (~1480–1600 LoC / 11 tasks; the same at-but-not-over posture as the phase-16/17 no-split decisions; tightly coupled) → **single un-split phase; ADR-0050 does NOT fire.**
- Flipped ROADMAP row `18` `planned → in-progress`. Advanced STATE.md to `18` state-2-complete / state-3-next.

## Task 1 — `envoy-config` schema (`dynamic_resources` + `validate_clusters` + deferred cluster-reference validation)

**Preamble (read before starting):**
- **Goal:** Add `Bootstrap.dynamic_resources: Option<DynamicResources>` (+ the `#[serde(skip)] dynamic_clusters: Option<Vec<Cluster>>` side-field + `all_clusters()` + `cds_configured_but_unloaded()`), the `DynamicResources`/`ConfigSource`/`PathConfigSource` structs (all `deny_unknown_fields`; lds/ads/api_config_source/watched_directory rejected), `RouteConfiguration.validate_clusters: Option<bool>` (parse-and-accept), 3 new `ConfigError` variants (`UnsupportedResourceApiVersion`/`CdsFileError`/`CdsParseError`), and the deferred cluster-reference validation (the two `UnknownCluster` sites + the `Http2ClusterFromHttp1Listener` gate defer iff `cds_configured_but_unloaded()`). TDD per the PLAN Task 1 test list.
- **§6.2 lock-ins that bind this task:** L8 (`resource_api_version` accepts `"V3"`/absent, rejects others); L12b (the `validate_clusters` field is parse-and-accept; envoy-rust's own enforcement is defer-then-revalidate, NOT Envoy's literal runtime-503 semantics); L7 (zero-static-clusters bootstraps are valid — already true via `#[serde(default)]` on `StaticResources.clusters`).
- **Anchors (verified at HEAD `3acf7367b`):** `Bootstrap` `crates/envoy-config/src/bootstrap.rs:8-29` (the xDS-reservation comment at `:19-24` is UPDATED by this task to note phase 18 consumed the reservation); `RouteConfiguration` `:908-912`; `validate` `:1851`; the tcp_proxy cluster check `:2013-2020`; the HCM route check + H2-from-H1 gate `:2205-2227` (the `.expect` at `:2214` must be restructured to `if let` so deferred references cannot panic); `ConfigError` `lib.rs:44-60+`.
- **Carry-forward warning:** `Bootstrap` gained fields — any exhaustive `Bootstrap` struct literal in OTHER crates' tests breaks and must be extended with `dynamic_resources: None, dynamic_clusters: None` in the SAME commit (the phase-16/17 Task-1 workspace-compile lesson). Run `cargo build --workspace --all-targets` before committing.
- **Verification:** `cargo test -p envoy-config` (PASS; the existing UnknownCluster tests at `:3691`/`:6009`/`:6023` must stay green) + `cargo build --workspace --all-targets` + `cargo build -p envoy-config` (standalone, per `project_isolated_crate_build_blindspot`) + `cargo clippy --workspace --all-targets --all-features -- -D warnings` + `cargo fmt --all -- --check`.

_(Task entries are appended below by the state-3 executor — one per task, with quoted verification output + the two-stage review verdicts, per the 06.2 → 17 cadence.)_

### Task 1 — COMPLETE (code commit `ce7abce5b`)

**Landed:** `Bootstrap.dynamic_resources: Option<DynamicResources>` (+ `#[serde(skip)] dynamic_clusters: Option<Vec<Cluster>>` side-field + `all_clusters()` + `pub(crate) cds_configured_but_unloaded()`); `DynamicResources`/`ConfigSource`/`PathConfigSource` structs (all `deny_unknown_fields` — lds_config/ads_config/api_config_source/watched_directory rejected, proven by test); `RouteConfiguration.validate_clusters: Option<bool>` (parse-and-accept, L12b doc comment); 3 new `ConfigError` variants (`UnsupportedResourceApiVersion`/`CdsFileError`/`CdsParseError`) + lib.rs re-exports; the `resource_api_version` V3-or-absent validator check; the deferred cluster-reference validation (`defer_cluster_refs` snapshot before the `&mut` listener loop, threaded into BOTH UnknownCluster sites [tcp_proxy + `validate_hcm` new bool param] + the `Http2ClusterFromHttp1Listener` gate's `.expect` restructured to `if let` — structurally panic-free on deferred references); the Node xDS-reservation comment updated (phase 18 consumed the reservation). Workspace-compile fold-in: 2 `Bootstrap` literals (envoy-cluster) + 26 `RouteConfiguration` literal sites (envoy-http1/envoy-http2 hcm.rs incl. `clone_route_config` propagation) extended in the same commit.

**Verification (quoted):**
- `cargo test -p envoy-config` → `test result: ok. 309 passed; 0 failed; 0 ignored` (was 302; +7 new tests — the 6 PLAN-prescribed + a tcp_proxy deferral sibling)
- `cargo build --workspace --all-targets` → clean; `cargo build -p envoy-config` (standalone) → clean
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` → clean; `cargo fmt --all -- --check` → clean

**Two-stage review:**
- **Spec compliance:** 1 issue found (the Node xDS-reservation comment at `bootstrap.rs:89-94` not updated as the PLAN requires) → **fixed in-task** (commit amended `84e796ba1` → `ce7abce5b`); re-verified compliant.
- **Code quality:** zero Critical; **1 Important (forward-looking, dispositioned per-PLAN):** the two reference-check sites consult `static_resources.clusters` (not `all_clusters()`), which Task 3's post-merge re-validation needs — **this is exactly PLAN Task 3 Step 3b** (the check sites migrate to the merged list at Task 3); carried as a binding Task-3 input, NOT a Task-1 defect. Minor notes: `all_clusters()` is defined-but-unused until Task 3 (per-plan); the bool threading through `validate_hcm` judged appropriate (no enum needed).

### Task 2 — COMPLETE (code commit `33ecf55cd`)

**Landed:** new `crates/envoy-config/src/cds.rs` — `parse_cds_file(path: &str, contents: &str) -> Result<Vec<Cluster>, ConfigError>` via the internally-tagged `#[serde(tag = "@type")]` `CdsResource` enum (the ADR-0014 TypedConfig pattern; single `Cluster` variant renamed to the full type URL). Envelope (`CdsFile`) is accept-and-ignore (NO deny_unknown_fields — `version_info`/`type_url`/`nonce` tolerated, L1); per-resource `Cluster` keeps deny_unknown_fields strictness (L4 recorded divergence); malformed YAML / missing `@type` / wrong `@type` → `CdsParseError { path, message }` with serde line/column detail; every parsed cluster runs `validate_cluster`. **Step 3b refactor:** `validate()`'s per-cluster loop body extracted verbatim (order-preserving) into `pub(crate) fn validate_cluster(cluster: &Cluster)` — shared single source of truth between `validate()` and `parse_cds_file`; cross-cluster/listener checks stay in `validate()`. lib.rs: `pub mod cds;` + `pub use cds::parse_cds_file;`.

**Verification (quoted):**
- `cargo test -p envoy-config` → `test result: ok. 317 passed; 0 failed; 0 ignored` (was 309; +8 new cds tests)
- `cargo build -p envoy-config` (standalone) → clean; `cargo build --workspace --all-targets` → clean
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` → clean; `cargo fmt --all -- --check` → clean

**Two-stage review:**
- **Spec compliance:** ✅ compliant (reviewer additionally probe-verified that deny_unknown_fields genuinely fires through the internally-tagged enum on an otherwise-valid cluster — serde_yaml 0.9.34 honors the inner strictness). Implementer deviation accepted: test fixtures carry `lb_policy: ROUND_ROBIN` (mandatory field in the real Cluster schema; the PLAN's draft YAML omitted it).
- **Code quality:** zero Critical / zero Important / 3 Minors — **2 fixed in-task pre-push** (the `rejects_unknown_fields_in_resource` test tightened to a fully-valid-cluster fixture + `CdsParseError`-message assertion so it isolates the deny_unknown_fields gate; the DiscoveryResponse-envelope test now also asserts the cluster name); 1 optional polish (message-assertions on the remaining negative tests) carried to the state-5 inventory.

### Task 3 — COMPLETE (code commit `e793f5609`)

**Landed:** `pub fn load_dynamic_resources(&mut Bootstrap)` in `envoy-config/src/lib.rs` — sync `std::fs::read_to_string` (NO tokio; the fuzz target `parse_bootstrap` stays pure) → `CdsFileError` on read failure → `parse_cds_file` → the **L9 collision-skip merge** (static-wins + intra-file-first-wins, each `tracing::warn!`) → `dynamic_clusters = Some(merged)` (unconditional — flips `cds_configured_but_unloaded()` false even when all entries skip) → **post-merge re-validation** (`validate()` re-runs; the previously-deferred reference checks now enforce against the effective list). **The reference checks migrated to the effective cluster list** (closes the Task-1 review finding): `validate()` snapshots `Vec<&Cluster>` from the two disjoint fields before the `&mut` listener loop; the tcp_proxy UnknownCluster site, `validate_hcm` (signature → `&[&Cluster]`), and the H2-from-H1 gate all consult it. **7-site consumer migration to `all_clusters()`:** envoy-bin (parse → load → `Arc::new` restructure + the per-cluster TLS loop), envoy-cluster `from_bootstrap`, H1/H2 `PoolManager::for_bootstrap`, envoy-health `Scheduler::spawn`; OutlierManager untouched (iterates the already-merged ClusterManager). Deps: `tracing = "0.1"` added to envoy-config (+ `tempfile = "3"` dev-dep); Cargo.lock updated. 7 new TDD tests incl. `dynamic_cluster_resolves_deferred_route_reference` (deferred reference resolves post-load) + `unresolved_route_reference_fatal_after_load` (a reference in NEITHER list → `UnknownCluster`, not a panic).

**Verification (quoted):**
- `cargo test -p envoy-config` → `test result: ok. 324 passed; 0 failed; 0 ignored` (was 317; +7 new load tests)
- 4 standalone builds (`-p envoy-config`/`-p envoy-cluster`/`-p envoy-http1`/`-p envoy-http2`) → all clean
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` → clean; `cargo fmt --all -- --check` → clean
- `cargo test --workspace --no-fail-fast` (full, incl. all 25 Docker-gated differential fixtures — Docker UP, real containers) → **84 result lines / 1032 passed / 0 failed / exit 0** (the phase-17 baseline was ~1010 across 84 lines; +22 = the Task 1–3 new tests). All 25 differential fixtures `... ok` (verified against real `envoyproxy/envoy:v1.33.0` containers — the harness has no skip-without-Docker mode; it fails if Docker is down).
- **Two pre-existing flakes encountered and cleared on this task's verification runs (NOT regressions; both in pre-existing fixtures; recorded for the state-5 inventory):** (1) `crates/envoy-bin/tests/upstream_active_health_check.rs` `unhealthy_endpoint_returns_synth_503_no_healthy_upstream` — the documented helper-compile-during-test race (the test's own comment names it): Task 3's envoy-config change forced cold helper rebuilds during the first `cargo test --workspace`, blowing the 30s `wait_ready` budget; cleared after pre-building `tests/helpers/*` (5.04s on re-run). (2) differential `tcp_proxy_fixture` — a transient bind-time port collision (`Address already in use` on the harness-picked backend port, likely TIME_WAIT residue from the aborted first run); port free immediately after; passed on re-run.

**Two-stage review:**
- **Spec compliance:** ✅ compliant (read-only review; all 3 reference-check sites verified on the effective list; all 7 consumer sites verified migrated — grep shows no `static_resources.clusters` iteration stragglers; the `Some(vec![])` flip verified; main.rs error propagation verified fatal; fuzz directory verified untouched).
- **Code quality:** **Ready to merge — Yes**; zero Critical / zero Important / 4 Minors — **1 fixed in-task pre-push** (a contradictory "Cloned (rather than borrowed)" doc comment on the effective-list snapshot reworded — the snapshot holds `&Cluster` refs); 2 carried to the state-5 inventory (the idempotent-but-redundant regex recompilation on re-validation; the duplicated `MINIMAL_CDS` test const across modules); **1 carried as a BINDING Task-8 input:** `load_dynamic_resources` is NOT all-or-nothing (re-validation failure leaves `dynamic_clusters` already `Some`) — the Task-8 backstop must treat a failed load as leaving the bootstrap unusable (envoy-bin exits on error, so no production impact). **Double-validation deep-dive:** static clusters validate twice (pure/idempotent), dynamic clusters exactly once (inside `parse_cds_file`), nothing zero times; the only stateful validator (`compile_safe_regex`) is value-idempotent and unshared at re-validation time.

### Task 4 — COMPLETE (code commit `a6c0de764`)

**Landed:** the conditional `cluster_manager.*` stat family in `ClusterManager::from_bootstrap` (`crates/envoy-cluster/src/cluster.rs`) — the project's first top-level-scope (non-resource-prefixed) stat family. Registered ONLY when `dynamic_resources.cds_config` is configured (the §5.2 inertness discipline / L10): `cluster_manager.cds.{update_attempt: 1, update_success: 1, update_failure: 0, update_rejected: 0}` (counters; failure/rejected register at 0 and are structurally unreachable non-zero per L4 all-fatal) + `cluster_manager.cluster_added` (counter) + `cluster_manager.active_clusters` (gauge), both = the MERGED manager size (static + dynamic, per L3 — Envoy counts all clusters). Error wrapping mirrors the existing `StatsRegistration` pattern via a counter-typed `mk` closure. 3 new TDD tests (registry-snapshot absence proof / the 6-name value proof on the fixture-0026 shape / the static-inclusion count proof).

**Verification (quoted):**
- `cargo test -p envoy-cluster` → `test result: ok. 88 passed; 0 failed; 0 ignored` (was 85; +3)
- `cargo build -p envoy-cluster` (standalone) → clean; `cargo build --workspace --all-targets` → clean
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` → clean; `cargo fmt --all -- --check` → clean

**Two-stage review:**
- **Spec compliance:** ✅ compliant, no issues (all 11 verification points pass: the condition is exactly `cds_config.is_some()`; the count is the merged map size; kinds/names/values/placement byte-exact; the absence test scrapes the full registry snapshot; only cluster.rs changed).
- **Code quality:** **Ready to merge — Yes**; zero Critical / zero Important / 3 Minors — **1 fixed in-task pre-push** (a clarifying comment that `active_clusters` is the lone gauge and can't route through the counter-typed `mk` closure); 2 carried (the test-helper reuse refactor opportunity → Task 8; an optional belt-and-suspenders test for the unreachable configured-but-unloaded state). **The load-bearing architectural check verified sound:** dropped stat handles survive in registry snapshots (the registry owns its own `Arc` clone; `Counter`/`Gauge` use atomic interior mutability) — fixture 0026's Task-7 scrape WILL see these stats.

### Task 5 — COMPLETE (code commit `059257d25`)

**Landed:** the `/config_dump` `ClustersConfigDump` entry (`crates/envoy-admin/src/endpoint.rs`, the only file changed) — `ConfigDumpEntry::Clusters` variant (`@type = type.googleapis.com/envoy.admin.v3.ClustersConfigDump`) + `StaticClusterEntry`/`DynamicClusterEntry`/`TaggedCluster` lifetime-borrowing structs. The inner `cluster` object carries its own `@type` + the full flattened cluster config (the serde flatten-on-nested-struct approach worked — the flatten+internally-tagged limitation only applies to flatten on the enum's own variant content). `static_clusters`/`dynamic_active_clusters` keys omit-when-empty (L5); NO `version_info` key; `last_updated` shares the Bootstrap entry's single render-time `format_iso8601` value. Emitted ONLY when `dynamic_resources.cds_config` is configured, pushed after the Bootstrap entry → `configs[1]` (Envoy's order). 5 new TDD tests (conditional emission / shape+@types+name+last_updated / empty-key omission both directions / §5.5 BootstrapConfigDump separation).

**Verification (quoted):**
- `cargo test -p envoy-admin` → `test result: ok. 79 passed; 0 failed` (was 74; +5)
- `cargo build -p envoy-admin` (standalone) → clean; `cargo build --workspace --all-targets` → clean
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` → clean; `cargo fmt --all -- --check` → clean

**Two-stage review:**
- **Spec compliance:** ✅ compliant, no issues — the reviewer dumped the actual serialized JSON via a scratch test and confirmed the L5 shape (correct @types, correct flatten nesting alongside the full cluster config, empty-key omission both directions, conditional emission on `cds_config` specifically, `configs[1]` ordering, fixture-0014 single-entry regression preserved).
- **Code quality:** **Ready to merge — Yes**; zero Critical / zero Important / 3 cosmetic Minors carried to the state-5 inventory (test-helper near-duplication `handler_from_bootstrap`/`handler_with_bootstrap`; a doc-comment phrasing nit on the version_info omission rationale; `CLUSTER_TYPE_URL` const placement). **Forward-looking Task-7 advisory recorded:** envoy-rust's cluster JSON uses snake_case field names while Envoy's /config_dump proto3-JSON defaults to camelCase for multi-word fields — irrelevant for fixture 0026 (it asserts only `...cluster.name`, a single-word field identical on both sides) but binding if any future fixture asserts deeper nested cluster fields.

### Task 6 — COMPLETE (code commit `d7c804679`)

**Landed:** harness CDS support in `tests/differential/src/{lib.rs,upstream.rs}` — (1) `{{CDS_PATH}}` detection (`needs_cds`) + per-side `cds.yaml` rendering through each side's kv map + temp-dir writes (`cds-upstream.yaml`/`cds-subject.yaml`), all BEFORE the main configs render (the TLS-PKI dependency shape); (2) `upstream::start` gains `cds_file: Option<&Path>` → `with_copy_to(CDS_CONTAINER_PATH, <canonicalized host path>)`; the container path is the compile-time const `/etc/envoy-cds/cds.yaml` (L1 `.yaml`-extension structurally guaranteed); (3) the subject side substitutes `{{CDS_PATH}}` → the host temp path; (4) `Driver::Http1KeepAlive` gains `#[serde(default)] admin_scrapes: Vec<AdminScrapeCase>`, asserted through the SHARED `assert_admin_scrape_case` fn extracted verbatim from the `Driver::AdminScrape` arm (which now calls it too — no duplicated json_shape logic); (5) **[in-review Critical fix]** ALL backend-launch detection scans (`BACKEND_PORT`/`TLS_BACKEND_PORT`/`HTTP1_BACKEND_PORT`/`HTTP2_BACKEND_PORT`) + the `BACKEND_HOST` gate now scan the COMBINED source (main templates + CDS template) via the testable `scan_needs_marker` helper; (6) **[in-review Important fix]** a `residual_marker` guard errors loudly (naming the marker) if a rendered CDS rendition still contains `{{...}}`. 7 new unit tests (per-side substitution / .yaml constraint / serde round-trip + default-empty / CDS-only marker detection / residual-marker guard).

**Verification (quoted):**
- `cargo test -p differential --lib` → `test result: ok. 118 passed; 0 failed; 1 ignored` (was ~111; the 1 ignored is the pre-existing Docker-gated upstream unit test)
- `cargo build -p differential` → clean; `cargo build --workspace --all-targets` → clean
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` → clean; `cargo fmt --all -- --check` → clean

**Two-stage review:**
- **Spec compliance:** ✅ compliant — the `assert_admin_scrape_case` extraction verified line-for-line verbatim (fixtures 0011/0014 behavior preserved); the run_fixture ordering verified (CDS files written before main-config render and container start); with_copy_to arg order matches the TLS-PKI precedent; zero existing fixtures contain `{{CDS_PATH}}`; fixtures 0020–0025 deserialize unchanged.
- **Code quality:** **1 Critical FOUND AND FIXED IN-TASK pre-push** — the backend-launch detection scanned only the MAIN templates, so fixture 0026's CDS-only `{{HTTP1_BACKEND_PORT}}` marker would never spawn the echo backend (the fixture would fail on its first Docker run with a confusing unsubstituted-marker parse error); fixed via the combined-source `scan_needs_marker` + TDD test. **1 Important FIXED IN-TASK** (the residual-marker guard — turns silent render misses into named errors). 2 cosmetic Minors carried (unconditional `subject_cds_path` computation on non-CDS fixtures; a doc-comment wording nit). **Task-7 guidance recorded by the reviewer:** the exact `admin_scrapes` expectations.yaml syntax; `walk_pointer` supports array indices (`configs.1...`); the keep-alive arm requires `{{ADMIN_PORT}}` on both sides.

### Task 7 — COMPLETE (code commit `4c6ce3c4c`)

**Landed:** fixture `0026-xds-file-based-cds` (envoy.yaml + envoy-rust.yaml [the fixture-0008 HCM boilerplate verbatim + the CDS deltas: `node:` (L12a) + `dynamic_resources.cds_config.path_config_source.path: {{CDS_PATH}}` + `validate_clusters: false` (L12b) + route → `dynamic_backend` + NO static `clusters:` key (L7)] + cds.yaml [the L1 bare `resources:` envelope; 0008's cluster moved into the CDS file as `dynamic_backend`] + expectations.yaml + README.md) + the Docker-gated wrapper `tests/differential/tests/xds_file_based_cds.rs`. Expectations: `Driver::Http1KeepAlive`, 1 GET (200 + byte-exact echo `expected_body` asserted per side), the 6 `cluster_manager.*` stats value-exact (1/1/0/0/1/1, L3), `cluster.dynamic_backend.upstream_{rq,cx}_total` 1/1, `http.ingress_http1.downstream_rq_{total,2xx}` 1/1, + the `/config_dump` `admin_scrapes` json_shape case (`configs.1.dynamic_active_clusters.0.cluster.name == dynamic_backend`, L5).

**Verification (quoted):**
- **Fixture 0026 PASSED BILATERALLY ON THE FIRST RUN** (the controller-run isolated test): `test xds_file_based_cds_fixture ... ok` / `test result: ok. 1 passed; 0 failed` — and again post-fix with the strengthened body assertion (`finished in 1.33s`). The `cds.update_success: 1` assertion can only pass if real Envoy genuinely loaded the CDS file — the load-bearing differential proof of the phase.
- **The full 26-fixture regression** (`cargo test -p differential --no-fail-fast`, Docker UP): **25 of 26 fixture tests green in the loaded run incl. 0026**; the 2 failures were the DOCUMENTED pre-existing flake family (fixture `0012-access-log-file-sink` — the access-log fire-and-forget race, `envoy=1 envoy-rust=0`; and the lib test `tls_echo_backend_spawns_and_echoes` — a helper TLS-handshake race), both **cleared on isolated re-run** (`0.97s` / `0.16s`): NOT regressions.
- `cargo fmt --all -- --check` → clean; `cargo clippy -p differential --all-targets --all-features -- -D warnings` → clean.

**Two-stage review:**
- **Spec compliance:** ✅ compliant (all 9 requirements; the 0008-boilerplate comparison shows only the established divergence kinds [bind address / generate_request_id / request_headers_to_remove / admin] + the documented CDS deltas; the json_shape path verified resolvable; exactly 6 new files, zero modifications).
- **Code quality:** **1 Important FOUND AND FIXED IN-TASK pre-push** — the fixture prose claimed a "harness-automatic bilateral byte-exact body comparison" that the `Http1KeepAlive` driver does not perform, and no `expected_body` was asserted (the data-plane proof was status-only); fixed by adding the per-request `expected_body: { kind: byte_exact, body: "method: GET\npath: /\nheaders:\n  host: dynamic_backend\nbody: \n" }` (empirically captured from the Docker run; asserted byte-exact on EACH side — the 0024/0025 pattern) + rewording the over-claiming prose in all 4 files (README / expectations / cds.yaml / wrapper). 1 Minor carried (the structural-vs-asserted 0008-equivalence framing).

### Task 8 — COMPLETE (code commit `4c7bd2aac`)

**Landed:** the in-process backstop `crates/envoy-bin/tests/xds_file_based_cds.rs` (672 lines; the only file changed; tests-only — zero production code changes). Five paths, each booting its own `envoy-bin` subprocess (kill_on_drop + wait_ready + raw-TcpStream HTTP — the established backstop pattern copied faithfully): **(i)** happy path (data-plane 200 + backend body through the CDS-loaded cluster + the 6 `cluster_manager.*` stats 1/1/0/0/1/1 + both ClustersConfigDump assertions); **(ii)** missing CDS file → fatal exit + "reading CDS file" diagnostic + port-never-accepts (L4a); **(iii)** malformed CDS file → fatal exit + "parsing CDS file" diagnostic (L4b — **the recorded-divergence proof** vs Envoy's warn-and-serve); **(iv)** static/dynamic collision → boot succeeds + **the static backend's distinct body served on the data plane** (L9 static-wins) + `static_clusters` present + `dynamic_active_clusters` key absent; **(v)** inertness → zero `cluster_manager.*` names in the full /stats scrape + exactly 1 config_dump entry (§5.2). The negative-path harness drains stdout+stderr concurrently with `child.wait()` (the pipe-deadlock-safe pattern) and scans the combined stream (envoy-bin's tracing subscriber writes fatal errors to stdout, not stderr — verified against main.rs).

**Verification (quoted):**
- `cargo test -p envoy-bin --test xds_file_based_cds` → `test result: ok. 5 passed; 0 failed`
- `cargo test -p envoy-bin` (full suite incl. all pre-existing backstops) → all binaries green on a settled machine (one self-inflicted environmental stall during the arc — overlapping cargo test runs caused macOS dyld startup stalls hitting pre-existing tests too; cleared when the duplicate runs were killed; recorded as a new flake-family member)
- `cargo clippy -p envoy-bin --all-targets --all-features -- -D warnings` → clean; `cargo fmt --all -- --check` → clean

**Two-stage review:**
- **Spec compliance:** ✅ compliant (all 5 paths' assertions verified real and load-bearing; the stdout-vs-stderr deviation verified legitimate — envoy-bin's tracing fmt subscriber defaults to stdout; the Task-3 binding input honored — process-level assertions only, no Bootstrap inspection after failed loads).
- **Code quality:** **Ready to merge** (after 1 cosmetic fix applied in-task: the module-doc "stderr carries" → "the process diagnostic carries", matching the implementation); zero Critical / zero Important / 2 Minors — 1 fixed in-task, **1 carried as a state-5 inventory item: the backstop-helper duplication has crossed the phase-05.2 REVIEW M5 disposition threshold** (the `reserve_port`/`wait_ready`/`http1_oneshot`/`scrape_admin_stats` helper cluster is now copied across ≥4 backstop files; M5 said extract a shared test-support crate "when the third consumer appears" — the trigger is now met; extraction is a future hardening-phase item, correctly NOT done in this tests-only task).

### Task 9 — COMPLETE (code commit `78d393b4b`)

**Landed:** fuzz seed `crates/envoy-config/fuzz/corpus/parse_bootstrap/dynamic_resources_cds.yaml` (the fixture-0026 shape with literal ports — node + admin + `dynamic_resources.cds_config` [resource_api_version V3 + path_config_source] + HCM listener with `validate_clusters: false` + a route to the CDS-deferred `dynamic_backend` + zero static clusters — exercising the Task-1 deferral path; `parse_bootstrap` accepts it with no I/O). **The atomic three-edit:** seed + `.gitignore` allow-list entry + SUCCESS-array entry in one commit (the 09→16 lesson). **The opportunistic pre-existing-inconsistency fix SUCCEEDED:** `cluster_http2_protocol_options.yaml` (allow-listed since ~13.2 but missing from the SUCCESS array) parses cleanly and was restored to the array — **the corpus is now fully consistent: allow-list 29 = SUCCESS 25 + REJECT 3 + minimal 1.**

**Verification (quoted):**
- `cargo test -p envoy-config fuzz_corpus_seeds_parse_or_reject_cleanly` → `test result: ok. 1 passed; 0 failed`
- `cargo test -p envoy-config` → `test result: ok. 324 passed; 0 failed`
- Short-budget fuzz smoke (`cargo +nightly fuzz run parse_bootstrap -- -runs=100000 -max_total_time=60`) → `Done 100000 runs in 9 second(s)`, no crashes
- `cargo clippy -p envoy-config --all-targets --all-features -- -D warnings` → clean; `cargo fmt --all -- --check` → clean

**Two-stage review (combined pass for this mechanical 3-file task):**
- **Spec compliance:** ✅ compliant (exactly 3 files; fuzz Cargo.lock untracked; the full schema surface exercised; both registries updated; the count reconciliation verified; the opportunistic fix verified genuinely pre-existing at the parent commit).
- **Code quality:** zero Critical / zero Important / zero Minor — **Ready to merge: Yes.** The reviewer additionally proved the corpus gate test is not stale (renaming the seed away makes the gate FAIL — the test reads real files).

### Task 10 — COMPLETE (code commit `62bcecce6`)

**Landed:** the BEHAVIOR_CONTRACT extensions (`docs/envoy-rust/BEHAVIOR_CONTRACT.md`, the only file; +143/−2): **(1)** the "18 entries (file-based CDS)" stat block — the 6-name `cluster_manager.*` table (value-exact on fixture 0026; `update_failure`/`update_rejected` carry the L4 structurally-0 note; `cluster_added`/`active_clusters` carry the static-inclusion caveat) + the §5.2 conditional-registration-narrowing paragraph (recorded divergence per D-3.3) + the L3 Envoy-only 12-name enumeration; **(2)** the "xDS wire state machine" section's FIRST population — "Filesystem transport (`path_config_source`) — phase 18" recording the L1 byte-exact envelope + @type requirement + always-YAML leniency divergence, the L2 readiness-implies-loaded ordering, the L4 negative-path table (Envoy's 3-way split vs envoy-rust's all-fatal — the recorded divergence), the L9 static-wins rule, the L12 prerequisites + defer-then-revalidate enforcement, and the explicit gRPC/ADS-still-unpopulated note; **(3)** the `/config_dump` ClustersConfigDump row in "Admin endpoint body shapes" (the L5 shape, conditional emission, empty-key omission, the fixture-0026 anchor) + a tightening of the existing BootstrapConfigDump row (`configs[0]` + the §5.5 separation note).

**Verification:** docs-only (no cargo gates per the PLAN); markdown table syntax verified.

**Two-stage review (combined accuracy-focused pass):**
- **Spec compliance:** ✅ all three additions present and complete; scope clean; all 4 divergences explicit.
- **Factual accuracy:** **zero inaccuracies at any severity** — every load-bearing claim cross-checked against the implementation (`cluster.rs` registration block / `endpoint.rs` Clusters variant / `lib.rs` loader), the PLAN §6.2 lock-ins (the L4 parse-vs-semantic split, the byte-exact L1 file, the 12 Envoy-only names), fixture 0026's expectations, and ADR-0049's four reconciliations. The BootstrapConfigDump tightening verified consistent with fixture 0014. **Ready to merge: Yes.**

### In-arc fix — the CI-only host-gateway bug (code commit `f5873902a`)

**Found at the state-4 verification:** CI runs at the Task-9 and Task-10 pushes failed deterministically on `xds_file_based_cds_fixture` with `upstream: expected status 200 for GET /, got 503` — CI-only (local macOS always green). (The Task-7 and Task-8 push failures were the DOCUMENTED flake family — fixture 0012's access-log race and fixture 0025's readiness timeout respectively — which fail-fast-masked the real 0026 failure in those runs.) **Root cause (systematic diagnosis):** the ADR-0015 `host_uses_host_gateway` flag (`tests/differential/src/lib.rs:2505`) scanned ONLY the rendered upstream MAIN config for `host.docker.internal`; fixture 0026's only reference lives in the rendered upstream **CDS file** (the main config has zero clusters) → no `--add-host=host.docker.internal:host-gateway` on the Envoy container → on Linux CI the container cannot resolve the backend host → STRICT_DNS resolution fails → 503. macOS Docker Desktop resolves `host.docker.internal` natively regardless, which is why every local run passed. **This was the THIRD site of the Task-6-Critical bug class** (main-template-only scanning); the Task-6 fix covered the `needs_*backend` scans + the kv-map gate but missed this one. **Fix (TDD):** extracted the decision into the testable `uses_host_gateway(upstream_main, upstream_cds)` helper (true if either contains the marker; test verified red against the old single-scan logic); the rendered upstream CDS content hoisted out of its block. **Proof:** local fixture still green (`1 passed`); **the CI run at this fix's push (`26863221247`) is SUCCESS** — fixture 0026 green on Linux.

### Task 11 — STATE-4 PHASE-DONE VERIFICATION (this commit)

**§7.5 (a)–(e) ALL GREEN. Per-gate evidence (all at HEAD `f5873902a` unless noted):**

**(a) + (b) — all 26 Docker-gated differential fixtures green simultaneously:**
- **CI anchor run `26863221247`** (HEAD `f5873902a`, ubuntu-latest + Docker, 2026-06-03T04:12–04:15Z): `conclusion: success`; both jobs green (`build + test + lint`: success; `fuzz (parse_bootstrap, 30s)`: success). All 26 fixture tests `... ok` in the job log — `echo`/`tcp_proxy`/`admin_ready`/`tls_{downstream,upstream,sni}`/`http1_{direct_response,router_upstream}`/`http2_{direct_response,router_upstream}`/`admin_stats_prometheus`/`access_log_file_sink`/`http_filter_{header_mutation,local_rate_limit,rbac,fault}`/`admin_{config_dump_server_info,drain_listeners}`/`upstream_{active_health_check,connection_pooling_and_per_class_counters,h2_connection_pooling,outlier_detection_consecutive_5xx}`/`upstream_circuit_breaker_{max_pending_requests,budgets}`/`upstream_retry_on_5xx`/**`xds_file_based_cds`**.
- Local (macOS + Docker): fixture 0026 green 3× (Task-7 isolated run, the 26-fixture regression, the post-fix re-run); the 25 pre-existing fixtures green in the Task-7 regression run.

**(c) — h2spec ≥95% conformance:** `test h2spec_pass_rate_gate ... ok` in the CI anchor run (the parent-05 baseline holds; envoy-rust's H2 framing untouched by phase 18).

**(d) — fuzz clean:** local `cargo +nightly fuzz run parse_bootstrap -- -runs=200000 -max_total_time=120` → `Done 200000 runs in 20 second(s)`, zero crashes, on the 29-seed corpus; + the CI fuzz job success.

**(e) — the 5 stable-toolchain gates (local, fresh at `f5873902a`):**
- `cargo build --workspace --all-targets` → `Finished ... in 3m 31s`, exit 0
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` → `Finished ... in 4m 20s`, exit 0 (zero warnings — clippy was run PER TASK across the arc per `project_state3_arc_skips_clippy`)
- `cargo fmt --all -- --check` → clean, exit 0
- `cargo test --workspace --no-fail-fast` → 86 result lines / **1052 passed** / 1 failed → the 1 failure (`upstream_h2_connection_pooling`, 30.46s readiness timeout) is the DOCUMENTED helper-rebuild flake (the clippy `--all-features` run immediately before dirtied the shared target-dir fingerprints, making the test's nested `cargo run --manifest-path` helper build take 83s > the 30s budget); **re-run in isolation: `ok` in 2.05s**; the sibling helper-spawning backstops also re-verified green. NOT a regression (the same test is green in the CI anchor run).
- `cargo deny check` → `advisories ok, bans ok, licenses ok, sources ok`

**Plus the 4 standalone-crate builds** (`project_isolated_crate_build_blindspot`): `-p envoy-config` / `-p envoy-cluster` / `-p envoy-http1` / `-p envoy-http2` → all `Finished`, exit 0.

**Workspace test-count delta:** 1052 passed (the phase-17 baseline was ~1010) — the +42 are phase 18's new tests (7 Task-1 + 8 Task-2 + 7 Task-3 + 3 Task-4 + 5 Task-5 + 7 Task-6 incl. the 2 in-review fix tests + 1 Task-6 host-gateway fix test + 5 Task-8 + corpus-gate coverage).

**STATE advanced to state-4-complete / state-5-next.** Per §5.1 the state-5 code review (`superpowers:requesting-code-review` → REVIEW.md) is the NEXT session.
