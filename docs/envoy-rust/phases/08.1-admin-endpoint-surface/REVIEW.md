# Phase 08.1 (`08.1-admin-endpoint-surface`) — REVIEW

- **Phase id:** `08.1`
- **Slug:** `08.1-admin-endpoint-surface`
- **Reviewed range:** `7dbd984..HEAD` (28 commits, 26 files, ~5301 insertions / ~570 deletions per `git diff --stat 7dbd984..HEAD` — of which `docs/envoy-rust/STATE.md` +191/-… and `docs/envoy-rust/phases/08.1-admin-endpoint-surface/PROGRESS.md` +1612 are docs; the reviewed *code/fixture* surface is the remaining ~24 files). The arc is the state-2 PLAN-write base + 14 substantive task commits (each followed by a SHA-fixup commit per the established 2-commit pattern):
  - `7dbd984` — state-2 standalone PLAN.md + PROGRESS.md skeleton + Task 1 preamble (base of the range; docs-only design contract; 14 tasks; 6 PLAN-write SPEC corrections + 20 architecture-decision lock-ins).
  - `d43d97a` / `86b89de` — **Task 1**: `serialize_response` case-insensitive header dedupe + `reason_for_status(u16) -> &'static str` helper at `crates/envoy-admin/src/handler.rs` (closes 06.1 REVIEW I2 + M1; +162 LoC; 7 new dedupe-and-reason unit tests).
  - `c6368f4` / `5904cf9` — **Task 2**: `DRAIN_BUDGET` consolidated to `crates/envoy-listener/src/lib.rs` as a module-level `pub const`; both prior local-fn-scope + parallel module declarations deleted; `envoy-admin` re-imports (closes 06.1 REVIEW M4; +26 LoC; 3 new lockstep tests).
  - `7188e7f` / `508b67d` — **Task 3**: `Dispatch { Endpoint, NotFound, MethodNotAllowed { allow } }` enum + `AdminEndpoint::dispatch(method, path)` + `allowed_method(&self)` per-variant declaration; `render_405()` widened to take `allow: &'static str`; collapsed `handle_inner`'s hand-rolled 405 path. Closes 06.1 REVIEW M1 structurally (+129 LoC; 6 new dispatch unit tests).
  - `ec12450` / `b1ce7c5` — **Task 4**: `Bootstrap` `Serialize` derive cascade — 49 derive-line edits in `crates/envoy-config/src/bootstrap.rs` (`Deserialize` → `Serialize, Deserialize`) + 5 hand-rolled `impl serde::Serialize` blocks (`Route`, `RouteAction`, `SafeRegex`, `StringMatcher`, `HeaderMatcher`) for types that already have hand-rolled `Deserialize`. `serde_json = "1"` added to `crates/envoy-config/Cargo.toml`. Two roundtrip tests landed (`fixture_0008_bootstrap_roundtrips_yaml_to_json` + `minimal_bootstrap_serializes_to_json`). Post-review amend hardened `StringMatcher`/`HeaderMatcher` to emit `ignore_case`/`invert_match` unconditionally per PLAN architecture-decision lock-in #8.
  - `1a05eaa` / `1d546bc` — **Task 5**: `AdminHandler::new` widened from the 2-arg shape `(config, registry)` to the 6-arg shape `(config, registry, bootstrap, cluster_manager, start_instant, command_line_options)` per PLAN-time deviation #1 (SPEC §3 D13a had phrased "5-arg"; the PLAN settled on 6-arg because `command_line_options` is built once at construction time). `envoy-bin::main` wiring threaded through. `pub fn format_iso8601(t: SystemTime) -> String` wrapper landed at `crates/envoy-accesslog/src/lib.rs` per PLAN-write SPEC correction 2 (the existing `pub(crate)` was unreachable from `envoy-admin`). 5 `AdminHandler` accessors added.
  - `24f8382` / `615fc5d` — **Task 6**: `/config_dump` endpoint — `AdminEndpoint::ConfigDump` variant + `ConfigDumpBody<'a>` borrowed-reference shape + `ConfigDumpEntry` tagged enum (`@type = "type.googleapis.com/envoy.admin.v3.BootstrapConfigDump"`). `last_updated` rendered via the Task 5 `format_iso8601` wrapper. BEHAVIOR_CONTRACT row landed.
  - `f463a3c` / `d374417` — **Task 7**: `/server_info` endpoint — `AdminEndpoint::ServerInfo` variant + `ServerInfoBody<'a>` borrowed-reference shape; required JSON keys `state` / `version` / `node` / `uptime_current_epoch_seconds` / `uptime_all_epochs_seconds` / `hot_restart_version` / `command_line_options`. `state` is the literal `"LIVE"` per architecture-decision lock-in #3 (08.2's D5e will patch the value-binding source from the literal to `match DrainState::current() { ... }`; the struct shape does not change at the boundary). BEHAVIOR_CONTRACT row landed.
  - `1a9a3ed` / `1d29af2` — **Task 8**: `/clusters` endpoint + `ClusterManager::clusters(&self) -> impl Iterator<Item = ClusterHandle> + '_` accessor (PLAN-write SPEC correction 3 — the accessor did not previously exist). Text-plain output per Envoy v1.33's `/clusters` format; sorted-by-name deterministic ordering. BEHAVIOR_CONTRACT row landed.
  - `2bb3545` / `7aa44ab` — **Task 9**: `/listeners` endpoint — `AdminEndpoint::Listeners` variant reading from `Arc<Bootstrap>`. Text-plain `<listener_name>::<address>:<port>` per Envoy v1.33's default format; sorted-by-name deterministic ordering. BEHAVIOR_CONTRACT row landed (4-row admin endpoint body-shapes subsection now complete for 08.1's GET surface).
  - `9cf8831` / `2217b17` — **Task 10**: `BodyRule::JsonShape` + `BodyRule::TextLines` variants + `JsonSubtreeRule` struct + `walk_pointer` dotted-segment path walker over `serde_json::Value`. Added to `tests/differential/src/lib.rs`; 7 new unit tests in `body_rule_extension_tests`.
  - `6ee1ad7` / `d740f53` — **Task 11**: differential fixture `tests/fixtures/0014-admin-config-dump-server-info/` (5 files: `envoy.yaml`, `envoy-rust.yaml`, `expectations.yaml`, `inputs/payload.bin`, `README.md`) + Docker-gated wrapper `tests/differential/tests/admin_config_dump_server_info.rs`. `Driver::AdminScrape` widened from single-scrape to `{ pre_requests, scrapes: Vec<AdminScrapeCase> }` multi-case shape; fixture 0011's `expectations.yaml` migrated in lockstep. Strictness wiring + per-side line-prefix family + `Box<BodyRule>` on `Http1WithAccessLog` (clippy `large_enum_variant` fix) + `check_content_type` parameter-tolerance + `DIFFERENTIAL_DUMP_ADMIN` env-var + 50ms sleep-guard guarded on `!pre_requests.is_empty()`. 9 additional Task 11 strictness unit tests landed.
  - `7711915` / `0a2af1d` — **Task 12**: fuzz corpus seed `crates/envoy-config/fuzz/corpus/parse_bootstrap/admin_multi_endpoint_bootstrap.yaml` (admin + 1 listener with `filter_chains: []` + 2 STRICT_DNS clusters with populated localities + `lb_policy: ROUND_ROBIN` each). Five PLAN deviations applied (`connect_timeout` removed — schema-deny-unknown-fields; `endpoints: []` replaced with populated locality — `EmptyClusterEndpoints` validator; `lb_policy` added — mandatory field; second listener removed — `TooManyListeners` validator; corpus-walker `+nightly` invocation). Short-budget run 370 923 iterations clean.
  - `fcfb940` / `6a874b4` — **Task 13**: in-process backstop `crates/envoy-bin/tests/admin_config_dump_server_info.rs` (single `#[tokio::test]`; spawns `envoy-bin` against in-memory bootstrap with admin + 1 echo-filter listener + 1 STRICT_DNS cluster on `localhost:7001`; scrapes 4 admin endpoints; asserts JSON parse + required-key presence on `/config_dump` + `/server_info`, line-presence on `/clusters` + `/listeners`). `serde_json = "1"` added to `envoy-bin/Cargo.toml` `[dev-dependencies]`. PLAN-deviation #5: empty `filter_chains` replaced with one `envoy.filters.network.echo` filter to satisfy `envoy-bin/src/main.rs:158`'s startup invariant (`.expect("validator guarantees ≥1 filter")`).
  - `03e6435` / `0e2987d` — **Task 14**: state-4 phase-done verification + STATE-advance to state-5-next + one in-flight fixture-coverage fix landed alongside the state-4 evidence anchor per cold-start guidance. Fixture 0014's `/clusters` `allowlist_envoy_only_line_prefixes` extended from `["backend::192.168.65.254:"]` (macOS Docker Desktop bridge IP only) to `["backend::192.168.65.254:", "backend::172.17.0.1:"]` (adds Linux Docker `docker0` bridge IP). Cures the deterministic CI red at Tasks 11/12/13 pushes (CI runs `25962757953` + `25963715339` + `25964262139`); the fix is purely additive (no regression on macOS; CI is the Linux-side evidence anchor).
- **CI evidence cited:** PROGRESS.md Task 14 records CI run `https://github.com/pgdad/envoy-rust/actions/runs/25964680619` at HEAD `03e6435` (the substantive Task 14 commit), conclusion `success`, completed `2026-05-16T14:42:35Z`. Both jobs `success`: `build + test + lint` (~115 s; cache-warm; 11 steps green including the previously-CI-red `test (includes differential harness → Docker)` step now executing `admin_config_dump_server_info ... ok` at the Linux `docker0` bridge IP after this commit's fixture fix) + `fuzz (parse_bootstrap, 30s)` (~70 s; clean — the Task 12 `admin_multi_endpoint_bootstrap.yaml` seed exercised, 0 crashes). All 14 Docker-gated differential fixtures (`0001-tcp-echo` through `0014-admin-config-dump-server-info`) GREEN simultaneously in CI's `test` step. Workspace-wide: `cargo build` / `cargo clippy --all-targets --all-features -- -D warnings` / `cargo fmt --all -- --check` / `cargo test --workspace` / `cargo deny check` all clean. The `0e2987d` SHA-fixup commit is docs-only (PROGRESS placeholder resolution); CI did not need to re-run on it.
- **Reviewed:** 2026-05-16.

## §1. Verdict

**Approved with M-track follow-ups.**

Sub-phase 08.1 lands its 14 SPEC deliverables (D1 + D2 + D3 06.1 carryforward closures; D4 `Dispatch` enum + per-endpoint method-allowlist refactor; D5 `/server_info`; D6 `/config_dump` + `Bootstrap` `Serialize` derive cascade; D7 `/clusters` + `ClusterManager::clusters()` accessor; D8 `/listeners`; D13a `envoy-bin::main` shared-handle wiring partial — 3 of 4 handles; D15 harness `BodyRule::JsonShape` + `BodyRule::TextLines`; D17.1 fixture `0014`; D17.3a fuzz corpus seed; D17.4a in-process backstop; Task 14 state-4 verification) substantively and cleanly. The admin endpoint surface expands from 06.1's 3-endpoint GET set (`/ready`, `/stats`, `/stats/prometheus`) to a 7-endpoint GET set with the new admin GETs (`/config_dump`, `/server_info`, `/clusters`, `/listeners`) all running through the cleaned-up `serialize_response` + `Dispatch` machinery, and fixture 0014 bilaterally proves the endpoint shapes are wire-equivalent to upstream Envoy under the new harness body rules across both macOS Docker Desktop AND Linux Docker.

The cross-sub-phase architectural invariants from parent-08 SPEC §5 + 08.1 SPEC §5 all hold: `envoy-admin` stays sole-dep-owner of admin endpoints (no new top-level crate); HTTP/1.1-only admin (cross-sub-phase rule 3 from 06.1 — carried forward unchanged; no H2 admin surface leaked in); exact-match path routing + query-string strip (cross-sub-phase rule 5); method-strict per-endpoint allowlist (all 7 variants GET-only via `allowed_method() = "GET"`; 08.2's POST variants plug in additively with `"POST"`); `Bootstrap` `Serialize` derive cascade is mechanical and field-rename-symmetric to the existing `Deserialize` derives, with hand-rolled `Serialize` impls for the 5 types whose `Deserialize` is hand-rolled (`Route`, `RouteAction`, `SafeRegex`, `StringMatcher`, `HeaderMatcher`) mirroring the field-name + ordering + bool-emission shapes correctly; `ConfigDumpBody<'a>` + `ServerInfoBody<'a>` use borrowed-reference shape to avoid forcing a `Clone` cascade on `Bootstrap`; `/server_info`'s `state` field is the literal `"LIVE"` at 08.1 (08.2's D5e patches the value-binding source; the struct shape does not change at the boundary); ZERO new stats wired (08.2 owns the `server.live` / `server.state` / `listener_manager.total_listeners_active` gauges via `DrainState` + listener-observation hooks); `/ready` retains 06.1 semantics unchanged. The dep graph stays acyclic — the only new path-deps in 08.1 are workspace-internal (`envoy-admin → envoy-accesslog` for the `format_iso8601` wrapper; `envoy-admin → envoy-cluster` for `ClusterManager::clusters()`; `envoy-cluster` and `envoy-bin` already existed). No `unsafe` introduced in any new module (`#![forbid(unsafe_code)]` continues to bind at every crate root touched). **Zero new top-level Cargo deps** — `serde_json = "1"` is D-3.2-permitted and was already pulled transitively; Task 4 adds it as a direct dep on `envoy-config` (production), Task 13 adds it as a direct dep on `envoy-bin` `[dev-dependencies]` only. DECISIONS.md ledger head stays **ADR-0032**; no ADR was needed or skipped — the recommended no-foundations-grants posture per parent-08 SPEC §5.3 + 08.1 SPEC §7 + PLAN architecture-decision lock-in #18 was honored end-to-end.

**Zero Critical findings. Zero Important findings.** **Four Minor findings** (M1-M4): M1 — README.md:118-121 contradicts disk reality by claiming "No in-process backstop exists for this fixture" when Task 13 (`fcfb940`) landed exactly that backstop; doc-polish to bring the README in lockstep with the file tree. M2 — `value_may_differ_keys` field at `tests/differential/src/lib.rs:300` has no inline doc-comment (the variant-level doc covers the semantics, but a field-level one-liner would aid scanability). M3 — `command_line_options: BTreeMap<String, serde_yaml::Value>` is cloned per-request inside `/server_info` rendering at `crates/envoy-admin/src/handler.rs`; current 08.1 footprint is a single `config_path` entry so the cost is negligible, but if the map grows in a later phase, an `Arc<BTreeMap<...>>` wrap would be the no-cost-today forward-looking shape. M4 — `walk_pointer` at `tests/differential/src/lib.rs:379-394` produces a slightly opaque error message on a malformed dotted path containing an empty segment (`"node..id"` would yield `"key not found: "`); benign — fixtures never emit such paths — but a one-line guard would improve diagnosability.

Severity calibration matches the parent-06 / parent-07 cadence (06.1 / 06.2 / 06.3 / 07.1 / 07.2 all landed Approved-with-M-track). 08.1 ships its full surface with the disciplined per-task PROGRESS attestation pattern established at 06.1 and ratified at 07.1 — every code-changing task quotes the 5-command workspace gate including the full `cargo deny check` output (the 07.1-REVIEW doctrine reminder, honored at every Task 1-13 attestation); per-task workspace test counts inline; the in-process backstop attestation at Task 13; the comprehensive Task 14 state-4 CI anchor with both jobs and all 14 Docker-gated fixtures green simultaneously. The 93 PLAN-allowed deviations enumerated across the 13-task arc (1-3 per task; 6 on Task 4's mechanical cascade; 10 on Task 9's `/listeners` opportunistic closes; 11 on Task 11's fixture + harness amend pair) are honestly disclosed with file:line citations and rationale — each is a schema-constraint adaptation, line-number drift settlement, or test-module placement style choice that the PLAN explicitly authorized the executor to pick. Task 11/12/13's code-quality reviews each returned `Ready to merge — Yes, 0 Critical / 0 Important, 4-7 Minors deferred to phase-end REVIEW.md`; the 4 Minors recorded above are this state-5 holistic pass's accumulation of cross-task and surface-level concerns that the per-task reviews could not see in isolation. No finding rises above Minor; **per `BOOTSTRAP_PROMPT.md` §5.2 the phase does NOT re-enter state 3** — the verdict closes state 5 and the next session is the 08.1 state-6 close-out (which does NOT close parent-08 per the closing-sub-phase invariant — 08.2 owns parent-08 close-out and the MVP-trunk close).

## §2. Strengths

1. **The `AdminEndpoint` + `Dispatch` refactor is exemplary minimal-scope design.** `crates/envoy-admin/src/endpoint.rs` widens the existing `from_path(&str) -> Option<Self>` shape into a clean three-variant enum (`Endpoint(AdminEndpoint)` / `NotFound` / `MethodNotAllowed { allow: &'static str }`) with each `AdminEndpoint` variant declaring its allowed method internally (`allowed_method(&self) -> &'static str`); the previous hand-rolled GET-only 405 path at `handler.rs::handle_inner` collapsed into a single `match AdminEndpoint::dispatch(method, path) { ... }` covering all three arms. `render_405` widened to take the per-endpoint allow value and emit both an `Allow: <value>` header and a body mentioning the allowed method. The pattern is additive: 08.2's POST variants plug into the same `Dispatch` enum by declaring `allowed_method() = "POST"`; no further refactor of dispatch is needed. Closes 06.1 REVIEW M1 structurally — every endpoint now declares its 405 allow-list surface; the carryforward chain ends here.

2. **The `Bootstrap` `Serialize` derive cascade is mechanical, symmetric, and tested.** Task 4 (`ec12450`) added `Serialize` to 49 `Deserialize` derive lines in `crates/envoy-config/src/bootstrap.rs` and hand-rolled 5 `impl serde::Serialize` blocks for `Route` / `RouteAction` / `SafeRegex` / `StringMatcher` / `HeaderMatcher` (each mirrors the corresponding hand-rolled `Deserialize` field-name + ordering + bool-emission shape exactly). The PLAN architecture-decision lock-in #8 (`StringMatcher` / `HeaderMatcher` emit `ignore_case` / `invert_match` unconditionally) is implemented inline at `bootstrap.rs:998` / `:1192` — confirmed at this state-5 pass; this matches the original `Deserialize` `.unwrap_or(false)` defaulting and keeps the round-trip lossless. Two roundtrip tests landed (`fixture_0008_bootstrap_roundtrips_yaml_to_json` and `minimal_bootstrap_serializes_to_json`) — the first parses the largest non-trivial in-tree bootstrap (fixture 0008's HCM + STRICT_DNS + multi-route shape) through YAML → struct → JSON → struct → JSON and asserts idempotency; the second smoke-tests the minimal-valid bootstrap. The `#[serde(rename = "X")]` and `#[serde(rename_all = ...)]` attributes apply equally to both derives per serde semantics — no asymmetric `#[serde(skip)]` slipped in.

3. **The harness `BodyRule::JsonShape` + `BodyRule::TextLines` variants cover both the 08.1 and 08.2 admin-fixture shapes by construction.** `tests/differential/src/lib.rs` lines 290-301 (`JsonShape`) + 325-342 (`TextLines`) carry the full strictness model agreed at PLAN-time + the Task 11 per-side line-prefix extension. `JsonShape::required_keys` enforces bilateral presence; `required_subtree: Option<JsonSubtreeRule>` (lines 367-372, a separate struct deriving `PartialEq, Eq`) enforces dotted-path value equality via the new `walk_pointer` fn; `allowlist_envoy_only_keys` / `allowlist_envoy_rust_only_keys` handle bilateral key-set differences (e.g., uptime field naming); `value_may_differ_keys` allows shared keys to drift in value (e.g., `version`, `node`). `TextLines` mirrors the shape on lines: `required_lines` (exact match), `required_line_prefixes` (Task 11 NEW — for `<name>::endpoints` lines whose suffix varies), `allowlist_envoy_only_lines` / `allowlist_envoy_rust_only_lines` (exact-line allow-lists), `allowlist_envoy_only_line_prefixes` / `allowlist_envoy_rust_only_line_prefixes` (Task 11 NEW — for envoy's per-endpoint counter lines whose ephemeral port varies). 16 unit tests in `body_rule_extension_tests` discriminate each rule's pass / fail / shadow / non-shadow behavior. Error messages name the offending key or line and the failed rule.

4. **`Driver::AdminScrape` widening to `{ pre_requests, scrapes: Vec<AdminScrapeCase> }` is correctly minimal and `pre_requests`-once-only.** `tests/differential/src/lib.rs:140-144` widens the variant to a vector; fixture 0011's `expectations.yaml` migrates cleanly to a single-element `scrapes:` array with no semantic change (its existing `BodyRule::PrometheusExposition` still works). The `pre_requests` field stays at the variant level (not per-case), and the dispatch code at line 1328-1330 + line 2224 consumes `pre_requests` on the first sub-case only — the 50ms sleep-guard fires once per fixture, not per sub-case, avoiding fan-out latency. Fixture 0014 supplies an empty `pre_requests` (admin-only; no data-plane traffic); 08.2 will add `AdminAction`-bearing `pre_requests` for fixture 0015's drain assertion via the same plumbing.

5. **The `Bootstrap` Serialize cascade `Cargo.toml` discipline is correct.** `serde_json = "1"` was added to `crates/envoy-config/Cargo.toml` at Task 4 (production) and to `crates/envoy-bin/Cargo.toml` at Task 13 (`[dev-dependencies]` only, alphabetically inserted). Both additions are D-3.2-permitted (`serde_json` was already pulled transitively by many other workspace members so no new license-encounter is introduced; `cargo deny check` clean every task); the no-foundations-grants posture per 08.1 SPEC §7 + parent-08 SPEC §5.3 is preserved end-to-end. ADR-0032 stays the ledger head; ADR-0033 stays reserved-available.

6. **Carryforward closures are clean and structurally reinforced.** Task 1 (`d43d97a`) closes 06.1 REVIEW I2 (`serialize_response` case-insensitive header dedupe via `eq_ignore_ascii_case` over the 4 standard headers; `connection: close` deliberately outside the dedupe set per the 06.1 no-keep-alive posture) AND I2's companion 06.1 REVIEW M1 (`reason_for_status(u16) -> &'static str` covering 200/400/404/405/500/503; default fallback `"OK"`). Task 2 (`c6368f4`) closes 06.1 REVIEW M4 (`DRAIN_BUDGET` lifted to `crates/envoy-listener/src/lib.rs` as a module-level `pub const`; the parallel module-level constant in `envoy-admin` and the local-fn-scope const in `envoy-listener::Listener::serve` both deleted; `envoy-admin` re-imports). Task 3 (`7188e7f`) structurally reinforces 06.1 M1 — every endpoint now declares its 405-method-allowlist surface via `Dispatch::MethodNotAllowed { allow }`; the carryforward chain ends here. All three closures are verified by the cited file:line edits + the 7+3+6 new unit tests landed at the respective tasks; the chain 06.1 → 06.2 → 06.3 → 07.1 → 07.2 → 08.1 ends at this sub-phase for all three items.

7. **Fixture 0014's bilateral assertion is structurally sound and cross-platform.** `tests/fixtures/0014-admin-config-dump-server-info/expectations.yaml` (171 lines) drives 4 sub-cases through `Driver::AdminScrape` with the right body rule per endpoint: `JsonShape` for `/config_dump` + `/server_info` (with `required_subtree` pinning the `BootstrapConfigDump` envelope's `@type` and `state == "LIVE"` value-exact respectively, `value_may_differ_keys` for shared-but-divergent fields like `version` / `node`, per-side `allowlist_*_keys` for the uptime field-naming split); `TextLines` for `/clusters` + `/listeners` (with the bilateral cluster-name line required, envoy-only allowlist for the per-endpoint counter-line abundance covering BOTH `backend::192.168.65.254:` macOS Docker Desktop AND `backend::172.17.0.1:` Linux Docker `docker0` bridge IPs per the Task 14 fix). The fixture passes on both development (macOS) and CI (ubuntu-latest Linux) platforms simultaneously. The `envoy.yaml` ↔ `envoy-rust.yaml` divergences (bind address, `request_headers_to_remove`) are the minimal Envoy-default-injection neutralization mirroring the fixture-0008 / 0011 / 0013 precedent.

8. **In-process backstop is fast, isolated, and diagnostic-aware.** `crates/envoy-bin/tests/admin_config_dump_server_info.rs` (230 LoC) spawns `envoy-bin` against an in-memory bootstrap (admin + 1 listener with 1 `envoy.filters.network.echo` filter + 1 STRICT_DNS cluster resolving `localhost:7001`), uses `reserve_port()` for binding isolation, `kill_on_drop(true)` + explicit `kill()` for cleanup, `Connection: close` + `shutdown(Write)` to avoid the 5s admin idle-read timeout, and `stderr(Stdio::piped())` with conditional dump on `wait_ready` failure for diagnosability. Scrapes all 4 admin endpoints; asserts JSON parse + required-key presence on `/config_dump` + `/server_info` and line-presence on `/clusters` + `/listeners`. Complements fixture 0014's bilateral Docker-gated assertion with a Docker-free happy-path scrape running in ~3-5 seconds. The 07.2 `http_filter_header_mutation.rs` stderr-dump convention is correctly adopted (it surfaced the Task 13 deviation #5 `validator guarantees ≥1 filter` panic during the in-arc iteration; suppressing stderr would have left a silent "Connection refused").

9. **PROGRESS.md is self-narrating and disclosure-disciplined across all 14 tasks.** The Task 1 preamble enumerates the 6 PLAN-write SPEC corrections + the 20 architecture-decision lock-ins + the 06.1 carryforward inventory; every code-changing task carries a "Deviations from PLAN" section with file:line citations and rationale (93 PLAN-allowed deviations across the state-3 arc; +2 at Task 14 — total 95 — all openly narrated); every task's attestation quotes the full 5-command workspace gate including the full `cargo deny check` output (the 07.1-REVIEW doctrine reminder honored mechanically); the Task 11 PROGRESS narrates the 2-iteration empirical seeding loop (capture run → seed allow-lists → validation run → GREEN; total iterations 2 of the brief's 5-iteration budget); Task 12 PROGRESS narrates the 5 schema-constraint adaptations (no `connect_timeout`, populated locality, mandatory `lb_policy`, single-listener cap, `+nightly` invocation) and the 370 923-iteration fuzz run; Task 13 PROGRESS records the runtime-discovered `filter_chains: []` panic and the trivial-echo-filter workaround; Task 14 PROGRESS records the full CI diagnosis cadence (`gh run rerun` → `gh run view --log-failed` → bisect-not-needed; failure pinned to fixture 0014 Linux/macOS bridge-IP asymmetry).

10. **State-4 evidence anchor is complete, both jobs green, both platforms covered.** PROGRESS Task 14 cites CI run `25964680619` at HEAD `03e6435`, conclusion `success`, completed `2026-05-16T14:42:35Z`. Both jobs green: `build + test + lint` (115 s; cache-warm; all 11 steps green including the previously-CI-red `test (includes differential harness → Docker)` step now executing `admin_config_dump_server_info ... ok` at the Linux `docker0` bridge IP) + `fuzz (parse_bootstrap, 30s)` (70 s; clean; the Task 12 seed exercised). All 14 Docker-gated fixtures (`0001-tcp-echo` through `0014-admin-config-dump-server-info`) GREEN simultaneously in CI; h2spec held at the 05.2 baseline 99.31% (08.1 engaged no H2-framing surfaces); workspace tests across 50+ binaries clean. The evidence-discipline regression flagged at 05.3 REVIEW I3 + 06.3 REVIEW I3 is honored — a real CI URL + HEAD SHA + completion timestamp + per-gate quoted output are all present.

## §3. Issues

### Critical (must fix before phase commit)

None.

### Important (close in 08.1 or carry forward with named owner phase)

None.

### Minor (awareness-only / track forward)

**M1. Fixture 0014 README contradicts disk reality on the in-process backstop.** `tests/fixtures/0014-admin-config-dump-server-info/README.md` lines 118-121 claim: *"the fixture is green at the Docker-gated CI level (`tests/differential/tests/admin_config_dump_server_info.rs`). No in-process backstop exists for this fixture (the differential level is the only level that exercises the upstream-Envoy admin output)."* This was correct at Task 11 (`6ee1ad7`) — the README was authored there — but Task 13 (`fcfb940`) landed exactly that in-process backstop at `crates/envoy-bin/tests/admin_config_dump_server_info.rs` (230 LoC). The README was not updated in lockstep. **Why it matters:** D-3.4 stranger-readability — a future maintainer hitting a failure at the in-process level might double back to the README, find the "no in-process backstop exists" claim, and waste time hunting for an inconsistency. **Fix sketch:** replace lines 118-121 with: *"the fixture is green at the Docker-gated CI level (`tests/differential/tests/admin_config_dump_server_info.rs`) AND the in-process happy-path level (`crates/envoy-bin/tests/admin_config_dump_server_info.rs`). The differential level provides the bilateral assertion against upstream Envoy; the in-process level provides a fast Docker-free smoke test of the 4-endpoint happy path."* **Disposition:** awareness-only documentation drift; **close opportunistically at the 08.1 state-6 close-out commit** (which already touches docs — fold the README fix into that commit), or **at the 08.2 state-1 brainstorm** if the close-out runs first. Named owner: 08.1 state-6 close-out session. Does NOT gate the state-6 close-out (D-3.6 green-build invariant is unaffected; the README claim is non-load-bearing).

**M2. `value_may_differ_keys` field on `BodyRule::JsonShape` has no inline field-level doc-comment.** `tests/differential/src/lib.rs:300` declares `pub value_may_differ_keys: Vec<String>`; the variant-level doc (lines 281-284) explains the semantics correctly (*"keys present on BOTH sides and NOT in `value_may_differ_keys` MUST serialize equal"*), and the dispatch arm at lines 2560-2561 implements it correctly, but a one-line doc-comment on the field itself would aid scanability when a maintainer is reading the struct declaration in isolation (e.g., from an LSP hover). **Why it matters:** very minor stranger-readability — the semantics are documented at the variant level and implemented correctly; the cost of adding a field-level comment is trivial; the cost of missing it is occasional confusion when a YAML schema lookup lands directly on the field. **Fix sketch:** prepend `/// Shared keys whose values may differ bilaterally; presence is required, value equality is not.` above line 300. **Disposition:** awareness-only documentation polish; **close opportunistically at the next phase that touches `tests/differential/src/lib.rs`** (likely 08.2 D16 when `Driver::AdminScrape` is widened with `post_admin_assertions`). Named owner: 08.2.

**M3. `command_line_options: BTreeMap<String, serde_yaml::Value>` is cloned per-request inside `/server_info` rendering.** `crates/envoy-admin/src/handler.rs` clones the `BTreeMap` from `&self` into the borrowed `ServerInfoBody<'a>` on every `/server_info` GET. At 08.1's scope the map has a single entry (`config_path`) so the clone cost is negligible — the comment at the construction site explicitly notes "small in 08.1 — single `config_path` entry". **Why it matters:** purely forward-looking — if a later phase widens the CLI surface (more flags, more options, runtime-RTDS-fed entries) the per-request clone scales linearly with the map size. **Fix sketch (if ever needed):** wrap the map in `Arc<BTreeMap<...>>` at construction time and clone the `Arc` instead. ~5 LoC; mechanical. **Disposition:** awareness-only forward-looking note; **carry forward indefinitely** — activates only if a future deployment surfaces a wide CLI option set. Named owner: whichever future phase first widens `envoy-bin`'s CLI surface beyond the current single-flag (`-c <config>`) shape.

**M4. `walk_pointer` produces a slightly opaque error on a dotted path with an empty segment.** `tests/differential/src/lib.rs:379-394` walks a dotted-segment path like `"node..id"` (double dot) by `dotted_path.split('.')` — the empty segment between the two dots becomes the literal empty string `""`, which then `parse::<usize>()` fails, then `cur.get("")` fails on the object, and the error message becomes `"key not found: "` (with a trailing blank). The fixture authors never emit malformed paths like this in practice (every dotted-path subtree assertion in fixture 0014's `expectations.yaml` is well-formed), so the error path is unreachable in normal use. **Why it matters:** very minor — if a future fixture is hand-edited and accidentally introduces a double-dot, the trailing-blank error message is harder to diagnose than a structured one (e.g., *"empty path segment at position N in 'node..id'"*). **Fix sketch:** add a one-line guard at the top of `walk_pointer` body: `if dotted_path.split('.').any(|s| s.is_empty()) { anyhow::bail!("dotted path contains empty segment: {dotted_path:?}"); }`. **Disposition:** awareness-only diagnosability nit; **close opportunistically at the next phase that touches `walk_pointer`** (likely 08.2 D16 or whichever later phase extends `JsonShape` further). Named owner: 08.2 or later harness-touch phase.

## §4. Carryforward inventory

For each Important / Minor item: closure plan.

| ID | Severity | Item | Closure plan |
|---|---|---|---|
| M1 | Minor | Fixture 0014 README claims "no in-process backstop exists" but the backstop landed at Task 13 (`fcfb940`) | **Close opportunistically at the 08.1 state-6 close-out commit** — fold the 3-line README update into the state-6 commit alongside the ROADMAP / STATE / next-skill rewrites (the state-6 commit already touches docs; this is additive). If the close-out runs without addressing it, **fold into 08.2's state-1 brainstorm session** (which has natural occasion to revisit fixture 0014 documentation when sketching fixture 0015's parallel shape). Named owner: 08.1 state-6 close-out. Does NOT gate the close-out (non-load-bearing claim; doc drift only). |
| M2 | Minor | `BodyRule::JsonShape::value_may_differ_keys` field has no inline doc-comment | **Close opportunistically at 08.2 D16** when `Driver::AdminScrape` is widened with `post_admin_assertions`. The same edit pass naturally touches the surrounding rule declarations. ~1-line edit. Named owner: 08.2. |
| M3 | Minor | `command_line_options` `BTreeMap` cloned per-request in `/server_info` rendering — currently negligible (single entry); scales linearly with map size if CLI surface grows | **Carry forward indefinitely.** Activates only if a future phase widens the CLI surface beyond the single-flag `-c <config>` shape. ~5 LoC `Arc<BTreeMap<...>>` wrap when needed. Named owner: whichever future phase first widens `envoy-bin`'s CLI surface. |
| M4 | Minor | `walk_pointer` produces opaque error on dotted path with empty segment (e.g., `"node..id"` → `"key not found: "`) | **Close opportunistically at 08.2 D16 or whichever later harness-touch phase first extends `walk_pointer` / `JsonShape`.** ~3-line guard at function head. Named owner: 08.2 or later. |

**Process note (awareness-only, no carryforward).** Task 13 deviation #5 surfaced a real schema-vs-runtime inconsistency that is worth flagging for the 08.2 PLAN-writer: the `envoy-config` parser ACCEPTS `filter_chains: []` (no validator rejects empty filter chains at this layer), but `envoy-bin/src/main.rs:158` panics with `.expect("validator guarantees ≥1 filter")` when no filter is present. The Task 13 PROGRESS narrates this clearly and the workaround (add a trivial `envoy.filters.network.echo` filter) is correct for an admin-only backstop, but this is effectively a **schema-constraint drift item** that 08.2's `admin_drain_listeners.rs` in-process backstop will face symmetrically. The 08.2 PLAN-writer should either (a) add a parse-time validator to `envoy-config` rejecting empty `filter_chains` (closes the inconsistency at the schema layer; small validator extension), or (b) document the trivial-filter workaround inline in the 08.2 backstop's bootstrap construction and move on (matches the Task 13 disposition; zero schema-layer work). Recommended: option (b) at 08.2 state-3 + option (a) deferred to a hardening phase. **Do not re-open Task 13** — its disposition is correct.

**Standing carryforwards inherited but not closed in 08.1** (per parent-07 SPEC §4 + 07.2 REVIEW §4 + 08.1 SPEC §4):

- **07.2 REVIEW M1 / M2 / M3** — not engaged by 08.1 (`HttpFilterInstance::build` position plumbing / `apply_mutations` Overwrite O(n²) YAGNI / fixture-0013 `expected_body` coupling). Carry forward unchanged per 07.2's named-owner dispositions.
- **Phase 06.3 REVIEW I1** (verification-discipline gap — workspace-tests-clean does not include Docker-gated bilateral fixture runs) — **structurally closed at 07.1**; the 08.1 arc continues the two-layer attestation pattern (per-task PROGRESS test-bucket attestation at every code-changing task + the comprehensive Task 14 state-4 CI anchor). Carries no further. The cross-platform CI red at Tasks 11/12/13 was caught at exactly the two-layer pattern's intended signal — the local Docker Desktop layer was green; the CI Linux layer surfaced the bridge-IP-asymmetry; Task 14's fix restored simultaneous green. The discipline worked.
- **Phase 06.3 REVIEW I2** (synthetic 5xx backend + 4-class `pre_requests` deferred from 06.3 Task 11) — **carries forward unchanged through 08.1**. 08.1 introduced no synthetic-backend infrastructure; the upstream-robustness family is still the natural close site.
- **Phase 06.2 REVIEW M1 / M2 / M4 / M5** — not engaged by 08.1; carry forward unchanged.
- **Phase 06.1 REVIEW I2 / M1 / M4** — **all closed in 08.1.** See §5 below.
- **Phase 06.1 REVIEW M2 / M3 / M5 / M6** — carry forward indefinitely.
- **Phase 05.3 REVIEW I2** (typed-error chain dissolution at the H2 dispatch site via `format!("{e}")`) — **carries forward unchanged**. 08.1 does not re-engage the H2 dispatch site.
- **Phase 05.2 REVIEW I1 / I2 / I3** (CI h2spec tarball SHA-256 / `Http2Error` write-path variant rename / `MalformedH2HeaderBlock` overload split) — not engaged by 08.1 (no `.github/workflows/ci.yml` edit; no h2 codec-edge surface touched); carry forward unchanged.
- **Phase 04.1 REVIEW M5 / M9** (Cargo.lock cadence ratification ADR) — **carries forward unchanged through 08.1** per 08.1 SPEC §7's no-foundations-grants posture honored end-to-end. 08.1 introduced zero new top-level Cargo deps (`serde_json = "1"` added at Task 4 to `envoy-config` and Task 13 to `envoy-bin/[dev-dependencies]` is D-3.2-permitted and was already pulled transitively; the new path-deps on `envoy-admin → envoy-accesslog` and `envoy-admin → envoy-cluster` are workspace-internal). The `Cargo.lock` diff in the reviewed range is minimal (workspace-internal path-dep registrations only). The cadence pick stays unforced.
- **Phase 04.1 REVIEW M7 / M-claim / M1 / M2 / M4** — not engaged by 08.1; carry forward unchanged.
- **Phase 02.2 REVIEW M1** (`*EchoBackend::Drop` polling loop blocks on `std::thread::sleep`) — **standing carryforward**. 08.1's fixture 0014 reuses the existing `Http1EchoBackend` harness primitive unchanged (via fixture 0008's bootstrap shape inheritance); the chain continues.

## §5. Disposition decisions

**Closed in 08.1** (3 carryforwards from 06.1 REVIEW — all confirmed by the holistic review):

- **06.1 REVIEW I2** (`serialize_response` injects 5 standard headers unconditionally — duplicate-header hazard if a future endpoint sets the same name) — **CLOSED at Task 1 (`d43d97a`, D1).** `crates/envoy-admin/src/handler.rs::serialize_response` rewrote the default-header emission block with case-insensitive dedupe via a `has_header` closure calling `eq_ignore_ascii_case`. The 4 standard admin headers (`cache-control`, `x-content-type-options`, `server`, `date`) are now dedupe-guarded; the always-emitted `connection: close` line is intentionally OUTSIDE the dedupe set per the 06.1 no-keep-alive posture. 7 new unit tests in `serialize_response_dedupe_and_reason_tests` verify the dedupe is case-insensitive AND that caller-provided headers override (the test `dedupe_preserves_caller_provided_cache_control` is genuinely discriminating — it constructs a response with a custom `cache-control` value and asserts only one `cache-control:` line lands on the wire, with the caller's value). The chain 06.1 → 06.2 → 06.3 → 07.1 → 07.2 → 08.1 ends at this commit. This REVIEW.md ratifies the closure.

- **06.1 REVIEW M1** (`resp.reason.unwrap_or("OK")` at `serialize_response` — every status code reports "OK" as the reason phrase if `reason` is `None`) — **CLOSED at Task 1 (`d43d97a`, D2).** `reason_for_status(u16) -> &'static str` helper landed at module level in `crates/envoy-admin/src/handler.rs` covering 200 → "OK", 400 → "Bad Request", 404 → "Not Found", 405 → "Method Not Allowed", 500 → "Internal Server Error", 503 → "Service Unavailable" (fallback "OK"). `serialize_response`'s status-line construction now uses `resp.reason.unwrap_or_else(|| reason_for_status(resp.status))`. Two new unit tests verify (i) a `Response { status: 503, reason: None, ... }` renders `"503 Service Unavailable"` and (ii) an explicit `reason` overrides the helper. **Structurally reinforced at Task 3 (`7188e7f`, D4)** via the `Dispatch::MethodNotAllowed { allow }` arm — every endpoint now declares its 405-method-allowlist surface via `allowed_method(&self) -> &'static str`, so the 405 reason phrase is no longer hand-rolled per-endpoint. The chain 06.1 → 06.2 → 06.3 → 07.1 → 07.2 → 08.1 ends at this commit.

- **06.1 REVIEW M4** (`DRAIN_BUDGET` is duplicated across two source files — `crates/envoy-admin/src/handler.rs:28` (module-level) and `crates/envoy-listener/src/lib.rs:165` (local-fn-scope inside `Listener::serve`)) — **CLOSED at Task 2 (`c6368f4`, D3).** A single `pub const DRAIN_BUDGET: Duration = Duration::from_secs(5);` at module level in `crates/envoy-listener/src/lib.rs`; both prior declarations deleted; `crates/envoy-admin` re-imports via the existing `envoy-listener` crate dep with `use envoy_listener::DRAIN_BUDGET;`. 3 new lockstep tests (one verifying the constant is `pub const` at the module level; one verifying the value is 5 seconds; one verifying `envoy-admin` resolves the imported identifier to the same numeric value — a compile-time tautology after the hoist; the test exists to lock the lockstep). The chain 06.1 → 06.2 → 06.3 → 07.1 → 07.2 → 08.1 ends at this commit.

**Continuing carryforwards by named owner phase:** see §4 standing carryforwards inventory above.

**No new carryforwards generated by 08.1 that gate any future phase.** The 4 Minor findings (M1 README doc-drift, M2 field-level doc-comment, M3 forward-looking `Arc<BTreeMap<...>>`, M4 `walk_pointer` empty-segment error message) are all awareness-only / close-opportunistically; none has a hard-gated named owner. M1 is folded into the 08.1 state-6 close-out commit by recommended disposition; M2 and M4 are folded into 08.2 D16 by recommended disposition; M3 is indefinite-carry.

## §6. Phase-done gate (§7.5) verification

Per `BOOTSTRAP_PROMPT.md` §7.5 and 08.1 SPEC §1 acceptance signal (a)-(f), re-attested at the Task 14 state-4 evidence anchor — PROGRESS Task 14 cites CI run `25964680619` at HEAD `03e6435` (the substantive Task 14 commit), conclusion `success`, completed `2026-05-16T14:42:35Z`:

**(a) New differential fixture `0014-admin-config-dump-server-info` green.** ✓ — `admin_config_dump_server_info` (0014) passed in CI run `25964680619` / job `build + test + lint` at the Linux Docker `docker0` bridge IP (`backend::172.17.0.1:*` per-endpoint counter lines absorbed by the Task 14 allowlist extension). PROGRESS Task 11 additionally records the Docker-gated local run GREEN on the second empirical iteration (Task 11 deviation #11 + the Task 14 fix together establish the cross-platform-portable allowlist).

**(b) Pre-existing 13 differential fixtures green simultaneously.** ✓ — all 13 (`0001-tcp-echo` through `0013-http-filter-header-mutation`) GREEN simultaneously alongside the new fixture 0014 inside the unified `cargo test --workspace` invocation in CI run `25964680619`. PROGRESS Task 14 §(b) enumerates all 14 fixtures by name with `... ok` status. **All 14 fixtures green in the same CI run on Linux Docker; all 14 also green simultaneously at local macOS Docker Desktop per PROGRESS Task 11/12/13/14 local attestations.**

**(c) `h2spec` ≥95% pass with `known-failures.txt` unchanged.** ✓ — 99.31% (`h2spec_pass_rate_gate` PASS; 05.2 baseline 99.31% carried forward unchanged; `known-failures.txt` unchanged — 08.1 engages no H2-framing surfaces; HTTP/1.1-only admin invariant from 06.1 preserved).

**(d) `parse_bootstrap` fuzz target clean for short-budget CI run.** ✓ — Job 2 `fuzz (parse_bootstrap, 30s)` conclusion `success`; the Task 12 `admin_multi_endpoint_bootstrap.yaml` seed is in the corpus and was exercised (PROGRESS Task 12 additionally records a local short-budget run of 370 923 iterations clean in 31 s; PROGRESS Task 14 records a re-run of 485 183 iterations clean in 31 s). No new fuzz target ships in 08.1.

**(e) Stable-toolchain gates clean.** ✓ — Job 1 `build + test + lint` includes all five gates: `cargo fmt --all -- --check` (clean); `cargo clippy --workspace --all-targets --all-features -- -D warnings` (clean); `cargo build --workspace --all-targets` (clean); `cargo test --workspace` (all 50+ test binaries green at HEAD `03e6435` — including the 14 differential integration buckets, 13 `envoy-bin` integration buckets, 12 lib buckets, h2spec conformance bucket, helper-crate buckets; 0 failed across the workspace); `cargo deny check` (`advisories ok, bans ok, licenses ok, sources ok` — the pre-existing `license-not-encountered` warnings on unmatched `deny.toml` allow-list entries per ADR-0005 are not new from 08.1 and not errors). Every per-task PROGRESS attestation (Tasks 1-13 substantive) quotes the full `cargo deny check` output per the 07.1-REVIEW doctrine reminder.

**(f) REVIEW.md verdict** lands at state-5 in this session per the `SKILL_ROUTING.md` state-5 transition. **Verdict per §1: Approved with M-track follow-ups** (zero Critical; zero Important; four Minor — M1 README doc-drift, M2 field-level doc-comment, M3 forward-looking `Arc<BTreeMap<...>>`, M4 `walk_pointer` empty-segment error message; all awareness-only / close-opportunistically). Gate (f) closes at this REVIEW.md verdict.

**All six §7.5 gates disposition: (a) PASS / (b) PASS / (c) PASS / (d) PASS / (e) PASS / (f) CLOSE-at-this-REVIEW.md.**

## §7. Per-task code-quality reviewer finding dispositions

The 08.1 state-3 arc ran each task through a fresh subagent's two-stage review (spec-compliance + code-quality). PROGRESS records the per-task review residue; disposition per task:

**Task 1 reviewer — accepted-as-noted Minors only.** Landed clean at `d43d97a`; the 3 deviations (helper-signature adaptation to `envoy_http1::Response`'s `reason: Option<&'static str>` + `body: bytes::Bytes` actual shape; `unwrap_or_else` not `as_deref().unwrap_or_else`; `connection: close` clarifying comment) are mechanical adaptations to disk reality that the PLAN authorized the executor to pick. No fix round; no residue.

**Task 2 reviewer — accepted-as-noted Minors only.** Landed clean at `c6368f4`; the 3 deviations (line-number drift settlement; unqualified `Duration` matching file style; sibling vs nested test-module placement) are PLAN-authorized style picks. No fix round; no residue.

**Task 3 reviewer — accepted-as-noted Minors only.** Landed clean at `7188e7f`; the 4 deviations (sibling test-module placement; `render_405` body-shape adoption per PLAN Step 4 explicit "executor adapts"; line-number drift; `Bytes::from(format!(...))` idiomatic conversion) are PLAN-authorized adaptations. No fix round; no residue.

**Task 4 reviewer — one post-review amend (in-arc).** Landed at `ec12450`; the 6 deviations include the full 49-line derive cascade inventory (the PLAN estimated 25-32; the file grew between SPEC and PLAN) and the 5 hand-rolled `impl Serialize` block landings. The post-review amend hardened `StringMatcher`/`HeaderMatcher` to emit `ignore_case`/`invert_match` unconditionally per PLAN architecture-decision lock-in #8 (the original draft conditionally emitted on non-default values — incorrect, because a YAML input with `ignore_case: true` would round-trip back to `false` if the JSON omitted the field; defaulting is the parser's job, not the emitter's). The holistic state-5 review independently confirms the unconditional-emit shape at `bootstrap.rs:998` / `:1192`. No residue.

**Task 5 reviewer — accepted-as-noted Minors only.** Landed clean at `1a05eaa`; the 8 deviations are call-site enumeration + parameter-order picks + the 6-arg-not-5-arg settlement per PLAN-write SPEC correction 4 + the `format_iso8601` `pub` wrapper landing per PLAN-write SPEC correction 2. No fix round; no residue.

**Task 6 reviewer — accepted-as-noted Minors only.** Landed clean at `24f8382`; the 8 deviations are `serde_json::to_vec_pretty` vs `to_string` byte-shape pick + `last_updated` rendering via the Task 5 `format_iso8601` wrapper + `ConfigDumpEntry` enum tagging shape settlement + BEHAVIOR_CONTRACT row authoring. No fix round; no residue.

**Task 7 reviewer — accepted-as-noted Minors only.** Landed clean at `f463a3c`; the 9 deviations are `state: "LIVE"` literal (per architecture-decision lock-in #3), `uptime_*` epoch arithmetic, `command_line_options: BTreeMap` shape, hot-restart-version literal `"disabled"`, version literal `"envoy-rust 0.1.0"`, `ServerInfoBody<'a>` borrowed shape, BEHAVIOR_CONTRACT row, and minor field-ordering settlements. No fix round; no residue.

**Task 8 reviewer — accepted-as-noted Minors only.** Landed clean at `1a9a3ed`; the 8 deviations are `ClusterManager::clusters()` accessor landing per PLAN-write SPEC correction 3 + `/clusters` 2-line minimum per architecture-decision lock-in #10 + sorted-by-name ordering + BEHAVIOR_CONTRACT row. No fix round; no residue.

**Task 9 reviewer — accepted-as-noted Minors only.** Landed clean at `2bb3545`; the 10 deviations are `/listeners` 1-line shape per Envoy v1.33 default format + sorted-by-name ordering + opportunistic closures of small grep-discovered nits + BEHAVIOR_CONTRACT row (completing the 4-row admin-endpoint body-shapes subsection for the GET surface). No fix round; no residue.

**Task 10 reviewer — accepted-as-noted Minors only.** Landed clean at `9cf8831`; the 10 deviations include `JsonSubtreeRule` extracted as a struct (rather than inline tuple) for `PartialEq, Eq` cleanliness, `walk_pointer` returning `Result` not `Option`, and the per-side-line-prefix family deferred to Task 11 (when fixture 0014's empirical iteration surfaced the need). No fix round; no residue.

**Task 11 reviewer — one in-arc amend.** Landed at `6ee1ad7` post-amend; the 11 deviations include the post-amend strictness wiring tightening (Task 10's variant accepted bilateral asymmetry; Task 11's amend tightened to strict-required-keys + per-side allowlist asymmetry only), the per-side line-prefix family addition, the `Box<BodyRule>` clippy fix, `check_content_type` parameter-tolerance, `DIFFERENTIAL_DUMP_ADMIN` env-var, and the 50ms sleep-guard. The empirical seeding loop took 2 iterations (capture → seed → validate → GREEN) — well within the 5-iteration brief budget. No residue from the in-arc amend.

**Task 12 reviewer — accepted-as-noted Minors only.** Landed clean at `7711915`; the 5 deviations are 4 schema-constraint adaptations (no `connect_timeout`, populated locality, mandatory `lb_policy`, single-listener cap) + the `+nightly` invocation correction. The 4 schema-constraint adaptations are PLAN-anticipated drift-verification items the cold-start prompt explicitly named as carry-forward concerns to 08.2. No fix round; no residue.

**Task 13 reviewer — accepted-as-noted Minors only.** Landed clean at `fcfb940`; the 6 deviations include the 4 Task-12-mirrored schema constraints + the 5th runtime constraint (`filter_chains: []` panics at `envoy-bin/src/main.rs:158`) + the `serde_json` `[dev-dependencies]` addition. The 5th runtime constraint is a real schema-vs-runtime inconsistency carried forward to 08.2 (see §4 Process note). The `stderr(Stdio::piped())` + conditional dump on failure adoption from 07.2 was the diagnostic safety net that surfaced the panic in-arc; suppressing stderr would have left a silent "Connection refused".

**Task 14 reviewer — uncontroversial state-4 commit; no separate code-quality review per the 07.2 Task 10 / 06.1 Task 14 / 06.3 Task 12 state-4-commit precedent.** The 2 deviations are (a) the in-flight fixture-coverage fix landing at the state-4 commit per cold-start guidance and (b) the 2-commit pattern (substantive + SHA-fixup) applied to Task 14 because the predecessor Task 13 CI run was deterministic-failure cured by the fixture fix. Both deviations are PLAN-anticipated and cold-start-prompt-named. The fixture-allowlist line addition is data, not code; no production surface changed at this commit.

**Carryforward summary across the per-task reviewer findings:** 1 task (Task 4) carried one in-arc amend; 1 task (Task 11) carried one in-arc amend; 11 tasks (1, 2, 3, 5, 6, 7, 8, 9, 10, 12, 13) landed with only accepted-as-noted Minors. The holistic state-5 pass re-raises 4 awareness-only Minor concerns the per-task reviews could not see in isolation (§3 M1 README ↔ Task 13 cross-task lockstep drift; §3 M2 field-level doc-comment opportunistic polish; §3 M3 forward-looking BTreeMap clone; §3 M4 `walk_pointer` empty-segment error diagnosability). None rises above Minor. No per-task finding is propose-cleanup-in-08.1 — the state-4 evidence anchor at `03e6435` / CI run `25964680619` is complete; reopening would force a re-run of the state-4 gate without a commensurate quality gain. The M1 README fix folds into the 08.1 state-6 close-out commit by recommended disposition.

## §8. State-4 evidence re-attestation

Per the state-5 reviewer's mandate to re-attest each of the §7.5 phase-done gate's six sub-gates against the Task 14 state-4 anchor (PROGRESS Task 14; CI run `25964680619` HEAD `03e6435` `success` `2026-05-16T14:42:35Z`):

| Gate | Disposition | Evidence |
|---|---|---|
| **(a)** New differential fixture `0014` green | **PASS** | `admin_config_dump_server_info` (0014) passed in CI run `25964680619` / job `build + test + lint` at the Linux Docker `docker0` bridge IP after the Task 14 cross-platform fix. PROGRESS Task 11 + Task 14 record the Docker-gated local run GREEN on both platforms. |
| **(b)** Pre-existing 13 fixtures green simultaneously | **PASS** | All 13 (`0001-0013`) + the new `0014` GREEN simultaneously inside the unified `cargo test --workspace` in CI run `25964680619`; job conclusion `success`. PROGRESS Task 14 enumerates all 14 by name. **14/14 in one run.** |
| **(c)** h2spec ≥95% with known-failures.txt unchanged | **PASS** | 99.31% (`h2spec_pass_rate_gate` PASS; 05.2 baseline carried forward; `known-failures.txt` unchanged — 08.1 engages no H2-framing surfaces; HTTP/1.1-only admin invariant from 06.1 preserved). |
| **(d)** parse_bootstrap fuzz clean for short-budget CI run | **PASS** | Job 2 `fuzz (parse_bootstrap, 30s)` conclusion `success`; the Task 12 `admin_multi_endpoint_bootstrap.yaml` seed in the corpus, exercised. PROGRESS Task 12 records a local 370 923-iteration run, no crash; PROGRESS Task 14 records a local 485 183-iteration re-run, no crash. |
| **(e)** Stable-toolchain gates clean (fmt / clippy / build / test / deny) | **PASS** | All 5 within Job 1 `build + test + lint` clean: fmt clean; clippy `-D warnings` clean; build clean; test all 50+ binaries green (0 failed) at HEAD `03e6435`; cargo deny `advisories ok, bans ok, licenses ok, sources ok`. Every per-task PROGRESS attestation (Tasks 1-13 substantive) quotes the full `cargo deny check` output. |
| **(f)** REVIEW.md approved | **CLOSE at this REVIEW.md** | Verdict per §1: **Approved with M-track follow-ups** (zero Critical; zero Important; four Minor — all awareness-only / close-opportunistically). Gate (f) closes at this verdict. |

**All six §7.5 gates: (a)-(e) PASS / (f) CLOSE-at-this-REVIEW.md.** 08.1's state-4 evidence is complete; state-5 closes at this REVIEW.md commit; state-6 is a separate session.

---

*End of REVIEW.md. 08.1 phase-done state-5 complete. Verdict: **Approved with M-track follow-ups** (zero Critical; zero Important; four Minor). Per `BOOTSTRAP_PROMPT.md` §5.2, the phase does NOT re-enter state 3 — no Critical or Important findings. All three 06.1 REVIEW carryforwards (I2 `serialize_response` case-insensitive header dedupe; M1 `reason_for_status` helper at the response-line level + structurally reinforced via the `Dispatch::MethodNotAllowed { allow }` arm; M4 `DRAIN_BUDGET` module-level hoist) are confirmed closed. The next session enters the **08.1 state-6 close-out** — which does NOT close parent-08 per the closing-sub-phase invariant (08.2 owns parent-08 close-out + the MVP-trunk close). The state-6 close-out commit: flips ROADMAP row `08.1` `in-progress` → `done`; advances STATE.md to phase `08.2` lifecycle state 2 (next-skill `superpowers:writing-plans` scoped to the 08.2 SPEC at `docs/envoy-rust/phases/08.2-endpoint-triggered-drain/SPEC.md`); lands the state-6 close-out commit with title `phase 08.1: admin endpoint surface (config_dump, server_info, clusters, listeners) + 06.1 carryforward closures (I2, M1, M4)` per 08.1 SPEC §9 (no ADRs bracketed — no ADRs landed in 08.1 per the recommended no-foundations-grants posture honored end-to-end; ADR-0033 stays reserved-available for 08.2 if needed); and folds the §3 M1 README doc-drift fix (`tests/fixtures/0014-admin-config-dump-server-info/README.md` lines 118-121 acknowledging the in-process backstop) into the same docs-bearing commit per recommended disposition. The remaining 3 Minor findings (M2 field-level doc-comment, M3 forward-looking `Arc<BTreeMap<...>>`, M4 `walk_pointer` empty-segment error message) are all awareness-only / close-opportunistically with no hard-gated owner — they do NOT gate the state-6 close-out and are recorded in §4 for the state-6 STATE.md "Phase-08.1 rollovers" subsection.*
