# Phase 03.2 Progress

## Task 1 — envoy-config: FilterChainMatch struct + server_names + 5 parse-shape tests (2026-04-26)

- Commit: 1e1ea64
- Change: Inserted `FilterChainMatch` struct (single `server_names: Vec<String>` field, `#[serde(deny_unknown_fields)]`, `#[derive(Debug, Deserialize, PartialEq)]`) in `crates/envoy-config/src/bootstrap.rs`. Added `filter_chain_match: Option<FilterChainMatch>` as the first field on `FilterChain` (mirrors Envoy's bootstrap proto field ordering) and extended its derive set with `PartialEq` (Task 2's validator tests will rely on it). Re-exported `FilterChainMatch` from `crates/envoy-config/src/lib.rs`. Appended 5 parse-shape tests covering present / missing / empty-map / empty-list / unknown-field cases.
- Verification: `cargo test -p envoy-config --lib` reported 55 passed (50 pre-existing from 03.1 + 5 new). Workspace gate clean: `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check` all exit 0. Cargo.lock unchanged.
- Deviation from PLAN: none.

## Task 2 — envoy-config: 3 validator variants + 6 validator tests + 2 fuzz seeds (2026-04-26)

- Commit: 17d43cd
- Change: Added 3 cross-chain `ConfigError` variants to `crates/envoy-config/src/lib.rs` — `MultipleListenersWithOverlappingSni { listener, sni }` (variant name preserved verbatim from parent SPEC §7's projection even though the rule is intra-listener; doc-comment names the intra-listener semantics), `MultipleCatchAllFilterChains { listener }`, `MixedTlsAndPlaintextFilterChainsOnListener { listener }`. Extended `validate` in `crates/envoy-config/src/bootstrap.rs` with a single new block at the end of the per-listener loop body (single-loop preferred for cache locality per PLAN line 745) that walks each listener's `filter_chains` and enforces: (1) no two chains may declare the same SNI in `server_names` (lowercased canonical form via `HashSet<String>`); (2) at most one chain may have empty/missing `filter_chain_match` (catch-all); (3) when `≥ 2` chains exist, all must carry `transport_socket: TLS` or none. Appended 6 validator unit tests covering all 3 variants plus the case-insensitive overlap path and the 2 happy-path positives. Created 2 new fuzz-corpus seeds (`tls_multi_cert_sni.yaml`, `tls_overlapping_sni_reject.yaml`); whitelisted both in `crates/envoy-config/fuzz/.gitignore`. Extended the existing `fuzz_corpus_seeds_parse_or_reject_cleanly` test to cover both new seeds (precedent established in 03.1 Task 4 commit `0273f0e`); removed the `#[allow(clippy::single_element_loop)]` attribute since the reject loop is now 2 elements.
- Verification: `cargo test -p envoy-config --lib` reported 61 passed, 0 failed (50 from 03.1 + 5 from Task 1 + 6 from this task). Workspace gate clean: `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check` all exit 0. Fuzz subcrate smoke-built cleanly via `cargo build --manifest-path crates/envoy-config/fuzz/Cargo.toml` (cargo-fuzz toolchain installed locally). Cargo.lock unchanged.
- Deviation from PLAN: (1) **Test API adaptation** — PLAN test bodies call `bootstrap.validate().expect(...)`, but `validate` is a crate-private free function in `bootstrap.rs`, not a method on `Bootstrap`. The 6 new tests instead call `crate::parse_bootstrap(yaml)` (which performs parse + validate together) — this is the convention every existing 03.1 validator test already uses (e.g., `rejects_unknown_transport_socket_name` at `bootstrap.rs:1484`). Faithful translation: `assert!(matches!(err, ...))` matchers are unchanged. No new public API surface introduced. (2) **`.gitignore` whitelist update** — the 2 new seeds were added to `crates/envoy-config/fuzz/.gitignore`'s whitelist (the blanket `corpus/parse_bootstrap/*` ignore would otherwise exclude them); precedent from 03.1 Task 4. Staged in the same Task-2 commit.

## Task 3 — envoy-tls: SniResolver + ResolvesServerCert + 4 unit tests (2026-04-26)

- Commit: 130d256
- Change: Added `SniResolver { map: HashMap<String, Arc<CertifiedKey>>, default: Option<Arc<CertifiedKey>> }` (with a manual `Debug` impl that prints SNI keys + `has_default`) plus the `rustls::server::ResolvesServerCert` impl returning `map.get(sni).cloned().or_else(|| default.clone())` in `crates/envoy-tls/src/lib.rs`, immediately after the existing `SingleCertResolver`. Fields are `pub` so Task 4's `DownstreamTls::from_listener` can build the resolver by direct field assignment. Appended 4 SNI-resolver unit tests to `crates/envoy-tls/src/tests.rs`: `sni_resolver_routes_known_sni`, `sni_resolver_falls_back_to_default_on_miss`, `sni_resolver_returns_none_on_miss_without_default`, `sni_resolver_is_case_insensitive`. Each test drives a real localhost `TlsAcceptor` + `TlsConnector` handshake through the resolver and asserts byte-exact peer-cert DER (tests 1, 2, 4) or server-side handshake failure (test 3).
- Verification: `cargo test -p envoy-tls --lib` reported 14 passed, 0 failed (10 from 03.1 + 4 from this task). Workspace gate clean: `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check` all exit 0. Cargo.lock unchanged.
- Deviation from PLAN: PLAN's test bodies referenced helpers (`test_pki::gen_pki`, `certified_key_from`, `peer_cert_from_handshake`, `try_handshake`, `cert_contains_san`) that do not exist in 03.1's tests.rs (the actual helper module is named `mod pki`, with a single-leaf `Pki::build()`); PLAN line 1032 explicitly invites adaptation. Adaptations: (1) Extended `pki::build()` to also generate leaf-B (SAN `b.example.com`, distinct keypair, same CA) — added `leaf_b_cert_pem` + `leaf_b_key_pem` fields to the `Pki` struct (additive, no break to 03.1 tests). (2) Added `pki::certified_key_from_pem(cert_path, key_path) -> CertifiedKey` mirroring `lib.rs`'s crate-private `load_certified_key` to avoid widening crate visibility for a test-only consumer. (3) Added `pki::cert_der_at(path) -> CertificateDer<'static>` so the SNI-routing assertions are byte-exact peer-cert DER comparisons (no x509 parser, no SAN substring scan). (4) For test 2 (default-on-miss) and test 3 (none-on-miss-no-default) the SNI is `unknown.example.com`, which would normally fail client-side SAN validation before we can inspect what the server returned — so introduced a scoped `mod sni_capture` with a `CapturingVerifier` that implements `rustls::client::danger::ServerCertVerifier` to capture the presented end-entity DER without enforcing SAN (modelled on the in-test `StaticResolver` shape in `mod upstream_pki`). Test 3 then asserts the server-side `acceptor.accept` returns `Err` (rustls aborts with `unrecognized_name` when `resolve()` returns None), proving the contract directly. (5) Tests use the established `#[tokio::test(flavor = "multi_thread")]` async style with inlined bind/spawn/connect (matching 03.1's `single_cert_resolver_returns_same_cert_regardless_of_sni`) rather than introducing a generic `peer_cert_from_handshake` helper — each test body is short enough that inlining is clearer.

## Task 4 — envoy-tls: DownstreamTls::from_listener + 1 integration test (2026-04-26)

- Commit: 64ed7b1
- Change: Added `DownstreamTls::from_listener(&envoy_config::Listener) -> Result<Self, TlsError>` to `crates/envoy-tls/src/lib.rs` (immediately before `accept`). Walks `listener.filter_chains`; for each chain that carries a `transport_socket` with a `DownstreamTlsContext`, loads the chain's first `tls_certificate` via the existing `load_certified_key(cert_path, key_path)` helper and wraps it in `Arc<CertifiedKey>`; for each SNI in `chain.filter_chain_match.server_names` inserts the key into a `HashMap<String, Arc<CertifiedKey>>` (lowercased per SniResolver contract); a chain with empty/missing `server_names` becomes the resolver's `default`. Builds a single `ServerConfig` via `ServerConfig::builder().with_no_client_auth().with_cert_resolver(Arc<SniResolver>)` (mirrors 03.1's `from_context` builder chain exactly). Added `use std::collections::HashMap;` to the file's import cluster. Added `from_listener_builds_multi_cert_config` integration test to `crates/envoy-tls/src/tests.rs` (plus a small `synth_listener_two_tls_chains` helper): synthesizes a Listener with two TLS chains (`a.example.com` → leaf-A; `b.example.com` → leaf-B), drives a per-SNI handshake via an inlined `probe` async closure, and asserts byte-exact peer-cert DER per SNI (proves cert SELECTION, not just successful handshake).
- Verification: `cargo test -p envoy-tls --lib` reported 15 passed, 0 failed (10 from 03.1 + 4 from Task 3 + 1 from this task). Workspace gate clean: `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check` all exit 0. Cargo.lock unchanged.
- Deviation from PLAN: Six PLAN-text drifts encountered, all pre-flagged in the task brief and adapted: (1) **`config` field visibility** — PLAN line 1324 claimed `DownstreamTls.config` is public; it is actually crate-private (`config: Arc<ServerConfig>` at lib.rs:69). The integration test lives in `mod tests` inside the same crate (`use crate::*;` at tests.rs:7), so crate-private fields are reachable directly. No `pub` accessor was added — that would have been unjustified API surface. (2) **`TlsError::ServerConfigBuild` variant** — PLAN line 1392 referenced this for the `with_protocol_versions` build step; the variant does not exist in `TlsError` (verified at lib.rs:39-63). The 03.1 `from_context` does NOT call `with_protocol_versions` — it uses the simpler `ServerConfig::builder().with_no_client_auth().with_cert_resolver(resolver)` chain. `from_listener` mirrors that exactly: no provider re-installation, no `with_protocol_versions`, no new error variant. (3) **`DataSource::as_path()`** — PLAN line 1369 referenced this method; `DataSource` only exposes `filename: String` (verified at envoy-config/src/bootstrap.rs:230). Used `Path::new(&cert.certificate_chain.filename)` and `Path::new(&cert.private_key.filename)` (matches 03.1 `from_context` pattern at lib.rs:86). (4) **`TlsError::EmptyTlsCertificates` variant** — PLAN line 1367 referenced it; does not exist. Used the existing `TlsError::DownstreamRequiresCert` (the variant `from_context` already uses for the same condition); the validator already rejects this case at config-load time, so this is defensive parity only. (5) **PKI helper field/function names** — PLAN test code referenced `test_pki::gen_pki()`, `pki.leaf_a` / `pki.leaf_b` with `.cert.pem()` and `.key_pair.serialize_pem()`, plus `test_pki::peer_cert_from_handshake_via_acceptor` / `test_pki::cert_contains_san`. None exist. Used the actual Task-3-extended `pki::build()` returning `Pki { leaf_cert_pem, leaf_key_pem, leaf_b_cert_pem, leaf_b_key_pem, ca_der_for_root_store, ... }` (PEMs are pre-written to disk inside `pki::build()`); used `pki::cert_der_at(path)` for byte-exact DER assertion (Task 3 helper). (6) **Test attribute** — PLAN line 1253 declared the test as `#[test]`, but the test needs an async runtime for the handshake. Used `#[tokio::test(flavor = "multi_thread")]` per the 03.1 + Task 3 convention. Plus one further deviation that wasn't in the task brief: (7) **Listener construction style** — PLAN's example test parses the Listener via `serde_yaml::from_str`, which would require adding `serde_yaml` as a dev-dep on `envoy-tls` (not currently present; only `envoy-config` depends on it). Constructed the `Listener` directly via the public envoy_config struct constructors instead — avoids the new dev-dep, sidesteps YAML escaping of TempDir paths, and matches the convention established by `pki::ds_context_with`. The wire-shape coverage of YAML parsing is already exercised by envoy-config's parse-shape tests; what `from_listener` is being tested for here is its handling of an in-memory `Listener` value, which is what it actually receives at runtime.

## Task 5 — envoy-tcp: upstream-TLS dial + 3 unit tests + M1 evaluation (2026-04-26)

- Commit: 8a60249
- Change: `TcpProxy` gained `upstream_tls: Option<Arc<envoy_tls::UpstreamTls>>` field plus a new `with_upstream_tls(cluster, &cfg, upstream_tls)` constructor (existing `new()` unchanged). Added a private trait alias `trait AsyncReadWrite: AsyncRead + AsyncWrite {}` (auto-impl'd) and a branched dial body in `handle::<S>`: after `TcpStream::connect`, when `self.upstream_tls.is_some()`, awaits `UpstreamTls::connect` and boxes both arms as `Box<dyn AsyncReadWrite + Send + Unpin>` for a unified bidirectional copy. ADR-0016 half-close posture preserved. Added `TcpProxyError::UpstreamTlsHandshake { source: envoy_tls::TlsError }` variant. Updated the `ConnectionHandler::handle` impl to also clone `self.upstream_tls` (cheap `Option<Arc<...>>` clone) when reconstructing the proxy in the boxed future. 3 new unit tests appended to `mod tests`: `proxies_to_tls_upstream_with_valid_cert`, `proxies_returns_err_on_upstream_tls_handshake_fail` (asserts both Display content and `TcpProxyError::UpstreamTlsHandshake` downcast), `proxies_to_tls_upstream_sends_sni_in_client_hello` (custom `ResolvesServerCert` captures `client_hello.server_name()` into a `Mutex` and asserts equality with `"envoy-rust.test"`).
- M1 carryforward decision: **DEFERRED — no use case surfaced; carries forward to phase 06.** Rationale: `UpstreamTlsHandshake { source }` is consistent with the existing `UpstreamConnect { addr, source }` and `CopyFailed { source }` shapes (bare `source`-only is the established cadence); the `tracing::debug!` log at the warn site already names the cluster via `self.cluster_name` so error-variant attribution would duplicate that. Phase 06 (stats) is the next phase that materially benefits from per-cluster error attribution (a `cluster.tls_handshake_failures` counter), so the M1 close-out lands more naturally there. No code changes for the accessor in this task. Cross-references: phase-02.1 REVIEW M1 (REVIEW.md:92, the original deferral); phase-02.2 REVIEW §4 recommendation 1 (REVIEW.md:119, retargeted to 03.x); phase-03.1 REVIEW §4 recommendation 2 (REVIEW.md:135, retargeted to 03.2 D4); phase-03.2 SPEC §D4 (SPEC.md:224, the opportunistic-evaluation framing).
- Verification: `cargo test -p envoy-tcp --lib` reported **11 passed, 0 failed** (8 pre-existing 02.2 + 03.1 tests + 3 new). Workspace gate clean: `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check` all exit 0. `Cargo.lock` updated by cargo (added `envoy-tls` + `tempfile` to `envoy-tcp`'s lock entry) but not staged per task brief.
- Deviation from PLAN: (1) **`envoy-tls` added rather than promoted** — PLAN line 1491 expected `envoy-tls` to be a pre-existing dev-dep needing promotion to `[dependencies]`; in fact 03.1 left it absent from envoy-tcp's manifest entirely (only the dev-deps `rcgen`, `rustls`, `rustls-pemfile`, `rustls-pki-types`, `tokio-rustls` were added). Added the new line `envoy-tls = { path = "../envoy-tls" }` to `[dependencies]` (alphabetical, between `envoy-listener` and `thiserror`). (2) **`tokio::io::split(upstream)` replaces `upstream.into_split()`** — the previous `into_split()` is `TcpStream`-specific and would not compile against the boxed `dyn AsyncReadWrite + Send + Unpin` upstream. Switched to the generic `tokio::io::split` (already documented in the existing 03.1 in-line comment for the downstream side; same generic helper accepts `T: AsyncRead + AsyncWrite + Unpin`). (3) **`DataSource` is a struct, not an enum** — PLAN's example test (line 1529) wrote `envoy_config::DataSource::Filename(...)`, but `DataSource` is `pub struct DataSource { pub filename: String }` (envoy-config/src/bootstrap.rs:230). Used the struct-literal form `envoy_config::DataSource { filename: pki.ca_pem_path.to_string_lossy().into_owned() }` in `upstream_ctx_for(...)`. (4) **No shared PKI helper module** — PLAN test bodies called `build_test_pki()`, `spawn_in_process_tls_echo_server(pki)`, `spawn_sni_capturing_acceptor(pki, mutex)`, `build_loopback_pair_and_drive_payload(proxy, payload)`, `drive_and_capture_error(proxy, payload)`, and `mk_handle_for_addr(addr)`. None of these helpers exist in 03.1's envoy-tcp tests. Mirrored the established 03.1 inline-PKI cadence (visible in `proxies_payload_through_tls_downstream_stream` at lib.rs:340+ and `tls_downstream_proxy_*` tests at lib.rs:467+): added a small `UpstreamPki` struct + `build_upstream_pki(leaf_san)` + `acceptor_from_pki(&pki)` + `upstream_ctx_for(&pki, sni)` test-only helpers and inlined the in-process echo backend per test. The existing `mk_handle("backend", addr)` already does the single-endpoint cluster-handle construction the PLAN's hypothetical `mk_handle_for_addr` would have done. (5) **`tempfile` added as a new dev-dep on envoy-tcp** — required for the on-disk CA PEM the `UpstreamTlsContext.validation_context.trusted_ca.filename` resolves to. envoy-tls already uses `tempfile = "3"` as a dev-dep; mirrored the same pin. Staged in the same Task-5 commit alongside the runtime `envoy-tls` addition.
