# Phase 03.2 Progress

## Task 1 — envoy-config: FilterChainMatch struct + server_names + 5 parse-shape tests (2026-04-26)

- Commit: 1e1ea64
- Change: Inserted `FilterChainMatch` struct (single `server_names: Vec<String>` field, `#[serde(deny_unknown_fields)]`, `#[derive(Debug, Deserialize, PartialEq)]`) in `crates/envoy-config/src/bootstrap.rs`. Added `filter_chain_match: Option<FilterChainMatch>` as the first field on `FilterChain` (mirrors Envoy's bootstrap proto field ordering) and extended its derive set with `PartialEq` (Task 2's validator tests will rely on it). Re-exported `FilterChainMatch` from `crates/envoy-config/src/lib.rs`. Appended 5 parse-shape tests covering present / missing / empty-map / empty-list / unknown-field cases.
- Verification: `cargo test -p envoy-config --lib` reported 55 passed (50 pre-existing from 03.1 + 5 new). Workspace gate clean: `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check` all exit 0. Cargo.lock unchanged.
- Deviation from PLAN: none.

## Task 2 — envoy-config: 3 validator variants + 6 validator tests + 2 fuzz seeds (2026-04-26)

- Commit: 17d43cd
- Change: Added 3 cross-chain `ConfigError` variants to `crates/envoy-config/src/lib.rs` — `MultipleListenersWithOverlappingSni { listener, sni }` (variant name preserved verbatim from parent SPEC §7's projection even though the rule is intra-listener; doc-comment names the intra-listener semantics), `MultipleCatchAllFilterChains { listener }`, `MixedTlsAndPlaintextFilterChainsOnListener { listener }`. Extended `validate` in `crates/envoy-config/src/bootstrap.rs` with a single new block at the end of the per-listener loop body (single-loop preferred for cache locality per PLAN line 745) that walks each listener's `filter_chains` and enforces: (1) no two chains may declare the same SNI in `server_names` (lowercased canonical form via `HashSet<String>`); (2) at most one chain may have empty/missing `filter_chain_match` (catch-all); (3) when `≥ 2` chains exist, all must carry `transport_socket: TLS` or none. Appended 6 validator unit tests covering all 3 variants plus the case-insensitive overlap path and the 2 happy-path positives. Created 2 new fuzz-corpus seeds (`tls_multi_cert_sni.yaml`, `tls_overlapping_sni_reject.yaml`); whitelisted both in `crates/envoy-config/fuzz/.gitignore`. Extended the existing `fuzz_corpus_seeds_parse_or_reject_cleanly` test to cover both new seeds (precedent established in 03.1 Task 4 commit `0273f0e`); removed the `#[allow(clippy::single_element_loop)]` attribute since the reject loop is now 2 elements.
- Verification: `cargo test -p envoy-config --lib` reported 61 passed, 0 failed (50 from 03.1 + 5 from Task 1 + 6 from this task). Workspace gate clean: `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check` all exit 0. Fuzz subcrate smoke-built cleanly via `cargo build --manifest-path crates/envoy-config/fuzz/Cargo.toml` (cargo-fuzz toolchain installed locally). Cargo.lock unchanged.
- Deviation from PLAN: (1) **Test API adaptation** — PLAN test bodies call `bootstrap.validate().expect(...)`, but `validate` is a crate-private free function in `bootstrap.rs`, not a method on `Bootstrap`. The 6 new tests instead call `crate::parse_bootstrap(yaml)` (which performs parse + validate together) — this is the convention every existing 03.1 validator test already uses (e.g., `rejects_unknown_transport_socket_name` at `bootstrap.rs:1484`). Faithful translation: `assert!(matches!(err, ...))` matchers are unchanged. No new public API surface introduced. (2) **`.gitignore` whitelist update** — the 2 new seeds were added to `crates/envoy-config/fuzz/.gitignore`'s whitelist (the blanket `corpus/parse_bootstrap/*` ignore would otherwise exclude them); precedent from 03.1 Task 4. Staged in the same Task-2 commit.

## Task 3 — envoy-tls: SniResolver + ResolvesServerCert + 4 unit tests (2026-04-26)

- Commit: 130d256
- Change: Added `SniResolver { map: HashMap<String, Arc<CertifiedKey>>, default: Option<Arc<CertifiedKey>> }` (with a manual `Debug` impl that prints SNI keys + `has_default`) plus the `rustls::server::ResolvesServerCert` impl returning `map.get(sni).cloned().or_else(|| default.clone())` in `crates/envoy-tls/src/lib.rs`, immediately after the existing `SingleCertResolver`. Fields are `pub` so Task 4's `DownstreamTls::from_listener` can build the resolver by direct field assignment. Appended 4 SNI-resolver unit tests to `crates/envoy-tls/src/tests.rs`: `sni_resolver_routes_known_sni`, `sni_resolver_falls_back_to_default_on_miss`, `sni_resolver_returns_none_on_miss_without_default`, `sni_resolver_is_case_insensitive`. Each test drives a real localhost `TlsAcceptor` + `TlsConnector` handshake through the resolver and asserts byte-exact peer-cert DER (tests 1, 2, 4) or server-side handshake failure (test 3).
- Verification: `cargo test -p envoy-tls --lib` reported 14 passed, 0 failed (10 from 03.1 + 4 from this task). Workspace gate clean: `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check` all exit 0. Cargo.lock unchanged.
- Deviation from PLAN: PLAN's test bodies referenced helpers (`test_pki::gen_pki`, `certified_key_from`, `peer_cert_from_handshake`, `try_handshake`, `cert_contains_san`) that do not exist in 03.1's tests.rs (the actual helper module is named `mod pki`, with a single-leaf `Pki::build()`); PLAN line 1032 explicitly invites adaptation. Adaptations: (1) Extended `pki::build()` to also generate leaf-B (SAN `b.example.com`, distinct keypair, same CA) — added `leaf_b_cert_pem` + `leaf_b_key_pem` fields to the `Pki` struct (additive, no break to 03.1 tests). (2) Added `pki::certified_key_from_pem(cert_path, key_path) -> CertifiedKey` mirroring `lib.rs`'s crate-private `load_certified_key` to avoid widening crate visibility for a test-only consumer. (3) Added `pki::cert_der_at(path) -> CertificateDer<'static>` so the SNI-routing assertions are byte-exact peer-cert DER comparisons (no x509 parser, no SAN substring scan). (4) For test 2 (default-on-miss) and test 3 (none-on-miss-no-default) the SNI is `unknown.example.com`, which would normally fail client-side SAN validation before we can inspect what the server returned — so introduced a scoped `mod sni_capture` with a `CapturingVerifier` that implements `rustls::client::danger::ServerCertVerifier` to capture the presented end-entity DER without enforcing SAN (modelled on the in-test `StaticResolver` shape in `mod upstream_pki`). Test 3 then asserts the server-side `acceptor.accept` returns `Err` (rustls aborts with `unrecognized_name` when `resolve()` returns None), proving the contract directly. (5) Tests use the established `#[tokio::test(flavor = "multi_thread")]` async style with inlined bind/spawn/connect (matching 03.1's `single_cert_resolver_returns_same_cert_regardless_of_sni`) rather than introducing a generic `peer_cert_from_handshake` helper — each test body is short enough that inlining is clearer.

## Task 4 — envoy-tls: DownstreamTls::from_listener + 1 integration test (2026-04-26)

- Commit: 64ed7b1
- Change: Added `DownstreamTls::from_listener(&envoy_config::Listener) -> Result<Self, TlsError>` to `crates/envoy-tls/src/lib.rs` (immediately before `accept`). Walks `listener.filter_chains`; for each chain that carries a `transport_socket` with a `DownstreamTlsContext`, loads the chain's first `tls_certificate` via the existing `load_certified_key(cert_path, key_path)` helper and wraps it in `Arc<CertifiedKey>`; for each SNI in `chain.filter_chain_match.server_names` inserts the key into a `HashMap<String, Arc<CertifiedKey>>` (lowercased per SniResolver contract); a chain with empty/missing `server_names` becomes the resolver's `default`. Builds a single `ServerConfig` via `ServerConfig::builder().with_no_client_auth().with_cert_resolver(Arc<SniResolver>)` (mirrors 03.1's `from_context` builder chain exactly). Added `use std::collections::HashMap;` to the file's import cluster. Added `from_listener_builds_multi_cert_config` integration test to `crates/envoy-tls/src/tests.rs` (plus a small `synth_listener_two_tls_chains` helper): synthesizes a Listener with two TLS chains (`a.example.com` → leaf-A; `b.example.com` → leaf-B), drives a per-SNI handshake via an inlined `probe` async closure, and asserts byte-exact peer-cert DER per SNI (proves cert SELECTION, not just successful handshake).
- Verification: `cargo test -p envoy-tls --lib` reported 15 passed, 0 failed (10 from 03.1 + 4 from Task 3 + 1 from this task). Workspace gate clean: `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check` all exit 0. Cargo.lock unchanged.
- Deviation from PLAN: Six PLAN-text drifts encountered, all pre-flagged in the task brief and adapted: (1) **`config` field visibility** — PLAN line 1324 claimed `DownstreamTls.config` is public; it is actually crate-private (`config: Arc<ServerConfig>` at lib.rs:69). The integration test lives in `mod tests` inside the same crate (`use crate::*;` at tests.rs:7), so crate-private fields are reachable directly. No `pub` accessor was added — that would have been unjustified API surface. (2) **`TlsError::ServerConfigBuild` variant** — PLAN line 1392 referenced this for the `with_protocol_versions` build step; the variant does not exist in `TlsError` (verified at lib.rs:39-63). The 03.1 `from_context` does NOT call `with_protocol_versions` — it uses the simpler `ServerConfig::builder().with_no_client_auth().with_cert_resolver(resolver)` chain. `from_listener` mirrors that exactly: no provider re-installation, no `with_protocol_versions`, no new error variant. (3) **`DataSource::as_path()`** — PLAN line 1369 referenced this method; `DataSource` only exposes `filename: String` (verified at envoy-config/src/bootstrap.rs:230). Used `Path::new(&cert.certificate_chain.filename)` and `Path::new(&cert.private_key.filename)` (matches 03.1 `from_context` pattern at lib.rs:86). (4) **`TlsError::EmptyTlsCertificates` variant** — PLAN line 1367 referenced it; does not exist. Used the existing `TlsError::DownstreamRequiresCert` (the variant `from_context` already uses for the same condition); the validator already rejects this case at config-load time, so this is defensive parity only. (5) **PKI helper field/function names** — PLAN test code referenced `test_pki::gen_pki()`, `pki.leaf_a` / `pki.leaf_b` with `.cert.pem()` and `.key_pair.serialize_pem()`, plus `test_pki::peer_cert_from_handshake_via_acceptor` / `test_pki::cert_contains_san`. None exist. Used the actual Task-3-extended `pki::build()` returning `Pki { leaf_cert_pem, leaf_key_pem, leaf_b_cert_pem, leaf_b_key_pem, ca_der_for_root_store, ... }` (PEMs are pre-written to disk inside `pki::build()`); used `pki::cert_der_at(path)` for byte-exact DER assertion (Task 3 helper). (6) **Test attribute** — PLAN line 1253 declared the test as `#[test]`, but the test needs an async runtime for the handshake. Used `#[tokio::test(flavor = "multi_thread")]` per the 03.1 + Task 3 convention. Plus one further deviation that wasn't in the task brief: (7) **Listener construction style** — PLAN's example test parses the Listener via `serde_yaml::from_str`, which would require adding `serde_yaml` as a dev-dep on `envoy-tls` (not currently present; only `envoy-config` depends on it). Constructed the `Listener` directly via the public envoy_config struct constructors instead — avoids the new dev-dep, sidesteps YAML escaping of TempDir paths, and matches the convention established by `pki::ds_context_with`. The wire-shape coverage of YAML parsing is already exercised by envoy-config's parse-shape tests; what `from_listener` is being tested for here is its handling of an in-memory `Listener` value, which is what it actually receives at runtime.

## Task 5 — envoy-tcp: upstream-TLS dial + 3 unit tests + M1 evaluation (2026-04-26)

- Commit: 8a60249
- Change: `TcpProxy` gained `upstream_tls: Option<Arc<envoy_tls::UpstreamTls>>` field plus a new `with_upstream_tls(cluster, &cfg, upstream_tls)` constructor (existing `new()` unchanged). Added a private trait alias `trait AsyncReadWrite: AsyncRead + AsyncWrite {}` (auto-impl'd) and a branched dial body in `handle::<S>`: after `TcpStream::connect`, when `self.upstream_tls.is_some()`, awaits `UpstreamTls::connect` and boxes both arms as `Box<dyn AsyncReadWrite + Send + Unpin>` for a unified bidirectional copy. ADR-0016 half-close posture preserved. Added `TcpProxyError::UpstreamTlsHandshake { source: envoy_tls::TlsError }` variant. Updated the `ConnectionHandler::handle` impl to also clone `self.upstream_tls` (cheap `Option<Arc<...>>` clone) when reconstructing the proxy in the boxed future. 3 new unit tests appended to `mod tests`: `proxies_to_tls_upstream_with_valid_cert`, `proxies_returns_err_on_upstream_tls_handshake_fail` (asserts both Display content and `TcpProxyError::UpstreamTlsHandshake` downcast), `proxies_to_tls_upstream_sends_sni_in_client_hello` (custom `ResolvesServerCert` captures `client_hello.server_name()` into a `Mutex` and asserts equality with `"envoy-rust.test"`).
- M1 carryforward decision: **DEFERRED — no use case surfaced; carries forward to phase 06.** Rationale: `UpstreamTlsHandshake { source }` is consistent with the existing `UpstreamConnect { addr, source }` and `CopyFailed { source }` shapes (bare `source`-only is the established cadence); the `tracing::debug!` log at the warn site already names the cluster via `self.cluster_name` so error-variant attribution would duplicate that. Phase 06 (stats) is the next phase that materially benefits from per-cluster error attribution (a `cluster.tls_handshake_failures` counter), so the M1 close-out lands more naturally there. No code changes for the accessor in this task. Cross-references: phase-02.1 REVIEW M1 (REVIEW.md:92, the original deferral); phase-02.2 REVIEW §4 recommendation 1 (REVIEW.md:119, retargeted to 03.x); phase-03.1 REVIEW §4 recommendation 2 (REVIEW.md:135, retargeted to 03.2 D4); phase-03.2 SPEC §D4 (SPEC.md:224, the opportunistic-evaluation framing).
- Verification: `cargo test -p envoy-tcp --lib` reported **11 passed, 0 failed** (8 pre-existing 02.2 + 03.1 tests + 3 new). Workspace gate clean: `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check` all exit 0. `Cargo.lock` updated by cargo (added `envoy-tls` + `tempfile` to `envoy-tcp`'s lock entry) but not staged per task brief.
- Deviation from PLAN: (1) **`envoy-tls` added rather than promoted** — PLAN line 1491 expected `envoy-tls` to be a pre-existing dev-dep needing promotion to `[dependencies]`; in fact 03.1 left it absent from envoy-tcp's manifest entirely (only the dev-deps `rcgen`, `rustls`, `rustls-pemfile`, `rustls-pki-types`, `tokio-rustls` were added). Added the new line `envoy-tls = { path = "../envoy-tls" }` to `[dependencies]` (alphabetical, between `envoy-listener` and `thiserror`). (2) **`tokio::io::split(upstream)` replaces `upstream.into_split()`** — the previous `into_split()` is `TcpStream`-specific and would not compile against the boxed `dyn AsyncReadWrite + Send + Unpin` upstream. Switched to the generic `tokio::io::split` (already documented in the existing 03.1 in-line comment for the downstream side; same generic helper accepts `T: AsyncRead + AsyncWrite + Unpin`). (3) **`DataSource` is a struct, not an enum** — PLAN's example test (line 1529) wrote `envoy_config::DataSource::Filename(...)`, but `DataSource` is `pub struct DataSource { pub filename: String }` (envoy-config/src/bootstrap.rs:230). Used the struct-literal form `envoy_config::DataSource { filename: pki.ca_pem_path.to_string_lossy().into_owned() }` in `upstream_ctx_for(...)`. (4) **No shared PKI helper module** — PLAN test bodies called `build_test_pki()`, `spawn_in_process_tls_echo_server(pki)`, `spawn_sni_capturing_acceptor(pki, mutex)`, `build_loopback_pair_and_drive_payload(proxy, payload)`, `drive_and_capture_error(proxy, payload)`, and `mk_handle_for_addr(addr)`. None of these helpers exist in 03.1's envoy-tcp tests. Mirrored the established 03.1 inline-PKI cadence (visible in `proxies_payload_through_tls_downstream_stream` at lib.rs:340+ and `tls_downstream_proxy_*` tests at lib.rs:467+): added a small `UpstreamPki` struct + `build_upstream_pki(leaf_san)` + `acceptor_from_pki(&pki)` + `upstream_ctx_for(&pki, sni)` test-only helpers and inlined the in-process echo backend per test. The existing `mk_handle("backend", addr)` already does the single-endpoint cluster-handle construction the PLAN's hypothetical `mk_handle_for_addr` would have done. (5) **`tempfile` added as a new dev-dep on envoy-tcp** — required for the on-disk CA PEM the `UpstreamTlsContext.validation_context.trusted_ca.filename` resolves to. envoy-tls already uses `tempfile = "3"` as a dev-dep; mirrored the same pin. Staged in the same Task-5 commit alongside the runtime `envoy-tls` addition.

## Task 6 — envoy-bin: multi-cert + upstream-TLS wiring (2026-04-26)

- Commit: fadb9b1
- Change: Inserted a per-cluster `Arc<UpstreamTls>` construction loop in `crates/envoy-bin/src/main.rs::run` between the `cluster_mgr` build and the listener dispatch block; the loop walks `bootstrap.static_resources.clusters`, builds `UpstreamTls::from_context(&ctx)` once for each cluster carrying `transport_socket: UpstreamTlsContext`, and stores `Arc<UpstreamTls>` in a `HashMap<String, _>` keyed by cluster name. The `Downstream(_)` match arm on a cluster's transport_socket bails defensively (validator already rejected this via `MismatchedTransportSocketDirection { side: "cluster" }`). In the `TCP_PROXY_FILTER` arm, replaced the unconditional `TcpProxy::new(cluster, tp_cfg)` with a match on `upstream_tls_by_cluster.get(&tp_cfg.cluster)` selecting `with_upstream_tls(...)` when present and `new(...)` otherwise. Replaced the single-chain `from_context`-only pre-pass with the SPEC §D5 step 1 three-way dispatch: plaintext (no chain has `transport_socket`) → bare proxy with no `DownstreamTls`; single-chain + `transport_socket` + no `server_names` → `from_context` (03.1 path, fixtures 0001-0004); else (≥ 2 chains OR any chain has `server_names`) → `from_listener(listener_cfg)` (03.2 path, fixture 0006). Wrapping into `TlsAcceptingHandler { tls, inner: proxy }` is preserved; the construction order inside the arm is now: extract tp_cfg → resolve `cluster` from cluster_mgr → build `proxy` (Step C) → compute `downstream_tls` (Step B) → wrap if `Some`.
- Verification: `cargo test --workspace --lib --bins` reported all crates green (envoy-bin lib 37 passed / 1 ignored, envoy-tls 15, envoy-tcp 11, envoy-config 61, envoy-listener 6, envoy-cluster 19, envoy-tls-server-test 8, tcp-echo-server bin 8); the 03.1 in-process integration test `cargo test -p envoy-bin --test tls_downstream` (which exercises the single-chain `from_context` path) still passes 1/1 — single-cert path preserved, no regressions. Workspace gate clean: `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check` all exit 0.
- Deviation from PLAN: (1) **Single-listener structure preserved** — PLAN's pseudocode at lines 1903-2000 shows an outer `for listener in &bootstrap.static_resources.listeners` loop, but the existing 02.1+ envoy-bin posture is single-listener (validator caps `listeners.len() ≤ 1` via `TooManyListeners` and the run body uses `if let Some(listener_cfg) = bootstrap.static_resources.listeners.first()`). Kept that boundary verbatim; applied the multi-cert and upstream-TLS dispatch INSIDE the existing `if let Some(...)` block. (2) **First-chain-first-filter pattern preserved** — the existing per-listener filter extraction at the top of the listener block reads `filter_chains.first().and_then(|c| c.filters.first())`; this is correct for the 03.2 fixtures because both chains in fixture 0006 route to the same `tcp_proxy` filter targeting the same backend cluster (per PLAN line 1942), so the dispatch arm only needs to be entered once. The new TLS pre-pass walks all chains for SNI/transport_socket detection, but the proxy itself remains a single shared `Arc<TcpProxy>` wrapped (or not) by `TlsAcceptingHandler` — this is structurally equivalent to PLAN's per-chain filter walk for the single-filter-per-chain phase 03.2 envelope. Phase 07's filter chain framework will iterate. Cargo.lock unchanged-by-task; the lockfile churn observed locally was incidental and was NOT staged per task brief.

## Task 7 — envoy-bin: in-process tls_upstream + tls_sni integration tests (2026-04-26)

- Commit: 122de5b
- Change: Added `crates/envoy-bin/tests/tls_upstream.rs` (~190 LoC) and `crates/envoy-bin/tests/tls_sni.rs` (~270 LoC). Each spawns `envoy-bin` as a subprocess via `env!("CARGO_BIN_EXE_envoy-bin")` with `kill_on_drop(true)` and drives an end-to-end round-trip. `tls_upstream`: builds an rcgen CA + leaf with SAN `envoy-rust.test`, spawns an in-process `tokio_rustls::TlsAcceptor` echo server with the leaf, writes a temp YAML config wiring envoy-bin's plaintext listener to the TLS upstream cluster (`UpstreamTlsContext` with `sni: "envoy-rust.test"` and `validation_context.trusted_ca.filename` pointing at the on-disk CA PEM), drives a plaintext payload round-trip through envoy-bin, asserts byte-exact. `tls_sni`: builds an rcgen CA + two leaves (`a.example.com`, `b.example.com`) sharing the CA, spawns an in-process plaintext TCP echo backend, writes a YAML with two filter chains (each `filter_chain_match.server_names` constrained to one SNI, each carrying its own `DownstreamTlsContext.tls_certificates`), then runs two TLS handshakes (one per SNI) through a shared `tokio_rustls::TlsConnector` configured with the test CA in its `RootCertStore`, asserts each post-handshake `peer_certificates()[0]` matches the expected leaf via byte-exact DER comparison, and round-trips a per-probe payload. Both tests use `tempfile::tempdir()` for the PKI directory and `tempfile::NamedTempFile` for the YAML config (mirrors the 03.1 `tls_downstream.rs` cadence). Reuse the inline `reserve_port()` + exponential-backoff `wait_ready()` helpers from `tls_downstream.rs`; per PLAN line 2278 the optional `tests/common/mod.rs` extraction is deferred.
- Verification: `cargo test -p envoy-bin --test tls_upstream` reported `1 passed; 0 failed` in 0.84s. `cargo test -p envoy-bin --test tls_sni` reported `1 passed; 0 failed` in 0.43s. `cargo test --workspace --lib --bins` reported all crates green (envoy-tcp 11, envoy-tls 15, envoy-config 61, envoy-listener 6, envoy-cluster 19, envoy-bin lib 37/1 ignored, envoy-tls-server-test 8, tcp-echo-server bin 8). Workspace gate clean: `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check` all exit 0. Cargo.lock not staged.
- Deviation from PLAN: (1) **Direct rcgen → rustls DER conversion** for the in-process TLS echo upstream — PLAN's `spawn_tls_echo_server` (lines 2230-2235) round-trips through `rustls_pemfile::pkcs8_private_keys` and `rustls_pemfile::certs` to parse the on-disk PEMs back into rustls types, which would require adding `rustls-pemfile` as a new dev-dep on envoy-bin. Used `leaf_cert.der().clone().into_owned()` and `leaf_kp.serialize_der()` directly into `rustls::ServerConfig::builder().with_single_cert(vec![cert_der], PrivateKeyDer::Pkcs8(...))`. The on-disk CA PEM is still written (envoy-bin's `UpstreamTlsContext.validation_context.trusted_ca.filename` requires a path), but the in-process upstream-server uses DER directly. No `rustls-pemfile` dev-dep added. (2) **`-c <path>` for the config flag** rather than PLAN's `--config-path <path>` — both forms are accepted by envoy-bin's `parse_argv` (argv.rs:40), but the existing 03.1 `tls_downstream.rs` (line 147) uses `-c`; mirrored that for consistency. (3) **Byte-exact peer-cert DER comparison** for the SNI per-probe assertion — PLAN's `cert_contains_san(&cert, "a.example.com")` would require either a SAN-substring scan over raw DER or an x509 parser dep. Tasks 3 + 4 already established byte-exact DER comparison as the rigorous form (`assert_eq!(presented.as_ref(), expected_leaf_der.as_ref())` using DERs captured from rcgen at PKI-build time). Mirrored that form here — strictly more rigorous than substring-match and avoids both an x509 parser dep and any false-positive risk from substring overlap. (4) **`stderr(Stdio::inherit())`** rather than PLAN's `stderr(Stdio::piped())` — mirrors `tls_downstream.rs` (line 150); envoy-bin's startup tracing surfaces in the test output if `wait_ready` ever times out, no extra capture plumbing needed. (5) **`child.start_kill()` + bounded `tokio::time::timeout(_, child.wait())` for teardown** — mirrors the 03.1 `tls_downstream.rs` cadence (lines 182-183) rather than PLAN's `child.kill().await` + unbounded `child.wait().await`; `kill_on_drop(true)` is the safety net. Cargo.lock observed local churn (envoy-tcp's Task-5 deps) but NOT staged per task brief.

## Task 8 — differential: TlsTcpProbeList + drive_tls_probes + render_yaml extensions (2026-04-26)

- Commit: cef8a53
- Change: Added `TlsTcpProbe { sni: String, expected_cn: Option<String> }` struct (with `serde(deny_unknown_fields)` + `#[serde(default)]` on `expected_cn`) and `Driver::TlsTcpProbeList { probes: Vec<TlsTcpProbe> }` enum variant in `tests/differential/src/lib.rs`. Added `drive_tls_probes(addr, payload, probes, root_store) -> Result<Vec<Vec<u8>>>` helper mirroring `drive_tls`'s ADR-0006/0007 discipline once per probe — fresh TCP connect + TLS handshake per SNI, optional `expected_cn` match via the existing 03.1 `check_cn_or_san` (DER-substring scan), `read_exact(payload.len())`, 100ms trailing-byte poll, graceful `tls.shutdown()`. Extended `port_key` match in `run_fixture` to fold `TlsTcpProbeList` into the `"PORT"` arm alongside `TcpEcho` + `TlsTcp`. Added `needs_tls_backend` detection (`{{TLS_BACKEND_PORT}}` substring scan over both templates) that bails clearly with `"TlsEchoBackend not yet wired up — pending Task 9"`. Added the `Driver::TlsTcpProbeList { probes }` dispatch arm after the existing `Driver::TlsTcp` arm, using the same CA-loading pattern as the `TlsTcp` arm (`pki.ca_pem_path` → `rustls_pemfile::certs` → `RootCertStore`); calls `drive_tls_probes` once per side and drops upstream/subject without a final `assert_equivalence` call (byte-equality is enforced inside the helper per probe; per-probe `expected_cn` matches enforce cert selection on each side independently — both sides succeeding ⇒ equivalent cert selection per SNI). Appended 4 unit tests: 2 for the new `Driver::TlsTcpProbeList` parse shape (with + without `expected_cn`; RED-then-GREEN per TDD discipline for the new variant), 2 for `render_yaml` substituting `LEAF_B_*` (fixture 0006 second filter chain) and `SERVER_*` (fixture 0005 tls-echo-server) keys.
- Verification: `cargo test -p differential --lib` reported **41 passed, 0 failed, 1 ignored** (37 from 03.1 + 4 new — 2 render_yaml + 2 parse-shape; the PLAN-predicted 39 underestimated by 2 because the new `Driver::TlsTcpProbeList` variant required RED-first parse-shape tests per TDD doctrine D-3.1, not just the 2 render_yaml tests). Workspace gate clean: `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check` all exit 0. `Cargo.lock` not staged.
- Deviation from PLAN: (1) **`tls.rs` NOT modified** — PLAN Step 3 directs touching `tests/differential/src/tls.rs` to add LEAF_B_* + SERVER_* fields and extend the path-map methods; the 03.1 implementation already pre-staged all 7 PEM fields on `TlsTestPki` (`ca_pem_path`, `leaf_a_cert`, `leaf_a_key`, `leaf_b_cert`, `leaf_b_key`, `server_cert`, `server_key`) and populated `envoy_side_paths()` + `subject_side_paths()` + `container_mounts()` entries for `LEAF_B_CERT_PATH`/`LEAF_B_KEY_PATH`/`SERVER_CERT_PATH`/`SERVER_KEY_PATH` per the "harmless to expose now" comment at `tls.rs:91`. Skipped PLAN Step 3 entirely; the task commit touches only `tests/differential/src/lib.rs`. (2) **No final `assert_equivalence` call in the `Driver::TlsTcpProbeList` dispatch arm** — PLAN's example body adapted from the `TlsTcp` arm hints at one, but byte-equality is enforced *inside* `drive_tls_probes` per probe (each probe writes `payload` and reads-exact `payload.len()` bytes; the read fails fast on mismatch) and per-probe `expected_cn` matches enforce cert selection on each side independently. The "both proxies select the same cert for the same SNI" property is the conjunction of the two per-side `expected_cn` matches succeeding — implicit. Documented in the variant rustdoc + the dispatch-arm comment. (3) **2 extra parse-shape tests beyond PLAN's 2 render_yaml tests** — TDD doctrine D-3.1 requires RED-then-GREEN for any new variant; the 2 parse tests (`expectations_parse_tls_tcp_probe_list_driver` + `expectations_parse_tls_tcp_probe_list_without_expected_cn`) were the RED step for `Driver::TlsTcpProbeList`. Final test count is 41 (37 + 2 render_yaml + 2 parse) rather than PLAN's predicted 39. (4) **Informational: `Driver::HttpGet` has TWO fields (`path`, `host`)** — PLAN's outdated reference at line 2614 omitted `host`; HttpGet was not touched in this task (only the new variant added), so this is documentation drift, not a code deviation.

## Task 9 — differential: TlsEchoBackend + 2 unit tests + run_fixture wiring (2026-04-26)

- Commit: aade793
- Change: Added `TlsEchoBackend { port, child: Option<Child>, _server_cert, _server_key }` struct + `spawn(server_cert, server_key) -> Result<Self>` (5s readiness budget vs TcpProxyBackend's 1s — TLS server has more startup work) + `port()` + `container_host()` accessors + `Drop` impl (mirrors TcpProxyBackend: `start_kill()` + 50ms-poll/2s-deadline fallback) + `locate_tls_echo_server() -> Result<PathBuf>` helper (mirrors `locate_tcp_echo_server` shape: walks two parents up from `CARGO_MANIFEST_DIR`, honors `CARGO_TARGET_DIR`, picks `debug`/`release` per `cfg!(debug_assertions)`, adds `.exe` on Windows) in `tests/differential/src/backend.rs`. Wired `run_fixture` in `tests/differential/src/lib.rs`: replaced Task 8's `bail!("TlsEchoBackend not yet wired up — pending Task 9")` placeholder with a real `TlsEchoBackend::spawn(&pki.server_cert, &pki.server_key)` call gated on `needs_tls_backend = upstream_template.contains("{{TLS_BACKEND_PORT}}") || subject_template.contains("{{TLS_BACKEND_PORT}}")`; the spawn requires `tls_pki.is_some()` (errors with `"TLS backend implies TLS pki shape"` otherwise). Added `TLS_BACKEND_PORT` substitution to BOTH `upstream_kvs` and `subject_kvs` building blocks. Generalized the `BACKEND_HOST` push so it fires when EITHER `backend_port_str.is_some() || tls_backend_port_str.is_some()` (was previously gated only on `BACKEND_PORT`); fixture 0005 (TLS-only, no plaintext `BACKEND_PORT`) now gets the host substitution it needs. Appended 2 unit tests: `tls_echo_backend_spawns_and_echoes` (TLS handshake via `tokio_rustls::TlsConnector` + payload round-trip + byte-equality) and `tls_echo_backend_drop_terminates_child` (port-released-after-Drop assertion via `std::net::TcpStream::connect` after a 200ms grace). Both early-return with stderr skip-message when `locate_tls_echo_server` returns `Err` — phase-02.2 TcpProxyBackend cadence.
- Verification: `cargo test -p differential --lib` reported **43 passed, 0 failed, 1 ignored** (41 from Task 8 + 2 new — both skip-pass before Task 10 builds the binary; the new variants register as `ok` rather than `ignored` because they call `eprintln!(...); return;` rather than `#[ignore]`-marked). Workspace gate clean: `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check` all exit 0. `Cargo.lock` not staged (incidental envoy-tcp dep churn from Task 5 still local).
- Deviation from PLAN: (1) **`upstream.rs` NOT touched** — PLAN Step 8's `git add` line includes `tests/differential/src/upstream.rs`, but verification (`upstream.rs:76`) confirms the mount-fan-out already walks `pki.container_mounts()` in an iterator-driven `for (host_path, container_path)` loop landed in 03.1 and extended in Task 8 (TlsTestPki's `container_mounts()` already returns all 7 PEM entries — CA + leaf-A + leaf-B + server). The Task 9 commit therefore stages only `backend.rs` + `lib.rs`. The PLAN's commit body was adapted to mention this verification rather than claim a code change. (2) **`BACKEND_HOST` push generalized** — was previously inside the `if let Some(bp) = backend_port_str` arm in both `upstream_kvs` and `subject_kvs` builders, fires only for plaintext-backend fixtures (0003). Lifted to a separate `if backend_port_str.is_some() || tls_backend_port_str.is_some()` arm so fixture 0005 (TLS-backend-only) also gets the substitution. This is the actual content-change vs PLAN's example, which kept the original gating (PLAN line 2872 et seq); the task brief explicitly called out the generalization. (3) **PLAN field-name reference `pki.ca_pem` corrected to `pki.ca_pem_path`** — `TlsTestPki` exposes `ca_pem_path: PathBuf` (tls.rs:25), not `ca_pem`; the unit tests use `&pki.ca_pem_path`. (4) **Final test count is 43, not PLAN-predicted 41** — the brief's "matches Task 8's count" framing is wrong: the 2 new tests early-return successfully (each prints a stderr skip-message and returns `()`), which the harness reports as `ok` (a pass). They are not `#[ignore]`-marked. After Task 10 builds `tls-echo-server` the count stays at 43 but the tests stop early-returning and exercise real TLS round-trips. (5) **`locate_tls_echo_server` is `fn` (not `pub(crate) fn`)** — mirrored the actual shape of `locate_tcp_echo_server` at `backend.rs:91`; the tests live in the same file's `mod tests` and have crate-internal access without a visibility annotation.

## Task 10 — tls-echo-server helper crate (2026-04-26)

- Commit: f591392
- Change: New workspace member `tests/helpers/tls-echo-server/` (sibling of `tcp-echo-server` from phase 02.1) with rustls server-side TLS termination on top. Hand-parsed argv (`--port`, `--cert`, `--key`, `--help`, `--version`) with typed `ArgvError` (variants: `MissingFlag(&'static str)`, `MissingValue`, `InvalidPort`, `Trailing`, `HelpRequested`, `VersionRequested`). Tokio runtime: idempotent `aws_lc_rs::default_provider().install_default()`, single-cert `with_single_cert(...)` server config (no SNI multiplexing — single-purpose helper), `tokio_rustls::TlsAcceptor` accept loop with `tokio::signal::ctrl_c` shutdown + 5s `DRAIN_BUDGET` + abort-stragglers. Exit codes: 0 clean, 1 runtime error, 2 argv error. 5 unit tests: 4 argv (`argv_parses_full_invocation`, `argv_rejects_missing_cert`, `argv_rejects_missing_key`, `argv_shows_help`) + 1 multi-threaded TLS round-trip (`accepts_and_echoes_via_tls` — rcgen-built CA + leaf with SAN `envoy-rust.test`, 22-byte payload byte-exact echo). Added `"tests/helpers/tls-echo-server"` to root `Cargo.toml` `[workspace] members` (alphabetical placement after `tests/helpers/tcp-echo-server`). `#![forbid(unsafe_code)]` per D-3.8.
- Verification: `cargo test -p tls-echo-server` reported **5 passed, 0 failed, 0 ignored**. `cargo test --workspace --lib --bins` reported all crates green: envoy-config 61, envoy-listener 6, envoy-tcp 11, envoy-tls 15, envoy-cluster 19, envoy-bin lib 37 + 1 ignored, envoy-tls-server-test 8, tcp-echo-server bin 8, **tls-echo-server bin 5**, differential 43 + 1 ignored. Task 9's `tls_echo_backend_spawns_and_echoes` and `tls_echo_backend_drop_terminates_child` now exercise the real binary end-to-end (no longer skip-pass via `locate_tls_echo_server` Err early-return). Workspace gate clean: `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check` all exit 0. `Cargo.lock` modified locally by cargo (added the rustls-pemfile + new tls-echo-server stanza family) but **NOT staged** per phase-precedent (deferred to Task 13's gate; matches the cadence established in 03.1 Task 5 `5f92c08` for new workspace members).
- Deviation from PLAN: (1) **All in `main.rs` (no separate `runtime.rs` module)** — PLAN at lines 3200/3312 sketches a `mod runtime` split; mirrored the tcp-echo-server cadence (single 272-LoC `main.rs` with inline runtime), keeping the file at ~340 LoC including all 5 tests. Single-file is easier to follow for a helper this small. (2) **Function name `parse_argv` (not `parse_args`)** — PLAN's Step 3/4 used `parse_args`; tcp-echo-server's existing exported name is `parse_argv`, so consistency wins over PLAN drift. The 4 argv tests all reference `parse_argv`. (3) **5th test uses rcgen DER directly for the client's `RootCertStore::add(...)`** — `ca_cert.der().clone().into_owned()` rather than round-tripping through PEM + `rustls_pemfile::certs(...)`; mirrors the clean Task-7 pattern in `crates/envoy-bin/tests/tls_upstream.rs`. The on-disk PEMs (server cert + key, written via `tempfile::tempdir`) feed the spawned `run(args)` which still uses `rustls_pemfile` to parse them, exercising the production code path. (4) **`Cargo.lock` not staged** — deferred to Task 13 per phase-precedent (Task 5 of 03.1 commit `5f92c08`).
