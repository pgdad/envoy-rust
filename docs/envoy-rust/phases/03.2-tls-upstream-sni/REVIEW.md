# Phase 03.2 REVIEW — upstream TLS origination + multi-cert SNI cert selection + `tls-echo-server` helper + fixtures 0005 + 0006

- **Base:** `64ea760` (phase 03.1 done commit — `phase 03.1: envoy-tls foundation + downstream TLS termination + fixture 0004 [ADR-0018, ADR-0019]`)
- **Head:** `85685a3` (phase 03.2 Cargo.lock sync after state-4 phase-done gate — `phase 03.2: sync Cargo.lock with phase 03.2 dep graph`)
- **Files:** 33 changed (+7875 / -63). Net code-only delta is closer to ~1500 LoC, matching SPEC §5 estimates; the headline figure is inflated by the 4339-line `PLAN.md` and 208-line `PROGRESS.md`, the 332-LoC `tls-echo-server` helper crate, two 5-file fixtures, and the larger-than-PLAN Task 5 envoy-tcp tests block (~370 LoC including helper PKI scaffolding inline). New library surface lands in `crates/envoy-tls/src/lib.rs` (`SniResolver` struct + `DownstreamTls::from_listener` constructor; +94 / 0 in lib.rs), `crates/envoy-tcp/src/lib.rs` (`upstream_tls: Option<Arc<UpstreamTls>>` field + `with_upstream_tls` constructor + branched dial via `Box<dyn AsyncReadWrite + Send + Unpin>` + new `TcpProxyError::UpstreamTlsHandshake { source }` variant; +373 / -22), `crates/envoy-config/src/bootstrap.rs` (`FilterChainMatch` struct + `server_names` field + 3 new validator arms + 11 new tests; +496 / -2), and `crates/envoy-bin/src/main.rs` (per-cluster `Arc<UpstreamTls>` construction loop + 3-way listener TLS dispatch; +129 / -3). New harness surface lands in `tests/differential/src/lib.rs` (`Driver::TlsTcpProbeList` variant + `TlsTcpProbe` struct + `drive_tls_probes` helper + `run_fixture` extensions for `{{TLS_BACKEND_PORT}}` + 4 new tests; +268 / -22), `tests/differential/src/backend.rs` (sibling `TlsEchoBackend` + `locate_tls_echo_server` + 2 new tests; +187 / 0), the new helper crate `tests/helpers/tls-echo-server/` (Cargo.toml + 332-LoC main.rs with 5 tests), 2 new in-process integration tests under `crates/envoy-bin/tests/` (`tls_upstream.rs` 185 LoC; `tls_sni.rs` 271 LoC), 2 new Docker-gated acceptance tests under `tests/differential/tests/` (`tls_upstream.rs` + `tls_sni.rs`, 19 LoC each), and 2 new fixtures `tests/fixtures/0005-tls-upstream/` + `tests/fixtures/0006-tls-sni/` (5 files each). 2 new fuzz-corpus seeds (`tls_multi_cert_sni.yaml`, `tls_overlapping_sni_reject.yaml`); the existing `fuzz_corpus_seeds_parse_or_reject_cleanly` test is extended to cover both. The Cargo.lock sync at `85685a3` adds the new `tls-echo-server` package stanza + cross-edge bumps for `envoy-tcp`'s new `envoy-tls` and `tempfile` deps (Task 5). No new ADRs land — the SPEC §7 / PLAN §3 explicitly anticipated none, and the ledger is byte-identical from ADR-0001 through ADR-0019.
- **Reviewed:** 2026-04-26
- **Verdict:** **Approved with fixes** — state 5 complete pending the §7 close-out (STATE.md text advance, mechanical). I1 closed in-phase via §7; M1–M5 tracked forward to phase 04 / later per §4.

---

## 1. Summary

Phase 03.2 lands the second TLS-aware data-plane property of the project (multi-cert SNI cert selection on the downstream side) and the upstream-TLS *consumer* wiring that 03.1 had pre-staged on the library side. The implementation reads as a careful continuation of 03.1's posture rather than a re-architecture: every doctrinal axis, every public-surface boundary, and every harness invariant from 03.1 is preserved unchanged. envoy-tls grows by exactly two surface elements — the `SniResolver` struct (`crates/envoy-tls/src/lib.rs:193-219`; SNI-keyed `ResolvesServerCert` impl with optional catch-all default; rustls 0.23's `ClientHello::server_name()` already returns lowercase per parent-SPEC §6 signpost 21, so the `HashMap::get` is a direct case-folded lookup with no double-lowercase bug), and the `DownstreamTls::from_listener(&envoy_config::Listener)` constructor (`crates/envoy-tls/src/lib.rs:114-157`; walks `listener.filter_chains`, builds a single `ServerConfig` whose `ResolvesServerCert` is an `Arc<SniResolver>`). 03.1's single-cert `from_context` stays untouched and continues to back fixtures 0001-0004 — the picker between `from_context` and `from_listener` lives in envoy-bin per SPEC §3 D1's drop-in posture. envoy-config grows by the `FilterChainMatch` struct (`bootstrap.rs:131-139`; single `server_names: Vec<String>` field, `#[serde(deny_unknown_fields)]`), the `filter_chain_match: Option<FilterChainMatch>` field on `FilterChain`, and three new cross-chain validator rules (`bootstrap.rs:344-398`) that the executor cleanly fits into the existing per-listener loop body for cache locality. envoy-tcp's `TcpProxy` gains `upstream_tls: Option<Arc<envoy_tls::UpstreamTls>>` plus a `with_upstream_tls` constructor (the existing 03.1 `new` constructor stays plaintext-default, preserving the phase-02.2-vintage call sites byte-identically); `handle::<S>` grows a branched dial body that boxes both arms as `Box<dyn AsyncReadWrite + Send + Unpin>` (a private trait alias auto-impl'd for `T: AsyncRead + AsyncWrite`) so the bidirectional copy stays a single code path, and `TcpProxyError::UpstreamTlsHandshake { source: envoy_tls::TlsError }` lands as the typed-error variant for upstream handshake failures. envoy-bin grows by the per-cluster `Arc<UpstreamTls>` construction loop (`crates/envoy-bin/src/main.rs:86-115`; built once at startup, keyed by cluster name into `HashMap<String, Arc<UpstreamTls>>`, threaded into `TcpProxy::with_upstream_tls` per filter-chain dispatch), and the 3-way listener TLS dispatch (`main.rs:177-221`; plaintext / single-cert via `from_context` / multi-cert via `from_listener`). The dispatch's "single-chain + no `server_names`" predicate keeps the 03.1 path byte-identical, which is the right invariant — fixtures 0001-0004 must not regress. The differential harness gains `Driver::TlsTcpProbeList { probes: Vec<TlsTcpProbe> }` + the `TlsTcpProbe { sni, expected_cn }` struct, the `drive_tls_probes` helper (which mirrors `drive_tls`'s ADR-0006/0007 read-exact + 100ms trailing-byte poll discipline once per probe), the sibling `TlsEchoBackend` (mirrors `TcpProxyBackend`'s SIGKILL-Drop posture), and the `run_fixture` extensions threading `{{TLS_BACKEND_PORT}}` detection + the new dispatch arm. The new `tls-echo-server` helper crate (`tests/helpers/tls-echo-server/src/main.rs`; 332 LoC including 5 tests) is the sibling of the 02.1-vintage `tcp-echo-server` with rustls server-side termination on top — single-cert `with_single_cert(...)` (no SNI multiplexing — single-purpose helper), `tokio_rustls::TlsAcceptor` accept loop with `tokio::signal::ctrl_c` shutdown + 5s `DRAIN_BUDGET` + abort-stragglers. Fixtures 0005 (plaintext downstream → upstream TLS to `tls-echo-server`; `driver.kind: tcp_echo`) and 0006 (multi-cert SNI dispatch on a single downstream listener with 2 filter chains; `driver.kind: tls_tcp_probe_list` with 2 probes) land per SPEC §3 D8 verbatim; both `inputs/payload.bin` files are byte-identical to fixture 0001's payload (verified by the executor in PROGRESS Tasks 11 and 12). The 03.1-landed `Cluster::name()` M1 carryforward is **deferred to phase 06** with an explicit cross-references note pinned in mid-phase commit `cdd5651` (rationale: `UpstreamTlsHandshake { source }` is consistent with the existing `UpstreamConnect { addr, source }` and `CopyFailed { source }` shapes; the `tracing::debug!` at the warn site already names the cluster via `self.cluster_name`; phase 06's stats family is the natural close-out point — see Task 5 PROGRESS).

The work reads cleanly against doctrine on every axis I checked. **D-3.1 TDD discipline**: each implementation task in PROGRESS.md narrates tests landing alongside (or RED-first ahead of) the impl. Task 1 lands 5 parse-shape tests with the schema additions; Task 2 lands 6 validator tests + 2 fuzz-corpus seeds with the validator rules; Task 3 lands `SniResolver` + 4 unit tests in the same commit; Task 4 lands `from_listener` + 1 multi-cert handshake test; Task 5 lands the upstream-TLS dial + 3 unit tests including the SNI capture (Mutex-backed `CapturingResolver`); Task 6 lands the envoy-bin wiring (covered by the existing 03.1 `tls_downstream` integration test which still passes 1/1, plus Task 7); Task 7 lands the in-process `tls_upstream.rs` + `tls_sni.rs` integration tests; Task 8 RED-firsts 2 parse-shape tests + 2 render_yaml tests for the harness extensions; Task 9 lands `TlsEchoBackend` + 2 unit tests (which skip-pass before Task 10 builds the helper binary, then run end-to-end after); Task 10 lands the helper crate + 5 unit tests; Tasks 11/12 land the fixtures + Docker-gated acceptance tests. Every commit pairs with a progress-note commit, plus the mid-phase Task-5 cross-refs follow-up commit `cdd5651` (M1 deferral rationale). **D-3.2 permitted foundations**: zero new dep additions to D-3.2's allow-list — the entire phase rides on rustls + tokio-rustls + rustls-pemfile + rustls-pki-types + rcgen + tempfile already permitted via 03.1's ADR-0018 + ADR-0019. The Cargo.lock sync at `85685a3` is a clean sub-100-line add (1 new package stanza for `tls-echo-server` + cross-edge bumps inside existing crates' stanzas to direct-pin envoy-tcp's freshly-added `envoy-tls` + `tempfile` deps from Task 5, both already in the lockfile transitively from 03.1). **D-3.4 PROGRESS cold-readability**: PROGRESS.md is dense (208 lines) but every task entry carries Commit / Change / Verification / Deviation-from-PLAN sections; deviations are itemized and named (e.g., Task 5 lists 5 deviations including the `envoy-tls`-added-rather-than-promoted finding and the `tempfile` dev-dep landing; Task 4 lists 7 deviations including the `DataSource::as_path()` non-existence and the in-memory `Listener` synthesis vs YAML parse). A stranger could replay the phase from PROGRESS alone. **D-3.5 append-only ADRs**: `git diff 64ea760..85685a3 -- docs/envoy-rust/DECISIONS.md` is empty (no diff). ADR-0001 through ADR-0019 are byte-identical to phase 03.1 done state. **D-3.6 green-build first-attempt**: PROGRESS Task 13 reports the state-4 phase-done gate clean on first attempt; locally-reverified (see "State-4 gate evidence" below) all five gate commands exit 0 with no fix-during-gate commits. **D-3.7 Envoy pin**: `git diff 64ea760..85685a3 -- docs/envoy-rust/ENVOY_TARGET.md` is empty. **D-3.8 unsafe-code forbid**: verified `#![forbid(unsafe_code)]` at line 1 of every workspace crate root including the new `tests/helpers/tls-echo-server/src/main.rs:1`. **D-3.9 toolchain pin**: `git diff 64ea760..85685a3 -- rust-toolchain.toml` is empty.

SPEC §3 deliverables D1–D10 all land in the described shape, with several well-disclosed plan-time deviations. **D1** (`envoy-tls` SNI multi-cert resolver + `from_listener` constructor) ships exactly the shape SPEC §3 D1 describes: `SniResolver { map, default }` with the lowercase-keyed `HashMap` and the `or_else(|| self.default.clone())` miss-fallthrough; `from_listener` walks `listener.filter_chains` and treats a chain with empty/missing `filter_chain_match` as the catch-all (`map.insert` for non-empty `server_names`; `default = Some(...)` for empty). 5 new unit tests landed verbatim against SPEC's enumeration (`sni_resolver_routes_known_sni`, `sni_resolver_falls_back_to_default_on_miss`, `sni_resolver_returns_none_on_miss_without_default`, `sni_resolver_is_case_insensitive`, `from_listener_builds_multi_cert_config`); the in-test PKI helper `pki::build()` was extended to also generate leaf-B for these tests (see Task 3 deviation 1). **D2** (envoy-config schema extensions, 03.2 portion) ships `FilterChainMatch` + `server_names` exactly per SPEC, plus all three new validator variants (`MultipleListenersWithOverlappingSni { listener, sni }` with the doc-comment naming the intra-listener semantics; `MultipleCatchAllFilterChains { listener }`; `MixedTlsAndPlaintextFilterChainsOnListener { listener }`). 6 new validator unit tests + 2 new fuzz-corpus seeds; the `fuzz_corpus_seeds_parse_or_reject_cleanly` test is extended (and the `#[allow(clippy::single_element_loop)]` attribute removed since the reject loop is now 2 elements). **D3** (envoy-tcp upstream-TLS dial, 03.2 portion of the parent-phase D4) ships the `upstream_tls: Option<Arc<UpstreamTls>>` field, the `with_upstream_tls` constructor, the branched dial via `Box<dyn AsyncReadWrite + Send + Unpin>`, and the new `TcpProxyError::UpstreamTlsHandshake { source }` variant — verbatim against SPEC §3 D3's pseudocode. The `tokio::io::split(upstream)` swap-in for the boxed arm replaces the previous TcpStream-specific `into_split()` cleanly (the 03.1 generic-stream lift had already prepared the downstream side; Task 5's lift-on-the-upstream-side mirrors it). **D4** (M1 carryforward — `Cluster::name()` accessor) — DEFERRED to phase 06 with the cross-refs commit `cdd5651` documenting the rationale and the four cross-references (phase-02.1 REVIEW M1, phase-02.2 REVIEW §4 rec 1, phase-03.1 REVIEW §4 rec 2, phase-03.2 SPEC §D4). **D5** (envoy-bin wiring, 03.2 portion) ships the per-cluster `Arc<UpstreamTls>` construction loop + the 3-way listener TLS dispatch + the threading into `TcpProxy::with_upstream_tls`. The `Downstream(_)` arm on a cluster's transport_socket bails defensively (validator already rejected this via `MismatchedTransportSocketDirection { side: "cluster" }`); the 3-way dispatch's `single-chain + no server_names → from_context` predicate keeps fixtures 0001-0004's path byte-identical. **D6** (differential harness extensions) ships `Driver::TlsTcpProbeList` + `TlsTcpProbe` + `drive_tls_probes` (mirroring `drive_tls`'s ADR-0006/0007 discipline once per probe), plus the `run_fixture` extensions threading `{{TLS_BACKEND_PORT}}` detection + the `BACKEND_HOST` push generalized to fire for either backend variant (was previously gated only on `BACKEND_PORT` in 03.1's Task-8 cadence). **D7** (`tls-echo-server` helper crate) ships the new workspace member as a sibling of `tcp-echo-server` (phase 02.1) — single-file `main.rs` (332 LoC including 5 tests; PROGRESS Task 10 deviation 1 explains the single-file vs `mod runtime` split; SPEC's PLAN sketch was a hint, not load-bearing); single-cert `with_single_cert(...)` (no SNI multiplexing — single-purpose helper); `tokio_rustls::TlsAcceptor` accept loop with `tokio::signal::ctrl_c` shutdown + 5s `DRAIN_BUDGET` + abort-stragglers. 5 unit tests cover argv parse + multi-threaded TLS round-trip. **D8** (fixtures 0005 + 0006) lands all 10 files; `inputs/payload.bin` byte-identical to fixture 0001's payload in both fixtures (verified by the executor via `diff` in PROGRESS Tasks 11 and 12). The Docker-gated acceptance tests under `tests/differential/tests/` are 19 LoC each, mirroring 03.1's `tls_downstream.rs` shape verbatim. **D9** (CI workflow) — no `.github/workflows/ci.yml` change needed; the existing `--workspace` invocation transparently picks up the new fixtures and integration tests. **D10** (ADRs to land during execution) — none anticipated, none landed. SPEC §7 explicitly says "no new ADRs anticipated."

SPEC §6 signposts are honored. The signposts that materially bind on 03.2 (lifted from the parent-phase 03 SPEC + 03.2 SPEC §6): **signpost 21** (rustls 0.23 `ClientHello::server_name()` returns lowercase) — `SniResolver::resolve` at `crates/envoy-tls/src/lib.rs:215-218` does a direct `self.map.get(sni)` without re-lowercasing (correct under the lowercase-from-rustls contract); `from_listener` keys the map with `sni.to_lowercase()` (defensive — the validator also rejects case-different overlaps but the resolver assumes nothing about input case). The `sni_resolver_is_case_insensitive` test pins the contract end-to-end. **signpost 4** (crypto-provider install discards `Err`) — `crates/envoy-bin/src/main.rs:54-60` continues to call `let _ = envoy_tls::install_default_crypto_provider();` exactly as 03.1 left it; the new helper `tests/helpers/tls-echo-server/src/main.rs:87` mirrors the same `let _ = ...` idiom. **signpost 8** (forbid unsafe_code) — verified at every crate root including the new `tls-echo-server`. **signpost 14** (no ALPN) — neither the new `SniResolver` nor `from_listener` builders call `with_alpn_protocols`; fixture 0006's YAMLs do not include `alpn_protocols`. **signpost 19** (TLS handshake errors logged at `warn!`, not propagated into the listener's accept loop) — the upstream-TLS handshake failure path in `crates/envoy-tcp/src/lib.rs:103-108` boxes `TcpProxyError::UpstreamTlsHandshake { source }` through to envoy-listener's accept-loop `warn!`-and-continue posture, exactly mirroring the 03.1 downstream-side accept-loop discipline. The `proxies_returns_err_on_upstream_tls_handshake_fail` test at `crates/envoy-tcp/src/lib.rs:851-908` pins both the Display content and the typed `TcpProxyError::UpstreamTlsHandshake` downcast.

State-4 gate evidence is solid. PROGRESS Task 13 reports gate-clean on first attempt; locally re-verified at HEAD `85685a3`: `cargo build --workspace --all-targets` exits 0 (Finished line only); `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0 (no warnings); `cargo fmt --all -- --check` exits 0 (no output); `cargo test --workspace --lib --bins` reports **176 tests passed, 0 failed, 1 ignored** across 9 test result lines (43 + 19 + 8 + 61 + 6 + 11 + 15 + 8 + 5 = 176; the single ignored test is `differential`'s Docker-gated TcpProxyBackend smoke carried forward from 02.2). `cargo deny check` is `advisories ok, bans ok, licenses ok, sources ok` with the now-five `license-not-encountered` warnings preserved (`0BSD` joined the existing four since 03.1; explicitly allowed in `deny.toml` but not currently claimed by any dep — intentional headroom). The Cargo.lock sync at `85685a3` is a single-file commit (139 inserts + 1 delete) per the phase-01 (`4955252`) / phase-02.1 (`dea4d16`) / phase-02.2 (`2146014`) / phase-03.1 (`eb039e6`) precedent. The Docker-gated `tls_upstream_fixture` and `tls_sni_fixture` are skipped locally per Task 11/12 PROGRESS notes (Docker Desktop daemon not running on dev host); CI (`ubuntu-latest`) runs them per ADR-0010. The fuzz job is also skipped locally (cargo-fuzz subcommand not installed); CI runs it.

The executor's self-audit discipline is excellent. PROGRESS Task 4 documents seven non-trivial PLAN-text drifts including `DataSource::as_path()` non-existence (PLAN line 1369; actual struct is `pub struct DataSource { pub filename: String }` so used `Path::new(&cert.certificate_chain.filename)`), the `TlsError::ServerConfigBuild` non-existence (PLAN line 1392; mirrored 03.1's `from_context` builder chain exactly with no new error variant), and the in-memory `Listener` synthesis vs YAML parse (PLAN's example would have required adding `serde_yaml` as a dev-dep on envoy-tls). Task 5 documents five deviations including the `envoy-tls` add-vs-promote correction (the PLAN expected it as a dev-dep needing promotion; in fact 03.1 left it absent from envoy-tcp's manifest entirely, so it was added freshly to `[dependencies]`), the `tokio::io::split(upstream)` swap-in (PLAN expected `into_split()` but that's TcpStream-specific and would not compile against the boxed `dyn AsyncReadWrite`), and the `DataSource` struct-vs-enum correction (PLAN at line 1529 wrote `DataSource::Filename(...)` but it's a struct, so used struct-literal form). Task 6 documents the single-listener-structure preservation (PLAN's pseudocode showed an outer `for listener in &bootstrap.static_resources.listeners` loop; the existing 02.1+ posture is single-listener via `listeners.first()`, validator-capped via `TooManyListeners`). Task 7 documents the direct rcgen → rustls DER conversion (avoids adding `rustls-pemfile` as a new dev-dep on envoy-bin; mirrors the clean Task-7 pattern). Task 8 documents 4 deviations including the 2 RED-first parse-shape tests beyond PLAN's 2 render_yaml tests (TDD doctrine D-3.1 requires RED-then-GREEN for any new variant) and the absence of the final `assert_equivalence` call in the new dispatch arm (byte-equality is enforced inside `drive_tls_probes` per probe). Task 9 documents 5 deviations including the `upstream.rs` no-op (the 03.1 implementation already pre-staged all 7 PEM fields on `TlsTestPki` per the "harmless to expose now" comment) and the `BACKEND_HOST` push generalization. Task 10 documents 4 deviations including the `parse_argv` (not `parse_args`) name choice (mirrors `tcp-echo-server`'s established cadence) and the rcgen-DER-direct pattern for the test PKI. All deviations are well-justified, documented in PROGRESS.md, and inline-commented at the affected sites. The mid-phase Task-5 follow-up commit `cdd5651` is particularly clean — small, single-purpose, with a clear M1-carryforward rationale that names the four cross-references.

One mechanical issue surfaces. **STATE.md is stale by two lifecycle steps** — at HEAD `85685a3` it still reads "phase 03.2 lifecycle state 3 (PLAN.md exists, implementation incomplete)", which was the state 2→3 snapshot from commit `b2c704e`. All 13 PLAN.md tasks are implementation-complete; the state-4 phase-done gate cleared (commit `4344f10`); the Cargo.lock sync followed (`85685a3`); this REVIEW is the state-5 input. This is the *exact same* I1 finding as phase-03.1's REVIEW.md (which itself echoed phase-02.2's I1, which echoed phase-02.1's I2); the close-out shape is identical (advance STATE.md text in the same commit that lands REVIEW.md). Tracked as I1 below and remediated in §7.

---

## 2. Strengths

- **Doctrine conformance end-to-end.** `#![forbid(unsafe_code)]` at line 1 of `tests/helpers/tls-echo-server/src/main.rs` (the only new crate root in the phase) and at every existing crate root (verified). Every dep used in 03.2 was already permitted under the D-3.2 allow-list extended by 03.1's ADR-0018 + ADR-0019 — zero new dep additions, zero new ADRs needed (per SPEC §7's explicit "no new ADRs anticipated" framing). Append-only ADR ledger preserved exactly (`git diff 64ea760..85685a3 -- docs/envoy-rust/DECISIONS.md` is empty; ADR-0001–0019 byte-identical to 03.1 done state). Toolchain pin untouched (`rust-toolchain.toml` not in the diff). Envoy version pin untouched (`docs/envoy-rust/ENVOY_TARGET.md` not in the diff).

- **`SniResolver` is the textbook drop-in replacement for `SingleCertResolver`.** `crates/envoy-tls/src/lib.rs:193-219` ships exactly the shape SPEC §3 D1 describes — `pub struct SniResolver { pub map, pub default }` (fields are `pub` so `from_listener` can build the resolver by direct field assignment without needing a constructor; the public surface is internal-to-the-crate effectively, given `SniResolver` is a private impl detail in the consuming `from_listener` flow but exposed for unit-test reach). The manual `Debug` impl at lines 201-208 prints SNI keys + `has_default` — useful diagnostic that doesn't leak `CertifiedKey` internals. The `resolve` impl at lines 211-218 is one expression with `.cloned()` + `.or_else(|| self.default.clone())` — correct miss-fallthrough; rustls's `ClientHello::server_name()` returns `None` for SNI-less ClientHellos, in which case `resolve` returns `None` (no map probe) and rustls aborts with `unrecognized_name` if there's no catch-all default. This is the right contract.

- **`DownstreamTls::from_listener` is mechanically minimal.** `crates/envoy-tls/src/lib.rs:114-157` is one tight loop body. The `let-else` on `chain.transport_socket` continues for plaintext-chain skipping; the inner `let-else` on `TransportSocketTypedConfig::Downstream(ctx)` defensively continues on any future `Upstream(_)` arm reachability (validator already rejects mismatched direction on listeners; the comment at line 124 names the validator-rejection invariant). `server_names.is_empty()` selects the catch-all branch (`default = Some(certified_key)`); the non-empty arm iterates and inserts into the map keyed lowercase. The final `ServerConfig::builder().with_no_client_auth().with_cert_resolver(resolver)` chain mirrors 03.1's `from_context` exactly — `from_context` and `from_listener` differ only in resolver construction.

- **`from_listener_builds_multi_cert_config` test is byte-exact, not SAN-substring.** `crates/envoy-tls/src/tests.rs` (Task 4 test) drives two in-process TLS handshakes (one per SNI: `a.example.com`, `b.example.com`) and asserts the post-handshake `peer_certificates()[0]` matches the expected leaf via byte-exact DER comparison (using the executor's `pki::cert_der_at(path)` helper added in Task 3). PROGRESS Task 4 deviation 5 explicitly names this — strictly more rigorous than the harness-side `check_cn_or_san` substring scan and avoids both an x509 parser dep and any false-positive risk from substring overlap. Tasks 3 and 7 establish this byte-exact-DER pattern as the rigorous form across the phase.

- **`SniResolver` test 3 (none-on-miss-without-default) uses a `CapturingVerifier` to inspect the server's failure path directly.** PROGRESS Task 3 deviation 4 documents the cleverness: the `unknown.example.com` SNI would normally fail client-side SAN validation before we can inspect what the server returned, so the executor introduced a scoped `mod sni_capture` with a `CapturingVerifier` that implements `rustls::client::danger::ServerCertVerifier` to capture the presented end-entity DER without enforcing SAN. Test 3 then asserts the server-side `acceptor.accept` returns `Err` (rustls aborts with `unrecognized_name` when `resolve()` returns `None`), proving the contract directly. This is a real engineering cost worth flagging — a less rigorous reviewer would have settled for an indirect signal.

- **`TcpProxy::handle::<S>` branched dial is a clean adaptation of the 03.1 generic-stream lift.** `crates/envoy-tcp/src/lib.rs:75-134` keeps the 03.1 generic signature intact (`S: AsyncRead + AsyncWrite + Unpin + Send + 'static`); the body branches on `self.upstream_tls.as_ref()` after `TcpStream::connect`, with both arms unifying into `Box<dyn AsyncReadWrite + Send + Unpin>` for a single-code-path bidirectional copy. The private `trait AsyncReadWrite: AsyncRead + AsyncWrite {}` alias at line 18 + the auto-impl at line 19 is the minimal cost — adds no public surface, makes the boxing arm tractable. The boxed arm forces the generic `tokio::io::split(upstream)` instead of the 03.1 `into_split()` (TcpStream-specific); PROGRESS Task 5 deviation 2 names the necessity and the generic helper accepts both `TcpStream` and `TlsStream<TcpStream>`. ADR-0016 half-close posture is preserved verbatim (`tokio::select!` over the two `tokio::io::copy` futures; EOF on either side propagates via Drop on the write half).

- **`ConnectionHandler::handle` trait-impl wrapper correctly clones `upstream_tls`.** `crates/envoy-tcp/src/lib.rs:162-188` updates the trait-impl wrapper to also clone `self.upstream_tls` (cheap `Option<Arc<...>>` clone — Arc bump on `Some`, no-op on `None`) when reconstructing the proxy in the boxed future. The pattern mirrors the 03.1 cluster + cluster_name clone exactly. The reconstructed `TcpProxy { cluster, cluster_name, upstream_tls }` then calls `proxy.handle::<TcpStream>(downstream).await`, dispatching to the inherent generic method per SPEC §6 signpost 3 option α.

- **`proxies_returns_err_on_upstream_tls_handshake_fail` test asserts both Display and downcast.** `crates/envoy-tcp/src/lib.rs:851-908` (Task 5 test 2) tests the upstream-TLS handshake failure mode with two independent CAs (the proxy's UpstreamTls trust bundle is built from CA2; the upstream presents a leaf signed by CA1; cert-verification must fail). The assertion at lines 901-907 is unusually rigorous — `let downcast = err.downcast_ref::<TcpProxyError>().expect(...); assert!(matches!(downcast, TcpProxyError::UpstreamTlsHandshake { .. }), ...);` — pinning the typed-error variant directly rather than just substring-matching the Display string. This is the right shape; a future error-message refactor would not silently weaken this test.

- **`proxies_to_tls_upstream_sends_sni_in_client_hello` uses a Mutex-backed `CapturingResolver`.** `crates/envoy-tcp/src/lib.rs:914-1002` (Task 5 test 3) builds a custom `ResolvesServerCert` impl that captures `client_hello.server_name()` into an `Arc<Mutex<Option<String>>>` before delegating to a normal CertifiedKey resolver. After the round-trip completes, the test asserts `captured_sni.as_deref() == Some("envoy-rust.test")`. This is the *only* way to verify the SNI-on-the-wire property at the unit-test level — there's no ClientHello introspection API on `tokio_rustls::TlsConnector`. The test is in the right place (envoy-tcp's lib tests, not envoy-tls's), since it pins the `TcpProxy::with_upstream_tls` → `UpstreamTls::connect` wiring rather than the `UpstreamTls::from_context` parse path.

- **envoy-bin 3-way listener TLS dispatch is shape-correct and order-preserving.** `crates/envoy-bin/src/main.rs:177-221` does the SPEC §D5 step 1 dispatch exactly: `any_chain_has_tls = listener_cfg.filter_chains.iter().any(|c| c.transport_socket.is_some())`; `any_chain_has_server_names = listener_cfg.filter_chains.iter().any(|c| c.filter_chain_match.as_ref().map(|m| !m.server_names.is_empty()).unwrap_or(false))`; the predicate `if !any_chain_has_tls { None } else if listener_cfg.filter_chains.len() == 1 && !any_chain_has_server_names { Some(from_context) } else { Some(from_listener) }`. The single-cert path stays byte-identical to 03.1 (the 03.1 in-process `tls_downstream.rs` integration test passes 1/1 unchanged per Task 6 verification); the multi-cert path picks `from_listener` exactly when the predicate intends (≥ 2 chains OR any chain has `server_names`). The `expect("any_chain_has_tls implies Some on the single chain")` at line 204 documents the invariant boundary.

- **Per-cluster `Arc<UpstreamTls>` construction loop is build-once, reuse-everywhere.** `crates/envoy-bin/src/main.rs:86-115` builds `Arc<UpstreamTls>` once per cluster at startup (rustls's `ClientConfig` is `Send + Sync` and re-used across every per-connection invocation of `handle`); `HashMap<String, Arc<UpstreamTls>>` keyed by cluster name is populated up front; the dispatch arm at lines 165-175 looks up the entry and selects `with_upstream_tls(...)` or `new(...)` as appropriate. The `Downstream(_)` arm bails defensively (validator already rejected via `MismatchedTransportSocketDirection { side: "cluster" }`); the bail message names the validator-rejection invariant.

- **`Driver::TlsTcpProbeList` is correctly modeled with `serde(deny_unknown_fields)` at every level.** `tests/differential/src/lib.rs:36-71` ships `Driver::TlsTcpProbeList { probes: Vec<TlsTcpProbe> }` and `TlsTcpProbe { sni, expected_cn: Option<String> }` both with `#[serde(deny_unknown_fields)]`; `expected_cn` is `#[serde(default)]`. The 2 RED-first parse-shape tests (`expectations_parse_tls_tcp_probe_list_driver`, `expectations_parse_tls_tcp_probe_list_without_expected_cn`) pin both happy paths. The dispatch arm at `tests/differential/src/lib.rs:776-803` enforces byte-equality *inside* `drive_tls_probes` per probe (no final `assert_equivalence` call needed — see PROGRESS Task 8 deviation 2; the rustdoc on the variant explains why).

- **`drive_tls_probes` is a faithful per-probe lift of `drive_tls`.** `tests/differential/src/lib.rs:288-357` mirrors `drive_tls`'s body structure exactly — open `TcpStream::connect`, build `tokio_rustls::TlsConnector` once outside the loop (correct: the trust bundle is shared across probes), per probe: parse `ServerName`, connect, optional `expected_cn` check via `check_cn_or_san`, write payload, `read_exact(payload.len())`, ADR-0007 100ms trailing-byte poll, graceful `tls.shutdown()`, drop. The per-probe `with_context(|| format!("... probe sni={:?}", probe.sni))` wrapping is excellent diagnostic posture — every error path names which probe failed.

- **`TlsEchoBackend` is the textbook sibling of `TcpProxyBackend`.** `tests/differential/src/backend.rs:99-168` has Drop-with-SIGKILL + 50ms-poll/2s-deadline fallback (mirrors `TcpProxyBackend` line-for-line); `spawn` reserves a port, locates the binary, spawns with `--port <port> --cert <path> --key <path>` argv + `kill_on_drop(true)` + `RUST_LOG=warn` env; `wait_accept_ready` with 5s budget (vs `TcpProxyBackend`'s 1s — TLS server has more startup work installing the crypto provider + building `ServerConfig`). The 2 unit tests at lines 295-367 mirror the `TcpProxyBackend` tests; both early-return with stderr skip-message when `locate_tls_echo_server` returns `Err` (the `cargo test --workspace` invocation builds all binaries, so the skip-pass only fires for `cargo test -p differential` in isolation). Container-host-via-`host.docker.internal` discipline preserved per ADR-0015.

- **`tls-echo-server` helper crate is single-file and ~300 LoC including tests.** `tests/helpers/tls-echo-server/src/main.rs` is 332 LoC; the executor's PROGRESS Task 10 deviation 1 explains the single-file vs `mod runtime` split choice (mirrors `tcp-echo-server`'s established cadence, easier to follow for a helper this small). Hand-parsed argv (`parse_argv` not `parse_args` per Task 10 deviation 2 — consistency with `tcp-echo-server`'s exported name); typed `ArgvError` (6 variants); 4 argv tests + 1 multi-threaded TLS round-trip test. The runtime body uses `with_single_cert(...)` (no SNI multiplexing — single-purpose helper); accept loop uses `tokio::select!` over `ctrl_c` shutdown + `listener.accept()`; 5s `DRAIN_BUDGET` + abort-stragglers on shutdown; per-connection task spawned into `JoinSet` with handshake-failure → `tracing::warn!` posture mirroring envoy-listener's accept-loop discipline.

- **Fixture 0005 lands without a downstream-TLS hop.** `tests/fixtures/0005-tls-upstream/expectations.yaml:1` is `driver.kind: tcp_echo` (NOT `tls_tcp`). PROGRESS Task 11 explicitly notes the rationale: "the *downstream* is plaintext; the TLS hop is exercised on the upstream side via the byte round-trip succeeding through `tls-echo-server` whose handshake requires SAN-match + CA-validation." This is the right shape — fixture 0005 isolates upstream TLS origination from downstream TLS termination cleanly. Both fixtures' `inputs/payload.bin` are byte-identical to fixture 0001's payload (executor verified with `diff` exit 0 in Tasks 11 and 12).

- **Fixture 0006 uses `tls_tcp_probe_list` with explicit per-probe `expected_cn`.** `tests/fixtures/0006-tls-sni/expectations.yaml:1-7` declares 2 probes, one per SNI, each asserting the leaf cert's CN/SAN matches via the harness's `check_cn_or_san` substring scan. Both proxies must select the same cert for the same SNI for the fixture to pass — this is exactly the cert-selection invariant the SNI multi-cert resolver is supposed to provide. The `equivalence.response_body: byte_exact` at line 7 is left for the implicit per-probe write/read-exact pair inside `drive_tls_probes` (see Task 8 deviation 2 for the rationale on no final `assert_equivalence` call).

- **In-process `tls_sni.rs` integration test asserts byte-exact peer-cert DER per probe.** `crates/envoy-bin/tests/tls_sni.rs:218-231` and `:247-260` capture `peer_certificates()[0]` via `.clone().into_owned()` and assert byte-exact DER equality against the rcgen-built leaf-A and leaf-B DERs respectively. PROGRESS Task 7 deviation 3 explicitly chose this rigorous form over PLAN's `cert_contains_san(...)` substring scan — strictly more rigorous than substring-match and avoids both an x509 parser dep and any false-positive risk from substring overlap. This test is the in-process backstop for the differential `tls_sni_fixture` (which only runs in CI under Docker).

- **Mid-phase Task-5 cross-refs commit `cdd5651` is single-purpose and well-targeted.** Following the Task-5 implementation commit `8a60249` and progress note `bf9f6ea`, the executor noticed the M1 carryforward needed explicit cross-references documented and landed a separate progress-note follow-up commit `cdd5651` with a single-purpose body adding the four cross-references (phase-02.1 REVIEW M1 / phase-02.2 REVIEW §4 rec 1 / phase-03.1 REVIEW §4 rec 2 / phase-03.2 SPEC §D4) and the deferral rationale. This is the right discipline — the alternative would have been a noisy amend or a later catch-up paragraph in PROGRESS Task 13.

- **CI gate cleared on first attempt.** PROGRESS Task 13 reports build/clippy/fmt/test/deny all clean with no fix-during-gate commits. Local re-verification on HEAD `85685a3`: `cargo fmt --all -- --check` exits 0 with no output; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0; `cargo build --workspace --all-targets` exits 0; `cargo test --workspace --lib --bins` reports 176 + 1 ignored matching PROGRESS exactly; `cargo deny check` exits 0 with the now-five `license-not-encountered` warnings preserved. Matches phase-03.1 / 02.2 / 02.1 first-attempt-green pattern.

- **Commit-message hygiene matches the established precedent.** The Cargo.lock-sync commit `85685a3` follows the phase-01 (`4955252`) / phase-02.1 (`dea4d16`) / phase-02.2 (`2146014`) / phase-03.1 (`eb039e6`) precedent — single-file commit, narrative message naming the phase tasks that caused the drift. Task commits follow the pattern `phase 03.2: <crate> — <summary>`; progress-note commits are paired with a `phase 03.2: progress note (task N)` form. The mid-phase Task-5 follow-up `cdd5651` (`phase 03.2: progress note (task 5 — M1 cross-refs)`) makes the multi-step Task-5 cadence visible. No `[ADR-NNNN]` annotation needed because no ADRs landed in 03.2 (per SPEC §7).

---

## 3. Issues

### Critical

None.

### Important

**I1. `STATE.md` is stale by two lifecycle steps; must advance in the same commit that lands REVIEW.md.** *(Closed in-phase — see §7.)*

`/Users/esa/git/envoy-rust/docs/envoy-rust/STATE.md:13` at HEAD `85685a3` reads:

```
**status:** phase 03.2 lifecycle **state 3 (PLAN.md exists, implementation incomplete)** — `SPEC.md` landed at `f256d2c` (alongside the ADR-0017 split decision and the 03.1 SPEC); `PLAN.md` landed at `df91b06` (the previous commit). …
```

This was the state 2→3 snapshot from commit `b2c704e`. Reality at HEAD `85685a3`: all 13 PLAN.md tasks are implementation-complete (commits `1e1ea64` through `e478e35`); the state-4 phase-done gate cleared (commit `4344f10`); the Cargo.lock sync followed (`85685a3`); this REVIEW is the state-5 input. The phase-03.1 (`1748cd2`), phase-02.2 (`fc87505`), phase-02.1 (`379937b`), and phase-01 (`f436c29`) precedents all show the same finding remediated identically: STATE.md advances in the same commit that lands REVIEW.md.

*Why it matters:* STATE.md is the single source of truth for "what next" per its own top-of-file docstring. A stranger cold-starting at HEAD `85685a3` and reading STATE.md would route to `superpowers:subagent-driven-development` for state-3 execution — already complete. Not a doctrine violation (BOOTSTRAP_PROMPT.md §5.1 is "one state per session," not "one STATE.md commit per state"), but a material readability regression vs. the precedent.

*Fix:* state 5 closes with a commit that (a) lands this REVIEW.md and (b) advances STATE.md to state 5 (approved; implementation frozen; state-6 next). State 6 then lands the ROADMAP `03.2` flip, the parent `03` flip (per the ROADMAP-schema invariant — both sub-phases are now `done`), and STATE advance to phase `04` per SPEC §8. Closed by this REVIEW.md commit — see §7.

### Minor

**M1. `needs_tls_pki` detection in `run_fixture` is asymmetric across upstream and subject templates.** *(Tracked forward.)*

`tests/differential/src/lib.rs:572-578` checks five token substrings against `upstream_template` (`{{LEAF_A_CERT_PATH}}`, `{{LEAF_A_KEY_PATH}}`, `{{CA_PATH}}`, `{{LEAF_B_CERT_PATH}}`, `{{SERVER_CERT_PATH}}`) but only two against `subject_template` (`{{LEAF_A_CERT_PATH}}`, `{{CA_PATH}}`). For 03.2 this is non-bug — every fixture that needs PKI references at minimum `{{LEAF_A_CERT_PATH}}` or `{{CA_PATH}}` on the subject side (verified for fixtures 0004, 0005, 0006), so the asymmetric check produces the correct answer. But a future fixture whose subject YAML uses `{{LEAF_B_*}}` or `{{SERVER_*}}` without `{{LEAF_A_CERT_PATH}}` or `{{CA_PATH}}` (e.g., a TLS-only-leaf-B subject configuration) would silently fail PKI generation. The Task-8 reviewer flagged this as M1; the more defensive form would be to extract a `template_needs_tls_pki(template: &str) -> bool` helper that runs all 7 substring checks, then OR the upstream and subject results.

*Fix:* leave as-is. The current shape is sufficient for 03.2's fixture set; flag for awareness when phase 04's brainstorm considers a TLS-only-leaf-B subject template.

**M2. Vestigial `_server_cert` / `_server_key` fields on `TlsEchoBackend`.** *(Awareness-only.)*

`tests/differential/src/backend.rs:102-103` carries `_server_cert: PathBuf` and `_server_key: PathBuf` as alive-keepers. The doc-comment at lines 95-98 acknowledges they are decorative — the spawn lifetime borrows the paths into the child process via the `tokio::process::Command` argv (`spawn` returns immediately and the child holds its own copies; the Rust-side `PathBuf`s could be dropped right after the spawn call), but the `TlsTestPki` `TempDir` outlives the backend by the binding-order discipline in `run_fixture`. The cost is two `PathBuf` allocations per backend (negligible). The benefit is a slightly more defensive lifetime story if a future refactor changes when the child reads the PEMs (e.g., a hot-reload path that re-reads on SIGHUP). The Task-9 reviewer flagged this as M2; either keeping or removing is fine.

*Fix:* leave as-is. The fields cost nothing and document the alive-keeper discipline at the type level.

**M3. `drive_tls` and `drive_tls_probes` share ~80% body; PLAN-time choice was duplication over abstraction.** *(Awareness-only.)*

`tests/differential/src/lib.rs:213-266` (`drive_tls`) and `:288-357` (`drive_tls_probes`) duplicate the per-probe handshake + write/read-exact + 100ms trailing-byte poll body. PLAN-time choice was duplication (avoid an early premature abstraction); the duplication cost is ~50 LoC and a maintenance-cost-on-divergence — if ADR-0006/0007 evolve (e.g., the trailing-byte poll budget changes from 100ms to 250ms), both helpers must be updated in lockstep. The alternative would be to factor out a `drive_one_tls_probe(connector, addr, payload, sni, expected_cn) -> Result<Vec<u8>>` helper called once by `drive_tls` and N times by `drive_tls_probes`. For 03.2 this is awareness-only — both helpers are co-located and a divergent edit would surface in code review.

*Fix:* leave as-is for 03.2; consider the factor-out when phase 04 (HCM) or phase 05 (HTTP/2) adds a third `drive_*` helper that would benefit from sharing the same shape.

**M4. `expectations.yaml` for fixture 0006 omits `equivalence.response_status` but the `equivalence` block is still emitted.** *(Awareness-only.)*

`tests/fixtures/0006-tls-sni/expectations.yaml:6-7` carries `equivalence.response_body: byte_exact`. This `byte_exact` rule never fires for `Driver::TlsTcpProbeList` because the dispatch arm at `tests/differential/src/lib.rs:776-803` does not call `assert_equivalence` (byte-equality is enforced inside `drive_tls_probes` per probe). The rule is effectively decorative. PROGRESS Task 8 deviation 2 documents this; the executor chose to keep the `equivalence` block for visual parity with fixtures 0001-0005's expectations YAML shape (each declares `response_body: byte_exact`). A reader who expected a final `assert_equivalence` call would be misled briefly; the rustdoc on `Driver::TlsTcpProbeList` at `lib.rs:53-62` explains the implicit-conjunction discipline.

*Fix:* leave as-is. The `equivalence` block is consistent with fixtures 0001-0005's shape and the rustdoc explains the difference; flagging only because a future planner may consider tightening `Expectations` to reject `equivalence.response_body` when `Driver::TlsTcpProbeList` is in play.

**M5. Two `tls-echo-server` argv tests omit some negative cases ankled by 03.1's tcp-echo-server tests.** *(Awareness-only.)*

`tests/helpers/tls-echo-server/src/main.rs:213-254` lands 4 argv tests + 1 round-trip test (5 total). The 4 argv tests cover happy-path parse, missing `--cert`, missing `--key`, and `--help`. The 02.1-vintage `tcp-echo-server` (`tests/helpers/tcp-echo-server/src/main.rs`) lands 8 tests — 4 argv (parse, missing port flag, missing value, non-numeric port, trailing arg, help) + 1 round-trip + 1 drain-budget. `tls-echo-server`'s argv tests miss `argv_rejects_missing_port_flag`, `argv_rejects_non_numeric_port`, and `argv_rejects_trailing_argument` (each of those failure modes is implemented by the `parse_argv` body but not regression-tested). The PLAN may have predicted only the 5 tests for parity with the SPEC §D7 enumeration; the omission is a tighter test-fan-out, not a defect.

*Fix:* leave as-is for 03.2. A follow-up could add the missing 3 tests to bring `tls-echo-server` up to `tcp-echo-server`'s coverage parity; flagging only because a stranger reading both helpers side-by-side will notice the asymmetry.

---

## 4. Recommendations

**Forward to phase 04 (and later phases):**

1. **Close the `Cluster::name()` accessor opportunistically when stat-name attribution lands.** The M1 carryforward — first opened in phase-02.1 REVIEW M1, retargeted in phase-02.2 REVIEW §4 rec 1, retargeted in phase-03.1 REVIEW §4 rec 2, and explicitly deferred in phase-03.2 Task 5 PROGRESS — points at phase 06 (stats) as the natural close-out. Phase 04 (HCM HTTP/1.1) or phase 05 (HTTP/2) may surface the need earlier if their per-cluster error attribution wants a typed accessor; the executor should evaluate at brainstorm time and either close the M1 there or formally re-defer to phase 06.

2. **`x509-parser` is still deferred but the ground for it is shifting.** Phase 03.1 REVIEW §4 rec 1 anticipated `check_cn_or_san`'s DER-substring scan stopping being sufficient when 03.2's `Driver::TlsTcpProbeList` exercises three or more leafs in sequence. 03.2 only exercises two leafs (`a.example.com`, `b.example.com`), and the byte-exact peer-cert DER comparison in the in-process `tls_sni.rs` test sidesteps the substring-scan concern entirely. Carry forward as-is; phase 04 / 05 will likely need structured cert introspection if mTLS or peer-cert-attribution headers land. Land a new ADR (likely ADR-0020) when needed and pull `x509-parser` as a dev-test-harness-only dep under the same shape as ADR-0018.

3. **`with_copy_to(target, source)` API quirk — keep the inline comment in `upstream::start`.** Phase-03.1 REVIEW recommendation 4 anticipated this would recur in 03.2; in practice, Task 9 PROGRESS deviation 1 confirms `upstream.rs` was *not* touched in 03.2 because the 03.1 implementation already pre-staged the iterator-driven mount loop walking `pki.container_mounts()`. The inline comment at `upstream.rs:73-77` remains intact; carry the recommendation forward to phase 04 verbatim.

4. **`TypedConfig` enum will grow one variant per filter** across phases 04 (HCM HTTP/1.1), 05 (HTTP/2), 06 (stats / access-logs). Carries over from phase-02.2 REVIEW §4 unchanged.

5. **Round-robin distribution-equivalence assertion remains unit-test-only** (parent-brainstorm Q1 decision). Carries over from phase-02.2 REVIEW §4 unchanged.

6. **If parallel fixture execution arrives** (a future phase's harness widening), revisit `TcpProxyBackend::Drop` per phase-02.2 M1 — `TlsEchoBackend` (landed in 03.2) inherits the same SIGKILL-Drop posture and would be impacted identically. Both backends use `child.start_kill()` + 50ms-poll/2s-deadline fallback in `Drop`; a parallel-fixture harness with overlapping port reuse may surface races in either Drop body. Carries over from phase-02.2 REVIEW §4 + phase-03.1 REVIEW §4 rec 7 unchanged.

7. **`enable_half_close: true` flip-fixture** is the obvious follow-on per ADR-0016. Whichever phase first needs an asymmetric-close use case lands its own ADR + extends `TcpProxy::handle::<S>` with an explicit half-close-propagation mode. The TLS path is no different (TLS does not propagate half-close any differently than plaintext for the byte-exact contract; ADR-0016 explicitly cross-references this; the 03.2 branched dial preserves the ADR-0016 select-on-copy posture for both arms). Carries over from phase-03.1 REVIEW §4 rec 8 unchanged.

8. **`tls_params` floor (TLS 1.3 only) under a new ADR** if rustls-vs-Envoy version negotiation drifts during the 03.2 Docker-gated CI run for fixtures 0005 + 0006. Not anticipated; the differential harness's `Driver::TlsTcp` and `Driver::TlsTcpProbeList` do not pin the version, and rustls + aws-lc-rs's defaults overlap with Envoy v1.33.0's defaults on TLS 1.2 + 1.3. SPEC §6 signpost 20 + ADR-0019's consequences both pre-articulate this. Carries over from phase-03.1 REVIEW §4 rec 9 unchanged.

9. **Optional: factor out `drive_one_tls_probe` helper** when phase 04 / 05 adds a third `drive_*` helper that would benefit from sharing the per-probe handshake body shape. Tracked as M3 above; non-blocking for 03.2.

10. **Optional: bring `tls-echo-server` argv-test coverage to parity with `tcp-echo-server`.** Add `argv_rejects_missing_port_flag`, `argv_rejects_non_numeric_port`, `argv_rejects_trailing_argument`. Tracked as M5 above; non-blocking for 03.2.

---

## 5. Files reviewed

Absolute paths opened during this review:

- `/Users/esa/git/envoy-rust/BOOTSTRAP_PROMPT.md` (D-3.1 / D-3.2 / D-3.5 / D-3.8 / D-3.9 doctrine)
- `/Users/esa/git/envoy-rust/Cargo.toml` (workspace members; `tests/helpers/tls-echo-server` added alphabetically after `tests/helpers/tcp-echo-server`)
- `/Users/esa/git/envoy-rust/Cargo.lock` (via `git diff` — clean Task-5 / Task-10 cross-edge bumps + new tls-echo-server stanza)
- `/Users/esa/git/envoy-rust/crates/envoy-tls/src/lib.rs` (full read — SniResolver + from_listener + 03.1 retained surface)
- `/Users/esa/git/envoy-rust/crates/envoy-tls/src/tests.rs` (function-list spot-check; new tests `sni_resolver_*`, `from_listener_builds_multi_cert_config`)
- `/Users/esa/git/envoy-rust/crates/envoy-tcp/src/lib.rs` (full read — branched dial + 3 new tests)
- `/Users/esa/git/envoy-rust/crates/envoy-tcp/Cargo.toml` (verified `envoy-tls` added to `[dependencies]`, `tempfile` added to `[dev-dependencies]`)
- `/Users/esa/git/envoy-rust/crates/envoy-bin/src/main.rs` (full read — per-cluster Arc<UpstreamTls> loop + 3-way dispatch)
- `/Users/esa/git/envoy-rust/crates/envoy-bin/tests/tls_upstream.rs` (head; in-process TLS upstream backstop)
- `/Users/esa/git/envoy-rust/crates/envoy-bin/tests/tls_sni.rs` (full read — byte-exact peer-cert DER per probe)
- `/Users/esa/git/envoy-rust/crates/envoy-config/src/lib.rs` (via `git diff` — 3 new ConfigError variants + FilterChainMatch re-export)
- `/Users/esa/git/envoy-rust/crates/envoy-config/src/bootstrap.rs` (FilterChainMatch + validator extensions; full structural read of the schema header + the validator's per-listener loop)
- `/Users/esa/git/envoy-rust/crates/envoy-config/fuzz/corpus/parse_bootstrap/tls_multi_cert_sni.yaml` (referenced)
- `/Users/esa/git/envoy-rust/crates/envoy-config/fuzz/corpus/parse_bootstrap/tls_overlapping_sni_reject.yaml` (referenced)
- `/Users/esa/git/envoy-rust/crates/envoy-config/fuzz/.gitignore` (verified 2 new whitelist entries)
- `/Users/esa/git/envoy-rust/tests/differential/src/lib.rs` (full read — Driver::TlsTcpProbeList + drive_tls_probes + run_fixture extensions + 4 new render_yaml/parse-shape tests)
- `/Users/esa/git/envoy-rust/tests/differential/src/backend.rs` (full read — TlsEchoBackend + locate_tls_echo_server + 2 new tests)
- `/Users/esa/git/envoy-rust/tests/differential/src/tls.rs` (referenced; 03.1 implementation pre-staged all 7 PEM fields; not modified in 03.2 per Task 8 deviation 1)
- `/Users/esa/git/envoy-rust/tests/differential/src/upstream.rs` (referenced; not modified in 03.2 per Task 9 deviation 1)
- `/Users/esa/git/envoy-rust/tests/differential/tests/tls_upstream.rs` (referenced; 19 LoC Docker-gated)
- `/Users/esa/git/envoy-rust/tests/differential/tests/tls_sni.rs` (referenced; 19 LoC Docker-gated)
- `/Users/esa/git/envoy-rust/tests/helpers/tls-echo-server/Cargo.toml` (verified)
- `/Users/esa/git/envoy-rust/tests/helpers/tls-echo-server/src/main.rs` (full read — 332 LoC including 5 tests)
- `/Users/esa/git/envoy-rust/tests/fixtures/0005-tls-upstream/envoy.yaml` (referenced)
- `/Users/esa/git/envoy-rust/tests/fixtures/0005-tls-upstream/envoy-rust.yaml` (full read)
- `/Users/esa/git/envoy-rust/tests/fixtures/0005-tls-upstream/expectations.yaml` (full read — `driver.kind: tcp_echo`)
- `/Users/esa/git/envoy-rust/tests/fixtures/0005-tls-upstream/inputs/payload.bin` (verified byte-identical to 0001 via PROGRESS Task 11)
- `/Users/esa/git/envoy-rust/tests/fixtures/0006-tls-sni/envoy.yaml` (referenced)
- `/Users/esa/git/envoy-rust/tests/fixtures/0006-tls-sni/envoy-rust.yaml` (full read)
- `/Users/esa/git/envoy-rust/tests/fixtures/0006-tls-sni/expectations.yaml` (full read — `tls_tcp_probe_list` with 2 probes)
- `/Users/esa/git/envoy-rust/tests/fixtures/0006-tls-sni/inputs/payload.bin` (verified byte-identical to 0001 via PROGRESS Task 12)
- `/Users/esa/git/envoy-rust/docs/envoy-rust/DECISIONS.md` (verified zero diff; ADR-0001–0019 byte-identical to 03.1 done state)
- `/Users/esa/git/envoy-rust/docs/envoy-rust/STATE.md` (verified stale per I1)
- `/Users/esa/git/envoy-rust/docs/envoy-rust/ROADMAP.md` (referenced; row 03.2 remains `in-progress` per Task 13 prediction; flips in state-6 commit)
- `/Users/esa/git/envoy-rust/docs/envoy-rust/SKILL_ROUTING.md` (state-5 transition rules)
- `/Users/esa/git/envoy-rust/docs/envoy-rust/MISSION.md` (D-3.1 / D-3.2 / D-3.5 / D-3.8 / D-3.9 doctrine, full read)
- `/Users/esa/git/envoy-rust/docs/envoy-rust/phases/03.2-tls-upstream-sni/SPEC.md` (the design contract; structural read of §1, §3 D1–D10, §6 signposts)
- `/Users/esa/git/envoy-rust/docs/envoy-rust/phases/03.2-tls-upstream-sni/PROGRESS.md` (full read — all 13 tasks + state-4 gate verification)
- `/Users/esa/git/envoy-rust/docs/envoy-rust/phases/03.2-tls-upstream-sni/PLAN.md` (referenced for plan-deviation cross-checks; per-task scan)
- `/Users/esa/git/envoy-rust/docs/envoy-rust/phases/03.1-tls-foundation-downstream/REVIEW.md` (shape precedent, full read)
- `/Users/esa/git/envoy-rust/docs/envoy-rust/phases/02.2-listener-tcp-proxy/REVIEW.md` (shape precedent + rollover cross-reference)
- `/Users/esa/git/envoy-rust/docs/envoy-rust/phases/02.1-config-cluster/REVIEW.md` (shape precedent)

Local commands run:

- `git -C /Users/esa/git/envoy-rust log --oneline 64ea760..85685a3` → 29 commits including the parent PLAN landing (`df91b06`), STATE-3 advance (`b2c704e`), 13 task commits + 13 progress-note commits (one per task) + 1 mid-phase Task-5 cross-refs commit (`cdd5651`) + state-4 gate (`4344f10`) + Cargo.lock sync (`85685a3`).
- `git -C /Users/esa/git/envoy-rust diff --stat 64ea760..85685a3` → 33 files, +7875 / -63.
- `git -C /Users/esa/git/envoy-rust diff 64ea760..85685a3 -- docs/envoy-rust/DECISIONS.md` → empty (no ADR additions or deletions).
- `git -C /Users/esa/git/envoy-rust diff 64ea760..85685a3 -- rust-toolchain.toml docs/envoy-rust/ENVOY_TARGET.md` → empty (toolchain pin and Envoy pin untouched).
- `cargo fmt --all -- --check` at HEAD `85685a3` → exit 0 (no output).
- `cargo build --workspace --all-targets` at HEAD `85685a3` → exit 0 (Finished line only).
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` at HEAD `85685a3` → exit 0 (no warnings).
- `cargo test --workspace --lib --bins` at HEAD `85685a3` → 176 passed + 0 failed + 1 ignored across 9 test result lines (43 + 19 + 8 + 61 + 6 + 11 + 15 + 8 + 5 = 176; matches PROGRESS Task 13 exactly).
- `cargo deny check` at HEAD `85685a3` → `advisories ok, bans ok, licenses ok, sources ok` (with five `license-not-encountered` warnings: `0BSD`, `BSD-2-Clause`, `MPL-2.0`, `Unicode-DFS-2016`, `Zlib` — all explicitly allowed, none currently claimed).
- `grep -n '#!\[forbid(unsafe_code)\]' crates/envoy-{bin,cluster,config,listener,tcp,tls}/src/{lib,main}.rs tests/helpers/{tcp-echo-server,tls-echo-server}/src/main.rs tests/differential/src/lib.rs` → all 9 crate roots clean at line 1 (including the new `tests/helpers/tls-echo-server/src/main.rs:1`).
- `grep -c '^## ADR-' docs/envoy-rust/DECISIONS.md` → 19 (unchanged from 03.1 done state).

---

## 6. Initial verdict

**Approved with fixes** (initial review, HEAD `85685a3`).

No Critical blockers. One Important finding (I1, STATE.md stale by two lifecycle steps), mechanical and remediated in §7. Five Minor findings (M1–M5), all tracked forward to phase 04 or later — none touch production code or block state 5/6.

The new `SniResolver` + `DownstreamTls::from_listener` constructor in envoy-tls; the upstream-TLS dial + new `TcpProxyError::UpstreamTlsHandshake { source }` variant + `Box<dyn AsyncReadWrite + Send + Unpin>` branched dial in envoy-tcp; the per-cluster `Arc<UpstreamTls>` construction loop + the 3-way listener TLS dispatch in envoy-bin; the `FilterChainMatch` struct + 3 new validator rules + 11 new tests in envoy-config; the `Driver::TlsTcpProbeList` + `drive_tls_probes` + `TlsEchoBackend` + `run_fixture` extensions in the differential harness; the new `tls-echo-server` helper crate (sibling discipline of `tcp-echo-server`); the in-process `tls_upstream.rs` + `tls_sni.rs` integration tests; fixtures 0005-tls-upstream + 0006-tls-sni; the 2 new fuzz-corpus seeds; and the Cargo.lock sync are all shape-correct, test-backed, and doctrine-compliant. The executor's self-audit discipline (Task 4 seven well-disclosed deviations from the PLAN's stale references; Task 5 the `envoy-tls` add-vs-promote correction + the `tokio::io::split(upstream)` swap-in + the `tempfile` dev-dep addition; Task 6 the single-listener-structure preservation; Task 7 the direct rcgen → rustls DER conversion to avoid a new dev-dep on envoy-bin; Task 8 the 2 RED-first parse-shape tests beyond PLAN's prediction; Task 9 the `upstream.rs` no-op verification + the `BACKEND_HOST` push generalization; Task 10 the `parse_argv` consistency choice + the `Cargo.lock` deferral cadence) and first-attempt CI-gate greenness are material positive signals. The mid-phase Task-5 cross-refs commit `cdd5651` is a particularly clean piece of single-purpose discipline — small, targeted, with the four cross-references inline. State 5 may complete in this session by committing REVIEW.md + the STATE-advance per §7; state 6 then lands the ROADMAP `03.2` flip + the parent `03` flip (per the ROADMAP-schema invariant — both sub-phases are now `done`), advances STATE.md to phase `04` (lifecycle state 1; SPEC.md does not yet exist; next-skill `superpowers:brainstorming`), and uses commit-message format `phase 03.2: upstream TLS origination + multi-cert SNI cert selection + tls-echo-server + fixtures 0005 + 0006` per SPEC §9 / `BOOTSTRAP_PROMPT.md` §5.3.

---

## 7. State-5 close-out — I1 remediation (2026-04-26)

I1 is a mechanical remediation (STATE.md text advance) that does not touch production code, does not alter doctrine, and does not change the review's technical findings. A narrow re-review by `superpowers:requesting-code-review` is not warranted — the precedent (phase-03.1 §7 closing I1 in commit `1748cd2`; phase-02.2 §7 closing I1 in `fc87505`; phase-02.1 §7 closing I2 in `379937b`; phase-01 §9 close-out at `f436c29`) already validated this close-out form four times.

### I1 — STATE.md advance

- Commit: this commit (lands alongside REVIEW.md).
- Diff: `docs/envoy-rust/STATE.md` — `**status:**` advanced from "phase 03.2 lifecycle state 3 (PLAN.md exists, implementation incomplete)" to "phase 03.2 lifecycle state 5 (REVIEW.md approved; state-6 next)"; "Next expected skill" rewritten for the state-6 phase-done gate (no further skill — state 6 is the phase-done commit per BOOTSTRAP_PROMPT.md §5.3); "Last commit" reference updated to point at this REVIEW commit; "Last updated" stamp refreshed. No `Notes` section rewriting; rollover tracking (M1–M5) delegated to this REVIEW.md §3–§4.
- Phase-03.1 precedent followed: `1748cd2` shape (STATE-advance commit that lands REVIEW.md and flips STATE.md text in one atomic move).

### M1–M5

Tracked forward per §3 and §4. None are state-5 or state-6 blockers.

### Final verdict

**Approved** (state 5 complete). HEAD is the commit landing this REVIEW.md + STATE-advance. Next session executes state 6: phase-done commit that flips ROADMAP row `03.2` `status` → `done` AND parent row `03` `status` → `done` (per the ROADMAP-schema invariant: parent flips when all sub-phases are `done`; row `03.1` is already `done` as of phase-03.1's state-6 commit `64ea760`), advances STATE.md to phase `04` (slug TBD; lifecycle state 1; SPEC.md does not yet exist; next-skill `superpowers:brainstorming`), and uses commit-message format `phase 03.2: upstream TLS origination + multi-cert SNI cert selection + tls-echo-server + fixtures 0005 + 0006` per SPEC §9 / `BOOTSTRAP_PROMPT.md` §5.3. State 6 does not require further review work.
