# Phase 05.1 REVIEW — Fixture-hardening preamble: `ClusterType::StrictDns` + 5-fixture coordinated edit + phase-02.1 I3 close

- **Base:** `f1804a7` (parent of 05.1's state-2 PLAN.md commit `f23d08f`; the parent-05 state-2 close-out — last commit before any 05.1 work).
- **Head:** `a64d9fc` (Task 4 progress-note backfill — last 05.1 commit before state 6).
- **Files:** 22 changed (+2450 / −83). Net Rust delta is approximately +145 LoC schema (envoy-config) + ~120 LoC runtime + ~25 LoC mechanical cross-crate async churn (envoy-cluster + envoy-tcp + envoy-http1) + ~10 LoC YAML fixture flips + 35 LoC new fuzz seed; the headline figure is inflated by the 1680-line PLAN.md and the 159-line PROGRESS.md. Schema additions land in `crates/envoy-config/src/bootstrap.rs` (`ClusterType` enum extended from single-variant `Static` to `Static | StrictDns` at lines 60-72 with a SCREAMING_SNAKE_CASE serde tag; 6 new validator tests at lines 4287–4528; 1 new corpus-walk slice entry around line 2150). Runtime additions land in `crates/envoy-cluster/src/cluster.rs` (`ClusterError::DnsResolutionFailed { cluster, address, source }` variant at lines 116–121; `from_bootstrap` promoted from `pub fn` to `pub async fn` at line 134; STRICT_DNS resolution branch via `tokio::net::lookup_host(..).await` around lines 170–200 with explicit zero-result guard; 3 new unit tests at lines 569–694, including `static_cluster_constructs_with_literal_ip` which closes phase-02.1 REVIEW I3). `crates/envoy-cluster/Cargo.toml` gains a direct `tokio = { version = "1", features = ["net", "rt", "macros"] }` dep. `crates/envoy-bin/src/main.rs:83` gains a single `.await` token. Forced cross-crate test-helper churn lands in `crates/envoy-tcp/src/lib.rs` (`mk_handle` promoted to `async fn` plus 11 call-site `.await` updates) and `crates/envoy-http1/src/hcm.rs` (`cluster_mgr_with_endpoint`, `cluster_mgr_empty`, `hcm_config_single_route`, `build_test_config` promoted to `async fn` plus ~13 call-site `.await` updates). 1 new fuzz seed `crates/envoy-config/fuzz/corpus/parse_bootstrap/strict_dns_cluster.yaml` (35 lines) plus the `.gitignore` allow-list extension. The 10-file YAML fixture flip lands in `tests/fixtures/{0003,0004,0005,0006,0008}/{envoy.yaml,envoy-rust.yaml}`. `Cargo.lock` synced inline at Task 2 (single line: `tokio` added to `envoy-cluster`'s dep list). `docs/envoy-rust/DECISIONS.md` gains ADR-0023 (+13 lines exactly per `git diff f1804a7..a64d9fc -- docs/envoy-rust/DECISIONS.md`).
- **Reviewed:** 2026-05-02.
- **Verdict:** **Approved with M-track follow-ups** — state 5 complete. No blocking issues in the 05.1 surface itself. The state-4 phase-done gate is **RED** on Docker-gated fixture 0008 (with 0003/0004/0005/0006 NOT RUN because `cargo test` short-circuits) — meaning SPEC §3 D4's Docker-gated 5-fixture green re-baseline is unmet at this commit and SPEC §3 D5's "Closes phase-04.3 REVIEW C-1" is **NOT** discharged here. The deviation is well-disclosed in PROGRESS.md Task 4 and the b7fe910 commit message, and the closure is appropriately deferred to a follow-up sub-phase under proper SPEC + ADR discipline (per PLAN.md's "if a fixture remains red → re-enter state 3" guidance, here translated to "exit 05.1 cleanly with the schema + runtime + YAML preamble landed; re-enter the C-1 trace under a follow-up sub-phase rather than absorbing root-cause diagnosis into this fixture-hardening preamble whose SPEC explicitly excluded it"). The decision to discard the prior in-session 6-patch root-cause expansion (commits `9279895` / `2d3d679` / `339b3c7`, preserved on local branch `backup/task4-scope-creep-2026-05-02`) and rebuild Task 4 as a 0-LoC verification-only commit is doctrinally correct: it preserves PLAN.md's Task 4 contract + the SPEC §7 "no new ADRs at this task" invariant + D-3.6 minimalism, at the cost of a forward-tracked C-1. The schema (D1) + runtime (D2) + fixture-flip (D3) deliverables and the I3 close-out (D5) all land in the described shape, with one well-disclosed planner-time refinement (Signpost A: error variant landed on `ClusterError::DnsResolutionFailed` rather than the SPEC §3 D1-projected `ConfigError::ClusterDnsResolutionFailed`; documented in ADR-0023's "Implementation refinement" provenance footer + PROGRESS.md Task 1 + PLAN.md signpost A) and one well-disclosed forced-cross-crate-churn deviation (Signpost: PLAN.md enumerated only the 5 envoy-cluster + 1 envoy-bin call sites of `from_bootstrap`; the workspace also contains 3 additional test-helper callers in envoy-tcp + envoy-http1 which had to be promoted to `async fn` to keep the workspace compiling — purely mechanical `.await` propagation with no behavioral change).

---

## 1. Summary

Phase 05.1 is the narrow fixture-hardening preamble for parent phase 05 (HTTP/2 cleartext data plane), executing in 4 substantive tasks across 11 commits between base `f1804a7` and head `a64d9fc`. Task 1 (`bfabcb6` + Task-1 review fix `7391a4e`) lands the `ClusterType::StrictDns` schema variant in `envoy-config` plus ADR-0023 inline; Task 2 (`f7a555d`) lands the async `from_bootstrap` promotion + the STRICT_DNS resolution branch via `tokio::net::lookup_host` in `envoy-cluster` plus the new `ClusterError::DnsResolutionFailed` variant + the I3-closing positive-`Static` regression guard; Task 3 (`0ce0aa2`) lands the coordinated 10-file YAML edit flipping `type: STATIC` → `type: STRICT_DNS` across the 5 affected fixtures; Task 4 (`b7fe910` + `a64d9fc` backfill) materializes the state-4 phase-done gate evidence in git history while honestly recording the red CI outcome on fixture 0008.

The implementation reads as a careful, narrowly-scoped continuation of the 04.3 close-out posture rather than a re-architecture: every doctrinal axis from earlier phases is preserved (`#![forbid(unsafe_code)]` unchanged, `anyhow` boundary unchanged, append-only ADR ledger preserved at `ADR-0022 → ADR-0023`, Envoy version pin untouched, toolchain pin untouched, no new top-level workspace dep — `tokio` was already a top-level dep elsewhere in the workspace, only newly direct on envoy-cluster). The only public-surface delta is an enum-variant addition + an error-variant addition + a function-signature `async` lift, all on three small typed boundaries. The new STRICT_DNS code at `crates/envoy-cluster/src/cluster.rs` lines 170–200 reads cleanly: it produces a `Vec<SocketAddr>` from `tokio::net::lookup_host(host:port).await`, defends against the documented zero-result-on-success edge case via an explicit `if resolved.is_empty()` branch synthesizing an `io::Error` of kind `NotFound`, and feeds the resolved set into the existing endpoint vector for round-robin LB pickup.

The work reads cleanly against doctrine on every axis I checked. **D-3.1 TDD discipline**: each implementation task in PROGRESS.md narrates tests landing alongside (or RED-first ahead of) impl; Task 1 lands 6 envoy-config validator tests + 1 corpus-walk extension; Task 2 lands 3 envoy-cluster tests (positive Static, STRICT_DNS happy-path, NXDOMAIN-equivalent failure) + updates 5 existing tests for `async`. Test-count delta from 04.3 close: envoy-config 139 → 145 (+6), envoy-cluster 11 → 14 (+3); the workspace's other test counts are unchanged (cross-crate async-helper churn was mechanical and added zero new tests). **D-3.2 permitted foundations**: zero new top-level dep additions; the `tokio` dep added to `crates/envoy-cluster/Cargo.toml` is a direct-on-this-crate addition, not a workspace-level new dep — `tokio` is already pulled by envoy-tcp/envoy-http1/envoy-tls/envoy-listener/envoy-bin per the existing 02.1+ shape, and `tokio::net::lookup_host` is on D-3.2's permitted-foundations list. The conscious rejection of `trust-dns-resolver` / `hickory-resolver` is recorded in ADR-0023's options-considered (v) and SPEC §6 signpost 5. **D-3.4 PROGRESS cold-readability**: PROGRESS.md is concise (159 lines for 4 tasks) and every task entry carries Commit / Change summary / Verification tail / Deviations sections; the Task 1 review-fix is preserved as a sub-section rather than overwriting the Task 1 entry. **D-3.5 append-only ADRs**: `git diff f1804a7..a64d9fc -- docs/envoy-rust/DECISIONS.md` shows exactly +13 lines: the new ADR-0023 block (the parent-05 row in the ledger summary at line 415 was not edited; ADR-0022 is unchanged). ADR-0023 is well-shaped: Date / Status / Context / Options-considered (5 alternatives weighed, including the rejected "use a different DNS resolver" option which would have required a permitted-foundations-extension ADR) / Decision / Rationale / Consequences (enumerated to 9 specific bullets) / Provenance (with the "Implementation refinement" footer documenting the SPEC §3 D1 → ADR-0023 placement drift). **D-3.6 green-build first-attempt**: `cargo build` / `cargo clippy --all-features -- -D warnings` / `cargo fmt --check` all clean at HEAD `a64d9fc` (locally re-verified — see "State-4 gate evidence" below). The state-4 verification at `cargo test --workspace` is RED on `differential::http1_router_upstream_fixture` (fixture 0008), which is the documented C-1-not-yet-closed deviation rather than a regression in 05.1's actual surface. **D-3.7 Envoy pin / D-3.9 toolchain pin**: both diff empty over the review range. **D-3.8 unsafe-code forbid**: verified `#![forbid(unsafe_code)]` continues at the crate-roots of envoy-config and envoy-cluster (no new crates were added in 05.1).

SPEC §3 deliverables D1–D5 land in the following shape, with the well-disclosed Signpost-A drift:

- **D1 — `ClusterType::StrictDns` schema variant in envoy-config.** Lands at `crates/envoy-config/src/bootstrap.rs:60-72` exactly as SPEC §3 D1 enumerates: `pub enum ClusterType { Static, StrictDns }` with the existing `#[serde(rename_all = "SCREAMING_SNAKE_CASE", deny_unknown_fields)]` derive applied. The 6 SPEC-projected validator tests all land verbatim (`parses_cluster_with_type_strict_dns`; `parses_cluster_with_type_static_unchanged`; `rejects_cluster_with_type_logical_dns`; `rejects_cluster_with_unknown_type_value`; `parses_cluster_with_type_strict_dns_with_multi_endpoint_load_assignment`; `validates_strict_dns_cluster_does_not_require_literal_ip_endpoints`) at `bootstrap.rs:4287-4528`. The new fuzz seed `crates/envoy-config/fuzz/corpus/parse_bootstrap/strict_dns_cluster.yaml` (35 lines: full bootstrap with one tcp_proxy listener + one STRICT_DNS cluster with a `localhost` endpoint) lands as projected, and the in-tree `fuzz_corpus_seeds_parse_or_reject_cleanly` walk-list at `bootstrap.rs` is correctly extended (the Task-1 review fix `7391a4e` caught the planner-time omission — without that fix, the in-tree gate would silently skip the new seed). The conscious deviation: the `ConfigError::ClusterDnsResolutionFailed` error variant projected in SPEC §3 D1 was not added to envoy-config; the equivalent `ClusterError::DnsResolutionFailed` lives in envoy-cluster instead — see Signpost A drift below.

- **D2 — `Cluster::new` extension for STRICT_DNS resolution in envoy-cluster.** Lands at `crates/envoy-cluster/src/cluster.rs:134` (the `pub async fn from_bootstrap` signature) and lines 170–200 (the STRICT_DNS branch). `from_bootstrap` is promoted from `pub fn` to `pub async fn`. The `match cfg.cluster_type` arm dispatches to either the existing `SocketAddr::from_str` literal-IP path (unchanged from 02.1) or the new `tokio::net::lookup_host(target).await` DNS-resolution path. The pseudocode in SPEC §3 D2 maps essentially line-for-line to the implementation. The defensive zero-result branch correctly synthesizes a `std::io::Error::new(NotFound, "DNS resolution returned zero addresses")` so the `DnsResolutionFailed.source` field is populated uniformly across the platform-divergent NXDOMAIN-vs-empty outcomes. The 3 SPEC-projected unit tests all land: `static_cluster_constructs_with_literal_ip` at `cluster.rs:569` (the I3-closing positive Static regression guard — see D5 below); `strict_dns_cluster_resolves_localhost_at_build_time` at `cluster.rs:607` (asserts `picked.port() == 7000` and `picked.ip().is_loopback()`); `strict_dns_cluster_returns_dns_resolution_failed_on_nxdomain` at `cluster.rs:651` (uses `.invalid` TLD per RFC 6761 §6.4). The 5 existing `from_bootstrap` tests (`from_bootstrap_builds_single_endpoint_cluster`, `from_bootstrap_builds_three_endpoint_cluster`, `from_bootstrap_rejects_empty_cluster`, `from_bootstrap_rejects_duplicate_cluster_name`, `from_bootstrap_rejects_malformed_endpoint_address`) are correctly promoted to `#[tokio::test] async fn` with `.await` on the `from_bootstrap` call. The single envoy-bin call site at `crates/envoy-bin/src/main.rs:83` gains exactly one `.await` token.

- **D3 — Coordinated 5-fixture YAML edit.** Lands at commit `0ce0aa2`. The 10 YAML files (`tests/fixtures/{0003,0004,0005,0006,0008}/{envoy.yaml,envoy-rust.yaml}`) each have exactly one line changed: `type: STATIC` → `type: STRICT_DNS` on the cluster whose endpoints reference `{{BACKEND_HOST}}`. Verified via `grep -n "type: STRICT_DNS"` — all 10 lines match. Verified via `grep -n "type: STATIC"` — empty across the 5 fixtures (no straggler). The 3 unaffected fixtures (0001 / 0002 / 0007) are untouched, as projected. The optional `dns_lookup_family: V4_ONLY` knob was correctly omitted per SPEC §6 signpost 6's recommended posture ("rely on AUTO default unless CI flakes").

- **D4 — Verification deliverable (Docker-gated CI re-push).** Lands at commit `b7fe910` as a 0-LoC PROGRESS.md edit + the verification text. **This deliverable is the open issue at state 5: the CI run is RED, not green.** PROGRESS.md Task 4 quotes CI run URL `https://github.com/pgdad/envoy-rust/actions/runs/25258722850` and the per-fixture matrix: 0001/0002/0007 GREEN; 0008 RED (`response_status: exact` mismatch — upstream Envoy returns 503, envoy-rust subject returns 200); 0003/0004/0005/0006 NOT RUN (because `cargo test` exits at the first failing test binary, alphabetic order picks 0008 before 0003). The b7fe910 commit message accurately mirrors this outcome and explicitly states "§7.5 NOT met at this commit. Phase-04.3 REVIEW C-1 NOT materially closed; closure deferred to a follow-up sub-phase under proper SPEC + ADR discipline." See §3 Important I1 below for the full disposition.

- **D5 — Closes phase-02.1 REVIEW I3.** The positive `Static` regression guard `static_cluster_constructs_with_literal_ip` at `crates/envoy-cluster/src/cluster.rs:569` is structurally meaningful only because Task 1 added a second `ClusterType` variant — was un-writable in 02.1 / 02.2 / 03.1 / 03.2 / 04.1 / 04.2 / 04.3 because the single-variant enum had no other arm against which to discriminate `Static`. The test exercises the YAML parse → `from_bootstrap` → `ClusterManager.get → pick_endpoint` chain end-to-end on a literal-IP `127.0.0.1:7000` endpoint, asserting the resolved `SocketAddr` equals the expected value. The carryforward chain from phase-02.1 → 02.2 → 03.1 → 03.2 → 04.1 → 04.2 → 04.3 → 05.1 ends at this commit. STATE.md's "Carryforwards" / "Notes" section bookkeeping for this close-out is projected to land at the 05.1 state-6 commit per SPEC §6 signpost 17.

SPEC §6 signposts inherited from parent-05 are honored: **Signpost 14 (Cargo.lock cadence)** is observed inline at Task 2 (single-line `tokio` dep addition to envoy-cluster's `[[package]]` block in Cargo.lock); **Signpost 16 (PLAN.md cadence — pre-Task-1 standalone commit)** is observed (PLAN.md commit `f23d08f` is independent of the Task 1 commit `bfabcb6`); **Signpost 21 (ADR ledger projection)** matches: ADR-0022 (parent-05 split) was the last ADR pre-05.1; ADR-0023 lands at Task 1; the ledger remains append-only with no renumbering. 05.1-local signposts are honored: **Signpost 5 (`tokio::net::lookup_host` chosen, not third-party DNS resolver)** — the `crates/envoy-cluster/src/cluster.rs:177` call site uses `tokio::net::lookup_host` exclusively; **Signpost 6 (`dns_lookup_family` not parsed in 05.1)** — verified absent from `bootstrap.rs::ClusterType` and absent from the 5 fixture YAMLs; **Signpost 9 (`from_bootstrap` async promotion)** — applied (the function was sync at HEAD `e626862`); **Signpost 10 (unit-test target = `localhost`)** — applied; **Signpost 11 (`.invalid` TLD with empty-host fallback)** — applied (no fallback needed; `.invalid` resolves cleanly to NotFound on the local machine per the test verification); **Signpost 12 (`cargo deny check` no-op)** — observed (locally re-verified clean); **Signpost 13 (`#![forbid(unsafe_code)]` unchanged)** — verified at the crate-roots of envoy-config and envoy-cluster; **Signpost 14 (`anyhow` boundary unchanged)** — verified (envoy-cluster returns typed `ClusterError`; envoy-bin's `?` chain absorbs into `anyhow`); **Signpost 15 (no `BEHAVIOR_CONTRACT.md` edits)** — verified (`git diff f1804a7..a64d9fc -- docs/envoy-rust/BEHAVIOR_CONTRACT.md` is empty); **Signpost 16 (fuzz seed file path consistency)** — verified at `crates/envoy-config/fuzz/corpus/parse_bootstrap/strict_dns_cluster.yaml`.

State-4 gate evidence is honest about the red CI. PROGRESS Task 4 (lines 102–157) reports gate-status correctly: local `cargo build` clean; local `cargo clippy --all-features -- -D warnings` clean; local `cargo fmt --check` clean; local `cargo test --workspace` RED on the documented 0008 fixture; `cargo +nightly fuzz run parse_bootstrap -- -max_total_time=30` clean (769302 runs in 31 seconds); `cargo deny check` clean; CI run `25258722850` matrix red on 0008 with 0003/0004/0005/0006 NOT RUN. Locally re-verified at HEAD `a64d9fc` (2026-05-02): `cargo build --workspace --all-targets` exits 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0 with no warnings; `cargo fmt --all -- --check` exits 0 with no output; `cargo test --package envoy-cluster --lib` reports 14 passed / 0 failed; `cargo test --package envoy-config --lib` reports 145 passed / 0 failed.

The executor's self-audit discipline is excellent given the unusual Task 4 outcome. Task 4's narrative in PROGRESS.md does not euphemize the red CI — paragraph 6 explicitly names "PLAN.md projected this commit would materially close phase-04.3 REVIEW C-1 at a green CI run; reality is a red CI run. The PLAN template's 'all 8 fixtures green / 5 RESTORED + 3 unchanged' rendering does not match the captured CI matrix and was not used. The verification commit lands regardless, to materialize the gate-evidence artifact in git history per BOOTSTRAP_PROMPT.md §7.5; C-1 closure itself moves to a future follow-up sub-phase whose SPEC will be brainstormed against the captured 0008 status-mismatch trace." The narrative also discloses the prior in-session aborted expansion attempt (commits `9279895` / `2d3d679` / `339b3c7`, since reset and force-pushed away; preserved on local branch `backup/task4-scope-creep-2026-05-02`) — confirmed via `git branch --list 'backup/*'` showing the preservation branch exists. Discarding the inline expansion preserves the PLAN's "0 LoC of code changes" Task 4 contract and the SPEC §7 "no new ADRs at this task" invariant; this is the doctrinally correct disposition for an unanticipated mid-task scope-creep, mirroring the 04.3 disposition for cross-phase carryforwards.

---

## 2. Strengths

- **Doctrine conformance end-to-end.** `#![forbid(unsafe_code)]` continues at the crate-roots of envoy-config and envoy-cluster (no new crates were added in 05.1, so no new forbid annotations to verify). Zero new top-level dep additions; the `tokio` dep added directly to `crates/envoy-cluster/Cargo.toml` is a direct-on-this-crate addition (not a workspace-level new dep — `tokio` was already pulled by envoy-tcp/envoy-http1/envoy-tls/envoy-listener/envoy-bin per the existing 02.1+ shape). `cargo deny check` clean (locally re-verified). Append-only ADR ledger preserved exactly: `git diff f1804a7..a64d9fc -- docs/envoy-rust/DECISIONS.md` shows +13 lines (the ADR-0023 block at lines 424–435), and ADR-0022 is unedited. Toolchain pin untouched. Envoy version pin untouched.

- **`Cluster::from_bootstrap`'s STRICT_DNS branch is rigorously defensive.** `crates/envoy-cluster/src/cluster.rs:170-200`. (a) The `tokio::net::lookup_host` call is correctly fed a `host:port` formatted target string at line 177 — this is the standard Rust idiom; the port is preserved through the resolver and lands on the `SocketAddr`s in the resulting iterator. (b) The error-mapping closure captures `cluster.name`, `address` (the configured DNS-name string, not the `host:port` target), and the underlying `std::io::Error` for full diagnostic provenance. (c) The defensive zero-result branch correctly handles the documented platform-divergent case where `lookup_host` returns an empty iterator on success rather than as an `io::Error` (NXDOMAIN behavior varies across platforms); the synthesized `io::Error::new(NotFound, "DNS resolution returned zero addresses")` ensures the `DnsResolutionFailed.source` field is populated uniformly. (d) The resolved `Vec<SocketAddr>` is `extend`-merged into the cluster's endpoint list — multi-address resolution is permitted, and the existing `Cluster::pick` round-robin cursor will naturally iterate across the resolved set. (e) The choice to use `Vec<SocketAddr>` rather than `Arc<RwLock<Vec<SocketAddr>>>` correctly defers the `dns_refresh_rate`-driven re-resolution surface to a future phase (per parent-05 SPEC §4 / 05.1 SPEC §4 / ADR-0023 consequences); the simpler one-shot resolution suffices for the C-1 fix.

- **The async promotion of `from_bootstrap` is mechanically minimal and correctly propagates.** The single envoy-bin call site at `crates/envoy-bin/src/main.rs:83` adds exactly one `.await` token. The 5 existing tests in envoy-cluster gain `#[tokio::test] async fn` + `.await` updates with no test-body changes. The 3 forced cross-crate test-helper promotions in envoy-tcp + envoy-http1 (see §4 awareness-only entry) are purely mechanical: `mk_handle` in envoy-tcp goes from `fn → async fn` with 11 caller `.await` updates; `cluster_mgr_with_endpoint` / `cluster_mgr_empty` / `hcm_config_single_route` / `build_test_config` in envoy-http1 go from `fn → async fn` with ~13 caller `.await` updates. No test-body assertions changed; no values were lost in the conversion. `cargo test --package envoy-cluster --lib` and `cargo test --package envoy-config --lib` both re-verified clean at HEAD.

- **`ClusterType::StrictDns` schema variant is the minimum viable scope.** `crates/envoy-config/src/bootstrap.rs:60-72`. The single-variant enum extension to `Static | StrictDns` reuses the existing `#[serde(rename_all = "SCREAMING_SNAKE_CASE", deny_unknown_fields)]` derive — the `STRICT_DNS` serde tag maps mechanically with zero schema-side validator-code changes. The conscious rejection of `LogicalDns` (ADR-0023 options-considered (ii)) and of the `dns_lookup_family` / `respect_dns_ttl` / `dns_resolvers` / `dns_refresh_rate` knobs (ADR-0023 consequences + 05.1 SPEC §4 deferrals) keeps the diff tight at ~15 LoC schema for the chosen scope. Doc comments directly cite ADR-0023 and the deferral rationale.

- **The 6 envoy-config validator tests cover the full parse-side surface.** `crates/envoy-config/src/bootstrap.rs:4287-4528`. The positive STRICT_DNS happy-path (`parses_cluster_with_type_strict_dns`); the regression-guard for the unchanged STATIC parse path (`parses_cluster_with_type_static_unchanged`); the LOGICAL_DNS deferral documentation as a parser-rejection test (`rejects_cluster_with_type_logical_dns` — explicitly notes that if a future phase lifts the deferral, this test renames + flips its assertion); the unknown-tag rejection (`rejects_cluster_with_unknown_type_value`); the multi-endpoint shape (`parses_cluster_with_type_strict_dns_with_multi_endpoint_load_assignment` — verifies that DNS-name endpoints are stored as raw strings at parse time, not resolved); and the load-bearing parse-side discriminator (`validates_strict_dns_cluster_does_not_require_literal_ip_endpoints` — explicit assertion that the `EndpointParse` ClusterError check is NOT triggered for STRICT_DNS clusters at parse time, because the runtime resolution lives in envoy-cluster's `from_bootstrap`, not in the envoy-config validator). The sixth test is particularly load-bearing as a doctrine-fit assertion: it pins the parse/validate boundary discipline (envoy-config does NOT do DNS resolution; envoy-cluster does).

- **The 3 envoy-cluster unit tests cover the full runtime-side surface.** `crates/envoy-cluster/src/cluster.rs:569-694`. The positive Static regression guard (`static_cluster_constructs_with_literal_ip` — closes phase-02.1 REVIEW I3); the STRICT_DNS happy-path (`strict_dns_cluster_resolves_localhost_at_build_time` — uses `localhost` per SPEC §6 signpost 10's rationale, asserts both `picked.port() == 7000` (port preserved through resolver) and `picked.ip().is_loopback()` (resolves to IPv4 or IPv6 loopback)); the NXDOMAIN-equivalent failure (`strict_dns_cluster_returns_dns_resolution_failed_on_nxdomain` — uses `this-host-does-not-exist.invalid` per RFC 6761 §6.4 reservation; asserts the error variant + the diagnostic fields). The I3-closing test is structurally meaningful only because Task 1 added the second `ClusterType` variant — it would have been vacuous prior to 05.1.

- **ADR-0023 is well-shaped and self-consistent.** `docs/envoy-rust/DECISIONS.md:424-433`. Date / Status / Context / Options-considered (5 alternatives weighed: (i) StrictDns-only chosen; (ii) StrictDns + LogicalDns rejected on D-3.6 minimalism; (iii) StrictDns + dns_refresh_rate rejected on minimalism; (iv) literal-IP fixture replacement rejected on cross-platform brittleness; (v) trust-dns-resolver / hickory-resolver rejected on D-3.2 permitted-foundations grounds) / Decision / Rationale / Consequences (9 enumerated bullets including the I3 close-out, the C-1 close-out, the M-claim deferral, and the future-knob deferrals) / Provenance (with a substantive "Implementation refinement" footer documenting the SPEC §3 D1 → ADR-0023 placement drift). The provenance footer is textbook D-3.5 ambiguity-resolution discipline: it names the SPEC pseudocode inconsistency (the SPEC §3 D2 pseudocode mixed `ConfigError::*` and `ClusterError::*` variants in the same `?` chain, which is mechanically inconsistent), names the chosen resolution (variant lands on `ClusterError`), and names the rationale (envoy-cluster's typed-error chain stays unchanged per signpost 14; the alternative cross-crate wrapper-variant approach would be mechanically larger; the diagnostic shape is identical regardless of placement).

- **The new fuzz seed `strict_dns_cluster.yaml` is correctly registered in the in-tree walk-list.** `crates/envoy-config/src/bootstrap.rs` (`fuzz_corpus_seeds_parse_or_reject_cleanly` slice). The Task-1 review fix `7391a4e` caught the planner-time omission — without the walk-list extension, the in-tree `cargo test --package envoy-config` gate would have silently skipped the new seed (the test enumerates seeds via an explicit `&[..]` slice, not `read_dir`, per phase-04.2 / phase-04.3 precedent). The fix landed end-of-slice positioning in the feature-grouped order (tcp_proxy → tls → hcm → route → cluster) matching the existing seed-naming convention. Verified the seed itself parses cleanly: 35 lines, full bootstrap with one tcp_proxy listener + one STRICT_DNS cluster + one `localhost:7000` endpoint, tagged with a `node:` block per the existing seed-shape precedent. The seed's `localhost` choice (not `host.docker.internal`) is correct per SPEC §3 D1 — the seed is for the parse pipeline, not the resolution pipeline; if the seed ever drives a downstream resolution path, `localhost` is universally resolvable.

- **The 5-fixture coordinated YAML edit is mechanically uniform.** Verified all 10 lines flip from `type: STATIC` to `type: STRICT_DNS` cleanly, with no whitespace re-indent (the replacement string is the same indent level as the original), no extra knobs added (no `dns_lookup_family` / `respect_dns_ttl` per SPEC §6 signpost 6's "rely on AUTO default" recommended posture), and no unrelated edits bundled into Task 3's commit (`git show 0ce0aa2 --stat` confirms exactly 10 files changed, +5 / -5 net per file). The 3 unaffected fixtures (0001 / 0002 / 0007) are untouched, as projected — `grep -rn "type: STATIC\|host.docker.internal" tests/fixtures/0001*/ tests/fixtures/0002*/ tests/fixtures/0007*/` returns empty. The single-bundled-commit cadence (vs the alternative per-fixture cadence) was correctly chosen per SPEC §6 signpost 8 — the 10 edits are mechanically identical and the differential property is "all 5 fixtures green simultaneously"; splitting would muddy the gate signal.

- **PROGRESS.md cadence is consistent.** Every task has a section; the Task-1 review-fix is preserved as a sub-section rather than overwriting; state-4 section quotes verification outputs verbatim (per Task 4's lines 102-157); the red-CI deviation is named directly without euphemism; the aborted in-session expansion is disclosed (line 159 paragraph 6) including the preserved-on-backup-branch citation. The "Deviations from PLAN" subsection of each task carries either "None" (Task 3) or a typed list of named signposts (Task 1's Signpost A; Task 2's Signpost A + Signpost B + Signpost E NOT triggered; Task 4's PLAN-projection-vs-reality drift).

- **Commit-message hygiene matches the established precedent.** Task commits follow `phase 05.1: <feature> [+ <ADR-NNNN>]` for the substantive landings (`bfabcb6` carries `ClusterType::StrictDns + ADR-0023 + 6 validator tests + fuzz seed`; `f7a555d` carries `tokio dep + async from_bootstrap + STRICT_DNS branch + I3 close`); review-fix commits use `phase 05.1: <crate> — task N review fix (<summary>)` (e.g., `7391a4e`); progress-note commits are `phase 05.1: progress note (task N [— <kicker>])`. The Task-4 verification commit `b7fe910`'s message is unusually frank about the red CI outcome (16-line body explicitly stating "§7.5 NOT met at this commit. Phase-04.3 REVIEW C-1 NOT materially closed; closure deferred to a follow-up sub-phase under proper SPEC + ADR discipline") — this is exactly the commit-message hygiene the project's audit-trail discipline rewards.

---

## 3. Issues

### Critical

None.

### Important

**I1. State-4 phase-done gate is RED on Docker-gated fixture 0008; SPEC §3 D4 + D5's "Closes phase-04.3 REVIEW C-1" is unmet at this commit.** *(Procedural / well-disclosed; closure deferred to a follow-up sub-phase.)*

PROGRESS.md Task 4 lines 138–149 records the per-fixture matrix from CI run `25258722850`:

```
tests/differential/tests/admin_ready.rs            GREEN    (fixture 0002)
tests/differential/tests/echo.rs                   GREEN    (fixture 0001)
tests/differential/tests/http1_direct_response.rs  GREEN    (fixture 0007)
tests/differential/tests/http1_router_upstream.rs  RED      (fixture 0008 — response status mismatch under
                                                              `response_status: exact`: upstream 503, subject 200)
tests/differential/tests/tcp_proxy.rs              NOT RUN  (fixture 0003)
tests/differential/tests/tls_downstream.rs         NOT RUN  (fixture 0004)
tests/differential/tests/tls_sni.rs                NOT RUN  (fixture 0006)
tests/differential/tests/tls_upstream.rs           NOT RUN  (fixture 0005)
```

SPEC §3 D4 (acceptance signal (a)) projected "the 5 Docker-gated differential fixtures `tests/fixtures/0003-tcp-proxy/`, `tests/fixtures/0004-tls-downstream/`, `tests/fixtures/0005-tls-upstream/`, `tests/fixtures/0006-tls-sni/`, and `tests/fixtures/0008-http1-router-upstream/` are green at the Docker-gated CI level, with the CI run URL + the 5 individual test results quoted inline in `PROGRESS.md`." Reality: 0008 is red (cluster's status-mismatch is no longer a `host.docker.internal`-malformed-IP startup failure — that's the C-1 trace, which IS fixed by the schema + runtime + YAML preamble — but a different defect surfaces at the upstream-routing path and produces a 503 from the upstream Envoy container vs a 200 from the envoy-rust subject). 0003/0004/0005/0006 are NOT RUN because `cargo test` exits at the first failing test binary (alphabetic order picks `http1_router_upstream` before `tcp_proxy`). SPEC §3 D5 piggybacked on D4: "**This deliverable also materially closes phase-04.3 REVIEW C-1.** The C-1 carryforward chain ends at 05.1's state-4 phase-done verification: a green CI run on the 5 affected fixtures is the substantive close-out." That close-out is unmet.

**My verdict choice on this finding: (b) "Approved with M-track follow-ups" — the deferral of C-1 closure to a follow-up sub-phase is appropriate.** Rationale:

1. **The deferral is well-disclosed.** PROGRESS.md Task 4 paragraph 6 names the deviation directly: "PLAN.md projected this commit would materially close phase-04.3 REVIEW C-1 at a green CI run; reality is a red CI run." The b7fe910 commit message says the same thing in 16 lines of body text. The handoff is materialized in git history; a future reviewer of the 05.1-followup sub-phase will not be misled.

2. **The 0008 red is NOT the C-1 trace; it's a different defect that the C-1-fix exposed.** The C-1 trace is "Envoy v1.33 rejects `host.docker.internal` under `type: STATIC` with a `malformed IP address` startup error". The 05.1 schema + runtime + YAML preamble fixed THAT trace — both proxies now start cleanly with the STRICT_DNS-flipped fixtures. The new 0008 red is "fixture 0008's upstream Envoy returns 503 while envoy-rust subject returns 200" under `response_status: exact`. This is a different defect — likely in the upstream-routing path through `host.docker.internal` to the host-process http1-echo-server, possibly an http1-echo-server reachability issue or an upstream-Envoy upstream-cluster-mgmt issue at the host-gateway boundary. Diagnosing it requires a fresh SPEC + ADR brainstorm (per BOOTSTRAP_PROMPT.md §5.1's one-state-per-session discipline + D-3.5 ambiguity-resolution); folding the diagnosis into 05.1's fixture-hardening preamble would inflate this phase's scope past its SPEC contract (which explicitly limits 05.1 to schema growth + runtime growth + YAML edits).

3. **The aborted in-session expansion correctly preserved discipline.** The prior 6-patch attempt (commits `9279895` / `2d3d679` / `339b3c7`, since reset and force-pushed away to `backup/task4-scope-creep-2026-05-02`) introduced inline root-cause patches without a SPEC anchor or an ADR landing — that's exactly the doctrine-violation D-3.5 + D-3.6 are designed to prevent. The reset-and-rebuild discipline preserves the SPEC §7 "no new ADRs at this task" invariant + the PLAN's "0 LoC of code changes" Task 4 contract; the work is preserved on a backup branch for future SPEC-anchored adoption. This is the doctrinally correct disposition, not a regression.

4. **Local stable-toolchain gate is green.** All unit-test surface (envoy-config 145 + envoy-cluster 14 + the rest of the workspace continuing to pass per `cargo test --workspace --exclude differential` per PROGRESS Task 2 line 56) is clean. The Docker-gated suite is gated on the differential test binary infrastructure; the local stable-toolchain gate's RED is purely the Docker-on-macOS networking issue on fixture 0008.

5. **Phase 05.1's SPEC acceptance signal §1(a)-(f) is partially achievable without the full close-out:** (a) is partially red (0008 red; 0003/0004/0005/0006 NOT RUN; 0001/0002/0007 GREEN); (b) is GREEN (the 3 unaffected fixtures continue to pass per the CI matrix); (c) is N/A (no conformance suites run in 05.1; h2spec attaches in 05.2); (d) is GREEN (the fuzz short-budget run is clean: 769302 runs in 31 seconds); (e) is GREEN locally (all 5 stable-toolchain commands pass); (f) is what this REVIEW is producing.

6. **Doctrine fit.** PLAN.md "Signpost A" + "If a fixture remains red → re-enter state 3" guidance contemplated this exact scenario — the planner anticipated the possibility that the schema + runtime + YAML preamble might be necessary but not sufficient. The recommended posture in PLAN.md was either (i) re-enter state 3 if the fix is in-scope and small, or (ii) defer to a follow-up sub-phase if the diagnosis surface is non-trivial. The executor chose (ii); given the ~6-patch surface that the aborted attempt produced, (ii) is the right call.

The recommended forward work is a dedicated `05.1.1-fixture-hardening-followup` (or similar slug) sub-phase whose state-1 brainstorm starts from the captured 0008 status-mismatch trace, the testcontainers logs from CI run `25258722850`, and the 6 root-cause patches preserved on `backup/task4-scope-creep-2026-05-02`. That sub-phase's SPEC + PLAN should explicitly enumerate the diagnosis surface, the per-fix LoC budget, any new ADRs needed (e.g., if the fix touches the upstream-routing path or the http1-echo-server contract), and the verification gate (5-fixture green Docker-gated CI run as the substantive close-out of phase-04.3 REVIEW C-1). See §4 carryforward "C-1" below.

The reviewer notes that an alternative verdict — "Approved with fixes" with state-3 loop-back inside 05.1 — is defensible if the reviewer prefers minimum-cross-phase-bleed. I rejected this option for the reasons above: the executor already attempted the inline-fix posture (the aborted 6-patch expansion) and discarded it for cause; reverting to that posture under reviewer pressure would re-trigger the same scope-creep without the SPEC-anchor that would discipline it.

### Minor

None of the minor findings rise to the level requiring a fix at 05.1 close. They are all awareness-only carryforwards or process notes; see §4.

---

## 4. Awareness-only / minor findings

**A1. Forced cross-crate test-helper churn in envoy-tcp + envoy-http1 was not enumerated in PLAN.md but is mechanically minimal and behaviour-neutral.** *(Awareness-only; PROGRESS.md Task 2 names this directly.)*

PLAN.md's Task 2 step-list enumerated only the 5 callers of `from_bootstrap` in `crates/envoy-cluster/src/cluster.rs::tests` and the 1 call site in `crates/envoy-bin/src/main.rs:83`. The workspace also contains 3 additional test-helper callers that PLAN.md did not list:

- `crates/envoy-tcp/src/lib.rs::tests::mk_handle` — promoted from `fn → async fn` with 11 caller `.await` updates.
- `crates/envoy-http1/src/hcm.rs::tests::cluster_mgr_with_endpoint` — promoted from `fn → async fn` with 5 caller `.await` updates.
- `crates/envoy-http1/src/hcm.rs::tests::cluster_mgr_empty` + the helpers `hcm_config_single_route` and `build_test_config` that call it — all promoted to `async fn` with cascading `.await` updates (~13 sites total across the file).

Without these the workspace would not compile (E0728 — `await is only allowed inside async functions`). The fix is purely mechanical: `fn → async fn` + append `.await` at each call site; no test-body assertions changed; no values lost in the conversion. PROGRESS Task 2 lines 61–67 disclose this directly. Verified via `git diff f1804a7..a64d9fc -- crates/envoy-tcp/src/lib.rs crates/envoy-http1/src/hcm.rs` — the diffs are 100% mechanical, no behavioural changes.

*Fix:* leave as-is for 05.1 (the PLAN.md omission is acknowledged in PROGRESS.md and the in-source diffs are clean). For future phases, planner-time `grep -rn "from_bootstrap(" crates/` discipline before declaring the call-site enumeration would catch this class of cross-crate sync→async dependency. Not a forward carryforward.

**A2. `tokio` was a NEW direct dep on envoy-cluster, not a feature flip on an existing dep.** *(Awareness-only; SPEC §3 D2's "Cross-crate dependency note" was incorrect at HEAD.)*

SPEC §3 D2's last paragraph reads: "`tokio::net::lookup_host` requires the `net` feature on `tokio`. `crates/envoy-cluster/Cargo.toml` already pulls `tokio = { version = "1", features = ["net", "rt", ...] }` per the 04.x shape." This was incorrect at HEAD `e626862`: envoy-cluster did NOT carry `tokio` as a direct dep at all (verified via `git show e626862:crates/envoy-cluster/Cargo.toml` shows `[dependencies] envoy-config + thiserror`; `tokio` is absent). The PLAN.md signpost B and the executor's Task 2 deviation log both name this directly. The correct posture was to add `tokio` as a NEW direct dep on envoy-cluster (not a feature flip), which the implementation does at `crates/envoy-cluster/Cargo.toml` (`tokio = { version = "1", features = ["net", "rt", "macros"] }` in `[dependencies]` + `tokio = { version = "1", features = ["macros", "rt"] }` in `[dev-dependencies]`). This is NOT a new top-level workspace dep — `tokio` is already pulled by envoy-bin / envoy-listener / envoy-tcp / envoy-http1 / envoy-tls per the existing 02.1+ shape — but it is a new path-level dep on envoy-cluster's `[dependencies]`. The Cargo.lock single-line addition reflects this exactly.

*Fix:* leave as-is for 05.1 (correctly resolved at PLAN-write + Task 2 execution). For future phases, SPEC writeup-time `grep "tokio" crates/<target>/Cargo.toml` discipline before declaring "already pulled" would catch this. Not a forward carryforward.

**A3. The state-4 verification's PROGRESS.md Task 4 phase-04.1 REVIEW M-claim line is a continued deferral, not a closure.** *(Awareness-only; correctly handled.)*

PROGRESS.md Task 4 line 155 reads "Phase-04.1 REVIEW M-claim stays deferred per the 04.3 disposition." This is correct: 05.1 was projected (per 05.1 SPEC §1 "Cross-phase items unblocked but not closed at 05.1") to UNBLOCK the M-claim's masking by removing the Docker-gated regression — but the M-claim itself (the per-function `drive_http1` unit test in `tests/differential/src/lib.rs::tests`) is a separate scope item that 05.1 explicitly does NOT close. Given that 05.1 also did not close C-1 (the masking is in fact only partially removed — the C-1 trace itself is fixed, but a different 0008-specific defect remains), the M-claim's masking is also only partially removed. The carryforward-chain narrative continues exactly as projected: "Phase-04.1 REVIEW M-claim is unblocked but stays deferred per the 04.3 disposition; carryforward chain continues."

*Fix:* leave as-is. Will re-enter the carryforward narrative at the 05.1-followup sub-phase's state-6 STATE.md edit.

**A4. The in-tree `static_cluster_constructs_with_literal_ip` test (D5's I3-closing test) is structurally meaningful, not vacuous.** *(Awareness-only; structural verification.)*

I verified that the I3-closing test at `crates/envoy-cluster/src/cluster.rs:569` is a real positive guard, not a token assertion. The test (a) parses a STATIC-typed cluster YAML through `parse_bootstrap`; (b) calls `from_bootstrap(&bootstrap).await`, which dispatches into the `match cfg.cluster_type { Static => ..., StrictDns => ... }` arm and exercises the literal-IP path; (c) asserts `mgr.get("backend").unwrap().pick_endpoint().unwrap() == "127.0.0.1:7000".parse().unwrap()`. This is structurally meaningful only because the second `ClusterType` variant exists; pre-05.1, the `match` arm would have been a single-arm match on `ClusterType::Static` and the test would have been vacuous (any literal-IP YAML would have hit the same code path; there was no second arm to fall through to incorrectly). With `StrictDns` now landing alongside, the regression guard genuinely defends against schema/runtime regressions where a future refactor inadvertently routes Static through the StrictDns arm or vice-versa. The doc-comment names this directly.

*Fix:* none. Awareness-only — confirms the I3 close-out is valid.

**A5. `crates/envoy-config/fuzz/Cargo.lock` is generated by `cargo fuzz` and is untracked-not-ignored.** *(Awareness-only; pre-existing concern unrelated to 05.1.)*

`git check-ignore -v crates/envoy-config/fuzz/Cargo.lock` returns "NOT IGNORED"; `git log --all -- crates/envoy-config/fuzz/Cargo.lock` is empty (the file has never been committed). The fuzz directory's `.gitignore` (extended at Task 1 with the new strict_dns_cluster.yaml allow-list entry) lists `corpus/parse_bootstrap/*` + per-seed allow-list entries + `artifacts/` + `target/`, but not `Cargo.lock`. This is pre-existing — not a 05.1 regression — but it surfaces in `git status` as a permanently-untracked file that some workflows might mistakenly stage. Other workspace-stray untracked files (`rust_out`, `stdin` in the repo root) are also pre-existing.

*Fix:* leave as-is for 05.1 (out of scope; pre-existing). A future tidy-up phase (or a one-line root `.gitignore` extension for `crates/*/fuzz/Cargo.lock` and the stray artifact files) would close this; not blocking.

**A6. PROGRESS.md Task 1 line 7's LoC count "Total: ~145 LoC" includes a ~25-line ADR-0023 block; the actual source-code LoC delta in Task 1 is ~120.** *(Awareness-only; minor accuracy nit.)*

The breakdown reads "(15 schema + 80 unit tests + 25 fuzz seed YAML + 1 .gitignore + 25 ADR)". Adding to 146 (probably rounded to 145), but the 25-line ADR block lives in `docs/envoy-rust/DECISIONS.md` not in any source file — the diff shows DECISIONS.md +13 lines (not +25). The discrepancy is small and self-cancelling (the ADR is over-counted; the tests are under-counted) but the precision is imprecise. This is the kind of finding the 04.3 REVIEW would have logged as Minor; here it's awareness-only because the headline ("~145 LoC mostly tests + ADR") is directionally correct.

*Fix:* leave as-is. Not material.

---

## 5. Recommendations

**R1. Brainstorm the 05.1-followup sub-phase against the captured CI run `25258722850` artifacts + the `backup/task4-scope-creep-2026-05-02` patch series.** *(Required before state-6 of 05.1 if downstream phases are blocked on the C-1 closure; otherwise advisable as next-state-1 work.)*

The brainstorm should produce: (a) a SPEC enumerating the diagnosis surface for fixture 0008's `response_status: exact` mismatch (likely an upstream-routing defect at the host-gateway boundary — the 503 from upstream Envoy suggests its upstream cluster construction or the http1-echo-server reachability through host-gateway is failing in a different way than the 02.1-era StaticParse failure); (b) a per-fix LoC budget with explicit ADR projections (the aborted 6-patch series may need 1-3 ADRs to anchor the fixes); (c) the verification gate (5-fixture green Docker-gated CI run as the substantive close-out of phase-04.3 REVIEW C-1). The 6 patches preserved on `backup/task4-scope-creep-2026-05-02` should be reviewed for re-adoption — some may be re-usable as-is; some may need re-architecture under the new SPEC's discipline.

**R2. STATE.md "Carryforwards" section bookkeeping at 05.1 state-6 should list THREE active carryforwards, not two.** *(Procedural; aligns with PROGRESS.md Task 4 paragraph 5.)*

SPEC §6 signpost 17 projected the state-6 STATE.md edit would land two carryforward closures (C-1, I3) and one continued deferral (M-claim). Reality at state-5: I3 closes cleanly (the in-source test lands; the I3 close-out is independent of the C-1 close-out); C-1 stays open (deferred to 05.1-followup); M-claim stays deferred. So the state-6 STATE.md edit should record:

- "Phase-02.1 REVIEW I3 — closed at this commit (`<state-6 SHA>`); the positive `Static` regression guard `static_cluster_constructs_with_literal_ip` lands at Task 2 commit `f7a555d`. Carryforward chain ends here."
- "Phase-04.3 REVIEW C-1 — NOT closed at 05.1 close; the schema + runtime + YAML preamble landed in 05.1 (Tasks 1/2/3) but the Docker-gated CI run remains red on fixture 0008 with a different defect than the original C-1 trace. Closure deferred to a 05.1-followup sub-phase that will brainstorm against the captured CI matrix and the backup branch's patch series. Carryforward chain continues."
- "Phase-04.1 REVIEW M-claim — unblocked posture is partial (the C-1 mask is partially removed; 0008's masking continues). Stays deferred per the 04.3 disposition. Carryforward chain continues."

The next-skill in STATE.md should advance to the 05.1-followup sub-phase's state-1 (brainstorm) — not to 05.2's state-3 — because the ROADMAP-schema invariant "parent flips to `done` only after all sub-phases are `done`" implies that 05.1 closing while leaving C-1 open requires either a renamed 05.1.1 follow-up or an explicit ROADMAP-row deferral. The choice between (a) splitting 05.1 retroactively into 05.1.1 (preamble) + 05.1.2 (follow-up) and (b) treating 05.1 as fully closed at the preamble work and tracking C-1 as a free-standing post-05.1 sub-phase is a state-6 disposition decision; the reviewer flags it for the orchestrator's call.

**R3. The next-phase planner reading PLAN.md should note that the "If a fixture remains red → re-enter state 3" guidance worked correctly here.** *(Doctrine validation; no fix needed.)*

PLAN.md anticipated this scenario (the schema + runtime + YAML preamble might be necessary but not sufficient for the C-1 fix) and explicitly empowered the executor to either (i) re-enter state 3 if the fix is small and in-scope, or (ii) defer to a follow-up sub-phase if the diagnosis surface is non-trivial. The executor chose (ii) — and the choice was validated by the aborted 6-patch attempt that surfaced the diagnosis-surface scale. This is a successful application of the project's own forward-track-vs-loop-back decision discipline; future phases should preserve this pattern.

---

## 6. Reviewer notes / scope of this review

- **Review base/head:** base `f1804a7` (parent of 05.1's state-2 PLAN.md commit `f23d08f`); head `a64d9fc` (Task 4 progress-note backfill — last 05.1 commit before state 6). 11 commits in range; `git log --oneline f1804a7..a64d9fc` enumerates each.

- **Files inspected** (key surface):
  - `docs/envoy-rust/phases/05.1-fixture-hardening/SPEC.md` (read in full — 5-deliverable SPEC, ~600 lines).
  - `docs/envoy-rust/phases/05.1-fixture-hardening/PROGRESS.md` (read in full — 4-task narrative, 159 lines).
  - `docs/envoy-rust/phases/05.1-fixture-hardening/PLAN.md` (relevant sections: Signpost A, Signpost B, Signpost E inspected via grep + targeted reads).
  - `docs/envoy-rust/DECISIONS.md` (ADR-0023 block at lines 424–435 read in full).
  - `crates/envoy-config/src/bootstrap.rs` (ClusterType at 60–72 + 6 new tests at 4287–4528 + corpus walk-list).
  - `crates/envoy-cluster/src/cluster.rs` (full file — STRICT_DNS branch around 170–200 + ClusterError variant at 116–121 + 3 new tests at 569–694).
  - `crates/envoy-cluster/Cargo.toml` (tokio dep).
  - `crates/envoy-bin/src/main.rs` (single `.await` at line 83).
  - `crates/envoy-tcp/src/lib.rs` (mk_handle async promotion + 11 call-site updates).
  - `crates/envoy-http1/src/hcm.rs` (4 helper async promotions + ~13 call-site updates).
  - `crates/envoy-config/fuzz/corpus/parse_bootstrap/strict_dns_cluster.yaml` (full 35-line seed).
  - `tests/fixtures/{0003,0004,0005,0006,0008}/{envoy.yaml,envoy-rust.yaml}` (10-file YAML flip; spot-checked via grep).

- **Verifications run locally (HEAD `a64d9fc`, 2026-05-02):**
  - `cargo build --workspace --all-targets` → exit 0; `Finished `dev` profile [unoptimized + debuginfo] target(s)`.
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings` → exit 0; no warnings.
  - `cargo fmt --all -- --check` → exit 0; no output.
  - `cargo test --package envoy-cluster --lib` → 14 passed / 0 failed / 0 ignored.
  - `cargo test --package envoy-config --lib` → 145 passed / 0 failed / 0 ignored.
  - `git diff f1804a7..a64d9fc -- crates/envoy-config/fuzz/corpus/parse_bootstrap/strict_dns_cluster.yaml` → +35 lines (new seed).
  - `git diff f1804a7..a64d9fc -- docs/envoy-rust/DECISIONS.md` → +13 lines (ADR-0023 block; ADR-0022 unchanged).
  - `git branch --list 'backup/*'` → `backup/task4-scope-creep-2026-05-02` exists (preserves the aborted 6-patch series).
  - `grep -rn "type: STATIC\|type: STRICT_DNS"` across all 10 affected fixture YAMLs → 10 STRICT_DNS hits, 0 STATIC hits in the 5 affected fixtures.
  - `git check-ignore -v crates/envoy-config/fuzz/Cargo.lock` → NOT IGNORED (pre-existing concern; flagged as A5).

- **Workspace-test surface NOT re-run locally:** `cargo test --workspace` (the project documents the deterministic 0008 red on macOS Docker; PROGRESS Task 4 + Task 2 confirm the workspace test count is otherwise unchanged from 04.3 close + 9 new tests in 05.1). I rely on PROGRESS.md's verification tail and the CI run `25258722850` matrix for the differential gate.

- **CI run `25258722850` artifacts NOT inspected directly:** the link is provided in PROGRESS.md Task 4 line 136. I rely on the executor's per-fixture summary (lines 138–149) for the CI matrix; the 503-vs-200 mismatch detail is taken from PROGRESS.md verbatim. A future state-1 brainstorm of the 05.1-followup sub-phase should pull the testcontainers logs from this CI run to anchor the diagnosis.

- **What is OUT of scope for this review:** the actual 0008 defect's root cause (deferred to 05.1-followup); the 6 root-cause patches preserved on `backup/task4-scope-creep-2026-05-02` (not graded against any SPEC); the 05.2 / 05.3 sibling SPECs (untouched in 05.1; will be brainstormed/planned in their own execution windows); BEHAVIOR_CONTRACT.md (untouched in 05.1 per SPEC §2); the parent-04 / parent-05 / earlier-phase artifacts (closed before this phase).

- **Verdict basis:** the work landed against a tight SPEC, with one well-disclosed planner-time refinement (Signpost A) that ADR-0023 documents in its "Implementation refinement" provenance footer; the work also surfaced one substantive deviation (the state-4 gate is RED, not GREEN as projected) that PROGRESS.md Task 4 + the b7fe910 commit message disclose directly without euphemism. The deferral of C-1 closure to a follow-up sub-phase is doctrinally correct given the diagnosis-surface scale (the aborted 6-patch attempt validates this) and is appropriately tracked. The schema + runtime + YAML preamble work that DOES land is high-quality, doctrine-conformant, and exercises the full parse + runtime + I3-closing surface SPEC §3 D1–D2 + D5 enumerated. **Verdict: Approved with M-track follow-ups.**

---

**Verdict: Approved with M-track follow-ups.**

State-5 complete. The 05.1 state-6 phase-done commit may flip ROADMAP row `05.1` `in-progress` → `done` with the understanding that:

1. SPEC §3 D4's Docker-gated 5-fixture green re-baseline is unmet at this commit.
2. SPEC §3 D5's "Closes phase-04.3 REVIEW C-1" is **NOT** discharged here; the C-1 carryforward chain continues to a 05.1-followup sub-phase.
3. SPEC §3 D5's "Closes phase-02.1 REVIEW I3" **IS** discharged here at Task 2 commit `f7a555d` via the in-source `static_cluster_constructs_with_literal_ip` test.
4. The aborted in-session 6-patch root-cause expansion is preserved on `backup/task4-scope-creep-2026-05-02` for future SPEC-anchored re-adoption.
5. STATE.md's "Carryforwards" section at the state-6 commit should reflect the three carryforwards listed in §5 R2.
6. The next-skill (per STATE.md) is the orchestrator's choice between (a) entering 05.2's PLAN-writing state if 05.1 is treated as fully closed at the preamble landing, or (b) brainstorming the 05.1-followup sub-phase first if 05.2 should not start until C-1 closes.
