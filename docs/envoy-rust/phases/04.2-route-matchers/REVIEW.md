# Phase 04.2 REVIEW — HTTP route header matcher fan-out + ADR-0021 (regex permitted)

- **Base:** `c5c40ec` (phase 04.1 close-out — `phase 04.1: HTTP/1.1 codec + HCM scaffold + direct_response + fixture 0007 [ADR-0020]`)
- **Head:** `e00f638` (state-4 phase-done gate verification — `phase 04.2: state-4 phase-done gate verification (task 12)`)
- **Files:** 18 changed (+5792 / -44). Net code-only delta is closer to ~2000 LoC (matches SPEC §5's ~1300-LoC point estimate plus the +560-LoC matcher-runtime sibling module that landed at Task 6 — see SPEC §6 signpost 19's "either inline or sibling is fine"; the executor took the sibling path); the headline figure is inflated by the 3562-line `PLAN.md` and 196-line `PROGRESS.md`. Net Rust delta lands across `crates/envoy-config/src/bootstrap.rs` (+1085 net: 5 new public types `HeaderMatcher` / `HeaderMatcherMode` / `SafeRegex` / `Int64Range` / `StringMatcher` / `StringMatcherMode`, additive `headers: Vec<HeaderMatcher>` field + `Clone` derive on `RouteMatch`, three hand-rolled `Deserialize` impls — `SafeRegex` ~50 LoC visitor, `StringMatcher` ~110 LoC visitor with `MODE_KEYS` + `ALL_KEYS` constants, `HeaderMatcher` ~130 LoC visitor with `set_mode` helper closure — `validate_header_matcher` + `compile_safe_regex` helpers, `validate` + `validate_hcm` borrow promotion to `&mut`, plus 26 new validator tests); `crates/envoy-config/src/lib.rs` (+47 / -3 net: 5 new `ConfigError` variants — `EmptyHeaderName`, `InvalidRegex { regex, source }`, `InvalidInt64Range { start, end }`, `UnknownHeaderMatcherMode { got }`, `UnknownStringMatcherMode { got }` — plus 6 new public re-exports and the `parse_bootstrap` `&mut` migration); `crates/envoy-config/src/matcher.rs` (NEW; +356 LoC = `HeaderMatcher::matches` + `StringMatcher::matches` inherent methods + 28 unit tests covering all 7 HeaderMatcher modes, all 5 StringMatcher modes, `invert_match` XOR cross-cuts, header-name case-insensitivity, header-value case-sensitivity by default, and the Task 6 review-fix's two `string_match_{prefix,suffix}_with_ignore_case_matches` regression tests); `crates/envoy-http1/src/hcm.rs` (+230 / -3 net: `route_matches` signature extension `(r, path) → (r, path, headers)` with the AND-combination clause, `clone_route_config` `headers: r.r#match.headers.clone()` line, 5 `RouteMatch` literal-site `headers: vec![]` patches across the test module, `build_test_config` test helper, and 5 new `tokio::test` integration tests — `route_with_no_headers_matches_unchanged`, `single_header_matcher_route_selected_when_match`, `single_header_matcher_route_skipped_when_no_match`, `multi_header_matcher_and_combination_all_match`, `multi_header_matcher_and_combination_one_fails`); `tests/differential/src/lib.rs` (+216 / -8 net: `Driver::Http1ProbeList { probes }` variant + new `Http1Probe` struct + `drive_http1` `extra_headers: &[(String, String)]` parameter + `Driver::Http1ProbeList` dispatch arm in `run_fixture` with subject.shutdown + drop(upstream) deliberately moved AFTER the probe loop + 2 new harness unit tests `parses_expectations_with_http1_probe_list` and `http1_probe_extra_headers_default_empty`). Fixture 0007 amendment: 4-file edit (`envoy.yaml` + `envoy-rust.yaml` add a head-of-Vec matcher route; `expectations.yaml` restructure from single `Driver::Http1` to two-probe `Driver::Http1ProbeList`; `README.md` 04.2 amendment paragraph + ADR-0021 reference) plus the new empty `inputs/payload-matcher.bin` placeholder. New 48-LoC fuzz seed `crates/envoy-config/fuzz/corpus/parse_bootstrap/route_with_header_matchers.yaml` exercising 5 of 7 HeaderMatcher modes simultaneously plus its `.gitignore` allow-list entry. `Cargo.toml` of envoy-config gains `regex = "1"` (under ADR-0021); `Cargo.lock` gains the `regex` dep edge in the envoy-config block; `deny.toml` `[licenses] allow` list gains `Unlicense` (needed for transitive `aho-corasick` + `memchr`). DECISIONS.md gains ADR-0021 (13 lines) appended at Task 1.
- **Reviewed:** 2026-04-27
- **Verdict:** **Approved with M-track follow-ups** — state 5 complete. No blocking issues. Two new in-phase findings (M8: `safe_regex_partial_eq` opaque-equality may surprise post-validate consumers; M9: ADR-0021's prose ↔ Cargo.lock cadence internal contradiction) and 7 carryforwards from 04.1 (M1–M7, with M5 partially addressed via Task 1 review-fix's PROGRESS disclosure but ADR text untouched per D-3.5; M6 still not closed — Task 9's tests cover Http1Probe parsing, not `drive_http1` itself). The state-4 gate is green on first attempt and the in-phase review-fix discipline (Tasks 2/3/4/5/6/7 each landed a review-fix commit) closed seven well-targeted findings before they propagated.

---

## 1. Summary

Phase 04.2 lands the HTTP route header-matcher fan-out: all 7 of Envoy's `HeaderMatcher` modes (`exact_match`, `prefix_match`, `suffix_match`, `safe_regex_match`, `range_match`, `present_match`, `string_match`) plus the 5-variant `StringMatcher` tagged union plus `invert_match: bool` plus `RouteMatch.headers: Vec<HeaderMatcher>`, with the `regex = "1"` runtime dep narrowly permitted under ADR-0021 at Task 1. The implementation reads as a careful continuation of the 04.1 posture rather than a re-architecture: every doctrinal axis, every public-surface boundary, and every harness invariant from earlier phases is preserved unchanged. The new schema lands in `crates/envoy-config/src/bootstrap.rs:333-748` with three hand-rolled `Deserialize` visitors (`SafeRegex` at lines 369-417 — single-key `regex: String` reader that establishes the visitor template; `StringMatcher` at lines 478-565 — field-name oneof discrimination over five mode keys plus the `ignore_case` peer; `HeaderMatcher` at lines 615-748 — field-name oneof discrimination over seven mode keys plus `name` plus `invert_match` peer with an inline `set_mode` closure for multi-mode-key collision detection). The validator extension lands at `crates/envoy-config/src/bootstrap.rs:1105-1153` (`validate_header_matcher` + `compile_safe_regex`); the runtime lands in the new sibling module `crates/envoy-config/src/matcher.rs` (sibling per SPEC §6 signpost 19's "either inline or sibling is fine"); the route-walker AND-combination lands in `crates/envoy-http1/src/hcm.rs:268-279` (a 4-line addition extending `route_matches` from path-only to path-AND-headers with short-circuit on first non-match per Envoy default `headers_match_options: ALL`).

The 5 new `ConfigError` variants in `crates/envoy-config/src/lib.rs:130-167` (`EmptyHeaderName`, `InvalidRegex { regex, source: regex::Error }`, `InvalidInt64Range { start, end }`, `UnknownHeaderMatcherMode { got }`, `UnknownStringMatcherMode { got }`) cover the validator's four mechanical rejection paths plus the two parser-side discriminator-key failures. (PROGRESS Task 1 line 6 says "4 ConfigError variants"; Task 3's commit `90dcb47` actually added the 5th — `UnknownStringMatcherMode` — alongside `StringMatcher`. PROGRESS Task 3 line 30 names the addition correctly. Net delta: 5 variants. Public API is `&'static str` for the discriminator-key payload via the `got: String` field name on the two `Unknown*Mode` variants — chosen for diagnostic clarity over the alternative `&'static str` of the validator's existing `UnknownTransportSocketName(String)` precedent.)

The runtime in `crates/envoy-config/src/matcher.rs` is the heart of the phase. `HeaderMatcher::matches` at `matcher.rs:19-50` does a single case-insensitive header-name lookup via `headers.iter().find(|(n, _)| n.eq_ignore_ascii_case(&self.name))` (HTTP/1.1 §3.2 compliant), dispatches by mode, XORs the result with `invert_match`. Per-mode arms: `ExactMatch` is `==` on `Option<&str>`; `PrefixMatch` / `SuffixMatch` use `is_some_and` + `starts_with` / `ends_with` (case-sensitive); `SafeRegexMatch` takes `sr.compiled.as_ref().expect("validator ensured HeaderMatcher SafeRegex compiled")` (the Task 6 review-fix's panic-message disambiguation — distinguishes from the StringMatcher SafeRegex site so a future regression panic localizes correctly without symbol info); `RangeMatch` parses the value as `i64` decimal then half-open `n >= start && n < end` (non-parseable returns `false` per SPEC §6 signpost 6 — match-fail not error); `PresentMatch(true)` checks `value.is_some()`; `PresentMatch(false)` returns `true` unconditionally (the subtle "no presence requirement" semantics per SPEC §6 signpost 7); `StringMatch` delegates to `StringMatcher::matches`. `StringMatcher::matches` at `matcher.rs:58-101` correctly applies `ignore_case` to Exact/Prefix/Suffix/Contains (case-folded comparison via `eq_ignore_ascii_case` + `to_ascii_lowercase`) and deliberately ignores it on `SafeRegex` (per Envoy proto: regex callers express case insensitivity via `(?i)`). The Task 6 review-fix's `str::get(..)` panic-safety fix on the Prefix/Suffix `ignore_case: true` branches (`matcher.rs:69-74` and `matcher.rs:77-83`) is the right defensive shape — bare `&value[..lit.len()]` would panic on multi-byte UTF-8 input where the literal length falls mid-codepoint; the `get(..)` form returns `None` instead and `is_some_and` short-circuits to `false`.

The HCM route walker integration at `crates/envoy-http1/src/hcm.rs:268-279` is a tight 12-line function. The path-side oneof match runs first; on path-mismatch it short-circuits to `false`; otherwise `r.r#match.headers.iter().all(|m| m.matches(headers))` AND-combines. Empty `headers` Vec is a no-op (Vec::iter::all returns true on empty) — the `route_with_no_headers_matches_unchanged` regression baseline at `hcm.rs:625-643` pins this. The 5 HCM integration tests at `hcm.rs:625-816` cover: (1) empty-headers regression baseline, (2) single-matcher selected on match, (3) single-matcher skipped on mismatch (falls through to second route), (4) two-matcher AND all-match selected, (5) two-matcher AND one-fails skipped. The Task 7 review-fix appended `Connection: close\r\n` to all 5 test request literals — without it the server-side `serve_connection` loop kept the connection open for the full 5s idle timeout before the test's `read_to_end` returned, costing ~5s per test (hcm.rs::tests::drive helper is a single-shot client). PROGRESS Task 7 review-fix line 99 documents the issue and the alignment with the 04.1 cadence.

The differential harness extensions in `tests/differential/src/lib.rs` introduce `Driver::Http1ProbeList { probes: Vec<Http1Probe> }` (lines 76-83), the `Http1Probe` struct (lines 155-180), and the `drive_http1` extra-headers parameter (line 697). The dispatch arm at `lib.rs:1171-1278` iterates probes, each running through the same 5-axis equivalence cascade as the single-probe `Driver::Http1` arm (envoy ↔ envoy-rust status equality; per-probe `expected_status`; envoy ↔ envoy-rust body equality; per-probe `expected_body: Http1BodyRule::ByteExact`; per-probe `expected_headers: Http1HeaderRule::SetEqualModuloAllowList`). The teardown ordering is correct: `subject.shutdown(Duration::from_secs(5)).await.ok()` and `drop(upstream)` happen AFTER the probe loop completes (`lib.rs:1276-1277`), not BEFORE each probe — matching the Driver::TlsTcpProbeList shape from 03.2 (which the 04.2 implementation explicitly mirrors per PROGRESS Task 9 line 112). This intentionally diverges from the singleton `Driver::Http1` arm (lines 1093-1094) which tears down before equivalence checks. Both shapes are correct; the divergence is required because `Driver::Http1ProbeList` reuses the same upstream + subject across probes whereas `Driver::Http1` is single-shot.

Fixture 0007's amendment (`tests/fixtures/0007-http1-direct-response/{envoy.yaml,envoy-rust.yaml}` lines 19-30 / 22-31 in the diff) places the matcher route at the head of `routes:` (so first-match-wins reaches it before the catch-all) with `match: { prefix: "/api/", headers: [{ name: "x-foo", exact_match: "bar" }] }` returning `direct_response: { status: 418, body: { inline_string: "teapot\n" } }`. The 04.1-landed catch-all `prefix: "/"` route stays second. Both YAMLs use Envoy proto field names verbatim (`exact_match`, `headers`, `prefix`, `inline_string`); both proxies deserialize the same wire shape — that's the differential property 04.2 newly exercises. `expectations.yaml` is restructured from single-probe to two-probe `Driver::Http1ProbeList` shape (default-route 200/"ok\n" + matcher-route 418/"teapot\n"); the matcher-route probe carries `extra_headers: [["X-Foo", "bar"]]`. The README amendment paragraph (README.md lines 47-72) names the property + lists ADR-0021 in the ADR references list.

The work reads cleanly against doctrine on every axis I checked. **D-3.1 TDD discipline**: each implementation task in PROGRESS.md narrates tests landing alongside the impl. Per-task tally: Task 2 +4 (Int64Range/SafeRegex parse-shape); Task 3 +5 (StringMatcher parse-shape + multi-mode-key); Task 4 +6+1 review-fix (HeaderMatcher parse-shape + multi-mode-key); Task 5 +10 (validator regex compile + Int64Range bounds + 7-mode acceptance); Task 6 +28+2 review-fix (matcher runtime); Task 7 +5 (HCM integration); Task 9 +2 (Http1Probe parsing). Total +63 tests delta vs phase 04.1 close (envoy-config 75→131 = +56; envoy-http1 19→24 = +5; differential lib 47→49 = +2). The envoy-bin (19) and other crate test counts are unchanged from 04.1. **D-3.2 permitted foundations**: exactly one new crate to the allow-list (`regex = "1"`, narrowly scoped via ADR-0021). All transitive surface (`regex-syntax`, `aho-corasick`, `memchr`) flows through the allow-list with `Unlicense` added to `deny.toml` to cover the latter two's MIT/Unlicense dual-license. ADR-0021 explicitly excludes general-purpose use; future filter-framework regex needs require a scope-extension ADR. **D-3.4 PROGRESS cold-readability**: PROGRESS.md is dense (196 lines) but every task entry carries Commit / Change / Verification / Deviation sections; deviations are itemized and named (Task 1 names the inline-Cargo.lock disclosure; Task 3 names the `clippy::bool_assert_comparison` fix; Task 4 names the 5-RouteMatch-literal-site cascade; Task 5 names the borrow-checker structural change in TCP_PROXY arm; Task 6 names the `cargo fmt` reformats; Task 7 names rustfmt's method-chain expansion; Task 9 names the multi-line variant reformat). A stranger could replay the phase from PROGRESS alone. **D-3.5 append-only ADRs**: `git log --oneline c5c40ec..e00f638 -- docs/envoy-rust/DECISIONS.md` reports exactly one commit (`984aedd` — Task 1 ADR-0021). ADR text is byte-identical from landing through HEAD (the prose carries `dedicated commit at the 04.2 state-4 phase-done gate per established phase precedent` despite the actual cadence diverging — see M9 below). **D-3.6 green-build first-attempt**: PROGRESS Task 12 reports the state-4 phase-done gate clean on first attempt; locally re-verified at HEAD `e00f638` (see "State-4 gate evidence" below) all five gate commands exit 0 with no fix-during-gate commits. **D-3.7 Envoy pin**: `git diff c5c40ec..e00f638 -- docs/envoy-rust/ENVOY_TARGET.md` is empty. **D-3.8 unsafe-code forbid**: `crates/envoy-config/src/lib.rs:1` and `crates/envoy-http1/src/lib.rs:1` both carry `#![forbid(unsafe_code)]`. The new `crates/envoy-config/src/matcher.rs` module declares `pub mod matcher;` in `lib.rs:8` and inherits the crate-root forbid (Rust's `#![forbid]` propagates to submodules). No new crates were added, so no fresh crate-root attribute landings to verify. **D-3.9 toolchain pin**: `git diff c5c40ec..e00f638 -- rust-toolchain.toml` is empty.

SPEC §3 deliverables D1–D5 all land in the described shape, with several well-disclosed plan-time deviations. **D1** (envoy-config schema additions): all 6 new types lands in bootstrap.rs (`HeaderMatcher` / `HeaderMatcherMode` / `SafeRegex` / `Int64Range` / `StringMatcher` / `StringMatcherMode`); the additive `headers: Vec<HeaderMatcher>` field is on RouteMatch with `#[serde(default)]` + the `Clone` derive needed for matcher-runtime test ergonomics; 4 of the 5 new ConfigError variants are wired via SPEC §3 D1's enumeration — the 5th `UnknownStringMatcherMode` is an additive symmetry with `UnknownHeaderMatcherMode` (PROGRESS Task 3 line 30 names the addition; well-justified). 26 envoy-config validator/parse tests cover the schema (vs SPEC §3 D1's "~20" estimate; the matcher-runtime tests in `matcher.rs` add 28 more for a total of 54 in envoy-config, beating the SPEC's "~25 + ~20" projection). The 1 new fuzz seed `route_with_header_matchers.yaml` lands at the right path (`crates/envoy-config/fuzz/corpus/parse_bootstrap/`) and exercises 5 of 7 modes simultaneously per SPEC §3 D1. The fuzz-corpus walk test `fuzz_corpus_seeds_parse_or_reject_cleanly` is extended with the new seed (PROGRESS Task 8). **D2** (ADR-0021): lands at Task 1 alongside the `regex = "1"` runtime-dep addition + the `deny.toml` `Unlicense` allow-list extension + the 4 `ConfigError` variant stubs. Provenance footer matches SPEC §7's projection (parent-04 SPEC §7's projected ADR number; ADR-0020 lands at parent-04 state-2 commit `1d9740d`; ADR-0021 lands at this Task 1 commit). **D3** (fixture 0007 amendment): all 5 amended/new files (`envoy.yaml`, `envoy-rust.yaml`, `expectations.yaml`, `README.md`, `inputs/payload-matcher.bin`) are present; route ordering (matcher-first, catch-all-second) preserves the first-match-wins discipline; both YAMLs share the same `routes:` shape with proto field name fidelity. **D4** (04.1 REVIEW M-track carryforward check): Task 11 of PROGRESS.md is dedicated to this audit and lists all 7 of M1–M7 with each item's deferral disposition. M5 specifically gets a partial address: Task 1's review-fix at `def3046` adds the inline-Cargo.lock cadence disclosure to PROGRESS; ADR-0021's prose still carries the contradicted "dedicated commit at the 04.2 state-4 phase-done gate" wording per D-3.5 (ADR text is append-only) — the contradiction is well-disclosed but not erased (see M9 below for the right resolution). M6 is explicitly NOT closed — Task 9 added 2 tests for `Http1Probe` parsing + `extra_headers` default but not `drive_http1` itself; PROGRESS Task 11 line 133 carries M6 forward to 04.3. **D5** (CI workflow): no `.github/workflows/ci.yml` change needed; the existing `--workspace` invocation transparently picks up the new envoy-config matcher-runtime tests + the amended fixture; the existing fuzz job picks up the new seed. Verified empty diff on `.github/`.

SPEC §6 signposts are honored. The ones that materially bind: **signpost 1** (hand-rolled `Deserialize` for field-name oneof) — `bootstrap.rs:369-417` (SafeRegex), `bootstrap.rs:478-565` (StringMatcher), `bootstrap.rs:615-748` (HeaderMatcher) all use `MapAccess` visitors with explicit per-key dispatch and unknown-key rejection via `M::Error::unknown_field`; the multi-mode-key collision is detected in two equivalent ways (StringMatcher uses inline `if mode.is_some()` per arm; HeaderMatcher uses an inline `set_mode` closure that returns `Err(E::custom("multiple mode keys (each variant is mutually exclusive)"))`). **signpost 2** (`Arc<regex::Regex>` compiled at config-load): `bootstrap.rs:1135-1146` (`compile_safe_regex` helper). **signpost 3** (AND across multiple matchers in route walker): `hcm.rs:278` (`r.r#match.headers.iter().all(|m| m.matches(headers))`). **signpost 4** (case-insensitive name, case-sensitive value default): `matcher.rs:22` (`n.eq_ignore_ascii_case(&self.name)`); the value match is case-sensitive on `==` / `starts_with` / `ends_with` by default. **signpost 5** (`invert_match` XOR semantics): `matcher.rs:49` (`mode_result ^ self.invert_match`). **signpost 6** (RangeMatch non-parseable = match-fail not error; half-open): `matcher.rs:37-39` (`value.and_then(|v| v.parse::<i64>().ok()).is_some_and(|n| n >= r.start && n < r.end)`). **signpost 7** (`PresentMatch(false)` = no presence requirement): `matcher.rs:40-45` (the `if *want_present { value.is_some() } else { true }` branch). **signpost 8** (Contains only via StringMatch): `bootstrap.rs:533` Contains only appears in StringMatcherMode, never as a top-level HeaderMatcherMode variant; the rustdoc at `bootstrap.rs:411-414` names the Envoy v1.33.0 rationale. **signpost 9** (validator runs at config-load; non-serde `compiled` field): `bootstrap.rs:351-358` (`compiled: Option<Arc<regex::Regex>>` field is absent from the `Deserialize` visitor's field set; the validator fills it via `compile_safe_regex`). **signpost 15** (`StringMatcher.ignore_case` Envoy-compat — affects Exact/Prefix/Suffix/Contains, NOT SafeRegex): `matcher.rs:60-99`; the `string_match_safe_regex_ignore_case_no_effect` test at `matcher.rs:298-308` pins the Envoy-compat behavior. **signpost 17** (`SafeRegex` PartialEq compares only `regex` String): `bootstrap.rs:362-366` — the `safe_regex_partial_eq_compares_only_regex_string` test at `bootstrap.rs:3403-3414` pins the property. **signpost 18** (alternative considered: `#[serde(flatten)]` + `try_from`): rejected per the SPEC; the hand-rolled visitor is the chosen approach and lands cleanly. **signpost 20** (Cargo.lock cadence): the SPEC's projection is "synced as a dedicated commit at the 04.2 state-4 phase-done gate"; the actual landing is inline at Task 1 (PROGRESS Task 1 deviation 1 + Task 12 line 188 disclose this). See M9.

State-4 gate evidence is solid. PROGRESS Task 12 reports gate-clean on first attempt; locally re-verified at HEAD `e00f638`: `cargo build --workspace --all-targets` exits 0 (Finished line only); `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0 (no warnings); `cargo fmt --all -- --check` exits 0 (no output); `cargo test --workspace --lib --bins` reports **275 passed, 0 failed, 1 ignored** across 10 result lines (49 + 19 + 8 + 131 + 24 + 6 + 11 + 15 + 8 + 5 = 276 slots = 275 passes + 1 differential ignored — the pre-existing 02.2 TcpProxyBackend smoke). `cargo deny check` reports `advisories ok, bans ok, licenses ok, sources ok` with the same 5 `license-not-encountered` warnings preserved (`0BSD`, `BSD-2-Clause`, `MPL-2.0`, `Unicode-DFS-2016`, `Zlib` — intentional headroom; not 04.2 regressions). The `Unlicense` license added to deny.toml at Task 1 is now actively claimed (transitive `aho-corasick` + `memchr`) and so does NOT generate a `license-not-encountered` warning — verified at `cargo deny check` output. The Cargo.lock landed inline at Task 1 commit `984aedd` (per PLAN Step 11); no additional Cargo.lock changes accumulate during Tasks 2-11 (PROGRESS Task 12 line 188 confirms this).

The Docker-gated `http1_direct_response_fixture` is skipped locally (no Docker daemon); CI runs it against the amended fixture (now driving both probes per Task 10). The fuzz job is also skipped locally (cargo-fuzz subcommand not installed); CI runs `cargo +nightly fuzz run parse_bootstrap -- -max_total_time=30` against the corpus extended with the new seed (per Task 8).

The executor's self-audit discipline is excellent. PROGRESS narrates 14+ deviations across 12 tasks, every one named and rationalized. Task 1 documents the well-disclosed Cargo.lock-inline cadence. Task 3 documents the `clippy::bool_assert_comparison` driven test rewrite (`assert_eq!(sm.ignore_case, false)` → `assert!(!sm.ignore_case)`). Task 4 documents the 5-RouteMatch-literal-site compile cascade in hcm.rs (PLAN named only `clone_route_config`; reality required patching the test module's 5 sites too — semantically inert `headers: vec![]` patches). Task 5 documents the borrow-checker forced structural change in `validate`'s TCP_PROXY arm: `tp.cluster.clone()` is extracted into `cluster_name` before the `&mut listeners` loop's mutable borrow becomes active, and the cluster lookup uses `bootstrap.static_resources.clusters.iter()` directly. The fix is semantically identical to the pre-04.2 form (PROGRESS Task 5 line 63 names the conflict and the resolution). Task 6 documents the `cargo fmt` reformats. Task 7 documents rustfmt's method-chain expansion of the `route_matches` call site. Task 9 documents the multi-line `Driver::Http1ProbeList` variant reformat. Every deviation is well-justified and PROGRESS-disclosed inline. The seven review-fix commits (Tasks 2, 3, 4, 5, 6, 7 each landed one) are particularly clean — small, single-purpose, with PROGRESS.md follow-up subsections rather than overwrites of the original task entries.

**State-4 gate evidence (re-verified locally at HEAD `e00f638` 2026-04-27):**
- `cargo fmt --all -- --check` → exit 0, no output.
- `cargo build --workspace --all-targets` → exit 0, `Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.10s`.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` → exit 0, `Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.11s`, no warnings.
- `cargo test --workspace --lib --bins` → **275 passed, 0 failed, 1 ignored** across 10 result lines (49/19/8/131/24/6/11/15/8/5; the 1 ignored is differential's pre-existing 02.2 TcpProxyBackend smoke).
- `cargo deny check` → `advisories ok, bans ok, licenses ok, sources ok` (with the same 5 `license-not-encountered` warnings as 04.1 — `0BSD`, `BSD-2-Clause`, `MPL-2.0`, `Unicode-DFS-2016`, `Zlib`; `Unlicense` is actively claimed by the regex transitive surface and so emits no warning).
- `git diff c5c40ec..e00f638 -- docs/envoy-rust/ENVOY_TARGET.md rust-toolchain.toml` → empty (both frozen artifacts unchanged). `git diff c5c40ec..e00f638 -- docs/envoy-rust/DECISIONS.md` → 13 lines, ADR-0021 only (append-only invariant preserved).

---

## 2. Strengths

- **Doctrine conformance end-to-end.** `#![forbid(unsafe_code)]` at line 1 of both `crates/envoy-config/src/lib.rs` and `crates/envoy-http1/src/lib.rs`. The new `crates/envoy-config/src/matcher.rs` inherits the crate-root forbid via `pub mod matcher;` (Rust's `#![forbid]` propagates to submodules). Exactly one new dep on the allow-list (`regex = "1"`, narrowly scoped via ADR-0021); no new crate-root attribute landings needed (no new crates). Append-only ADR ledger preserved exactly: `git log --oneline c5c40ec..e00f638 -- docs/envoy-rust/DECISIONS.md` shows exactly one commit (`984aedd` Task 1). Toolchain pin untouched. Envoy version pin untouched.

- **Hand-rolled `Deserialize` impls model Envoy's field-name oneof shape correctly.** `crates/envoy-config/src/bootstrap.rs:369-417` (SafeRegex), `bootstrap.rs:478-565` (StringMatcher), `bootstrap.rs:615-748` (HeaderMatcher). Each uses a `MapAccess` visitor with an explicit per-key dispatch table and a final `M::Error::unknown_field(other, ALL_KEYS)` arm — strictly more rigorous than `#[serde(untagged)]` (which would silently pick the first parsing variant) or `#[serde(tag = "...")]` (which only models a fixed-name discriminator). Multi-mode-key collisions are detected with informative error text: StringMatcher uses inline `if mode.is_some() { return Err(M::Error::custom("StringMatcher: multiple mode keys (each variant is mutually exclusive)")); }` per arm (`bootstrap.rs:497-504, 506-513, 515-522, 524-531, 533-540`); HeaderMatcher uses an inline `set_mode` closure (`bootstrap.rs:646-657`) that centralizes the same check. The `parses_safe_regex`, `parses_string_matcher_*` (3 tests), `parses_header_matcher_*` (4 tests), `rejects_two_string_matcher_mode_keys`, and `rejects_two_header_matcher_mode_keys` tests pin both the happy path and the collision-rejection.

- **`MODE_KEYS` const + missing-mode error formatter.** `bootstrap.rs:486-491` (StringMatcher) and `bootstrap.rs:625-635` (HeaderMatcher) declare `const MODE_KEYS: &[&str] = &[...]` for the missing-mode error formatter (`format!("HeaderMatcher: missing mode key (expected one of {MODE_KEYS:?})")`). The Task 4 review-fix introduced `MODE_KEYS` for HeaderMatcher specifically to prevent the error string from drifting if a future variant is added — matches StringMatcher's pattern. The two `ALL_KEYS` consts (`bootstrap.rs:481-485` for StringMatcher, `bootstrap.rs:626-635` for HeaderMatcher) feed the `M::Error::unknown_field(other, ALL_KEYS)` reject path so unknown YAML keys are rejected at parse time with the full allowed-key list visible in the error.

- **Validator regex-compile pass + `&mut Bootstrap` migration.** `bootstrap.rs:736-741` (`pub(crate) fn validate(bootstrap: &mut Bootstrap)`) and `bootstrap.rs:946-947` (`fn validate_hcm(hcm: &mut HttpConnectionManagerConfig)`) migrate to `&mut` to allow the `compile_safe_regex` mutation. The `compile_safe_regex` helper (`bootstrap.rs:1135-1146`) wraps `regex::Regex::new(&sr.regex)` and stores `sr.compiled = Some(Arc::new(re))` on success, returning `ConfigError::InvalidRegex { regex, source }` on failure — both fields named for diagnostic clarity. The TCP_PROXY arm at `bootstrap.rs:850-870` correctly preserves the read-only-validate-cluster-name semantics with a structural rewrite (`tp.cluster.clone()` extracted into `cluster_name` before the listener loop's mutable borrow becomes active); the inline 3-line comment at `bootstrap.rs:850-853` clarifies the asymmetry with the HCM arm's `as_mut()` form. Task 5 review-fix's commit `48e615c` is the comment-only follow-up that made the asymmetry self-documenting.

- **`validate_header_matcher` dispatches by mode with the right per-arm action.** `bootstrap.rs:1108-1135` rejects empty `name` first (the cheap check); then dispatches on `&mut hm.mode`: `ExactMatch | PrefixMatch | SuffixMatch | PresentMatch` are no-op (no validator-side check — the parser already produced a well-typed value); `SafeRegexMatch(sr)` calls `compile_safe_regex(sr)?` (the regex compile pass); `RangeMatch(r)` rejects `r.start >= r.end` with `InvalidInt64Range { start, end }` (the half-open empty-interval rejection); `StringMatch(sm)` then sub-dispatches over `&mut sm.mode` with the same shape — the inner `SafeRegex(sr) → compile_safe_regex(sr)?` arm is the only substantive case (Exact/Prefix/Suffix/Contains are no-op). The two-level nested `match` correctly handles the StringMatcher SafeRegex case without recursion.

- **Matcher runtime is byte-correct on every mode-arm I traced.** `crates/envoy-config/src/matcher.rs:19-50` implements `HeaderMatcher::matches`: case-insensitive header-name lookup via `eq_ignore_ascii_case` (HTTP/1.1 §3.2 compliant); `value.is_some_and(...)` short-circuits absent-header to `false` for value-bearing modes; `RangeMatch` parses the value as `i64` decimal then half-open `n >= start && n < end` (boundary covered by 5 `range_match_*` tests at `matcher.rs:213-254`); `PresentMatch(false)` returns `true` unconditionally (the subtle SPEC §6 signpost 7 behavior; covered by `present_match_false_returns_true_when_present` and `present_match_false_returns_true_when_absent` at `matcher.rs:267-277`); the final `mode_result ^ self.invert_match` XOR is the right shape (one XOR with the bool, both branches verified by `invert_match_inverts_exact_match_result` and `invert_match_inverts_present_match_result` at `matcher.rs:344-355`).

- **`StringMatcher::matches` correctly applies `ignore_case` to Exact/Prefix/Suffix/Contains and explicitly skips it on SafeRegex.** `matcher.rs:60-101`: Exact uses `eq_ignore_ascii_case` when `ignore_case`; Prefix/Suffix use `value.get(..lit.len()).is_some_and(|prefix| prefix.eq_ignore_ascii_case(lit))` (the Task 6 review-fix's panic-safety form via `str::get(..)` instead of bare slicing — defensive against multi-byte UTF-8 input where `lit.len()` falls mid-codepoint); Contains uses `value.to_ascii_lowercase().contains(&lit.to_ascii_lowercase())`; SafeRegex calls `sr.compiled.as_ref().expect("validator ensured StringMatcher SafeRegex compiled").is_match(value)` directly with no `ignore_case` interaction (per Envoy proto: regex callers use `(?i)` inline flag). The `string_match_safe_regex_ignore_case_no_effect` test at `matcher.rs:298-308` constructs a SafeRegex with literal `^beta$` and `ignore_case: true`, then asserts the matcher rejects `"BETA"` and accepts `"beta"` — pins the Envoy-compat behavior.

- **`expect` panic-message disambiguation per call site.** `matcher.rs:34` reads `expect("validator ensured HeaderMatcher SafeRegex compiled")`; `matcher.rs:88` reads `expect("validator ensured StringMatcher SafeRegex compiled")`. The Task 6 review-fix introduced this disambiguation; a future regression panic will localize to the correct call site without symbol information. Mirrors the parent project's existing precedent of validator-ensured `expect` strings (e.g., 02.2's `expect("validator ensured cluster present")`).

- **HCM route walker AND-combination correctly short-circuits on empty headers Vec.** `crates/envoy-http1/src/hcm.rs:268-279`: `path_match` is computed first; on path-mismatch the function returns `false` without touching the headers Vec; on path-match `r.r#match.headers.iter().all(|m| m.matches(headers))` AND-combines with short-circuit on first non-match. Empty Vec returns `true` (the regression baseline; covered by `route_with_no_headers_matches_unchanged` at `hcm.rs:625-643` driving `GET /healthz` with no extra headers and asserting 200). The 5 HCM integration tests cover: regression baseline (empty headers Vec); single-matcher selected on match (matcher route 418 selected when X-Foo: bar present); single-matcher skipped on mismatch (matcher route skipped, falls through to default 200); two-matcher AND all-match selected; two-matcher AND one-fails skipped. Every test sends `Connection: close\r\n` to avoid the 5s idle-timeout penalty per Task 7's review-fix discipline.

- **`Driver::Http1ProbeList` teardown ordering is correct and intentional.** `tests/differential/src/lib.rs:1171-1278`. The probe loop iterates `for probe in probes`, each running upstream + subject `drive_http1` + the 5-axis equivalence cascade; `subject.shutdown(Duration::from_secs(5)).await.ok()` and `drop(upstream)` happen AFTER the loop completes (`lib.rs:1276-1277`). This intentionally diverges from the singleton `Driver::Http1` arm (`lib.rs:1093-1094` tears down before equivalence checks). Both shapes are correct: `Driver::Http1` is single-shot so the equivalence checks can run after teardown without touching the proxies; `Driver::Http1ProbeList` reuses the proxies across probes so teardown must wait. Mirrors `Driver::TlsTcpProbeList` from 03.2 (per PROGRESS Task 9 line 112). The listener-port substitution arm at `lib.rs:836-841` includes `Driver::Http1ProbeList` in the `"PORT"` group (the same group as `Driver::Http1`, `Driver::TcpEcho`, `Driver::TlsTcp`, `Driver::TlsTcpProbeList`) — verified.

- **Fixture 0007 amendment preserves first-match-wins discipline.** `tests/fixtures/0007-http1-direct-response/envoy.yaml` and `envoy-rust.yaml` place the matcher route at the head of `routes:` (lines 19-27 / 22-30 in the diff respectively) — so a request to `/api/widgets` with `X-Foo: bar` reaches the matcher route 418 before the catch-all `prefix: "/"` 200 OK. A request without `X-Foo: bar` (e.g. the default-route probe's `GET /healthz`) skips the matcher (path-mismatch on `/healthz` not starting with `/api/`) and falls through to the catch-all. Both YAMLs use Envoy proto field names verbatim — no per-side divergence beyond the already-existing 04.1 bind-address + admin-block per-side differences.

- **`extra_headers: [["X-Foo", "bar"]]` YAML round-trips cleanly via tuple-pair Vec.** `tests/differential/src/lib.rs:165-169` declares `pub extra_headers: Vec<(String, String)>` with `#[serde(default)]`. serde_yaml round-trips a Vec of 2-element sequences as Vec<(T, U)> via the standard tuple deserialization path; the YAML `[["X-Foo", "bar"]]` → `vec![("X-Foo".into(), "bar".into())]` round-trip is pinned by `parses_expectations_with_http1_probe_list` at `lib.rs:1953-1996`. The default-empty test `http1_probe_extra_headers_default_empty` at `lib.rs:1999-2008` pins the no-extra-headers path. Both tests parse the YAML directly via `serde_yaml::from_str::<Http1Probe>` and `serde_yaml::from_str::<Expectations>`, exercising the actual deserialization pipeline rather than constructing the values programmatically.

- **`Http1Probe` carries `#[serde(deny_unknown_fields)]`.** `tests/differential/src/lib.rs:159` — defends against typos in fixture YAML (e.g. `extra_header` singular instead of `extra_headers`). The 04.1-landed `Http1Method` / `Http1BodyRule` / `Http1HeaderRule` discriminator-key shape carries forward unchanged.

- **Fuzz seed exercises 5 of 7 modes simultaneously in a single `headers:` Vec.** `crates/envoy-config/fuzz/corpus/parse_bootstrap/route_with_header_matchers.yaml:21-37` declares one Route with 5 HeaderMatchers (`exact_match`, `safe_regex_match`, `range_match`, `present_match`, `string_match` with `contains: "beta"` + `ignore_case: true`) — exercises the hand-rolled visitor's per-key dispatch on all 5 modes simultaneously. The fuzz target `parse_bootstrap` exercises serde + the validator's regex-compile pass; the fuzzer mutates the YAML keys/values and the validator either accepts (compiled regex stored) or rejects (typed ConfigError). The corpus-walk test `fuzz_corpus_seeds_parse_or_reject_cleanly` at `bootstrap.rs:2017-2024` is extended with the new seed (PROGRESS Task 8 line 105). Plausible-but-irrelevant regex pattern (`^v[0-9]+$`); the fuzzer never executes the regex against header values.

- **ADR-0021 is well-formed against the SPEC's projected text.** `docs/envoy-rust/DECISIONS.md` ADR-0021 (lines 395-405 in the file; 13 lines added at HEAD) carries Date / Status / Context / Options-considered (4 options enumerated, 3 rejected with rationale) / Decision (narrow scope: `regex = "1"` for HeaderMatcher::SafeRegex + StringMatcher::SafeRegex compilation at config-load time) / Rationale (removes per-phase-ADR churn; treats first-use as the foundation grant) / Consequences (lock-cadence prose + cargo deny check posture + future-scope-extension expectation) / Provenance (parent-04 SPEC §7 projection, ADR-0020 lands at parent-04 state-2 commit `1d9740d`, ADR-0021 lands at this commit). Matches the SPEC's verbatim projection at SPEC §7 with one notable deviation: the Consequences section's prose says "as a dedicated commit at the 04.2 state-4 phase-done gate per established phase precedent" but the actual cadence is inline at Task 1 — see M9 below.

- **Seven in-phase review-fix commits closed seven well-targeted issues before propagation.** Tasks 2 / 3 / 4 / 5 / 6 / 7 each landed exactly one review-fix commit + a paired progress-note. Task 2 fix `5a6b950` clarified the SafeRegex `Deserialize` doc-comment rationale (replaced the misleading `#[serde(skip)]` justification with the visitor-template-setting prose). Task 3 fix `3d9f985` tightened `rejects_two_string_matcher_mode_keys` to verify the error message contains "multiple mode keys" or "mutually exclusive". Task 4 fix `17f991a` added `rejects_two_header_matcher_mode_keys` test + introduced `MODE_KEYS` const for the missing-mode error formatter (mirrors StringMatcher). Task 5 fix `48e615c` added a 3-line comment to TCP_PROXY arm clarifying the read-only `as_ref()` intent. Task 6 fix `81c6dde` was the most substantive: replaced bare byte-slicing in StringMatcher Prefix/Suffix `ignore_case: true` branches with `str::get(..)` (panic-safety), disambiguated the two `expect` panic messages, added `string_match_prefix_with_ignore_case_matches` + `string_match_suffix_with_ignore_case_matches` regression tests for the panic-safety fix. Task 7 fix `8330a86` appended `Connection: close\r\n` to all 5 new HCM test request literals (avoids 5s idle-timeout penalty). All seven review-fixes are small (1-3 file edits each), single-purpose, and follow the `phase 04.2: <crate> — Task N review fix (<summary>)` commit-message form. PROGRESS subsections were ADDED rather than overwriting the original task entries — preserves the audit trail.

- **PROGRESS.md cadence is consistent.** Every task has a section; review-fix follow-ups added subsections rather than overwriting; state-4 section quotes verification outputs verbatim. The Cargo.lock-inline disclosure at Task 1 deviation 1 is well-disclosed (says explicitly "ADR-0021's Consequences section's 'dedicated state-4 commit' prose is now contradicted but per D-3.5 the ADR text is append-only and remains untouched"). PLAN.md late-landing at `160caf0` (right before state-4) is also disclosed (PROGRESS Task 12 line 144 — "Reviewer-of-state-5 may flag this as a process consistency concern"; mirrors 04.1's same pattern).

- **CI gate cleared on first attempt.** PROGRESS Task 12 reports build/clippy/fmt/test/deny all clean with no fix-during-gate commits. Local re-verification on HEAD `e00f638`: all 5 commands exit 0 on first attempt. Matches phase-04.1 / 03.2 / 03.1 / 02.2 / 02.1 first-attempt-green pattern.

- **Commit-message hygiene matches the established precedent.** Task commits follow `phase 04.2: <crate> — <summary> (task N)`; progress-note commits are `phase 04.2: progress note (task N)`; review-fix commits use `phase 04.2: <crate> — Task N review fix (<summary>)`. The `[ADR-0021]` annotation appears only on Task 1's commit `984aedd` (where the ADR landed), correctly omitted from the other 11 task commits.

---

## 3. Issues

### Critical

None.

### Important

None.

### Minor

**M8. `safe_regex_partial_eq_compares_only_regex_string` test asserts `compiled: None == compiled: Some(_)` is true — surprising for post-validate consumers.** *(Awareness-only.)*

`crates/envoy-config/src/bootstrap.rs:3403-3414` constructs `SafeRegex { regex: "x", compiled: None }` and `SafeRegex { regex: "x", compiled: Some(Arc::new(Regex::new("x").unwrap())) }`, then asserts `assert_eq!(a, b)` succeeds (the `compiled` field is opaque to PartialEq; `bootstrap.rs:362-366`). The test correctly pins SPEC §6 signpost 17's design intent ("PartialEq compares only the `regex` String"). However, the impl is *strictly* opaque — even `SafeRegex { regex: "x", compiled: Some(re_a) } == SafeRegex { regex: "x", compiled: Some(re_b) }` is `true` regardless of whether `re_a` and `re_b` come from the same pattern compile. For 04.2 this is non-bug — no consumer in the codebase compares SafeRegex values post-validate (the route walker only consults `compiled` via `expect`); test setup is entirely structural (pre-validate values with `compiled: None`). Phase 04.3 / future filter phases that may compare RouteConfiguration values *after* validate (e.g. for config-diff in xDS) should be aware that `SafeRegex` PartialEq is opaque to the compile-state, which means two RouteConfigurations whose only difference is "validator ran on one but not the other" will compare equal.

*Fix:* leave as-is for 04.2. Track forward into the first phase that compares RouteConfiguration values post-validate. If post-validate comparison is needed, the right shape is to add an `is_compiled()` accessor on SafeRegex or to compare the unparsed-pattern + a compile-state boolean explicitly.

**M9. ADR-0021's Consequences section's "dedicated state-4 commit" prose contradicts the actual Cargo.lock-inline cadence.** *(Awareness-only; well-disclosed but partially unresolvable.)*

`docs/envoy-rust/DECISIONS.md` ADR-0021 Consequences section reads "`Cargo.lock` gains `regex` + transitive surface (`regex-syntax`, `aho-corasick`, `memchr`) as a dedicated commit at the 04.2 state-4 phase-done gate per established phase precedent (phase-01 `4955252`, phase-02.1 `dea4d16`, phase-02.2 `2146014`, phase-03.1 `eb039e6`, phase-03.2 `85633e7`)." The actual cadence is inline at Task 1 commit `984aedd` (PROGRESS Task 1 deviation 1; PROGRESS Task 12 line 188 confirms the lock was clean at state-4). Per D-3.5 ADRs are append-only — ADR-0021 cannot be edited — so the contradiction stands as a permanent inline-ADR-vs-actual-cadence discrepancy. PROGRESS Task 1 names this explicitly: "ADR-0021's Consequences section (now landed and append-only per D-3.5) states the lock will sync 'as a dedicated commit at the 04.2 state-4 phase-done gate per established phase precedent.' Reality: PLAN.md Task 1 Step 11's `git add ... Cargo.lock` stages the lock inline." The disclosure is adequate; the ADR remains accurate per its date-of-landing (the PLAN's intention at that moment was Step 11's inline `git add` per the 04.1 inline-at-scaffold precedent — but the ADR's prose at that same moment incorrectly named the 03.x precedent without checking 04.1's actual cadence). Two equally-acceptable resolutions exist for future phases: (a) supersede ADR-0021 with ADR-NNNN that re-states the lock-cadence prose to match the actual 04.x posture; (b) leave ADR-0021 as-is and rely on PROGRESS-disclosure for the audit trail. M5 (carryforward from 04.1 REVIEW) recommended standardizing on one cadence; 04.2 inherited the 04.1 inline cadence which is doctrine-conformant per `BOOTSTRAP_PROMPT.md` ("if cargo build forces it, that's fine to include with the main commit but document in PROGRESS.md").

*Fix:* leave as-is for 04.2 (ADR text is immutable; PROGRESS disclosure is adequate). Track forward into the next phase that adds a workspace dep or workspace member: pick a cadence consciously and either (a) supersede ADR-0021 with prose that matches the chosen cadence or (b) state in the new phase's PLAN/PROGRESS that the inline cadence is the project's standardized posture.

**M10. PLAN.md late-landing at state-4 mirrors 04.1's same pattern but is still a process-consistency concern.** *(Awareness-only.)*

`docs/envoy-rust/phases/04.2-route-matchers/PLAN.md` (3562 lines) was committed at `160caf0` immediately before the state-4 gate (commit `e00f638`). PROGRESS Task 12 line 144 names this: "PLAN.md was committed at `160caf0` immediately before the state-4 gate as a dedicated commit `phase 04.2: state-2 PLAN.md (late-landing per 04.1 inline-at-Task-1 precedent)`. Mirrors the 04.1 pattern of PLAN.md landing inline rather than at a clean state-2 close-out (PROGRESS Task 1 of 04.1 documents the same well-disclosed deviation). Reviewer-of-state-5 may flag this as a process consistency concern." The doctrine-prescribed flow per BOOTSTRAP_PROMPT.md §5 is state 2 → write PLAN.md → commit → state 3 → write PROGRESS.md per task; the 04.1 + 04.2 reality is "execute tasks against an in-session PLAN, then commit PLAN.md as a single late commit before state-4." Functionally the reviewer can verify the PLAN against the executed shape since the file is now on disk; the concern is that a *different* fresh session entering at state 4 would have no PLAN.md to consult during the brief window between Task 12's gate-execute and the late commit. The 04.1 REVIEW did not call this out; doing so here for consistency.

*Fix:* leave as-is for 04.2 (PLAN.md is on disk at HEAD; reviewable). Track forward: the 04.3 planner should commit PLAN.md at clean state-2 close-out (i.e. before any task commits) per BOOTSTRAP_PROMPT.md §5 to break the 04.1 → 04.2 inline-PLAN precedent.

**M11. `Http1Probe.extra_headers: Vec<(String, String)>` could collide with duplicate header name semantics if a future probe needs two `Set-Cookie` rows.** *(Awareness-only.)*

`tests/differential/src/lib.rs:165-169` declares `extra_headers` as `Vec<(String, String)>` — preserves order and allows duplicates. The `drive_http1` request-builder at `lib.rs:707-711` correctly emits each `(n, v)` pair as a separate `\r\n`-terminated header line, so duplicates ARE preserved on the wire. However, the response-side header diff via `diff_headers` (per 04.1 REVIEW M1) uses `find()` for value lookup and silently ignores duplicate-header value mismatches on the response side. For 04.2 fixture 0007's amendment this is non-bug (the matcher-route 418 + default-route 200 responses both have unique header rows). Phase 04.3 (upstream proxying) may surface fixtures where the upstream emits `Set-Cookie` or duplicate `Vary` rows; at that point the response-side `diff_headers` and the request-side `extra_headers` need a coordinated fix. This is the same M1 carryforward from 04.1 REVIEW; 04.2 made it slightly more relevant by adding the request-side Vec form, but did not exercise duplicates.

*Fix:* leave as-is for 04.2. Track forward into 04.3 alongside the M1 carryforward.

---

## 4. Forward work (M-track)

Numbered M-items for the closing-out commit reference. None block state 6.

1. **M1 — `diff_headers` duplicate-header semantics** (carryforward from 04.1 REVIEW M1). `tests/differential/src/lib.rs` uses `find()` for value lookup, silently ignoring duplicate-header value mismatches. Track forward into 04.3 (upstream proxying may surface fixtures with `Set-Cookie` / `Vary` duplicates).

2. **M2 — Body-drain idle timeout returns `Ok(())` silently** (carryforward from 04.1 REVIEW M2). `crates/envoy-http1/src/hcm.rs:167` — known divergence per 04.1 PROGRESS Task 10 review-fix. Track forward into 04.3 or hardening pass.

3. **M3 — `envoy-http1`'s forward-looking `envoy-cluster` dep** (carryforward from 04.1 REVIEW M3). `crates/envoy-http1/Cargo.toml:10` — pre-staging for 04.3, no consumer in 04.1 or 04.2. Awareness-only; consume the dep at 04.3 or drop if a different shape emerges.

4. **M4 — `strip_port` IPv6-Host correctness** (carryforward from 04.1 REVIEW M4). `crates/envoy-http1/src/hcm.rs:246-251` uses `rfind(':')` which is incorrect for bare-IPv6 Host (validator-rejected for now, but defense-in-depth would tighten). Track forward into 04.3.

5. **M5 — Cargo.lock sync cadence standardization** (carryforward from 04.1 REVIEW M5; partially addressed via 04.2 Task 1 review-fix's PROGRESS disclosure). Both 04.1 and 04.2 landed Cargo.lock inline at scaffold/Task-1 time; the 03.x precedent had dedicated tail commits. ADR-0021's Consequences prose contradicts the actual 04.x cadence (see M9 below). Track forward into the next phase that adds a workspace dep or member: pick a cadence consciously and either supersede ADR-0021 or state in PLAN/PROGRESS that inline is the project's posture.

6. **M6 — `drive_http1` per-function unit test** (carryforward from 04.1 REVIEW M6; **NOT closed in 04.2**). 04.2 Task 9 added 2 tests for `Http1Probe` parsing + `extra_headers` default — these cover the new schema, not `drive_http1` itself. PROGRESS Task 11 line 133 explicitly carries M6 forward to 04.3. Track forward into 04.3 where the fixture-0007 `Driver::Http1ProbeList` is the natural anchor for an in-process unit test.

7. **M7 — `TlsAcceptingHandler.inner` generalization for HCM+TLS** (carryforward from 04.1 REVIEW M7). 04.2 introduces no TLS-bearing HCM fixtures. Track forward into phase 05+'s brainstorm (HTTP/2 with TLS, mTLS).

8. **M8 — `safe_regex_partial_eq` opaque-equality may surprise post-validate consumers.** `crates/envoy-config/src/bootstrap.rs:362-366` SafeRegex PartialEq compares only `regex: String`; two values with `compiled: None` vs `compiled: Some(_)` compare equal regardless of pattern-compile state. Non-bug for 04.2 (no post-validate comparison consumer); track forward to the first phase that compares RouteConfiguration values post-validate (e.g. xDS config-diff).

9. **M9 — ADR-0021's "dedicated state-4 commit" prose contradicts the actual inline cadence.** D-3.5 forbids ADR edits; the contradiction is permanent at the ADR's date-of-landing. PROGRESS Task 1 deviation 1 is the audit trail. Track forward alongside M5: the next phase's planner should pick a cadence consciously and either supersede ADR-0021 or document the inline cadence as the project's posture.

10. **M10 — PLAN.md late-landing at state-4** mirrors 04.1's same pattern. Functionally reviewable (PLAN is on disk at HEAD); concern is that a fresh session entering at state-4 would have no PLAN.md to consult during the brief window before the late commit. Track forward: 04.3 planner should commit PLAN.md at clean state-2 close-out per BOOTSTRAP_PROMPT.md §5 to break the precedent.

11. **M11 — `Http1Probe.extra_headers: Vec<(String, String)>` duplicate-header semantics on response side.** Coupled with M1; the 04.2 request-side Vec form preserves duplicates correctly but the response-side `diff_headers` does not. Track forward into 04.3 alongside M1.

Earlier-phase carryforwards still relevant at 04.2 close (inherited from 04.1 REVIEW §4):

12. **`Cluster::name()` accessor (M1 from phase-02.1 / 02.2 / 03.1 / 03.2 REVIEWs).** Still deferred to phase 06 (stats family) per phase-03.2 REVIEW §4 rec 1 — 04.2 did not need typed cluster-name accessors. Phase 04.3 (upstream HTTP/1.1) will revisit.

13. **`x509-parser` deferred ADR.** Phase-03.1 REVIEW §4 rec 1 / 03.2 REVIEW §4 rec 2. Still deferred — 04.2 did not introduce mTLS or peer-cert-attribution headers.

14. **`enable_half_close: true` flip-fixture.** Phase-03.2 REVIEW §4 rec 7. Still deferred — 04.2 did not introduce asymmetric-close semantics.

---

## 5. In-phase fixes (closed)

This section preserves the audit trail of issues caught and resolved in-phase by review-fix commits. The same cadence as phase-04.1 — review-fix commits are *additive* (not amends) and PROGRESS.md gains a follow-up subsection rather than overwriting the original task entry.

### Task 2 review fix — commit `5a6b950` (followed by progress note `7d83e84`)

- **Issue:** SafeRegex's hand-rolled `Deserialize` doc-comment used a misleading "`#[serde(skip)]` would leave the field absent" justification; the actual rationale is the visitor-template-setting (Tasks 3 + 4 reuse the visitor pattern for field-name oneof).
- **Fix:** rewrote the doc-comment at `bootstrap.rs:381-394` to name the visitor-template rationale explicitly + the two-phase init contract (visitor produces `compiled: None`; validator fills it).
- **Verification:** comment-only change; gate commands clean (build / clippy / fmt / test / deny all exit 0; 79 tests passing — unchanged).

### Task 3 review fix — commit `3d9f985` (followed by progress note `c0db2c5`)

- **Issue:** `rejects_two_string_matcher_mode_keys` only checked `is_err()`; a future error-shape regression (e.g. accidentally widening the visitor to silently pick the first key) would not be caught.
- **Fix:** tightened the assertion to verify the error message contains "multiple mode keys" or "mutually exclusive". Mirrors the existing `rejects_unknown_string_matcher_mode_key` assertion style. Note: `clippy::err_expect` (-D warnings) required `expect_err()` instead of `.err().expect()` — semantically identical.
- **Verification:** `cargo test -p envoy-config rejects_two_string_matcher_mode_keys` → 1 passed; full crate test 84 passed; gate clean.

### Task 4 review fix — commit `17f991a` (followed by progress note `6b5ad4e`)

- **Issue (2 findings):** (1) `set_mode`'s guard branch (the multi-mode-key collision invariant in HeaderMatcher's visitor) was untested at the schema-layer, even though StringMatcher's symmetric path was. (2) The missing-mode error string was hardcoded as a literal rather than formatted from a `MODE_KEYS` const, so a future variant addition would silently leave the error string out of sync.
- **Fix:** added `rejects_two_header_matcher_mode_keys` test (mirrors Task 3's `rejects_two_string_matcher_mode_keys`); introduced `MODE_KEYS` const in HeaderMatcher's `Deserialize` impl (sibling to `ALL_KEYS`) and refactored the missing-mode error to use `format!("...{MODE_KEYS:?}")`.
- **Verification:** `cargo test -p envoy-config --lib` → 91 passed (was 90; +1); gate clean.

### Task 5 review fix — commit `48e615c` (followed by progress note `244177d`)

- **Issue:** the TCP_PROXY_FILTER arm in `validate` uses `as_ref()` while the HCM_FILTER arm uses `as_mut()` (introduced in Task 5 for the regex-compile mutation). Without an inline comment the asymmetry reads as a possible bug.
- **Fix:** added a 3-line comment block to the TCP_PROXY_FILTER arm at `bootstrap.rs:850-853` clarifying that the `as_ref()` borrow is intentional (TCP_PROXY validation is read-only).
- **Verification:** comment-only change; gate clean; 101 tests still passing.

### Task 6 review fix — commit `81c6dde` (followed by progress note `2e8a00c`)

- **Issue (3 findings):** (1) **Panic safety:** bare `&value[..lit.len()]` byte-slicing in StringMatcher's Prefix/Suffix `ignore_case: true` branches would panic on multi-byte UTF-8 input where `lit.len()` falls mid-codepoint. (2) **Panic-message disambiguation:** the two `expect("validator ensured compiled")` sites had identical panic messages; a future regression panic would not localize without symbol info. (3) **Test coverage gap:** the Prefix/Suffix `StringMatcher` branches were untested directly (only reachable via `HeaderMatcherMode::StringMatch`).
- **Fix:** replaced bare byte-slicing with `value.get(..lit.len()).is_some_and(|prefix| prefix.eq_ignore_ascii_case(lit))` (and analogously for Suffix); disambiguated the two `expect` strings to "validator ensured HeaderMatcher SafeRegex compiled" and "validator ensured StringMatcher SafeRegex compiled"; added `string_match_prefix_with_ignore_case_matches` + `string_match_suffix_with_ignore_case_matches` regression tests at `matcher.rs:312-331`.
- **Verification:** `cargo test -p envoy-config --lib` → 131 passed (was 129; +2); gate clean.

### Task 7 review fix — commit `8330a86` (followed by progress note `9b7f39a`)

- **Issue:** the 5 new HCM integration tests (route_with_no_headers_matches_unchanged, single_header_matcher_*, multi_header_matcher_*) lacked `Connection: close\r\n` in their request literals, so the server-side `serve_connection` loop kept the connection open until the 5s idle timeout fired before the test's `read_to_end()` returned — costing ~5s per test (5 tests × 5s = 25s wall in isolation).
- **Fix:** appended `Connection: close\r\n` to each of the 5 test request literals at `hcm.rs:633-815`. Aligns with the established 04.1 cadence (every 04.1 test in `hcm.rs` sends `Connection: close`).
- **Verification:** all 5 new tests pass in <1s each (was ~5s); 24 envoy-http1 tests passing; gate clean.

All seven review-fix commits land before downstream consumers and follow the additive `phase 04.2: <crate> — Task N review fix (<summary>)` commit-message form. PROGRESS subsections were added rather than overwriting the original task entries — the audit trail is complete.

---

## 6. Doctrine compliance

- **`#![forbid(unsafe_code)]`** — present at `crates/envoy-config/src/lib.rs:1` and `crates/envoy-http1/src/lib.rs:1`. The new `crates/envoy-config/src/matcher.rs` module is declared via `pub mod matcher;` in `crates/envoy-config/src/lib.rs:8` and inherits the crate-root forbid (Rust's `#![forbid]` propagates to submodules; the doctrine requires the attribute *on every crate root*, not on every file). No new crates were added in 04.2, so no fresh crate-root attribute landings to verify.
- **No new deps without ADR** — verified by `git diff c5c40ec..e00f638 -- Cargo.toml crates/*/Cargo.toml tests/**/Cargo.toml`: exactly one new dep (`regex = "1"` on `crates/envoy-config/Cargo.toml`) under ADR-0021. Transitive surface (`regex-syntax`, `aho-corasick`, `memchr`) flows through the Cargo.lock under MIT/Apache-2.0/Unlicense; `Unlicense` was added to `deny.toml`'s `[licenses] allow` list at Task 1 alongside the ADR landing.
- **ADR ledger advanced from 20 to 21** — `git log --oneline c5c40ec..e00f638 -- docs/envoy-rust/DECISIONS.md` shows exactly one commit (`984aedd` Task 1, ADR-0021). ADR-0001 through ADR-0020 are byte-identical to phase-04.1 close. ADR-0021's text is byte-identical from landing through HEAD (verified by `git diff 984aedd..e00f638 -- docs/envoy-rust/DECISIONS.md` empty).
- **Toolchain pin untouched** — `git diff c5c40ec..e00f638 -- rust-toolchain.toml` empty.
- **Envoy version pin untouched** — `git diff c5c40ec..e00f638 -- docs/envoy-rust/ENVOY_TARGET.md` empty.
- **D-3.1 TDD discipline** — every implementation task pairs with tests landing alongside (or RED-first ahead of) the impl. Per-task tally per PROGRESS: Task 2 +4 (Int64Range/SafeRegex); Task 3 +5 (StringMatcher) + Task 3 review-fix tightens assertion; Task 4 +6 (HeaderMatcher) + Task 4 review-fix +1 (`rejects_two_header_matcher_mode_keys`); Task 5 +10 (validator); Task 6 +28 (matcher runtime) + Task 6 review-fix +2 (`string_match_{prefix,suffix}_with_ignore_case_matches`); Task 7 +5 (HCM integration); Task 9 +2 (Http1Probe); Tasks 1, 8, 10, 11, 12 add no new tests directly (Task 1 is the ADR + dep stub; Task 8 extends the corpus-walk enumeration; Task 10 amends fixture 0007; Task 11 is the M-track audit; Task 12 is the gate). Total +63 tests in-phase (envoy-config 75→131 = +56; envoy-http1 19→24 = +5; differential lib 47→49 = +2). Matches PROGRESS Task 12 line 178's "+63" delta exactly.
- **D-3.2 permitted foundations** — exactly one new external dep (`regex = "1"`, narrowly scoped via ADR-0021 to `HeaderMatcher::SafeRegex` + `StringMatcher::SafeRegex` compilation at config-load time). ADR-0021 explicitly excludes general-purpose use; future filter-framework regex needs require a scope-extension ADR.
- **D-3.4 PROGRESS cold-readability** — every task has Commit / Change / Verification / Deviation sections; review-fix follow-ups added subsections rather than overwriting; state-4 section quotes verification outputs verbatim.
- **D-3.5 append-only ADRs** — verified single-commit diff on `docs/envoy-rust/DECISIONS.md`. ADR-0021's prose contradicts the actual Cargo.lock cadence (M9) — but the contradiction is preserved exactly per the append-only invariant; the ADR text was not retroactively edited. PROGRESS Task 1 deviation 1 is the audit trail.
- **D-3.6 green-build first-attempt** — gate green at HEAD `e00f638` on first attempt; locally re-verified (5/5 commands exit 0).
- **D-3.7 Envoy pin** — verified empty diff on `docs/envoy-rust/ENVOY_TARGET.md`.
- **D-3.8 unsafe-code forbid** — verified at every workspace crate root; the new `matcher.rs` submodule inherits via `pub mod matcher;` declaration in `lib.rs`.
- **D-3.9 toolchain pin** — verified empty diff on `rust-toolchain.toml`.

---

## 7. Verdict

**Approved with M-track follow-ups.**

Reviewed 2026-04-27 against HEAD `e00f638`. No Critical, no Important. 4 new Minor findings (M8 — `safe_regex_partial_eq` opaque-equality awareness; M9 — ADR-0021 prose ↔ Cargo.lock cadence contradiction; M10 — PLAN.md late-landing process consistency; M11 — `Http1Probe.extra_headers` duplicate semantics coupled with M1) plus 7 carryforwards from 04.1 (M1–M7, with M5 partially addressed via Task 1 review-fix's PROGRESS disclosure but ADR text untouched per D-3.5; M6 still open — Task 9's tests cover Http1Probe parsing, not `drive_http1` itself per PROGRESS Task 11 line 133) plus 3 earlier-phase carryforwards (Cluster::name accessor, x509-parser ADR, enable_half_close fixture). None are state-5 or state-6 blockers; none touch production-code correctness on fixture 0007's two probes; none invalidate the state-4 gate's green status. The 11 M-track items track forward into 04.3 / phase 05 / hardening pass per §4.

The new envoy-config schema additions (`HeaderMatcher` + `HeaderMatcherMode` + `SafeRegex` + `Int64Range` + `StringMatcher` + `StringMatcherMode` with three hand-rolled `Deserialize` visitors); the validator's regex-compile + Int64Range bounds-check + matcher-walk extensions plus the `&mut Bootstrap` migration with the borrow-checker-forced TCP_PROXY structural rewrite; the new sibling `crates/envoy-config/src/matcher.rs` module with `HeaderMatcher::matches` + `StringMatcher::matches` inherent methods + 28 unit tests covering all 7 HeaderMatcher modes + all 5 StringMatcher modes + cross-cuts (case-insensitivity, case-sensitivity by default, `invert_match` XOR, `string_match_safe_regex_ignore_case_no_effect` Envoy-compat); the HCM route walker's AND-combination clause + 5 HCM integration tests; the differential harness's `Driver::Http1ProbeList` + `Http1Probe` + `extra_headers` parameter + dispatch arm with deferred-teardown + 2 new harness unit tests; fixture 0007's amendment with the matcher-route + default-route two-probe shape preserving first-match-wins discipline; the 5-mode-simultaneous fuzz seed + corpus-walk extension; ADR-0021 narrowly permitting `regex = "1"` for header / route matching at config-load time; and the 5 new `ConfigError` variants are all shape-correct, test-backed, and doctrine-compliant.

The executor's self-audit discipline is excellent: 14+ deviations narrated across 12 tasks, every one named and rationalized; the seven in-phase review-fix commits (Tasks 2/3/4/5/6/7) closed seven well-targeted issues before propagation, with the Task 6 panic-safety fix (`str::get(..)`) being particularly load-bearing once Task 7 wires the matcher into the HCM hot path. The well-disclosed Cargo.lock-inline cadence at Task 1 + the PROGRESS audit trail covering the ADR-0021 prose contradiction (M9) + the M-track carryforward audit at Task 11 are textbook examples of D-3.4 cold-readability.

State 5 completes by committing REVIEW.md as a single-file commit, matching phase-04.1's `b6e305d` cadence ("phase 04.1: state 5 REVIEW.md Approved with M-track follow-ups" — REVIEW.md only, no STATE.md advance because there are no in-phase I-findings to fix; the same posture applies here). State 6 (next session) then lands the ROADMAP `04.2` flip `in-progress` → `done` (parent row `04` stays `in-progress` until 04.3 lands per the schema invariant), advances STATE.md to phase `04.3-router-upstream` (lifecycle state 2; SPEC.md exists from the parent-04 state-2 split commit `1d9740d`, PLAN.md does not; next-skill `superpowers:writing-plans`), and uses the commit-message format from SPEC §9:

```
phase 04.2: HTTP route header matchers + ADR-0021 (regex permitted) [ADR-0021]
```

per SPEC §9 / `BOOTSTRAP_PROMPT.md` §5.3.

State 6 does not require further review work.
