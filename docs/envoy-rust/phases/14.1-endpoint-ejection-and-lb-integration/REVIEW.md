# Phase 14.1 (`14.1-endpoint-ejection-and-lb-integration`) — REVIEW

- **Phase id:** `14.1`
- **Slug:** `14.1-endpoint-ejection-and-lb-integration`
- **Parent:** `14` (`14-outlier-detection`). **First of two sub-phases** the parent-14 state-2 SPLIT carved phase 14 into (ADR-0040; the §6.2 empirical revision is ADR-0041). 14.1 is the **NON-closing** sub-phase — with this Approved verdict, the next session's state-6 close-out flips ROADMAP row `14.1` `in-progress → done` ALONE (parent row `14` stays `in-progress` until 14.2 closes, per the closing-sub-phase invariant). 14.1 is the **third Upstream-robustness-family foundation slice** (after 12.1 active-HC `EndpointHealth` and 13.1 H1-pool) — config schema + validator + per-endpoint `EndpointEjection` state machine + `pick()` ejection-exclusion + the 7-name outlier-detection stat namespace — proven by **regression-equivalence** (all 21 existing Docker-gated fixtures stay green simultaneously; the machinery is inert when `outlier_detection` is unconfigured per SPEC §5.3), exactly the **05.1 / 07.1 / 12.1 "foundation slice, no new fixture" pattern**. The response-receipt hook (D4), the ejection sweeper (D7), the differential fixture `0022`, and the in-process backstop (D8.3) all land in **14.2**.

- **Reviewed range:** `0170d1e..5bbb37c` — the **6 implementation task commits** (`git log --oneline 0170d1e^..5bbb37c` → 6; the Task 7 state-4 docs-only verification commit `7730e446` is HEAD but carries no code, so it is excluded from the code review). Diffstat (`git diff --stat 0170d1e^..5bbb37c`, base `9a56e85` = the state-2 PLAN-write): **1480 insertions(+), 24 deletions(-)** across 8 files — production: `crates/envoy-cluster/src/cluster.rs` +616/−~22, `crates/envoy-cluster/src/ejection.rs` +443 (new file), `crates/envoy-cluster/src/lib.rs` +2, `crates/envoy-config/src/bootstrap.rs` +368, `crates/envoy-config/src/lib.rs` +34/−2; data/docs: `crates/envoy-config/fuzz/corpus/parse_bootstrap/cluster_outlier_detection.yaml` +26, `crates/envoy-config/fuzz/.gitignore` +1, `docs/envoy-rust/BEHAVIOR_CONTRACT.md` +14.
  - `0170d1e` — **Task 1**: D1 envoy-config `OutlierDetection` schema (5 `Option<_>` fields — `consecutive_5xx`, `consecutive_gateway_failure`, `interval`, `base_ejection_time`, `max_ejection_percent`; `#[serde(deny_unknown_fields)]`; derives `Debug, Serialize, Deserialize, PartialEq`, **NO `Clone`** per lock-in #5 — byte-identical to the on-disk `Cluster`/`CircuitBreakers` cascade) + `Cluster.outlier_detection: Option<OutlierDetection>` field (default `None`; the §5.3 inert invariant) + the `lib.rs` re-export + 2 by-hand `envoy_config::Cluster {}` test-literal compile-fixes. 3 parse tests. Spec ✅ + code-quality **Approved** (3 cosmetic Minors deferred to state 5).
  - `10980b1` — **Task 2**: D2 `validate_outlier_detection` (per-cluster loop, after `validate_circuit_breakers`, no `continue` bypass) + 3 `ConfigError` variants (`InvalidOutlierDetectionThreshold` / `InvalidOutlierDetectionTiming` / `InvalidMaxEjectionPercent`, each `cluster`-contexted); empty `{}` ACCEPTED; reuses `parse_duration` (rejects parse-failure AND zero-duration AND sub-second decimals); `max_ejection_percent` range `[0,100]` (0 and 100 accepted). 10 validator tests. Spec ✅ + code-quality **Approved** (3 cosmetic Minors deferred).
  - `e506b9c` — **Task 3**: D3 `EndpointEjection` state machine (new `crates/envoy-cluster/src/ejection.rs`; `EndpointEjectionStats` 6 shared `Arc` handles [1 Gauge + 5 Counters] + `DetectorType` + `EjectionDecision`; status-driven classifier — 5xx → consecutive_5xx, 502/503/504 → ALSO consecutive_gateway_failure, non-5xx → reset both; `Relaxed` atomics; initial never-ejected; eject/un-eject swap-edged + idempotent). 13 unit tests. One PLAN-code-vs-clippy reconciliation (`502 | 503 | 504` → `502..=504`; behavior-identical). Spec ✅ + code-quality **Approved** (2 Minors deferred).
  - `09bc34a` — **Task 4**: D5 `Cluster::pick()` fast-path-inert (`endpoint_health.is_none() && outlier_detection.is_none()` → byte-for-byte phase-02 round-robin) + slow-path AND-composition (`healthy && !ejected`; each `None` filter vacuously true; 12.1 strictly-below panic preserved; empty-eligible → `None`) + `Cluster::record_response` (cap `floor(host_count*pct/100)`; cap-met → `ejections_overflow.inc()` per detection-tick without ejecting; else eject 5xx-wins-ties; **NO production caller at 14.1** — by design) + `OutlierDetectionState` + `ClusterHandle::record_response` delegate + 4 runtime `Cluster {}` literal compile-fixes. 10 tests. Spec ✅ + code-quality **Approved** (3 Minors deferred).
  - `63188e4` — **Task 5**: D6 `from_bootstrap` configured-OD arm registers the 7-name minimum-viable stat subset (1 Gauge `ejections_active` + 6 Counters) via `mk_counter`/`mk_gauge` closures + `ClusterError::StatsRegistration` mapping; Envoy v3 defaults 5/5/10 via `unwrap_or`; per-endpoint `EndpointEjection` Vec sharing the stats; **unconfigured clusters register ZERO OD stats** (the regression gate); BEHAVIOR_CONTRACT.md 7-row block + `allowlist_envoy_only` paragraph. 4 `#[tokio::test]`s (incl the false-pass-proof presence-then-value gauge test). Spec ✅ + code-quality **Approved** (2 Minors deferred).
  - `5bbb37c` — **Task 6**: D8.2 `parse_bootstrap` fuzz seed `cluster_outlier_detection.yaml` + `.gitignore` allow-list + `fuzz_corpus_seeds_parse_or_reject_cleanly` SUCCESS-array entry — all three in ONE commit; seed git-TRACKED + SUCCESS-array (not reject); corpus 21 → 22. Spec ✅ + code-quality **Approved** (2 cosmetic Minors deferred).
- **State-4 evidence anchor:** Task 7 verification commit `7730e446` (HEAD), CI run **`26639712806`** `completed / success` (confirmed via `gh run list --branch main --limit 1` at this session); `git rev-parse HEAD origin/main` → both `7730e446`.
- **Reviewed:** 2026-05-29.

## §1. Verdict

**Approved with M-track follow-ups.**

Phase 14.1 lands its 5 carved parent-14 deliverables (D1 envoy-config `OutlierDetection` schema — 5 `Option<_>` fields, `deny_unknown_fields`, derives byte-identical to the on-disk `Cluster` cascade [`Debug, Serialize, Deserialize, PartialEq`, NO `Clone`, WITH `Serialize` for `/config_dump`]; D2 validator — `validate_outlier_detection` called for every cluster in the per-cluster `validate()` loop with no bypass, 3 `cluster`-contexted `ConfigError` variants, threshold/timing/percent checks all test-locked, empty `{}` accepted, panic-free on the fuzzed surface; D3 `EndpointEjection` — a hand-rolled per-endpoint state machine with a status-driven classifier [5xx → consecutive_5xx; 502/503/504 → also consecutive_gateway_failure; non-5xx → reset both], swap-edged idempotent eject/un-eject, `Relaxed` atomics, initial never-ejected; D5 LB-integration — `pick()`'s fast path verified **byte-for-byte the phase-02 round-robin** [the §5.3 inert-when-unconfigured invariant — the load-bearing regression-equivalence proof], the slow path AND-composing `is_healthy() && !is_ejected()` with each `None` filter vacuously true + the 12.1 strictly-below panic threshold preserved + empty-eligible → `None`, plus `Cluster::record_response`'s cap-enforcement [integer-floor `floor(N*pct/100)`, overflow re-fires per detection-tick, 5xx-wins-ties] declared but uncalled at 14.1; D6 — the 7-name minimum-viable stat subset [1 Gauge + 6 Counters per ADR-0041 §6.2 item-2] registered only when `outlier_detection` is configured, zero OD stats on unconfigured clusters, the BEHAVIOR_CONTRACT 7-row block + the `allowlist_envoy_only` paragraph; plus the D8.2 `parse_bootstrap` fuzz seed extending the corpus 21 → 22) substantively and cleanly through the existing envoy-config + envoy-cluster + envoy-stats foundations.

The cross-phase architectural invariants from the parent-14 SPEC §5 all hold: `EndpointEjection` STATE lives in `envoy-cluster` (new `ejection.rs` module — NOT a new crate), so `pick()` reads it cycle-free; the state machine + classifier + cap check are hand-rolled per D-3.2 atop std `AtomicBool`/`AtomicU32` + `envoy_stats` primitives; **zero new top-level Cargo deps + zero new workspace path-deps** (`git diff Cargo.toml Cargo.lock 9a56e85..5bbb37c` is empty); no `unsafe` introduced (`#![forbid(unsafe_code)]` continues to bind at every touched crate root per D-3.8); no H2-framing-path touch (h2spec ≥95% holds vacuously). **NO ADR landed in the 14.1 lifecycle** (PLAN lock-in #2 — 14.1 introduces no new crate, no foundations grant, no wire-level contract revision; the `EndpointEjection` `Relaxed`-ordering choice is covered by the existing `cluster.rs` cursor precedent + the 12.1 `EndpointHealth` precedent). The DECISIONS.md ledger head is **ADR-0041** (count 42); next available **ADR-0042**. BEHAVIOR_CONTRACT.md extended with the 7-row outlier-detection block at the Task 5 commit `63188e4` per the 06.x → 13.2 cadence (the rows land at the task where the surface is first wired).

**The foundation-slice structure is deliberate and correct.** `Cluster::record_response` + `EndpointEjection::eject` + `try_un_eject` + `EjectionDecision::any` have **no production caller at 14.1** — verified by grep across `crates/` (only test callers). This is BY DESIGN per the PLAN: the response-receipt hook (14.2 D4) and the ejection sweeper (14.2 D7) are the production callers. The uncalled methods are the documented 14.2 hand-off contract, NOT dead code — they are exercised thoroughly by 14.1 unit tests (classifier-per-status-class, threshold-crossing, reset, eject/un-eject idempotency, cap-overflow re-fire, 5xx-wins-ties at the unit level). Because `is_ejected()` always returns `false` at 14.1 (no caller transitions the state), every `pick()` on every fixture-configured cluster takes the inert path, and the 21 Docker-gated fixtures stay green — the entire reason 14.1 needs no new fixture, and it holds.

**Zero Critical findings. Zero Important findings.** The state-5 review dispatched a **3-cluster subagent pass** over the reviewed range, sliced by deliverable cohesion matching the diffstat (Cluster A = the envoy-config schema + validator, Tasks 1–2; Cluster B = the envoy-cluster `EndpointEjection` + `pick()` + `record_response`, the runtime core, Tasks 3–4; Cluster C = the from_bootstrap stats wiring + BEHAVIOR_CONTRACT + fuzz seed, Tasks 5–6). Each cluster reviewer read the actual on-disk code (`git diff` + `Read` + `Grep`) per the standing precedent-verification discipline, not the PROGRESS narrative; Cluster B additionally diffed `pick()` against `9a56e85:cluster.rs` to verify the §5.3 byte-for-byte invariant and grepped for production callers of the ejection methods to confirm there are none. **Cluster A returned `Ready to merge: Yes` (zero Critical + zero Important); Cluster C returned `Ready to merge: Yes` (zero Critical + zero Important); Cluster B returned `Ready to merge: With fixes` raising two items as "Important"** — both of which the controller calibrates DOWN to Minor on examination (rationale in §3 and §7): (i) the `Relaxed`-ordering single-writer premise is the exact analog of the 12.1 M2 finding (a forward dependency the 14.2 review owns, NOT a 14.1 defect — no production writer exists), and (ii) the `cluster_record_response_picks_5xx_detector_on_ties` integration test is weak-but-not-false and the 5xx-wins-ties contract IS genuinely guarded at the unit level (`ejection.rs` `eject_increments_active_and_enforced_counters` asserts `enforced_consecutive_5xx == 1` and `enforced_consecutive_gateway_failure == 0`). **Nine Minor findings** (M1–M9) — all awareness-only / close-opportunistically. **Per `BOOTSTRAP_PROMPT.md` §5.2 the phase does NOT re-enter state 3** — no Critical or Important findings survive calibration; the verdict closes state 5 and the next session is the phase-14.1 state-6 close-out.

**Phase 14.1 is the third Upstream-robustness-family foundation-slice REVIEW.md** — the verdict + REVIEW shape continue the 06.x → 12.1 → 13.1 → 13.2 post-MVP standard: `Approved with M-track follow-ups` disposition; forward-carry inheritance for unexercised-pattern Minor findings; explicit calibration rationale per finding; full §7.5 6-gate re-attestation against the state-4 CI anchor. 14.1 engages NO carryforward closures (PLAN lock-in #24 — the 13.2 A-M2 stale-comment is unmoved; 14.1 touches no `envoy-http1`); it generates 9 new Minor carryforwards (none gating).

## §2. Strengths

1. **The §5.3 inert-when-unconfigured invariant is genuinely byte-for-byte — the single most important correctness property, and it is verified.** Cluster B diffed the new `pick()` against `9a56e85:crates/envoy-cluster/src/cluster.rs` and confirmed the fast-path guard (`cluster.rs:188-192`) — `if self.endpoint_health.is_none() && self.outlier_detection.is_none() { let i = self.cursor.fetch_add(1, Ordering::Relaxed); return Some(self.endpoints[i % total]); }` — reproduces the pre-14.1 round-robin instruction-for-instruction; only the *entry condition* gained the second `is_none()` conjunct. A cluster with no `outlier_detection` (and no `health_checks`) ⇒ `pick()` is exactly phase-02 round-robin ⇒ the 21 existing fixtures see ZERO behavior change. `pick_inert_when_neither_filter_configured` locks this behaviorally, and the 21-fixture Docker-gated suite proves it end-to-end (acceptance gate (b)). This is the entire reason 14.1 needs no new fixture, and it holds.

2. **The schema is a faithful, on-disk-verified match to the established `Cluster` cascade.** Cluster A confirmed `OutlierDetection` (`bootstrap.rs:1218`) carries `#[derive(Debug, Serialize, Deserialize, PartialEq)]` + `#[serde(deny_unknown_fields)]`, NO `Clone` — byte-identical to `Cluster` (`bootstrap.rs:54`) and `CircuitBreakers` (`bootstrap.rs:105`). All 5 fields are `Option<_>` with `#[serde(default)]`. `deny_unknown_fields` rejection is **proven, not assumed**: `outlier_detection_rejects_unknown_fields` feeds `success_rate_minimum_hosts` and asserts `ConfigError::Yaml`. The struct is re-exported from `lib.rs:20`.

3. **The validator runs for every cluster and is panic-free on attacker-controlled config.** Cluster A confirmed `validate_outlier_detection(cluster)` sits in the per-cluster `for` loop (`bootstrap.rs:1783-1785`) with no `continue` bypass. It contains no `unwrap`/`expect`/indexing/arithmetic; `parse_duration` parses via `u64` + `Duration::from_*` with no overflow path. The duration guard rejects BOTH `0.5s` (parse failure) and `0s` (zero) to the correct `field`; the empty-block guard (`let Some(od) = ... else { return Ok(()) }` + all-`if let Some` arms) reaches `Ok(())` for an all-`None` block. Boundary coverage is genuine — separate tests assert `max_ejection_percent` 0 (accept), 100 (accept), 101 (reject), zero-duration reject for both timing fields, and the empty-block accept, each matching the exact variant + `cluster`/`field`/`value` context. Important because this surface is exercised by the `parse_bootstrap` fuzz target.

4. **The classifier is exactly right on every status boundary.** Cluster B verified `ejection.rs:134` uses `status / 100 == 5` (covers 500–599) and `ejection.rs:139` uses `matches!(status, 502..=504)` for the gateway-failure subset — an inclusive range, so 502/503/504 tick both detectors, 500/501/505 tick only consecutive_5xx, and any non-5xx (incl 499) hits the reset arm (`ejection.rs:163-168`) resetting **both** counters. Tests cover 500-only, 502/503/504, and 4xx-reset. The already-ejected short-circuit (`ejection.rs:131-133`) returns `EjectionDecision::default()` before any counter mutation (verified: `ejections_detected_consecutive_5xx` does not re-tick while ejected).

5. **eject/un-eject are swap-edged, idempotent, and convergent.** `ejection.rs:177` `swap(true)` returns the prior value; the gauge/counter increments fire only `if !was`. `try_un_eject` `swap(false)` decrements + resets only `if was`. The triple-eject test asserts the `ejections_active` gauge stays 1; the un-eject-when-not-ejected test asserts no spurious decrement. The 5xx-wins-ties detector selection (`cluster.rs:289-293`) is genuinely covered at the unit level by `eject_increments_active_and_enforced_counters` (asserts `enforced_consecutive_5xx == 1`, gateway `== 0`) and its mirror `eject_for_gateway_failure_increments_the_gateway_counter`.

6. **The AND-composition and cap arithmetic are correct.** `is_eligible(i)` (`cluster.rs:196-207`) computes `healthy && not_ejected`, each filter defaulting to `true` when its `Option` is `None`; the strictly-below panic comparison (`cluster.rs:212`) and the `eligible_idx.is_empty() → None` arm (`cluster.rs:225`) are preserved from 12.1; the 4-endpoint AND test exercises all four quadrants. `cap_count = (total * max_ejection_percent) / 100` is integer-floor; `cluster_record_response_honors_max_ejection_percent_cap` asserts overflow goes 1→2 across two distinct cap-blocked endpoints (the per-detection-tick re-fire, not once-per-host).

7. **The D6 stats wiring is exactly 7 names, correct kinds, zero leakage.** Cluster C cross-checked `from_bootstrap`'s OD arm against `EndpointEjectionStats`: exactly 1 Gauge (`ejections_active`) + 6 Counters via the `mk_gauge`/`mk_counter` closures — no 8th, none missing. `from_bootstrap_omits_outlier_detection_stats_when_unconfigured` asserts `!name.contains("outlier_detection")` across the full snapshot (the regression gate (b)). Borrow ordering is sound (`endpoints_state` built from `endpoints.iter()` before the move into the `Cluster` literal); registration errors map to `ClusterError::StatsRegistration`; defaults 5/5/10 are validated by `from_bootstrap_outlier_detection_uses_envoy_defaults_when_omitted`.

8. **The positive registration test is genuinely false-pass-proof.** Cluster C confirmed `from_bootstrap_outlier_detection_active_gauge_reads_zero_at_construct` asserts PRESENCE via `registry.snapshot().iter().any(...)` FIRST, then reads the value via the idempotent `register_gauge`, with an inline comment documenting why (the idempotent register would mask a missing `from_bootstrap` registration). Matches the 12.1 `membership_healthy` hardening exactly — a recovered-in-phase discipline carried into 14.1 by construction.

9. **The fuzz corpus seed honors the 3-files-same-commit discipline.** Cluster C verified via `git show --stat 5bbb37c` that all three edits land in one commit — the seed `cluster_outlier_detection.yaml`, the `.gitignore` allow-list line, AND the `fuzz_corpus_seeds_parse_or_reject_cleanly` SUCCESS-array entry. `git check-ignore` exits non-zero (tracked); `git ls-files` lists it; it is in the SUCCESS (not reject) array; the structure is valid (all 5 fields at Envoy v3 defaults) and non-redundant (the only OD-cluster YAML in the 22-seed corpus). Corpus 21 → 22.

10. **The BEHAVIOR_CONTRACT block is format-consistent and honestly scoped.** Cluster C confirmed the `**14.1 entries (outlier detection):**` heading + 3-column `| Stat name | Equivalence | Rationale |` table mirrors the 12.1 `**12.1 entries (active health checking):**` sub-table precisely; the Equivalence cells honestly scope "reads 0 at 14.1"; the `allowlist_envoy_only` paragraph defers the additional Envoy-side names per the 12.2/13.x precedent (the prose count is off by one — see M8, no differential impact).

11. **Zero new Cargo deps; zero new path-deps; zero ADRs; zero `unsafe`; D-3.7/D-3.9 unaffected; D-3.2 inviolate.** `git diff Cargo.toml Cargo.lock 9a56e85..5bbb37c` is empty. `rust-toolchain.toml` + `ENVOY_TARGET.md` untouched. All touched crate roots keep `#![forbid(unsafe_code)]`. NO ADR landed (lock-in #2); ledger head stays ADR-0041, next available ADR-0042.

## §3. Issues

### Critical (must fix before phase commit)

None.

### Important (close in 14.1 or carry forward with named owner phase)

None. Cluster B raised two items at "Important" severity; the controller calibrates both DOWN to Minor (M4 and M5 below). The calibration rationale is explicit per finding (§7): M4 is the exact 12.1-M2 analog — a forward dependency the 14.2 review owns, not a 14.1 defect, because no production writer exists in 14.1; M5 is a weak-but-not-false integration test whose underlying 5xx-wins-ties contract IS genuinely guarded at the unit level (`ejection.rs` `eject_increments_active_and_enforced_counters`), so the marginal coverage loss is small and reopening state 3 to strengthen a ~10-LoC test assertion would force a full state-4 re-gate without a commensurate correctness gain.

### Minor (awareness-only / track forward)

**M1. `crates/envoy-config/src/bootstrap.rs` `validate_outlier_detection` uses `v < 1` on `u32` threshold fields where `== 0` reads marginally clearer** (`bootstrap.rs:2628`, `:2637`). Functionally identical on an unsigned type; clippy did NOT fire `absurd_extreme_comparisons` under `-D warnings`; `< 1` matches the lock-in #7 wording ("must be `>= 1`"). **Disposition:** awareness-only style nit; **no action recommended** (this exactly mirrors the 12.1 M1 finding — the project has a standing no-action disposition for it). Named owner: none.

**M2. No `Serialize` round-trip test for `OutlierDetection`.** The struct carries `Serialize` for the `/config_dump` path, but no test serializes a populated `OutlierDetection` and asserts field-name/skip output. **Why it matters:** the `/config_dump` contract is the stated reason `Serialize` is on the derive. **Why Minor:** the derive is mechanical, the parse tests exercise `Deserialize`, and the `Cluster` cascade's `/config_dump` is already covered at the 08.1 level. **Disposition:** opportunistic ~3-LoC test add if a future phase touches this schema. Named owner: none.

**M3. `crates/envoy-config/src/bootstrap.rs` `validate_outlier_detection` doc comment references a stale line number** — it cites `parse_duration` at `bootstrap.rs:2401`, but the function is at `bootstrap.rs:2459`. **Why it matters:** D-3.4 stranger-readability; inline line-number references rot through phase additions (this same drift was a PLAN-write SPEC correction — the SPEC sketched `:2289`). **Why Minor:** purely cosmetic; a name reference alone suffices. **Disposition:** ~1-LoC fix (drop the line number) opportunistically when this validator is next touched (14.2 does NOT touch envoy-config). Named owner: none.

**M4. `crates/envoy-cluster/src/ejection.rs` `EndpointEjection` `Relaxed`-ordering correctness rests on the single-writer-per-endpoint premise, which is documented (the inline contract note + lock-in #11) but NOT enforced by the type/API — a forward dependency the 14.2 review MUST verify.** `record_response`, `eject`, and `try_un_eject` all use `Ordering::Relaxed` on a `&self` receiver, so nothing in the type system prevents two threads from concurrently mutating the same `EndpointEjection`; the read-modify-write in `record_response` (`fetch_add` then conditional `inc`) and the swap-edges in eject/un-eject are race-free ONLY under single-writer-per-endpoint. **Why it matters:** when 14.2 wires the response-receipt hook (D4) AND the ejection sweeper (D7), both touch an endpoint's `EndpointEjection`; if they are not serialized (per-endpoint lock, actor, or single-task ownership), the gauge/counter edges can double-fire or tear and the `ejected` flag can lost-update. **Why it does NOT rise above Minor for 14.1:** there is no production writer in 14.1 (verified by grep — only test callers), `pick()`'s read-side `is_ejected()` is a single un-tearing `AtomicBool` load, and the premise is the recommended design — this is a sanctioned 14.2 handoff, the exact analog of the 12.1 M2 finding (which 12.1 also classified Minor and named the 12.2 review as owner). **Disposition:** **close at the 14.2 review** — verify the D4 hook + D7 sweeper instantiate a single serialized writer per endpoint before relying on `Relaxed`; rework to a CAS loop / `fetch_update` only if a many-writer topology ever becomes necessary. Named owner: **phase 14.2 review**. The most substantive of the nine.

**M5. `crates/envoy-cluster/src/cluster.rs` `cluster_record_response_picks_5xx_detector_on_ties` does not actually assert the detector choice (weak integration test + a vestigial binding).** The test ejects via a 503 (which crosses both detectors), asserts `is_ejected()`, then binds `let stats_active = &od.endpoints[0]; let _ = stats_active;` (`cluster.rs:2238-2239`) — a no-op that never reads `ejections_enforced_consecutive_5xx` vs `..._gateway_failure`. The test name promises "picks 5xx detector on ties" but only verifies that ejection occurred. **Why it matters:** the integration-level tie assertion is vacuous, so a future regression in `record_response`'s detector-passing to `eject` would not be caught at this seam. **Why it does NOT rise above Minor:** the 5xx-wins-ties behavior is real (`cluster.rs:289-293`) AND genuinely unit-tested at the `eject` level (`ejection.rs` `eject_increments_active_and_enforced_counters` asserts `enforced_consecutive_5xx == 1` and `enforced_consecutive_gateway_failure == 0`); `record_response → eject(detector)` is a thin delegation; the test is weak, not false, and not passing-on-wrong-behavior. This was a known by-design choice at the Task-4 two-stage review (the helper deliberately returns only the `EndpointEjection` Arcs, not the `EndpointEjectionStats`, so the integration test cannot reach the per-detector counters). **Disposition:** **close at 14.2 D4** — when `record_response` gets its production caller (the response-receipt hook), the integration path matters more; extend `mk_handle_with_health_and_ejection` to also return the shared `EndpointEjectionStats` and strengthen the assertion to `enforced_consecutive_5xx == 1 && enforced_consecutive_gateway_failure == 0` (~10 LoC), and drop the vestigial `let _ = stats_active;`. Named owner: **phase 14.2**. (Raised independently by Cluster B [as Important — calibrated down] and Cluster C [as Minor].)

**M6. `crates/envoy-cluster/src/cluster.rs` `max_ejection_percent = 0` edge ("0% ⇒ eject nothing; overflow on the first crossing") is untested and uncommented at the cap site** (`cluster.rs:286`). With `max_ejection_percent = 0`, `cap_count = 0`, so the first crossing hits `active_count (0) >= cap_count (0)` → overflow, never ejecting. This is a surprising-but-deliberate edge. **Why it matters:** a stranger reading the cap site could misread "0% = eject nothing" as a bug. **Why Minor:** the behavior is internally consistent with "cap met → overflow" and no fixture/spec requires a different 0% semantic at 14.1. **Disposition:** opportunistic ~5-LoC test + a one-line comment at the cap site citing §6.2 item-4, naturally folded in at 14.2 D4 where the cap path becomes production-reachable. Named owner: phase 14.2.

**M7. `crates/envoy-cluster/src/cluster.rs` `cap_count` consumes `max_ejection_percent` without a `debug_assert!` documenting the validator's `<= 100` guarantee** (`cluster.rs:286`, `(total * od.max_ejection_percent as usize) / 100`). `total` is bounded by endpoint count and `max_ejection_percent` by the D2 validator (`<= 100`), so no realistic overflow. **Why Minor:** correct as written; the assertion would only document an invariant the validator already enforces. **Disposition:** opportunistic `debug_assert!(od.max_ejection_percent <= 100)` at the consumption site if a future phase touches it. Named owner: none.

**M8. `docs/envoy-rust/BEHAVIOR_CONTRACT.md` `allowlist_envoy_only` paragraph claims "14" deferred Envoy-side names but enumerates only 13** (`BEHAVIOR_CONTRACT.md:177`): 5 `_detected_/_enforced_` pairs (= 10) + 3 legacy aliases (`ejections_total`, `ejections_consecutive_5xx`, `ejections_success_rate`) = 13, against the stated "remaining 14". The count "14" is itself consistent with the §2.1 tally (21 total Envoy names − 7 emitted = 14). **This defect is inherited verbatim from `14.1-.../SPEC.md:50`** (identical "14" + identical 13-name list), which was ratified by ADR-0041 — the contract author faithfully transcribed the canonical SPEC. **Why it matters:** the canonical contract's self-count is off by one. **Why it does NOT rise above Minor:** **zero differential-equivalence impact** — `allowlist_envoy_only` keys on Envoy's actual fixture-time emission set, NOT on this prose list; the 7 emitted names + their kinds are correct and value-exact. **Why I did NOT fix it in this docs-only review commit:** determining the dropped 14th name authoritatively requires either re-running the §6.2 Docker verification (forbidden — locked at `0a4d225` by ADR-0041) or an empirical re-touch of the Envoy emission set, which happens naturally at 14.2 D8.1 where fixture-0022's `expectations.yaml` engages the real Envoy stat tree. **Disposition:** **reconcile at 14.2 D8.1** — when the fixture's `expectations.yaml` empirically lists the deferred names, correct the count/enumeration in BOTH the SPEC §2.1 and the contract (~1 LoC each). Named owner: phase 14.2.

**M9. `crates/envoy-cluster/src/cluster.rs` `pick()` rustdoc cites both "§5.4 / §5.3"** — a minor spec-cite redundancy. **Why Minor:** harmless; both citations are accurate (§5.3 inert invariant, §5.4 reused exclusion seam). **Disposition:** no action recommended. Named owner: none.

**Confirmed-closed / non-issues at task time (not live carryforwards):**
- **Task-3 `502 | 503 | 504` → `502..=504` reconciliation** — a clippy `manual_range_patterns` fix under `-D warnings`; behavior-identical (same 502/503/504 set). Not a finding.
- **Task-5 `mk_counter`/`mk_gauge` closure idiom** vs the 5 pre-existing inline per-cluster registrations — Cluster C confirmed the closure approach is the better one (DRYs the 7 registrations + the error mapping); a localized two-idiom split, not a defect. No action.
- **Task-5 `unwrap_or(5/5/10)` magic numbers** — adequately documented inline with §6.2 item-1 citations; named consts become worthwhile only if 14.2 needs the same defaults. No action.
- **Task-6 seed `static_resources:`-before-`admin:` key order** — matches `cluster_health_check.yaml`; parse-irrelevant. No action.
- **Task-4 `pick()` iterates the eligible set twice** (`.count()` then `.collect()`) — defensible for small clusters + matches the 12.1 M3 pattern (standing no-action). No action.
- **Task-4 index-aligned-Vec coupling** between `OutlierDetectionState.endpoints` + `Cluster.endpoints` + `endpoint_health` — enforced by construction, matches the 12.1 health pattern + documented alignment contract. No action.

## §4. Carryforward inventory

For each new Minor: closure plan. (No Critical or Important items.)

| ID | Severity | Item | Closure plan |
|---|---|---|---|
| **M1** | Minor | `validate_outlier_detection` `< 1` vs `== 0` on `u32` thresholds | **No action recommended** (clippy clean; matches lock-in #7 wording; mirrors the standing 12.1 M1 no-action). Named owner: none. Non-blocking. |
| **M2** | Minor | No `Serialize` round-trip test for `OutlierDetection` | **Opportunistic** ~3-LoC test if the schema is next touched. Named owner: none. Non-blocking. |
| **M3** | Minor | `validate_outlier_detection` doc cites stale `bootstrap.rs:2401` (actual `:2459`) | **Opportunistic** ~1-LoC fix (drop the line number) when the validator is next touched. Named owner: none. Non-blocking. |
| **M4** | Minor | `EndpointEjection` `Relaxed` correctness rests on single-writer-per-endpoint, not type-enforced | **Close at the 14.2 review** — verify the D4 hook + D7 sweeper instantiate a single serialized writer per endpoint before relying on `Relaxed`; CAS-loop rework only if many-writer becomes necessary. Named owner: **phase 14.2 review**. Non-blocking for 14.1 (no live writer exists). The exact 12.1-M2 analog. |
| **M5** | Minor | `cluster_record_response_picks_5xx_detector_on_ties` weak integration test + vestigial `let _ = stats_active;` | **Close at 14.2 D4** — expose `EndpointEjectionStats` from the test helper; assert `enforced_consecutive_5xx == 1 && enforced_consecutive_gateway_failure == 0`; drop the vestigial binding (~10 LoC). Behavior already unit-tested at `eject` level. Named owner: **phase 14.2**. Non-blocking. |
| **M6** | Minor | `max_ejection_percent = 0` edge untested + uncommented at the cap site | **Opportunistic at 14.2 D4** — ~5-LoC test + a one-line cap-site comment citing §6.2 item-4. Named owner: phase 14.2. Non-blocking. |
| **M7** | Minor | `cap_count` lacks a `debug_assert!(max_ejection_percent <= 100)` at the consumption site | **No action recommended** (validator already enforces `<= 100`); opportunistic 1-LoC assert if touched. Named owner: none. Non-blocking. |
| **M8** | Minor | `BEHAVIOR_CONTRACT.md` `allowlist_envoy_only` claims "14" deferred names, enumerates 13 (inherited from the ADR-0041-ratified SPEC §2.1) | **Reconcile at 14.2 D8.1** — correct the count/enumeration in both SPEC §2.1 and the contract when fixture-0022's `expectations.yaml` empirically lists the deferred names (~1 LoC each). Zero differential impact (`allowlist_envoy_only` keys on actual emission). Named owner: phase 14.2. Non-blocking. |
| **M9** | Minor | `pick()` rustdoc §5.4/§5.3 cite redundancy | **No action recommended**. Named owner: none. Non-blocking. |

**None of the new carryforwards gate the state-6 close-out OR any future phase.** M4 is the most substantive — a forward correctness-verification dependency the 14.2 review must discharge before the D4 hook + D7 sweeper rely on `Relaxed`. M5 + M6 + M8 are 14.2-engaged fold-ins (M5 a ~10-LoC test strengthening where the integration path goes live; M6 a cap-edge test; M8 a doc-count reconciliation at the fixture). M1 + M2 + M3 + M7 + M9 are pure style/doc/test-polish with no named owner.

**Standing carryforwards inherited but NOT closed in 14.1** (per PROGRESS Task 1 preamble carryforward disposition + SPEC §6.9; 14.1 engaged NONE — lock-in #24, it touches no `envoy-http1` and ships no fixture/harness):

- **13.2 A-M2** (the stale `tokio::sync::Mutex` comment at `crates/envoy-http1/src/pool.rs:322`) — **NOT engaged in 14.1** (foundation slice; no `envoy-http1` touch). The A-M2 close opportunity defers to **14.2 D4**, which DOES touch `envoy-http1::router`.
- **13.2 REVIEW 13 Minors + 13.1 REVIEW 9 Minors + 12.2 REVIEW 11 active Minors + 12.1 REVIEW M1–M4 (esp M2 the `EndpointHealth` single-writer premise — verified at the 12.2 review) + phase-11 M1–M8 + 10 M2/M3/M4/D1/D2/T1 + 09 M1/D1/D2/T1/T2/T3 + 08.x/07.2/06.x/05.x residuals + 04.1 REVIEW M5/M9 Cargo.lock cadence** — carry forward unchanged; none engaged by the 14.1 config + state-machine + LB-integration + stats surface. Full inventories in the respective phase REVIEW.md §4/§5 sections.
- **ADR-0028** (H1-listener × H2-cluster dispatch deferral) — **REMAINS OPEN** per ADR-0039 Consequences; the named owner is a follow-up foundations-pivot phase, NOT 14.1 (and NOT 14.2).
- **04.1 REVIEW M5 / M9 Cargo.lock cadence ratification** — carries forward unchanged; zero new top-level Cargo deps, zero new workspace path-deps, Cargo.lock diff at the reviewed range is EMPTY (`git diff Cargo.toml Cargo.lock 9a56e85..5bbb37c`).

## §5. Disposition decisions

**Closed in 14.1:** **none.** 14.1 engaged no carryforward (lock-in #24). The 13.2 A-M2 stale-comment is unmoved (14.1 touches no `envoy-http1`); this REVIEW.md ratifies that 14.1 made no carryforward progress (correct per the ADR-0040 split — 14.1 ships no fixture/harness and no router/pool touch).

**Continuing carryforwards by named owner phase:** see §4 standing carryforwards inventory above.

**New carryforwards generated by 14.1** (9 Minor M1–M9 — none with hard-gated owners):

- **M4** (the `EndpointEjection` `Relaxed` single-writer premise) — **close at the 14.2 review** (verify the D4 hook + D7 sweeper instantiate one serialized writer per endpoint before relying on `Relaxed`). The most substantive; a forward dependency, NOT a 14.1 defect. Named owner: **phase 14.2 review**.
- **M5** (the weak tie-detector integration test) — close at 14.2 D4 where the integration path goes live. Named owner: phase 14.2.
- **M6** (the `max_ejection_percent = 0` edge) — close opportunistically at 14.2 D4. Named owner: phase 14.2.
- **M8** (the `allowlist_envoy_only` count/enumeration mismatch) — reconcile at 14.2 D8.1 in both SPEC + contract. Named owner: phase 14.2.
- **M1 / M2 / M3 / M7 / M9** — style/doc/test-polish; no action recommended / opportunistic; no named owner.

**Process note (awareness-only, no carryforward).** The phase-14.1 state-3 arc landed clean on the first push per task — no mid-execution corrective sequence, NO inline ADR (lock-in #2), and only ONE PLAN-code-vs-clippy reconciliation (`502 | 503 | 504` → `502..=504`, behavior-identical). The §6.2 empirical verification was locked at the parent-14 split `0a4d225` by ADR-0041; the 14.1 PLAN-writer baked the locked facts into the lock-ins without re-running Docker, and the implementation matched them with no surprise. The two-stage per-task review surfaced only Minor findings (zero Critical / zero Important / no fold-ins across all 6 tasks), so the state-5 review inherited a clean surface. The state-5 controller calibrated Cluster B's two "Important" flags DOWN to Minor (M4 + M5) with explicit per-finding rationale (§3 / §7) — neither is a 14.1 correctness defect; per §5.2 the phase does NOT re-enter state 3. Awareness-only; the doctrine works as designed.

## §6. Phase-done gate (§7.5) verification

Per `BOOTSTRAP_PROMPT.md` §7.5 and 14.1 SPEC §1 acceptance signal (a)–(f), re-attested at the Task 7 state-4 evidence anchor — PROGRESS Task 7 + the state-4 STATE.md narrative cite CI run **`26639712806`** at HEAD `7730e446` (the docs-only Task 7 commit; since Task 7 carries no code, this IS the verified-and-reviewed code state), conclusion `completed / success`, created `2026-05-29T13:21:48Z`, wall `4m22s`. **Confirmed `completed / success` at this state-5 review via `gh run list --branch main --limit 1`**; `git rev-parse HEAD origin/main` → both `7730e446`. The state-4 session also performed a full LOCAL run (Docker available) corroborating every gate.

**(a) New differential fixture green.** ✓ — **vacuously satisfied: 14.1 ships NO new differential fixture by design** (the 05.1 / 07.1 / 12.1 foundation-slice pattern; the configured-OD path stays inert-and-unexercised until 14.2 wires the response-receipt hook + sweeper + fixture together — `is_ejected()` always returns `false` at 14.1 because no caller transitions the state). Fixture `0022-upstream-outlier-detection-consecutive-5xx` lands in 14.2.

**(b) Pre-existing 21 differential fixtures (`0001-0021`) green simultaneously.** ✓ — the load-bearing 14.1 differential proof. All 21 (`0001-tcp-echo` through `0021-upstream-h2-connection-pooling`) GREEN simultaneously in CI run `26639712806` AND re-confirmed LOCALLY: `cargo test -p differential -- --include-ignored` → 0 failed, all 21 Docker-gated fixture binaries `... ok` bilaterally vs `envoyproxy/envoy:v1.33.0` (Docker 28.0.4). **21/21 in one run** with the outlier-detection machinery present-but-inert — no fixture configures `outlier_detection`, so every `pick()` takes the both-filters-None fast path = byte-for-byte phase-02 round-robin (the §5.3 invariant, verified on-disk by Cluster B against `9a56e85:cluster.rs`). Both documented-flake-prone binaries (`access_log_file_sink`, `upstream_h2_connection_pooling`) ran green this pass.

**(c) `h2spec` ≥95%.** ✓ — held vacuously at the parent-05 baseline **99.31%**. 14.1 touched zero H2-framing code (only envoy-config, envoy-cluster, docs, fuzz corpus) — confirmed by the diffstat (no `envoy-http2` framing-path file touched). No re-run required; the baseline is unaffected.

**(d) `parse_bootstrap` fuzz target clean for short-budget CI run.** ✓ — on the extended 22-seed corpus. The Task 6 `cluster_outlier_detection.yaml` seed is in the corpus + listed in the SUCCESS array (Task 6 commit `5bbb37c`, all three coupled edits verified on-disk by Cluster C; `git check-ignore` exits non-zero, `git ls-files` lists it). Local re-run: `cargo +nightly fuzz run parse_bootstrap --fuzz-dir crates/envoy-config/fuzz -- -runs=200000` → `Done 200000 runs in 19 second(s)`, 0 crashes, exit 0. No new fuzz target ships in 14.1 (Task 6 extended the existing `parse_bootstrap` corpus per the 09/10/11/12.1/13.x cadence).

**(e) Stable-toolchain gates clean (fmt / clippy / build / test / deny).** ✓ — all five clean in CI run `26639712806` + reproduced in the local re-run: `cargo build --workspace --all-targets` (Finished); `cargo clippy --workspace --all-targets --all-features -- -D warnings` (no warnings — incl. the lock-in #4 dead-handle check: every OD stat handle is incremented by 14.1-tested code paths, no 0-forever registered counter); `cargo fmt --all -- --check` (clean); `cargo test --workspace` (**922 passed / 0 failed / 2 ignored** across 76 binaries — the 12.1/13.x baseline + the 14.1 new tests: envoy-config +13, envoy-cluster +27 [Task 3 ×13, Task 4 ×10, Task 5 ×4]); `cargo deny check` (`advisories ok, bans ok, licenses ok, sources ok`; the two `license-not-encountered` warnings for `Unicode-DFS-2016` / `Zlib` are pre-existing unmatched-allowance warnings, not failures). The documented local-only `upstream_h2_connection_pooling` nested-cargo-under-`cargo test --workspace` build-lock-contention flake (first-run only; re-ran green in isolation + on the warm full re-run; CI does not see it; NOT a 14.1 regression — 14.1 touched no envoy-bin/helper/H2 code) is the kin of the 13.2 C-M3 local-flake note.

**(f) REVIEW.md verdict** lands at state-5 in this session per the `SKILL_ROUTING.md` state-5 transition. **Verdict per §1: Approved with M-track follow-ups** (zero Critical; zero Important; 9 Minor M1–M9 — all awareness-only / close-opportunistically; M4 a 14.2-review forward dependency; M5/M6/M8 14.2-engaged fold-ins; M1/M2/M3/M7/M9 no-action / opportunistic; none gates the state-6 close-out). Gate (f) closes at this REVIEW.md verdict.

**All six §7.5 gates disposition: (a) PASS (vacuous, no new fixture) / (b) PASS / (c) PASS (vacuous, no H2 touch) / (d) PASS / (e) PASS / (f) CLOSE-at-this-REVIEW.md.**

## §7. Per-cluster code-quality reviewer finding dispositions

The phase-14.1 state-5 review ran a 3-cluster subagent pass over the reviewed range `0170d1e..5bbb37c` (the 6 implementation task commits; the Task 7 state-4 docs-only commit excluded). Each cluster reviewer read the actual on-disk code (`git diff` + `Read` + `Grep`) per the standing precedent-verification discipline, not the PROGRESS narrative. Disposition per cluster:

**Cluster A — Tasks 1–2 reviewer (envoy-config schema + validator) — Zero Critical + Zero Important + 3 cosmetic Minors. Verdict: Ready to merge: Yes.** Verified on-disk: `OutlierDetection`'s 5 fields + derives byte-identical to the `Cluster`/`CircuitBreakers` cascade (NO `Clone`; `-D warnings` build clean); `deny_unknown_fields` rejection proven by `outlier_detection_rejects_unknown_fields`; the 3 `ConfigError` variants each `cluster`-contexted; `validate_outlier_detection` CALLED for every cluster with no `continue` bypass; the duration guard rejects BOTH `0.5s` and `0s` to the right `field`; empty `{}` accepted; `max_ejection_percent` boundaries 0/100/101 test-locked; the validator is panic-free on the fuzzed surface (no unwrap/index/arithmetic; `parse_duration` overflow-free). Surfaced 3 cosmetic Minors → **M1** (`< 1` vs `== 0`, no action — clippy clean) + **M2** (no `Serialize` round-trip test) + **M3** (stale `:2401` doc line-number). **No residue rising above Minor.**

**Cluster B — Tasks 3–4 reviewer (`EndpointEjection` + `pick()` + `record_response`, the runtime core) — Zero Critical + 2 items raised at Important (both calibrated DOWN to Minor) + 3 micro-Minors. Verdict (reviewer): Ready to merge: With fixes; (controller): Ready to merge: Yes.** Verified on-disk: the §5.3 inert fast-path is byte-for-byte the pre-14.1 round-robin (diffed against `9a56e85:cluster.rs`); AND-composition vacuous-true for `None`; the classifier exact on every status boundary (`status/100==5`, `502..=504`); already-ejected short-circuit prevents counter mutation; non-5xx resets BOTH counters; eject/un-eject swap-edged + idempotent; cap arithmetic integer-floor + overflow re-fires per detection-tick; **grep confirmed NO production caller** of `record_response`/`eject`/`try_un_eject` (only tests — the documented 14.2 hand-off). Raised at Important → calibrated to **M4** (the `Relaxed` single-writer premise — a forward dependency the 14.2 review owns, the exact 12.1-M2 analog; the reviewer itself said "flag, not fix, at 14.1") + **M5** (the vacuous tie-detector integration test — but the 5xx-wins-ties contract IS unit-tested at the `eject` level, so weak-not-false). Micro-Minors → **M6** (`max_ejection_percent = 0` edge) + **M7** (`cap_count` `debug_assert!`) + **M9** (pick() rustdoc cite redundancy). **No residue rising above Minor after controller calibration** — see §3 Important for the explicit downgrade rationale.

**Cluster C — Tasks 5–6 reviewer (from_bootstrap stats + BEHAVIOR_CONTRACT + fuzz seed) — Zero Critical + Zero Important + 2 cosmetic Minors. Verdict: Ready to merge: Yes.** Verified on-disk (live `cargo test`): exactly 7 stat names with correct kinds (`ejections_active` Gauge; 6 Counters), no 8th/missing; the regression gate holds (`from_bootstrap_omits_outlier_detection_stats_when_unconfigured` asserts `!name.contains("outlier_detection")` — 5 OD tests pass); the positive test is false-pass-proof (snapshot PRESENCE before the idempotent value read); defaults 5/5/10 validated; borrow ordering sound; `ClusterError::StatsRegistration` mapping correct; all three fuzz-seed coupled edits in one commit (`git show --stat` + `check-ignore` non-zero + `ls-files`); the contract block format matches the 12.1 sibling. Surfaced 2 Minors → **M8** (the `allowlist_envoy_only` "14" claim vs 13-name enumeration, inherited verbatim from the ADR-0041-ratified SPEC §2.1; zero differential impact) + the same tie-test observation as Cluster B (consolidated into **M5**, which Cluster C independently rated Minor). **No residue rising above Minor.**

**Carryforward summary across the 3-cluster reviewer findings:** 0 Criticals surfaced; 0 Importants survive controller calibration (Cluster B's two Important flags → M4 + M5, downgrade rationale explicit in §3). The surfaced items consolidate into the 9 named Minors (M1–M9) in §3. **Per `BOOTSTRAP_PROMPT.md` §5.2 the phase does NOT re-enter state 3** — no Critical or Important findings survive; reopening to strengthen a ~10-LoC test (M5) or add a forward-dependency comment (M4) would force a full state-4 re-gate without a commensurate correctness gain, and the state-4 evidence anchor at CI run `26639712806` HEAD `7730e446` is complete + bilaterally green. The calibration matches the 06.x → 12.1 → 13.x precedent (all landed `Approved with M-track follow-ups`) — the project's mature-state-3 doctrine working as designed.

## §8. State-4 evidence re-attestation

Per the state-5 reviewer's mandate to re-attest each §7.5 sub-gate against the Task 7 state-4 anchor (PROGRESS Task 7; CI run `26639712806` HEAD `7730e446` `completed / success` `2026-05-29T13:21:48Z`; + a full local run):

| Gate | Disposition | Evidence |
|---|---|---|
| **(a)** New differential fixture green | **PASS (vacuous)** | 14.1 ships NO new differential fixture by design (the 05.1 / 07.1 / 12.1 foundation-slice pattern; configured-OD path inert-and-unexercised until 14.2 — `is_ejected()` always `false` at 14.1). Fixture `0022` lands in 14.2. |
| **(b)** Pre-existing 21 fixtures (`0001-0021`) green simultaneously | **PASS** | All 21 GREEN simultaneously in CI run `26639712806` (differential harness → Docker step). Re-confirmed LOCALLY: `cargo test -p differential -- --include-ignored` → 0 failed, all 21 Docker-gated fixture binaries `... ok` bilaterally vs `envoyproxy/envoy:v1.33.0`. The load-bearing 14.1 regression-equivalence proof; the §5.3 fast path verified byte-for-byte against `9a56e85:cluster.rs` by Cluster B. |
| **(c)** h2spec ≥95% | **PASS (vacuous)** | **99.31%** (parent-05 baseline, unaffected). 14.1 touched zero H2-framing code (diffstat: only envoy-config, envoy-cluster, docs, fuzz). No re-run required. |
| **(d)** parse_bootstrap fuzz clean for short-budget CI run | **PASS** | The Task 6 `cluster_outlier_detection.yaml` seed in corpus + SUCCESS array (all 3 coupled edits verified on-disk by Cluster C). Local re-run: `Done 200000 runs in 19 second(s)`, 0 crashes. Corpus 21 → 22. |
| **(e)** Stable-toolchain gates clean (fmt / clippy / build / test / deny) | **PASS** | All 5 within CI run `26639712806` clean: fmt clean; clippy `-D warnings` clean (incl. the dead-handle check — every OD stat handle is exercised); build clean; test **922 passed / 0 failed / 2 ignored**; cargo deny `advisories ok, bans ok, licenses ok, sources ok` (benign unmatched-license-allowance notices unchanged). The local-only `upstream_h2_connection_pooling` build-lock-contention flake is environmental (not a 14.1 regression). |
| **(f)** REVIEW.md approved | **CLOSE at this REVIEW.md** | Verdict per §1: **Approved with M-track follow-ups** (zero Critical; zero Important; 9 Minor M1–M9 — none has a hard-gated owner; none gates the state-6 close-out). Gate (f) closes at this verdict. |

**All six §7.5 gates: (a) PASS (vacuous) / (b) PASS / (c) PASS (vacuous) / (d) PASS / (e) PASS / (f) CLOSE-at-this-REVIEW.md.** Phase 14.1's state-4 evidence is complete; state-5 closes at this REVIEW.md commit; state-6 (the phase-14.1 NON-closing-sub-phase close-out + ROADMAP row `14.1` `in-progress → done` flip + STATE.md advance to `14.2` state-2-next) is a separate session per `BOOTSTRAP_PROMPT.md` §5.1.

---

*End of REVIEW.md. Phase 14.1 state-5 complete. Verdict: **Approved with M-track follow-ups** (zero Critical; zero Important; 9 Minor M1–M9). Per `BOOTSTRAP_PROMPT.md` §5.2, the phase does NOT re-enter state 3 — no Critical or Important findings (Cluster B's two Important flags calibrated down to M4 + M5 with explicit rationale). Phase 14.1 is the **third Upstream-robustness-family foundation slice** (after 12.1 active-HC and 13.1 H1-pool) — the inert-when-unconfigured foundation of passive outlier-detection ejection (config schema + validator + `EndpointEjection` state machine + `pick()` ejection-exclusion + the 7-name stat namespace), proven by regression-equivalence (all 21 Docker-gated fixtures green simultaneously with the machinery inert when `outlier_detection` is unconfigured — the §5.3 invariant verified byte-for-byte against `9a56e85:cluster.rs`). The state-4 evidence anchor at CI run `26639712806` HEAD `7730e446` is structurally complete + bilaterally green across the full 21-fixture Docker-gated surface + h2spec 99.31% (held vacuously — no H2 touch) + parse_bootstrap fuzz clean on the 22-seed corpus + 5 stable-toolchain gates + workspace `922 passed / 0 failed / 2 ignored`. 14.1 engaged NO carryforward closures (lock-in #24 — the 13.2 A-M2 stale-comment is a 14.2 D4 opportunity; 14.1 touches no envoy-http1); it generates 9 new Minor carryforwards (M4 the `EndpointEjection` `Relaxed` single-writer-per-endpoint premise — a forward dependency the **14.2 review** must verify before the D4 hook + D7 sweeper rely on it, the exact 12.1-M2 analog; M5 the weak tie-detector integration test + M6 the `max_ejection_percent = 0` edge — 14.2 D4 fold-ins; M8 the `allowlist_envoy_only` count/enumeration mismatch — a 14.2 D8.1 reconciliation; M1/M2/M3/M7/M9 style/doc/test-polish — no action / opportunistic). None gates the state-6 close-out. NO ADR landed in the 14.1 lifecycle (lock-in #2); the DECISIONS.md ledger head is **ADR-0041** (count 42); next available **ADR-0042**. The next session enters the **phase-14.1 state-6 close-out** — flips ROADMAP row `14.1` `in-progress → done` ALONE (a NON-closing sub-phase per ADR-0040; parent row `14` stays `in-progress` until 14.2 closes); advances STATE.md to `14.2` state-2-next (the 14.2 SPEC exists; PLAN.md does not → `superpowers:writing-plans` scoped to the 14.2 SPEC; do NOT re-run the §6.2 Docker verification — the findings are locked at `0a4d225` by ADR-0041); lands the state-6 close-out commit per the 14.1 SPEC §9 commit-message format. M4 is the standing input the 14.2 lifecycle inherits — the D4 response-receipt hook + D7 sweeper must instantiate exactly one serialized writer per endpoint for the `Relaxed` ordering to remain sound.*
