# Phase 12.2 (`12.2-active-http-probe-and-fixture`) — REVIEW

- **Phase id:** `12.2`
- **Slug:** `12.2-active-http-probe-and-fixture`
- **Parent:** `12` (`12-upstream-active-health-check`). **Second + closing sub-phase** of the parent-12 split (parent-12 state-2 PLAN-write `4f9ba04`; ADR-0036). 12.2's state-6 commit flips parent ROADMAP row `12` `in-progress → done` per the closing-sub-phase invariant (the 02.2 / 03.2 / 07.2 / 08.2 precedent). With this Approved verdict, the next session's state-6 close-out commit flips ROADMAP rows `12.2` **AND** `12` to `done` simultaneously (the closing-sub-phase row-flip pair) and advances STATE.md to "awaiting next planning". 12.2 lands the **observable behavior + the differential fixture** of active HTTP health checking — the slice 12.1's foundation made possible: the new `envoy-health` crate (the project's first periodic-background primitive); the `no healthy upstream` synth-503 body reconciliation (ADR-0037 implementation); fixture `0019-upstream-active-health-check` (the FIRST Upstream-robustness-family differential fixture); the synthetic health-aware backend harness primitive (the 06.3 REVIEW I2 down-payment); the settle-then-probe driver; the in-process H1 backstop (both convergence directions); and the parse_bootstrap corpus seed extension (corpus 19 → 20). The discriminating differential observable is **status 503 + body `no healthy upstream` (byte-exact)** at `/` after the settle window, bilaterally produced by `envoyproxy/envoy:v1.33.0` and envoy-rust on identical YAMLs.

- **Reviewed range:** `6a3b332..adc50b7` — **11 commits total** (`git log --oneline 6a3b332..adc50b7 | wc -l` → 11; all phase-12.2 commits, no interleaved non-phase commits). The state-2 base `6a3b332` is the standalone-PLAN-write commit (8 tasks; 25 lock-ins; §6.2 verification locked at the parent-12 split — NOT re-run; NO inline ADR); the HEAD `adc50b7` is the state-5 review fold-in commit. Diffstat: `2836 insertions(+), 10 deletions(-)` across 28 files.
  - `50c001c` — **Task 1**: D4 `envoy-health` crate scaffold + periodic probe task + `ClusterHandle::health_probe_targets` accessor + envoy-bin wiring + M2 single-writer-contract comment. New workspace member `crates/envoy-health/` (clean DAG: `envoy-health → {envoy-cluster, envoy-config, envoy-http1, envoy-stats, tokio, tokio-util, thiserror, tracing}`; cycle-free verified). 8 envoy-health unit tests + 2 envoy-cluster accessor tests. Spec ✅ + code-quality **Approved** (Task 1 amend folded in a rationale clarification + a documentation correctness fix).
  - `34729fb` — **Task 2**: D7 `health_check.{attempt,success,failure}` counters + BEHAVIOR_CONTRACT 12.2 block + M4 12.1 `membership_healthy` Equivalence-cell fold-in. The 3 counters register at `Scheduler::spawn` per (cluster, endpoint) and increment in the probe loop (`attempt.inc()` per tick; `success.inc()` or `failure.inc()` per probe). 2 registration-attestation tests. The `_scheduler` test-shutdown hygiene Minor surfaced at the code-quality review (deferred to state-5 per 12.1 carryforward discipline; folded into REVIEW §4 below). Spec ✅ + code-quality **Approved**.
  - `48a4849` — **Task 3**: D6.2 `synth_no_healthy_upstream` helper at `crates/envoy-http1/src/hcm.rs:951` + `hcm.rs:585` arm reconciles to 19-byte body per ADR-0037 + BEHAVIOR_CONTRACT `## Response body — no-healthy-upstream synth-503` subsection. Exactly one call-site change (`hcm.rs:585`); the connect-fail 502 + send-fail 502 paths (`hcm.rs:524` + `:568`) keep `synth_status` (empty body). 1 unit test asserts the 19-byte body + 5 standard headers + content-length `"19"`. 2 stale `:582`/`:866-887` line-ref Minors in doc comments surfaced at code-quality review (pre-existing drift; deferred to state-5; folded into REVIEW §4). Spec ✅ + code-quality **Approved**.
  - `c2c5335` — **Task 4**: D7.1 synthetic health-aware HTTP/1.1 backend (the **06.3 REVIEW I2 down-payment**) + `HealthAwareHttp1Backend` harness primitive. New workspace helper crate `tests/helpers/health-aware-http1-backend/` (Cargo.toml + `src/main.rs`, ~131 LoC main; hand-rolled HTTP/1.1 per D-3.2; `httparse + tokio + tracing + tracing-subscriber + anyhow` deps only — NO `hyper`/`axum`/`warp`). New `HealthAwareHttp1Backend` struct in `tests/differential/src/backend.rs:251-313` mirroring the `Http1EchoBackend` shape. **DOWN-PAYMENT, NOT full I2 closure** — the residual per-class `downstream_rq_3xx/4xx/5xx` + `cluster.<name>.upstream_rq_5xx` wire coverage + the `upstream_cx_total` `value-exact` tightening stays tied to connection pooling per the 06.3 REVIEW §3 disposition; PROGRESS attributes the down-payment honestly per D-3.4. 2 code-quality Minors (CLI bounds-check panic + `status_reason` "OK" fallback; folded into REVIEW §4). Spec ✅ + code-quality **Approved**.
  - `9ce6d61` — **Task 4 follow-up**: drop unused `bytes` dep from `health-aware-http1-backend` helper (code-quality review Important). In-phase recovery commit (NOT amend-and-force-push — Task 4 was already pushed; system safety protocol forbids force-push to main). The IN-PHASE-RECOVERY PRECEDENT that subsequent follow-ups (`b1cb25c`, `adc50b7`) mirror. Spec ✅ post-follow-up + clippy/build/test clean.
  - `cc8e531` — **Task 5**: D7.2 fixture `0019-upstream-active-health-check/` + `Driver::Http1AfterSettle` enum variant + Docker wrapper. The **FIRST Upstream-robustness-family differential fixture** + the FIRST fixture to drive synth-503 from the no-healthy-upstream arm bilaterally. Fixture YAMLs match the project-wide convention (envoy.yaml carries `admin:` + `0.0.0.0` bind + `generate_request_id: false`; envoy-rust.yaml binds `127.0.0.1` and omits — symmetric to fixture 0008's pattern). Integer-second durations per §6.2 item-6 (`interval: 1s`, `timeout: 1s`). Docker run PASSED bilaterally locally. 2 docs-only Minors (PROGRESS "7 prior consumers" enumeration + README's 5-header enumeration; folded into REVIEW §4). Spec ✅ + code-quality **Approved**.
  - `e259f70` — **Task 6**: D7.3 in-process H1 backstop at `crates/envoy-bin/tests/upstream_active_health_check.rs` (both convergence directions; 5-header presence assertion on 503 per 10 REVIEW M1). 2 backstop integration tests (`unhealthy_endpoint_returns_synth_503_no_healthy_upstream` + `healthy_endpoint_passes_through_to_backend`). 1 Minor (hand-rolled `http1_get` parser silently drops malformed header lines; folded into REVIEW §4). Spec ✅ + code-quality **Approved**.
  - `39e55a5` — **Task 7**: D-corpus `parse_bootstrap` seed `hcm_upstream_active_health_check.yaml` (corpus 19 → 20) + `.gitignore` allow-list entry + `fuzz_corpus_seeds_parse_or_reject_cleanly` SUCCESS-array extension — **all three in ONE commit** (the 09/10/11/12.1 Task-6 lesson). The new HCM + router + HC-configured-cluster + panic-disabled + route-gating seed is non-redundant vs the 12.1 header-only HC seed. Spec ✅ + code-quality **Approved**.
  - `8dbea41` — **Task 8**: state-4 phase-done verification + STATE advance to state-5-next. Docs-only (PROGRESS + STATE). The §7.5 (a)–(e) gate GREEN on a fresh local run. **However CI run `26345311915` for this HEAD settled `completed / failure`** — the differential fixture-0019 cold-build readiness race the Task 8 follow-up closes.
  - `b1cb25c` — **Task 8 follow-up**: bump `HealthAwareHttp1Backend::spawn()` readiness deadline from `Duration::from_secs(3)` to `Duration::from_secs(30)` for CI cold-build. CI run `26348699615` settled `completed / success` in 2m19s — the CI gate the Task 8 state-4 verification mis-attested (local was warm-cached; CI was cold-cached). In-phase recovery per the Task 4 follow-up precedent.
  - `adc50b7` — **Task 8 state-5 review fold-in (THIS commit's predecessor → effectively the state-5 commit context)**: Cluster B Importants I1 (`HealthAwareHttp1Backend::spawn` stderr `piped` → `inherit` to match the 4 sibling backends in the same file; closes the latent pipe-buffer deadlock landmine) + I2 (in-process backstop `wait_ready(backend_addr, 10s)` → `30s` at both `unhealthy_endpoint_returns_synth_503_no_healthy_upstream:193` and `healthy_endpoint_passes_through_to_backend:235`; closes the cold-cache test-ordering race) + Cluster A M1 (drop unused `bytes = "1"` dep from `crates/envoy-health/Cargo.toml`). State-3 re-entry per `BOOTSTRAP_PROMPT.md` §5.2.

- **CI evidence cited:** PROGRESS Task 8 follow-up + this REVIEW cite CI run **`26348699615`** at HEAD `b1cb25c` (the Task 8 follow-up) conclusion `completed / success` wall `2m19s` AS THE FIRST CI green of the state-3 execution arc. The state-5 fold-in commit `adc50b7` re-attests via a fresh CI run (`gh run list --branch main --commit adc50b7 --limit 1` `completed / success` at this state-5 session). `git rev-parse HEAD origin/main` → both `adc50b7` (HEAD == origin, the expected normal state). Local re-runs at this state-5 session confirm: 5 stable-toolchain gates clean (`cargo test --workspace` → **844 passed / 0 failed / 2 ignored** across 72 test-result lines; clippy clean under `-D warnings`; fmt clean; deny ok). The full local bilateral Docker re-run at the Task 8 state-4 verification (HEAD `39e55a5` + the `8dbea41` docs commit) produced **127 passed / 0 failed / 0 ignored** across 21 result lines — 19/19 Docker-gated fixtures green simultaneously vs `envoyproxy/envoy:v1.33.0` (the load-bearing 12.2 differential proof; including the new `upstream_active_health_check_fixture` at 6.33s).
- **Reviewed:** 2026-05-23.

## §1. Verdict

**Approved with M-track follow-ups.**

Phase 12.2 lands its 3 carved parent-12 deliverables (D4 `envoy-health` crate + periodic probe task + scheduler + envoy-bin wiring — the project's FIRST periodic-background primitive, hand-rolled per D-3.2 atop `tokio::time::interval` + `tokio_util::sync::CancellationToken` + `envoy-http1::Client`; with the `Scheduler::spawn` topology instantiating exactly ONE `tokio::spawn`-ed `probe_loop` per (cluster, endpoint) pair as the load-bearing **M2 single-writer-per-endpoint** contract verification; the 3 `cluster.<name>.health_check.{attempt,success,failure}` counter rows in `BEHAVIOR_CONTRACT.md` per §6.2 item-4 [`name-required, value-may-differ` per timing-dependence]; the M4 12.1 `membership_healthy` Equivalence-cell fold-in; D6.2 the `synth_no_healthy_upstream` helper + the EXACTLY-ONE-LINE `hcm.rs:585` arm change [the connect-fail 502 + send-fail 502 paths keep `synth_status`] + the new `## Response body — no-healthy-upstream synth-503` BEHAVIOR_CONTRACT subsection per ADR-0037 [body byte-exact `no healthy upstream` = 19 bytes, hex `6e 6f 20 68 65 61 6c 74 68 79 20 75 70 73 74 72 65 61 6d`, no trailing newline, 5 standard headers]; D7.1 the synthetic health-aware backend harness primitive + `HealthAwareHttp1Backend` struct in `tests/differential/src/backend.rs` — the **06.3 REVIEW I2 down-payment** [NOT full closure; residual stays with connection pooling]; D7.2 fixture `0019-upstream-active-health-check` + `Driver::Http1AfterSettle` enum variant + Docker wrapper [the FIRST Upstream-robustness-family differential fixture]; D7.3 in-process H1 backstop covering BOTH convergence directions with the 5-standard-header presence assertion on the 503 per 10 REVIEW M1; plus the D-corpus `parse_bootstrap` seed extending the corpus 19 → 20) substantively and cleanly through the existing envoy-cluster + envoy-config + envoy-http1 + envoy-stats + tokio foundations + the new `envoy-health` crate.

The cross-phase architectural invariants from the parent-12 SPEC §5 all hold: the new `envoy-health` crate sits ABOVE `envoy-cluster` and `envoy-http1` with deps `{envoy-cluster, envoy-config, envoy-http1, envoy-stats, tokio, tokio-util, thiserror, tracing}` — **clean DAG, no cycle** (`cargo build --workspace` enforces; Cargo.lock confirms no member depends on `envoy-health`); the probe loop + scheduler are hand-rolled per D-3.2 atop `tokio::time::interval` + `tokio_util::sync::CancellationToken` + `envoy-http1::Client` (no new top-level Cargo dep — `bytes` was declared and dropped at the state-5 fold-in `adc50b7` per Cluster A M1); no `unsafe` introduced (`#![forbid(unsafe_code)]` continues to bind at every touched crate root per D-3.8); no H2-framing-path touch (`git diff --name-only 6a3b332..adc50b7 | grep envoy-http2` empty — h2spec ≥95% holds vacuously at the parent-05 baseline 99.31%); the synth-503 reconciliation is surgically narrow (the new `synth_no_healthy_upstream` helper is used ONLY at `hcm.rs:585`; the connect-fail + send-fail 502 paths keep `synth_status`'s empty body — verified in-the-source by Cluster A). **NO ADR landed in the 12.2 lifecycle** (PLAN lock-in #2; SPEC §7 — 12.2 introduces no new foundations grant, no wire-level contract revision; the `envoy-health` crate creation is ordinary structure with a clean DAG, no creation ADR per the parent-12 state-1 brainstorm + state-2 split lock-in; ADR-0037 was pinned at the parent-12 split `4f9ba04` and IMPLEMENTED at Task 3, not re-decided). The DECISIONS.md ledger head is **ADR-0037** (count 38); next available **ADR-0038**. BEHAVIOR_CONTRACT.md extended with 1 new `Stat-name mapping` block (`**12.2 entries (active health checking — counters):**`, 3 counter rows) at Task 2 + the M4 12.1 `membership_healthy` Equivalence-cell edit at Task 2 + the new `## Response body — no-healthy-upstream synth-503` subsection at Task 3 — all per the 06.x → 12.1 cadence (contract extensions land at the task where the surface is first wired).

**Phase 12.2 is the FIRST phase since the MVP trunk closed to land an in-phase state-5 REVIEW fold-in (`adc50b7`).** Cluster B surfaced 2 Important findings (I1 stderr pipe-drain landmine + I2 backstop cold-cache race) that did NOT manifest in the §7.5 gate at this state-5 session, but ARE latent landmines for a future run with `RUST_LOG=debug` set (I1) or with test-execution-order flipped (I2). Both were 1-2 line mechanical fixes; both landed in the in-phase recovery commit `adc50b7` per `BOOTSTRAP_PROMPT.md` §5.2 (review feedback re-entry → step 3, NOT step 4 — the executing session re-ran TDD-equivalent verification: 5 stable-toolchain gates clean post-fix, with the workspace test count unchanged at 844/0/2 because the fix touches test-infrastructure budgets + a dead dep). The remaining 14 Minor findings (4 Cluster A + 6 Cluster B + 4 Cluster C) are all awareness-only / close-opportunistically per the standing post-MVP-trunk discipline. **Zero Critical findings remain. Zero Important findings remain (I1 + I2 closed in-phase at `adc50b7`).**

**The state-5 review dispatched a 3-cluster subagent pass over the reviewed range, sliced by deliverable cohesion matching the diffstat** (Cluster A = Tasks 1–3 — the periodic-probe primitive + synth-503 reconciliation; Cluster B = Tasks 4–6 + Task 8 follow-up — the synthetic-backend harness + fixture 0019 + in-process backstop; Cluster C = Task 7 + docs — the fuzz seed + cross-doc consistency + PLAN-lock-in audit). Each cluster reviewer read the actual on-disk code (`git diff` + `Read` + `Grep`) per the standing precedent-verification discipline, not the PROGRESS narrative; Cluster A bilaterally verified the M2 single-writer-per-endpoint topology (workspace-wide grep on `record_success`/`record_failure` confirms the only production call sites are `crates/envoy-health/src/probe.rs:51,56`); Cluster B verified the Task 8 follow-up's 3s→30s bump closed the CI-red sequence (`gh run list` confirms `b1cb25c` GREEN at 2m19s after 4 RED runs in a row on tasks 5-8); Cluster C audited all 25 PLAN lock-ins for on-disk compliance (every load-bearing claim verified). Cluster A + Cluster C returned `Ready to merge: Yes` with 0 Critical + 0 Important; Cluster B returned `Ready to merge: With fixes` with 0 Critical + 2 Important — both Importants closed at `adc50b7`. **Per `BOOTSTRAP_PROMPT.md` §5.2 the phase RE-ENTERED state 3** at the fold-in commit, then re-attested the gates locally (5 stable-toolchain gates + workspace 844/0/2 unchanged) + via the post-fold-in CI run.

**Phase 12.2 is the SECOND Upstream-robustness-family REVIEW.md** — the verdict + REVIEW shape continue the 06.x → 07.x → 08.x → 09 → 10 → 11 → 12.1 post-MVP standard: `Approved with M-track follow-ups` disposition; forward-carry inheritance for unexercised-pattern Minor findings; explicit calibration rationale per finding; full §7.5 6-gate re-attestation against the post-fold-in CI anchor. 12.2 ENGAGES carryforward 06.3 REVIEW I2 (DOWN-PAYMENT at Task 4 — synthetic-backend harness primitive; NOT full closure — residual stays with connection pooling per 06.3 REVIEW §3); closes 12.1 REVIEW **M2** (Task 1 API-boundary single-writer-contract comment + Cluster A's bilateral topology verification) + **M4** (Task 2 ~1-LoC Equivalence-cell edit). It generates 14 new Minor carryforwards (none gating; full inventory in §3 + §4 below).

## §2. Strengths

1. **The M2 single-writer-per-endpoint topology is verifiably tight — the load-bearing 12.1 REVIEW carryforward correctness verification.** Cluster A's workspace-wide grep on `record_success`/`record_failure` confirms the ONLY production call sites are `crates/envoy-health/src/probe.rs:51,56`. The `Arc<EndpointHealth>` for an endpoint flows: `from_bootstrap` (`cluster.rs:521-530`) → stored in `Cluster.endpoint_health[i]` (read-only by `pick()` at `cluster.rs:156,166`) → cloned out by `health_probe_targets()` (`cluster.rs:244-254`) → moved into a single `tokio::spawn`ed `probe_loop` (`scheduler.rs:101-118`). The probe loop is the only mutator. The new `EndpointHealth` doc-comment at `health.rs:21-31` documents this contract explicitly at the API boundary. **Bilateral topology verification: passes.** The 12.1 REVIEW M2 forward-correctness-verification dependency closes cleanly.

2. **The D6.2 ADR-0037 implementation is surgically narrow + byte-exact.** Cluster A verified: `synth_no_healthy_upstream` (`hcm.rs:951-971`) mirrors `synth_status`'s 5-header shape verbatim modulo body + content-length; `Bytes::from_static(b"no healthy upstream")` evaluates to length 19 (the hex bytes per ADR-0037 — `6e 6f 20 68 65 61 6c 74 68 79 20 75 70 73 74 72 65 61 6d` — match `b"no healthy upstream"`); `body.len().to_string()` (NOT `"19".to_string()`) means future drift cannot desync the content-length from the body bytes. The EXACTLY-ONE-LINE call-site change at `hcm.rs:585` (`synth_status(503, close)` → `synth_no_healthy_upstream(close)`) is verified by direct grep; the connect-fail 502 (`hcm.rs:524`) + send-fail 502 (`hcm.rs:568`) paths keep `synth_status` (verified UNTOUCHED). The unit test at `hcm.rs:3045-3072` asserts status 503; body bytes; exact length; the 5 standard header names in canonical order; AND content-length `"19"`. The new BEHAVIOR_CONTRACT subsection at `:27-37` is structurally clean.

3. **Inert-when-unconfigured (the §5.4 architectural invariant) verified bilaterally.** Test `spawns_zero_tasks_when_no_hc_configured` (scheduler.rs:227-241) + `registers_no_counters_when_no_hc_configured` (`:284-304`) prove the gate. The production gate at `scheduler.rs:49-52` (`continue` on no HC) is structurally identical to the test condition. A cluster with no `health_checks` carries `endpoint_health: None` → `health_probe_targets()` returns `None` → no probe task spawns + no counters register → the 18 pre-existing fixtures see ZERO behavior change (the §5.4 inert-when-unconfigured invariant); the new fixture 0019 is the ONLY Docker-gated fixture exercising the configured-HC path. The state-4 Docker run produced **19/19 fixtures green simultaneously** confirming this end-to-end.

4. **The clean DAG enforcement is structural + cycle-free.** Cluster A confirmed via Cargo.lock: `envoy-health` depends on `{envoy-cluster, envoy-config, envoy-http1, envoy-stats, tokio, tokio-util, thiserror, tracing}`; NO member depends on `envoy-health` (`grep -rn "envoy-health" crates/*/Cargo.toml` returns only the `[dependencies]` block of `crates/envoy-bin/Cargo.toml:17` per the consumer wiring). The `cargo build --workspace` is the build-time enforcement that no future refactor can silently introduce a cycle. The new crate sits cleanly above the existing tree without disrupting `envoy-cluster ← envoy-http1` (the pre-existing 04.3 dependency).

5. **The 5 standard HTTP/1.1 response headers are correctly asserted bilaterally on the synth-503 path.** Cluster B verified: the in-process backstop at `crates/envoy-bin/tests/upstream_active_health_check.rs:208-219` iterates the 5-header roster `{server, date, content-length, content-type, connection}` AND asserts content-length=19 at `:225` (per 10 REVIEW M1). The fixture `0019-upstream-active-health-check/expectations.yaml` asserts the same 5-header roster via the existing harness `BodyRule::EqualsAfterTrim` + a `headers:` block — the 503 path is bilaterally header-checked end-to-end vs `envoyproxy/envoy:v1.33.0`.

6. **The settle-then-probe driver is minimal + sound.** Cluster B verified `Driver::Http1AfterSettle` in `tests/differential/src/lib.rs:2207-2289` reuses `drive_http1` directly + applies the 5-axis equivalence cascade identically to `Driver::Http1`. The settle math holds: `settle_ms: 3500` ≥ `interval (1000) × unhealthy_threshold (1) + timeout (1000) + margin (1500) = 3500ms` per SPEC §2 item-1, with adequate 1.5s margin atop the 2s minimum convergence. The new variant is gated by fixture-directory name (`fixture_name == "0019-..."`) at the backend-dispatcher hook (`lib.rs:1737-1768`), so the 6 prior `{{BACKEND_PORT}}` consumers stay on `TcpProxyBackend` unchanged (no collateral test damage).

7. **The 3-files-same-commit fuzz-seed discipline (PLAN lock-in #24) holds at Task 7.** Cluster C verified via `git show --stat 39e55a5` that all three load-bearing files land together: the seed `crates/envoy-config/fuzz/corpus/parse_bootstrap/hcm_upstream_active_health_check.yaml` (56 lines); the `.gitignore` allow-list line at `crates/envoy-config/fuzz/.gitignore:26`; the `fuzz_corpus_seeds_parse_or_reject_cleanly` SUCCESS-array extension at `crates/envoy-config/src/bootstrap.rs:3541`. The seed is tracked-not-gitignored (`git check-ignore` exits non-zero); the SUCCESS-array test passes on the on-disk file at this session (`cargo test -p envoy-config --lib fuzz_corpus_seeds_parse_or_reject_cleanly` → `1 passed`). The seed exercises the FULL HCM + router + HC-configured-cluster + panic-disabled + route-gating shape — non-redundant vs the 12.1 header-only HC seed; the only HCM-bearing HC-cluster YAML in the 20-seed corpus.

8. **The two-stage per-task review discipline recovered every blocking finding in-phase — the state-3 arc carried zero Critical/Important to state 5 at the per-task level (Importants surfaced at the holistic state-5 review, not in any per-task review).** Across Tasks 1–8, the per-task code-quality reviewers surfaced and the implementer recovered: Task 1 amend (rationale clarification + documentation correctness fix); Task 4 follow-up `9ce6d61` (drop unused `bytes` dep — Important code-quality finding). The holistic state-5 3-cluster pass surfaced 2 NEW Importants (Cluster B I1 stderr drain + I2 backstop wait_ready) that the per-task reviews missed because they were latent landmines (only manifesting under `RUST_LOG=debug` or test-execution-order flipping); both were folded in at `adc50b7` per `BOOTSTRAP_PROMPT.md` §5.2.

9. **The Task 8 follow-up `b1cb25c` cadence is the in-phase recovery exemplar.** Cluster B verified: the bump was mechanical (one-token `Duration::from_secs(3)` → `Duration::from_secs(30)` + a 3-line comment update); the follow-up landed as a NEW commit (not amend-and-force-push — Task 8 was already pushed; system safety protocol forbids force-push to main); CI run `26348699615` for `b1cb25c` settled `completed / success` in 2m19s after 4 RED runs in a row on Tasks 5–8 with the SAME `health-aware-http1-backend did not become ready` signature; PROGRESS attributes the CI-red root cause honestly (cold `cargo run --manifest-path` of the helper exceeds 3s; locally the helper is pre-cached by `cargo build --workspace --all-targets`). The pattern repeats at `adc50b7` (state-5 fold-in) — same in-phase-recovery shape, same NEW commit, same local-gate re-confirmation. This sets the precedent for future Upstream-robustness-family phases.

10. **The 06.3 REVIEW I2 down-payment is attributed honestly per D-3.4.** Cluster B verified PROGRESS lines 215-224 + 461-469 + 1024-1029 explicitly call out "DOWN-PAYMENT, NOT full I2 closure" and tie the residual (per-class `downstream_rq_3xx/4xx/5xx` + `cluster.<name>.upstream_rq_5xx` wire coverage + `upstream_cx_total` `value-exact` tightening) to connection pooling per 06.3 REVIEW §3. No over-claim of full closure. The synthetic-backend harness primitive (`HealthAwareHttp1Backend` + the `health-aware-http1-backend` helper crate) is the smallest viable down-payment — the project's first synthetic backend harness, ready to be reused by future upstream-robustness phases (outlier-detection, circuit-breaker, retries) that need a configurable-status backend.

11. **The Cargo.lock cadence ratification + no new top-level Cargo deps.** Cluster A + Cluster C confirmed: `git diff Cargo.toml Cargo.lock 6a3b332..adc50b7` is non-empty (the new `envoy-health` crate + helper crate registration in `members:`, and one `bytes` dep DROPPED at the state-5 fold-in `adc50b7`) but adds **ZERO new top-level Cargo deps** (the new `envoy-health` crate uses pre-existing foundations: `envoy-cluster`/`envoy-config`/`envoy-http1`/`envoy-stats`/`tokio`/`tokio-util`/`thiserror`/`tracing`; the helper crate uses `httparse`/`tokio`/`tracing`/`tracing-subscriber`/`anyhow` — all pre-existing). All touched crate roots keep `#![forbid(unsafe_code)]`; ENVOY_TARGET pin (`v1.33.0`) unchanged at `tests/differential/src/upstream.rs:13`; `rust-toolchain.toml` untouched (D-3.7 + D-3.9 unaffected).

12. **The Cluster B I1 + I2 in-phase fold-in (`adc50b7`) is mechanical + close-paired with detailed in-line comments.** Cluster B's two Importants both landed as 1-2 line mechanical edits + in-line code comments naming the deliberate divergence rationale (the `HealthAwareHttp1Backend` `inherit` vs the in-process backstop's `piped`; the backstop `wait_ready(30s)` vs the listener-side 10s). The local-gate re-confirmation post-fold-in: 5 gates clean; workspace test count unchanged at 844/0/2 (the fix touches test-infrastructure budgets + drops a dead dep — no test logic change). Mirrors the Task 4 follow-up + Task 8 follow-up cadence.

## §3. Issues

### Critical (must fix before phase commit)

None.

### Important (close in 12.2 or carry forward with named owner phase)

None — both Cluster B Importants closed in-phase at `adc50b7`.

**Closed-at-state-5 fold-in (`adc50b7`):**

- **I1 (Cluster B; CLOSED).** `HealthAwareHttp1Backend::spawn` stderr `Stdio::piped()` without drain — latent ~64 KB pipe-buffer deadlock landmine under verbose logging. Closed at `adc50b7` by flipping to `Stdio::inherit()` to match the 4 sibling backends in the same file (`TcpProxyBackend`, `TlsEchoBackend`, `Http1EchoBackend`, `Http2EchoBackend`). In-line comment at `tests/differential/src/backend.rs:274-283` names the deliberate divergence vs the in-process backstop's `piped`-with-test-runner-drain convention.

- **I2 (Cluster B; CLOSED).** In-process backstop `wait_ready(backend_addr, Duration::from_secs(10))` at `crates/envoy-bin/tests/upstream_active_health_check.rs:193` + `:235` — racy under cold cargo target/ (same root cause as the Task 8 follow-up). Closed at `adc50b7` by bumping both to `Duration::from_secs(30)` matching the harness-side budget. In-line comments cross-reference the Task 8 follow-up rationale. The listener-readiness polls (`:197`, `:239`) stay at 10s (envoy-bin is built via `CARGO_BIN_EXE_envoy-bin`, not `cargo run`, so the cold-build asymmetry doesn't apply).

### Minor (awareness-only / track forward)

**14 Minor findings — 4 Cluster A + 6 Cluster B + 4 Cluster C. None gating; all awareness-only / close-opportunistically per the standing post-MVP-trunk discipline. M2 is a one-line ergonomic; the rest are doc-precision, narrative-finishing-touch, or surface-hygiene.**

**Cluster A Minors (Tasks 1–3, periodic-probe primitive + synth-503 reconciliation):**

**A-M2. `Scheduler::task_count()` is `pub` despite being doc-labeled "test helper" (`crates/envoy-health/src/scheduler.rs:133-136`).** Production callers in `envoy-bin` never invoke it; only the unit tests in `scheduler.rs::tests` do. **Why it matters:** D-3.4 surface hygiene — leaks an API method that production code does not need; `#[doc(hidden)]` or `#[cfg(test)]` would tighten the surface. **Why it does NOT rise above Minor:** doesn't break anything; the cleanup is opportunistic, not gating. **Fix sketch:** add `#[doc(hidden)]` (matching the `ClusterManager::empty()` precedent at `cluster.rs:300`) or scope to `pub(crate)` + expose a test-only accessor via `#[cfg(test)]`. **Disposition:** awareness-only; close opportunistically at the next phase touching `envoy-health` (likely the outlier-detection phase, which extends the same scheduler primitive). Named owner: next upstream-robustness phase that opens `envoy-health/src/scheduler.rs`.

**A-M3. PROGRESS Task 1 does not record the `crates/envoy-health/src/lib.rs:26-31` re-export deviation from PLAN lock-in #4's sketch.** PLAN sketched `pub use scheduler::*; + pub use probe::*;`; the on-disk implementation uses tighter named re-exports (`pub use error::HealthError;` + `pub use scheduler::Scheduler;`) with `probe` kept `mod`-private. The deviation is BETTER (tighter encapsulation; `probe_loop`/`ProbeError` correctly stay `pub(crate)`). **Why it matters:** D-3.4 — any PLAN deviation should land in PROGRESS so future archaeologists can read both inputs. **Why it does NOT rise above Minor:** the deviation improves encapsulation; no functional change. **Fix sketch:** the state-5 fold-in subsection at PROGRESS `:1268-1276` (the new `### Task 8 state-5 review fold-in` subsection) records the deviation explicitly — folded in at `adc50b7`. **Disposition:** CLOSED at `adc50b7`.

**A-M4. `scheduler.rs:91` `.expect("HC-configured cluster has health_probe_targets")` is sound but un-tested negatively.** The expect cannot fire under the structural invariant that `from_bootstrap` and `Scheduler::spawn` use the same `cfg.health_checks.first().is_some()` predicate. If a future refactor decoupled those two predicates, this `expect` would become a latent panic site. **Why it matters:** future-correctness invariant note. **Why it does NOT rise above Minor:** the structural invariant is currently airtight; the comment at `cluster.rs:88` and the parallel predicate at `:49-52` make the contract explicit. **Fix sketch:** future refactors that touch `from_bootstrap`'s `endpoint_health = if let Some(hc) = cfg.health_checks.first()` branching should pair-touch this `expect`. **Disposition:** awareness-only forward-invariant note; no in-phase action. Named owner: any future refactor that opens both `cluster.rs::from_bootstrap` and `scheduler.rs::spawn`.

**Cluster B Minors (Tasks 4–6 + Task 8 follow-up, synthetic-backend harness + fixture 0019 + in-process backstop):**

**B-M1. Helper `parse_args()` indexes `args[i + 1]` without bounds check — `--port` without value panics (`tests/helpers/health-aware-http1-backend/src/main.rs:62-74`).** Running `health-aware-http1-backend --port` (no value) panics with `"index out of bounds"`. Pre-existing in-phase Task 4 reviewer Minor. **Disposition:** awareness-only; close opportunistically at any future helper-CLI extension. Named owner: next phase that extends the helper CLI.

**B-M2. Helper `status_reason()` defaults to `"OK"` for non-200/503 statuses (`tests/helpers/health-aware-http1-backend/src/main.rs:125-131`) — misleading reason phrase if a future fixture configures `--data-status 502`.** A `--data-status 502` would render `HTTP/1.1 502 OK\r\n`, a malformed status line. Pre-existing in-phase Task 4 reviewer Minor. **Disposition:** awareness-only; close opportunistically. Named owner: next phase that extends `--data-status` beyond 200/503.

**B-M3. `http1_get()` backstop parser at `crates/envoy-bin/tests/upstream_active_health_check.rs:52-85` is hand-rolled with several brittleness surfaces.** Assumes `": "` (with space) framing; status-line split assumes ≥2 tokens; body assumes Content-Length OR `Connection: close` framing (no chunked support). Pre-existing in-phase Task 6 reviewer Minor. **Disposition:** awareness-only; replace with `httparse` for robustness if a future backstop grows complexity. Named owner: next phase that extends this backstop.

**B-M4. `HealthAwareHttp1Backend::Drop` lacks the sibling 2s-poll/50ms-sleep deterministic-exit wait — comment claims "matches Http1EchoBackend" but doesn't (`tests/differential/src/backend.rs:307-313` vs `:225-239`).** Both work (kill_on_drop=true delivers SIGKILL regardless), so the wait loop is only about test-side observability of the exit. New Minor surfaced at this state-5 review. **Disposition:** awareness-only; either fix is a 7-line block copy from `Http1EchoBackend::Drop` if deterministic exit timing is wanted; otherwise update the comment to disclose the divergence. Named owner: next phase that touches `HealthAwareHttp1Backend`.

**B-M5. `tests/fixtures/0019-upstream-active-health-check/{envoy.yaml, envoy-rust.yaml}` are not literally byte-identical** — envoy.yaml carries `admin:` + `0.0.0.0` bind + `generate_request_id: false`; envoy-rust.yaml binds `127.0.0.1` and omits the other two. This matches the project-wide convention (verified vs fixture 0008). The SPEC §3 D7.1 wording "identical" should be read as "identical for the differential surface, modulo the established per-proxy boilerplate divergences." **Disposition:** awareness-only; the SPEC wording could be tightened in a future revision (e.g., "identical-modulo-boilerplate") but the on-disk fixture is consistent with the convention. Named owner: none.

**B-M6. `HealthAwareHttp1Backend::Drop`'s explicit `start_kill()` is redundant given `kill_on_drop(true)` is already set at spawn (`tests/differential/src/backend.rs:307-313`).** Tokio's `kill_on_drop` already fires SIGKILL when `Child` is dropped; the explicit `start_kill()` is style-only redundancy. Comment at `:309` acknowledges it. **Disposition:** awareness-only; no action. Named owner: none.

**Cluster C Minors (Task 7 + docs, fuzz seed + cross-doc consistency):**

**C-M1. PROGRESS Task 7 cites stale line numbers for the SUCCESS array (`docs/envoy-rust/phases/12.2-active-http-probe-and-fixture/PROGRESS.md:915-917`).** "Runs 3522-3540, REJECT array 3548-3551, minimal.yaml line 3564" but on-disk `bootstrap.rs` reads SUCCESS at `:3522-3541` (one line longer post-edit), REJECT at `:3550-3552`, and `let minimal =` at `:3565`. Per-line numerical drift; structurally correct (the array did extend by one entry; the relative ordering is right). **Disposition:** awareness-only; mirrors the 12.1 Cluster-C "seed key-order" Minor for the same cosmetic-narrative class. Named owner: none.

**C-M2. PROGRESS Task 7 corpus-inventory arithmetic is mildly tangled (`PROGRESS.md:918-924`).** "19 success + 3 reject + 1 minimal + 2 fuzz-corpus auxiliary" obscures the actual reconciliation (25 tracked seeds = 20 success + 3 reject + 1 `let minimal =` + 1 `include_str!`). Substantive claim (all seeds tested, no orphan) holds. **Disposition:** awareness-only narrative-finishing-touch; no action. Named owner: none.

**C-M3. STATE.md `:13` Active phase paragraph uses "THIS commit" wording from the state-4 verification commit `8dbea41`; intentionally stale per the Task 8 follow-up posture (STATE.md does not re-advance at follow-up commits).** The state-5 commit naturally refreshes the Active phase paragraph and supersedes it (THIS REVIEW.md commit + the STATE.md advance per §1 of `BOOTSTRAP_PROMPT.md` §5 state 5 → 6 cadence). **Disposition:** CLOSED at this state-5 STATE.md advance (the next commit in this session). Named owner: this commit.

**C-M4. PROGRESS Task 8 §7.5 (e) per-task new-test attribution arithmetic slightly off (`PROGRESS.md:999`).** Per-task split mis-distributes (Task 1 lands 6 envoy-health unit + 2 cluster accessor, not "7 + 2"; Task 2 lands 2 registration, not "3"; Task 3 lands 1 synth, not "2"; aggregate 832 → 844 = +12 confirmed). **Disposition:** awareness-only; aggregate holds, per-task split is narrative finishing-touch only. Named owner: none.

**Confirmed-closed at task time (not live carryforwards):**

- **Task 1 amend** — rationale clarification + documentation correctness fix; folded in at the Task 1 commit `50c001c` itself; awareness-only, no action.
- **Task 4 code-quality Important (unused `bytes` dep in helper crate)** — closed at the Task 4 follow-up `9ce6d61`. Not a live carryforward.
- **A-M1 (unused `bytes` dep in envoy-health/Cargo.toml)** — CLOSED at state-5 fold-in `adc50b7` per Cluster A recommendation.
- **A-M3 (PROGRESS deviation note for lib.rs re-exports)** — CLOSED at state-5 fold-in `adc50b7` (PROGRESS Task 8 state-5 fold-in subsection records the deviation).
- **C-M3 (STATE.md `:13` "THIS commit" wording)** — CLOSED at this state-5 STATE.md advance.

## §4. Carryforward inventory

For each Minor: closure plan. (No Critical or Important items remain; I1 + I2 closed in-phase at `adc50b7`.)

| ID | Severity | Item | Closure plan |
|---|---|---|---|
| **A-M2** | Minor | `Scheduler::task_count()` `pub` despite "test helper" label | **Close opportunistically** at the next upstream-robustness phase touching `envoy-health/src/scheduler.rs` (likely outlier-detection). `#[doc(hidden)]` or `pub(crate)` + `#[cfg(test)]` accessor. Named owner: next phase opening this file. Non-blocking. |
| **A-M4** | Minor | `scheduler.rs:91` `.expect("HC-configured cluster has health_probe_targets")` un-tested negatively | **Awareness-only forward invariant**; future refactors that decouple `from_bootstrap` + `Scheduler::spawn` predicates must pair-touch this `expect`. Named owner: any future refactor opening both call sites. Non-blocking. |
| **B-M1** | Minor | Helper `parse_args()` panics on `--port` without value | **Close opportunistically** at any future helper-CLI extension. Named owner: next phase extending the helper CLI. Non-blocking. |
| **B-M2** | Minor | Helper `status_reason()` defaults to `"OK"` for non-200/503 statuses | **Close opportunistically** at any future fixture configuring `--data-status` outside {200, 503}. Named owner: next phase extending `--data-status`. Non-blocking. |
| **B-M3** | Minor | `http1_get()` backstop parser hand-rolled brittleness | **Close opportunistically**; replace with `httparse` if a future backstop grows complexity. Named owner: next phase extending this backstop. Non-blocking. |
| **B-M4** | Minor | `HealthAwareHttp1Backend::Drop` comment claims "matches Http1EchoBackend" but lacks the 2s-poll wait | **Close opportunistically**; either 7-line block-copy from `Http1EchoBackend::Drop` OR update the comment to disclose the divergence. Named owner: next phase touching `HealthAwareHttp1Backend`. Non-blocking. |
| **B-M5** | Minor | Fixture envoy.yaml / envoy-rust.yaml not literally byte-identical (per-proxy boilerplate divergences) | **No action recommended** (matches project-wide convention; SPEC wording could tighten in a future revision). Named owner: none. Non-blocking. |
| **B-M6** | Minor | `HealthAwareHttp1Backend::Drop` redundant `start_kill()` given `kill_on_drop(true)` | **No action recommended** (style-only redundancy; comment acknowledges). Named owner: none. Non-blocking. |
| **C-M1** | Minor | PROGRESS Task 7 stale SUCCESS-array line numbers | **No action recommended** (cosmetic narrative drift; structural claim holds). Named owner: none. Non-blocking. |
| **C-M2** | Minor | PROGRESS Task 7 corpus arithmetic narrative tangled | **No action recommended** (substantive claim holds; narrative finishing-touch only). Named owner: none. Non-blocking. |
| **C-M4** | Minor | PROGRESS Task 8 §7.5 (e) per-task test-count attribution slightly off | **No action recommended** (aggregate 832→844 holds; per-task split narrative finishing-touch). Named owner: none. Non-blocking. |

**Process note (awareness-only, no carryforward).** Phase 12.2 is the FIRST phase since the MVP trunk closed to land an in-phase state-5 REVIEW fold-in (`adc50b7`). The 12.1 / 09 / 10 / 11 state-5 reviews all returned `Approved with M-track follow-ups` with 0 Critical + 0 Important at the per-cluster level + at the holistic state-5 level; 12.2 broke that streak at the HOLISTIC state-5 pass (Cluster B I1 + I2 — latent landmines that the per-task reviews missed because they only manifest under unusual runtime conditions). The in-phase recovery worked exactly as `BOOTSTRAP_PROMPT.md` §5.2 prescribes — state-3 re-entry, mechanical fix, local-gate re-confirmation, CI re-attestation — and the result is a stronger phase (zero residual Importants, the I1/I2 landmines closed). Awareness-only; the doctrine works as designed.

**Standing carryforwards inherited but not closed in 12.2** (per PROGRESS Task 1 preamble carryforward disposition; 12.2 ENGAGED **06.3 REVIEW I2 DOWN-PAYMENT** at Task 4 — NOT full closure):

- **06.3 REVIEW I2** (the synthetic-backend harness infrastructure; the Upstream-robustness-family named owner) — **DOWN-PAYMENT landed at Task 4 / D7.1** (`HealthAwareHttp1Backend` + the `health-aware-http1-backend` helper crate). Full I2 closure (per-class `downstream_rq_3xx/4xx/5xx` + `cluster.<name>.upstream_rq_5xx` wire coverage + the `cluster.<name>.upstream_cx_total` `value-exact` tightening) remains TIED TO **connection pooling** per the 06.3 REVIEW §3 disposition. 12.2 makes the smallest viable down-payment; future upstream-robustness phases (outlier-detection, circuit-breaker, retries) can reuse the helper + struct, but full I2 closure waits for connection pooling.
- **12.1 REVIEW M1 + M3** — carry forward unchanged; **not engaged** (12.2 touched the surfaces but the M1 validator `< 1` vs `== 0` style nit + M3 `pick()` two-pass micro-style are both no-action awareness-only). Named owners per the 12.1 REVIEW §4 (none).
- **Phase-11 REVIEW M1–M8** (the `instance.rs`/`pipeline.rs` doc-drift trio M1/M2/M3; the M4/M5 fault-validator coverage; the M6 test-helper-style; the M7/M8 NON-ISSUES) — carry forward unchanged; **not engaged** (12.2 touches no HTTP-filter file). Named owners per the phase-11 REVIEW §4 (next HTTP-filter-family phase touching `instance.rs`/`pipeline.rs`).
- **10 M2/M3/M4/D1/D2/T1**, **09 M1/D1/D2/T1/T2/T3**, **08.2 REVIEW M1-M8 + T1-T3**, **08.1 REVIEW M3**, **07.2 REVIEW M2/M3**, **06.2 REVIEW M1/M2/M4/M5**, **06.1 REVIEW M2/M3/M5/M6**, **05.3 REVIEW I2**, **05.2 REVIEW I1/I2/I3**, **04.1 REVIEW M5/M9/M-claim/M1/M2/M4/M7**, **02.2 REVIEW M1**, **Phase-00 I3 SIGKILL→SIGTERM** — carry forward indefinitely unless coincidentally engaged. None engaged by the 12.2 surface (which touches only `crates/envoy-health/` [new], `crates/envoy-cluster/src/{cluster.rs, health.rs}` [accessor + M2 comment], `crates/envoy-bin/src/main.rs` [wiring], `crates/envoy-http1/src/hcm.rs` [D6.2 helper + arm], `crates/envoy-stats` registrations [via envoy-health], docs, tests/fixtures/0019, tests/helpers/health-aware-http1-backend, tests/differential).
- **04.1 REVIEW M5 / M9 Cargo.lock cadence ratification** — carries forward unchanged; **zero new top-level Cargo deps** at this phase (the new `envoy-health` crate + the helper crate use pre-existing foundations; one `bytes` dep DROPPED at the state-5 fold-in `adc50b7`). Cargo.lock diff at the reviewed range is non-empty but ratified by Cluster A + Cluster B as deps-on-existing-foundations only.

## §5. Disposition decisions

**Closed in 12.2:**

- **12.1 REVIEW M2** (the `EndpointHealth` `Relaxed`-ordering single-writer-per-endpoint forward-correctness-verification dependency) — CLOSED at Task 1 via the API-boundary single-writer-contract comment on `EndpointHealth` AND the `Scheduler::spawn` topology (one `JoinHandle` per (cluster, endpoint) pair; verified bilaterally by Cluster A — workspace-wide grep confirms `crates/envoy-health/src/probe.rs:51,56` are the only `record_success`/`record_failure` call sites).
- **12.1 REVIEW M4** (the `cluster.<name>.membership_healthy` BEHAVIOR_CONTRACT Equivalence-column self-containment) — CLOSED at Task 2 via the ~1-LoC contract-row cell edit at the natural revisit site (where the gauge becomes driven by the live probe task).
- **Cluster B I1** (state-5 review — `HealthAwareHttp1Backend::spawn` stderr pipe-drain landmine) — CLOSED at state-5 fold-in `adc50b7` via `Stdio::piped()` → `Stdio::inherit()`.
- **Cluster B I2** (state-5 review — backstop `wait_ready(10s)` cold-cache race) — CLOSED at state-5 fold-in `adc50b7` via `Duration::from_secs(10)` → `Duration::from_secs(30)` at both `:193` + `:235`.
- **Cluster A M1** (state-5 review — unused `bytes` dep in `envoy-health/Cargo.toml`) — CLOSED at state-5 fold-in `adc50b7` via dropping the dep line.
- **Cluster A M3** (state-5 review — PROGRESS deviation note for lib.rs re-exports) — CLOSED at state-5 fold-in `adc50b7` (PROGRESS Task 8 state-5 fold-in subsection records the deviation).
- **Cluster C M3** (state-5 review — STATE.md `:13` "THIS commit" wording staleness) — CLOSED at this state-5 STATE.md advance.

**06.3 REVIEW I2** — **DOWN-PAYMENT landed at Task 4** (synthetic-backend harness primitive); **NOT full closure** — residual stays with connection pooling per the 06.3 REVIEW §3 disposition. PROGRESS attributes honestly per D-3.4.

**Continuing carryforwards by named owner phase:** see §4 standing carryforwards inventory above.

**New carryforwards generated by 12.2** (11 Minor: A-M2, A-M4, B-M1, B-M2, B-M3, B-M4, B-M5, B-M6, C-M1, C-M2, C-M4 — none with hard-gated owners):

- **A-M2** (`Scheduler::task_count()` surface hygiene) — close opportunistically at the next phase touching `envoy-health/src/scheduler.rs`.
- **A-M4** (`scheduler.rs:91` `.expect` un-tested negatively) — awareness-only forward invariant.
- **B-M1** (helper `parse_args()` bounds-check) — close opportunistically at any future helper-CLI extension.
- **B-M2** (helper `status_reason()` "OK" fallback) — close opportunistically.
- **B-M3** (backstop `http1_get()` parser brittleness) — close opportunistically.
- **B-M4** (`HealthAwareHttp1Backend::Drop` wait-loop divergence) — close opportunistically.
- **B-M5** (fixture YAML "identical-modulo-boilerplate") — no action.
- **B-M6** (`HealthAwareHttp1Backend::Drop` redundant `start_kill`) — no action.
- **C-M1** (PROGRESS Task 7 stale line numbers) — no action.
- **C-M2** (PROGRESS Task 7 corpus arithmetic) — no action.
- **C-M4** (PROGRESS Task 8 per-task test attribution) — no action.

**None of the new carryforwards gate the state-6 close-out OR any future phase.** All are awareness-only / close-opportunistically per the standing post-MVP-trunk discipline.

## §6. Phase-done gate (§7.5) verification

Per `BOOTSTRAP_PROMPT.md` §7.5 and 12.2 SPEC §1 acceptance signal (a)–(f), re-attested at the post-state-5-fold-in CI anchor — CI run **`26349590402`** at HEAD `adc50b7` (the state-5 fold-in commit) settled `completed / success` in `2m42s` (created `2026-05-24T02:23:08Z`). The Task 8 follow-up CI run `26348699615` at HEAD `b1cb25c` settled `completed / success` in 2m19s (the FIRST CI green of the state-3 execution arc). The state-4 commit `8dbea41` settled `completed / failure` on the cold-build readiness race, which the Task 8 follow-up `b1cb25c` closed mechanically (3s → 30s readiness deadline at `tests/differential/src/backend.rs:281`) — confirmed via `gh run list --branch main --limit 5` at this state-5 session. The state-5 fold-in commit `adc50b7` (this REVIEW.md's predecessor) re-attests via a fresh CI run. The state-4 session ALSO performed a full LOCAL bilateral Docker re-run corroborating every gate.

**(a) New differential fixture green.** ✓ — fixture `0019-upstream-active-health-check` GREEN at the local Docker re-run at the Task 8 state-4 verification (`cargo test -p differential -- --include-ignored` listed `upstream_active_health_check_fixture` at 6.33s — the load-bearing 12.2 differential proof bilaterally vs `envoyproxy/envoy:v1.33.0`). The discriminating observable (status 503 + body `no healthy upstream` byte-exact + 5-header presence) verified bilaterally per ADR-0037 + 10 REVIEW M1. CI re-attestation at the Task 8 follow-up `b1cb25c` + this state-5 fold-in `adc50b7`.

**(b) Pre-existing 18 differential fixtures (`0001-0018`) green simultaneously.** ✓ — all 18 (`0001-tcp-echo` through `0018-http-filter-fault`) GREEN simultaneously alongside the new `0019` in the local Docker re-run + the CI run `26348699615` (Task 8 follow-up, GREEN at 2m19s after the 4 RED runs the Task 8 follow-up closed). **19/19 in one run** bilaterally vs `envoyproxy/envoy:v1.33.0` — the load-bearing 12.2 regression-equivalence proof; the §5.4 inert-when-unconfigured invariant holds (the 18 pre-existing fixtures + the 0019 fixture all use STRICT_DNS clusters; only 0019 configures `health_checks`).

**(c) `h2spec` ≥95%.** ✓ — held vacuously at the parent-05 baseline **99.31%**. 12.2 touched zero H2-framing code (`git diff --name-only 6a3b332..adc50b7 | grep envoy-http2` → empty). No re-run required.

**(d) `parse_bootstrap` fuzz target clean for short-budget CI run.** ✓ — `cd crates/envoy-config && cargo +nightly fuzz run parse_bootstrap -- -runs=200000` at the Task 8 state-4 → `Done 200000 runs in 17 second(s)`, 0 crashes (cov 13414, ft 36698, corp 3485/2007Kb at completion; cov +111 / ft +2002 / corpus +281 entries vs the 12.1 state-4 baseline of cov 13303 — the new `hcm_upstream_active_health_check.yaml` seed [Task 7] materially extended coverage, as expected for a first-of-kind HCM + router + HC-configured-cluster + panic-disabled + route-gating seed). `fuzz_corpus_seeds_parse_or_reject_cleanly` SUCCESS array now 20 entries (was 19 at 12.1 close). Re-verified at this state-5 session via `cargo test -p envoy-config --lib fuzz_corpus_seeds_parse_or_reject_cleanly` → 1 passed.

**(e) Stable-toolchain gates clean (fmt / clippy / build / test / deny).** ✓ — all five clean in CI run `26348699615` + CI run `26349590402` (the state-5 fold-in) + reproduced LOCALLY at this state-5 session: `cargo build --workspace --all-targets` (`Finished dev profile [unoptimized + debuginfo] target(s) in 1m 11s` post-fold-in; warm cache); `cargo clippy --workspace --all-targets --all-features -- -D warnings` (`Finished dev profile [unoptimized + debuginfo] target(s) in 1m 07s` post-fold-in, no warnings); `cargo fmt --all -- --check` (clean exit 0; no diff); `cargo test --workspace` (**844 passed / 0 failed / 2 ignored** — 72 test-result lines aggregated; +12 new tests over the 12.1 baseline 832: Task 1 ×8 envoy-health unit + envoy-cluster accessor; Task 2 ×2 registration; Task 3 ×1 synth helper; Task 5 ×1 fixture-0019 differential; Task 6 ×2 backstop directions; +misc); `cargo deny check` (`advisories ok, bans ok, licenses ok, sources ok`; the pre-existing harmless `Unicode-DFS-2016` + `Zlib` unmatched-license warnings unchanged).

**(f) REVIEW.md verdict** lands at state-5 in this session per the `SKILL_ROUTING.md` state-5 transition. **Verdict per §1: Approved with M-track follow-ups** (zero Critical; zero Important; 11 active Minor carryforwards [3 closed in-phase, leaving 11 live: A-M2, A-M4, B-M1, B-M2, B-M3, B-M4, B-M5, B-M6, C-M1, C-M2, C-M4] — all awareness-only / close-opportunistically; none has a hard-gated owner; none gates the state-6 close-out). Gate (f) closes at this REVIEW.md verdict.

**All six §7.5 gates disposition: (a) PASS / (b) PASS / (c) PASS (vacuous, no H2 touch) / (d) PASS / (e) PASS / (f) CLOSE-at-this-REVIEW.md.**

## §7. Per-cluster code-quality reviewer finding dispositions

The phase-12.2 state-5 review ran a 3-cluster subagent pass over the reviewed range `6a3b332..adc50b7` (11 commits total). Each cluster reviewer read the actual on-disk code (`git diff` + `Read` + `Grep`) per the standing precedent-verification discipline, not the PROGRESS narrative. Disposition per cluster:

**Cluster A — Tasks 1–3 reviewer (periodic-probe primitive + synth-503 reconciliation) — Zero Critical + Zero Important + 4 Minors.** Verdict: **Ready to merge: Yes.** The reviewer verified on-disk: the new `crates/envoy-health/` crate in full (Cargo.toml, lib.rs, scheduler.rs, probe.rs, error.rs); the M2 single-writer-per-endpoint topology bilaterally (workspace-wide grep on `record_success`/`record_failure` confirms `probe.rs:51,56` are the ONLY production call sites — the load-bearing 12.1 REVIEW M2 forward-correctness-verification dependency closes); the `EndpointHealth` doc-comment at `health.rs:21-31` documents the contract explicitly at the API boundary; the `ClusterHandle::health_probe_targets()` accessor at `cluster.rs:236-254` is the minimal additive surface; the `envoy-bin` wiring at `main.rs:129-140` + the post-amend error-exit drain at `:413-431` (`first_err` pattern collects errors without short-circuiting; `health_scheduler.shutdown().await` always runs); the D6.2 helper at `hcm.rs:951-971` + the EXACTLY-ONE-LINE call-site change at `:585` + the connect-fail + send-fail 502 paths UNTOUCHED at `:524` + `:568`; the BEHAVIOR_CONTRACT 12.2 counter rows + M4 fold-in + new Response-body subsection structurally clean; the clean DAG verified cycle-free via Cargo.lock; `#![forbid(unsafe_code)]` at `lib.rs:1` per D-3.8; 8/8 envoy-health unit tests pass locally. Surfaced 4 Minors → **A-M1** (unused `bytes` dep — CLOSED at state-5 fold-in) + **A-M2** (`Scheduler::task_count()` `pub` visibility) + **A-M3** (PROGRESS deviation note — CLOSED at state-5 fold-in) + **A-M4** (`scheduler.rs:91` `.expect` un-tested negatively). **No residue rising above Minor.**

**Cluster B — Tasks 4–6 + Task 8 follow-up reviewer (synthetic-backend harness + fixture 0019 + in-process backstop) — Zero Critical + 2 Important (both CLOSED at state-5 fold-in) + 6 Minors.** Verdict: **Ready to merge: With fixes** (Importants closed at `adc50b7`; now **Ready: Yes** post-fold-in). The reviewer verified on-disk: the helper crate is hand-rolled per D-3.2 (only `anyhow + httparse + tokio + tracing + tracing-subscriber` — no forbidden `hyper`/`axum`/`warp`); `#![forbid(unsafe_code)]` at `src/main.rs:1`; helper binds `0.0.0.0:port` per ADR-0024-26 helper-bind discipline; `HealthAwareHttp1Backend` uses `tokio::process::Command` + `.kill_on_drop(true)` per 09 REVIEW M3; the Task 8 follow-up bump is mechanical (one-token + comment update) + correctly scoped + CI confirmed (`26348699615` green at 2m19s after 4 reds in a row); the `bytes` removal is clean (Cargo.lock diff scoped to `health-aware-http1-backend` only); the 06.3 REVIEW I2 down-payment attribution is honest per D-3.4; the Task 6 backstop covers both convergence directions + 5-header assertion + content-length=19; the settle math is sound (`settle_ms: 3500` ≥ 3500ms minimum); the workspace + ADR posture clean (`git diff -- docs/envoy-rust/DECISIONS.md` empty); the `Driver::Http1AfterSettle` dispatch is minimal + reads cleanly; ENVOY_TARGET pin unchanged. Surfaced 2 Importants → **I1** (stderr pipe-drain landmine — CLOSED at state-5 fold-in `adc50b7`) + **I2** (backstop `wait_ready(10s)` cold-cache race — CLOSED at state-5 fold-in `adc50b7`). Surfaced 6 Minors → **B-M1** (helper `parse_args` bounds) + **B-M2** (helper `status_reason` "OK" fallback) + **B-M3** (backstop `http1_get` parser brittleness) + **B-M4** (`HealthAwareHttp1Backend::Drop` wait-loop divergence) + **B-M5** (fixture YAML "identical-modulo-boilerplate") + **B-M6** (`HealthAwareHttp1Backend::Drop` redundant `start_kill`). **Post-fold-in residue: Zero Critical + Zero Important + 6 Minor.**

**Cluster C — Task 7 + docs reviewer (fuzz seed + cross-doc consistency + PLAN-lock-in audit) — Zero Critical + Zero Important + 4 Minors.** Verdict: **Ready to merge: Yes.** The reviewer audited all 25 PLAN lock-ins for on-disk compliance: lock-in #1 (no nest-split: 8 tasks confirmed via `git log`); lock-in #2 (no new ADR: ledger head stays `ADR-0037` with count 38; `git diff 6a3b332..HEAD -- docs/envoy-rust/DECISIONS.md` empty); lock-in #3 (§6.2 verification locked at parent-12 split); lock-in #7 (M4 fold-in at Task 2 — verified at `BEHAVIOR_CONTRACT.md:142`); lock-in #13 (integer-second durations in fixture + seed); lock-in #14 (synth_no_healthy_upstream helper at `hcm.rs:951`, `body.len()` pattern); lock-in #15 (EXACTLY-ONE-LINE `hcm.rs:585` change); lock-in #18 (BEHAVIOR_CONTRACT 12.2 block at `:144`); lock-in #23 (in-process backstop both directions + 5-header presence); lock-in #24 (Task 7 seed in ONE commit — verified via `git show --stat 39e55a5`); lock-in #25 (subagent-driven execution per PROGRESS `:242-247`). The 3-files-same-commit discipline holds; seed is tracked-not-gitignored; the SUCCESS-array test passes on the on-disk file; the seed YAML matches the PLAN-spec byte-for-byte modulo markdown fence delta; non-redundancy vs 12.1's `cluster_health_check.yaml` is real (HCM + router + panic-disabled + route gating); PROGRESS narrative truthfulness spot-checked across multiple claims (line numbers, file paths, exact-one-line changes); STATE.md structurally correct with prior subsections preserved verbatim; ROADMAP.md untouched (row 12.2 stays in-progress at state-5-prep per the closing-sub-phase invariant); CI gate fully closed bilaterally. Surfaced 4 Minors → **C-M1** (PROGRESS Task 7 stale line numbers) + **C-M2** (PROGRESS Task 7 corpus arithmetic tangled) + **C-M3** (STATE.md "THIS commit" wording staleness — CLOSED at this state-5 STATE.md advance) + **C-M4** (PROGRESS Task 8 per-task test-attribution arithmetic slightly off). **No residue rising above Minor.**

**Carryforward summary across the 3-cluster reviewer findings:** 0 Criticals + 2 Importants (both CLOSED in-phase at `adc50b7`) + 14 Minor (3 CLOSED in-phase at `adc50b7` and at this state-5 STATE.md advance; 11 active carryforwards). **Per `BOOTSTRAP_PROMPT.md` §5.2 the phase RE-ENTERED state 3** at `adc50b7` to close I1 + I2, then re-attested the gates (local + CI). Post-fold-in, no Critical or Important findings remain. The calibration matches the 06.x → 11 → 12.1 precedent (all landed `Approved with M-track follow-ups`) — the project's mature-state-3 doctrine working as designed; the only departure is that 12.2 surfaced (and closed) 2 holistic Importants whose latent-landmine nature evaded the per-task reviews.

## §8. State-4 evidence re-attestation

Per the state-5 reviewer's mandate to re-attest each of the §7.5 phase-done gate's six sub-gates against the state-4 anchor (PROGRESS Task 8 state-4 verification + the Task 8 follow-up CI green at `b1cb25c` + the state-5 fold-in re-attestation):

| Gate | Disposition | Evidence |
|---|---|---|
| **(a)** New differential fixture green | **PASS** | Fixture `0019-upstream-active-health-check` GREEN at the local Docker re-run at the Task 8 state-4 verification + re-confirmed via CI run `26348699615` at HEAD `b1cb25c` (the Task 8 follow-up CI green; 2m19s) + CI run `26349590402` (the state-5 fold-in re-attestation at HEAD `adc50b7`, 2m42s). The discriminating observable (status 503 + body `no healthy upstream` byte-exact + 5-header presence) verified bilaterally per ADR-0037. |
| **(b)** Pre-existing 18 fixtures (`0001-0018`) green simultaneously | **PASS** | All 18 GREEN simultaneously with the new `0019` in the local Docker re-run (`cargo test -p differential -- --include-ignored` → 127 passed / 0 failed / 0 ignored across 21 result lines) + CI run `26348699615`. **19/19 in one run** bilaterally vs `envoyproxy/envoy:v1.33.0`. The load-bearing 12.2 regression-equivalence proof; the §5.4 inert-when-unconfigured invariant holds (only 0019 configures `health_checks`). |
| **(c)** h2spec ≥95% | **PASS (vacuous)** | **99.31%** (parent-05 baseline, unaffected). 12.2 touched zero H2-framing code (`git diff --name-only 6a3b332..adc50b7 | grep envoy-http2` → empty). No re-run required. |
| **(d)** parse_bootstrap fuzz clean for short-budget CI run | **PASS** | `cargo +nightly fuzz run parse_bootstrap -- -runs=200000` at the Task 8 state-4 → `Done 200000 runs in 17 second(s)`, 0 crashes (cov 13414 vs 12.1 baseline 13303 — +111 from the new Task 7 seed). `fuzz_corpus_seeds_parse_or_reject_cleanly` SUCCESS array now 20 entries; re-verified at this state-5 session via `cargo test -p envoy-config --lib fuzz_corpus_seeds_parse_or_reject_cleanly` → 1 passed. CI `fuzz (parse_bootstrap)` job conclusion `success` in CI run `26348699615` + CI run `26349590402` (the state-5 fold-in). |
| **(e)** Stable-toolchain gates clean (fmt / clippy / build / test / deny) | **PASS** | All 5 within CI run `26348699615` + state-5 fold-in CI run clean: fmt clean; clippy `-D warnings` clean; build clean; test **844 passed / 0 failed / 2 ignored** (+12 over the 12.1 baseline 832 — the 12.2 state-3 unit + integration tests); cargo deny `advisories ok, bans ok, licenses ok, sources ok`. LOCAL re-run at this state-5 session reproduced `844 / 0 / 2` exactly post-fold-in. |
| **(f)** REVIEW.md approved | **CLOSE at this REVIEW.md** | Verdict per §1: **Approved with M-track follow-ups** (zero Critical; zero Important — 2 Importants CLOSED in-phase at `adc50b7`; 11 active Minor carryforwards [A-M2, A-M4, B-M1 to B-M6, C-M1, C-M2, C-M4] — all awareness-only / close-opportunistically; none has a hard-gated owner; none gates the state-6 close-out). Gate (f) closes at this verdict. |

**All six §7.5 gates: (a) PASS / (b) PASS / (c) PASS (vacuous) / (d) PASS / (e) PASS / (f) CLOSE-at-this-REVIEW.md.** Phase 12.2's state-4 evidence is structurally complete (the Task 8 follow-up `b1cb25c` closed the cold-build CI-red gap that the local-only state-4 verification mis-attested); state-5 closes at this REVIEW.md commit; state-6 (the phase-12.2 close-out + ROADMAP rows `12.2` AND `12` `in-progress → done` flip per the closing-sub-phase invariant + STATE.md advance to "awaiting next planning") is a separate session per `BOOTSTRAP_PROMPT.md` §5.1.

---

*End of REVIEW.md. Phase 12.2 state-5 complete. Verdict: **Approved with M-track follow-ups** (zero Critical; zero Important — 2 surfaced Importants I1+I2 CLOSED in-phase at state-5 fold-in `adc50b7`; 14 Minor surfaced — 3 closed in-phase, 11 active carryforwards). Per `BOOTSTRAP_PROMPT.md` §5.2, the phase RE-ENTERED state 3 at the state-5 fold-in to close I1+I2 + Cluster A M1 + A-M3, then re-attested the gates (5 stable-toolchain gates clean post-fold-in + workspace test count unchanged at 844/0/2 + CI re-attestation). Phase 12.2 is the **second Upstream-robustness-family REVIEW.md** and the **closing sub-phase** of the parent-12 split — the active HTTP health-check probe task (the project's FIRST periodic-background primitive in the new `envoy-health` crate; clean DAG over envoy-cluster/envoy-config/envoy-http1/envoy-stats/tokio with no cycle; M2 single-writer-per-endpoint topology bilaterally verified) + the no-healthy-upstream synth-503 body reconciliation per ADR-0037 (byte-exact `no healthy upstream` = 19 bytes; surgically narrow `hcm.rs:585` arm change with connect-fail + send-fail 502 paths UNTOUCHED) + fixture `0019-upstream-active-health-check` (the FIRST Upstream-robustness-family differential fixture; bilateral 19/19 Docker-gated fixtures green simultaneously) + the synthetic health-aware backend harness primitive (the 06.3 REVIEW I2 DOWN-PAYMENT; NOT full closure — residual stays with connection pooling) + the settle-then-probe driver + the in-process H1 backstop (both convergence directions; 5-standard-header presence assertion on 503 per 10 REVIEW M1) + the parse_bootstrap corpus seed extension (corpus 19 → 20). 12.2 ENGAGED carryforward closures: **12.1 REVIEW M2** (Task 1 API-boundary single-writer-contract comment + bilateral topology verification by Cluster A) + **12.1 REVIEW M4** (Task 2 ~1-LoC contract-row Equivalence-cell edit) + **06.3 REVIEW I2 DOWN-PAYMENT** (Task 4 — NOT full closure). NO ADR landed in the 12.2 lifecycle (PLAN lock-in #2); the DECISIONS.md ledger head is **ADR-0037** (count 38); next available **ADR-0038**. The next session enters the **phase-12.2 state-6 close-out** — flips ROADMAP rows `12.2` AND parent-12 `12` `in-progress → done` SIMULTANEOUSLY (the closing-sub-phase invariant; mirrors the 02.2 / 03.2 / 07.2 / 08.2 precedent); advances STATE.md to "awaiting next planning"; lands the state-6 close-out commit per the 12.2 SPEC §8 commit-message format `phase 12.2: active HTTP health-check probe task (envoy-health crate) + no-healthy-upstream body + fixture 0019 [parent 12 done] [ADR-0037]`. With the parent-12 close, the Upstream-robustness family has its FIRST closed phase + the FIRST 19-fixture Docker-gated bilaterally-green baseline + the foundation periodic-background primitive that future outlier-detection / circuit-breaker / retries / connection-pooling phases reuse.*
