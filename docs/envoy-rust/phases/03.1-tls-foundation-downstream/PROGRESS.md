# Phase 03.1 Progress

## Task 1 — ADRs 0018 + 0019 (2026-04-26)

- Commit: f93a062
- Change: appended ADR-0018 (rcgen + tempfile permitted as dev-test-harness-only foundations) and ADR-0019 (tokio-rustls + rustls-pemfile covered by the rustls foundations grant) to DECISIONS.md.
- Verification: `grep -c '^## ADR-00' docs/envoy-rust/DECISIONS.md` → 19 (ADR-0001 through ADR-0019).

## Task 2 — envoy-config — TransportSocket envelope + TLS context types (2026-04-26)

- Commit: db52844
- Change: Added 8 new types to `bootstrap.rs` (TransportSocket, TransportSocketTypedConfig, DownstreamTlsContext, UpstreamTlsContext, CommonTlsContext, TlsCertificate, CertificateValidationContext, DataSource — all with deny_unknown_fields); added `transport_socket: Option<TransportSocket>` to FilterChain and Cluster; extended `pub use` re-exports and added `TLS_TRANSPORT_SOCKET` constant in `lib.rs`; updated `ConfigError::Yaml` Display to include source message; fixed envoy-cluster test fixtures for new Cluster field. Landed 5 parse-shape tests (2 happy-path, 3 deny_unknown_fields regressions).
- Verification: `cargo test -p envoy-config bootstrap::tests` → 43 passed (38 pre-existing + 5 new); `cargo test -p envoy-cluster` → 8 passed; `cargo build --workspace --all-targets` + `cargo clippy --workspace --all-targets --all-features -- -D warnings` + `cargo fmt --all -- --check` all exit 0. Cargo.lock unchanged.

## Task 3 — envoy-config — TLS validator extensions + 5 new ConfigError variants (2026-04-26)

- Commit: 9202f31
- Change: Extended `ConfigError` in `lib.rs` with 5 new variants (UnknownTransportSocketName, MismatchedTransportSocketDirection, EmptyTlsCertificates, MissingValidationContext, EmptyUpstreamSni). Extended `validate(...)` in `bootstrap.rs` with per-cluster and per-listener TLS arms that enforce direction, certificate cardinality, validation_context presence, and SNI non-emptiness. Landed 7 new tests (rejects_unknown_transport_socket_name, rejects_downstream_tls_context_on_cluster, rejects_upstream_tls_context_on_listener, rejects_downstream_with_empty_tls_certificates, rejects_upstream_with_tls_certificates, rejects_upstream_without_validation_context, rejects_upstream_with_empty_sni).
- Verification: `cargo test -p envoy-config --lib` → 50 passed (38 base + 5 Task 2 + 7 Task 3). Workspace gate: `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check`, `cargo test --workspace --lib --bins` all exit 0. Per-crate counts: differential 31, envoy-bin 19, tcp-echo-server 8, envoy-config 50, envoy-listener 6, envoy-tcp 4, envoy-cluster 8 (total 126). Cargo.lock unchanged.
- Note on SPEC §D2 test-count drift: SPEC §D2 estimated 10 TLS-related tests across Tasks 2+3. Actual landed count is 12 (5 in Task 2 + 7 in Task 3). The two extras are rejects_upstream_without_validation_context and rejects_upstream_with_empty_sni — both SPEC-named validator tests that were included in the PLAN's Task 3 expansion (PLAN line 637 explains this as "reviewer-cost rounding"). Neither gate (LoC, task count) is materially affected.

## Task 4 — envoy-config — 3 fuzz corpus seeds (TLS-shaped YAML) (2026-04-26)

- Commit: 0273f0e
- Change: Created 3 new fuzz corpus seed files in `crates/envoy-config/fuzz/corpus/parse_bootstrap/`: (1) `tls_downstream_single_cert.yaml` — full happy-path downstream TLS termination config; (2) `tls_malformed_at_type.yaml` — parseable YAML with an unrecognized `@type` discriminator URL (`...UnknownContext`) that the `TransportSocketTypedConfig` tagged-enum rejects, exercising serde's default rejection on unknown variants; (3) `tls_upstream_validation_context.yaml` — full happy-path upstream TLS origination with SNI and validation context. Updated `.gitignore` to whitelist the three new seeds (corpus pattern was already in place). Renamed existing test `fuzz_corpus_tcp_proxy_seeds_parse` to `fuzz_corpus_seeds_parse_or_reject_cleanly`, rewrote it to parse all 4 happy-path seeds (tcp_proxy_single_endpoint, tcp_proxy_round_robin_triple, tls_downstream_single_cert, tls_upstream_validation_context) plus verify that tls_malformed_at_type rejects cleanly without panic, and re-assert that minimal.yaml (phase-00 baseline) still parses. Added `#[allow(clippy::single_element_loop)]` on rejection arm to permit future extension without code churn. envoy-config test count remains 50 (test was renamed, not added).
- Verification: `cargo test -p envoy-config bootstrap::tests::fuzz_corpus_seeds_parse_or_reject_cleanly` → 1 passed. Step 6 (local fuzz smoke test `cargo +nightly fuzz run parse_bootstrap -- -max_total_time=5`) skipped: `cargo-fuzz` not installed; CI fuzzer (ADR-0010 `max_total_time=30`) runs on every push. Workspace gate: `cargo build --workspace --all-targets` exit 0, `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0, `cargo fmt --all -- --check` exit 0, `cargo test --workspace --lib --bins` → all 7 crates pass (differential 31, envoy-bin 19, tcp-echo-server 8, envoy-config 50, envoy-listener 6, envoy-tcp 4, envoy-cluster 8; total 126). Cargo.lock unchanged.
- Note on Step 6 skip: cargo-fuzz CLI not available in current environment. CI runs the full `cargo +nightly fuzz run parse_bootstrap -- -max_total_time=30 corpus/parse_bootstrap/` job on every push per ADR-0010; the new seeds are automatically exercised there. Local verification deferred to push-time CI run (standard practice for ADR-0010 compliance).

## Task 5 — Scaffold crates/envoy-tls/ skeleton + workspace member (2026-04-26)

- Commit: 5f92c08
- Change: Created `crates/envoy-tls/Cargo.toml` (verbatim from PLAN lines 1388-1414) with `envoy-config` path dep, `rustls = "0.23"` (no-default-features, +std+tls12), `rustls-pki-types = "1"`, `rustls-pemfile = "2"`, `tokio-rustls = "0.26"` (no-default-features, +aws-lc-rs), `tokio` (net/io-util/macros/sync), `thiserror = "2"`, `tracing = "0.1"`; dev-deps `tokio` (rt-multi-thread/net/io-util/macros/sync/time), `rcgen = "0.13"`, `tempfile = "3"`. Created `crates/envoy-tls/src/lib.rs` as compiling stub with `#![forbid(unsafe_code)]` and crate-level doc comment (verbatim from PLAN lines 1420-1431). Added `crates/envoy-tls` to root `Cargo.toml` workspace members alphabetically between `crates/envoy-tcp` and `tests/differential`.
- Verification: `cargo build --workspace --all-targets` → `Compiling envoy-tls v0.0.0` visible, `Finished dev profile target(s) in 20.39s`; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo fmt --all -- --check` exit 0; `cargo test -p envoy-tls` → `test result: ok. 0 passed; 0 failed` (unit tests + doc-tests both 0). `cargo deny check` → `advisories ok, bans ok, licenses ok, sources ok`. `tokio-rustls = "0.26"` resolved to 0.26.4; both `aws-lc-rs` (hyphen, as PLAN) and `aws_lc_rs` (underscore alias) are valid feature names — PLAN form used, no deviation. `RUSTSEC-2025-0134` (rustls-pemfile unmaintained) continue to be covered by existing `[advisories].ignore`. Cargo.lock changed (new deps: rustls, aws-lc-rs, aws-lc-sys, tokio-rustls, rustls-pki-types, rustls-pemfile, rcgen, etc.) but NOT staged per project convention. Files staged: `Cargo.toml` (root), `crates/envoy-tls/Cargo.toml`, `crates/envoy-tls/src/lib.rs`.

## Task 6 — envoy-tls::DownstreamTls + cert loader + SingleCertResolver + 7 tests (2026-04-26)

- Commit: 05301bf
- Change: Replaced `crates/envoy-tls/src/lib.rs` stub with full implementation: `TlsError` enum (8 variants: FileRead, CertParse, KeyParse, RustlsConfig, InvalidServerName, Handshake, CaParse, DownstreamRequiresCert), `DownstreamTls` struct with `from_context` + `accept`, `SingleCertResolver` (private, implements `ResolvesServerCert`, returns same `Arc<CertifiedKey>` for any `ClientHello`), `load_certified_key` (private fn, reads cert+key PEMs via `rustls-pemfile`, produces `CertifiedKey` via `aws_lc_rs::sign::any_supported_type`). Created `crates/envoy-tls/src/tests.rs` with `mod pki { build(), ds_context_with() }` test helper (CA + leaf via rcgen 0.13, written to TempDir) + 7 tests: `loads_single_cert_server_config` (loopback TLS handshake with TLS version assertion), `rejects_empty_tls_certificates`, `rejects_malformed_cert_pem`, `rejects_missing_key_pem`, `single_cert_resolver_returns_same_cert_regardless_of_sni` (3-SNI loop, server-side only assertion), `accept_returns_handshake_error_on_garbage_input`, `crypto_provider_install_is_idempotent`.
- Verification: `cargo test -p envoy-tls` → `test result: ok. 7 passed; 0 failed`; `cargo build --workspace --all-targets` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo fmt --all -- --check` exit 0. Cargo.lock modified but not staged.
- API-drift adjustments:
  1. **`ProtocolVersion` no `PartialOrd` (PLAN line 1698):** PLAN used `>=` comparison on `rustls::ProtocolVersion`. In rustls 0.23 the enum does not implement `PartialOrd`. Adjusted to `matches!(v, TLSv1_2 | TLSv1_3)` — semantically equivalent (both supported versions satisfy ≥ 1.2).
  2. **`DownstreamTls` needs `#[derive(Debug)]` (not in PLAN):** `expect_err` requires `T: Debug` on `Result<T, E>`; added `#[derive(Debug)]` to `DownstreamTls`. The struct only wraps `Arc<ServerConfig>` which derives `Debug` in rustls 0.23.
  3. **Clippy `ptr_arg` on `ds_context_with` params (PLAN line 1629):** PLAN signature used `&PathBuf`; clippy -D warnings requires `&Path`. Changed to `&Path` — call sites pass `&PathBuf` which auto-derefs.
  4. **rcgen 0.13.2 `KeyPair::generate()` (no-arg):** Confirmed available at rcgen 0.13.2 — PLAN form used verbatim, no adjustment needed.
  5. **`CertificateDer::into_owned()`:** Confirmed in `rustls-pki-types 1.14.0` — PLAN form used verbatim.

## Task 8 — envoy-tcp: generic-stream lift + 4 TLS-flavored tests (2026-04-26)

- Commits: d038c4b (code) + ada727b (progress note)
- Change: Generalized `TcpProxy::handle` from concrete `TcpStream` to `pub async fn handle<S>(&self, downstream: S) -> Result<...> where S: AsyncRead + AsyncWrite + Unpin + Send + 'static`. Rewrote `ConnectionHandler for TcpProxy` as a thin wrapper: clones `cluster` (ClusterHandle, Arc-internal) and `cluster_name` (String), constructs a fresh `TcpProxy` inside the `Box::pin(async move { ... })` block, and calls `proxy.handle::<tokio::net::TcpStream>(downstream)`. Swapped `downstream.into_split()` (TcpStream-specific) for `tokio::io::split(downstream)` (generic). Added 4 new tests: `proxies_payload_through_tls_downstream_stream`, `proxies_payload_with_plaintext_stream_unchanged`, `tls_downstream_proxy_closes_upstream_on_downstream_close`, `tls_downstream_proxy_returns_err_on_upstream_connect_refused`.
- Dev-deps added to envoy-tcp Cargo.toml: `rcgen = "0.13"`, `rustls = "0.23"` (no-default-features, +std+tls12), `rustls-pemfile = "2"`, `rustls-pki-types = "1"`, `tokio-rustls = "0.26"` (no-default-features, +aws-lc-rs). `tempfile = "3"` omitted (PLAN listed it but none of the 4 tests use it — cleaner without).
- **Plan correction (CRITICAL note 1):** PLAN's Task 8 dev-deps list did not include `rustls-pemfile`. All 4 tests call `rustls_pemfile::private_key(...)`. Added `rustls-pemfile = "2"` to dev-deps to fix the missing import.
- **Trait-impl-clones-fields pattern (PLAN line 2664 / SPEC §6 signpost 3, option α):** The `ConnectionHandler::handle` trait method takes `&self` but the `BoxFuture` must be `'static`. We cannot capture `self` directly. Option α (clone the two Arc-bearing fields and reconstruct a `TcpProxy` in the async block) was chosen to preserve the trait shape verbatim (zero diff to `envoy-listener`). `ClusterHandle` is `Arc<Cluster>` internally (`#[derive(Clone, Debug)]`), so the clone is O(1) reference-count increment.
- **`tokio::io::split` vs `into_split`:** `into_split()` is `TcpStream`-only. `tokio::io::split(downstream)` works for any `AsyncRead + AsyncWrite + Unpin`. Upstream side retains `into_split()` since upstream is always `TcpStream` in phase 03.1.
- Verification: `cargo test -p envoy-tcp` → 8 passed (4 phase-02.2 base + 4 new); workspace gate: `cargo build --workspace --all-targets`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check`, `cargo test --workspace --lib --bins` all exit 0. Per-crate counts: differential 31, envoy-bin 19, tcp-echo-server 8, envoy-config 50, envoy-listener 6, envoy-tcp 8, envoy-cluster 8, envoy-tls 10 (total 140). Cargo.lock modified but not staged.

## Task 9 — envoy-bin: TLS handler adapter + crypto provider install + integration test (2026-04-26)

- Commits: b28dc0a (initial), 5ee49f1 (second sub-task commit), 1799cc5 (refactor: route crypto provider install through envoy-tls)
- Change (b28dc0a + 5ee49f1): Updated `crates/envoy-bin/Cargo.toml`: added `envoy-tls = { path = "../envoy-tls" }` and `rustls = "0.23"` (no-default-features, +std+tls12) to `[dependencies]`; added `rcgen = "0.13"`, `rustls = "0.23"` (dev), `rustls-pki-types = "1"`, `tokio-rustls = "0.26"` (no-default-features, +aws-lc-rs) to `[dev-dependencies]`. Created `crates/envoy-bin/src/tls_handler.rs`: `TlsAcceptingHandler { tls: Arc<DownstreamTls>, inner: Arc<TcpProxy> }` with `ConnectionHandler::handle` impl that calls `tls.accept(downstream).await` then delegates `inner.handle(post_handshake).await` (inherent generic method on TcpProxy). Modified `crates/envoy-bin/src/main.rs`: added `mod tls_handler;` declaration; inserted crypto provider install after `parse_bootstrap`; replaced the `TCP_PROXY_FILTER` arm with TLS-aware variant. Created `crates/envoy-bin/tests/tls_downstream.rs`: rcgen PKI (CA + leaf for a.example.com), `build_pki()` helper, in-process plaintext echo backend, `CARGO_BIN_EXE_envoy-bin` child process with YAML config, `tokio_rustls::TlsConnector` client, byte-exact round-trip assertion.
- Change (1799cc5 — follow-up refactor per SPEC §3 D1 / ADR-0019): Added `pub fn install_default_crypto_provider() -> Result<(), Arc<rustls::crypto::CryptoProvider>>` to `crates/envoy-tls/src/lib.rs` (doc comment notes the architectural reason). Updated `crates/envoy-bin/src/main.rs` to call `envoy_tls::install_default_crypto_provider()` instead of `rustls::crypto::aws_lc_rs::default_provider().install_default()`. Removed `rustls` from `[dependencies]` in `crates/envoy-bin/Cargo.toml`; `rustls` remains in `[dev-dependencies]` (used by `tests/tls_downstream.rs`). envoy-bin no longer holds a direct rustls runtime dep — routing through envoy-tls restores compliance with SPEC §3 D1 and ADR-0019. Return type is `Result<(), Arc<CryptoProvider>>` (what rustls 0.23 `install_default()` actually returns; the PLAN doc-comment said `Result<(), CryptoProvider>` — matched to actual API).
- Verification: `cargo test -p envoy-bin --test tls_downstream` → `test result: ok. 1 passed; 0 failed`. Workspace gate (post-refactor): `cargo build --workspace --all-targets` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo fmt --all -- --check` exit 0; `cargo test --workspace --lib --bins` → all crates pass (differential 31, envoy-bin 19, tcp-echo-server 8, envoy-config 50, envoy-listener 6, envoy-tcp 8, envoy-cluster 8, envoy-tls 10; total 140). `cargo test -p envoy-bin --test tls_downstream` → 1 passed. Cargo.lock modified but not staged.
- Deviations from PLAN:
  1. **`rustls` runtime dep in envoy-bin closed by refactor (1799cc5)**: Initial commit added `rustls` to `[dependencies]` because `main.rs` called `rustls::crypto::...` directly (SPEC §3 D1 violation). Refactor routes the call through `envoy_tls::install_default_crypto_provider()` and removes the direct dep, restoring architectural compliance.
  2. **Integration test passes `-c` flag** (PLAN omitted it): PLAN test code at line 3267 passed `.arg(cfg_file.path())` without `-c`. `argv::parse_argv` requires `-c <path>` or `--config-path <path>`; bare positional paths are rejected with `ArgvError::UnknownFlag`. Added `.arg("-c")` before `.arg(cfg_file.path())` to match the existing `tcp_proxy.rs` test pattern.
  3. **`let mut leaf_params` → `let leaf_params`** (clippy unused-mut warning): PLAN line 3180 used `let mut`. `CertificateParams::new()` returns the params directly (no mutability needed before `signed_by`). Changed to `let leaf_params` to keep clippy -D warnings clean.
  4. **`cargo fmt` reformatted three blocks**: `if let Some(ts) = chain.transport_socket.as_ref()` in `main.rs`, the `tls.accept(...).map_err(...)` chain in `tls_handler.rs`, and `connector.connect(...).await` in `tls_downstream.rs`. All semantically identical, just reformatted to rustfmt's line-length wrapping style.

## Task 10 — Differential harness: TlsTestPki + Driver::TlsTcp + render_yaml TLS keys (2026-04-26)

- Commit: 898e172
- Change: Created `tests/differential/src/tls.rs` with `TlsTestPki` struct (7 pub path fields + private `_dir: TempDir`), `generate()` (CA + leaf-a/leaf-b/server via rcgen 0.13), `envoy_side_paths()` (container `/etc/envoy-rust-tls/` map), `subject_side_paths()` (host tmpdir map), `container_mounts()` (Vec of host/container pairs), private `build_ca()` + `build_leaf()` helpers. Added `pub const ENVOY_SIDE_DIR`. Updated `tests/differential/Cargo.toml` to add `rcgen = "0.13"`, `rustls = "0.23"` (no-default-features, +std+tls12), `rustls-pemfile = "2"`, `rustls-pki-types = "1"`, `tokio-rustls = "0.26"` (no-default-features, +aws-lc-rs) to `[dependencies]` (not dev-deps). Added `pub mod tls;` to `tests/differential/src/lib.rs`. Extended `Driver` enum with `TlsTcp { sni: String, #[serde(default)] expected_cn: Option<String> }` variant. Added `Driver::TlsTcp { .. } => "PORT"` arm in `run_fixture`'s port_key match and a stub `bail!` arm in the dispatch match (wired up in Task 11). Added 2 render_yaml tests in lib.rs::tests (`render_yaml_substitutes_tls_paths_for_envoy_side`, `render_yaml_substitutes_tls_paths_for_subject_side`).
- Verification: `cargo test -p differential tls::tests` → 4 passed; `cargo test -p differential render_yaml_substitutes_tls_paths` → 2 passed; `cargo test -p differential --lib` → 37 passed (31 pre-existing + 6 new), 1 ignored (Docker-gated). Workspace gate: `cargo build --workspace --all-targets` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo fmt --all -- --check` exit 0; `cargo test --workspace --lib --bins` → all crates pass (differential 37, envoy-bin 19, tcp-echo-server 8, envoy-config 50, envoy-listener 6, envoy-tcp 8, envoy-cluster 8, envoy-tls 10; total 146). Cargo.lock modified but not staged.
- Deviation from PLAN: Added `Driver::TlsTcp` arms to both match sites in `run_fixture` (port_key: `"PORT"`; dispatch: stub `bail!`). The PLAN's Step 4 says to extend the enum but doesn't explicitly note that the exhaustive matches in `run_fixture` must also be updated — this is a necessary consequence of Rust's exhaustive pattern checking. The stub `bail!` is the correct placeholder until Task 11 wires up `drive_tls`.

## Task 7 — envoy-tls::UpstreamTls + CA loader + 3 tests (2026-04-26)

- Commit: 6bd308d
- Change: Extended `crates/envoy-tls/src/lib.rs` with: new imports (`ClientConfig`, `RootCertStore`, `ServerName`), `UpstreamTls` struct (`#[derive(Debug)]`, fields `config: Arc<ClientConfig>` + `server_name: ServerName<'static>`) with `from_context(cfg: &UpstreamTlsContext) -> Result<Self, TlsError>` (loads CA PEM into `RootCertStore`, builds `ClientConfig`, rejects IP-literal SNI via `parse_dns_server_name`) and `async fn connect(upstream: TcpStream) -> Result<TlsStream<TcpStream>, TlsError>` (drives `TlsConnector::connect`). Added private `load_root_store(ca_path: &Path)` (reads CA PEM via `rustls-pemfile::certs`, populates `RootCertStore`, rejects empty/malformed input with `TlsError::CaParse`) and private `parse_dns_server_name(sni: &str)` (parses via `ServerName::try_from`, accepts `DnsName` only, rejects `IpAddress` + catch-all with `TlsError::InvalidServerName`). Extended `crates/envoy-tls/src/tests.rs` with `mod upstream_pki` helper (CA + server cert with SAN `envoy-rust.test`, writes to TempDir, builds `CertifiedKey` via rustls-pemfile) and 3 tests: `loads_upstream_client_config` (loopback handshake, version assertion), `upstream_rejects_invalid_sni` (IP literal `127.0.0.1` → `TlsError::InvalidServerName`), `upstream_rejects_untrusted_cert` (different CA's server cert → `TlsError::Handshake`).
- Verification: `cargo test -p envoy-tls` → `test result: ok. 10 passed; 0 failed` (7 from Task 6 + 3 from Task 7). Workspace gate: `cargo build --workspace --all-targets` exit 0; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0; `cargo fmt --all -- --check` exit 0. Cargo.lock modified but not staged.
- API-drift adjustments:
  1. **`ProtocolVersion` no `PartialOrd` (PLAN line 2221):** Same as Task 6 — adjusted `>=` comparison to `matches!(v, TLSv1_2 | TLSv1_3)`.
  2. **`UpstreamTls` needs `#[derive(Debug)]` (not in PLAN):** `expect_err` requires `T: Debug`; added `#[derive(Debug)]` (wraps `Arc<ClientConfig>` + `ServerName<'static>`, both are `Debug`).
  3. **Clippy `ptr_arg` on `us_context_with` (PLAN line 2156):** PLAN used `&PathBuf`; clippy -D warnings requires `&Path`. Changed to `&std::path::Path` — call sites pass `&pki.ca_pem` (`PathBuf`) which auto-derefs.
  4. **`load_root_store` simplified per CRITICAL note 1:** Dropped the dead-code `.or_else(|e| Err(e))?` from the PLAN; used the cleaner single `.map_err(|_| TlsError::CaParse {...})?` form.
  5. **`DnsName::to_owned()` confirmed:** `rustls-pki-types 1.14.0` `DnsName<'a>` has `to_owned() -> DnsName<'static>`; PLAN form used verbatim.

## Task 11 — Differential harness: drive_tls + run_fixture TLS dispatch + upstream::start PEM mounts (2026-04-26)

- Commit: (see below)
- Change:
  - `tests/differential/src/upstream.rs`: Updated `start` signature to `async fn start(envoy_yaml_path: &Path, host_gateway: bool, tls_pki: Option<&crate::tls::TlsTestPki>) -> Result<UpstreamProxy>`. Body iterates `pki.container_mounts()` calling `request = request.with_copy_to(container_path, host_path)` for each PEM pair. Updated the `starts_upstream_envoy_and_exposes_host_port` test call site to pass `None`.
  - `tests/differential/src/lib.rs`: Added `drive_tls` (async, mirrors `drive_tcp`'s ADR-0006/0007 discipline over a `tokio_rustls::TlsConnector`; optional `expected_cn` check via `check_cn_or_san`). Added `check_cn_or_san` (DER-substring scan, lowercase; intentionally minimal for 03.1's rcgen-built PKI — PLAN line 4054 defers `x509-parser` to a follow-up ADR). Extended `run_fixture`: (a) TLS template detection (`needs_tls_pki` via `contains` on 7 tokens), (b) `port_key` now uses `TcpEcho | TlsTcp { .. }` combined arm, (c) substitution maps changed from `Vec<(&str, &str)>` to `Vec<(&str, String)>` with `upstream_tls_paths`/`subject_tls_paths` merges, (d) `_refs` intermediates at both `render_yaml` call sites, (e) `tls_pki.as_ref()` threaded to `upstream::start`, (f) `Driver::TlsTcp` dispatch arm replaces the Task-10 `bail!` stub with real `drive_tls` calls + `RootCertStore` construction from `pki.ca_pem_path`.
- API-drift from PLAN:
  - **`with_copy_to_container` does not exist in testcontainers 0.23.3.** The actual API is `with_copy_to(target: impl Into<String>, source: impl Into<CopyDataSource>)` where `PathBuf` implements `From<PathBuf> for CopyDataSource`. Adapted argument order: `request.with_copy_to(container_path, host_path)`. No ADR required (mechanical surface adaptation per SPEC §6 signpost 7).
  - **`check_cn_or_san` uses DER-substring scan (intentional):** Per PLAN line 4054, the minimal DER scan is sufficient for the harness's rcgen-built certs where SAN values are unique ASCII strings. Phase 03.2 may pull `x509-parser` under a follow-up ADR if structured cert introspection is needed.
  - **`rcgen::KeyPair::generate()` scratch key in `check_cn_or_san` removed:** PLAN included a scratch `KeyPair::generate()` invocation (lines 4023-4024) that was immediately silenced with `let _ = kp`. Removed after rustfmt and cleanup — the DER scan does not use rcgen at all; the scratch key was a vestigial artifact from an earlier draft approach. No semantic change.
- Verification: `cargo test --workspace --lib --bins` → all 8 crates pass (differential 37 + 1 ignored, envoy-bin 19, tcp-echo-server 8, envoy-config 50, envoy-listener 6, envoy-tcp 8, envoy-cluster 8, envoy-tls 10; total 146). `cargo build --workspace --all-targets` exit 0. `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0. `cargo fmt --all -- --check` exit 0.
