# Phase 20 (`20-xds-file-based-rds`) — PROGRESS

> Running log, updated by the executor on each task completion (the 06.2 → 19 cadence).
> One entry per PLAN task; quote the verifying command output. The state-3 arc runs
> `cargo clippy --workspace --all-targets --all-features -- -D warnings` PER TASK
> (NOT deferred to state-4) per `project_state3_arc_skips_clippy`.

**PLAN:** `docs/envoy-rust/phases/20-xds-file-based-rds/PLAN.md`
**SPEC:** `docs/envoy-rust/phases/20-xds-file-based-rds/SPEC.md`
**Scope ADRs:** ADR-0051 (the xDS-family continuation pick [file-based RDS over CDS/LDS file watching / file-based EDS / the blocked gRPC family] + the four §0 findings [the config-source machinery + envelope parser are reused, the new shape is the HCM `route_config` XOR `rds` mutual exclusivity — the FIRST HCM-scoped dynamic resource; the merge-into-effective-`route_config` design needs no HCM runtime refactor; RDS stats are PER-HCM-scoped — a new registration topology; no pre-existing allow-listed Envoy-side RDS surface — the §6.2 stat verification is load-bearing] + the minimum-viable scope/deferral ledger). ADR-0052 (the §6.2 empirical-verification reconciliation — **FIRES**: item 5 [the `/config_dump` `configs[]` ordering] diverged; items 1 + 4/6 MATCHED).

---

## State-2 PLAN-write (this commit)

- Performed the HEAVY SPEC §6.2 empirical verification against `envoyproxy/envoy:v1.33.0` (digest `sha256:56da5afd…`; Docker on darwin; foreground general-purpose subagent; an `rds`-configured static-listener HCM [`stat_prefix: ingress_http`, `rds: { route_config_name: local_route, config_source: { path_config_source: { path: /etc/envoy-rds/rds.yaml } } }`, NO inline `route_config`] + a `cds_config` composition [cluster `dynamic_backend`] + a host HTTP backend + admin `/stats`, `/stats?format=json`, `/config_dump` scrapes + the missing/malformed/semantic-invalid/`route_config_name`-mismatch/both/neither negative variants). Findings L1–L11 locked into PLAN.md "§6.2 empirical lock-ins" + ADR-0052. **Items 1 + 4/6 CONFIRM the projections; item 5 DIVERGES → ADR-0052 FIRES.**
  - **L1 (envelope / `rds`-on-HCM) MATCH:** both bare `resources:` + full `DiscoveryResponse` accepted; per-resource `@type: …route.v3.RouteConfiguration`; HCM block `rds: { route_config_name, config_source }`; `resource_api_version` OPTIONAL. Mirrors CDS/LDS.
  - **L2 (readiness) MATCH:** RDS route table active before `/ready` 200; no warm-up (init manager blocks on RDS). envoy-rust mirrors via synchronous load.
  - **L3 (per-HCM stats) MATCH, `config_reload` IN the subset:** prefix `http.<stat_prefix>.rds.<route_config_name>.`; the 5-name subset `update_attempt:1 / update_success:1 / update_failure:0 / update_rejected:0 / config_reload:1` (config_reload DOES tick at initial load). Envoy-only (NOT asserted): `version`/`version_text`/`update_time`/`config_reload_time_ms`/`update_empty`/`init_fetch_timeout`/`update_duration`.
  - **L4 (negative-path) MATCH:** the CDS/LDS 3-way split (missing=Envoy-hard-exit; malformed=`update_failure:1`+serve; semantic-invalid=`update_rejected:1`+serve). envoy-rust = ALL FATAL (the ADR-0049 decision-2 all-fatal posture extended to RDS) → `update_failure`/`update_rejected` register at 0, structurally unreachable; backstop-only.
  - **L6 (`route_config_name` mismatch) MATCH:** Envoy `update_rejected:1` + runtime 404; envoy-rust FATAL (`RdsRouteConfigNotFound`); backstop-only.
  - **L7 (RDS+CDS composition) — an RDS→CDS route needs NO `validate_clusters: false`** (RDS behaves like LDS, NOT CDS-static; the ADR-0050 L6 finding CONFIRMED for the RDS context). envoy-rust keeps defer-then-revalidate (route to a cluster in NEITHER list → `UnknownCluster` fatal — the recorded narrow divergence). The shared `rds.yaml` carries no `validate_clusters`.
  - **L8 (wire shape + field tolerance) MATCH + RDS file SHAREABLE:** the response is shape-identical to inline; a minimal RouteConfiguration needs only `name` + vh `name`/`domains` + a route `match` + an action — all envoy-rust accepts → **fixture 0028 uses ONE shared `rds.yaml`** (rendered per-side via the kv map, like the shared `cds.yaml`), NOT the per-side templates phase-19 LDS needed.
  - **L9 (both/neither) MATCH (both fatal on BOTH proxies):** Envoy both→`oneof` reject, neither→PGV `route_specifier required`; envoy-rust parse-time exactly-one-of → `AmbiguousRouteSource`/`MissingRouteSource`. No differential divergence.
  - **L10 (conditionality) MATCH:** an inline-`route_config` HCM emits ZERO `http.<prefix>.rds.*` names. envoy-rust gates on the owning HCM's `rds.is_some()`.
  - **L11 (version) Envoy-only;** not asserted.
  - **L5 (RoutesConfigDump shape + `configs[]` ordering) DIVERGES — the ADR-0052 trigger:** the dynamic entry is `{ "@type": ".../RoutesConfigDump", "dynamic_route_configs": [ { "route_config": { "@type": ".../RouteConfiguration", "name", "virtual_hosts" }, "last_updated" } ] }` (NO `version_info`); **Envoy's `configs[]` order is Bootstrap[0] Clusters[1] Listeners[2] ScopedRoutes[3] Routes[4] Secrets[5]** — Routes at index **4**, not the projected 3 (Clusters[1]/Listeners[2] NOT displaced → fixtures 0026/0027 hold); Envoy ALSO emits a `RoutesConfigDump` (`static_route_configs`) even without RDS. **Reconciliation (ADR-0052):** envoy-rust keeps conditional emission (rds-only) → lands at envoy-rust `configs[2]` on fixture 0028; the index mismatch (envoy-rust [2] vs Envoy [4]) is bridged by a per-side `JsonSubtreeRule` path override in the harness (PLAN Task 6) used by fixture 0028's config_dump assertion (Task 7).
- Performed the PLAN-time SPEC-correction pass (read-only Explore subagent + controller re-verification by direct grep) against HEAD `a3ef29786`. **All 13 SPEC §0/§3 anchors CONFIRMED; zero drift.** Corrections recorded in PLAN.md "PLAN-time SPEC corrections": C9 (H2 reuses `HCMConfig` via type alias — no separate H2 production read site/stats registration; only the 8 H2 test construction sites migrate); C15 (the curated fuzz corpus is **30**, not the Explore's reported 31 — the enumerated `.gitignore` list is 30 → 31 with the new seed, matching SPEC §1 (d)); C16 (the exactly-one-of check runs at PARSE time, NOT in the post-merge re-validation — avoids the both-Some-after-load false positive); C17 (the `xds_file.rs` consolidation [M19-1] is DEFERRED by deliberate risk-managed decision — write a new `rds.rs` mirroring `lds.rs` to keep the two green CDS/LDS modules untouched under the less-margin budget). D1 migration enumerated: **11 `HttpConnectionManagerConfig` struct-literal construction sites** (3 in `envoy-http1` tests `:2525`/`:2596`/`:3233`; 8 in `envoy-http2` tests) + production read sites (`bootstrap.rs:2370-2375` validator, `envoy-http1/src/hcm.rs:200`/`:211` `clone_route_config`, `:1177` vhost match) + ~9 `bootstrap.rs` test reads.
- Evaluated the §6.1 split gate against the §6.2-refined surface (**~1100–1450 LoC / 11 tasks**; the per-side harness addition is ~15 LoC, the L8 shared-`rds.yaml` simplification removes the per-side-RDS-template cost) → **single un-split phase; ADR-0053 does NOT fire** (reserved-but-unconsumed).
- Flipped ROADMAP row `20` `planned → in-progress`. Advanced STATE.md to `20` state-2-complete / state-3-next.

## Task 1 — `envoy-config` schema (`rds` + `route_config`→Option + exactly-one-of validator + the D1 migration sweep)

**Preamble (read before starting):**
- **Goal:** Add the `Rds { route_config_name, config_source }` struct (reusing `ConfigSource`/`PathConfigSource` verbatim; `deny_unknown_fields` on `Rds`), change `HttpConnectionManagerConfig.route_config` from `RouteConfiguration` to `Option<RouteConfiguration>` + add `rds: Option<Rds>`, add the parse-time exactly-one-of check (`check_route_sources` → `MissingRouteSource`/`AmbiguousRouteSource`), make `validate_hcm` skip inline-route validation when `route_config` is `None`, add the 5 new `ConfigError` variants, and FIX ALL 11 construction sites + the read sites in the same commit. TDD per the PLAN Task 1 test list (groups a–g).
- **§6.2 lock-ins that bind this task:** L1 (`Rds` shape = `route_config_name` + `config_source`; `resource_api_version` optional inside `config_source` — already optional in the reused struct); L9 (both/neither both fatal — the exactly-one-of variants); the `api_config_source`/`ads`/`watched_directory` deferral stays rejected by `ConfigSource`'s existing `deny_unknown_fields` (unchanged).
- **PLAN-time corrections that bind this task:** C9 (the 8 H2 construction sites are test-only — H2 reuses `HCMConfig`); C16 (exactly-one-of at PARSE time via `check_route_sources`, callable over the merged set in Task 3 — NOT inside `validate()`; `validate_hcm` must early-return `Ok` when `route_config` is `None`).
- **Anchors (verified at HEAD `a3ef29786`):** `HttpConnectionManagerConfig` `crates/envoy-config/src/bootstrap.rs:524-549` (`route_config` `:547`, `stat_prefix` `:527`); `ConfigSource`/`PathConfigSource` `:107-123` (reused by `Rds`); `RouteConfiguration` `:1017-1030`; `validate_hcm` `:2330` (reads `:2370-2375`); the validator HCM-extraction pattern `:2151`; `ConfigError` (`Cds*`/`Lds*` variants) in `lib.rs:75-96`; the 11 construction sites + the read sites enumerated in PLAN "PLAN-time SPEC corrections" / the D1 carry-forward warning.
- **Carry-forward warning (D1 — the workspace stays RED until done):** `route_config` becoming `Option` breaks every construction + read site across `envoy-http1` + `envoy-http2` + `envoy-config` tests. Fix ALL 11 construction sites (`route_config: Some(rc), rds: None`) + the read sites in THIS commit (the phase-18/19 struct-literal-sweep lesson). Run `cargo build --workspace --all-targets` before committing; do NOT split the sweep across commits.
- **Verification:** `cargo test -p envoy-config` (PASS; inline-route fixtures parse identically) + `cargo build --workspace --all-targets` + the 4 standalone-crate builds (per `project_isolated_crate_build_blindspot`) + `cargo clippy --workspace --all-targets --all-features -- -D warnings` + `cargo fmt --all -- --check`.

**Completion (code commit `ba34125f`):**

- **Implemented:** the `Rds { route_config_name, config_source }` struct (`bootstrap.rs:113-118`; `deny_unknown_fields`; derive set matches `ConfigSource`; `config_source` reused verbatim, `resource_api_version` stays optional inside it — L1); `HttpConnectionManagerConfig.route_config` → `Option<RouteConfiguration>` + sibling `rds: Option<Rds>` (`bootstrap.rs:564-568`, both `#[serde(default, skip_serializing_if = "Option::is_none")]`); the 5 `ConfigError` variants `MissingRouteSource`/`AmbiguousRouteSource`/`RdsFileError`/`RdsParseError`/`RdsRouteConfigNotFound` (`lib.rs:97-126`, CDS/LDS `#[source]` style; `Rds` re-exported `lib.rs:24`); `check_route_sources(&Bootstrap)` as `pub(crate)` (`bootstrap.rs:2502-2527`; walks static listeners' filter chains; `(route_config.is_some(), rds.is_some())` truth table), called in `parse_bootstrap` (`lib.rs:574`) AFTER deserialize, BEFORE `validate()` — the C16 parse-time placement; `validate_hcm` Option-guard (`bootstrap.rs:2395-2398` `let Some(route_config) = hcm.route_config.as_mut() else { return Ok(()) }`; all inner reads rewired; no cardinality re-check, so the post-load both-Some state stays valid).
- **D1 migration sweep — EXACTLY 11 construction sites** (the envoy-config struct, pattern `route_config: RouteConfiguration {`): envoy-http1 `hcm.rs` ×3 + envoy-http2 `hcm.rs` ×8 → each `route_config: Some(...), rds: None,`. The 15 `route_config: Arc::new(RouteConfiguration {` LOCAL-struct sites (the `HCMConfig.route_config: Arc<RouteConfiguration>` field, `envoy-http1/src/hcm.rs:113`) left UNTOUCHED. Read sites: `envoy-http1/src/hcm.rs:200` → `cfg.route_config.as_ref().expect("route_config populated post-load — §5.3 invariant")`; the 6 `bootstrap.rs` test reads → `.route_config.as_ref().unwrap()...`; the local `:1177` vhost match (reads the `Arc` field) unchanged.
- **Tests:** 7 TDD tests (a)–(g) added (rds parse + structural equality; `resource_api_version` optional; inline-route regression; neither→`MissingRouteSource`; both→`AmbiguousRouteSource`; unknown-field-in-`rds` rejected; deferred `config_source` surfaces still rejected).
- **Verification (quoted):** `cargo test -p envoy-config rds_schema` → **7 passed; 0 failed**. `cargo test -p envoy-config` → **354 passed; 0 failed**. `cargo build --workspace --all-targets` → Finished clean. `cargo fmt --all` + `cargo clippy --workspace --all-targets --all-features -- -D warnings` → clean (no lints). Standalone builds `-p envoy-config`/`-p envoy-cluster`/`-p envoy-http1`/`-p envoy-http2` → all Finished.
- **Two-stage review:** spec-compliance **✅ PASS** (independent verification: exactly 11 envoy-config-struct sites migrated, all 15 `Arc::new` local-struct sites untouched, `Rds` derive set matches `ConfigSource`, `check_route_sources` crate-visible + called before `validate()`, `validate_hcm` early-returns on None without cardinality re-check, all 7 tests real assertions, `cargo test -p envoy-config` re-run 354 passed; no extra/unrequested changes — `ConfigSource` untouched). code-quality **✅ APPROVED** (0 Critical / 0 Important / 3 Minors).
- **Recorded Minors (carried to state-5):**
  - **M20-T1-a (the `expect` panic window):** `clone_route_config(cfg.route_config.as_ref().expect(...))` at `envoy-http1/src/hcm.rs:200` is a sound invariant only AFTER Task 3 lands the RDS loader — between Task 1 and Task 3 an rds-only config that passes `parse_bootstrap` reaches `from_config` with `route_config: None` and would panic. Not a shipped regression (no fixture exercises it; the phase completes Task 3). **Closed structurally by Task 3** (the loader populates every rds HCM's `route_config`). Consider returning an `Http1Error` instead of `expect` as belt-and-suspenders (non-blocking).
  - **M20-T1-b:** tests (f)/(g) assert only `.is_err()` while their comments claim `deny_unknown_fields`/`Yaml` causes — tighten to `matches!(err, ConfigError::Yaml(_))` to assert the claimed mechanism (trivial; (d)/(e) already match precisely).
  - **M20-T1-c:** `check_route_sources` walks only `static_resources.listeners` (correct at parse time) — **Task 3 Step 4 MUST re-run it over the merged static+dynamic listener set** so an LDS-supplied HCM with neither/both source is caught (otherwise it slips past `validate`'s None-early-return into the M20-T1-a panic).
- **Carry-forward to Task 3:** close M20-T1-a + M20-T1-c (the RDS loader + the re-run of `check_route_sources` over `all_listeners()`).

## Task 2 — RDS file parser (`rds.rs`)

**Completion (code commit `c1219d6ff`):**

- **Implemented:** `crates/envoy-config/src/rds.rs` — a new sibling module mirroring `lds.rs`/`cds.rs` (the M19-1 `xds_file.rs` consolidation DEFERRED per C17). `parse_rds_file(path, contents) -> Result<Vec<RouteConfiguration>, ConfigError>`: `RdsFile { #[serde(default)] resources: Vec<RdsResource> }` (NO `deny_unknown_fields` on the envelope → `version_info` + other DiscoveryResponse keys silently dropped, exactly as `lds.rs`); `RdsResource` = `#[serde(tag = "@type")]` enum with the single variant `type.googleapis.com/envoy.config.route.v3.RouteConfiguration`; serde_yaml errors → `RdsParseError { path, message }`. No file I/O, no name-selection (deferred to Task 3). `lib.rs` gains `pub mod rds;` + `pub use rds::parse_rds_file;` (consistent with the `cds`/`lds` re-exports).
- **Divergence from the PLAN snippet (correct):** the snippet showed an explicit `version_info: Option<String>` field; `lds.rs` carries none (relies on the absent `deny_unknown_fields`). The implementer followed `lds.rs` as the authority — verified sound by test (b).
- **Tests:** 6 TDD tests (a)–(f): bare `resources:` envelope; full `DiscoveryResponse` (`version_info: v1`) ignored; multiple RouteConfigurations; non-RouteConfiguration `@type` rejected; malformed YAML rejected; missing `@type` rejected. Real assertions (incl. `RdsParseError` path/message matching).
- **Verification (quoted):** `cargo test -p envoy-config rds` → **13 passed; 0 failed** (6 new `rds::` + 7 Task-1 `rds_schema`). `cargo clippy -p envoy-config --all-targets --all-features -- -D warnings` → Finished, zero warnings. fmt clean.
- **Two-stage review (combined, small task):** spec-compliance **✅ APPROVED** (structural mirror of `lds.rs` confirmed; exact `@type` string; no `deny_unknown_fields` on envelope so DiscoveryResponse accepted; no file I/O / name-selection; re-export consistent; all 6 tests real). code-quality **✅ APPROVED — zero issues** (Critical/Important/Minor all none; idiom-for-idiom fidelity to `lds.rs`; valid minimal RouteConfiguration test YAML; precisely scoped).

## Task 3 — `load_dynamic_resources` RDS pass + §5.7 ordering + effective-`route_config` population

**Completion (code commit `67ab8bd7b`):**

- **Implemented (A) M20-T1-c closure:** `check_route_sources` (`bootstrap.rs:2503`) now iterates `bootstrap.all_listeners()` (was `static_resources.listeners`) — behavior-neutral at parse time (`dynamic_listeners` is `#[serde(skip)]` → always `None` when `parse_bootstrap` calls it), and reaches LDS-merged HCMs when re-called. Re-called in `load_dynamic_resources` (`lib.rs:701`) AFTER the LDS merge, BEFORE the RDS pass — while rds-HCMs still have `route_config: None`, so the `(true,true)` arm cannot falsely trip.
- **(B) RDS pass** (`lib.rs:711-749`): disjoint two-field mutable borrow `(&mut static_resources.listeners, &mut dynamic_listeners)` chained `iter_mut().chain(...flatten())`; per HCM with `rds`: `std::fs::read_to_string` → `RdsFileError`; `rds::parse_rds_file` → `RdsParseError`; name-select via `position(|rc| rc.name == name).map(|i| parsed.remove(i))` (MOVE, not clone) → `RdsRouteConfigNotFound`; `hcm.route_config = Some(selected)`. `TypedConfig::HttpConnectionManager` fully-qualified. `had_rds_hcm` Copy-bool, borrow ends at loop close.
- **(C) Extended gate** (`lib.rs:759`): `dynamic_clusters.is_some() || dynamic_listeners.is_some() || had_rds_hcm` → an rds-only bootstrap re-validates its populated `route_config` once (catches `UnknownCluster` against the merged cluster set + `EmptyVirtualHosts`). `load_dynamic_resources` doc-comment updated for the RDS pass + re-check + extended gate + §5.7.
- **§6.2 lock-ins held:** L4 (missing/malformed RDS fatal), L6 (name-mismatch fatal), L7 (RDS→CDS route resolves because CDS merges first; route to a cluster in neither list is fatal `UnknownCluster`; no `validate_clusters` on the RDS RouteConfiguration).
- **Tests:** 8 TDD tests (a)–(h) in `bootstrap.rs` test module (alongside the existing CDS/LDS load tests — the implementer's reasonable placement choice; they share the `tempfile` + helper scaffolding): loads+populates; missing→`RdsFileError`; malformed→`RdsParseError`; mismatch→`RdsRouteConfigNotFound`; §5.7 CDS+RDS composition resolves (`dynamic_backend`); unresolved→`UnknownCluster("nope")` (not panic); empty vhosts→`EmptyVirtualHosts` post-merge; (h) LDS HCM with neither source→`MissingRouteSource` (M20-T1-c witness via a real LDS-file path).
- **Verification (quoted):** `cargo test -p envoy-config` → **368 passed; 0 failed**. `cargo build --workspace --all-targets` → exit 0. `cargo fmt --all` + `cargo clippy --workspace --all-targets --all-features -- -D warnings` → exit 0, no warnings. Standalone builds `-p envoy-config`/`-p envoy-cluster`/`-p envoy-http1`/`-p envoy-http2` → all exit 0.
- **Two-stage review:** spec-compliance **✅ PASS** (ordering verified: LDS merge → `check_route_sources` re-call → RDS populate → extended-gate validate; the false-trip ordering bug does NOT exist; name-select moves not clones; borrow ends before validate; all 8 tests assert specific variants with real temp files; no `xds_file.rs` consolidation, no CDS/LDS branch edits, `expect` at hcm.rs:200 untouched). code-quality **✅ APPROVED** (0 Critical / 0 Important / 3 optional Minors).
- **Carry-forward Minors CLOSED:** **M20-T1-a CLOSED** (every HCM now resolves to `route_config: Some(_)` post-load → the `expect("route_config populated post-load")` at `envoy-http1/src/hcm.rs:200` is a sound invariant). **M20-T1-c CLOSED** (`check_route_sources` over `all_listeners()`, re-called post-merge). M20-T1-b (test (f)/(g) `.is_err()` tightening) remains open (trivial, state-5).
- **New optional Minors (carried to state-5):** **M20-T3-a** a shared RDS file is re-read per HCM (acceptable at startup — no memoization needed); **M20-T3-b** the CDS/LDS-vs-RDS borrow-strategy divergence is justified (in-place HCM mutation has no single field to clone-then-write) but slightly under-documented; **M20-T3-c** no merged-set `AmbiguousRouteSource` test (only the parse-time static both-case + the merged-set missing-case (h); both arms share the `all_listeners()` traversal so risk is near-zero).

## Task 4 — per-HCM `http.<stat_prefix>.rds.<route_config_name>.*` stats (`register_rds_stats`)

**Completion (code commit `668bfbd7c`):**

- **Implemented:** `pub fn register_rds_stats(bootstrap, registry) -> Result<(), ListenerError>` (`crates/envoy-listener/src/lib.rs:402`) mirroring `register_lds_stats` but PER-HCM: walks `bootstrap.all_listeners()` → filter_chains → filters → `TypedConfig::HttpConnectionManager(hcm)` gated on `hcm.rds.is_some()`; base `http.{stat_prefix}.rds.{route_config_name}`; `update_attempt`/`update_success`/`config_reload` `.add(1)`, `update_failure`/`update_rejected` registered at 0 (L3/L4). Inline-route HCMs register nothing (§5.2). main.rs call (`crates/envoy-bin/src/main.rs:116`) immediately after `register_lds_stats`, `.context(...)?`. The all-fatal posture (L4) makes the deterministic values valid → NO handle threading (resolves the SPEC §0-finding-3 worry).
- **Tests:** 3 (a inertness across all 4 cds/lds combinations; b the 5-name subset with exact values 1/1/0/0/1; c per-HCM keying `a/r1` + `b/r2` with cross-contamination negative checks). One TDD fix: `listener_filters: vec![]` added to the test helper (the phase-05.4 `Listener` field).
- **Verification (quoted):** `cargo test -p envoy-listener` → **36 passed; 0 failed** (3 new `rds_stats`). `cargo build --workspace --all-targets` clean. `cargo clippy -p envoy-listener --all-targets --all-features -- -D warnings` → zero warnings. fmt clean.
- **Two-stage review (combined):** spec-compliance **✅ PASS** (all_listeners walk, rds gate, exact name format, values 1/1/1/0/0, mk-idiom + error-mapping mirror lds, main.rs placement after lds with `.context(...)?`; test (a) covers the cds/lds-configured inertness witness; test (b) all 5 names+values; test (c) both families + negative cross-checks; register_counter idempotent → no duplicate-name panic). code-quality **✅ APPROVED** (0 Critical / 0 Important / 3 Minors).
- **New optional Minors (carried to state-5):** **M20-T4-a** the inertness test helper `mk_inline_route_bootstrap` builds listeners with NO HCM filter (witnesses inertness by HCM-absence, not by an actual inline-route-HCM rds=None) — the invariant still holds; a tighter inline-route-HCM witness is covered downstream by fixtures 0026/0027 (Task 7) + the backstop inertness case (Task 8); **M20-T4-b** the inner `mk` closure is re-declared per innermost loop iteration (necessary — it captures `base` by ref — but stylistically differs from the lds template's function-scope `mk`); **M20-T4-c** no unit test placing an rds HCM in `dynamic_listeners` (the `all_listeners()` merge is separately tested in envoy-config; composition-ready by construction). [**M20-T4-c CLOSED by Task 5**: the Task-5 `cds_lds_rds_bootstrap_routes_at_configs_3` test puts the rds HCM in `dynamic_listeners` and asserts it is picked up via `all_listeners()`.]

## Task 5 — `/config_dump` `RoutesConfigDump` entry (conditional emission)

**Completion (code commit `b1ef487da`):**

- **Implemented:** `ConfigDumpEntry::Routes { #[serde(skip_serializing_if="Vec::is_empty")] dynamic_route_configs: Vec<DynamicRouteConfigEntry> }` (renamed `type.googleapis.com/envoy.admin.v3.RoutesConfigDump`), placed AFTER the `Listeners` variant. Supporting structs mirror `DynamicClusterEntry`/`TaggedCluster`: `DynamicRouteConfigEntry { route_config: TaggedRouteConfig, last_updated }`, `TaggedRouteConfig { #[serde(rename="@type")] type_url, #[serde(flatten)] route_config: &RouteConfiguration }`, const `ROUTE_CONFIG_TYPE_URL`. NO `version_info` anywhere (L5). The flatten-on-nested-struct sidesteps serde's internally-tagged-enum limitation (proven by TaggedCluster/TaggedListener); emits `{@type, name, virtual_hosts}` since the RDS RouteConfiguration's `validate_clusters` is `None` (skip-if-none). `render_config_dump` push (after the Listeners block, before body assembly): walks `all_listeners()` → filter chains → `TypedConfig::HttpConnectionManager(hcm) if hcm.rds.is_some()` → maps `hcm.route_config.as_ref()`; `configs.push(Routes{...})` ONLY when non-empty (inertness guard). Clusters/Listeners blocks untouched.
- **Tests:** 5 (a emission: outer @type + `dynamic_route_configs[0].route_config.name == local_route` + inner @type + iso8601 last_updated + version_info absent; b inertness ×2: inline-route HCM under cds + bare bootstrap; c ordering: cds+rds→Routes at `configs[2]`, cds+lds+rds→Listeners[2]+Routes[3]).
- **Verification (quoted):** `cargo test -p envoy-admin` → **91 passed; 0 failed** (5 new). `cargo build --workspace --all-targets` clean. `cargo clippy -p envoy-admin --all-targets --all-features -- -D warnings` → zero warnings. fmt clean.
- **Two-stage review (combined):** spec-compliance **✅ PASS** (exact @type URLs; no version_info; flatten mirrors TaggedCluster; push after Listeners, before body, gated non-empty; Clusters/Listeners untouched; all 5 tests verified incl. the configs[2]/[3] indices + post-load both-Some construction). code-quality **✅ APPROVED — zero issues** (0 Critical / 0 Important / 0 Minor; exact idiom mirror; thorough coverage; no scope creep).

## Task 6 — harness `{{RDS_PATH}}` shared-template + per-side `JsonSubtreeRule` path override

**Completion (code commit `5a39a5c63`):**

- **Piece 1 (per-side `JsonSubtreeRule`):** `JsonSubtreeRule` (`tests/differential/src/lib.rs`) gains `#[serde(default)] path_envoy: Option<String>` + `#[serde(default)] path_envoy_rust: Option<String>`; `path` becomes `#[serde(default)]` (backward-compatible — fixtures 0014/0026/0027 deserialize unchanged). Accessors `envoy_path()`/`rust_path()` = `path_envoy/path_envoy_rust.as_deref().unwrap_or(&self.path)`. The subtree match resolves per-side; the `expected`-comparison block unchanged (both sides still asserted == expected). The 4 in-code `JsonSubtreeRule {...}` literals updated. Reconciles the ADR-0052 L5 config_dump index divergence (Envoy `configs[4]` vs envoy-rust `configs[2/3]`).
- **Piece 2 (`{{RDS_PATH}}` SHARED template, mirroring `cds.yaml` — L8):** `needs_rds` detection; read ONE shared `rds.yaml`; render per-side via the two kv maps (residual-marker guards on both renditions); write `rds-upstream.yaml`+`rds-subject.yaml`; inject `RDS_PATH` into both kv maps (upstream→`RDS_CONTAINER_PATH`, subject→host temp path); `backend_scan_sources` bumped `[&str;4]`→`[&str;5]` (+rds_scan); host-gateway scan extended with the rendered upstream RDS. `upstream.rs`: `pub const RDS_CONTAINER_PATH = "/etc/envoy-rds/rds.yaml"`; `rds_file: Option<&Path>` param on `start(...)`; `with_copy_to(RDS_CONTAINER_PATH, ...)` mount. `#[allow(clippy::too_many_arguments)]` on `start` (8 args now — independent fixture-feature toggles; a params struct would add indirection without clarifying dispatch — reviewer-concurred).
- **Tests:** 3 lib unit tests (shared-path regression; per-side override with a DECOY node — Envoy body has `"WRONG"` at configs[2] + target at configs[4], rust has target at configs[2] → fails if override ignored/swapped; the `{{RDS_PATH}}` render test mirroring the CDS one).
- **Verification (quoted):** `cargo test -p differential --lib` → **126 pass** (the 3 new + existing; 1 Docker test ignored). One unrelated environmental flake (`backend::tests::http1_echo_backend_spawns_and_echoes` — port-bind/subprocess race under parallel load; passes in isolation; the `project_flaky_access_log_fixture_0012` family, NO RDS involvement). `cargo build --workspace --all-targets` clean. `cargo clippy -p differential --all-targets --all-features -- -D warnings` → clean. fmt clean.
- **Two-stage review:** spec-compliance **✅ PASS** (both pieces verified vs the CDS template; per-side override works independently per side [the decoy test]; `rds.yaml` genuinely SHARED [one file rendered twice, NOT per-side]; no fixture regression; `upstream::start` gained `rds_file`; scans cover both rendered + unrendered RDS sources consistently). code-quality **✅ APPROVED** (0 Critical / 0 Important / 3 Minors).
- **New optional Minors (carried to state-5):** **M20-T6-a (notable)** the CDS+LDS+RDS dynamic-file render/write/guard block is now TRIPLICATED — a deferred "extract a dynamic-file render helper" item, directly analogous to the `xds_file.rs` parser-consolidation deferral (M19-1 / PLAN C17); consistent with the project's deliberate-defer posture; **M20-T6-b** `rds_scan` in `backend_scan_sources` is acknowledged dead weight (the RDS file references cluster NAMES not host/port markers, so it never matches `BACKEND_PORT`) — defensive symmetry, honestly commented; **M20-T6-c** test-harness file growth (`lib.rs` now ~7283 lines, +262 this commit) — pre-existing largeness, no new structural debt beyond M20-T6-a.

## Task 7 — fixture `0028-xds-file-based-rds` + Docker-gated wrapper

**Completion (code commit `a4d136265`):**

- **Authored:** `tests/fixtures/0028-xds-file-based-rds/{envoy.yaml, envoy-rust.yaml, rds.yaml, cds.yaml, expectations.yaml, README.md}` + `tests/differential/tests/xds_file_based_rds.rs` (the Docker wrapper, mirroring `xds_file_based_lds.rs`). NO code changes (the harness Task-6 support was sufficient — no gap found).
- **Topology:** STATIC listener whose HCM is RDS-configured (`rds: { route_config_name: local_route, config_source.path_config_source.path: {{RDS_PATH}} }`, NO inline route_config) + static `static_backend` cluster + `cds_config`→`cds.yaml` (`dynamic_backend`). SHARED `rds.yaml` (ONE file): `RouteConfiguration local_route`, `/static`→static_backend, `/dynamic`→dynamic_backend, NO `validate_clusters` (L7). Two GETs over one H1 keep-alive (`Driver::Http1KeepAlive`); `stat_prefix: ingress_http1`.
- **The byte-exact-body challenge (FIRST shared-route + byte-exact fixture) — converged empirically via local Docker:** the shared `rds.yaml` cannot carry Envoy-only `request_headers_to_remove` (envoy-rust's `RouteConfiguration`/`VirtualHost` use `deny_unknown_fields`), so the Envoy-side stripping moved to the per-side MAIN config (`envoy.yaml`; `envoy-rust.yaml` omits all): `generate_request_id: false` (x-request-id) + an `envoy.filters.http.header_mutation` filter removing `x-forwarded-for`/`x-forwarded-proto` (HCM-added, pre-filter-chain) + the router's `suppress_envoy_headers: true` (strips the router-injected `x-envoy-*` request headers incl. `x-envoy-expected-rq-timeout-ms`, which is added downstream of decode filters where header_mutation can't reach) + a `response_mutations` re-add of `x-envoy-upstream-service-time` (which `suppress_envoy_headers` also drops; presence-only assertion, value differs; envoy-rust emits it natively). Final byte-exact bodies: `"method: GET\npath: /static\nheaders:\n  host: static_backend\nbody: \n"` (+ `/dynamic`).
- **expected_stats:** RDS 5-name subset `http.ingress_http1.rds.local_route.{update_attempt:1,update_success:1,update_failure:0,update_rejected:0,config_reload:1}` + CDS family (`cluster_manager.cds.*` 1/1/0/0, `cluster_added:2`, `active_clusters:2`) + `cluster.static_backend.upstream_rq_total:1` + `cluster.dynamic_backend.upstream_rq_total:1` + `http.ingress_http1.downstream_rq_total:2`/`downstream_rq_2xx:2` — internally consistent (2 clusters, 2 requests, 1 conn).
- **config_dump (per-side `JsonSubtreeRule` override, ADR-0052 L5):** rule 1 `path_envoy: configs.4.dynamic_route_configs.0.route_config.name` (Envoy: Bootstrap[0]/Clusters[1]/Listeners[2]/ScopedRoutes[3]/Routes[4]) / `path_envoy_rust: configs.2.dynamic_route_configs.0.route_config.name` (envoy-rust: Bootstrap[0]/Clusters[1]/Routes[2] — Listeners gated off, no lds) → `expected: local_route`; rule 2 `path: configs.1.dynamic_active_clusters.0.cluster.name` → `dynamic_backend` (ClustersConfigDump at configs[1] on BOTH sides). Indices confirmed against the local run + `endpoint.rs:585-610`.
- **Verification (quoted):** local Docker run `cargo test -p differential --test xds_file_based_rds` → **1 passed; 0 failed** (both probes 200, byte-exact bilateral, all stats, both config_dump rules), against `envoyproxy/envoy:v1.33.0`. `cargo build --workspace --all-targets` + `cargo clippy --workspace --all-targets --all-features -- -D warnings` + `cargo fmt --all -- --check` → all clean.
- **Two-stage review (combined):** spec-compliance **✅ PASS** (shared rds.yaml free of envoy-rust-rejected fields; main configs differ only in Envoy-only strippers; stats internally consistent; config_dump indices verified; wrapper mirrors LDS; the suppress_envoy_headers + response_mutations re-add SOUND — envoy-rust emits `x-envoy-upstream-service-time` natively per `router.rs:54,159`). code-quality **CHANGES REQUESTED → RESOLVED**: 1 Important (the README + expectations.yaml header comment described an ABANDONED stripping design [header_mutation-only, "deliberately avoid suppress_envoy_headers"] contradicting the shipped envoy.yaml which USES suppress_envoy_headers + response_mutations) — **FIXED in-task at the amended commit** (both doc comments rewritten to match the shipped config; envoy.yaml's own inline comments were already correct); 0 Critical / 0 other Important / 1 Minor (keep the "envoy-rust emits x-envoy-upstream-service-time natively" note — retained).

## Task 8 — in-process RDS backstop (happy + 6 negative + inertness)

**Completion (code commit `9eb65b26a`):**

- **Implemented:** `crates/envoy-bin/tests/xds_file_based_rds.rs` (890 lines) — the helper block copied VERBATIM from `xds_file_based_lds.rs` (M18-9 N≥4 duplication note in the file header — extract-a-test-support-crate stays a future hardening item per C17); the RDS adaptation drops `lds_config`, adds `rds_file` (emits a `@type`-tagged `RouteConfiguration` envelope, NOT a Listener) + 4 listener builders (`rds_listener_block`/`inline_listener_block`/`ambiguous_listener_block`/`neither_listener_block`); `stat_prefix: ingress_http1`; `kill_on_drop(true)` spawn discipline (phase-10 M3); `reserve_port()` (no hard-coded ports).
- **8 cases:** (i) happy — both probes 200 + the 5-name `http.ingress_http1.rds.local_route.*` subset at L3 values + a RoutesConfigDump with `route_config.name == local_route` (located by SCANNING `configs[]` for the `@type` — more robust than a hard-coded index); (ii) missing→`RdsFileError`; (iii) malformed→`RdsParseError`; (iv) name-mismatch→`RdsRouteConfigNotFound`; (v) unknown-cluster `nope`→`UnknownCluster` (the L7 recorded divergence); (vi) both→`AmbiguousRouteSource`; (vii) neither→`MissingRouteSource`; (viii) inertness — a genuine INLINE-route HCM (no rds) boots, `/config_dump` has NO RoutesConfigDump + `/stats` has no `.rds.` name.
- **Verification (quoted):** `cargo build -p envoy-bin` clean; `cargo test -p envoy-bin --test xds_file_based_rds` → **8 passed; 0 failed**; `cargo clippy -p envoy-bin --tests --all-features -- -D warnings` → 0 warnings; fmt clean. (Pre-built `envoy-bin` first per `project_flaky_access_log_fixture_0012`.)
- **Two-stage review (combined):** spec-compliance **✅ PASS** (all 8 cases assert real behavior; EVERY negative-path needle independently cross-checked against the actual `ConfigError` `#[error(...)]` strings in `lib.rs` — all match; case (viii) is a genuine inline-route inertness witness). code-quality **✅ APPROVED** (0 Critical / 0 Important / 1 Minor: static_backend + dynamic_backend point at the same in-process backend so (i)/(viii) prove routing-reaches-a-backend but not strictly distinct per-cluster paths — the per-cluster distinction is established by fixture 0028; acceptable for a backstop).
- **Carry-forward Minor CLOSED:** **M20-T4-a CLOSED by Task 8 case (viii)** (the tighter inline-route-HCM inertness witness the Task-4 unit test lacked is now present + green).

## Task 9 — fuzz seed `hcm_rds_route_config.yaml` (curated corpus 30 → 31)

**Completion (code commit `d19be5147`):**

- **Implemented (atomic, one commit):** `crates/envoy-config/fuzz/corpus/parse_bootstrap/hcm_rds_route_config.yaml` (a minimal bootstrap whose HCM uses `rds: { route_config_name: local_route, config_source: { resource_api_version: V3, path_config_source: { path: /etc/envoy-rds/rds.yaml } } }`, NO inline route_config; one STRICT_DNS `static_backend` cluster; `codec_type: HTTP1`; router filter — modelled on `dynamic_resources_cds.yaml`/`dynamic_resources_lds.yaml`) + the `.gitignore` allow-list line `!corpus/parse_bootstrap/hcm_rds_route_config.yaml` + the entry in the `fuzz_corpus_seeds_parse_or_reject_cleanly` replay test (`bootstrap.rs:4206`, `// 20 Task 9` — matching the `// 18`/`// 19 Task 9` precedent). Corpus 30 → 31.
- **`parse_bootstrap` accepts the seed:** rds present + route_config absent → the exactly-one-of check passes; `validate_hcm` early-returns Ok on the None route_config; no cds_config so no route-cluster check fires. The fuzz target does NO file I/O so the referenced `/etc/envoy-rds/rds.yaml` need not exist.
- **Verification (quoted):** the replay test `cargo test -p envoy-config fuzz_corpus_seeds_parse_or_reject_cleanly` → **1 passed; 0 failed** (now includes the new seed); fmt clean. (A reviewer also directly parsed the seed via a throwaway `parse_bootstrap(...).is_ok()` test → passed, then removed it; tree clean.)
- **Review:** ✅ (atomicity confirmed; corpus count 31; direct parse-acceptance verified; seed references `rds:` with zero `route_config:`; conventions match the existing dynamic seeds). The reviewer's one observation (the seed was not in the replay-test list) was **closed in-task** by adding it to `fuzz_corpus_seeds_parse_or_reject_cleanly` (folded into the amended commit) — regression protection now matches the cds/lds seeds.

## Task 10 — BEHAVIOR_CONTRACT RDS rows (stats + xDS RDS extension + RoutesConfigDump)

**Completion (code commit `865a2e0ab`, docs-only, +146 lines):**

- **Implemented:** three additions to `docs/envoy-rust/BEHAVIOR_CONTRACT.md`, mirroring the phase-18 CDS + phase-19 LDS precedents exactly: (1) **Stat-name mapping** — the 5-name `http.<stat_prefix>.rds.<route_config_name>.` subset (attempt/success/config_reload value-exact 1, failure/rejected value-exact 0) + the per-HCM scoping note (fixture 0028 = `http.ingress_http1.rds.local_route.`) + the conditional-registration narrowing (inline-route HCMs emit none; all 27 pre-existing fixtures unaffected) + the Envoy-only-NOT-asserted enumeration; (2) **xDS wire state machine** — a new "Filesystem transport — phase 20 RDS extension" subsection AFTER the phase-19 LDS one, mirroring its §(a)–(f) shape, recording L1–L11 (envelope/rds-on-HCM; readiness; negative 3-way split + all-fatal + name-mismatch; exactly-one-of both/neither fatal; RDS+CDS composition + no-validate_clusters + defer-then-revalidate `UnknownCluster`; conditional-emission + the `configs[4]`/`configs[2]` index reconciliation; + L8/L10/L11 notes); (3) **Admin endpoint body shapes** — a RoutesConfigDump row (`dynamic_route_configs[].{route_config{@type,name,virtual_hosts}, last_updated}`, NO version_info, conditional emission, the index divergence + per-side reconciliation).
- **Review (combined): ✅ APPROVED** — EVERY factual claim cross-checked against the actual code: the 5 stat names+values vs `register_rds_stats` (`lib.rs:417-427`); the error variants vs the actual `ConfigError` + Task-3/8 behavior; the `configs[4]`/`configs[2]` indices vs fixture 0028 `expectations.yaml` + Task-5 `endpoint.rs`; the RoutesConfigDump shape (no version_info) vs the Task-5 structs; L7 vs PROGRESS Task 3; the "27 pre-existing fixtures" count. Zero inaccuracies, zero internal contradictions; faithful phase-19 §(a)–(f) mirror. 0 Critical / 0 Important / 1 pre-existing Minor (a stat-name abbreviation style in the (c) table that intentionally mirrors the phase-19 LDS precedent — not introduced by this task).
