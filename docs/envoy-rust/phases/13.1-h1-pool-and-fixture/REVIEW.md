# Phase 13.1 (`13.1-h1-pool-and-fixture`) — REVIEW

- **Phase id:** `13.1`
- **Slug:** `13.1-h1-pool-and-fixture`
- **Parent:** `13` (`13-connection-pooling`). **First (non-closing) sub-phase** of the parent-13 split (parent-13 state-2 split commit `1b1c8f5`; ADR-0038). Per the closing-sub-phase invariant established by the 02.2 / 03.2 / 07.2 / 08.2 / 12.2 precedents, 13.1's state-6 close-out flips ROADMAP row `13.1` `in-progress → done` ALONE; the parent-13 row + sibling `13.2` row stay `in-progress` / `planned` respectively. The eventual closing sub-phase 13.2 will fire the parent-13 close simultaneously at its own state-6 commit. With this Approved verdict, the next session's state-6 close-out commit advances STATE.md to point at `13.2` state-2-next (the 13.2 SPEC exists; PLAN.md does not — next-next session writes the 13.2 PLAN via `superpowers:writing-plans`). 13.1 lands the **H1 connection-pool primitive + the envoy-config `circuit_breakers` schema + the H1 router-proxy-arm pool integration + the configurable-status synthetic backend extension + fixture 0020 + the per-class counter wire bilateral coverage closing 06.3 REVIEW I2 (a)** — the H1-only half of phase-13 connection pooling. The discriminating differential observable is **`cluster.backend_cluster.upstream_cx_total = 1`** under the single-downstream-keep-alive-conn 10-request driver against a `circuit_breakers: { thresholds: [{ max_connections: 4 }] }` cluster, bilaterally produced by `envoyproxy/envoy:v1.33.0` and envoy-rust on identical YAMLs.

- **Reviewed range:** `e64672b..52f43f7` — **12 commits total** (11 state-3 + state-4 commits at HEAD `592d9e7` + this state-5 fold-in `52f43f7`). The state-2 base `e64672b` is the standalone-PLAN-write commit (10 tasks; 18 architecture lock-ins; §6.2 verification LOCKED at the parent-13 split — NOT re-run; NO inline ADR); the HEAD `52f43f7` is the state-5 review fold-in commit. Diffstat ≈ `3000+ insertions(+), 200 deletions(-)` across the production crates `envoy-config` + `envoy-http1` + `envoy-cluster` (1 constructor) + `envoy-bin` (wiring) + the test infrastructure (helpers, fixtures, harness driver, in-process backstop, docs).
  - `502e899` — **Task 1**: D1 envoy-config `Cluster.circuit_breakers` schema. 3 new types (`CircuitBreakers`, `Thresholds`, `RoutingPriority`) + `Cluster.circuit_breakers: Option<CircuitBreakers>` field + lib.rs re-exports + 2 envoy-cluster test-constructor extensions. 3 TDD tests. Spec ✅.
  - `7a0a86b` — **Task 2**: D2 envoy-config `validate_circuit_breakers` + 3 `ConfigError` variants (`UnsupportedMultipleCircuitBreakerThresholds`, `UnsupportedCircuitBreakerPriority`, `InvalidMaxConnections`). Validator wired at `parse_bootstrap`. 4 TDD tests (variant + field-binding via `matches!`). Spec ✅.
  - `368d6ef` — **Task 3**: D3 H1Pool primitive + PoolGuard RAII + idle sweeper + H1PoolManager. New file `crates/envoy-http1/src/pool.rs` (~506 LoC) + `ConnGaugeGuard::from_gauge` pub constructor + envoy-http1 Cargo.toml +1 `tokio-util` sub-crate dep (already a transitive dep of tokio; not a new top-level dep). 5 TDD tests. 6 PLAN deviations documented in PROGRESS Task 3 (collapsible_if rewrite; two-phase sweep-collect-under-lock; tokio-util sub-crate add; `from_gauge` test scope; hand-rolled `Debug`; `_sweepers` field underscore-prefix). Spec ✅.
  - `490bb96` — **Task 4**: D4 H1 router-arm dispatch through `H1Pool::acquire` — **the LOAD-BEARING integration task**. Introduced local `enum StreamHandle { Pooled(PoolGuard), OneShot(ClientStream) }` covering pool dispatch (connect-on-miss, idle-reuse, overflow → 503, connect-failure → 502); preserved OneShot path for HCMConfigs that pass `pool_mgr: None`. Removed dead tier-1 `cached_upstream` micro-cache (lock-in #5; `grep cached_upstream crates/envoy-http1/` now zero hits). Migrated `cx_total.inc` from `hcm.rs:514` into pool's connect-on-miss (lock-in #6). envoy-bin wires `H1PoolManager::for_bootstrap` at `main.rs:137-143` (correctly between cluster_mgr construction + health_scheduler spawn). 1 TDD integration test. Spec ✅ at the integration level; cx_active double-count regression caught at the post-commit review + closed at fold-in below.
  - `8e3774d` — **Task 4 code-quality fold-in**: cx_active double-count fix. Pre-fix: HCM-level outer `_cx_guard` (`cluster.cx_active_guard()`) + PoolGuard inner `_cx_active_guard` both fired `cx_active.inc()` against the SAME shared `Arc<Gauge>` (idempotent same-kind contract → both writes hit the same gauge) → broken value `cx_active: 2` on pool path. Post-fix: outer `_cx_guard` relocated to `Option<ConnGaugeGuard>` conditional Some-only on `Ok(StreamHandle::OneShot(_))`. 1 TDD regression test `h1_hcm_pool_path_does_not_double_count_cx_active` (drives one request, holds backend open via `tokio::sync::oneshot`, sleeps 200ms, asserts `cx_active.value() == 1`) — implementer verified by revert-and-test that the test FAILS pre-fix with `assertion left == right failed: left: 2, right: 1`. 2 cheap-fold-in Minor comments (PoolError::Overflow no-cx_total rationale + OneShot no-invalidate-analog rationale; 1-3 line comment additions). Spec ✅. In-phase recovery via NEW commit (NOT amend-and-force-push; mirrors 12.2 in-phase-recovery cadence).
  - `f785711` — **Task 5**: D7-H1 pool stats wiring + 2 new BEHAVIOR_CONTRACT rows. New rows `cluster.<name>.upstream_cx_destroy` (value-exact, 0-failures case) + `cluster.<name>.upstream_cx_http1_total` (value-exact). **Explicit non-tightening of existing `upstream_cx_total` row at `BEHAVIOR_CONTRACT.md:89` per PLAN lock-in #3** (row tightening defers to 13.2 D7.1 — the 06.3 REVIEW I2 (b) closure site; fires only when both H1 + H2 pools tighten uniformly, since the row mentions no protocol carve-out). 1 new registration-presence unit test `h1_pool_manager_registers_cx_destroy_and_cx_http1_total_per_h1_cluster` (snapshots the registry; asserts both names appear). Spec ✅.
  - `8b7f951` — **Task 6**: D8 configurable-status backend `--per-path` flag. Extended `tests/helpers/health-aware-http1-backend/src/main.rs` with `--per-path PATH=STATUS[,...]` flag + deterministic per-class body bytes (`"moved\n"` / `"not found\n"` / `"server error\n"`). 3 new TDD unit tests (the helper's FIRST `#[cfg(test)] mod tests` block: parse + dispatch-priority + per-class body bytes). **Test-helper-only; zero production touch** (`git show --stat 8b7f951 -- crates/` empty). Spec ✅.
  - `ec50093` — **Task 7**: D9.1 + D10 fixture 0020 + Driver::Http1KeepAlive harness extension — **THE bilateral discriminating-observable per parent-13 SPEC §6.2 item-iv**. ATOMIC commit per PLAN lock-in #4 — new fixture `0020-upstream-connection-pooling-and-per-class-counters/` (envoy.yaml + envoy-rust.yaml + expectations.yaml) + new `Driver::Http1KeepAlive` serde variant (`kind: http1_keep_alive`) + 2 supporting structs (`KeepAliveRequest` + `KeepAliveExpectedStat`) + 2 dispatch helpers (`read_h1_response_status` + `scrape_admin_stat`) + dispatch arm + 3 harness-wiring extensions + per-fixture `--per-path` selector + `HealthAwareHttp1Backend::spawn_with_per_path` shim. Fixture 0020 GREEN bilaterally in 3.45s. **3 PLAN deviations documented honestly per D-3.4:** (i) helper backend keep-alive support task-time-discovered (necessary for pool-reuse property; preserves fixture 0019 byte-equal); (ii) cluster per-class `upstream_rq_{2,3,4}xx` DEFERRED (Cluster only carries `_total` + `_5xx` today per `crates/envoy-cluster/src/cluster.rs:71-76`; PLAN-spec 12 expected-stats rows trimmed to 9 + the bilateral HCM-side per-class closure still satisfies 06.3 REVIEW I2 (a)); (iii) `scrape_admin_stat` carved out as standalone helper. **THIS is the 06.3 REVIEW I2 (a) full-closure site.** 1 TDD round-trip serde unit test for `Driver::Http1KeepAlive`. Spec ✅.
  - `b94e4b2` — **Task 8**: D9.3 in-process H1 backstop. New `crates/envoy-bin/tests/upstream_connection_pooling.rs` (~322 lines) mirroring the 12.2 `upstream_active_health_check.rs` shape. ONE downstream H1 keep-alive conn driving 10 sequential GETs (4×/, 1×/301, 2×/404, 3×/500). **5-standard-header presence assertion on every non-2xx response per 10 REVIEW M1.** Subprocess discipline: `kill_on_drop(true)` + `Stdio::null()/piped()` per 09 REVIEW M3. Backend / envoy-bin readiness budgets at 30s / 10s (matching the 12.2 state-5 Cluster B I2 fold-in pattern). Spec ✅.
  - `8f14d5c` — **Task 9**: D11 `parse_bootstrap` fuzz seed `cluster_circuit_breakers.yaml`. Mechanical 3-sibling-file edit: new seed file (the FULL `circuit_breakers.thresholds[0].{priority, max_connections}` shape; only seed in the 21-seed corpus exercising `circuit_breakers`) + `.gitignore` allow-list line + SUCCESS array extension. Corpus 20 → 21 SUCCESS seeds. **Controller-direct landing** per `feedback_execution_style` mid-arc latitude (5-line edit too mechanical to warrant subagent overhead). Spec ✅.
  - `13bb5cc` — **Task 9 PROGRESS close-out**: pure docs follow-up restoring the Task 9 PROGRESS subsection that slipped the Task 9 commit due to a parallel Edit-vs-commit race. No production drift. Spec ✅.
  - `592d9e7` — **Task 10**: state-4 phase-done verification + STATE advance to state-5-next. Docs-only (PROGRESS Task 10 subsection + STATE.md 4-pointer rewrites + `### Phase-13.1 state-3 execution arc` Notes subsection). §7.5 (a)–(e) gate GREEN on a fresh local run + CI run `26377609763` settled `completed / success` in 2m29s.
  - `52f43f7` — **Task 10 state-5 review fold-in (THIS commit's predecessor → state-5 commit context)**: closes Cluster A I1 (PoolGuard::Drop tokio::spawn runtime-availability guard) + I2 (sweeper interval ≥1ms clamp) + the REVIEW §4 access-log flake mitigation. 2 new TDD regression tests + 1 structural harness fix. State-3 re-entry per `BOOTSTRAP_PROMPT.md` §5.2.

- **CI evidence cited:** PROGRESS Task 10 + this REVIEW cite CI run **`26377609763`** at HEAD `592d9e7` (the state-4 commit) `completed / success` in 2m29s AS THE PRE-FOLD-IN ANCHOR (the FULL state-3 execution arc green); CI run `26395285596` at HEAD `52f43f7` (the state-5 fold-in commit) re-attests the post-fold-in gates. `gh run list --branch main --commit 52f43f7 --limit 1` returns the fold-in CI as the most recent run. The Task 9 PROGRESS-close-out commit `13bb5cc` saw CI run `26375100437` FAIL on the `access_log_file_sink` test with `envoy=1 envoy-rust=0 line count mismatch` — an environmental flake where envoy-rust's fire-and-forget access-log emit task hadn't flushed when the harness's post-exists 100ms sleep elapsed. **The state-4 commit `592d9e7` settled GREEN immediately after, confirming the flake was transient — not a regression from Tasks 8/9.** The state-5 fold-in mitigates the flake structurally by replacing the harness's wait-for-files-to-EXIST loop with a wait-for-files-to-be-NON-EMPTY loop (preserving the existing 5s budget + the post-loop 100ms OS-flush yield). Local re-runs at this state-5 session confirm: 5 stable-toolchain gates clean (`cargo build --workspace --all-targets` `Finished` in 10.92s warm; `cargo clippy --workspace --all-targets --all-features -- -D warnings` `Finished` in 1m 33s, zero warnings; `cargo fmt --all -- --check` clean; **`cargo test --workspace` → 867 passed / 0 failed / 2 ignored across 74 result lines** [+2 over the state-4 baseline 865 = the 2 new A-I1 + A-I2 regression tests]; `cargo deny check` `advisories ok, bans ok, licenses ok, sources ok`). The full local bilateral Docker re-run at the Task 10 state-4 verification (HEAD `592d9e7`) produced **129 passed / 0 failed / 0 ignored across 22 result lines — 20/20 Docker-gated fixtures green simultaneously** vs `envoyproxy/envoy:v1.33.0` (the load-bearing 13.1 differential proof, including the new `upstream_connection_pooling_and_per_class_counters_fixture` at 3.45s and `upstream_active_health_check_fixture` (0019) regression-equivalence preserved per Task 7's helper keep-alive extension).
- **Reviewed:** 2026-05-25.

## §1. Verdict

**Approved with M-track follow-ups.**

Phase 13.1 lands its 9 carved parent-13 deliverables (D1 envoy-config `Cluster.circuit_breakers` schema with the 3 new types + serde `deny_unknown_fields` correctly slamming the door on the 6 phase-13-deferred threshold fields + the `RoutingPriority` SCREAMING_SNAKE_CASE serialization; D2 `validate_circuit_breakers` with the 3 ergonomically-named `ConfigError` variants + first-error-wins discipline + 4 TDD tests through `parse_bootstrap`; D3 the headline `H1Pool` primitive + `PoolGuard` RAII handle + idle sweeper as the second periodic-background primitive [after 12.2's `envoy-health::Scheduler`] + `H1PoolManager` as the external registry [sibling to `ClusterManager`, NOT a field on Cluster — the cycle-resolution pick (b) per PLAN lock-in #1, mirroring the 12.2 `envoy-health::Scheduler` precedent verbatim] + cleanly-cancellable shutdown via `tokio_util::sync::CancellationToken`; D4 the H1 router-proxy-arm pool integration with the local `enum StreamHandle { Pooled(PoolGuard), OneShot(ClientStream) }` dichotomy + tier-1 cache removal + `cx_total` migration into pool's connect-on-miss + envoy-bin wiring; D7-H1 the 2 new BEHAVIOR_CONTRACT rows [`cluster.<name>.upstream_cx_destroy` value-exact 0-failures case + `cluster.<name>.upstream_cx_http1_total` value-exact] with **explicit non-tightening of the existing `upstream_cx_total` row at `BEHAVIOR_CONTRACT.md:89`** per PLAN lock-in #3 — the row tightening defers to 13.2 D7.1 [the 06.3 REVIEW I2 (b) closure site, firing when both H1 + H2 pools tighten uniformly]; D8 the configurable-status backend `--per-path` extension on the 12.2-landed `health-aware-http1-backend` helper [test-helper-only; zero production touch]; D9.1 + D10 the atomic landing of fixture `0020-upstream-connection-pooling-and-per-class-counters` + the `Driver::Http1KeepAlive` harness variant + Docker wrapper [THE 06.3 REVIEW I2 (a) full-closure site — per-class `downstream_rq_{2,3,4,5}xx` bilateral assertions + `cluster.upstream_rq_5xx` bilateral assertion; cluster per-class `upstream_rq_{2,3,4}xx` extension deferred as a small follow-up because envoy-rust's `Cluster` carries only `_total + _5xx` today]; D9.3 the in-process H1 backstop covering pool reuse + per-class counter math + the 5-standard-header presence assertion on every non-2xx response per 10 REVIEW M1; D11 the `parse_bootstrap` corpus seed extension 20 → 21) substantively and cleanly through the existing envoy-cluster + envoy-config + envoy-http1 + envoy-stats + tokio + tokio-util foundations + the new `H1Pool` / `H1PoolManager` primitives inside `envoy-http1`.

The cross-phase architectural invariants from the 13.1 SPEC §5 all hold: the new pool primitive lives entirely INSIDE `envoy-http1` (`envoy-http1` already depended on `envoy-cluster` per the 04.3 precedent — no new dep direction; `H1PoolManager` is an external registry sibling to `ClusterManager`, not a field on `Cluster` — **no cycle introduced**, verified by `cargo build --workspace`); the pool primitive + idle sweeper are hand-rolled per D-3.2 atop std + tokio + tokio-util + bytes + envoy-stats + the existing envoy-http1 internal types — **no new top-level Cargo dep** (`tokio-util` sub-crate dep added to envoy-http1's Cargo.toml at `crates/envoy-http1/Cargo.toml:18` already exists as a transitive dep of tokio + workspace member of envoy-health/envoy-bin; the addition is sub-crate plumbing only); no `unsafe` introduced (`#![forbid(unsafe_code)]` continues to bind at every touched crate root per D-3.8 — verified at `envoy-http1/src/lib.rs:1`, `envoy-cluster/src/lib.rs:1`, `envoy-config/src/lib.rs:1`); the H2-framing code path is type-system-only-touched (Task 4's `HCMConfig::from_config` threading of `pool_mgr: Option<Arc<H1PoolManager>>` at `crates/envoy-http2/src/hcm.rs` — no codec change, no frame handling change, no SETTINGS/HEADERS/DATA emission path touched — h2spec ≥95% holds vacuously at the parent-05 baseline 99.31%); the §5.4 inert-when-unconfigured invariant holds (only fixture 0020 configures `circuit_breakers`; the 19 pre-existing fixtures see their per-call `upstream_cx_total` semantics unchanged AT the assertion level because all existing assertions were `name-required, value-may-differ`); the §5.6 `ConnGaugeGuard` RAII pattern is correctly REUSED (each `PoolGuard` owns one `ConnGaugeGuard` via the new `from_gauge` constructor; the cluster-level outer guard is now OneShot-only post the Task 4 fold-in, eliminating the double-count regression). **NO ADR landed in the 13.1 lifecycle** (PLAN lock-in #16; SPEC §7 — 13.1 introduces no new foundations grant, no wire-level contract revision, no new crate; the cycle-resolution decision was settled at PLAN-write via the external-`H1PoolManager` pattern). DECISIONS.md ledger head stays **ADR-0038** (count 39); next available **ADR-0039**. BEHAVIOR_CONTRACT.md extended with 1 new `**13.1 entries (H1 connection pool):**` block at Task 5 (2 counter rows) — the established 06.x → 12.2 cadence (contract extensions land at the task where each surface is first wired).

The state-5 review dispatched a **3-cluster subagent pass** over the reviewed range `e64672b..592d9e7` (12 commits total at dispatch, expanded to 13 by the post-review state-5 fold-in `52f43f7`), sliced by deliverable cohesion matching the diffstat: **Cluster A** = Tasks 1, 2, 3, 5 — envoy-config schema + validator + H1Pool primitive + pool stats wiring; **Cluster B** = Tasks 4 + 4-fold-in + 8 — H1 router-arm pool integration + cx_active double-count fix + in-process backstop; **Cluster C** = Tasks 6, 7, 9 — backend `--per-path` extension + fixture 0020 + Driver::Http1KeepAlive + parse_bootstrap fuzz seed. Each cluster reviewer read the actual on-disk code (`git diff` + `Read` + `Grep`) per the standing precedent-verification discipline, not the PROGRESS narrative. Cluster A returned `Ready to merge: With fixes` with **0 Critical + 2 Important (A-I1 PoolGuard::Drop runtime-availability guard + A-I2 sweeper interval ≥1ms clamp) + 1 deferred-to-13.2 (A-I3 spurious-overflow race under concurrent acquire/release) + 5 Minor (A-M1 through A-M5)**; the 2 Importants were 1-2 line mechanical fixes — both closed at the state-5 fold-in commit `52f43f7` per `BOOTSTRAP_PROMPT.md` §5.2 (review feedback re-entry → step 3); A-I3 deferred to 13.2 per the reviewer's own recommendation (cleanest fix touches the H1+H2 pool Drop's architecture jointly). Cluster B returned `Ready to merge: Yes` with **0 Critical + 0 Important + 3 Minor (B-M1 through B-M3)**; Cluster B explicitly verified the Task 4 fold-in's cx_active regression test is genuinely sensitive (HIGH CONFIDENCE evidence chain: pre-fix code shape from `git show 490bb96` + PoolGuard's `acquire_cx_active_guard` fires on the SAME shared `Arc<Gauge>` via idempotent same-kind registration + test asserts in-flight `cx_active == 1` after 200ms settle + PROGRESS:720-723 implementer attestation of revert-and-verify `assertion left == right failed: left: 2, right: 1`). Cluster C returned `Ready to merge: Yes` with **0 Critical + 0 Important + 4 Minor (C-M1 through C-M4)**; Cluster C verified all 3 named state-5 ratifications PASS (Task 7 helper keep-alive fold-in correctly honors HTTP/1.1 default keep-alive + fixture 0019 byte-equal preservation structurally proven; cluster per-class `upstream_rq_{2,3,4}xx` deferral principled + I2 (a) closure semantics preserved + deferral discoverable via 3 surfaces). **Post-fold-in, no Critical or Important findings remain.** The 12 active Minor carryforwards are all awareness-only / close-opportunistically per the standing post-MVP-trunk discipline.

**Phase 13.1 is the SECOND phase since the MVP trunk closed to land an in-phase state-5 REVIEW fold-in (`52f43f7`),** following the 12.2 fold-in `adc50b7` precedent. Cluster A's 2 Importants did NOT manifest at the §7.5 gate at this state-5 session (the local `cargo test --workspace` passed at 865/0/2 pre-fold-in) but ARE latent landmines: A-I1 would panic any `PoolGuard` dropped after a runtime exit (e.g. fast SIGTERM mid-drain, panic-during-test); A-I2 would panic a future config-driven `idle_timeout = 0`. Both were 1-2 line mechanical fixes paired with TDD regression tests; both landed in the in-phase recovery commit `52f43f7` per `BOOTSTRAP_PROMPT.md` §5.2. The 3rd in-fold-in mitigation — the access-log flake fix — is structural (replaces the harness's wait-for-files-to-EXIST loop with a wait-for-files-to-be-NON-EMPTY loop), closing the race that produced CI run `26375100437`'s `envoy=1 envoy-rust=0` failure at predecessor HEAD `13bb5cc`; the existing 5s budget + post-loop 100ms OS-flush yield are preserved.

**Phase 13.1 is the THIRD Upstream-robustness-family REVIEW.md** (after 12.1 + 12.2) — the verdict + REVIEW shape continue the 06.x → 07.x → 08.x → 09 → 10 → 11 → 12.1 → 12.2 post-MVP standard: `Approved with M-track follow-ups` disposition; forward-carry inheritance for unexercised-pattern Minor findings; explicit calibration rationale per finding; full §7.5 6-gate re-attestation against the post-fold-in CI anchor. **13.1 ENGAGES** the named carryforward **06.3 REVIEW I2 (a) — FULLY CLOSED** at Task 7 (fixture 0020's per-class HCM `downstream_rq_{2,3,4,5}xx` bilateral assertions + `cluster.upstream_rq_5xx` bilateral assertion). **13.1 DEFERS** **06.3 REVIEW I2 (b) — to 13.2 D7.1** (the `upstream_cx_total` BEHAVIOR_CONTRACT row tightening to `value-exact` fires when both H1 + H2 pools tighten uniformly; PLAN lock-in #3). 13.1 generates 12 new Minor carryforwards (none gating; full inventory in §3 + §4 below) + 1 deferred-to-13.2 (A-I3 spurious-overflow race) + 1 known-deferred small follow-up (cluster per-class `upstream_rq_{2,3,4}xx` counter family extension).

## §2. Strengths

1. **The cycle-resolution pick (b) external `H1PoolManager` is verifiably clean.** Cluster A confirmed: `envoy-http1` retains its existing `envoy-cluster` dep (pre-existing from 06.1-era, NOT introduced by this commit per `git show 368d6ef -- crates/envoy-http1/Cargo.toml`); `H1PoolManager` at `crates/envoy-http1/src/pool.rs:295-375` is correctly a sibling registry to `ClusterManager`, NOT a field on `Cluster` — mirrors the 12.2 `envoy-health::Scheduler` precedent verbatim. envoy-bin wires `H1PoolManager::for_bootstrap` at `crates/envoy-bin/src/main.rs:137-143`, exactly between `cluster_mgr` construction (line 123) and `health_scheduler` spawn (line 150). NO new top-level Cargo dep introduced. NO modification to `envoy-cluster::Cluster`'s struct shape (only `ConnGaugeGuard` gains a public `from_gauge` constructor — a 4-line clean addition per `crates/envoy-cluster/src/cluster.rs`). NO ADR fires (lock-in #16 — ordinary structure, same shape 12.2 used).

2. **The Task 4 fold-in cx_active double-count fix has a genuinely sensitive TDD regression test (HIGH CONFIDENCE).** Cluster B verified by reading the pre-fix code shape via `git show 490bb96 -- crates/envoy-http1/src/hcm.rs:497-501` (the unconditional `let _cx_guard = cluster.cx_active_guard();` firing `cx_active.inc()` BEFORE pool dispatch) AND PoolGuard's own `acquire_cx_active_guard` at `pool.rs:233-236` (calls `self.cx_active.inc()` then `ConnGaugeGuard::from_gauge(Arc::clone(&self.cx_active))`) — the gauge handles are the SAME Arc per the same-kind-idempotent contract (verified at `pool.rs:350-351` registration paired with cluster's). The regression test `h1_hcm_pool_path_does_not_double_count_cx_active` at `hcm.rs:3389-3589` drives one request, holds the backend open via `tokio::sync::oneshot`, sleeps 200ms (generous for in-process TCP + synchronous inc paths), asserts `cx_active.value() == 1` — pre-fix would have been 2; post-fix is 1. The implementer's PROGRESS attestation at `PROGRESS.md:720-723` reports the revert-and-verify experiment showed exactly `assertion left == right failed: left: 2, right: 1` — the precise failure mode the analysis predicts.

3. **The 06.3 REVIEW I2 (a) closure is honestly attributed per D-3.4.** Cluster C verified: PROGRESS Task 1 preamble at `PROGRESS.md:55-58` defines the I2 (a) closure surface as "per-class downstream_rq_3xx/4xx/5xx + cluster.upstream_rq_5xx wire-level bilateral coverage"; fixture `0020-*/expectations.yaml:50-58` carries the full HCM-side per-class block + the cluster `_5xx` + the 2 pool-reuse counters. The PLAN deviation #2 (cluster per-class `upstream_rq_{2,3,4}xx` deferral) is principled — envoy-rust's `Cluster` carries only `upstream_rq_total + upstream_rq_5xx` Arc<Counter> fields today (verified at `crates/envoy-cluster/src/cluster.rs:60-76`); adding the 3 missing per-class counter families requires 3 new fields + 3 register calls + 3 increment sites at the H1/H2 response paths — a genuine 1-task follow-up not a hand-wave. The deferral is discoverable via 3 independent surfaces: (a) inline 7-line deferral note at `tests/fixtures/0020-*/expectations.yaml:27-34` naming the missing counters + the source file:line + the owner phase + the byte-equal re-grow path; (b) PROGRESS.md:1160-1182 documents the deferral with task-time discovery rationale; (c) STATE.md carries the deferral as a known-deferred follow-up carryforward.

4. **The atomic Task 7 commit honors lock-in #4.** Cluster C verified via `git show --stat ec50093` that the fixture YAMLs, the `Driver::Http1KeepAlive` serde variant (`kind: http1_keep_alive`), the 2 supporting structs, the 2 dispatch helpers, the 3 harness-wiring extensions, the `--per-path` selector, the `spawn_with_per_path` shim, AND the Docker-gated wrapper test ALL land in ONE commit — neither piece is meaningful in isolation. `git log --follow tests/fixtures/0020-*/expectations.yaml` returns exactly that single SHA.

5. **The clean DAG enforcement is structural + cycle-free.** Cluster A confirmed via Cargo.lock + `cargo build --workspace`: the new `H1Pool` primitive lives inside `envoy-http1` (which already depends on `envoy-cluster` per the 04.3 precedent); `envoy-cluster` does NOT depend on `envoy-http1` (no back-edge); `H1PoolManager` is an external registry held by `envoy-bin` (not a field on Cluster). The build-time enforcement is what keeps future refactors from silently introducing a cycle.

6. **The 5 standard HTTP/1.1 response headers are correctly asserted bilaterally on the per-class non-2xx paths in the in-process backstop.** Cluster B verified: `crates/envoy-bin/tests/upstream_connection_pooling.rs:339-341` (`assert_5_standard_headers_present`) runs on every non-2xx response (301, 404, 500) per 10 REVIEW M1; the fixture 0020 expectations.yaml carries the same 5-header roster check via the existing harness header rules — the 4 non-2xx classes are bilaterally header-checked end-to-end vs `envoyproxy/envoy:v1.33.0` AND in-process.

7. **The 3-files-same-commit fuzz-seed discipline (the 09/10/11/12 Task-7 lesson) holds at Task 9.** Cluster C verified via `git show --stat 8f14d5c`: the seed `crates/envoy-config/fuzz/corpus/parse_bootstrap/cluster_circuit_breakers.yaml`; the `.gitignore` allow-list line at `crates/envoy-config/fuzz/.gitignore:27`; the `fuzz_corpus_seeds_parse_or_reject_cleanly` SUCCESS-array extension at `crates/envoy-config/src/bootstrap.rs:3619` — ALL three load-bearing files land together. The seed exercises the FULL `circuit_breakers.thresholds[0].{priority, max_connections}` shape (the only seed in the 21-seed corpus with `circuit_breakers`); non-redundant vs the existing 20 SUCCESS seeds.

8. **The Task 4 fold-in cadence is the in-phase recovery exemplar.** Cluster B verified: the fold-in landed as a NEW commit (not amend-and-force-push — Task 4 was already pushed; system safety protocol forbids force-push to main); the fix is mechanical (relocating `_cx_guard` from unconditional outer-scope to OneShot-conditional inner-scope); PROGRESS attributes the bug + the empirical revert-and-verify proof honestly per D-3.4. This sets the precedent for the second in-phase recovery at the state-5 fold-in `52f43f7`.

9. **The Cargo.lock cadence ratification + no new top-level Cargo deps.** Cluster A + Cluster C confirmed: `git diff Cargo.toml Cargo.lock e64672b..52f43f7` is non-empty (envoy-http1's Cargo.toml gains a sub-crate `tokio-util` dep — already a workspace member dep for `envoy-bin` + `envoy-health`; version `0.7` + features `["rt"]` exactly match `envoy-health/Cargo.toml:19`) but adds **ZERO new top-level Cargo deps**. The helper crate `tests/helpers/health-aware-http1-backend` was extended additively at Task 6 + Task 7 with no new deps. All touched crate roots keep `#![forbid(unsafe_code)]`; ENVOY_TARGET pin (`v1.33.0`) unchanged at `tests/differential/src/upstream.rs:13`; `rust-toolchain.toml` untouched (D-3.7 + D-3.9 unaffected).

10. **The state-5 fold-in `52f43f7` is mechanical + close-paired with detailed in-line comments + 2 new TDD regression tests.** A-I1 landed as a Drop-impl restructuring with `Handle::try_current()` guard + inline comment naming the shutdown / test-runtime-exit scenarios; A-I2 landed as a one-line `.max(Duration::from_millis(1))` clamp + inline comment naming the SPEC §2 item-iii future-config rationale; access-log flake mitigation landed as a structural loop replacement + inline comment citing CI run `26375100437` failure mode. Workspace test count post-fold-in 867/0/2 (+2 over state-4 baseline 865 = the 2 new regression tests). Mirrors the 12.2 state-5 fold-in `adc50b7` cadence.

11. **Inert-when-unconfigured (the §5.4 architectural invariant) verified end-to-end via the Docker 20/20 run.** Only fixture 0020 configures `circuit_breakers` (`grep circuit_breakers tests/fixtures/*/envoy.yaml` returns exactly fixture 0020). The 19 pre-existing fixtures (`0001-tcp-echo` through `0019-upstream-active-health-check`) all carry NO `circuit_breakers` block → the H1 pool runs with hardcoded defaults (`max_connections: 1024`; `idle_timeout: 60s`) on those fixtures' clusters → existing presence-only `upstream_cx_total` assertions cannot regress (per the §5.4 reasoning at SPEC line 24). The state-4 Docker run produced **20/20 fixtures green simultaneously** (`cargo test -p differential -- --include-ignored` → 129 passed / 0 failed / 0 ignored across 22 result lines) confirming this end-to-end.

12. **The 3 Task 7 PLAN deviations are all honest improvements per D-3.4** (NOT shortcuts). Cluster C verified: deviation #1 (helper backend keep-alive support) is a load-bearing pool-reuse enabler — without it, the helper closes per request, defeating the H1 pool's reuse property AND breaking fixture 0019 byte-equal (which sends `Connection: close` per probe, so the helper still closes per probe — regression-equivalence preserved). Deviation #2 (cluster per-class `upstream_rq_{2,3,4}xx` deferral) is principled per §2.3 above. Deviation #3 (`scrape_admin_stat` carved-out) is a helper refactor improving testability + reuse for future fixtures.

## §3. Issues

### Critical (must fix before phase commit)

None.

### Important (close in 13.1 or carry forward with named owner phase)

None — both Cluster A Importants closed in-phase at `52f43f7`.

**Closed-at-state-5 fold-in (`52f43f7`):**

- **A-I1 (Cluster A; CLOSED).** `PoolGuard::Drop` blindly called `tokio::spawn` at `crates/envoy-http1/src/pool.rs:118` + `:129`. If a `PoolGuard` was ever dropped after the tokio runtime shut down (fast SIGTERM mid-drain, `#[tokio::test]` runtime exit before all guards drop, panic-during-test), `tokio::spawn` would panic with "there is no reactor running" — which would mask the actual shutdown / test-failure cause. Closed at `52f43f7` by guarding both `tokio::spawn` sites behind `tokio::runtime::Handle::try_current()`; on `Err`, the stream drops synchronously (its own Drop closes the TCP socket) and the async return-to-pool / established-decrement bookkeeping is skipped. `cx_destroy.inc()` on the invalidate path stays synchronous (counter inc does not need a runtime). `_cx_active_guard`'s Drop still fires at field-drop time → `upstream_cx_active.dec()`. New TDD regression test `pool_guard_drop_outside_runtime_does_not_panic` at `pool.rs:592-616` builds the pool + acquires a guard inside a short-lived `Builder::new_current_thread` runtime, drops the runtime, then drops the guard — pre-fix this would panic; post-fix the test passes + `cx_active` correctly decrements to 0.

- **A-I2 (Cluster A; CLOSED).** `H1Pool::spawn_idle_sweeper` computed `interval_period = pool.idle_timeout / SWEEPER_DIVISOR` at `crates/envoy-http1/src/pool.rs:246`. Today `DEFAULT_IDLE_TIMEOUT = 60s` → 15s (safe), but `H1Pool::new` accepts `idle_timeout: Duration` publicly and SPEC §2 item-iii defers a future config-driven knob — `tokio::time::interval(Duration::ZERO)` panics. Closed at `52f43f7` by clamping the interval to `.max(Duration::from_millis(1))` — one-line defensive guard. New TDD regression test `spawn_idle_sweeper_with_zero_idle_timeout_does_not_panic` at `pool.rs:618-630` constructs a pool with `idle_timeout: Duration::ZERO`, spawns the sweeper, sleeps 10ms (the clamped 1ms interval ticks at least once), cancels the token, asserts the sweeper exits cleanly without panic.

### Minor (awareness-only / track forward)

**12 Minor findings — 5 Cluster A + 3 Cluster B + 4 Cluster C. None gating; all awareness-only / close-opportunistically per the standing post-MVP-trunk discipline.**

**Cluster A Minors (Tasks 1-3 + Task 5 — envoy-config schema + H1Pool primitive + stats wiring):**

**A-M1. `_sweepers` field naming + no explicit `shutdown()` on `H1PoolManager` (`pool.rs:299`).** Stored as `_sweepers: Vec<JoinHandle<()>>` (leading underscore = "owned but unread"); shutdown is implicit via envoy-bin's `token.cancel()` at `main.rs:91`. Works correctly but the underscore-prefix is mildly misleading. **Disposition:** rename + add a `pub async fn shutdown(self)` mirroring `envoy-health::Scheduler::shutdown` for ergonomic parity. Named owner: 13.2 D5/D6 (joint H1/H2 lifecycle hardening).

**A-M2. `acquire_cx_active_guard` duplicates the inc+wrap pattern (`pool.rs:233-236`).** Re-implements `Cluster::cx_active_guard`'s `inc + wrap` against the shared `Arc<Gauge>`. The same-kind-idempotency contract (envoy-stats `registry.rs:141`) guarantees the gauge handle is the same Arc, but this is not asserted at the construction site. **Disposition:** add `debug_assert!(Arc::ptr_eq(...))` in `H1PoolManager::for_bootstrap` against the cluster handle's gauge. Named owner: next phase opening `pool.rs`.

**A-M3. Cluster A reviewer's "6 deviations" framing in the prompt vs PROGRESS Task 3's actual 3 documented adaptations (`PROGRESS.md:388-414`).** The PROGRESS documentation is honest per D-3.4 about the 3 it covers (collapsible_if, two-phase sweep, tokio-util sub-crate + `from_gauge` test scope); the "6 deviations" framing in the controller prompt counted implicit micro-deviations like the hand-rolled `Debug` impl + `_sweepers` underscore-prefix. **Disposition:** narrative-finishing-touch only; no action. Named owner: none.

**A-M4. `H1PoolManager::for_bootstrap` `.expect("cluster present in mgr (built from same bootstrap)")` at `pool.rs:332`.** Today envoy-bin always passes the same `&Bootstrap` it built the cluster_mgr from, so the invariant holds. A future caller wiring two different bootstraps would panic. **Disposition:** either change to a `Result` return + new `StatsError` variant, OR document the invariant as a precondition on the public method. Named owner: next phase opening `pool.rs`.

**A-M5. BEHAVIOR_CONTRACT row for `upstream_cx_destroy` rationale says "the second periodic-background primitive" (`BEHAVIOR_CONTRACT.md:154`).** Accurate (the idle sweeper is the second after the 12.2 active-HC scheduler). **Disposition:** no fix needed; awareness only.

**Cluster B Minors (Tasks 4 + 4-fold-in + 8 — H1 router-arm pool integration + cx_active fix + in-process backstop):**

**B-M1. Tempfile leak via `std::mem::forget` (`crates/envoy-bin/tests/upstream_connection_pooling.rs:276`).** Deliberately leaks the bootstrap `NamedTempFile` to ensure envoy-bin can re-open the path after the test driver moves past the spawn site. Documented in inline comment; mirrors the 12.2 `upstream_active_health_check.rs` precedent. **Disposition:** leave as-is; carry forward as a workspace-wide cleanup item to a future test-infrastructure pass.

**B-M2. Task 4 regression test's discriminating power is narrower than its comment implies (`crates/envoy-http1/src/hcm.rs:3196-3251`).** `h1_hcm_pool_reuses_upstream_conn_across_sequential_requests` drives 5 sequential GETs on ONE downstream conn → `cx_total == 1`. The pre-13.1 tier-1 `cached_upstream` micro-cache would ALSO have given `cx_total == 1` on this workload. The test proves "pool reuse works at the per-conn boundary" — not "pool > cache". The cross-downstream-conn discriminating observable lives only in fixture 0020 + the in-process backstop. The test comment at hcm.rs:3197-3201 acknowledges this honestly. **Disposition:** awareness-only; the true cross-downstream-conn discriminator lives in fixture 0020 (Docker-gated). Named owner: none.

**B-M3. Hand-rolled HTTP parser in `upstream_connection_pooling.rs` is brittle to missing Content-Length (`:103-107`).** Panics with `expect("content-length header present")` if the response lacks a CL header. Fine for the corpus (envoy-bin always synthesizes CL for the test responses); inherits the 12.2 B-M3 carryforward shape. **Disposition:** awareness-only; replace with a slimmer chunked-aware parser if a future backstop grows complexity. Named owner: next phase extending this backstop.

**Cluster C Minors (Tasks 6 + 7 + 9 — backend --per-path + fixture 0020 + Driver::Http1KeepAlive + fuzz seed):**

**C-M1. `read_h1_response_status` silently treats missing Content-Length as body-len 0 (`tests/differential/src/lib.rs:1559-1569`).** Parses CL with `.unwrap_or(0)` and skips body drain when `cl == 0`. If the helper backend ever regresses on CL emission (or a future driver hits a response from a non-helper origin), the next request on the keep-alive conn will read leftover body bytes as its own status line and surface as a confusing `malformed status line` error rather than a precise framing failure. The helper always emits CL today (`main.rs:200-205`) so this is defensive-only. **Disposition:** opportunistic close in 13.2 or a future driver phase if read_h1_response_status grows additional consumers; bail with `"Content-Length header missing"` when the header is absent.

**C-M2. `scrape_admin_stat` `Ok(0)` fallback for absent stat could mask a missing-stat regression (`tests/differential/src/lib.rs:1612-1613`).** Returns `Ok(0)` when the stat name is not present in the `/stats` body. Today fixture 0020 has no `expected_stats` entry with `value: 0`, so a missing stat on either side would FAIL the assertion (expected N ≠ 0). If a future fixture asserts `value: 0` for any counter, the helper would silently pass even when the stat is unregistered. **Disposition:** revisit when the first `value: 0` assertion lands; consider returning `Err` or `Option<u64>` when the name is absent. Named owner: next phase adding a `value: 0` stat assertion.

**C-M3. Stale doc-comment on the wrapper test file (`tests/differential/tests/upstream_connection_pooling_and_per_class_counters.rs:5-7`).** Claims the test asserts `cluster upstream_rq_{2,3,4,5}xx`, but per Task 7 PLAN deviation #2 only `upstream_rq_5xx` (and `_total`) lands. The actual `expected_stats` block in `expectations.yaml:55-58` is correct; only the wrapper-test doc-comment drifted. **Disposition:** opportunistic close in the cluster per-class follow-up phase (when the rows re-grow, the doc-comment becomes correct again). Named owner: the cluster per-class `upstream_rq_{2,3,4}xx` extension follow-up.

**C-M4. Helper keep-alive loop's `bail!("EOF before headers complete")` after partial-buffer EOF prints stack noise (`tests/helpers/health-aware-http1-backend/src/main.rs:159-164`).** Distinguishes empty-buffer EOF (clean) from partial-buffer EOF (error); cosmetic only (no test impact). **Disposition:** awareness-only; the existing handler is correct. Named owner: none.

**Confirmed-closed at task time (not live carryforwards):**

- **Task 4 fold-in `8e3774d`** — cx_active double-count fix + 1 TDD regression test + 2 cheap-fold-in Minor comments. Closed at task time. Not a live carryforward.
- **A-I1 + A-I2 (state-5 Cluster A Importants)** — CLOSED at state-5 fold-in `52f43f7`. Not live carryforwards.
- **REVIEW §4 access-log flake mitigation** — LANDED at state-5 fold-in `52f43f7`. The transient CI flake on `13bb5cc` is structurally precluded going forward.

## §4. Carryforward inventory

For each Minor: closure plan. (No Critical or Important items remain; A-I1 + A-I2 closed in-phase at `52f43f7`.)

| ID | Severity | Item | Closure plan |
|---|---|---|---|
| **A-I3** | Important-deferred | Spurious-overflow race under concurrent acquire/release | **Defer to 13.2 D5/D6** per the reviewer's own recommendation — cleanest fix touches the H1+H2 pool Drop's architecture jointly (`try_lock` or a lock-free per-endpoint slot). Non-blocking at 13.1's deterministic harness load (existing tests serialize acquires + sleep 50ms after drops). Named owner: 13.2. |
| **A-M1** | Minor | `_sweepers` field underscore-prefix + no explicit `shutdown()` on H1PoolManager | **Close at 13.2 D5/D6** (joint H1+H2 lifecycle hardening). Rename to `sweepers` + add `pub async fn shutdown(self)` mirroring `envoy-health::Scheduler::shutdown`. Non-blocking. |
| **A-M2** | Minor | `acquire_cx_active_guard` lacks `Arc::ptr_eq` debug-assert | **Close opportunistically** at next phase opening `pool.rs`. Non-blocking. |
| **A-M3** | Minor | "6 deviations" framing vs PROGRESS Task 3's 3 documented adaptations | **No action recommended** (narrative-finishing-touch; PROGRESS is honest per D-3.4). Named owner: none. |
| **A-M4** | Minor | `H1PoolManager::for_bootstrap` `.expect` future-caller panic surface | **Close opportunistically** — either `Result` return + new variant, OR document precondition. Named owner: next phase opening `pool.rs`. Non-blocking. |
| **A-M5** | Minor | BEHAVIOR_CONTRACT row rationale phrasing | **No action recommended.** Named owner: none. |
| **B-M1** | Minor | Tempfile leak via `std::mem::forget` in upstream_connection_pooling backstop | **No action recommended** (matches 12.2 precedent; deliberate per inline comment). Carry forward as a workspace-wide test-infrastructure cleanup item. Named owner: none. |
| **B-M2** | Minor | Task 4 unit test's discriminating power narrower than comment | **No action recommended** (true cross-downstream-conn discriminator lives in fixture 0020). Named owner: none. |
| **B-M3** | Minor | Backstop `http1_get` parser brittle to missing Content-Length | **Close opportunistically** at next phase extending the backstop. Named owner: next phase extending this backstop. Non-blocking. |
| **C-M1** | Minor | `read_h1_response_status` silent missing-CL fallback | **Close opportunistically** at 13.2 or a future driver phase. Bail with explicit framing-error. Named owner: next phase extending read_h1_response_status. |
| **C-M2** | Minor | `scrape_admin_stat` `Ok(0)` fallback could mask missing-stat regression | **Revisit when first `value: 0` assertion lands** — consider `Err` or `Option<u64>` return. Named owner: next phase asserting `value: 0` on any counter. |
| **C-M3** | Minor | Wrapper-test doc-comment stale (claims `upstream_rq_{2,3,4,5}xx`) | **Close opportunistically at cluster per-class follow-up** — when the deferred 3 rows land, the doc-comment becomes correct again. Named owner: cluster per-class `upstream_rq_{2,3,4}xx` extension follow-up. |
| **C-M4** | Minor | Helper keep-alive loop's `bail!` cosmetic stack noise on partial EOF | **No action recommended** (cosmetic only). Named owner: none. |

**None of the new carryforwards gate the state-6 close-out OR any future phase.** All are awareness-only / close-opportunistically per the standing post-MVP-trunk discipline. A-I3 is principally deferred to 13.2 per the reviewer's recommendation — also non-blocking at 13.1's load.

**Known-deferred small follow-up surfaced at Task 7 PLAN deviation #2 (NOT a REVIEW Minor; a separate-task follow-up):**

- **Cluster per-class `upstream_rq_{2,3,4}xx` counter family extension.** envoy-rust's `Cluster` (`crates/envoy-cluster/src/cluster.rs:60-76`) carries only `upstream_rq_total + upstream_rq_5xx` Arc<Counter> fields today. Adding the 3 missing per-class counter families requires 3 new fields + 3 register calls + 3 increment sites at the H1/H2 response paths. Genuine 1-task follow-up; surface estimate per PROGRESS:1160-1182. Named owner: a future upstream-robustness or observability-family phase that opens `cluster.rs:60-76`.

**Process note (awareness-only, no carryforward).** Phase 13.1 is the SECOND phase since the MVP trunk closed to land an in-phase state-5 REVIEW fold-in (`52f43f7`), following the 12.2 fold-in `adc50b7`. The in-phase recovery worked exactly as `BOOTSTRAP_PROMPT.md` §5.2 prescribes — state-3 re-entry, mechanical fix (2 Importants + 1 Minor M-track), local-gate re-confirmation (`cargo test --workspace` → 867/0/2 +2 over baseline), CI re-attestation (run `26395285596` at HEAD `52f43f7`). The result is a stronger phase (zero residual Importants, the A-I1/A-I2 latent landmines closed, the access-log flake structurally precluded). Awareness-only; the doctrine works as designed.

**Standing carryforwards inherited but not closed in 13.1** (per PROGRESS Task 1 preamble carryforward disposition; 13.1 ENGAGED **06.3 REVIEW I2 (a) FULL CLOSURE** at Task 7 — NOT (b) closure; (b) defers to 13.2 D7.1):

- **06.3 REVIEW I2 (a) — FULLY CLOSED at Task 7.** Not a live carryforward.
- **06.3 REVIEW I2 (b)** — DEFERS to 13.2 D7.1 (the `cluster.<name>.upstream_cx_total` BEHAVIOR_CONTRACT row tightening to `value-exact`; fires when both H1 + H2 pools tighten uniformly).
- **12.2 REVIEW Minor carryforwards (11 active: A-M2, A-M4, B-M1, B-M2, B-M3, B-M4, B-M5, B-M6, C-M1, C-M2, C-M4)** — carry forward unchanged; **not engaged** in 13.1 (the named seams weren't touched — A-* are in envoy-health which 13.1 doesn't touch; B-* on health-aware-http1-backend were extended at Task 6 + Task 7 but additively, not in B-M1..B-M6's named-seam patterns).
- **12.1 REVIEW M1 + M3** — carry forward unchanged; **not engaged**.
- **Phase-11 REVIEW M1-M8** — carry forward unchanged; **not engaged** (13.1 touches no HTTP-filter file).
- **10 M2/M3/M4/D1/D2/T1; 09 M1/D1/D2/T1/T2/T3; 08.2 REVIEW M1-M8 + T1-T3; 08.1 REVIEW M3; 07.2 REVIEW M2/M3; 06.2 REVIEW M1/M2/M4/M5; 06.1 REVIEW M2/M3/M5/M6; 05.3 REVIEW I2; 05.2 REVIEW I1/I2/I3; 04.1 REVIEW M5/M9/M-claim/M1/M2/M4/M7; 02.2 REVIEW M1; Phase-00 I3 SIGKILL→SIGTERM** — carry forward indefinitely unless coincidentally engaged. None engaged by the 13.1 surface (touches `envoy-config` + `envoy-http1` + `envoy-cluster` constructor + `envoy-bin` wiring + test infrastructure).
- **04.1 REVIEW M5 / M9 Cargo.lock cadence ratification** — carries forward unchanged; **zero new top-level Cargo deps** at this phase (the `tokio-util` sub-crate dep added to envoy-http1 is workspace-pre-existing as a member dep for envoy-bin + envoy-health). Cargo.lock diff at the reviewed range is non-empty but ratified by Cluster A + Cluster C as deps-on-existing-foundations only.

## §5. Disposition decisions

**Closed in 13.1:**

- **06.3 REVIEW I2 (a)** (per-class `downstream_rq_3xx/4xx/5xx` + `cluster.<name>.upstream_rq_5xx` wire-level bilateral coverage) — **FULLY CLOSED at Task 7** via fixture 0020's per-class assertions. PROGRESS attributes the closure honestly at PROGRESS.md:55-58 + 1141-1191; expectations.yaml carries the inline deferral note for the cluster per-class `_2xx/3xx/4xx` follow-up (envoy-rust's `Cluster` lacks those fields today).
- **Cluster A I1** (state-5 review — `PoolGuard::Drop` `tokio::spawn` runtime-availability guard) — CLOSED at state-5 fold-in `52f43f7` via `Handle::try_current()` guard + synchronous drop fall-through + new TDD regression test.
- **Cluster A I2** (state-5 review — sweeper interval ≥1ms clamp) — CLOSED at state-5 fold-in `52f43f7` via `.max(Duration::from_millis(1))` clamp + new TDD regression test.
- **REVIEW §4 access-log flake mitigation** (state-5 review M-track follow-up; recommended posture per the prompt + the 12.2 Task 8 follow-up `b1cb25c` precedent) — LANDED at state-5 fold-in `52f43f7` via wait-for-non-empty replacing wait-for-exists in the harness's access-log scrape loop. Structurally precludes the CI flake observed at `13bb5cc`.

**Deferred per reviewer recommendation:**

- **A-I3** (Cluster A — spurious-overflow race under concurrent acquire/release) — **DEFERRED to 13.2 D5/D6** per the reviewer's own recommendation. Cleanest fix touches the H1+H2 pool Drop's architecture jointly (`try_lock` or a lock-free per-endpoint slot); non-blocking at 13.1's deterministic harness load.

**06.3 REVIEW I2 (b)** — **DEFERS to 13.2 D7.1** (the `cluster.<name>.upstream_cx_total` BEHAVIOR_CONTRACT row tightening to `value-exact`; fires when both H1 + H2 pools tighten uniformly). PROGRESS attributes the deferral at PROGRESS.md:58 + BEHAVIOR_CONTRACT.md `13.1 entries` row rationale.

**Continuing carryforwards by named owner phase:** see §4 standing carryforwards inventory above.

**New carryforwards generated by 13.1** (12 Minor + 1 deferred-to-13.2 + 1 known-deferred small follow-up):

- **A-I3** (deferred to 13.2 D5/D6).
- **A-M1** through **A-M5** (5 Cluster A Minors — close at 13.2 D5/D6 OR opportunistic / no-action).
- **B-M1** through **B-M3** (3 Cluster B Minors — opportunistic / no-action).
- **C-M1** through **C-M4** (4 Cluster C Minors — opportunistic / no-action).
- **Cluster per-class `upstream_rq_{2,3,4}xx` counter family extension** (known-deferred small follow-up; not a REVIEW Minor — a separate-task surface for a future upstream-robustness or observability phase).

**None of the new carryforwards gate the state-6 close-out OR any future phase.** All Minors are awareness-only / close-opportunistically per the standing post-MVP-trunk discipline.

## §6. Phase-done gate (§7.5) verification

Per `BOOTSTRAP_PROMPT.md` §7.5 and 13.1 SPEC §1 acceptance signal (a)–(f), re-attested at the post-state-5-fold-in CI anchor — CI run **`26395285596`** at HEAD `52f43f7` (the state-5 fold-in commit) settled `completed / success` in **2m19s** (139s wall). The pre-fold-in CI run **`26377609763`** at HEAD `592d9e7` (the state-4 commit) settled `completed / success` in 2m29s — confirms the full state-3 execution arc is GREEN; the state-5 fold-in delta is the 3 mechanical fixes (A-I1 + A-I2 + access-log flake mitigation), all GREEN at this CI re-attestation. The state-4 session ALSO performed a full LOCAL bilateral Docker re-run corroborating every gate.

**(a) New differential fixture green.** ✓ — fixture `0020-upstream-connection-pooling-and-per-class-counters` GREEN at the local Docker re-run at the Task 10 state-4 verification (`cargo test -p differential -- --include-ignored` listed `upstream_connection_pooling_and_per_class_counters_fixture` at 3.45s — the load-bearing 13.1 differential proof bilaterally vs `envoyproxy/envoy:v1.33.0`). The discriminating observable (`cluster.backend_cluster.upstream_cx_total: 1` under the single-downstream-keep-alive-conn 10-request driver) verified bilaterally. CI re-attestation at HEAD `592d9e7` (`26377609763` GREEN).

**(b) Pre-existing 19 differential fixtures (`0001-0019`) green simultaneously.** ✓ — all 19 (`0001-tcp-echo` through `0019-upstream-active-health-check`) GREEN simultaneously with the new `0020` in the local Docker re-run + the CI run `26377609763`. **20/20 in one run** bilaterally vs `envoyproxy/envoy:v1.33.0` — the load-bearing 13.1 regression-equivalence proof; the §5.4 inert-when-unconfigured invariant holds (only 0020 configures `circuit_breakers`). Notable: `upstream_active_health_check_fixture` (0019) still passes — regression-equivalence confirmed against the Task 7 helper-backend keep-alive extension (the active-HC probes still send `Connection: close` so the helper closes per probe as fixture 0019 expects).

**(c) `h2spec` ≥95%.** ✓ — held at the parent-05 baseline **99.31%**. 13.1's only envoy-http2 touch is the Task 4 `HCMConfig::from_config` call-site threading of the new `pool_mgr: Option<Arc<H1PoolManager>>` parameter (`crates/envoy-http2/src/hcm.rs` — type-system only; no codec change, no frame handling change, no SETTINGS / HEADERS / DATA emission path touched). Locally skipped via the `h2spec_pass_rate_gate` test's `which h2spec` graceful-skip per 05.2 SPEC §3 D7 (no `h2spec` binary on the dev box); CI fires bilaterally with `h2spec` installed.

**(d) `parse_bootstrap` fuzz target clean for short-budget CI run.** ✓ — `cd crates/envoy-config && cargo +nightly fuzz run parse_bootstrap -- -runs=200000` at the Task 10 state-4 → `Done 200000 runs in 16 second(s)`, 0 crashes (cov **13636**, ft **37080**, corpus 21 SUCCESS seeds at completion; cov +222 / ft +382 over the 12.2 state-4 baseline 13414/36698 — exactly what a first-of-kind `circuit_breakers`-configured cluster seed should produce against the new 13.1 D1 + D2 schema + validator). `fuzz_corpus_seeds_parse_or_reject_cleanly` SUCCESS array now 21 entries (was 20 at 12.2 close). Re-verified at this state-5 session via `cargo test -p envoy-config --lib fuzz_corpus_seeds_parse_or_reject_cleanly` → 1 passed.

**(e) Stable-toolchain gates clean (fmt / clippy / build / test / deny).** ✓ — all five clean in CI run `26377609763` + reproduced LOCALLY at this state-5 session post-fold-in: `cargo build --workspace --all-targets` (`Finished` in 10.92s warm); `cargo clippy --workspace --all-targets --all-features -- -D warnings` (`Finished` in 1m 33s post-fold-in, no warnings); `cargo fmt --all -- --check` (clean exit 0; no diff); `cargo test --workspace` (**867 passed / 0 failed / 2 ignored** — 74 test-result lines aggregated post-fold-in; +2 over the state-4 baseline 865 = the 2 new A-I1 + A-I2 regression tests); `cargo deny check` (`advisories ok, bans ok, licenses ok, sources ok`; the pre-existing harmless `Unicode-DFS-2016` + `Zlib` unmatched-license-allowance notices unchanged from prior phases).

**(f) REVIEW.md verdict** lands at state-5 in this session per the `SKILL_ROUTING.md` state-5 transition. **Verdict per §1: Approved with M-track follow-ups** (zero Critical; zero Important — 2 Importants A-I1 + A-I2 CLOSED in-phase at `52f43f7`; 1 Important-deferred A-I3 carried to 13.2 per reviewer recommendation; 12 active Minor carryforwards [A-M1 through A-M5, B-M1 through B-M3, C-M1 through C-M4] — all awareness-only / close-opportunistically; none has a hard-gated owner; none gates the state-6 close-out). Gate (f) closes at this REVIEW.md verdict.

**All six §7.5 gates disposition: (a) PASS / (b) PASS / (c) PASS (parent-05 baseline 99.31%) / (d) PASS / (e) PASS / (f) CLOSE-at-this-REVIEW.md.**

## §7. Per-cluster code-quality reviewer finding dispositions

The phase-13.1 state-5 review ran a 3-cluster subagent pass over the reviewed range `e64672b..592d9e7` (12 commits at dispatch; the state-5 fold-in `52f43f7` brings the post-review HEAD to 13 commits). Each cluster reviewer read the actual on-disk code (`git diff` + `Read` + `Grep`) per the standing precedent-verification discipline, not the PROGRESS narrative. Disposition per cluster:

**Cluster A — Tasks 1, 2, 3, 5 reviewer (envoy-config schema + validator + H1Pool primitive + stats wiring) — Zero Critical + 2 Important (CLOSED at state-5 fold-in `52f43f7`) + 1 deferred-to-13.2 + 5 Minors.** Verdict: **Ready to merge: With fixes** (Importants closed at `52f43f7`; now **Ready: Yes** post-fold-in). The reviewer verified on-disk: the new `CircuitBreakers`/`Thresholds`/`RoutingPriority` types at `bootstrap.rs:1161-1191` correctly carry `deny_unknown_fields` slamming the 6 phase-13-deferred threshold fields; `Serialize` derives present (08.1 cascade ready); `skip_serializing_if = "Option::is_none"` on `Cluster.circuit_breakers` at `bootstrap.rs:96` keeps existing 18 fixtures' golden snapshots intact; `validate_circuit_breakers` at `bootstrap.rs:2522-2554` early-returns `Ok(())` on `None` (preserves non-regression), then dispatches the 3 rejection arms in 12.1 health-check style; the 4 TDD tests at `bootstrap.rs:8975-9105` are real end-to-end through `parse_bootstrap` (not unit calls on the validator); the H1Pool primitive's cycle resolution is verifiably clean (`H1PoolManager` sibling to ClusterManager, NOT a field on Cluster; `envoy-http1` retains existing `envoy-cluster` dep — NO new dep introduced; mirrors 12.2 `envoy-health::Scheduler` precedent verbatim); idle sweeper cancellation discipline uses `tokio_util::sync::CancellationToken` per parent-13 §5.5; two-phase lock sequence in `sweep_once` (`pool.rs:264-288`) is defensively correct; PoolGuard RAII three-path semantics correctly handle success-return + invalidate + automatic `_cx_active_guard` drop; the 5 TDD tests + the new `from_gauge` constructor are sound; the 6 PLAN deviations are documented honestly + are improvements over the PLAN sketch; the 2 new BEHAVIOR_CONTRACT rows at `:152-157` correctly disposition (`value-exact (0-failures case)` + `value-exact`) AND explicitly name the 13.2 D7.1 deferral site for the future `upstream_cx_total` tightening; the new registration-presence test `h1_pool_manager_registers_cx_destroy_and_cx_http1_total_per_h1_cluster` is real behavior verification. Surfaced 2 Importants → **A-I1** (Drop tokio::spawn runtime-availability guard) + **A-I2** (sweeper interval ≥1ms clamp); BOTH CLOSED at state-5 fold-in `52f43f7`. Surfaced 1 Important-deferred → **A-I3** (spurious-overflow race; deferred to 13.2 per the reviewer's own recommendation). Surfaced 5 Minors → A-M1 through A-M5 (deferred per §4). **Post-fold-in residue: Zero Critical + Zero Important + 5 Minor + 1 deferred-to-13.2.**

**Cluster B — Tasks 4 + 4-fold-in + 8 reviewer (H1 router-arm pool integration + cx_active fix + in-process backstop) — Zero Critical + Zero Important + 3 Minors.** Verdict: **Ready to merge: Yes.** The reviewer verified on-disk: surgical pool-dispatch wiring at `hcm.rs:521-606` with local `enum StreamHandle` covering Pooled (PoolGuard) + OneShot (ClientStream) paths; the 7 pre-existing `envoy-http1` tests + 7 in `envoy-http2`'s type-aliased site got `, None` added mechanically (zero behavior drift); tier-1 `cached_upstream` removal complete (`grep cached_upstream crates/envoy-http1/` returns zero hits); `cx_total.inc()` now has exactly 3 sites in `envoy-http1` (pool's connect-on-miss for production path, 2 fallback sites for no-pool-manager test paths — only ONE fires on the production pool path satisfying lock-in #6); envoy-bin wiring at `main.rs:137-143` correctly between cluster_mgr (line 123) + health_scheduler (line 150); the cx_active double-count fold-in is correctly engineered (the `Option<ConnGaugeGuard>` conditional Some-only on OneShot; PoolGuard's `_cx_active_guard` fires the SAME shared Arc<Gauge> via idempotent same-kind registration; Drop ordering reasoning matches Rust semantics — outer guard declared before consuming match drops AFTER inner handle); the regression test is genuinely sensitive (HIGH CONFIDENCE evidence chain via pre-fix code shape + PoolGuard's `acquire_cx_active_guard` + 200ms settle assertion + PROGRESS attestation of revert-and-verify `assertion left == right failed: left: 2, right: 1`); Task 8 backstop has faithful 12.2 shape parity (`reserve_port` + `wait_ready` + `Stdio::null()/piped()` + `kill_on_drop(true)` + 30s/10s budgets matching 12.2 Cluster B I2 fold-in + ONE downstream `TcpStream::connect` driving 10 sequential keep-alive requests + 5-standard-header assertion on every non-2xx via `assert_5_standard_headers_present`). Surfaced 3 Minors → **B-M1** (tempfile leak via `std::mem::forget` — deliberate per inline comment) + **B-M2** (Task 4 unit test discriminating power narrower than comment — true cross-conn observable lives in fixture 0020) + **B-M3** (backstop `http1_get` parser brittle to missing CL). **No residue rising above Minor.**

**Cluster C — Tasks 6 + 7 + 9 reviewer (backend `--per-path` + fixture 0020 + Driver::Http1KeepAlive + fuzz seed) — Zero Critical + Zero Important + 4 Minors.** Verdict: **Ready to merge: Yes.** The reviewer verified on-disk: Task 6 helper extension is correct (parse_per_path `HashMap<String, u16>`; dispatch priority `/healthz` → per-path → `/--data-status` fallback; per-class body bytes pinned; 3 TDD unit tests are real arrangement tests; ZERO production touch); the helper keep-alive loop is genuinely correct + well-commented (case-insensitive `Connection: close` detection; drains pipelined bytes via `buf.drain(..head_end)`; mirrors close decision into BOTH response `connection:` header AND post-write loop-exit dispatch); fixture 0019 byte-equal preservation structurally verified (0019 uses `Driver::Http1AfterSettle` which sends `Connection: close` per request → helper closes per probe as before); Task 7 atomic commit honors lock-in #4 (all 8 sub-components in ONE commit per `git show --stat ec50093`); fixture 0020 YAMLs are identical-modulo-boilerplate per project convention; the 10-request workload (4×/ + 1×/301 + 2×/404 + 3×/500) matches SPEC §3 D9.1 + the per-class HCM counter values; `Driver::Http1KeepAlive` serde tag `kind: http1_keep_alive` matches SPEC + has TDD round-trip test; `read_h1_response_status` handles Content-Length framing (silent missing-CL fallback flagged as C-M1); `scrape_admin_stat` returns `Ok(0)` for absent stat (flagged as C-M2); cluster per-class deferral is principled + I2 (a) closure preserved + discoverable via 3 surfaces; Task 9 is precisely the mechanical 3-files-one-commit edit (seed YAML + .gitignore line + SUCCESS array); `13bb5cc` follow-up is docs-only with no production drift. Surfaced 4 Minors → **C-M1** (read_h1_response_status silent missing-CL fallback) + **C-M2** (scrape_admin_stat `Ok(0)` could mask missing-stat regression) + **C-M3** (wrapper-test doc-comment stale) + **C-M4** (helper keep-alive cosmetic stack noise). **Carryforward verdict per the 3 named state-5 ratifications:** (a) Task 7 helper keep-alive fold-in — PASS; (b) cluster per-class deferral — 3 PASS (principled + closure semantics preserved + discoverable); (c) overall: ratifications stand. **No residue rising above Minor.**

**Carryforward summary across the 3-cluster reviewer findings:** 0 Criticals + 2 Importants (BOTH CLOSED in-phase at `52f43f7`) + 1 Important-deferred (A-I3 to 13.2 per reviewer recommendation) + 12 Minor (all live carryforwards per §4). **Per `BOOTSTRAP_PROMPT.md` §5.2 the phase RE-ENTERED state 3** at `52f43f7` to close A-I1 + A-I2 + the access-log flake mitigation, then re-attested the gates (local + CI). Post-fold-in, no Critical or Important findings remain. The calibration matches the 06.x → 12.2 precedent — the project's mature-state-3 doctrine working as designed; the in-phase recovery cadence repeats from 12.2's `adc50b7` precedent.

## §8. State-4 evidence re-attestation

Per the state-5 reviewer's mandate to re-attest each of the §7.5 phase-done gate's six sub-gates against the state-4 anchor (PROGRESS Task 10 state-4 verification + the CI green at `592d9e7` + the state-5 fold-in re-attestation at `52f43f7`):

| Gate | Disposition | Evidence |
|---|---|---|
| **(a)** New differential fixture green | **PASS** | Fixture `0020-upstream-connection-pooling-and-per-class-counters` GREEN at the local Docker re-run at the Task 10 state-4 verification + CI run `26377609763` at HEAD `592d9e7` (the state-4 commit; `completed / success` 2m29s) + CI run `26395285596` at HEAD `52f43f7` (the state-5 fold-in re-attestation; `completed / success` 2m19s). The discriminating observable (`cluster.backend_cluster.upstream_cx_total: 1` under the single-downstream-keep-alive-conn 10-request driver) verified bilaterally. |
| **(b)** Pre-existing 19 fixtures (`0001-0019`) green simultaneously | **PASS** | All 19 GREEN simultaneously with the new `0020` in the local Docker re-run (`cargo test -p differential -- --include-ignored` → **129 passed / 0 failed / 0 ignored across 22 result lines**) + CI run `26377609763`. **20/20 in one run** bilaterally vs `envoyproxy/envoy:v1.33.0`. The load-bearing 13.1 regression-equivalence proof; the §5.4 inert-when-unconfigured invariant holds (only 0020 configures `circuit_breakers`). |
| **(c)** h2spec ≥95% | **PASS (parent-05 baseline 99.31%)** | 13.1's only envoy-http2 touch is the Task 4 `HCMConfig::from_config` type-system-only threading of `pool_mgr: Option<Arc<H1PoolManager>>` — no codec change, no frame handling change. h2spec baseline 99.31% structurally preserved. CI re-attestation in run `26377609763` + `26395285596` (both `completed / success`). |
| **(d)** parse_bootstrap fuzz clean for short-budget CI run | **PASS** | `cargo +nightly fuzz run parse_bootstrap -- -runs=200000` at the Task 10 state-4 → `Done 200000 runs in 16 second(s)`, 0 crashes (cov **13636** / ft **37080** vs 12.2 baseline 13414/36698 — +222 cov / +382 ft from the new Task 9 seed). `fuzz_corpus_seeds_parse_or_reject_cleanly` SUCCESS array now 21 entries; re-verified at this state-5 session via `cargo test -p envoy-config --lib fuzz_corpus_seeds_parse_or_reject_cleanly` → 1 passed. CI `fuzz (parse_bootstrap)` job conclusion `success` in CI run `26377609763`. |
| **(e)** Stable-toolchain gates clean (fmt / clippy / build / test / deny) | **PASS** | All 5 within CI run `26377609763` clean (state-3 arc + state-4 verification) + reproduced LOCALLY at this state-5 session post-fold-in: fmt clean; clippy `-D warnings` clean; build clean (10.92s warm); test **867 passed / 0 failed / 2 ignored** across 74 result lines (+2 over the state-4 baseline 865 = the 2 new A-I1 + A-I2 regression tests in the state-5 fold-in); cargo deny `advisories ok, bans ok, licenses ok, sources ok`. The pre-state-4 baseline at HEAD `592d9e7` was 865/0/2; the state-5 fold-in at HEAD `52f43f7` adds exactly the 2 new TDD regression tests. |
| **(f)** REVIEW.md approved | **CLOSE at this REVIEW.md** | Verdict per §1: **Approved with M-track follow-ups** (zero Critical; zero Important — 2 Importants A-I1 + A-I2 CLOSED in-phase at `52f43f7`; 1 Important-deferred A-I3 carried to 13.2 per reviewer recommendation; 12 active Minor carryforwards [A-M1 through A-M5, B-M1 through B-M3, C-M1 through C-M4] — all awareness-only / close-opportunistically; none has a hard-gated owner; none gates the state-6 close-out). Gate (f) closes at this verdict. |

**All six §7.5 gates: (a) PASS / (b) PASS / (c) PASS / (d) PASS / (e) PASS / (f) CLOSE-at-this-REVIEW.md.** Phase 13.1's state-4 evidence is structurally complete; the state-5 fold-in `52f43f7` adds 2 new TDD regression tests + closes the latent A-I1/A-I2 landmines + structurally precludes the access-log flake; the fold-in's CI re-attestation at run `26395285596` settled `completed / success` in 2m19s; state-5 closes at this REVIEW.md commit; state-6 (the phase-13.1 close-out — flips ROADMAP row `13.1` `in-progress → done` ALONE per the non-closing-sub-phase invariant + advances STATE.md to `13.2` state-2-next) is a separate session per `BOOTSTRAP_PROMPT.md` §5.1.

---

*End of REVIEW.md. Phase 13.1 state-5 complete. Verdict: **Approved with M-track follow-ups** (zero Critical; zero Important — 2 surfaced Importants A-I1 + A-I2 CLOSED in-phase at state-5 fold-in `52f43f7`; 1 Important-deferred A-I3 carried to 13.2 per reviewer recommendation; 12 Minor surfaced — all active carryforwards). Per `BOOTSTRAP_PROMPT.md` §5.2, the phase RE-ENTERED state 3 at the state-5 fold-in to close A-I1 + A-I2 + the access-log flake mitigation, then re-attested the gates (5 stable-toolchain gates clean post-fold-in + workspace test count 867/0/2 = +2 over the state-4 baseline 865 from the 2 new TDD regression tests + CI re-attestation at HEAD `52f43f7`). Phase 13.1 is the **third Upstream-robustness-family REVIEW.md** (after 12.1 + 12.2) and the **first non-closing sub-phase** of the parent-13 split — the H1 connection-pool primitive (the project's THIRD periodic-background primitive after 12.2's active-HC scheduler — the H1 pool's idle sweeper, cleanly cancellable via `tokio_util::sync::CancellationToken`) + the envoy-config `Cluster.circuit_breakers` schema + validator + the H1 router-proxy-arm pool integration with the local `StreamHandle::{Pooled,OneShot}` dichotomy + tier-1 cache removal + `cx_total` migration into pool's connect-on-miss + the configurable-status backend `--per-path` extension on the 12.2-landed `health-aware-http1-backend` helper (test-helper-only) + atomic landing of fixture `0020-upstream-connection-pooling-and-per-class-counters` + `Driver::Http1KeepAlive` harness variant + Docker wrapper (THE 06.3 REVIEW I2 (a) full-closure site) + the in-process H1 backstop covering pool reuse + per-class counter math + the 5-standard-header presence assertion on every non-2xx response per 10 REVIEW M1 + the `parse_bootstrap` corpus seed extension 20 → 21 + the BEHAVIOR_CONTRACT.md `13.1 entries` block with 2 new value-exact rows (`upstream_cx_destroy` + `upstream_cx_http1_total`) + explicit non-tightening of the existing `upstream_cx_total` row per lock-in #3. 13.1 ENGAGED carryforward closure: **06.3 REVIEW I2 (a)** (FULLY CLOSED at Task 7). 13.1 DEFERS: **06.3 REVIEW I2 (b)** (to 13.2 D7.1). NO ADR landed in the 13.1 lifecycle (PLAN lock-in #16); the DECISIONS.md ledger head is **ADR-0038** (count 39); next available **ADR-0039**. The next session enters the **phase-13.1 state-6 close-out** — flips ROADMAP row `13.1` `in-progress → done` ALONE (per the non-closing-sub-phase invariant; mirrors the 12.1 → 12.2 cadence within parent-12); advances STATE.md to `13.2` state-2-next; lands the state-6 close-out commit per the 13.1 SPEC §8 commit-message format `phase 13.1: H1 connection pool + circuit_breakers schema + H1 router pool integration + fixture 0020 + per-class counter bilateral coverage (06.3 REVIEW I2 (a) closed)`. With 13.1's close, the Upstream-robustness family has its FIRST connection-pooling primitive + the FIRST 20-fixture Docker-gated bilaterally-green baseline + the foundation reusable PoolGuard / external-registry pattern that 13.2's H2 pool will mirror.*
