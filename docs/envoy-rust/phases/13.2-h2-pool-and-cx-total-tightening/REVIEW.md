# Phase 13.2 (`13.2-h2-pool-and-cx-total-tightening`) — REVIEW

- **Phase id:** `13.2`
- **Slug:** `13.2-h2-pool-and-cx-total-tightening`
- **Parent:** `13` (`13-connection-pooling`). **Second + closing sub-phase** of the parent-13 split (parent-13 state-2 split commit `1b1c8f5`; ADR-0038). Per the closing-sub-phase invariant established by the 02.2 / 03.2 / 07.2 / 08.2 / 12.2 precedents, 13.2's state-6 close-out commit (Task 8) flips ROADMAP rows `13.2` AND parent-13 `13` `in-progress → done` SIMULTANEOUSLY in ONE commit. With this `Approved with M-track follow-ups` verdict, the next session's state-6 commit advances STATE.md to `awaiting next planning`. 13.2 lands the **H2 connection-pool primitive + the H2 router-proxy-arm pool integration + the `cluster.<name>.upstream_cx_total` BEHAVIOR_CONTRACT row tightening to `value-exact` + the new H2-specific pool stat row + the H2-surface fixture 0021 + the in-process H2 backstop + 4 carryforward closures (06.3 REVIEW I2 (b) full closure + 13.1 REVIEW Cluster A-I3 deferred-Important + A-M1/A-M2/A-M4 Minors)** — the second half + the contract-surface deliverable of phase-13 connection pooling.
- **Reviewed range:** `8c7d8a2..76e4b82` — **12 commits total** (7 task commits + 4 fold-in commits + 1 state-3-partial closure docs commit). The state-2 base `8c7d8a2` is the standalone-PLAN-write commit (8 tasks; 17 architecture lock-ins; §6.2 inheritance from parent-13 LOCKED — NOT re-run; NO inline ADR at state-2); the HEAD `76e4b82` is the state-4 phase-done verification commit. Diffstat ≈ `+3300 / -650` lines across the production crates `envoy-http2` + `envoy-http1` + `envoy-cluster` (1 accessor pair) + `envoy-bin` (wiring + new backstop) + the test infrastructure (fixture 0021 + harness driver + Docker wrapper + docs).
  - `f692b53` — **Task 1**: D5 `crates/envoy-http2/src/pool.rs` (~856 LoC) — `H2Pool` + `H2PoolEntry` + `H2PoolGuard` (RAII) + `H2PoolManager` + `PoolError` + idle sweeper + 9 unit tests; joint H1+H2 sync-`parking_lot::Mutex` switch (the **13.1 REVIEW A-I3 deferred-Important closure**); A-M1 (sweepers rename + `pub async fn shutdown(self)` on both managers) + A-M2 (`Arc::ptr_eq` debug-assert at the gauge wiring site) + A-M4 (improved `.expect` message naming the single-bootstrap-per-process invariant); new `pub fn cx_active_arc(&self) -> &Arc<envoy_stats::Gauge>` accessor pair on Cluster/ClusterHandle (additive envoy-cluster surface). 9 files +1147/-94 LoC net. `parking_lot v0.12.5` + 4 chain deps land as new sub-crate deps (PROGRESS Deviation #3 — honestly named; workspace root Cargo.toml unchanged, preserving lock-in #14's no-new-top-level-dep invariant). Spec ✅.
  - `ae8d7cf` — **Task 1 fold-in (code-quality CRITICAL closure)**: H2 `H2PoolGuard::Drop` invalidate-path TOCTOU race close. Pre-fix the `active_streams.fetch_sub(1)` ran OUTSIDE the `connections` lock, opening a window where a concurrent Phase-1 walker could CAS-claim a slot on the to-be-evicted entry. Post-fix the invalidate branch takes `connections.lock()` BEFORE the fetch_sub and holds it across both the decrement and the `retain()`. Non-invalidate branch keeps the lock-free fetch_sub (correctness argument inline). Adds H1 race-regression stress test `pool_acquire_after_concurrent_release_1000_iterations_zero_spurious_overflows` at 1000 iter (~90ms; probabilistically race-sensitive). 3 files +144/-16 LoC net. Spec ✅.
  - `07006b4` — **Task 2**: D6 H2 router-arm pool integration. Replaces `pub type HCMConfig = Http1HCMConfig;` at `crates/envoy-http2/src/hcm.rs:27` with proper `pub struct HCMConfig { pub inner: Arc<Http1HCMConfig>, pub h2_pool_mgr: Option<Arc<H2PoolManager>> }` + `wrap` constructor (lock-in #2). Migrates H2 dispatch arm at `hcm.rs:286-296` from per-call `Client::connect` to `config.h2_pool_mgr.get(&cluster).acquire().await`. H1-cluster-in-H2-HCM arm at `:273-284` STAYS UNTOUCHED per lock-in #7 (unusual cross-protocol path). Outer `_cx_guard` relocates to conditional `Option<ConnGaugeGuard>` Some-only on H1 arm (lock-in #8 — mirrors 13.1 Task 4 code-quality fold-in). envoy-bin wires `H2PoolManager::for_bootstrap`. 1 new integration test `h2_hcm_pool_reuses_upstream_conn_across_sequential_requests`. 3 files +378/-34 LoC net. Spec ✅.
  - `ef6deda` — **Task 2 fold-in (code-quality IMPORTANT closure)**: corrects misleading cx_active fallthrough comment block at the `_cx_guard` relocation site (the original falsely claimed H1 OneShot arm doesn't fire cx_active + falsely claimed pre-Task-2 H2 arm didn't hold a guard — both halves wrong). Comment + PROGRESS narrative rewritten honestly. 2 files ~30 LoC net. Spec ✅.
  - `ab0e62f` — **Task 3** (controller-direct; docs-only): D7.2 deliverable. Appends `**13.2 entries (H2 connection pool):**` subsection to `docs/envoy-rust/BEHAVIOR_CONTRACT.md` with 1 new row `cluster.<name>.upstream_cx_http2_total` (disposition `value-exact`). Counter registration already at Task 1's `H2PoolManager::for_bootstrap`. 2 files +34/-1 LoC. Spec ✅.
  - `4ab2c61` — **Task 4** (controller-direct; docs-only): D7.1 deliverable — **the named 06.3 REVIEW I2 (b) FULL-CLOSURE site**. Tightens the existing `cluster.<name>.upstream_cx_total` row at `BEHAVIOR_CONTRACT.md:89` from `name-required, value-may-differ` to **`value-exact` (H1+H2 clusters under harness's single-downstream-keep-alive-conn driver); name-required, value-may-differ (TCP-proxy clusters carved out per parent-13 SPEC §4)**. Rationale documents increment-site relocation, TCP-proxy carve-out, conditional-on-driver nuance, and the closure attribution. 2 files +32/-2 LoC. Spec ✅.
  - `e2b8d1b` — **state-3-partial closure (ADR-0039 docs-only)**: lands **ADR-0039** ratifying the fixture-0021 topology pivot (H1-listener × H2-cluster → H2-listener × H2-cluster + new `Driver::Http2KeepAlive`) after the Task 5 first-attempt implementer surfaced an architectural blocker — envoy-rust's 06.3 D14.3 parse-time validator gate at `crates/envoy-config/src/bootstrap.rs:1997-2016` rejects the PLAN's prescribed topology per ADR-0028 deferral. ADR-0028 explicitly REMAINS OPEN at 13.2 close per ADR-0039 Consequences. 3 files +121/-2 LoC. Spec ✅.
  - `1ade3ef` — **Task 5 (reshaped per ADR-0039; subagent-driven)**: D9.1-H2 deliverable. New fixture `tests/fixtures/0021-upstream-h2-connection-pooling/` (3 YAMLs) with downstream HCM `codec_type: HTTP2` + H2-upstream cluster + `typed_extension_protocol_options.http2_protocol_options` + `circuit_breakers.thresholds[0].max_connections: 4` + STRICT_DNS + `dns_lookup_family: V4_ONLY`. New `Driver::Http2KeepAlive` enum variant + `drive_http2_keep_alive` helper (~150-200 LoC) at `tests/differential/src/lib.rs` mirroring `Driver::Http1KeepAlive`'s shape verbatim modulo the codec layer. Reuses existing `Http2EchoBackend` via the established `needs_http2_backend` template-marker block at `lib.rs:~1998` (the 05.3 D6.b precedent; no new discriminator arm needed — a positive simplification vs. the first-attempt). Docker wrapper at `tests/differential/tests/upstream_h2_connection_pooling.rs`. Bilateral fixture green: local 3.07s + CI 2m37s. 6 files ~573 net LoC including ~298 in lib.rs. Spec ✅.
  - `f7cd908` — **Task 5 fold-in (code-quality IMPORTANT closure)**: corrects misleading H2 teardown comment in `drive_http2_keep_alive` (the original claimed `drop(send_request)` triggers clean GOAWAY shutdown — empirically false because `conn_handle.abort()` synchronously preempts the Connection future). Rewritten to honestly state the abort is the load-bearing teardown step; matches sibling `drive_http2`'s honest framing verbatim modulo phrasing. 2 files +~8 LoC net. Spec ✅.
  - `84b0a4d` — **Task 6 (subagent-driven)**: D9.3-H2 deliverable. New in-process H2 backstop at `crates/envoy-bin/tests/upstream_h2_connection_pooling.rs` (~363 LoC). Synthesized bootstrap with H2 downstream listener + H2-upstream cluster (also pivoted to H2 downstream per ADR-0039; same parse-time gate applies to backstops). Spawns `http2-echo-server` helper via subprocess discipline per 09 REVIEW M3 (`tokio::process::Command + kill_on_drop(true) + Stdio::null/piped + 30s backend / 10s envoy-bin readiness budgets`). 5 sequential GET / over one downstream H2 conn; asserts 5 stats matching fixture 0021. 5-standard-header H1-specific check OMITTED per RFC 7540 §8.1.2.2 (forbids `connection` in H2); rationale documented inline. Spec ✅.
  - `2fef8ad` — **Task 6 fold-in (2 code-quality IMPORTANT closures)**: (1) appended discriminating-power caveat to the backstop's module-level docstring at `upstream_h2_connection_pooling.rs:48-58` (cold-readable per D-3.4 — the caveat now lives at the in-code surface, not just PROGRESS); (2) rewrote the `wait_ready` doc comment at `:85-101` with the empirically-correct 5-observation backend-ready justification (the spec-supplied "pool retries on handshake fail" rationale was empirically false — `crates/envoy-http2/src/pool.rs::acquire` is one-shot; the prior subagent's BLOCKED escalation correctly surfaced this; the controller's re-plan picked option (1) — rewrite with the verified 5-observation rationale citing helper accept-loop shape + SOCK_STREAM backlog + h2 client preface 10ms detection window + empirical ~2s pass + `wait_h2_accept_ready` precedent at `tests/differential/src/backend.rs:424-447`). 2 files touched.
  - `76e4b82` — **Task 7** (controller-direct; docs-only state-4 verification): quotes §7.5 (a)-(e) gate evidence. 2 files +132/-101 LoC: MODIFY PROGRESS.md (Task 7 subsection) + MODIFY STATE.md (4-top-pointer rewrite + new Notes subsection + Last-commit/Last-updated chained entries). STATE.md advances to state-4-complete / state-5-next.

- **CI evidence cited:** PROGRESS Task 7 + this REVIEW cite TWO CI run anchors, independently verified by Cluster C via `gh run view`:
  - **`26414774250`** — HEAD `2fef8ad` (Task 6 fold-in; the gate-(b) "21 Docker-gated fixtures green simultaneously" anchor): `completed / success` 2m37s on 2026-05-25 (18:37:29Z → 18:40:06Z). All 21 fixture wrappers green bilaterally vs `envoyproxy/envoy:v1.33.0`; gate-(c) h2spec ≥95% held at parent-05 baseline 99.31% (h2spec v2.6.0 installed at `.github/workflows/ci.yml:43-49`).
  - **`26416786136`** — HEAD `76e4b82` (Task 7 state-4 verification; the published-HEAD anchor): `completed / success` 2m53s on the rerun (initial run failed on a pre-existing `envoy-accesslog::file_sink::tests::file_sink_writes_one_record` flake — same family as the differential variant documented at 13.1 state-5; rerun green at 19:43:27Z).
  
  Local re-runs at this state-5 session confirm: backstop runs green in 2.06s; `gh run view 26416786136` returns `conclusion: success`, `headSha: 76e4b823e1b04ed23aa93984bae4cd1211792aa8`. Workspace test count post-state-4 **878 passed / 2 transient-flake / 1 ignored across 74 result lines** under `--test-threads=1` (the 2 transient-flake are documented pre-existing kernel-port-contention patterns at `subject::tests::starts_and_shuts_down_envoy_rust` + `tests::drive_admin_scrape_round_trips_against_envoy_bin_admin`; pass cleanly in isolation; CI does NOT see them with default parallelism). `parse_bootstrap` fuzz clean — cov **13745** / ft **37892** in 200000 runs (Δ vs 13.1 state-4 baseline = +109 cov / +812 ft, matching the new H2 pool + HCMConfig surface unlocked through the existing 21-seed corpus; no new corpus seed at 13.2 per PLAN lock-in #16).
- **Reviewed:** 2026-05-25.

## §1. Verdict

**Approved with M-track follow-ups.**

Phase 13.2 lands its 5 carved parent-13 deliverables (D5 the headline `H2Pool` primitive + `H2PoolEntry` (multiplex-aware) + `H2PoolGuard` RAII + `H2PoolManager` as the external registry [sibling to `ClusterManager`, NOT a field on `Cluster` — mirrors the 12.2 `envoy-health::Scheduler` + 13.1 `H1PoolManager` precedents verbatim] + idle sweeper as the third periodic-background primitive + cleanly-cancellable shutdown via `tokio_util::sync::CancellationToken` + `pub async fn shutdown(self)` on both H1+H2 managers; D6 the H2 router-proxy-arm pool integration with the new `envoy_http2::HCMConfig` wrapper struct [replacing the type-alias at `hcm.rs:27`; carries `Arc<envoy_http1::HCMConfig>` inner + `Option<Arc<H2PoolManager>>` — cycle-free, no inversion of the envoy-http2 → envoy-http1 dep direction] + H2-arm-only migration [the H1-cluster-in-H2-HCM cross-protocol arm at `:273-284` left untouched per lock-in #7] + outer `_cx_guard` relocation to conditional Some-only on H1 arm [mirrors 13.1 Task 4 code-quality fold-in verbatim] + envoy-bin wiring + 1 TDD integration test; D7.1 the `cluster.<name>.upstream_cx_total` BEHAVIOR_CONTRACT row tightening to `value-exact (H1+H2)` + explicit TCP-proxy carve-out — **the named 06.3 REVIEW I2 (b) FULL-CLOSURE site, combined with 13.1's I2 (a) closure to fully close the 7-phase-old 06.3 REVIEW I2 carryforward at parent-13 close**; D7.2 the new `cluster.<name>.upstream_cx_http2_total` BEHAVIOR_CONTRACT row [value-exact; sibling of 13.1's `upstream_cx_http1_total`; together they enumerate the per-protocol breakdown of `upstream_cx_total`]; D9.1-H2 the new fixture `0021-upstream-h2-connection-pooling` [reshaped per ADR-0039 from H1-downstream × H2-upstream to H2-downstream × H2-upstream after the Task 5 first-attempt surfaced the 06.3 D14.3 parse-time gate; preserves the discriminating-observable bilateral validation at `cluster.backend_cluster.upstream_cx_total: 1` + `upstream_cx_http2_total: 1` under 5 sequential single-stream requests over one downstream H2 conn] + the new `Driver::Http2KeepAlive` harness driver + `drive_http2_keep_alive` helper + reuses existing `Http2EchoBackend` via the established `needs_http2_backend` template-marker block; D9.3-H2 the in-process H2 backstop covering pool reuse end-to-end [mirrors the 13.1 H1 backstop shape verbatim modulo H2-specific substitutions per the 9-item substitution checklist; 5-standard-header H1-specific check honestly OMITTED per RFC 7540 §8.1.2.2]) substantively and cleanly through the existing envoy-cluster + envoy-config + envoy-http1 + envoy-http2 + envoy-stats + tokio + tokio-util foundations + the new `H2Pool` / `H2PoolManager` primitives inside `envoy-http2`.

The cross-phase architectural invariants from the 13.2 SPEC §5 all hold: the new pool primitive lives entirely INSIDE `envoy-http2` (which already depended on `envoy-cluster` per the 05.3 precedent — no new dep direction); `H2PoolManager` is an external registry held by `envoy-bin`, not a field on `Cluster` — **no cycle introduced**, verified by `cargo build --workspace`; the pool primitive + idle sweeper are hand-rolled per D-3.2 atop std + tokio + tokio-util + bytes + parking_lot + envoy-stats + the existing envoy-http2 internal types (including `h2::client::SendRequest` direct access via the `pub(crate)` widening + `#[derive(Clone)]` at `client.rs:75`) — **no new top-level Cargo dep** (`parking_lot` family lands as sub-crate-only at `crates/envoy-http1/Cargo.toml` + `crates/envoy-http2/Cargo.toml`; workspace root Cargo.toml unchanged; PROGRESS Task 1 Deviation #3 explicitly + honestly surfaces that `parking_lot` was NOT pre-existing as a tokio transitive at this workspace's resolved feature set — the 5 new lockfile entries `parking_lot v0.12.5` + `parking_lot_core v0.9.12` + `lock_api v0.4.14` + `scopeguard v1.2.0` + `redox_syscall v0.5.18` are deps-on-existing-foundations only, the no-new-top-level-dep invariant is preserved per lock-in #14, and the 04.1 REVIEW M5/M9 Cargo.lock cadence ratification accommodates); no `unsafe` introduced (`#![forbid(unsafe_code)]` continues to bind at every touched crate root per D-3.8 — verified at `envoy-http2/src/lib.rs:1`, `envoy-http1/src/lib.rs:1`, `envoy-cluster/src/lib.rs:1`); the h2spec ≥95% gate held at parent-05 baseline 99.31% (Task 2 touches the H2 upstream-client surface via `HCMConfig::wrap`, not the H2 downstream framer/codec — h2spec runs against the H2 listener); the §5.4 inert-when-unconfigured invariant holds (only fixture 0021 + the new backstop configure `circuit_breakers`; the H2 cluster fixture 0010 sees its per-call `upstream_cx_total` semantics now bilaterally pooled, but its assertion was presence-only so no regression); the §5.6 `ConnGaugeGuard` RAII pattern is correctly REUSED (each H2 PoolGuard owns one ConnGaugeGuard via the existing `from_gauge` constructor from 13.1; the outer `_cx_guard` is now Some-only on H1 arm, eliminating any double-count regression on the H2 production path).

**Exactly one ADR landed in the 13.2 lifecycle: ADR-0039** (the topology-pivot ADR at `e2b8d1b`; the only inline ADR in 13.2). ADR-0039 is doctrinally exemplary: the 3-option enumeration is complete; option (a) (closing ADR-0028 by extracting `envoy-http2::Client` to a new `envoy-http-client` crate) is rejected with explicit scope reasoning (~300-400 LoC + new architectural surface; fragments 13.2's atomic closing-sub-phase scope); option (c) (defer fixture 0021 entirely) is rejected because it materially weakens the I2 (b) closure attribution at Task 4; option (b) (the picked option — H2-downstream + H2-upstream + new `Driver::Http2KeepAlive`) is the minimum scope expansion preserving the discriminating-observable bilateral validation. The Consequences explicitly record that **ADR-0028 (H1-listener × H2-cluster dispatch deferral) REMAINS OPEN at 13.2 close** and names the closure path for a follow-up phase. The PLAN-time miss is owned honestly without rewriting the immutable PLAN (per the project's append-only doctrine). DECISIONS.md ledger head advances to **ADR-0039** (project-cumulative count 40); next available **ADR-0040**.

The state-5 review dispatched a **3-cluster subagent pass** over the reviewed range `8c7d8a2..76e4b82` (12 commits total: 7 task commits + 4 fold-in commits + 1 state-3-partial closure docs commit), sliced by deliverable cohesion matching the diffstat: **Cluster A (architectural-headline)** = Tasks 1 + 1-fold-in + 2 + 2-fold-in — H2 pool primitive + manager + HCMConfig wrapper + joint H1+H2 sync-Mutex switch + the H2 invalidate-Drop TOCTOU race close + cx_active fallthrough comment correction; **Cluster B (docs + carryforward closure)** = Tasks 3 + 4 + state-3-partial closure (ADR-0039) + 5 + 5-fold-in — the new `upstream_cx_http2_total` row + the `upstream_cx_total` value-exact tightening + ADR-0039 topology pivot + fixture 0021 + `Driver::Http2KeepAlive` + Http2EchoBackend wiring + H2 teardown honesty correction; **Cluster C (backstop + state-4)** = Tasks 6 + 6-fold-in + 7 — in-process H2 backstop + discriminating-power caveat + backend-ready justification + state-4 verification. Each cluster reviewer read the actual on-disk code (`git show` + `Read` + `Grep`) per the standing precedent-verification discipline, not the PROGRESS narrative.

Cluster A returned `Ready to merge: Yes` with **0 Critical + 0 Important + 5 Minor (A-M1 through A-M5)**; Cluster A explicitly verified the H2 invalidate-Drop TOCTOU race fix is structurally correct (the lock-ordering convention `connections → established` preserved; no deadlock risk; the comment block's "see the analysis below" cross-reference is mildly imprecise but the property genuinely holds), the joint H1+H2 sync-`parking_lot::Mutex` switch is complete (1000-iter H1 stress test produces 0 spurious Overflows; pre-fix shape would have detectably surfaced overflows at this iteration count per the implementer's revert-and-verify experiment), the HCMConfig wrapper is cycle-free (Cargo.lock confirms `envoy-http1` deps unchanged; no envoy-http2 reverse-dep), and no `parking_lot::Mutex` is held across `.await` anywhere in pool.rs (verified by tracing every lock-scope vs await boundary). Cluster B returned `Ready to merge: Yes` with **0 Critical + 2 Important + 4 Minor (B-M1 through B-M4)**; on calibration the 2 Importants are awareness-level (one is a forward-proofing-cap-not-exercised-by-current-workload nit, the other is an untested-edge-case for backend mid-stream disconnect) — both calibrated to Minor per the 13.1 precedent's M-track discipline (no in-phase fix warranted; both bubble correctly via existing `with_context` patterns if exercised). Cluster B explicitly verified that ADR-0039 is well-grounded against the actual 06.3 D14.3 gate at `crates/envoy-config/src/bootstrap.rs:1997-2016`, the BEHAVIOR_CONTRACT row tightening at Task 4 honors D-3.3 (the row TIGHTENS unambiguously for H1+H2 without loosening anything), and fixture 0021's envoy.yaml + envoy-rust.yaml are byte-equivalent modulo the standard bind-address + node-id + `generate_request_id` asymmetries (the 04.x precedent shape). Cluster C returned `Ready to merge: Yes` with **0 Critical + 0 Important + 4 Minor (C-M1 through C-M4)**; Cluster C explicitly verified both CI run anchors (`26414774250` for HEAD `2fef8ad` + `26416786136` for HEAD `76e4b82` — both `completed/success`), the H2 backstop runs green locally in 2.06s, the H1↔H2 backstop shape mirror is rigorous (the 9-item substitution checklist maps 1:1 to the code), and the Task 6 fold-in's 5-observation `wait_ready` justification is empirically correct (all 5 observations verified by direct read).

**Post-cluster-aggregation, no Critical or Important findings remain after calibration.** The 13 active Minor carryforwards are all awareness-only / close-opportunistically per the standing post-MVP-trunk discipline; full inventory in §4 below.

**Phase 13.2 is the THIRD Upstream-robustness-family REVIEW.md** (after 12.1 + 12.2 + 13.1) — the verdict + REVIEW shape continue the 06.x → 07.x → 08.x → 09 → 10 → 11 → 12.1 → 12.2 → 13.1 post-MVP standard: `Approved with M-track follow-ups` disposition; forward-carry inheritance for unexercised-pattern Minor findings; explicit calibration rationale per finding; full §7.5 6-gate re-attestation against the published-HEAD CI anchor. **13.2 ENGAGES** the named carryforward **06.3 REVIEW I2 (b) — FULLY CLOSED** at Task 4 (the BEHAVIOR_CONTRACT row tightening). **Combined with 13.1's I2 (a) closure (fixture 0020), the FULL 06.3 REVIEW I2 carryforward CLOSES at the parent-13 close** (Task 8 re-attributes at the closing-sub-phase commit). **13.2 also CLOSES** the **13.1 REVIEW Cluster A-I3 deferred-Important** (the spurious-overflow race; closed jointly across H1+H2 at Task 1 + Task 1 fold-in) + **13.1 REVIEW Cluster A-M1 + A-M2 + A-M4** (3 Cluster A Minors closed opportunistically at Task 1). **13.2 also LANDS exactly 1 new ADR (ADR-0039)** and **CARRIES FORWARD ADR-0028** (the H1-listener × H2-cluster dispatch deferral; per ADR-0039 Consequences; closure path documented — extract `envoy-http2::Client` to a new crate; ~300-400 LoC; out of 13.2 scope).

## §2. Strengths

1. **The H2 invalidate-Drop TOCTOU race fix at `ae8d7cf` is structurally correct.** Cluster A verified: the invalidate branch takes `connections.lock()` BEFORE `active_streams.fetch_sub(1)` and holds the lock across both the decrement and the `retain()` — serializing the decrement + eviction under the same lock that Phase-1 walkers in `acquire()` take means no walker can claim a slot on an entry between the moment its active_streams hits 0 and the moment it's evicted. The lock-ordering convention (`connections → established`) is preserved throughout the pool + sweeper, so no deadlock risk. The non-invalidate branch keeps the lock-free `fetch_sub` (correctness argument inline at the Drop body comment: the sweeper's `active_streams != 0` early-return + `try_claim_stream_slot`'s `last_idle = None` write provide structural safety). The CRITICAL finding from the prior code-quality reviewer is fully closed.

2. **The joint H1+H2 sync-`parking_lot::Mutex` switch (13.1 REVIEW A-I3 closure) is genuinely complete.** Cluster A verified: the H1 pool's `Handle::try_current()` guard from the 13.1 state-5 fold-in is structurally removed (Drop is now sync — the scenario the guard protected against is structurally unreachable); the 1000-iter `pool_acquire_after_concurrent_release_1000_iterations_zero_spurious_overflows` stress test in `crates/envoy-http1/src/pool.rs:702-729` is genuinely race-sensitive (the pre-fix `tokio::spawn`-from-Drop's deferred established-decrement would have detectably surfaced overflows at this iteration count; post-fix produces 0 spurious overflows in ~90ms). PROGRESS Task 1 Deviation #2's analysis of why the H1 32-iter test wasn't race-sensitive is technically rigorous and honestly disclosed. **No `parking_lot::Mutex` held across `.await` anywhere in pool.rs** (verified by tracing every `.lock()` → `.await` proximity; `Client::connect(...).await` at `pool.rs:316` sits cleanly between two scoped lock releases).

3. **The HCMConfig wrapper struct at `crates/envoy-http2/src/hcm.rs:27-53` is cycle-free + clean.** Cluster A confirmed via Cargo.lock + `cargo build --workspace`: `envoy-http2 → envoy-http1` dep direction preserved (envoy-http1 does NOT depend on envoy-http2 — no back-edge); the inner `Arc<Http1HCMConfig>` pattern is non-intrusive (all 8+ access sites cleanly read `config.inner.<field>`); the `wrap()` constructor's `Arc::clone` semantics are correct; lock-in #7 (the H1-cluster-in-H2-HCM cross-protocol arm at `hcm.rs:325-337` stays untouched) is genuinely preserved. The wrapper-struct pattern (vs. an `H2DispatchPort` trait on envoy-http1 + dyn-trait indirection) is the cleanest cycle-free addition.

4. **ADR-0039 is doctrinally exemplary.** Cluster B verified at `docs/envoy-rust/DECISIONS.md:816-836`: the 3-option enumeration is complete; option (a) is rejected with explicit scope reasoning (not premature dismissal — closing ADR-0028 genuinely requires ~300-400 LoC + a new crate + a foundations grant); the rationale names the discriminating-observable preservation explicitly; the Consequences explicitly record that ADR-0028 stays OPEN at 13.2 close. The PLAN-time miss is owned honestly without rewriting the immutable PLAN (per the project's append-only doctrine — PROGRESS Task 5 first-attempt + ADR-0039 together carry the corrected narrative).

5. **The Task 4 BEHAVIOR_CONTRACT row tightening (06.3 REVIEW I2 (b) FULL-CLOSURE) is precise per D-3.3.** Cluster B verified at `docs/envoy-rust/BEHAVIOR_CONTRACT.md:89`: the TCP-proxy carve-out is explicit (TCP fixtures `0001/0003/0004/0005/0006` keep their pre-13.2 disposition because their assertions are presence-only); the conditional-on-driver nuance (the value-exact disposition requires N requests over a single downstream keep-alive conn per parent-13 SPEC §6.2 item-iv) is stated explicitly; the increment-site source-of-truth (H1 pool's `acquire()` connect-on-miss + H2 pool's `acquire()` connect-on-miss) is named per protocol; the 06.3 REVIEW I2 (b) closure attribution is explicit. The row TIGHTENS unambiguously for H1+H2 and preserves the pre-13.2 disposition for TCP — no D-3.3 doctrinal loosening.

6. **Fixture 0021's envoy.yaml + envoy-rust.yaml are byte-equivalent modulo the standard asymmetries.** Cluster B verified by direct diff: asymmetries are limited to (i) header-comment differences, (ii) bind address `0.0.0.0` vs `127.0.0.1` (the host-subprocess precedent — fixtures 0019/0020), (iii) `node.id` (`phase-13-envoy` vs `phase-13-envoy-rust`), (iv) `generate_request_id: false` present on envoy side, omitted on envoy-rust side per the fixture-0019/0020 precedent. The `circuit_breakers` block, `typed_extension_protocol_options.http2_protocol_options`, route_config, and STRICT_DNS configuration are byte-identical. **No non-trivial topological asymmetry.**

7. **The H1↔H2 backstop shape mirror is rigorous.** Cluster C verified: the 9-item substitution checklist in PROGRESS Task 6 maps 1:1 to the code — same `reserve_port` / `wait_ready` / `scrape_admin_stats` / `assert_stat` / `spawn_backend` / `spawn_envoy_bin` helper shapes; same subprocess discipline (`kill_on_drop(true) + Stdio::null/piped + 30s backend / 10s envoy-bin readiness budgets`); same `max_connections: 4` circuit-breaker; same admin scrape pattern. The only H2-specific divergences (helper, codec_type, `typed_extension_protocol_options`, driver shape, 5-stat vs 9-stat assertion block, 500ms vs 200ms settle, omitted 5-standard-header check) are exactly what the substitution checklist names.

8. **The Task 6 fold-in's rewritten `wait_ready` justification is empirically correct.** Cluster C verified all 5 observations by direct read: (1) helper accept loop is poll-immediate after `TcpListener::bind` (verified at `tests/helpers/http2-echo-server/src/main.rs:99-122`); (2) standard SOCK_STREAM listen-backlog semantics; (3) h2 client preface uses ~10ms detection window (verified at `crates/envoy-http2/src/client.rs:42-56`); (4) empirical ~2s pass without flake (verified by re-run at this review); (5) `wait_h2_accept_ready` stronger guarantee exists at `tests/differential/src/backend.rs:424-447`. The spec-supplied "pool retries on handshake fail" rationale was empirically false (`pool.rs::acquire` is one-shot — verified); the prior subagent's BLOCKED escalation correctly surfaced this; the controller's re-plan picked option (1) — rewrite with the verified rationale per the project's append-only honesty discipline.

9. **The Task 6 fold-in's discriminating-power caveat at the in-code surface is the D-3.4 cold-readability exemplar.** Cluster C verified at `crates/envoy-bin/tests/upstream_h2_connection_pooling.rs:48-58`: a stranger reading the test file alone now sees the caveat that `upstream_cx_total: 1` + `upstream_cx_http2_total: 1` proves pool-reuse but does NOT independently discriminate stream-multiplex from a serial acquire+release cycle. The caveat names the bilateral evidence venue (fixture 0021 + the `drive_http2_keep_alive` driver), the backstop's role (in-process round-trip evidence), and the future-revisit hook (state-5 may surface a stronger in-process observable).

10. **CI evidence verified end-to-end via `gh run view`.** Cluster C independently confirmed both CI anchors: `26414774250` (gate-(b) anchor for HEAD `2fef8ad`) — `completed/success` 2m37s — and `26416786136` (state-4 HEAD anchor for HEAD `76e4b82`) — `completed/success` 2m53s on the rerun. h2spec install step verified at `.github/workflows/ci.yml:43-49` (h2spec v2.6.0 pinned). Gate-(c) delegation to CI is doctrinally honest. The fuzz delta `+109 cov / +812 ft` vs the 13.1 state-4 baseline is plausible (the new H2 pool primitive + HCMConfig wrapper unlock new reachable parser paths through the existing 21-seed corpus).

11. **The Cargo.lock cadence ratification + no new top-level Cargo deps.** Cluster A confirmed: `git diff Cargo.toml e64672b..76e4b82` is empty at the workspace root; the `parking_lot v0.12.5` family lands as sub-crate-only deps in `crates/envoy-http1/Cargo.toml` + `crates/envoy-http2/Cargo.toml`. PROGRESS Task 1 Deviation #3 surfaces the genuinely-new nature of `parking_lot` honestly. All touched crate roots keep `#![forbid(unsafe_code)]`; ENVOY_TARGET pin (`v1.33.0`) unchanged; `rust-toolchain.toml` untouched (D-3.7 + D-3.9 unaffected).

12. **Inert-when-unconfigured (the §5.4 architectural invariant) verified.** Only fixture 0021 + the H2 backstop configure `circuit_breakers` on H2 clusters (`grep circuit_breakers tests/fixtures/*/envoy.yaml` returns fixtures 0020 + 0021 only). The 19 pre-13.2 fixtures (`0001-tcp-echo` through `0019-upstream-active-health-check`) all carry NO `circuit_breakers` block → the H2 pool runs with hardcoded defaults (`max_connections: 1024`; `idle_timeout: 60s`) on those fixtures' clusters when applicable → existing presence-only `upstream_cx_total` assertions cannot regress on the H2 cluster fixture 0010 (which had no value-exact assertion). CI run `26414774250` confirms 21/21 fixtures green simultaneously.

## §3. Issues

### Critical (must fix before phase commit)

None.

### Important (close in 13.2 or carry forward with named owner phase)

None — Cluster B's 2 Importants calibrated to Minor on aggregation review per the M-track discipline; both are awareness-only without an actionable in-phase fix that wouldn't expand scope beyond the closing-sub-phase atomic surface.

**Closed-at-task-time during state-3 (not live carryforwards):**

- **Task 1 fold-in `ae8d7cf` (CRITICAL from prior code-quality review) — CLOSED.** H2 `H2PoolGuard::Drop` invalidate-path TOCTOU race close. See §2 Strength #1 for the structural-correctness argument.
- **Task 2 fold-in `ef6deda` (IMPORTANT) — CLOSED.** Misleading cx_active fallthrough comment corrected honestly per D-3.4.
- **Task 5 fold-in `f7cd908` (IMPORTANT) — CLOSED.** Misleading H2 teardown comment in `drive_http2_keep_alive` corrected to honestly state `conn_handle.abort()` is the load-bearing teardown step.
- **Task 6 fold-in `2fef8ad` (2 IMPORTANTs) — CLOSED.** (1) Discriminating-power caveat appended to the backstop's module docstring (D-3.4 cold-readability at the in-code surface); (2) `wait_ready` doc comment rewritten with the empirically-correct 5-observation backend-ready justification (the spec-supplied rationale was empirically false; the prior subagent's BLOCKED escalation correctly surfaced this; the controller's re-plan picked option (1) per `feedback_pick_recommendation`).

### Minor (awareness-only / track forward)

**13 Minor findings — 5 Cluster A + 4 Cluster B + 4 Cluster C. None gating; all awareness-only / close-opportunistically per the standing post-MVP-trunk discipline.**

**Cluster A Minors (Tasks 1 + 1-fold-in + 2 + 2-fold-in — H2 pool primitive + HCMConfig wrapper):**

**A-M1. TOCTOU comment cross-reference imprecise (`crates/envoy-http2/src/pool.rs:135-138`).** The invalidate-path Drop comment says "Walkers already inside the lock either already passed this entry (their CAS happened pre-evict; benign — see the analysis below) or hadn't reached it yet." The "see the analysis below" reference appears intended for the RETURN-TO-POOL path's stale-write analysis (lines 174-189), but that analysis is about a *different* race (lock-free `fetch_sub` in the non-invalidate path). The "benign" property genuinely holds (the orphaned walker's H2 connection stays alive via Arc/SendRequest clones; cx_destroy fires exactly once; cx_active is per-stream and decrements correctly); only the cross-reference is imprecise. **Disposition:** close opportunistically at a future phase opening `pool.rs`; add a short paragraph in the invalidate path's comment block explicitly walking the pre-evict-walker scenario. Named owner: next phase extending H2 pool.

**A-M2. Stale Debug-impl comment in H1 pool (`crates/envoy-http1/src/pool.rs:322`).** Reads "`H1Pool`'s internal `tokio::sync::Mutex` + per-pool `Counter`/`Gauge` Arcs aren't reflected here" — but Task 1 switched `idle`/`established` to `parking_lot::Mutex` (lines 57+59). One-character doc fix. **Disposition:** close opportunistically at next phase touching `pool.rs`. Cosmetic; non-blocking.

**A-M3. H2 race-regression test only 16 iter (`crates/envoy-http2/src/pool.rs:882-908`).** PROGRESS Task 1 fold-in acknowledges this honestly ("the H2 race window is now structurally closed by the lock ordering in Drop — a probabilistic test is not load-bearing on the H2 side"). The CRITICAL invalidate-path fix at `ae8d7cf` is verified by structural reasoning + the existing `invalidate_evicts_entry_and_increments_cx_destroy` unit test. No H2 1000-iter sibling exists. **Disposition:** awareness-only; acceptable per the structural argument. A future hardening phase could add an H2 stress test mirroring the H1 1000-iter shape. Named owner: optional follow-up.

**A-M4. H2 pool `max_streams` hardcoded to RFC default 100 (`crates/envoy-http2/src/pool.rs:333`).** The pool stores `max_streams: DEFAULT_MAX_CONCURRENT_STREAMS` at entry construction, regardless of the peer's `SETTINGS_MAX_CONCURRENT_STREAMS`. Under any peer advertising a lower cap (rare in practice; most servers permit ≥100), the pool would attempt to multiplex past the peer's cap and the h2 codec would block/error. Out of scope for fixture 0021 (5-request workload never hits 100). Consistent with SPEC §6.2 item-vi noting hardcoded default, but the discrepancy from upstream Envoy is not surfaced as a Deviation in PROGRESS. **Disposition:** close opportunistically at a future H2-pool refinement phase (read peer SETTINGS at handshake completion, plumb through to `H2PoolEntry.max_streams`). Named owner: future H2-pool refinement phase.

**A-M5. H2 pool multi-guard double-invalidate accounting (`crates/envoy-http2/src/pool.rs`).** Already tracked in STATE.md as pre-existing MINOR (introduced by Task 1's invalidate-Drop shape; if two H2 PoolGuards are concurrently held against the same multiplexed entry and BOTH are subsequently invalidated, the second invalidate-Drop hits a `retain` no-op but still decrements `established` AGAIN + fires `cx_destroy` AGAIN). Genuinely H2-specific (the H1 pool's per-Guard `Option<ClientStream>` makes the equivalent path idempotent). **Disposition:** carry-forward unchanged; the existing carryforward attribution is honest. Named owner: future H2-pool refinement phase.

**Cluster B Minors (Tasks 3 + 4 + state-3-partial + 5 + 5-fold-in — docs + carryforward + fixture 0021 + harness driver):**

**B-M1. Fixture 0021 `circuit_breakers.thresholds[0].max_connections: 4` is dead config under the current workload (`tests/fixtures/0021-upstream-h2-connection-pooling/envoy.yaml:~73`).** The workload (5 sequential GETs multiplexed onto 1 H2 connection, well under `DEFAULT_MAX_CONCURRENT_STREAMS = 100`) never reaches the second-connection threshold; `max_connections: 4` is never engaged. The 4-vs-1 headroom is harmless but the config carries no observable signal at this fixture. Consistent with fixture 0020's H1 pattern (also has the cap as a future-proofing knob). **Disposition:** no in-phase fix; awareness-only. The cap serves as forward-proofing against future workload extensions + visual parity with the 0020 sibling. Named owner: none.

**B-M2. `drive_http2_keep_alive` does not handle backend-side mid-stream disconnect explicitly (`tests/differential/src/lib.rs` — `drive_http2_keep_alive` body).** If the upstream echo backend dies between requests N and N+1, `sr.send_request(...)` returns Err which the helper surfaces via `with_context` — bubbling cleanly via the existing pattern. The path *probably* works (Err bubbles correctly) but is not validated by any test. PROGRESS Self-review acknowledges the sequential-only design. **Disposition:** no in-phase fix; awareness-only. Track forward for a future fixture exercising forced backend disconnect. Named owner: next phase introducing a fault-injection fixture.

**B-M3. `Driver::Http2KeepAlive`'s `clone()` of SendRequest per loop iteration is unnecessary under sequential await (`tests/differential/src/lib.rs:~1533`).** PROGRESS Task 5 Self-review item 1 already names this; the clone matches h2's documented multiplex idiom and is forward-compatible with parallel-stream extension. **Disposition:** track-forward without action; the clone is forward-compatible scaffolding. Named owner: none.

**B-M4. Discriminating-power gap at the fixture 0021 surface (`tests/fixtures/0021-upstream-h2-connection-pooling/expectations.yaml` + Cluster C's parallel observation at the backstop).** `upstream_cx_total: 1` + `upstream_cx_http2_total: 1` proves the H2 pool reuses ONE upstream conn but does NOT independently discriminate stream-multiplex from a serial acquire+release of one pooled conn 5 times (the same counter pair fires regardless of whether streams overlap). The gap is honestly named at PROGRESS Task 5 Self-review item 3 + at the backstop's in-code docstring (post Task 6 fold-in). A stronger in-process observable would be a peak-`upstream_cx_active` gauge snapshot during the dispatch loop, OR a per-pool-entry concurrent-stream counter — neither currently exposed in admin /stats. **Disposition:** carry-forward to a future H2-pool refinement phase that adds a per-entry peak-streams or `cx_active_peak` observable. The bilateral evidence at fixture 0021 + the in-process backstop is the strongest available today; the discriminating-power gap accepts as a known follow-up. Named owner: future H2-pool refinement phase.

**Cluster C Minors (Tasks 6 + 6-fold-in + 7 — in-process H2 backstop + state-4 verification):**

**C-M1. Omitted-5-header rationale at the in-code surface doesn't cite RFC 7540 §8.1.2.2 (`crates/envoy-bin/tests/upstream_h2_connection_pooling.rs:38-46`).** PROGRESS Task 6 substitution-item 8 cites the RFC; the in-code docstring elides it. The technical justification (per-non-2xx discipline + all-2xx workload) is sound; RFC citation is non-load-bearing for a stranger reading just the test file. **Disposition:** no action; the rationale is correct. Named owner: none.

**C-M2. H2 backstop test fn name `upstream_h2_connection_pooling` is less descriptive than the H1 sibling `pool_reuses_upstream_conn_and_counts_per_class` (`crates/envoy-bin/tests/upstream_h2_connection_pooling.rs:~281`).** Cosmetic asymmetry; harmless. **Disposition:** track-forward only. Named owner: none.

**C-M3. Local flakes named at Task 7** — 2 differential lib tests (`subject::tests::starts_and_shuts_down_envoy_rust` + `tests::drive_admin_scrape_round_trips_against_envoy_bin_admin`) under `--test-threads=1` + the `access_log_file_sink` test surface kernel-port-contention / wall-time-race flakes locally. PROGRESS claim "pass cleanly in isolation; pre-existing kernel-port-contention pattern" matches the Task 2 PROGRESS narrative + the 13.1 state-5 fold-in precedent. CI does not see these flakes. **Disposition:** carry-forward unchanged; surface again if they appear in CI. Named owner: a future test-infrastructure pass.

**C-M4. State-4 verification's gate-(b) "21 fixtures green simultaneously" delegated to CI anchor** (`26414774250`). Local Docker was unstable at the verification window; the 3 representative-fixture local corroboration (0010 + 0020 + 0021) + the CI-anchored full bilateral run are the evidence. The delegation is doctrinally honest per the 13.1 state-5 + 12.2 state-4 precedents (CI is the canonical gate anchor when the local environment is unstable). **Disposition:** no action; track forward as a known cadence — Docker-stable local environments produce the bilateral 21/21 result directly; the CI anchor preserves the gate's evidence chain. Named owner: none.

## §4. Carryforward inventory

For each Minor: closure plan. (No Critical or Important items remain.)

| ID | Severity | Item | Closure plan |
|---|---|---|---|
| **A-M1** | Minor | TOCTOU comment cross-reference imprecise at `crates/envoy-http2/src/pool.rs:135-138` | **Close opportunistically** at next phase opening `pool.rs`. Add a short paragraph explicitly walking the pre-evict-walker scenario. Non-blocking. |
| **A-M2** | Minor | Stale `tokio::sync::Mutex` reference in `pool.rs:322` Debug-impl comment | **Close opportunistically** at next phase touching `crates/envoy-http1/src/pool.rs`. One-character fix. Non-blocking. |
| **A-M3** | Minor | H2 race-regression test at 16 iter (no 1000-iter sibling to H1's stress test) | **No action recommended**; structurally adequate per the lock-ordering argument. Optional hardening phase could mirror H1's shape. Named owner: optional follow-up. |
| **A-M4** | Minor | H2 pool `max_streams` hardcoded to RFC default 100; not derived from peer SETTINGS | **Close at future H2-pool refinement phase**. Read peer SETTINGS at handshake completion; plumb to `H2PoolEntry.max_streams`. Out of scope for 13.2's closing-sub-phase atomic surface. |
| **A-M5** | Minor | H2 pool multi-guard double-invalidate accounting (pre-existing) | **Carry-forward unchanged.** Genuinely H2-specific (H1 pool's per-Guard `Option<ClientStream>` is idempotent). Named owner: future H2-pool refinement phase. |
| **B-M1** | Minor | Fixture 0021 `max_connections: 4` is dead config under the 5-stream workload | **No action recommended.** Mirrors fixture 0020's H1 pattern (forward-proofing knob). Awareness-only. Named owner: none. |
| **B-M2** | Minor | `drive_http2_keep_alive` doesn't explicitly handle backend mid-stream disconnect | **No action recommended.** Err bubbles via existing `with_context` pattern. Track forward for a future fault-injection fixture. Named owner: next phase introducing a fault-injection fixture. |
| **B-M3** | Minor | `Driver::Http2KeepAlive`'s `clone()` of SendRequest unnecessary under sequential await | **No action recommended.** Forward-compatible scaffolding; matches h2 multiplex idiom. Named owner: none. |
| **B-M4** | Minor | Discriminating-power gap at fixture 0021 + the in-process backstop (counter pair (1,1) cannot distinguish multiplex from serial reconnect) | **Close at future H2-pool refinement phase** that adds a per-entry peak-streams or `cx_active_peak` observable. Named owner: future H2-pool refinement phase. |
| **C-M1** | Minor | Omitted-5-header rationale at in-code surface doesn't cite RFC 7540 §8.1.2.2 | **No action recommended** (technical justification is sound; RFC citation non-load-bearing). Named owner: none. |
| **C-M2** | Minor | H2 backstop test fn name less descriptive than H1 sibling | **No action recommended** (cosmetic asymmetry). Named owner: none. |
| **C-M3** | Minor | Local flakes (kernel-port-contention + access-log wall-time race) | **Carry-forward unchanged**; surface if CI sees them. Named owner: future test-infrastructure pass. |
| **C-M4** | Minor | Gate-(b) "21 fixtures green simultaneously" delegated to CI anchor | **No action recommended** (doctrinally honest per the 13.1 / 12.2 precedents). Track-forward as a known cadence. Named owner: none. |

**None of the new carryforwards gate the state-6 close-out OR any future phase.** All are awareness-only / close-opportunistically per the standing post-MVP-trunk discipline.

**Carryforward closures attributed at 13.2 (no longer live):**

- **06.3 REVIEW I2 (b) — FULLY CLOSED at Task 4 `4ab2c61`.** Combined with 13.1's I2 (a) closure (fixture 0020), the FULL 06.3 REVIEW I2 carryforward CLOSES at parent-13 close (Task 8 re-attributes at the closing-sub-phase commit per the closing-sub-phase invariant).
- **13.1 REVIEW Cluster A-I3 — FULLY CLOSED jointly across H1+H2 at Task 1 `f692b53` + Task 1 fold-in `ae8d7cf`** (joint sync-`parking_lot::Mutex` switch + the H2 invalidate-Drop TOCTOU race close).
- **13.1 REVIEW Cluster A-M1 + A-M2 + A-M4 — FULLY CLOSED opportunistically at Task 1 `f692b53`** (rename `_sweepers → sweepers` + `pub async fn shutdown(self)` on both managers + `Arc::ptr_eq` debug-assert at the gauge wiring site + improved `.expect` message naming the single-bootstrap-per-process invariant).

**Carryforwards opened at 13.2:**

- **ADR-0028 (H1-listener × H2-cluster dispatch deferral) — REMAINS OPEN** per ADR-0039 Consequences. Closure path documented (extract `envoy-http2::Client` to a new crate to break the dep cycle; ~300-400 LoC; out of 13.2 scope; foundations grant + new crate). Named owner: a follow-up phase.

**Standing carryforwards inherited but not closed in 13.2** (per PROGRESS Task 1 preamble carryforward disposition; 13.2 ENGAGED 06.3 REVIEW I2 (b) FULL CLOSURE at Task 4 + 13.1 REVIEW Cluster A-I3/M1/M2/M4 at Task 1; the rest carry forward):

- **13.1 REVIEW Cluster A-M3 + A-M5 + B-M1..B-M3 + C-M1..C-M4** (9 Minors) — carry forward unchanged; **not engaged** in 13.2 (the named seams weren't touched in their named-seam patterns).
- **12.2 REVIEW Minor carryforwards** (11 active: A-M2, A-M4, B-M1..B-M6, C-M1, C-M2, C-M4) — carry forward unchanged; **not engaged** (the named seams in envoy-health + the 12.2-extended helper weren't touched in their patterns).
- **12.1 REVIEW M1 + M3** — carry forward unchanged; **not engaged**.
- **Phase-11 REVIEW M1-M8** — carry forward unchanged; **not engaged** (13.2 touches no HTTP-filter file).
- **10 M2/M3/M4/D1/D2/T1; 09 M1/D1/D2/T1/T2/T3; 08.2 REVIEW M1-M8 + T1-T3; 08.1 REVIEW M3; 07.2 REVIEW M2/M3; 06.2 REVIEW M1/M2/M4/M5; 06.1 REVIEW M2/M3/M5/M6; 05.3 REVIEW I2; 05.2 REVIEW I1/I2/I3; 04.1 REVIEW M5/M9/M-claim/M1/M2/M4/M7; 02.2 REVIEW M1; Phase-00 I3 SIGKILL→SIGTERM** — carry forward indefinitely unless coincidentally engaged. None engaged by the 13.2 surface (touches `envoy-http2` + `envoy-http1` + `envoy-cluster` accessor + `envoy-bin` wiring + test infrastructure + docs).
- **04.1 REVIEW M5 / M9 Cargo.lock cadence ratification** — carries forward unchanged; **zero new top-level Cargo deps** at this phase (the `parking_lot` family added at Task 1 is sub-crate-only at `crates/envoy-http1/Cargo.toml` + `crates/envoy-http2/Cargo.toml`; workspace root Cargo.toml unchanged). The Cargo.lock diff at the reviewed range is non-empty but ratified by Cluster A as deps-on-existing-foundations only.

**Known-deferred small follow-up carried forward from 13.1 — NOT engaged by 13.2:**

- **Cluster per-class `upstream_rq_{2,3,4}xx` counter family extension.** envoy-rust's `Cluster` carries only `upstream_rq_total + upstream_rq_5xx` Arc<Counter> fields today. The named seam `crates/envoy-cluster/src/cluster.rs:71-76` is NOT touched at 13.2. Continues to carry forward as a small standalone task for a future upstream-robustness or observability-family phase.

## §5. Process note (awareness-only, no carryforward)

Phase 13.2 is the **THIRD phase since the MVP trunk closed to land 4 in-phase mid-execution fold-ins** (`ae8d7cf` Task 1 CRITICAL + `ef6deda` Task 2 IMPORTANT + `f7cd908` Task 5 IMPORTANT + `2fef8ad` Task 6 2× IMPORTANT — 4 separate fold-in commits at state-3, not at state-5 re-entry). The cadence reflects the subagent-driven 3-cluster execution per `feedback_execution_style`: each task is dispatched to a fresh subagent, the implementer's commit is then reviewed inline by a code-quality reviewer subagent before the next task dispatches, and any findings land as a fold-in commit before the next task fires. This is a strictly stronger discipline than the 13.1 cadence (which landed only 1 in-phase state-3 fold-in at Task 4 + 1 in-phase state-5 fold-in at `52f43f7`) and structurally precludes the kind of late-state-5-fold-in scrambling that the 12.2 + 13.1 sessions experienced. The result is a phase where the state-5 review surfaces zero Critical and zero Important findings — the implementer + the inline code-quality reviewer + the controller had already closed every gating finding at task time.

Phase 13.2 is also the **FIRST phase to ratify an ADR (ADR-0039) mid-execution after the implementer's BLOCKED escalation surfaced an architectural blocker the PLAN-writer had missed**. The state-3-partial closure pattern at `e2b8d1b` is the project's first instance of a state-3 session closing at a task boundary with a topology-pivot ADR — preserving the next session's resumption shape per `BOOTSTRAP_PROMPT.md` §5.1 + leaving the PLAN immutable per the append-only doctrine + carrying the corrected narrative in PROGRESS + ADR-0039. The cadence works as designed; no doctrine change recommended.

**Phase 13.2 also delivers the closing milestone of the parent-13 connection-pooling family.** With Task 8 (the state-6 close-out commit at the next session), parent-13 closes simultaneously with 13.2 per the closing-sub-phase invariant — flipping ROADMAP rows `13.2` AND `13` `in-progress → done` SIMULTANEOUSLY in one commit. The Upstream-robustness family at that point has **two closed phases (12 + 13)** + the **foundation periodic-background primitive triad** (12.2 active-HC scheduler + 13.1 H1 pool idle sweeper + 13.2 H2 pool idle sweeper, all three sharing identical `tokio_util::sync::CancellationToken` cancellation discipline) + the **per-protocol connection pooling complete for H1 + H2** (TCP-proxy pooling carved out for a follow-up phase per the BEHAVIOR_CONTRACT row's explicit carve-out; H3/QUIC pooling defers to the HTTP/3 family) + the **FULL 06.3 REVIEW I2 closure** (both (a) from 13.1 + (b) from 13.2 D7.1) + **ADR-0039 ratified at state-3-partial** + **ADR-0028 carried forward** with the closure path documented.

---

*End of 13.2 REVIEW. Verdict: Approved with M-track follow-ups. The next session's state-6 close-out (Task 8) flips ROADMAP rows `13.2` AND parent-13 `13` `in-progress → done` SIMULTANEOUSLY per the closing-sub-phase invariant + re-attributes the FULL 06.3 REVIEW I2 closure at the closing commit + advances STATE.md to `awaiting next planning`.*
