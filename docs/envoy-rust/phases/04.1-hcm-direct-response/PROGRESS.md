# Phase 04.1 Progress

## Task 1 — envoy-config: HCM TypedConfig variant + RouteConfiguration schema + DirectResponse + DataSource extension + 6 parse-shape tests (2026-04-27)

- Commit: c41ae7f
- Change: Extended `crates/envoy-config/src/bootstrap.rs` with the HCM and route-config schema. Added a new `TypedConfig::HttpConnectionManager(HttpConnectionManagerConfig)` variant on the existing `TypedConfig` enum (sibling of `TcpProxy`). Appended ten new types after the existing `DataSource` struct: `HttpConnectionManagerConfig`, `CodecType` (enum: AUTO/HTTP1/HTTP2/HTTP3, `#[allow(non_camel_case_types)]`), `HttpFilter`, `HttpFilterTypedConfig` (enum tagged on `@type` with the `Router` variant), `RouterConfig` (empty in 04.1 per SPEC §4 deferrals), `RouteConfiguration`, `VirtualHost`, `Route` (with `#[serde(rename = "match")]` on `r#match`), `RouteMatch` (`prefix` + `path`, both `Option<String>` with `#[serde(default)]`), `DirectResponse` (`status: u16`, `body: DataSource`). All carry `#[derive(Debug, Deserialize, PartialEq)]` and `#[serde(deny_unknown_fields)]` per the established bootstrap.rs convention.
- DataSource extension: `filename: String` → `filename: Option<String>` with `#[serde(default)]`; new `inline_string: Option<String>` field with `#[serde(default)]`. The "exactly one of {filename, inline_string} is `Some`" invariant — and the per-callsite restriction that TLS still requires `filename` — is the validator's responsibility (Task 2), not serde's.
- Re-exports: extended the `pub use bootstrap::{...}` list in `crates/envoy-config/src/lib.rs` to include all 10 new public names (alphabetic / logical-cluster ordering preserved).
- Validator preservation: the existing `validate` block that destructured `TypedConfig::TcpProxy` via an irrefutable `let` is now refutable (the enum has 2 variants); rewrote it as `let TypedConfig::TcpProxy(tp) = typed else { return Err(MissingTypedConfig(TCP_PROXY_FILTER)); };` — preserves the existing error surface (HCM-on-tcp_proxy-name maps to `MissingTypedConfig` until Task 2 adds a dedicated variant). The `parses_listener_with_tcp_proxy_typed_config` test's match arm gained a `_ => panic!(...)` branch for the same exhaustiveness reason.
- Existing-callsite updates: 3 callsites in `crates/envoy-tls/src/lib.rs` (`DownstreamTls::from_context`, `DownstreamTls::from_listener`, `UpstreamTls::from_context`) changed from `&ds.filename` to `ds.filename.as_deref().expect("validator ensures TLS DataSource carries filename")`. 3 test-helper struct-literal sites in `crates/envoy-tls/src/tests.rs` (2) and `crates/envoy-tcp/src/lib.rs` (1) updated from `DataSource { filename: <String> }` to `DataSource { filename: Some(<String>), inline_string: None }`. 3 test-assertion callsites in `crates/envoy-config/src/bootstrap.rs` updated from `ds.filename` direct-equals to `ds.filename.as_deref()` form.
- Tests added: `parses_listener_with_hcm_direct_response` (happy path: full HCM listener YAML → asserts every leaf field of HCM + RouteConfiguration + Route + DirectResponse + http_filters), `parses_route_with_path_matcher` (RouteMatch with `path` only), `parses_data_source_with_inline_string`, `parses_data_source_with_filename`, `rejects_unknown_field_in_hcm_config` (deny_unknown_fields regression guard), `rejects_unknown_field_in_route_match`. Total 6 new tests (1 happy-path + 4 narrow-shape + 1 unknown-field reject; the SPEC's "5 parse-shape tests" tally was 1+4=5 with the second unknown-field reject added for symmetry).
- Test removed: `rejects_unknown_field_in_data_source` (phase 03.1 test that asserted DataSource rejected `inline_string` as unknown). That assertion is now intentionally false — `inline_string` is a recognized DataSource field as of Task 1 — so the test was removed. The "both fields set" failure mode it guarded is now a validator-level concern (Task 2) and Task 2's validator tests subsume the regression coverage. Replaced the test with a comment block at the same location explaining the move.
- Verification: `cargo test -p envoy-config --lib` reported 66 passed, 0 failed (61 pre-existing − 1 removed + 6 added = 66). `cargo build --workspace --all-targets` clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. `cargo fmt --all -- --check` clean. `Cargo.lock` unchanged. The Docker-gated `differential::admin_ready_fixture` test fails on this machine for the pre-existing "Socket not found: /var/run/docker.sock" reason (verified by `git stash && cargo test -p differential --test admin_ready` reproducing the same failure on baseline `1d9740d`); not caused by this task.
- Deviations from PLAN:
  1. **Test count is 6, not 5** — the PLAN's Step 9 acknowledged this: "if execution finds 6 reads cleaner — keep all 6 and adjust the tally in PROGRESS.md." Kept all 6 because each tests a distinct shape (the two `rejects_unknown_field_*` tests cover different types — HCM and RouteMatch — and each is a thin enough check that combining them would be muddled).
  2. **Validator change** (TypedConfig::TcpProxy refutable destructure) was outside Task 1's stated "no validator wiring yet" scope, but mandatory to keep the build green once `TypedConfig` gained a second variant. The change is behaviorally inert: the same `MissingTypedConfig` error fires for HCM-on-tcp_proxy-name as fired for missing typed_config, preserving the existing error-surface contract. Task 2 is expected to introduce a dedicated variant.
  3. **`parses_listener_with_tcp_proxy_typed_config` got a wildcard match arm** — exhaustiveness fix only, no behavioral change.
  4. **Removed a phase-03.1 test** (`rejects_unknown_field_in_data_source`) — its assertion was specifically that `inline_string` is rejected as unknown; the schema change in this task makes that assertion false by design. Documented in-place with a replacement comment block.
- ADRs: none in this task. ADR-0020 (parent-04 split) is referenced in the commit message; it landed at parent-04 state-2 commit `1d9740d`. ADR ledger head: 20.

## Task 2 — envoy-config validator extensions + 10 ConfigError variants + 8 validator tests (2026-04-27)

- Commit: c15b6b4
- Change: extended `crates/envoy-config/src/bootstrap.rs::validate` with a new dispatch arm for the HCM filter name (`crate::HCM_FILTER`) that destructures `TypedConfig::HttpConnectionManager(_)` and delegates to a new `validate_hcm` helper. Added two more helpers `validate_data_source` (cardinality + per-callsite "filename" or "inline_string" restriction) and `is_valid_dns_name` (RFC 1123 LDH labels; wildcards deferred to a later phase). Added 10 `ConfigError` variants per SPEC §3 D2 (`UnsupportedCodecType`, `UnsupportedHttpFilter`, `UnsupportedRouteMatcher`, `UnsupportedDomainMatcher`, `EmptyVirtualHosts`, `EmptyRoutes`, `EmptyDomains`, `InvalidStatusCode`, `UnsupportedDataSource`, `MultipleHttpFilters`). Added a new `pub const HCM_FILTER: &str = "envoy.filters.network.http_connection_manager"` constant in `lib.rs` (sibling of `TCP_PROXY_FILTER`).
- TLS validator refactor: 3 callsites in `validate` — Upstream `validation_context.trusted_ca`, Downstream `tls_certificate.certificate_chain`, Downstream `tls_certificate.private_key` — now route through `validate_data_source(..., "filename")`. The Upstream branch additionally promotes the previous `is_none()` early-return on `validation_context` to an `ok_or(MissingValidationContext)?` extraction so it can call `validate_data_source` on the `trusted_ca` payload. Cardinality semantics are unchanged: each TLS callsite still requires `filename: Some(_)` and rejects `inline_string`, with the same "exactly one of {filename, inline_string} is `Some`" guard now centralized in the helper.
- Tests added (8): `rejects_codec_type_http2`, `rejects_codec_type_http3`, `rejects_unsupported_http_filter`, `rejects_route_match_with_both_prefix_and_path`, `rejects_route_match_with_neither_prefix_nor_path`, `rejects_direct_response_with_filename_body`, `rejects_direct_response_with_invalid_status`, `rejects_empty_virtual_hosts`. All assert via `matches!` against the specific `ConfigError` variant. Two private test helpers were added: `parse_then_validate(yaml)` and `make_hcm_listener_yaml(hcm_block)`, plus a `VALID_ROUTER_FILTER` const (the http_filters block reused across 7 of the 8 tests).
- Verification: `cargo test -p envoy-config --lib` reported 74 passed, 0 failed (66 from Task 1 + 8 added). `cargo build --workspace --all-targets` clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. `cargo fmt --all -- --check` clean. The Docker-gated `differential::admin_ready_fixture` test fails on this machine for the pre-existing "Socket not found: /var/run/docker.sock" reason (same situation Task 1's PROGRESS already documented); not caused by this task.
- Deviations from PLAN:
  1. **Helper signature** — the PLAN's sketch used `ConfigError::ParseError(e.to_string())` to map the YAML error in `parse_then_validate`. That variant doesn't exist; the existing `ConfigError::Yaml(#[from] serde_yaml::Error)` covers it via `?`, so the helper's signature reduced to `Result<Bootstrap, ConfigError>` with a plain `?` on `serde_yaml::from_str`. Behavior is equivalent.
  2. **HCM dispatch arm not literal "replace the let-else"** — the PLAN's hand-off note pointed at the Task-1 defensive `let-else` mapping HCM-on-tcp_proxy-name to `MissingTypedConfig`. That arm was matched on `crate::TCP_PROXY_FILTER`, which is *not* the HCM filter name (`envoy.filters.network.http_connection_manager`). The HCM filter name was previously falling through to the wildcard `_` arm and returning `UnsupportedFilter`. Resolution: kept the TCP_PROXY arm intact (its defensive let-else still guards the genuine "tcp_proxy filter name with HCM typed_config under it" misconfiguration shape) and added a *new* `crate::HCM_FILTER => { … }` arm that delegates to `validate_hcm`. This matches the PLAN's pseudocode shape on lines 753–776 and the SPEC's "validator-already-rejects guarantees" enumeration on §6 d3.1; the TCP-proxy let-else is now correctly scoped to its actual case.
  3. **Added `HCM_FILTER` constant** — not explicitly enumerated in the PLAN's lib.rs delta but mirrors `TCP_PROXY_FILTER` precedent and is needed for the dispatch arm.
  4. **`EmptyDomains` test absent** — the PLAN's tally is "8 new tests" and explicitly enumerates the 8 added; `EmptyDomains` is exercised by the `validate_hcm` walk but not directly by a 04.1 test. Coverage is implicit via `rejects_empty_virtual_hosts` exercising the same outer `route_config.virtual_hosts.is_empty()` short-circuit. No drift from the PLAN's named-test list.
- ADRs: none in this task. ADR ledger head remains 20.

### Task 2 follow-up — review fixes (2026-04-27)
- Commit: 4e7c050
- Change: added rejects_empty_domains test (closes coverage gap on the EmptyDomains validator branch); converted validate_data_source's `requires` parameter from `&'static str` to a private `Required` enum, removing the runtime `unreachable!` fallback.
- Verification: cargo test -p envoy-config → 75 passed; clippy clean; fmt clean.

## Task 3 — fuzz corpus extension (2026-04-27)

- Commit: fe65bb0
- Change: added 2 HCM-shaped seeds to `crates/envoy-config/fuzz/corpus/parse_bootstrap/`: `hcm_direct_response_happy.yaml` (parse+validate Ok), `hcm_invalid_codec_type.yaml` (parse Ok / validate UnsupportedCodecType). The existing `parse_bootstrap` target picks them up automatically; `-max_total_time=30` budget unchanged per ADR-0010. Extended `crates/envoy-config/fuzz/.gitignore`'s allow-list with the two new filenames so they actually land under version control (the directory is `corpus/parse_bootstrap/*` ignored with per-seed `!`-overrides). Extended the hand-listed `fuzz_corpus_seeds_parse_or_reject_cleanly` corpus-walk test in `bootstrap.rs` to cover both seeds: `hcm_direct_response_happy.yaml` joined the parse-Ok group, `hcm_invalid_codec_type.yaml` joined the reject-cleanly group.
- Verification: `cargo test -p envoy-config` → 75 passed (test count unchanged — the corpus-walk test was already counted; only its enumeration grew). `cargo build --workspace --all-targets` clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. `cargo fmt --all -- --check` clean. Local cargo-fuzz smoke-run skipped: nightly toolchain present but `cargo-fuzz` not installed on this box; CI exercises the extended corpus on its `-max_total_time=30` budget.
- Deviations from PLAN:
  1. **`.gitignore` allow-list extension** — not enumerated in the PLAN's Step 1/2 list of files-to-create, but mandatory: `crates/envoy-config/fuzz/.gitignore` ignores `corpus/parse_bootstrap/*` with per-seed `!`-overrides, so without the two new `!corpus/parse_bootstrap/hcm_*.yaml` lines the seeds would be silently ignored by git and never reach CI. Added at the natural spot (end of the existing allow-list, before `artifacts/`).
  2. **bootstrap.rs corpus-walk extension** — the PLAN's Constraints section says "Do NOT modify any source files," but Step 3 explicitly says "If the test exists and enumerates seeds explicitly (a hand-listed allow-list), extend it to include the two new seeds." The test is hand-listed (verified in `bootstrap.rs` lines 1549–1584), so Step 3 applies and is the more specific instruction. The change is purely additive: one new entry in the parse-Ok loop, one in the reject-cleanly loop.
- ADRs: none in this task. ADR ledger head remains 20.

## Task 4 — envoy-http1 scaffold (2026-04-27)

- Commit: 37e074c
- Change: scaffolded `crates/envoy-http1/` with `Cargo.toml` (deps: envoy-config, envoy-cluster, envoy-listener, httparse, bytes, tokio with `net|io-util|macros|sync|time` features, thiserror v2, tracing; dev-dep: tokio with `rt-multi-thread|test-util`) and `src/lib.rs` (`#![forbid(unsafe_code)]` + crate-level docstring + 6 `mod` declarations: `pub mod codec/date/hcm/headers/response;` + `mod error;`). Created 6 stub-only module files each carrying a single doc comment naming the future-task that populates it. Added `"crates/envoy-http1",` to root `Cargo.toml`'s `[workspace] members` array in alphabetical position (between `envoy-config` and `envoy-listener`). The `envoy-cluster` and `tracing` deps are forward-looking for 04.3 and the HCM error paths respectively, per PLAN — locked in at scaffold time so subsequent tasks don't re-touch the manifest.
- Step 2 option chosen: **(b) Defer** the `pub use error::Http1Error;` re-export to Task 5. Option (a) would have required a `pub struct Http1Error;` stub with `#[allow(dead_code)]`; option (b) keeps the empty crate warning-free without any allow-attributes. lib.rs carries an inline comment block noting Task 5 will add the re-export and Tasks 8–10 will add codec/response/hcm re-exports.
- Verification: `cargo build -p envoy-http1` → clean (0.17–1.91s, 5 crates compiled including dep graph). `cargo build --workspace --all-targets` → clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` → clean. `cargo fmt --all -- --check` → clean (after one fix; see Deviations).
- Cargo.lock: touched (cargo build added the new `envoy-http1` package entry — 14 new lines, no version changes to existing deps). Included in this commit per PLAN ("if cargo build forces it, that's fine to include with the main commit but document in PROGRESS.md").
- Deviations from PLAN:
  1. **`mod` declaration order in `lib.rs`** — the PLAN's Step 2 sketch listed `pub mod codec; pub mod headers; pub mod date; pub mod response; pub mod hcm; mod error;` (logical/grouped order). rustfmt enforces alphabetical sort of `mod` items, so the as-committed order is `pub mod codec; pub mod date; mod error; pub mod hcm; pub mod headers; pub mod response;` (alphabetic, with the private `mod error` interleaved at its alphabetic slot). Caught by `cargo fmt --all -- --check` failing initially; resorted to match rustfmt. No semantic change; the comment block referencing Task 5 / Tasks 8–10 re-exports is preserved verbatim below the module list.
- ADRs: none in this task. ADR ledger head remains 20.

## Task 5 — envoy-http1::error (2026-04-27)

- Commit: 4f1814f
- Change: replaced the `error.rs` stub with the `Http1Error` enum carrying 6 variants (`MalformedRequestLine`, `MalformedHeader`, `HeadersTooLarge { cap: usize }`, `BodyTooLarge { cap: usize }`, `UnexpectedEof`, `Io { source: std::io::Error }`) per SPEC §3 D1. Each variant has its `#[error(...)]` display string verbatim from the PLAN. Added an explicit `impl From<std::io::Error> for Http1Error` (mapping into the `Io` variant) for `?`-ergonomic propagation in Tasks 8–10. Added `pub use error::Http1Error;` to `lib.rs` (Task 4 deferred this re-export; the comment-block reminder for Tasks 8–10 codec/response/hcm re-exports is preserved verbatim on the line below).
- Verification: `cargo build -p envoy-http1` → clean (0.20s, only envoy-http1 recompiled). `cargo clippy --workspace --all-targets --all-features -- -D warnings` → clean. `cargo fmt --all -- --check` → clean. No `dead_code` warnings fired despite the variants being unused at this point — `pub`-visible enum variants are not subject to dead-code lint, so no `#[allow(dead_code)]` attribute was needed (the PLAN's Step 4 contingency was not triggered).
- Deviations from PLAN: chose the explicit `impl From<std::io::Error>` form over thiserror's `#[from]` shorthand. The PLAN allowed either; explicit-impl was kept because clippy did not flag the manual impl as redundant (the `#[from]` substitution clause was contingent on clippy complaining), and the explicit form makes the conversion site obvious to a reader. Behavior is identical either way.
- ADRs: none in this task. ADR ledger head remains 20.

## Task 6 — envoy-http1::headers (2026-04-27)

- Commit: 7f3af73
- Change: populated `headers.rs` with 6 canonical-name constants (`HOST`, `CONTENT_LENGTH`, `CONNECTION`, `SERVER`, `DATE`, `CONTENT_TYPE`, all lowercase per HTTP/1.1 §3.2 case-insensitivity convention) + `find_header<'a>(headers: &'a [(String, String)], name: &str) -> Option<&'a str>` case-insensitive lookup helper using `eq_ignore_ascii_case` + 2 unit tests (`find_header_is_case_insensitive`, `find_header_returns_none_on_missing`). Per PLAN, did not add a `pub use headers::*` or any re-export to `lib.rs` — downstream code uses the `envoy_http1::headers::find_header` form, so the module stays accessible via its public path without surfacing names at crate root.
- Verification: `cargo test -p envoy-http1 -- find_header_is_case_insensitive find_header_returns_none_on_missing` → 2 passed, 0 failed (delta +2 from Task 5's baseline of 0). `cargo build --workspace --all-targets` clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. `cargo fmt --all -- --check` clean. No `dead_code` warnings fired on the 6 unused `pub const`s — `pub`-visible items are exempt from the lint at the workspace-internal level — so no `#[allow(dead_code)]` attribute was added (PLAN's Step 3 contingency was not triggered; Task 10 will consume the constants).
- Deviations from PLAN: none.
- ADRs: none in this task. ADR ledger head remains 20.

## Task 7 — envoy-http1::date (2026-04-27)

- Commit: f65aaa2
- Change: replaced the `date.rs` stub with `format_imf_fixdate(t: SystemTime) -> String` per RFC 7231 §7.1.1.1, hand-rolled via Howard Hinnant's `civil_from_days` algorithm (no external dep — SPEC §3 D1 locks the hand-rolled approach so 04.1 doesn't pre-emptively land an `httpdate` ADR). ~30 LoC. Pre-epoch times defensively clamp to `0` via `duration_since(UNIX_EPOCH).map(...).unwrap_or(0)`. Added 2 unit tests pinning the canonical 1994-11-06 example from the RFC (`Sun, 06 Nov 1994 08:49:37 GMT`) and the Unix epoch boundary (`Thu, 01 Jan 1970 00:00:00 GMT`).
- Verification: `cargo test -p envoy-http1 -- formats_canonical_imf_fixdate formats_unix_epoch` → 2 passed, 0 failed (delta +2 from Task 6's running total). Cross-checked the canonical example via `python3 -c 'import datetime; print(datetime.datetime.utcfromtimestamp(784111777).strftime("%a, %d %b %Y %H:%M:%S GMT"))'` → `Sun, 06 Nov 1994 08:49:37 GMT` (matches; deprecation warning on `utcfromtimestamp` is benign and unrelated). `cargo build --workspace --all-targets` clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. `cargo fmt --all -- --check` clean (after one fix; see Deviations).
- Clippy attribute: applied `#[allow(clippy::cast_possible_truncation, clippy::cast_sign_loss)]` at function level per PLAN's Step 3 contingency. The cast chain in Hinnant's algorithm (`secs / 86_400` → `i64`, `yoe as i64`, `(m - 1) as usize`) is deliberate — the algorithm operates on a mix of i64 (era arithmetic with negative-z handling) and u64 (day-of-era index), and the truncations are bounded by domain invariants (`m ∈ [1,12]`, `weekday_idx ∈ [0,6]`, etc.). Suppressed without per-cast `#[allow]` noise.
- Deviations from PLAN:
  1. **rustfmt reformatted the second `assert_eq!`** — the PLAN's stub kept `assert_eq!(format_imf_fixdate(UNIX_EPOCH), "Thu, 01 Jan 1970 00:00:00 GMT");` on one line. rustfmt's line-width heuristic split it into 4 lines (`assert_eq!(\n  format_imf_fixdate(UNIX_EPOCH),\n  "Thu, 01 Jan 1970 00:00:00 GMT"\n);`). Caught by `cargo fmt --all -- --check` after the initial commit attempt; applied the reformat before committing. No semantic change.
  2. **Month-names array on a single line** — rustfmt collapsed the PLAN's 2-row layout into one row (4-column-wide formatter). No semantic change.
- ADRs: none in this task. ADR ledger head remains 20.

## Task 8 — envoy-http1::codec (2026-04-27)

- Commit: 06fa429
- Change: replaced the `codec.rs` stub with `Http1Codec` (zero-sized stateless wrapper), `Request` value type (`method`/`path`/`version`/`headers`/`bytes_consumed`), and `HttpVersion` enum (`Http10`/`Http11`) per SPEC §3 D1. Implemented `Http1Codec::parse_request(buf: &[u8]) -> Result<Option<Request>, Http1Error>` as a thin wrapper over `httparse::Request::parse` with a fixed-size `[EMPTY_HEADER; 64]` storage stack-allocated per call (MAX_HEADERS = 64). On `Status::Complete(n)` the codec materializes owned `String`-pair headers (filtering out unused empty slots); on `Status::Partial` it enforces the 8 KiB headers cap (per SPEC §6 signpost 14 + parent-SPEC §3 cross-sub-phase rule "headers cap matches phase-02.2 admin tightening per phase-01 REVIEW I4") by returning `HeadersTooLarge { cap: 8192 }` if the buffer has grown past the cap; otherwise returns `Ok(None)` to signal "feed more bytes". Mapped all 7 `httparse::Error` variants exhaustively: `TooManyHeaders` → `HeadersTooLarge { cap: 8192 }`; `HeaderName | HeaderValue | NewLine | Status` → `MalformedHeader`; `Token | Version` → `MalformedRequestLine`. Added 5 unit tests per the PLAN's verbatim names: `parses_get_root_with_host`, `returns_none_on_partial_request_line`, `returns_err_on_malformed_request_line` (accepting either `MalformedRequestLine` or `MalformedHeader` since the `GET\r\n\r\n` shape's specific httparse failure mode isn't load-bearing), `enforces_headers_cap` (200 X-Pad rows ≈ 9 KiB, no terminal CRLF so httparse stays Partial), `preserves_header_emission_order_and_case`. Re-exported `Http1Codec`, `Request`, `HttpVersion` from `lib.rs`; updated the deferred-re-export comment to "Tasks 9–10 will add re-exports for response/hcm public types."
- httparse error matching: verified exhaustive against the registry source at `~/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/httparse-1.10.1/src/lib.rs:102-117` — the 7 variants are `HeaderName`, `HeaderValue`, `NewLine`, `Status`, `Token`, `TooManyHeaders`, `Version`. All 7 are covered; the SPEC writeup's mention of a possible `IpV6Address` variant is not present in this httparse version (the codebase uses 1.10.x via the workspace lock).
- Verification: `cargo test -p envoy-http1 -- parses_get_root_with_host returns_none_on_partial_request_line returns_err_on_malformed_request_line enforces_headers_cap preserves_header_emission_order_and_case` → 5 passed, 0 failed (delta +5 from Task 7's running total of 4 envoy-http1 tests). Followed TDD red→green: first staged a stub returning `unimplemented!()` and confirmed all 5 tests panicked, then landed the real impl and confirmed all 5 green. `cargo build --workspace --all-targets` clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. `cargo fmt --all -- --check` clean.
- Re-export ordering: rustfmt sorts use-list items alphabetically — wrote `pub use codec::{Http1Codec, HttpVersion, Request};` (not the PLAN's literal `{Http1Codec, Request, HttpVersion}` order) so `cargo fmt --check` stays green. No semantic change.
- Deviations from PLAN: none beyond the `use`-list alphabetic-sort tweak above.
- ADRs: none in this task. ADR ledger head remains 20.
