# Phase 04.1 Progress

## Task 1 — envoy-config: HCM TypedConfig variant + RouteConfiguration schema + DirectResponse + DataSource extension + 6 parse-shape tests (2026-04-27)

- Commit: c41ae7f
- Change: Extended `crates/envoy-config/src/bootstrap.rs` with the HCM and route-config schema. Added a new `TypedConfig::HttpConnectionManager(HttpConnectionManagerConfig)` variant on the existing `TypedConfig` enum (sibling of `TcpProxy`). Appended ten new types after the existing `DataSource` struct: `HttpConnectionManagerConfig`, `CodecType` (enum: AUTO/HTTP1/HTTP2/HTTP3, `#[allow(non_camel_case_types)]`), `HttpFilter`, `HttpFilterTypedConfig` (enum tagged on `@type` with the `Router` variant), `RouterConfig` (empty in 04.1 per SPEC §4 deferrals), `RouteConfiguration`, `VirtualHost`, `Route` (with `#[serde(rename = "match")]` on `r#match`), `RouteMatch` (`prefix` + `path`, both `Option<String>` with `#[serde(default)]`), `DirectResponse` (`status: u16`, `body: DataSource`). All carry `#[derive(Debug, Deserialize, PartialEq)]` and `#[serde(deny_unknown_fields)]` per the established bootstrap.rs convention.
- DataSource extension: `filename: String` → `filename: Option<String>` with `#[serde(default)]`; new `inline_string: Option<String>` field with `#[serde(default)]`. The "exactly one of {filename, inline_string} is `Some`" invariant — and the per-callsite restriction that TLS still requires `filename` — is the validator's responsibility (Task 2), not serde's.
- Re-exports: extended the `pub use bootstrap::{...}` list in `crates/envoy-config/src/lib.rs` to include all 10 new public names (alphabetic / logical-cluster ordering preserved).
- Validator preservation: the existing `validate` block that destructured `TypedConfig::TcpProxy` via an irrefutable `let` is now refutable (the enum has 2 variants); rewrote it as `let TypedConfig::TcpProxy(tp) = typed else { return Err(MissingTypedConfig(TCP_PROXY_FILTER)); };` — preserves the existing error surface (HCM-on-tcp_proxy-name maps to `MissingTypedConfig` until Task 2 adds a dedicated variant). The `parses_listener_with_tcp_proxy_typed_config` test's match arm gained a `_ => panic!(...)` branch for the same exhaustiveness reason.
- Existing-callsite updates: 3 callsites in `crates/envoy-tls/src/lib.rs` (`DownstreamTls::from_context`, `DownstreamTls::from_listener`, `UpstreamTls::from_context`) changed from `&ds.filename` to `ds.filename.as_deref().expect("validator ensures TLS DataSource carries filename")`. 3 test-helper struct-literal sites in `crates/envoy-tls/src/tests.rs` (2) and `crates/envoy-tcp/src/lib.rs` (1) updated from `DataSource { filename: <String> }` to `DataSource { filename: Some(<String>), inline_string: None }`. 3 test-assertion callsites in `crates/envoy-config/src/bootstrap.rs` updated from `ds.filename` direct-equals to `ds.filename.as_deref()` form.
- Tests added: `parses_listener_with_hcm_direct_response` (happy path: full HCM listener YAML → asserts every leaf field of HCM + RouteConfiguration + Route + DirectResponse + http_filters), `parses_route_with_path_matcher` (RouteMatch with `path` only), `parses_data_source_with_inline_string`, `parses_data_source_with_filename`, `rejects_unknown_field_in_hcm_config` (deny_unknown_fields regression guard), `rejects_unknown_field_in_route_match`. Total 6 new tests (1 happy-path + 4 narrow-shape + 1 unknown-field reject; the SPEC's "5 parse-shape tests" tally was 1+4=5 with the second unknown-field reject added for symmetry).
- Test removed: `rejects_unknown_field_in_data_source` (phase 03.1 test that asserted DataSource rejected `inline_string` as unknown). That assertion is now intentionally false — `inline_string` is a recognized DataSource field as of Task 1 — so the test was removed. The "both fields set" failure mode it guarded is now a validator-level concern (Task 2) and Task 2's validator tests subsume the regression coverage. Replaced the test with a comment block at the same location explaining the move.
- Verification: `cargo test -p envoy-config --lib` reported 66 passed, 0 failed (61 pre-existing − 1 removed + 6 added = 66). `cargo build --workspace --all-targets` clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. `cargo fmt --all -- --check` clean. `Cargo.lock` unchanged. The Docker-gated `differential::admin_ready_fixture` test fails on this machine for the pre-existing "Socket not found: /var/run/docker.sock" reason (verified by `git stash && cargo test -p differential --test admin_ready` reproducing the same failure on baseline `1d9740d`); not caused by this task.
- Deviations from PLAN:
  1. **Test count is 6, not 5** — the PLAN's Step 9 acknowledged this: "if execution finds 6 reads cleaner — keep all 6 and adjust the tally in PROGRESS.md." Kept all 6 because each tests a distinct shape (the two `rejects_unknown_field_*` tests cover different types — HCM and RouteMatch — and each is a thin enough check that combining them would be muddled).
  2. **Validator change** (TypedConfig::TcpProxy refutable destructure) was outside Task 1's stated "no validator wiring yet" scope, but mandatory to keep the build green once `TypedConfig` gained a second variant. The change is behaviorally inert: the same `MissingTypedConfig` error fires for HCM-on-tcp_proxy-name as fired for missing typed_config, preserving the existing error-surface contract. Task 2 is expected to introduce a dedicated variant.
  3. **`parses_listener_with_tcp_proxy_typed_config` got a wildcard match arm** — exhaustiveness fix only, no behavioral change.
  4. **Removed a phase-03.1 test** (`rejects_unknown_field_in_data_source`) — its assertion was specifically that `inline_string` is rejected as unknown; the schema change in this task makes that assertion false by design. Documented in-place with a replacement comment block.
- ADRs: none in this task. ADR-0020 (parent-04 split) is referenced in the commit message; it landed at parent-04 state-2 commit `1d9740d`. ADR ledger head: 20.

## Task 2 — envoy-config validator extensions + 10 ConfigError variants + 8 validator tests (2026-04-27)

- Commit: c15b6b4
- Change: extended `crates/envoy-config/src/bootstrap.rs::validate` with a new dispatch arm for the HCM filter name (`crate::HCM_FILTER`) that destructures `TypedConfig::HttpConnectionManager(_)` and delegates to a new `validate_hcm` helper. Added two more helpers `validate_data_source` (cardinality + per-callsite "filename" or "inline_string" restriction) and `is_valid_dns_name` (RFC 1123 LDH labels; wildcards deferred to a later phase). Added 10 `ConfigError` variants per SPEC §3 D2 (`UnsupportedCodecType`, `UnsupportedHttpFilter`, `UnsupportedRouteMatcher`, `UnsupportedDomainMatcher`, `EmptyVirtualHosts`, `EmptyRoutes`, `EmptyDomains`, `InvalidStatusCode`, `UnsupportedDataSource`, `MultipleHttpFilters`). Added a new `pub const HCM_FILTER: &str = "envoy.filters.network.http_connection_manager"` constant in `lib.rs` (sibling of `TCP_PROXY_FILTER`).
- TLS validator refactor: 3 callsites in `validate` — Upstream `validation_context.trusted_ca`, Downstream `tls_certificate.certificate_chain`, Downstream `tls_certificate.private_key` — now route through `validate_data_source(..., "filename")`. The Upstream branch additionally promotes the previous `is_none()` early-return on `validation_context` to an `ok_or(MissingValidationContext)?` extraction so it can call `validate_data_source` on the `trusted_ca` payload. Cardinality semantics are unchanged: each TLS callsite still requires `filename: Some(_)` and rejects `inline_string`, with the same "exactly one of {filename, inline_string} is `Some`" guard now centralized in the helper.
- Tests added (8): `rejects_codec_type_http2`, `rejects_codec_type_http3`, `rejects_unsupported_http_filter`, `rejects_route_match_with_both_prefix_and_path`, `rejects_route_match_with_neither_prefix_nor_path`, `rejects_direct_response_with_filename_body`, `rejects_direct_response_with_invalid_status`, `rejects_empty_virtual_hosts`. All assert via `matches!` against the specific `ConfigError` variant. Two private test helpers were added: `parse_then_validate(yaml)` and `make_hcm_listener_yaml(hcm_block)`, plus a `VALID_ROUTER_FILTER` const (the http_filters block reused across 7 of the 8 tests).
- Verification: `cargo test -p envoy-config --lib` reported 74 passed, 0 failed (66 from Task 1 + 8 added). `cargo build --workspace --all-targets` clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. `cargo fmt --all -- --check` clean. The Docker-gated `differential::admin_ready_fixture` test fails on this machine for the pre-existing "Socket not found: /var/run/docker.sock" reason (same situation Task 1's PROGRESS already documented); not caused by this task.
- Deviations from PLAN:
  1. **Helper signature** — the PLAN's sketch used `ConfigError::ParseError(e.to_string())` to map the YAML error in `parse_then_validate`. That variant doesn't exist; the existing `ConfigError::Yaml(#[from] serde_yaml::Error)` covers it via `?`, so the helper's signature reduced to `Result<Bootstrap, ConfigError>` with a plain `?` on `serde_yaml::from_str`. Behavior is equivalent.
  2. **HCM dispatch arm not literal "replace the let-else"** — the PLAN's hand-off note pointed at the Task-1 defensive `let-else` mapping HCM-on-tcp_proxy-name to `MissingTypedConfig`. That arm was matched on `crate::TCP_PROXY_FILTER`, which is *not* the HCM filter name (`envoy.filters.network.http_connection_manager`). The HCM filter name was previously falling through to the wildcard `_` arm and returning `UnsupportedFilter`. Resolution: kept the TCP_PROXY arm intact (its defensive let-else still guards the genuine "tcp_proxy filter name with HCM typed_config under it" misconfiguration shape) and added a *new* `crate::HCM_FILTER => { … }` arm that delegates to `validate_hcm`. This matches the PLAN's pseudocode shape on lines 753–776 and the SPEC's "validator-already-rejects guarantees" enumeration on §6 d3.1; the TCP-proxy let-else is now correctly scoped to its actual case.
  3. **Added `HCM_FILTER` constant** — not explicitly enumerated in the PLAN's lib.rs delta but mirrors `TCP_PROXY_FILTER` precedent and is needed for the dispatch arm.
  4. **`EmptyDomains` test absent** — the PLAN's tally is "8 new tests" and explicitly enumerates the 8 added; `EmptyDomains` is exercised by the `validate_hcm` walk but not directly by a 04.1 test. Coverage is implicit via `rejects_empty_virtual_hosts` exercising the same outer `route_config.virtual_hosts.is_empty()` short-circuit. No drift from the PLAN's named-test list.
- ADRs: none in this task. ADR ledger head remains 20.

### Task 2 follow-up — review fixes (2026-04-27)
- Commit: 4e7c050
- Change: added rejects_empty_domains test (closes coverage gap on the EmptyDomains validator branch); converted validate_data_source's `requires` parameter from `&'static str` to a private `Required` enum, removing the runtime `unreachable!` fallback.
- Verification: cargo test -p envoy-config → 75 passed; clippy clean; fmt clean.

## Task 3 — fuzz corpus extension (2026-04-27)

- Commit: fe65bb0
- Change: added 2 HCM-shaped seeds to `crates/envoy-config/fuzz/corpus/parse_bootstrap/`: `hcm_direct_response_happy.yaml` (parse+validate Ok), `hcm_invalid_codec_type.yaml` (parse Ok / validate UnsupportedCodecType). The existing `parse_bootstrap` target picks them up automatically; `-max_total_time=30` budget unchanged per ADR-0010. Extended `crates/envoy-config/fuzz/.gitignore`'s allow-list with the two new filenames so they actually land under version control (the directory is `corpus/parse_bootstrap/*` ignored with per-seed `!`-overrides). Extended the hand-listed `fuzz_corpus_seeds_parse_or_reject_cleanly` corpus-walk test in `bootstrap.rs` to cover both seeds: `hcm_direct_response_happy.yaml` joined the parse-Ok group, `hcm_invalid_codec_type.yaml` joined the reject-cleanly group.
- Verification: `cargo test -p envoy-config` → 75 passed (test count unchanged — the corpus-walk test was already counted; only its enumeration grew). `cargo build --workspace --all-targets` clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. `cargo fmt --all -- --check` clean. Local cargo-fuzz smoke-run skipped: nightly toolchain present but `cargo-fuzz` not installed on this box; CI exercises the extended corpus on its `-max_total_time=30` budget.
- Deviations from PLAN:
  1. **`.gitignore` allow-list extension** — not enumerated in the PLAN's Step 1/2 list of files-to-create, but mandatory: `crates/envoy-config/fuzz/.gitignore` ignores `corpus/parse_bootstrap/*` with per-seed `!`-overrides, so without the two new `!corpus/parse_bootstrap/hcm_*.yaml` lines the seeds would be silently ignored by git and never reach CI. Added at the natural spot (end of the existing allow-list, before `artifacts/`).
  2. **bootstrap.rs corpus-walk extension** — the PLAN's Constraints section says "Do NOT modify any source files," but Step 3 explicitly says "If the test exists and enumerates seeds explicitly (a hand-listed allow-list), extend it to include the two new seeds." The test is hand-listed (verified in `bootstrap.rs` lines 1549–1584), so Step 3 applies and is the more specific instruction. The change is purely additive: one new entry in the parse-Ok loop, one in the reject-cleanly loop.
- ADRs: none in this task. ADR ledger head remains 20.

## Task 4 — envoy-http1 scaffold (2026-04-27)

- Commit: 37e074c
- Change: scaffolded `crates/envoy-http1/` with `Cargo.toml` (deps: envoy-config, envoy-cluster, envoy-listener, httparse, bytes, tokio with `net|io-util|macros|sync|time` features, thiserror v2, tracing; dev-dep: tokio with `rt-multi-thread|test-util`) and `src/lib.rs` (`#![forbid(unsafe_code)]` + crate-level docstring + 6 `mod` declarations: `pub mod codec/date/hcm/headers/response;` + `mod error;`). Created 6 stub-only module files each carrying a single doc comment naming the future-task that populates it. Added `"crates/envoy-http1",` to root `Cargo.toml`'s `[workspace] members` array in alphabetical position (between `envoy-config` and `envoy-listener`). The `envoy-cluster` and `tracing` deps are forward-looking for 04.3 and the HCM error paths respectively, per PLAN — locked in at scaffold time so subsequent tasks don't re-touch the manifest.
- Step 2 option chosen: **(b) Defer** the `pub use error::Http1Error;` re-export to Task 5. Option (a) would have required a `pub struct Http1Error;` stub with `#[allow(dead_code)]`; option (b) keeps the empty crate warning-free without any allow-attributes. lib.rs carries an inline comment block noting Task 5 will add the re-export and Tasks 8–10 will add codec/response/hcm re-exports.
- Verification: `cargo build -p envoy-http1` → clean (0.17–1.91s, 5 crates compiled including dep graph). `cargo build --workspace --all-targets` → clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` → clean. `cargo fmt --all -- --check` → clean (after one fix; see Deviations).
- Cargo.lock: touched (cargo build added the new `envoy-http1` package entry — 14 new lines, no version changes to existing deps). Included in this commit per PLAN ("if cargo build forces it, that's fine to include with the main commit but document in PROGRESS.md").
- Deviations from PLAN:
  1. **`mod` declaration order in `lib.rs`** — the PLAN's Step 2 sketch listed `pub mod codec; pub mod headers; pub mod date; pub mod response; pub mod hcm; mod error;` (logical/grouped order). rustfmt enforces alphabetical sort of `mod` items, so the as-committed order is `pub mod codec; pub mod date; mod error; pub mod hcm; pub mod headers; pub mod response;` (alphabetic, with the private `mod error` interleaved at its alphabetic slot). Caught by `cargo fmt --all -- --check` failing initially; resorted to match rustfmt. No semantic change; the comment block referencing Task 5 / Tasks 8–10 re-exports is preserved verbatim below the module list.
- ADRs: none in this task. ADR ledger head remains 20.

## Task 5 — envoy-http1::error (2026-04-27)

- Commit: 4f1814f
- Change: replaced the `error.rs` stub with the `Http1Error` enum carrying 6 variants (`MalformedRequestLine`, `MalformedHeader`, `HeadersTooLarge { cap: usize }`, `BodyTooLarge { cap: usize }`, `UnexpectedEof`, `Io { source: std::io::Error }`) per SPEC §3 D1. Each variant has its `#[error(...)]` display string verbatim from the PLAN. Added an explicit `impl From<std::io::Error> for Http1Error` (mapping into the `Io` variant) for `?`-ergonomic propagation in Tasks 8–10. Added `pub use error::Http1Error;` to `lib.rs` (Task 4 deferred this re-export; the comment-block reminder for Tasks 8–10 codec/response/hcm re-exports is preserved verbatim on the line below).
- Verification: `cargo build -p envoy-http1` → clean (0.20s, only envoy-http1 recompiled). `cargo clippy --workspace --all-targets --all-features -- -D warnings` → clean. `cargo fmt --all -- --check` → clean. No `dead_code` warnings fired despite the variants being unused at this point — `pub`-visible enum variants are not subject to dead-code lint, so no `#[allow(dead_code)]` attribute was needed (the PLAN's Step 4 contingency was not triggered).
- Deviations from PLAN: chose the explicit `impl From<std::io::Error>` form over thiserror's `#[from]` shorthand. The PLAN allowed either; explicit-impl was kept because clippy did not flag the manual impl as redundant (the `#[from]` substitution clause was contingent on clippy complaining), and the explicit form makes the conversion site obvious to a reader. Behavior is identical either way.
- ADRs: none in this task. ADR ledger head remains 20.

## Task 6 — envoy-http1::headers (2026-04-27)

- Commit: 7f3af73
- Change: populated `headers.rs` with 6 canonical-name constants (`HOST`, `CONTENT_LENGTH`, `CONNECTION`, `SERVER`, `DATE`, `CONTENT_TYPE`, all lowercase per HTTP/1.1 §3.2 case-insensitivity convention) + `find_header<'a>(headers: &'a [(String, String)], name: &str) -> Option<&'a str>` case-insensitive lookup helper using `eq_ignore_ascii_case` + 2 unit tests (`find_header_is_case_insensitive`, `find_header_returns_none_on_missing`). Per PLAN, did not add a `pub use headers::*` or any re-export to `lib.rs` — downstream code uses the `envoy_http1::headers::find_header` form, so the module stays accessible via its public path without surfacing names at crate root.
- Verification: `cargo test -p envoy-http1 -- find_header_is_case_insensitive find_header_returns_none_on_missing` → 2 passed, 0 failed (delta +2 from Task 5's baseline of 0). `cargo build --workspace --all-targets` clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. `cargo fmt --all -- --check` clean. No `dead_code` warnings fired on the 6 unused `pub const`s — `pub`-visible items are exempt from the lint at the workspace-internal level — so no `#[allow(dead_code)]` attribute was added (PLAN's Step 3 contingency was not triggered; Task 10 will consume the constants).
- Deviations from PLAN: none.
- ADRs: none in this task. ADR ledger head remains 20.

## Task 7 — envoy-http1::date (2026-04-27)

- Commit: f65aaa2
- Change: replaced the `date.rs` stub with `format_imf_fixdate(t: SystemTime) -> String` per RFC 7231 §7.1.1.1, hand-rolled via Howard Hinnant's `civil_from_days` algorithm (no external dep — SPEC §3 D1 locks the hand-rolled approach so 04.1 doesn't pre-emptively land an `httpdate` ADR). ~30 LoC. Pre-epoch times defensively clamp to `0` via `duration_since(UNIX_EPOCH).map(...).unwrap_or(0)`. Added 2 unit tests pinning the canonical 1994-11-06 example from the RFC (`Sun, 06 Nov 1994 08:49:37 GMT`) and the Unix epoch boundary (`Thu, 01 Jan 1970 00:00:00 GMT`).
- Verification: `cargo test -p envoy-http1 -- formats_canonical_imf_fixdate formats_unix_epoch` → 2 passed, 0 failed (delta +2 from Task 6's running total). Cross-checked the canonical example via `python3 -c 'import datetime; print(datetime.datetime.utcfromtimestamp(784111777).strftime("%a, %d %b %Y %H:%M:%S GMT"))'` → `Sun, 06 Nov 1994 08:49:37 GMT` (matches; deprecation warning on `utcfromtimestamp` is benign and unrelated). `cargo build --workspace --all-targets` clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. `cargo fmt --all -- --check` clean (after one fix; see Deviations).
- Clippy attribute: applied `#[allow(clippy::cast_possible_truncation, clippy::cast_sign_loss)]` at function level per PLAN's Step 3 contingency. The cast chain in Hinnant's algorithm (`secs / 86_400` → `i64`, `yoe as i64`, `(m - 1) as usize`) is deliberate — the algorithm operates on a mix of i64 (era arithmetic with negative-z handling) and u64 (day-of-era index), and the truncations are bounded by domain invariants (`m ∈ [1,12]`, `weekday_idx ∈ [0,6]`, etc.). Suppressed without per-cast `#[allow]` noise.
- Deviations from PLAN:
  1. **rustfmt reformatted the second `assert_eq!`** — the PLAN's stub kept `assert_eq!(format_imf_fixdate(UNIX_EPOCH), "Thu, 01 Jan 1970 00:00:00 GMT");` on one line. rustfmt's line-width heuristic split it into 4 lines (`assert_eq!(\n  format_imf_fixdate(UNIX_EPOCH),\n  "Thu, 01 Jan 1970 00:00:00 GMT"\n);`). Caught by `cargo fmt --all -- --check` after the initial commit attempt; applied the reformat before committing. No semantic change.
  2. **Month-names array on a single line** — rustfmt collapsed the PLAN's 2-row layout into one row (4-column-wide formatter). No semantic change.
- ADRs: none in this task. ADR ledger head remains 20.

## Task 8 — envoy-http1::codec (2026-04-27)

- Commit: 06fa429
- Change: replaced the `codec.rs` stub with `Http1Codec` (zero-sized stateless wrapper), `Request` value type (`method`/`path`/`version`/`headers`/`bytes_consumed`), and `HttpVersion` enum (`Http10`/`Http11`) per SPEC §3 D1. Implemented `Http1Codec::parse_request(buf: &[u8]) -> Result<Option<Request>, Http1Error>` as a thin wrapper over `httparse::Request::parse` with a fixed-size `[EMPTY_HEADER; 64]` storage stack-allocated per call (MAX_HEADERS = 64). On `Status::Complete(n)` the codec materializes owned `String`-pair headers (filtering out unused empty slots); on `Status::Partial` it enforces the 8 KiB headers cap (per SPEC §6 signpost 14 + parent-SPEC §3 cross-sub-phase rule "headers cap matches phase-02.2 admin tightening per phase-01 REVIEW I4") by returning `HeadersTooLarge { cap: 8192 }` if the buffer has grown past the cap; otherwise returns `Ok(None)` to signal "feed more bytes". Mapped all 7 `httparse::Error` variants exhaustively: `TooManyHeaders` → `HeadersTooLarge { cap: 8192 }`; `HeaderName | HeaderValue | NewLine | Status` → `MalformedHeader`; `Token | Version` → `MalformedRequestLine`. Added 5 unit tests per the PLAN's verbatim names: `parses_get_root_with_host`, `returns_none_on_partial_request_line`, `returns_err_on_malformed_request_line` (accepting either `MalformedRequestLine` or `MalformedHeader` since the `GET\r\n\r\n` shape's specific httparse failure mode isn't load-bearing), `enforces_headers_cap` (200 X-Pad rows ≈ 9 KiB, no terminal CRLF so httparse stays Partial), `preserves_header_emission_order_and_case`. Re-exported `Http1Codec`, `Request`, `HttpVersion` from `lib.rs`; updated the deferred-re-export comment to "Tasks 9–10 will add re-exports for response/hcm public types."
- httparse error matching: verified exhaustive against the registry source at `~/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/httparse-1.10.1/src/lib.rs:102-117` — the 7 variants are `HeaderName`, `HeaderValue`, `NewLine`, `Status`, `Token`, `TooManyHeaders`, `Version`. All 7 are covered; the SPEC writeup's mention of a possible `IpV6Address` variant is not present in this httparse version (the codebase uses 1.10.x via the workspace lock).
- Verification: `cargo test -p envoy-http1 -- parses_get_root_with_host returns_none_on_partial_request_line returns_err_on_malformed_request_line enforces_headers_cap preserves_header_emission_order_and_case` → 5 passed, 0 failed (delta +5 from Task 7's running total of 4 envoy-http1 tests). Followed TDD red→green: first staged a stub returning `unimplemented!()` and confirmed all 5 tests panicked, then landed the real impl and confirmed all 5 green. `cargo build --workspace --all-targets` clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. `cargo fmt --all -- --check` clean.
- Re-export ordering: rustfmt sorts use-list items alphabetically — wrote `pub use codec::{Http1Codec, HttpVersion, Request};` (not the PLAN's literal `{Http1Codec, Request, HttpVersion}` order) so `cargo fmt --check` stays green. No semantic change.
- Deviations from PLAN: none beyond the `use`-list alphabetic-sort tweak above.
- ADRs: none in this task. ADR ledger head remains 20.

## Task 9 — envoy-http1::response (2026-04-27)

- Commit: 200a321
- Change: replaced the `response.rs` stub with the `Response` value type (`status: u16`, `reason: Option<&'static str>`, `headers: Vec<(String, String)>`, `body: Bytes`; `#[derive(Debug, Clone, PartialEq, Eq)]`), the `Http1Response` zero-sized writer with `pub async fn write_to<W: AsyncWrite + Unpin>(resp: &Response, w: &mut W) -> Result<(), Http1Error>`, and a private `canonical_reason(status: u16) -> &'static str` table covering 24 RFC 7231 §6.1 codes with `"OK"` as the unknown-code fallback. Per SPEC §3 D1, the writer is dumb: it serializes the status line + headers in caller-supplied emission order + CRLF + body, with no auto-computation of `Content-Length` (the HCM `synth_*` helpers in Task 10 own that). Pre-sizes the buffer with a heuristic (`64 + Σ(name+value+4) + body.len()`) for fewer reallocs. Writes via a single `write_all` + `flush` so the bytes hit the wire as one logical send. Added 2 unit tests per PLAN-verbatim names: `writes_status_line_headers_body` (200 OK happy path with 5-header set in the canonical synth_* order) and `writes_204_with_no_body` (covers the no-body branch and the canonical-reason table's 204 entry). Added `pub use response::{Http1Response, Response};` to `lib.rs`; updated the deferred-re-export comment to "Task 10 will add re-exports for hcm public types."
- Multi-line byte-literal: PLAN's Step 1 sketch used `\` line-continuation across indented source lines for the expected-bytes literal, which would absorb the indentation whitespace into the literal. Side-stepped by writing the expected bytes as a single-line `b"..."` (the PLAN's own follow-up note suggested this fix). No special escapes needed; both tests pass on the first run with no byte-mismatch.
- Verification: `cargo test -p envoy-http1 --lib response::` → 2 passed, 0 failed (delta +2 from Task 8's running total: 9 → 11 tests in envoy-http1). `cargo build --workspace --all-targets` clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. `cargo fmt --all -- --check` clean (after two fmt fixes; see Deviations).
- Deviations from PLAN:
  1. **rustfmt reformat — `.expect("write")` in `write_to_vec` helper** — rustfmt split `Http1Response::write_to(resp, &mut buf).await.expect("write");` across 3 lines (`Http1Response::write_to(resp, &mut buf)\n    .await\n    .expect("write");`). Caught by initial `cargo fmt --check`; applied the reformat. No semantic change.
  2. **rustfmt reformat — `Response` field comments** — rustfmt re-aligned the trailing `// 100..=599` and `// canonical reason …` comments after the `pub status: u16,` and `pub reason: Option<&'static str>,` fields by one space (because the longest line in the alignment group is `pub headers: Vec<(String, String)>,`, not the original PLAN sketch's longer comment alignment). Applied the reformat. No semantic change.
  3. **Single-line expected-bytes literal** — used a single-line `b"..."` for both expected-bytes assertions instead of the PLAN sketch's indented multi-line form, per the PLAN's own follow-up note flagging the indentation-absorption bug.
- ADRs: none in this task. ADR ledger head remains 20.

## Task 10 — envoy-http1::hcm (2026-04-27)

- Commit: 044dd58
- Change: implemented HCMConfig + HCM + serve_connection (per-connection state machine: read into BytesMut / parse via Http1Codec / route walk / dispatch / write / lifecycle / 5s idle read timeout / body drain / chunked-rejection-via-501) + build_response (Host validation → VH walk → route walk → hardcoded router-filter call site over `direct_response`) + synth_400/synth_404/synth_501/synth_direct_response helpers. The 5 hardcoded headers (server, date, content-length, content-type, connection) are emitted in that exact order by both synth helpers per SPEC §3 D1. Added 6 unit tests per PLAN-verbatim names: `direct_response_returns_status_and_body`, `host_match_strips_port`, `first_match_wins_on_routes`, `missing_host_returns_400`, `unknown_route_returns_404`, `connection_close_closes_socket`. Tests share a `drive(config, req_bytes) -> Vec<u8>` helper that spins a `TcpListener::bind("127.0.0.1:0")`, accepts one connection, runs `serve_connection`, and reads the response to EOF over an in-process socket pair. Added `pub use hcm::{HCM, HCMConfig};` to `lib.rs`.
- Clone helper retained: envoy-config types (`RouteConfiguration`, `VirtualHost`, `Route`, `RouteMatch`, `DirectResponse`, `DataSource`) do not derive `Clone`, so `clone_route_config` was kept in `hcm.rs` (per the PLAN's note this helper retires once those derives are added).
- Vestigial helpers removed from PLAN draft: the `let _ = read_n;` and `let _ = &mut resp;` lines from PLAN.md lines 2217 and 2246 were dropped — `read_n` is no longer bound (the inner timeout match arms `continue`/`return` directly), and `resp` is consumed unmodified by `Http1Response::write_to`. Both deletions clippy-clean (would otherwise have tripped `let_underscore_drop`).
- Verification: `cargo test -p envoy-http1` → 17 passed, 0 failed (Task 6/7/8/9/10 = 2/2/5/2/6 = 17 unit tests, matching the SPEC §3 D1 tally). `cargo build --workspace --all-targets` clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. `cargo fmt --all -- --check` clean (after one rustfmt-applied reformat; see Deviations).
- Deviations from PLAN:
  1. **Removed two vestigial `let _ = …` lines** — PLAN sketch lines 2217 and 2246 (documented in Constraints as "vestigial — remove if clippy flags"). Removed pre-emptively rather than after a clippy hit; no behavioral change.
  2. **rustfmt reformat — `tokio::time::timeout(...).await { … }`** — rustfmt collapsed the two-line `match … .await\n{` form back onto one line (`match … .await {`) at the inner read site of `serve_connection`. Applied the reformat. No semantic change.
  3. **rustfmt reformat — multi-line `assert!(resp_str.contains(...), "…: {resp_str}")` calls** — rustfmt re-collapsed several test assertions back onto a single line where they fit within the column limit. Applied. No semantic change.
- ADRs: none in this task. ADR ledger head remains 20.

### Task 10 follow-up — review fixes (2026-04-27)
- Commit: a6f7b5e
- Change: applied 4 review fixes to `crates/envoy-http1/src/hcm.rs`:
  1. **`tracing::warn!` on 400/404/501 paths** — added structured-field warns at the four error call sites (missing/empty-Host, no-VH-match, no-route-match, chunked-501). Closes the PLAN.md line 1151 deviation ("04.1's HCM uses `tracing::warn!` only on the 400/404/501 error paths") — the dep was already in `Cargo.toml` but had zero call sites until this commit. Placed at call sites in `build_response`/`serve_connection` rather than inside the synth helpers so each warn carries request context (method/path; host where available).
  2. **Empty Host header → 400** — combined the missing-Host and empty-Host (`Host: \r\n` → `Some("")`) cases into one `Some(h) if !h.is_empty() => h, _ => …` arm, sharing one `tracing::warn!` site. Same RFC §5.4 violation; differential gap (Task 16 would otherwise diverge against Envoy's behavior on empty Host).
  3. **`keep_alive_serves_two_requests` test** — added a 7th HCM test that pipes two requests over one connection (req1 default keep-alive, req2 `Connection: close` to trigger EOF), asserts `"HTTP/1.1 200 OK\r\n"` appears twice in the response stream. Exercises the parse-then-read loop's already-buffered-pipelined-bytes path on the second iteration — previously untested by the 6 PLAN-named tests.
  4. **`chunked_request_rejected_with_501` test** — added an 8th HCM test asserting `Transfer-Encoding: chunked` requests get `501 Not Implemented`. The 501 branch was in spec but untested.
- Verification: `cargo test -p envoy-http1` → 19 passed, 0 failed (17 → 19, +2). `cargo build --workspace --all-targets` clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. `cargo fmt --all -- --check` clean.
- Known divergences from RFC / Envoy that 04.1 deliberately accepts (carried forward, surfaced here per review):
  1. **HTTP/1.0 + explicit `Connection: keep-alive` → close** — matches PLAN reference; known divergence from RFC 7230 §6.3 + Envoy. 04.1's `close` decision in `serve_connection` is `Connection: close OR HTTP/1.0`, ignoring the explicit-keep-alive opt-in.
  2. **Idle-timeout-during-body-drain returns `Ok(())` silent close** — matches PLAN reference; the 5s idle-read timeout in the body-drain loop returns `Ok(())` rather than surfacing an error, dropping the pending response. Flag for revisit in 04.2 or hardening pass.
  3. **Non-chunked `Transfer-Encoding` values fall through to Content-Length framing** — known narrow scope; SPEC scopes only `chunked` for the 501 branch. Other `Transfer-Encoding` values (e.g. `identity`, `gzip`) are not detected and fall through to `Content-Length`-framed read.
  4. **`clone_route_config` is a hand-walk rather than `Clone`-derive on envoy-config types** — chose the path that keeps Task 10's diff scoped to `envoy-http1`. The helper retires once envoy-config types derive `Clone`.
- Deviations from review prescription: none — all four code fixes applied as suggested; both new tests written verbatim from the prompt's sketches.
- ADRs: none in this follow-up. ADR ledger head remains 20.

## Task 11 — envoy-bin HCM dispatch (2026-04-27)

- Commit: 39182ce
- Change: added `envoy-http1 = { path = "../envoy-http1" }` path-dep to `crates/envoy-bin/Cargo.toml` (alphabetic order). Added new `envoy_config::HCM_FILTER => { … }` match arm in `crates/envoy-bin/src/main.rs::run`'s per-listener filter dispatch, sibling of the existing `TCP_PROXY_FILTER` arm. The HCM arm extracts the `TypedConfig::HttpConnectionManager(hcm_cfg)` via `let-else` (mirroring the TcpProxy arm), builds `Arc<HCMConfig>` once via `HCMConfig::from_config(hcm_cfg)?`, wraps in `Arc<HCM>` typed as `Arc<dyn ConnectionHandler>`, hands to `envoy_listener::Listener::bind`, and emits `tracing::info!` with `addr` + `stat_prefix` on successful bind. Spawns the serve loop into the existing per-listener `JoinSet` (mirrors the TcpProxy arm).
- TLS-construction refactor (option B per the prompt): factored the 03.2 three-way downstream-TLS construction (plaintext / single-cert via `DownstreamTls::from_context` / multi-cert via `DownstreamTls::from_listener`) out of the TcpProxy arm into a free fn `build_downstream_tls_for_listener(listener_cfg) -> Result<Option<Arc<DownstreamTls>>>` placed between `run` and `shutdown_signal`. The TcpProxy arm now calls the helper in place of the inline ~37-line block. Verification confirms no regression on phases 03.1 / 03.2 fixtures (`tls_downstream_round_trips_through_envoy_bin` + `tls_sni_multi_cert_dispatch` + `tls_upstream_round_trip` all green through the new helper). Documented as a minor scope expansion per the prompt's explicit recommendation.
- HCM+TLS handling (deviation from option B's pure-helper-share path): the existing `tls_handler::TlsAcceptingHandler.inner` field is typed as `Arc<TcpProxy>` (not `Arc<dyn ConnectionHandler>`) — by design, per the inherent-generic `handle::<S>` precedent that keeps `ConnectionHandler` object-safe with a concrete `TcpStream` (per `tls_handler.rs` doc comment + parent SPEC §6 signposts 2/3). Wrapping HCM in TLS therefore requires a separate adapter generalization (or a parallel `TlsAcceptingHcmHandler`), which is deliberately out of scope for Task 11 and deferred to phase 05+ per SPEC §3 D4 ("the wiring is one line of dispatch and avoids an `unreachable!()` ahead of phase 05's first such fixture"). Took the prompt's option C in spirit for the wrap step: the HCM arm detects HCM+TLS via the shared `build_downstream_tls_for_listener` helper and `anyhow::bail!`s with a clear "not supported in phase 04.x; TlsAcceptingHandler is currently TcpProxy-only" message rather than silently serving plaintext on a TLS port. The plan-and-SPEC's "wired for forward-compat" intent is preserved by the bail-with-clear-message rather than a wrap-that-doesn't-typecheck. No 04.x fixture exercises this branch (the SPEC explicitly says "no 04.x fixture combines HTTP/1.1 + TLS").
- Verification: `cargo build -p envoy-bin` → clean. `cargo test -p envoy-bin` → 24 tests passed (19 unit + 5 integration: `admin_only`, `tcp_proxy`, `tls_downstream`, `tls_sni`, `tls_upstream` — all green through the refactored helper). `cargo build --workspace --all-targets` → clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` → clean. `cargo fmt --all -- --check` → clean (after one rustfmt-applied reformat; see Deviations).
- LoC delta: `crates/envoy-bin/Cargo.toml` +1; `crates/envoy-bin/src/main.rs` +103/-44 net (+59). `Cargo.lock` +1 (envoy-http1 listed under envoy-bin's deps).
- Deviations from PLAN:
  1. **Plan's pseudocode is variant-based; actual code is name-based.** PLAN sketched `match typed_config { Some(TypedConfig::HttpConnectionManager(_)) => … }`. Actual envoy-bin code dispatches on `filter.name.as_str()` against `envoy_config::HCM_FILTER` / `TCP_PROXY_FILTER` / `ECHO_FILTER` constants. Followed the actual shape per the prompt's explicit "What to do" guidance.
  2. **TLS construction factored to helper (option B, scope expansion).** Documented above. Benefits both arms; ~37 lines of inline logic → one helper call site each. No behavioral change on the TcpProxy path.
  3. **HCM+TLS detect-and-bail rather than wrap (option C in spirit for the wrap step).** Documented above. The wrap-style wiring sketched in the plan would not typecheck against the current `TlsAcceptingHandler` shape; bail-with-clear-message preserves the forward-compat intent while staying within Task 11's "no `tls_handler.rs` changes" constraint.
  4. **rustfmt reformat — `let hcm_config = …` block-into-single-line + helper `anyhow::bail!` argument.** rustfmt collapsed the multi-line `Arc::new(envoy_http1::HCM { config: hcm_config })` to one line and the helper's `anyhow::bail!("…",)` to a single line. Applied; no semantic change.
- ADRs: none in this task. ADR ledger head remains 20.

## Task 12 — envoy-bin integration test (2026-04-27)

- Commit: 3dc2bc3
- Change: added `crates/envoy-bin/tests/http1_direct_response.rs` — a Rust-native, no-Docker integration test that spawns `envoy-bin` against an inline HCM-direct_response YAML config (single listener / single filter chain / single VH `domains: ["*"]` / single route `prefix: "/"` → `direct_response { status: 200, body: { inline_string: "ok\n" } }` / one `envoy.filters.http.router` http_filter / `clusters: []`). The test drives a single `GET /healthz HTTP/1.1` over a TCP socket, parses with `httparse::Response::parse` (looped read until headers complete and `total >= headers_end + content_length`), and asserts: (i) status code 200, (ii) the 5 required response headers (`server`, `date`, `content-length`, `content-type`, `connection`) present via case-insensitive name comparison, (iii) `content-length: 3`, (iv) body bytes equal `"ok\n"`. Mirrors phase 02.2's `tests/tcp_proxy.rs` for the binary-locate (`env!("CARGO_BIN_EXE_envoy-bin")`) + retry-loop pattern; copied ~10 LoC of `reserve_port` + `wait_ready` per the prompt's "do not add `tests/common/mod.rs` for one shared helper" constraint. Subprocess is killed unconditionally on test exit (`kill_on_drop(true)` + explicit `child.kill().await; child.wait().await`); captured stderr is dumped to test stderr only on failure to aid post-mortem.
- No dev-deps added. `httparse = "1"` and `anyhow = "1"` are already runtime deps of `envoy-bin` (reachable from `tests/` directly without re-listing in `[dev-dependencies]`); `tempfile = "3"` is already in `[dev-dependencies]`. CLI flag confirmed `-c <path>` per `crates/envoy-bin/src/argv.rs`.
- Verification: `cargo test -p envoy-bin --test http1_direct_response` → 1 passed, 0 failed (PASS on first run, no flake observed across 2 runs). `cargo test -p envoy-bin` → 6/6 integration tests pass (`admin_only`, `http1_direct_response`, `tcp_proxy`, `tls_downstream`, `tls_sni`, `tls_upstream`); 19 envoy-http1 unit + library tests still green via the runtime path. Test count delta: 5 → 6 envoy-bin integration tests (+1). `cargo build --workspace --all-targets` clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean (after collapsing one nested `if outcome.is_err() { if let Some(...) = ... { … } }` per `clippy::collapsible_if`). `cargo fmt --all -- --check` clean (after one rustfmt-applied reformat of the `tokio::time::timeout(...)` line).
- LoC delta: `crates/envoy-bin/tests/http1_direct_response.rs` +209 (new file). No other source / Cargo.toml changes.
- Deviations from PLAN:
  1. **PLAN draft used `CARGO_BIN_EXE_envoy-rust`; actual is `CARGO_BIN_EXE_envoy-bin`.** Per the prompt's "Two execution-time fixups likely needed" note. Crate's `[[bin]]` is named `envoy-bin`, so cargo exposes the env var under that name. Fixed at write time.
  2. **PLAN draft used `Stdio::piped()` on stdout + `Stdio::piped()` on stderr.** Used `Stdio::null()` on stdout (not consumed by the test) and `Stdio::piped()` on stderr (only consumed on failure). Avoids back-pressure on the subprocess's stdout pipe if the test stalls past its read budget.
  3. **rustfmt reformat — `tokio::time::timeout(...)` argument layout.** rustfmt collapsed `tokio::time::timeout(\n    Duration::from_secs(5),\n    stream.read(...),\n)` onto one line that fits within the column limit, then re-broke the `.map_err(|_| { ... })??` continuation across lines. Applied; no semantic change.
  4. **clippy collapse — `if outcome.is_err() { if let Some(mut err_pipe) = ... }` → `if outcome.is_err() && let Some(mut err_pipe) = ...`.** Required by `clippy::collapsible_if` under `-D warnings` on rust 1.95. Uses the let-chain syntax that's stable as of edition 2024 (the crate is `edition = "2024"` per `Cargo.toml`). Same control flow.
- ADRs: none in this task. ADR ledger head remains 20.

## Task 13 — differential harness Http1 grammar (2026-04-27)

- Commit: fb2e9fe
- Change: extended `tests/differential/src/lib.rs` with the HTTP/1.1 driver grammar ahead of Task 14's async helper. Added `Driver::Http1 { method, path, host, expected_status?, expected_body?, expected_headers? }` variant (tag-dispatched as `kind: http1` via the existing serde attributes on `Driver`); `Http1Method::Get` enum (with `as_str() -> &'static str` returning `"GET"`); `Http1BodyRule::ByteExact { bytes: Vec<u8> }` (tag-dispatched on `kind`); `Http1HeaderRule::SetEqualModuloAllowList` unit variant; `AllowMode::NameRequired` Copy enum; `HEADER_ALLOW_LIST: &[(&str, AllowMode)]` const seeded with `("server", NameRequired)` and `("date", NameRequired)` per BEHAVIOR_CONTRACT (Task 15 populates the contract table itself); `DriveHttp1Result { status, headers, body }` struct (consumed by Task 14's `drive_http1`); and the `diff_headers(envoy, envoy_rust, allow_list) -> Result<()>` helper implementing set-equal-modulo-allow-list (case-insensitive name set equality + value-exact match for any name not on the allow-list). 3 unit tests on `diff_headers` per PLAN-verbatim names: `diff_headers_passes_set_equal_modulo_allow_list` (server+date values differ but both allow-listed → pass), `diff_headers_fails_on_value_diff_outside_allow_list` (content-length value mismatch → fail), `diff_headers_fails_on_name_set_diff` (envoy emits x-foo, envoy-rust does not → fail). The `port_key` match in `run_fixture` was extended to map `Driver::Http1` → `"PORT"` (HCM listener uses the normal listener port, not admin port). The dispatch `match` was given a placeholder `Driver::Http1` arm that performs the standard subject shutdown + upstream drop and then `bail!`s with "Driver::Http1 dispatch is not implemented yet (lands in phase 04.1 Task 14)" — chosen over `unreachable!` / `todo!` so a fixture rendered with `kind: http1` against a pre-Task-14 build surfaces a clear error rather than panic or silent pass. Task 14 replaces this stub with the real `drive_http1` + equivalence flow.
- httparse: already present at `tests/differential/Cargo.toml:14` under `[dependencies]` (used by `drive_http_get`); no dev-dep addition needed. The PLAN's "skip if already present" branch fires.
- Verification: `cargo test -p differential --lib -- diff_headers_passes_set_equal_modulo_allow_list diff_headers_fails_on_value_diff_outside_allow_list diff_headers_fails_on_name_set_diff` → 3 passed, 0 failed. `cargo test -p differential --lib` → 46 passed, 0 failed, 1 ignored (was 43 → 46, delta +3). `cargo build --workspace --all-targets` clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. `cargo fmt --all -- --check` clean (after one rustfmt-applied reformat of a long `vec![]` row in `diff_headers_passes_set_equal_modulo_allow_list`; see Deviations).
- LoC delta: `tests/differential/src/lib.rs` +185/-1 net (+184). No other source / Cargo.toml changes.
- Deviations from PLAN:
  1. **Renamed BodyRule/HeaderRule/HttpMethod → Http1BodyRule/Http1HeaderRule/Http1Method to avoid collision with the existing `BodyRule` enum used by `Equivalence::response_body`.** The existing `BodyRule` is a unit-variant enum (`ByteExact`) representing an equivalence rule (envoy ↔ envoy-rust); the new `Http1BodyRule` is a struct-variant enum (`ByteExact { bytes }`) representing a fixture's expected body bytes. Different semantics, same name → forced rename. The PLAN's draft used unqualified names; this collision wasn't anticipated at plan-write time. The new types are scoped to the Http1 driver path; the existing `BodyRule` is unchanged. Per the prompt's "option A — rename" recommendation. The 3 test names themselves were unaffected (they reference `diff_headers` + `HEADER_ALLOW_LIST`, not the renamed types).
  2. **Added `Driver::Http1` placeholder dispatch arm in `run_fixture`'s match (bail-with-clear-message).** Strictly speaking the prompt's "Do NOT add … `run_fixture` Driver::Http1 dispatch — those are Task 14" forbids the *real* dispatch arm, but adding the variant makes the existing match non-exhaustive and breaks the build. Chose a stub arm that bails with a clear "lands in Task 14" message rather than `unreachable!` / `todo!` (would panic) or `_ => {}` (would silently pass). Task 14 replaces this stub with the real `drive_http1` flow.
  3. **`port_key` match extended to map `Driver::Http1` → `"PORT"`.** Same compile-driven necessity as point 2. HCM-direct-response fixtures listen on a normal listener port, not admin port, so `"PORT"` matches the existing `Driver::TcpEcho` / `TlsTcp` branches.
  4. **rustfmt reformat — `("date", "Mon, 07 Nov ...")` row in `diff_headers_passes_set_equal_modulo_allow_list`.** rustfmt broke the long-tuple-literal row across 4 lines (`("date".to_string(), "Mon, 07 Nov 1994 12:00:00 GMT".to_string())` exceeded the column limit). Applied; no semantic change.
- ADRs: none in this task. ADR ledger head remains 20.

## Task 14 — differential harness drive_http1 + run_fixture dispatch (2026-04-27)

- Commit: 60fc624
- Change: added the `drive_http1(addr, method, path, host) -> Result<DriveHttp1Result>` async helper (open `TcpStream`, write request line + `Host:` + `Connection: close`, loop-read into `Vec<u8>` until `httparse::Response::parse` returns `Status::Complete(headers_end)`, capture every header value as UTF-8 `String` in order, parse `Content-Length` (default 0), then read remaining body bytes until `buf.len() >= headers_end + content_length`; returns `DriveHttp1Result { status, headers, body }`). Each socket read runs under a 5s `tokio::time::timeout`; peer EOF before headers complete or before the declared body is consumed surfaces as `Err` with diagnostics. Replaced Task 13's placeholder `Driver::Http1 { .. } => bail!("lands in phase 04.1 Task 14")` arm in `run_fixture` with the real dispatch: drive both `upstream_addr` and `subject_addr`, then apply the 3-axis equivalence cascade — (i) `response_status: exact` enforces `upstream.status == subject.status` (envoy ↔ envoy-rust); (ii) per-driver `expected_status: Some(es)` enforces `upstream.status == es && subject.status == es` (each side independently); (iii) `response_body: byte_exact` enforces `upstream.body == subject.body`; (iv) per-driver `expected_body: Http1BodyRule::ByteExact { bytes }` enforces `upstream.body == bytes && subject.body == bytes`; (v) per-driver `expected_headers: Http1HeaderRule::SetEqualModuloAllowList` calls `diff_headers(&upstream.headers, &subject.headers, HEADER_ALLOW_LIST)` for set-equal-modulo-allow-list. The `Equivalence` struct (lib.rs:87-94) was *not* extended with a `response_headers` field — the per-driver `expected_headers` is sufficient for fixture 0007 (Task 16) per PLAN's secondary-path note; documented as an asymmetry-against-status/body for an eventual 04.x cleanup.
- Subject shutdown + upstream drop happen *before* the equivalence checks (mirrors the existing `Driver::TcpEcho` / `Driver::HttpGet` arms — drives both, tears down both, *then* asserts), so a failed equivalence check still produces a clean container/subprocess teardown via Drop.
- No new unit tests (per PLAN: drive_http1 is exercised end-to-end by Task 16's Docker-gated fixture 0007 test). Task 13's 3 `diff_headers` tests cover the header-diff path.
- Verification: `cargo test -p differential --lib` → 46 passed, 0 failed, 1 ignored (Docker-gated `starts_upstream_envoy_and_exposes_host_port`); test count unchanged from Task 13. `cargo build --workspace --all-targets` clean. `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. `cargo fmt --all -- --check` clean (after one rustfmt-applied reformat of the inline `bail!` argument lists and the multi-line `diff_headers(...)` call; see Deviations).
- LoC delta: `tests/differential/src/lib.rs` +189/-6 net (+183). No other source / Cargo.toml changes.
- Deviations from PLAN:
  1. **Used the renamed Task 13 types throughout (`Http1Method`, `Http1BodyRule`, `Http1HeaderRule`).** The PLAN's pseudocode referenced `HttpMethod` / `BodyRule` / `HeaderRule`; the rename was already documented in Task 13's deviations and is carried forward here. The dispatch arm uses `Http1BodyRule::ByteExact { bytes }` (struct-variant) for the per-driver expected-body anchor and `BodyRule::ByteExact` (unit-variant on `Equivalence::response_body`) for the envoy ↔ envoy-rust diff.
  2. **`drive_http1`'s `tokio::time::timeout` wraps each `stream.read` directly rather than wrapping a small async block.** The PLAN's draft wrapped a closure that re-extended `buf` inside the closure, which the borrow checker rejects (the closure borrows `&mut buf` for the timeout duration, but `buf` is also borrowed by `httparse::Response::parse` later in the loop body). Restructured to read into a stack-allocated `[0u8; 4096]` chunk under the timeout, then extend `buf` *outside* the timeout. Same I/O behavior, no double-borrow. The body-read loop also uses this pattern.
  3. **Equivalence struct unchanged.** PLAN noted "if a `response_headers` field exists on `Equivalence`, also support that path" — it does not; only the per-driver `expected_headers` path is wired. Documented above.
  4. **rustfmt reformats — `bail!(...)` argument layout + multi-line `diff_headers(...)` call.** rustfmt collapsed several `bail!("..."\n  upstream: {}\n  subject:  {}", ...)` invocations onto compact multi-line forms, and re-broke the `diff_headers(&upstream_resp.headers, &subject_resp.headers, HEADER_ALLOW_LIST)` call across 5 lines. Applied; no semantic change.
- ADRs: none in this task. ADR ledger head remains 20.
- Open concern: drive_http1 has no per-function unit test in this task (PLAN-mandated). It is unreached by the existing 46 tests and only becomes covered by Task 16's Docker-gated fixture 0007 test. A regression in drive_http1 between Task 14 and Task 16 would not be caught by `cargo test -p differential --lib`. This is the PLAN's intentional cadence (the helper's behavior is a thin wrapper around well-tested deps — `tokio` I/O + `httparse`); the risk is bounded.

## Task 15 — BEHAVIOR_CONTRACT.md Header allow-list (2026-04-27)

- Commit: 43c4538
- Change: replaced `_(empty; populated starting phase 04)_` placeholder in `docs/envoy-rust/BEHAVIOR_CONTRACT.md` with a 2-row Header allow-list table (`server` + `date`, both name-required / value-may-differ). ADR-0011's deferral closes here — the contract now has its first populated entries, and the harness `HEADER_ALLOW_LIST` constant (Task 13) is the in-code mirror.
- Verification: harness constant + contract table are byte-identical in entry set (`server`, `date`) and ordering. ADR ledger unchanged at 20.
- Deviations: none.

## Task 16 — fixture 0007-http1-direct-response (2026-04-27)

- Commit: 7124f14
- Change: created `tests/fixtures/0007-http1-direct-response/` (5 files — `envoy.yaml` + `envoy-rust.yaml` + `inputs/payload.bin` (empty) + `expectations.yaml` + `README.md`); created `tests/differential/tests/http1_direct_response.rs` Docker-gated acceptance test (single `#[tokio::test]` wrapping `differential::run_fixture`, not `#[ignore]` per phase-03 precedent). Fixture drives `GET /healthz` against an HCM listener (single virtual_host `domains: ["*"]`, single route `match: { prefix: "/" }` → `direct_response: { status: 200, body: { inline_string: "ok\n" } }`) under the full 3-axis equivalence cascade: `response_status: exact` + `response_body: byte_exact` + per-driver `expected_headers: set_equal_modulo_allow_list` (with `expected_status: 200` and `expected_body: { kind: byte_exact, body: "ok\n" }` as per-side anchors). The harness's `Driver::Http1` dispatch (Task 14) drives both proxies, applies the cascade, and `diff_headers(&upstream, &subject, HEADER_ALLOW_LIST)` for the per-driver header rule (`server` + `date` allowed to differ in value; `content-length`, `content-type`, `connection` value-exact).
- Verification: `cargo build --workspace --all-targets` green; `cargo clippy --workspace --all-targets --all-features -- -D warnings` green; `cargo fmt --all -- --check` green; `cargo test --workspace --lib --bins` green (46 differential lib tests + 19 envoy-bin + 75 envoy-config + 19 envoy-config-fuzz-helpers + 6 envoy-http1 + 11 envoy-net + 15 envoy-tls + 8 tcp-echo-server + 5 tls-echo-server). The Docker-gated `http1_direct_response_fixture` is excluded by `--lib --bins` and runs via `cargo test --workspace` (CI). `cargo test -p differential --test http1_direct_response` fails locally with `failed to initialize a docker client: Socket not found: /var/run/docker.sock` — Docker is not available on this dev machine (per Task 1's report); same pre-existing failure mode as every other acceptance test from phase-02.2 onwards. CI exercises this against `ubuntu-latest`'s docker daemon.
- Deviations:
  1. **Changed `Http1BodyRule::ByteExact { bytes: Vec<u8> }` to `ByteExact { body: String }`.** Task 13's original schema used `bytes: Vec<u8>`. Empirical check (`/tmp/yaml_probe`) confirmed `serde_yaml`'s default `Vec<u8>` deserializer rejects YAML scalar strings — `serde_yaml::from_str::<Http1BodyRule>("kind: byte_exact\nbytes: \"ok\\n\"")` returns `invalid type: string "ok\n", expected a sequence`. So the PLAN's drafted `expected_body: { byte_exact: "ok\n" }` form would not parse under the actual Task 13 types regardless. Three options were available: (a) keep `Vec<u8>` + use array form `bytes: [111, 107, 10]` in fixture YAML; (b) add a string-to-bytes serde helper (`serde_bytes` crate or hand-rolled); (c) switch the field to `String`. Chose (c) — it's the cleanest, 04.x bodies are always text, fixture YAML stays readable as `body: "ok\n"`, and no new dependency. Updated `run_fixture`'s `Driver::Http1` dispatch in `lib.rs:1103-1118` to compare `upstream_resp.body != body.as_bytes()` rather than `&upstream_resp.body != bytes`. No callers outside `lib.rs` needed updating (greppable via `Http1BodyRule\|ByteExact { bytes`). The variant's doc comment records the rationale and signposts a future `serde_bytes` adoption if a non-UTF-8-body fixture lands. This is the user-recommended path documented in the Task 16 prompt.
  2. **Used `expected_headers: set_equal_modulo_allow_list` (snake_case scalar)** rather than the PLAN's drafted `expected_headers: { rule: set_equal_modulo_allow_list }` (struct form). The Task 13 type is `Http1HeaderRule::SetEqualModuloAllowList` (unit variant) with `#[serde(rename_all = "snake_case", deny_unknown_fields)]` and *no* `#[serde(tag)]`, so unit variants serialize as bare scalars (`set_equal_modulo_allow_list`) not struct maps. The PLAN's draft would have failed `deny_unknown_fields`.
  3. **Used `method: get` (lowercase)** rather than `method: GET`. Task 13's `Http1Method` has `#[serde(rename_all = "snake_case")]`, so the variant `Get` deserializes from the lowercase scalar.
  4. **Dropped the PLAN's drafted `equivalence.response_headers: { rule: set_equal_modulo_allow_list }`.** Task 14 confirmed `Equivalence` has only `response_status` + `response_body` fields; the per-driver `expected_headers` carries the header-rule contract. This asymmetry against status/body is documented in `lib.rs`'s `run_fixture` dispatch comment (lines 1031-1037) for an eventual 04.x cleanup; the fixture is fully sufficient as-is.
