# Phase 10 (`10-http-filter-rbac`) — REVIEW

- **Phase id:** `10`
- **Slug:** `10-http-filter-rbac`
- **Position:** **Second post-MVP-trunk feature-family phase** and the **second concrete HTTP-filter-family phase** (after phase-09 `local_ratelimit`). The MVP trunk 00→08 stands `done` as of parent-08.2 close-out `304ce98`; phase 09 stands `done` as of close-out `518140c`. With Approved verdict, the next session's state-6 close-out commit flips ROADMAP row `10` `in-progress → done` per the standalone-phase invariant (phase 10 is a standalone phase, NOT a sub-phase) and advances STATE.md to the next feature-family pick (brainstorm-driven per `BOOTSTRAP_PROMPT.md` §9). Phase 10 amortizes the phase-09-established framework + helper investment: it is the **first non-LocalRateLimit BILATERAL consumer** of the ADR-0033 H1 HCM `decorate_filter_synth_response` helper landed at phase-09 ADR-0033 Commit C `ae2cef0`, validating that helper's filter-agnostic design end-to-end.
- **Reviewed range:** `55abc61..56d381e` — **14 commits total** (`git log --oneline 55abc61..56d381e | wc -l` → 14; verified). Of these, **11 are phase-10 commits**: 7 substantive task commits (Tasks 1-7) + 3 docs-only fixup/SHA-record commits + 1 docs-only Task 8 state-4-reached commit + 1 docs-only Task 8 fixup. **2 commits are non-phase-10** and interleave between Task 5 (`c27f2d4`) and Task 6 (`f8be570`): a perf-hardening + bench-harness interlude (`2f28bbf` `perf(http1): TCP_NODELAY + per-connection upstream reuse + bench harness`) and its fmt+clippy fixup (`6c24cd1` `fix(http1): cargo fmt + clippy collapsible_if on perf-reuse code`); both are NOT assessed by this REVIEW.md (they sit outside the phase-10 surface). The state-2 base `55abc61` is the standalone-PLAN-write commit (which also lands ADR-0034 inline per SPEC §7 option A recommended posture); the HEAD `56d381e` is the Task 8 docs-only fixup closing 1 Important from the Task 8 two-stage code-quality review per the Task 6 + Task 7 fixup convention.
  - `3fbe9f5` — **Task 1**: D1 envoy-config schema (`RbacConfig` + `Rules` + `Action` + `Policy` + `Permission` + `PermissionSet` + `Principal` + `PrincipalSet` + `HttpFilterTypedConfig::Rbac` variant) + D2 validator (`validate_rbac_config` + `validate_permission_tree` + `validate_principal_tree` recursive descents + 6 new `ConfigError` variants: `EmptyRbacPolicies`, `EmptyRbacPolicyPermissions`, `EmptyRbacPolicyPrincipals`, `EmptyRbacPermissionSet`, `EmptyRbacPrincipalSet`, `RbacTreeTooDeep`) + `RBAC_TREE_MAX_DEPTH = 16` constant + `default_action` helper + transient `instance.rs` bridge arm + 26 unit tests (14 envoy-config schema/validator + 12 envoy-filter evaluator — Task 2 lands the evaluator). 4 files (+851 / −4).
  - `14a842c` — **Task 2**: D3 hand-rolled recursive tree-walk evaluator at `crates/envoy-filter/src/rbac.rs` (NEW; 243 LoC); `RuntimePermission` + `RuntimePrincipal` mirror enums (`Any(bool)` / `Header(HeaderMatcher)` / `AndRules-AndIds(Vec)` / `OrRules-OrIds(Vec)` / `NotRule-NotId(Box)`); `eval_permission` + `eval_principal` synchronous pure-compute recursive descent fns with `Iterator::all` (AND short-circuit) + `Iterator::any` (OR short-circuit) + recursive negation for Not; stack-bounded by parse-time `RBAC_TREE_MAX_DEPTH = 16`; 12 unit tests. 3 files (+384 / −1).
  - `da32137` — **Task 3**: D3 `RbacFilter` runtime + D6 2-counter stats wiring + D7.1 2 BEHAVIOR_CONTRACT.md Stat-name mapping rows. `RbacFilter { action, policies: Arc<Vec<RuntimePolicy>>, allowed_counter: Arc<Counter>, denied_counter: Arc<Counter> }` + `build_from_config(cfg, registry, hcm_stat_prefix: &str)` (THREE-arg signature; new precedent for any filter whose stat namespace embeds the HCM's stat_prefix at register time) + `decode_headers` short-circuits via `Decision::StopAndSend(synth_403())` on deny + `encode_headers` returns `Decision::Continue` unconditionally + `lower_permission` + `lower_principal` free helpers; stat namespace `http.<hcm_stat_prefix>.rbac.{allowed,denied}` matches upstream Envoy v1.33 parity (verified at SPEC §6.2 empirical run); counter increments EXACTLY ONCE per request at the decision site; 6 new runtime unit tests; 2 new BEHAVIOR_CONTRACT.md rows under `**10 entries (RBAC filter):**`. 4 files (+578 / −21).
  - `65181c1` — **Task 3 SHA-record fixup**: 1-line PROGRESS.md edit recording the Task 3 substantive commit SHA `da32137` in the per-task narrative. Docs-only; no code or test impact.
  - `84b508e` — **Task 4**: D4 `HttpFilterInstance::Rbac(RbacFilter)` variant + dispatch + signature widening + D5 **09 REVIEW M2 closure**. Added the new variant between `LocalRateLimit` and the `#[cfg(feature = "test-util")]` block; widened `HttpFilterInstance::build` signature `(hf, registry)` → `(hf, registry, hcm_stat_prefix: &str)` (3-arg); widened `FilterPipeline::build_from_config` correspondingly; threaded `&cfg.stat_prefix` at the **single** H1 HCM production call site `crates/envoy-http1/src/hcm.rs:185` (H2 reuses via `Http1HCMConfig` re-export per phase-09 wiring discipline; no second production call site); retired the 12 Tasks 3-4 interim `#[allow(dead_code)]` attrs; added 1 `build_rbac_succeeds` unit test (workspace 778 → 779). **D5 09 REVIEW M2 closure**: in-place ADR-0033 Consequences §iii(c)-end amendment paragraph inserted between Consequences (line 697) and Provenance (line 701) at `docs/envoy-rust/DECISIONS.md` — clarifies the H2 analogous gap as known-deferred + names the close site ("next HTTP-filter-family phase exercising filters bilaterally on H2; the H2 HCM `decorate_filter_synth_response_h2` analogue lands as a ~50-70 LoC + 2-test follow-up at that phase"); ADR-0033's Decision + Rationale + Provenance preserved verbatim per D-3.5 + SPEC §7 option D recommended posture (clarification, NOT supersession; DECISIONS.md ledger head stays at ADR-0034); phase-09 PROGRESS Commit C subsection gains a cross-ref note at `docs/envoy-rust/phases/09-http-filter-local-rate-limit/PROGRESS.md:~1204`. **The chain 09 → 10 ENDS for M2 at THIS commit.** 5 files (+263 / −52).
  - `c27f2d4` — **Task 5**: D8.1 fixture `tests/fixtures/0017-http-filter-rbac/` (5 files — `envoy.yaml` + `envoy-rust.yaml` + `expectations.yaml` + `README.md` + the implicit harness wrapper rule) + Docker-gated wrapper `tests/differential/tests/http_filter_rbac.rs` (single `#[tokio::test]` invoking `differential::run_fixture("0017-http-filter-rbac").await`). Bilateral 4-probe `Driver::Http1ProbeList` burst against `envoyproxy/envoy:v1.33.0` Docker: per-probe `extra_headers` `[[], [(x-rbac-pass, yes)], [(x-rbac-pass, no)], [(x-rbac-pass, yes)]]`; expected per-probe statuses `[403, 200, 403, 200]`; deny probes 1+3 assert `byte_exact` body `"RBAC: access denied"` (19 bytes per ADR-0034 — the 1-byte projection-error correction lands empirically here); allow probes 2+4 assert `byte_exact` body `"ok\n"` (via direct_response); `expected_headers: set_equal_modulo_allow_list` on all 4 (covered by the existing 04.1 `server` + `date` allow-list rows; no new Header allow-list row added per SPEC §2.2). **First non-LocalRateLimit BILATERAL consumer of the H1 HCM `decorate_filter_synth_response` helper** — the deny-probe 403 responses engage the helper end-to-end against both proxies, validating its filter-agnostic design. 4 PLAN deviations honestly disclosed in PROGRESS at dispatch time (probe field name `extra_headers` not `request_headers`; `{{PORT}}`/admin-port placeholder; wrapper signature; YAML quoting). Workspace test count 779 → 780. 6 files (+512 / −1).
  - `f8be570` — **Task 6**: D8.2 parse_bootstrap fuzz corpus seed `crates/envoy-config/fuzz/corpus/parse_bootstrap/hcm_rbac_filter.yaml` (~39-LoC YAML mirroring fixture 0017's bootstrap shape) + 1-line `crates/envoy-config/fuzz/.gitignore:23` allow-list entry + 1-line `crates/envoy-config/src/bootstrap.rs::tests::fuzz_corpus_seeds_parse_or_reject_cleanly` SUCCESS-array extension (the 17th entry). All 3 file edits land in one commit per PLAN lock-in #38 + the **09 Task 6 follow-up lesson** (phase-09 split this into Task 6 substantive + Task 6 follow-up `1effb0f`; phase-10 lands all 3 file edits in one Task 6 substantive commit). SUCCESS-array grows 16 → 17. 1 PLAN deviation (`codec_type: HTTP1` added per envoy-config schema requirement). 4 files (+122 / −1).
  - `8805df7` — **Task 6 fixup**: docs-only PROGRESS prose corrections closing 2 Important findings from the Task 6 two-stage code-quality review (parent-prose description reversal: `6c24cd1` is NOT the parent of `c27f2d4` but a non-phase-10 commit between Task 5 and Task 6; ignored count `1` → `2` per the consistent phase-10 attestation). 1 file (+4 / −3).
  - `dd95673` — **Task 7**: D8.3 in-process backstop `crates/envoy-bin/tests/http_filter_rbac.rs` (~253 LoC NEW file) per phase-10 SPEC §3 D8.3 + **09 REVIEW M3 named close site**. Adopted `tokio::process::Command + .kill_on_drop(true) + stdout: Stdio::null() + stderr: Stdio::piped()` discipline directly per PLAN lock-in #39 + #40 (direct code-spot-check of 07.2 `crates/envoy-bin/tests/http_filter_header_mutation.rs:181-189` + 08.2 `crates/envoy-bin/tests/admin_drain_listeners.rs:153-161` precedents performed at Task 7 dispatch time per the 09 REVIEW M3 Process note — "PROGRESS narratives that cite a precedent for an IMPORTANT-track finding should verify the precedent shape via direct code-spot-check before claiming 'same pattern'"). Single `#[tokio::test]` (NOT `multi_thread` per PLAN lock-in #39); 4 sequential GET probes with `x-rbac-pass` header values `[absent, yes, no, yes]`; assertions `[403, 200, 403, 200]` + body `"RBAC: access denied"` on 403 probes + body `"ok\n"` on 200 probes. Workspace test count 780 → 781 (the new `http_filter_rbac_in_process_backstop` test). **The chain 09 → 10 ENDS for M3 at THIS commit** via the doctrinally-clean substitute discipline (no `nix` crate; `kill_on_drop(true)` is the project-standard substitute for nix-based SIGTERM). 3 PLAN deviations honestly disclosed (`codec_type: HTTP1` added per schema requirement; `Stdio::piped()` on stderr per 08.2 precedent; `#[tokio::test]` not `multi_thread`). 2 files (+350 / −1).
  - `064a8a5` — **Task 7 fixup**: docs-only PROGRESS Task 7 "Total LoC delta" line correction per Task 6 fixup convention (replaced approximation `+253 / 1 deletion` with literal `git diff --shortstat` numbers + breakdown into 253 LoC from the new test file + 97 LoC from the PROGRESS subsection self-edit). Closes 1 Important from the Task 7 two-stage code-quality review. 1 file (+4 / −2).
  - `767687a` — **Task 8**: state-4 phase-done verification per `BOOTSTRAP_PROMPT.md` §7.5 + STATE advance from `state 2-complete / state-3-next` → `state 4-complete / state-5-next`. Docs-only commit. PROGRESS gets the state-4 evidence anchor subsection (replaces the line-1289 placeholder under the existing `### Task 8` header) mirroring the phase-09 Task 8 `a5ebddd` shape; STATE.md gets status/next-skill/last-commit/last-updated rewrites + new `### Phase-10 state-3 execution arc` subsection appended at end of Notes (mirrors the phase-09 `### Phase-09 state-3 execution arc + ADR-0033 corrective sequence + state-4 verification` subsection shape at STATE.md ~line 1406; phase-10's title is intentionally simpler because phase 10 had no mid-execution ADR corrective sequence — ADR-0034 landed inline at state-2 PLAN-write per the SPEC §6.2 process-improvement). All prior STATE.md subsections preserved verbatim per D-3.5 (append-only) + D-3.4 (context isolation). 2 files (+588 / −36).
  - `56d381e` — **Task 8 fixup**: docs-only PROGRESS Task 8 "Total LoC delta" line correction per Task 6 + Task 7 fixup convention (replaced approximation prose with literal `git diff --shortstat 064a8a5..767687a` numbers `+588 / -36 across 2 files` + per-file breakdown +419/-1 in PROGRESS.md + +169/-35 in STATE.md). Closes 1 Important from the Task 8 two-stage code-quality review. 1 file (+9 / −5). **Current local + origin HEAD.**

- **CI evidence cited:** PROGRESS Task 8 cites CI run **`26070911153`** (`https://github.com/pgdad/envoy-rust/actions/runs/26070911153`) at HEAD `064a8a5a6548380b2ae4ed61c0a2019b5fc725dd` (Task 7 fixup; immediate predecessor of the docs-only Task 8 commit `767687a`). Conclusion `success`; created `2026-05-19T01:39:25Z`; both jobs completed by `2026-05-19T01:41:27Z` (wall ~2m 2s). Both jobs `success`: `build + test + lint` (job 76651911980; wall ~1m 59s; all 9 steps green including `test (includes differential harness → Docker)` at ~49s executing all 17 Docker-gated differential integration buckets + 16 in-process `envoy-bin` integration buckets + all lib buckets + h2spec conformance gate + helper-crate buckets, 0 failed across the workspace) + `fuzz (parse_bootstrap, 30s)` (job 76651911990; wall ~1m 20s; `Done 223434 runs in 31 second(s)` — clean). All 17 Docker-gated fixtures (`0001-tcp-echo` through `0017-http-filter-rbac`) GREEN simultaneously per the 17-row timestamp table at PROGRESS lines 1413-1431. Workspace test count at the CI run: **781 passed; 0 failed; 2 ignored** (+1 over the predecessor Task 6 baseline 780; the +1 is Task 7's `http_filter_rbac_in_process_backstop` in-process backstop test). The docs-only Task 8 commit `767687a` ran its own CI run `26086762460` (`success` on both jobs in ~2m 14s at `2026-05-19T~08:54Z`) and the Task 8 fixup commit `56d381e` ran CI run `26087467319` (`success` on both jobs in ~2m 11s at `2026-05-19T~09:08Z`).
- **Reviewed:** 2026-05-19.

## §1. Verdict

**Approved with M-track follow-ups.**

Phase 10 lands its 8 SPEC deliverables (D1 envoy-config schema with `RbacConfig` + `Rules` + `Action` + `Policy` + `Permission` + `PermissionSet` + `Principal` + `PrincipalSet` + `HttpFilterTypedConfig::Rbac` variant; D2 validator with `validate_rbac_config` + recursive `validate_permission_tree`/`validate_principal_tree` + `RBAC_TREE_MAX_DEPTH = 16` constant + 6 new `ConfigError` variants; D3 hand-rolled recursive tree-walk evaluator + `RbacFilter` runtime + 4-cell SPEC §5.6 decision matrix; D4 `HttpFilterInstance::Rbac` variant + dispatch + 3-arg `build_from_config` signature widening; D5 09 REVIEW M2 closure via ADR-0033 Consequences in-place amendment per preferred close shape (a); D6 2-counter stats wiring under `http.<hcm_stat_prefix>.rbac.{allowed,denied}`; D7.1 2 BEHAVIOR_CONTRACT.md Stat-name mapping rows; D7.2 ADR-0033 Consequences amendment co-located at Task 4; D8.1 fixture 0017 + Docker-gated wrapper; D8.2 fuzz corpus seed `hcm_rbac_filter.yaml`; D8.3 in-process backstop with 09 REVIEW M3 closure via `tokio::process::Command + kill_on_drop(true)` discipline) substantively and cleanly. The `envoy.filters.http.rbac` filter ships end-to-end through the 07.x-established framework: HCM decode invocation → `pipeline.decode_headers` → `RbacFilter::decode_headers` → recursive `eval_permission`/`eval_principal` tree-walk against the request → SPEC §5.6 4-cell decision matrix (`Allow + match → ALLOW`, `Allow + no_match → DENY`, `Deny + match → DENY`, `Deny + no_match → ALLOW`) → on ALLOW `Decision::Continue` + `allowed` counter inc; on DENY `Decision::StopAndSend(FilterResponse{status: 403, body: Bytes::from_static(b"RBAC: access denied"), headers: vec![]})` + `denied` counter inc → H1 HCM writer-arm at `SynthFromDecode` → `decorate_filter_synth_response` adds the 5 standard HTTP/1.1 response headers (`server`, `date`, `content-length`, `content-type`, `connection`) → wire write. Fixture 0017 bilaterally proves this against upstream Envoy v1.33 across a 4-probe burst exercising both branches of the `x-rbac-pass: yes`/(absent|no) principal match; the in-process backstop proves the same wire shape without Docker on the same 4-probe burst.

The cross-phase architectural invariants from parent-07 + phase-09 + phase-10 SPEC §5 all hold: `envoy-filter` stays sole-dep-owner of HTTP filter chain iteration; `RbacFilter` lives at `crates/envoy-filter/src/rbac.rs` mirroring the 07.2 `header_mutation.rs` + 09 `local_rate_limit.rs` placement pattern; **zero new top-level Cargo deps + zero new workspace path-deps** (Cargo.toml + Cargo.lock untouched across all 11 phase-10 commits per PLAN lock-in #45); hand-rolled recursive tree-walk evaluator per D-3.2's "every individual filter ... must be written from scratch" doctrine (`Vec` + `Box` + `Iterator::all`/`any` + `bytes::Bytes` — all std-lib / D-3.2-permitted; no `cel-rust`, `cel-interpreter`, `regex-automata`, `ipnet`, `cidr` — all defer per SPEC §4); decode-only filter semantics (`encode_headers` returns `Decision::Continue` unconditionally — matches upstream Envoy v1.33's documented `envoy.filters.http.rbac` semantic); statelessness across requests (the `Arc<Vec<RuntimePolicy>>` is shared across all per-stream pipeline clones; the recursive evaluator is pure-compute); the 3 phase-10-deferred RBAC fields (`shadow_rules`, `shadow_rules_stat_prefix`, `track_per_rule_stats`) are rejected at parse time via `deny_unknown_fields` per SPEC §5.6 + §4; 8 of 10 candidate Permission/Principal sub-variants (IP/CIDR, URL path, metadata, authenticated, SNI, CEL conditions, etc.) defer per SPEC §4. No `unsafe` introduced in any new module (`#![forbid(unsafe_code)]` continues to bind at every touched crate root per D-3.8). DECISIONS.md ledger head **ADR-0034** at state-2 PLAN-write commit `55abc61` per SPEC §7 option A — empirical-verification 1-byte 403-body correction (`"RBAC: access denied\n"` 20-byte projection → `"RBAC: access denied"` 19-byte reality); **no ADRs landed during the phase-10 state-3 arc** per PLAN lock-in #41 (NO new ADR projected at any state-3 task; the remaining 3 conditional ADR-0034 slots per SPEC §7 options B/C/D all DEFER per recommended posture — option D's D5 amendment landed as in-place ADR-0033 amendment at Task 4 rather than a superseding ADR-0035). BEHAVIOR_CONTRACT.md extended with 2 new Stat-name mapping rows at Task 3 commit `da32137` per SPEC §6.6 cadence (no new Header allow-list row per SPEC §2.2 — the 04.1-landed `server` + `date` rows cover cross-proxy implementation-identifying differences; the 5 standard 403 headers are value-exact across proxies under the deterministic burst with `Connection: close` harness framing).

**The phase-10 state-3 arc landed clean on the first push per task** — no mid-execution corrective sequences, in contrast to phase-09's 4-commit ADR-0033 mid-execution corrective sequence A→B→C→D at Tasks 3-5 dispatch. This is the dividend from the **phase-10 SPEC §6.2 empirical-verification-at-PLAN-write process improvement** (which surfaced the 1-byte 403-body projection error at state-2 PLAN-write and ratified ADR-0034 inline at the state-2 commit `55abc61`, AVOIDING the phase-09 process gap that surfaced ADR-0033 only at Task 5 subagent dispatch). Phase 10 IS the **first phase to leverage the SPEC §6.2 empirical-verification process-improvement** directly + validates that the verify-at-PLAN-write doctrine works as intended — the projected 1-byte correction was caught BEFORE state-3 execution. The state-4 evidence anchor (CI run `26070911153` at HEAD `064a8a5` `success`) bilaterally validates the full state-3 surface: all 17 Docker-gated fixtures green simultaneously + h2spec held at 99.31% + parse_bootstrap fuzz clean on the 17-seed corpus. The Task 6 + Task 7 + Task 8 fixup commits (`8805df7`, `064a8a5`, `56d381e`) each closed 1-2 Important findings from the per-task two-stage code-quality reviews inline per the established fixup convention; **4 Importants total across the phase ALL closed inline at fixup commits**; ZERO Importants survive into state 5.

**Zero Critical findings. Zero Important findings.** **Five Minor findings** (M1-M5), **two doc-staleness items** (D1-D2), and **three test/audit-trail items** (T1-T3) — all awareness-only / close-opportunistically. Per-cluster subagent reviewers across the 4-cluster state-5 dispatch (Cluster A Tasks 1-2; Cluster B Tasks 3-4; Cluster C Tasks 5-6; Cluster D Tasks 7-8) surfaced 10 cluster-Minor + 11 cluster-Awareness findings total across the 4 clusters; the holistic state-5 pass consolidates these into the 10 named items below (5 Minor + 2 doc-staleness + 3 test/audit-trail), collapsing minor duplications + applying calibrated downgrade rationale per the phase-09 precedent. **None of the cluster reviewers surfaced any finding that the holistic pass needed to elevate to Important**, in contrast to the phase-09 holistic pass which downgraded 6 cluster-surfaced Importants to Minor + carry-forward. Phase-10's clean cluster-reviewer dispositions reflect the maturity dividend of (a) the SPEC §6.2 process-improvement avoiding mid-execution ADR drama and (b) the doctrinally-clean closure path for both 09 REVIEW M2 + M3 inheriting the named close shapes. The calibration matches the 06.x / 07.x / 08.x / 09 precedent (06.1 / 06.2 / 06.3 / 07.1 / 07.2 / 08.1 / 08.2 / 09 all landed Approved-with-M-track with the same consistent "zero Critical / zero Important / N Minor" verdict shape) — the project's mature-state-3 doctrine working as designed. **Per `BOOTSTRAP_PROMPT.md` §5.2 the phase does NOT re-enter state 3** — no Critical or Important findings; the verdict closes state 5 and the next session is the phase-10 state-6 close-out.

**Phase 10 IS the second post-MVP-trunk feature-family REVIEW.md** — ratifies the post-MVP cadence first set at phase-09 state-5 commit `10bb71c`. The state-5 verdict + REVIEW shape continues the 06.x → 07.x → 08.x → 09 post-MVP standard: standalone-phase shape; `Approved with M-track follow-ups` disposition for typical state-5 outcomes; forward-carry inheritance for unexercised-pattern Minor findings; explicit calibration rationale per finding; full §7.5 6-gate re-attestation against the state-4 CI anchor. Phase-10's clean state-3 arc + zero-Important state-5 verdict also ratifies the **SPEC §6.2 empirical-verification-at-PLAN-write process-improvement** as the future-state-1-brainstorm standard for any phase introducing a novel filter surface with empirically-discoverable wire-level projections.

## §2. Strengths

1. **The hand-rolled recursive tree-walk evaluator at `crates/envoy-filter/src/rbac.rs:66-87` is exemplary minimal-scope design.** `eval_permission` and `eval_principal` are 9-line each pure-compute recursive descents using `Iterator::all` (AND short-circuit on first false) + `Iterator::any` (OR short-circuit on first true) per SPEC §5.6 + PLAN lock-in #9; `NotRule`/`NotId` negate the recursive result; `Any(bool)` is a constant. Symmetric structure between Permission/Principal makes the surface obvious-to-stranger-reader (D-3.4). Synchronous + no I/O + no async + stack-bounded by the parse-time `RBAC_TREE_MAX_DEPTH = 16` validator gate. The 12 evaluator unit tests at `rbac.rs:272-377` exercise all 5 RuntimePermission variants on the permission side (Any ×2 polarity, Header ×3, AndRules ×2 short-circuit, OrRules ×2 short-circuit, NotRule ×1 dual-polarity, nested combinator ×1) + symmetric Principal coverage at runtime via the Task 3 runtime tests. Per cluster A's verification: `Iterator::all`/`Iterator::any` short-circuit semantics empirically confirmed via the `and_rules_short_circuits_on_first_false` / `or_rules_short_circuits_on_first_true` tests.

2. **The 4-cell SPEC §5.6 decision matrix is implemented as a single `matches!` collapse at `rbac.rs:173-197`.** The matrix `(action, any_policy_matches) => decision` collapses to two ALLOW cells (`(Allow, true) | (Deny, false)`); the other two cells fall through to DENY. The implementation is one match expression — minimal-scope, obviously-correct, exhaustively-tested. Counter increments fire EXACTLY ONCE at the decision site (`allowed_counter.inc()` on the ALLOW branch; `denied_counter.inc()` on the DENY branch) — no double-counting, one source of truth per PLAN lock-in #11. The `decode_headers_counters_increment_correctly` test at `rbac.rs:522-563` empirically asserts 2 allowed + 1 denied increments produce the expected counter values (a strong cross-checking test, not a smoke test).

3. **The 2-counter stats wiring is bilateral-parity-correct and exhaustively verified.** `rbac.rs:147-156` registers `format!("http.{hcm_stat_prefix}.rbac.{allowed,denied}")` via `registry.register_counter` at `build_from_config` time. The 2-name format matches upstream Envoy v1.33's documented stat tree exactly (verified by the controller-performed Docker probe at SPEC §6.2 empirical verification — `http.ingress_http.rbac.allowed: 1` + `http.ingress_http.rbac.denied: 1` post-1-allowed + 1-denied probes). The 2 counters share `Arc<Counter>` handles registered idempotently; multiple RBAC filter instances sharing an HCM stat_prefix increment the same underlying counter, correct under both proxies' shared-counter semantics. **Note**: upstream additionally emits `shadow_allowed`/`shadow_denied` at 0 unconditionally; phase-10 registers only the 2 primary counters since shadow_rules defers per SPEC §4; the differential fixture does NOT scrape RBAC stats (only the 4 HTTP status probes), so the 2-vs-4 stat-name-set divergence is not exercised bilaterally — recorded explicitly in ADR-0034 + PROGRESS Task 1 preamble.

4. **The D5 09 REVIEW M2 closure via in-place ADR-0033 Consequences amendment is doctrinally clean.** The amendment paragraph at `docs/envoy-rust/DECISIONS.md:699` is inserted between ADR-0033's Consequences (line 697) and Provenance (line 701) sections; ADR-0033's **Decision** + **Rationale** + **Provenance** sections are preserved verbatim — the amendment is correctly framed as a **clarification to Consequences**, NOT a supersession. DECISIONS.md ledger head stays at **ADR-0034** (no new superseding ADR-0035 created). The amendment correctly identifies the H2 writer-path empirical evidence: H2 `build_http_response` at `crates/envoy-http2/src/response.rs:29-50` returns the filter response verbatim and does NOT decorate with `server`/`date`/`content-type`. The close site for the H2 implementation deferral is named: "next HTTP-filter-family phase exercising filters bilaterally on H2 (the H2 HCM `decorate_filter_synth_response_h2` analogue lands as a ~50-70 LoC + 2-test follow-up at that phase)". The phase-09 PROGRESS cross-ref note at PROGRESS:~1204 is the symmetric forward-reference. Both halves of PLAN lock-in #37's close-shape landed. Per SPEC §2.3 + §7 option D recommended posture exactly. **09 REVIEW M2 CLOSED.**

5. **The D8.3 09 REVIEW M3 closure via `tokio::process::Command + kill_on_drop(true) + Stdio::null/piped` discipline is doctrinally clean.** `crates/envoy-bin/tests/http_filter_rbac.rs:180-186` declares `tokio::process::{Command, Stdio}` (NOT `std::process::Command`); `.kill_on_drop(true)` set on the Command builder; `stdout: Stdio::null()` (NOT `Stdio::piped()` per the un-drained-stdout-pipe risk M3 named); `stderr: Stdio::piped()` per the 08.2 precedent (captures envoy-bin's startup errors on failure). PLAN lock-in #40's direct code-spot-check of the 07.2 + 08.2 backstop precedents was performed at Task 7 dispatch time + recorded in PROGRESS lines 1202-1216 + cross-verified at this state-5 review against `crates/envoy-bin/tests/http_filter_header_mutation.rs:181-189` (07.2) + `crates/envoy-bin/tests/admin_drain_listeners.rs:153-161` (08.2). The Task 7 narrative honors the 09 REVIEW M3 Process note ("PROGRESS narratives that cite a precedent for an IMPORTANT-track finding should verify the precedent shape via direct code-spot-check before claiming 'same pattern'"). **09 REVIEW M3 CLOSED.**

6. **Fixture 0017's bilateral-green disposition validates the phase-09 ADR-0033 helper's filter-agnostic design.** The fixture uses a 4-probe `Driver::Http1ProbeList` burst with per-probe `extra_headers` `[[], [(x-rbac-pass, yes)], [(x-rbac-pass, no)], [(x-rbac-pass, yes)]]` against `envoyproxy/envoy:v1.33.0` Docker. Phase 10 is the **first non-LocalRateLimit BILATERAL consumer** of the H1 HCM `decorate_filter_synth_response` helper (landed at phase-09 ADR-0033 Commit C `ae2cef0`) — the deny-probe 403 responses engage the helper end-to-end against both proxies, validating its filter-agnostic design. The fixture passed GREEN at the state-4 CI evidence anchor (PROGRESS Task 8 quoted at line 1380: `test http_filter_rbac_fixture ... ok` + `1 passed; 0 failed; 0 ignored; finished in 0.79s`). The per-side YAML asymmetries (upstream-only admin block; upstream-only `generate_request_id: false`; bind addresses `0.0.0.0` vs `127.0.0.1`) are honestly disclosed in the fixture README.md + are non-load-bearing for the wire-level bilateral assertion (admin not probed; `x-request-id` not injected by envoy-rust; bind addresses converge through harness publishing).

7. **The in-process backstop at `crates/envoy-bin/tests/http_filter_rbac.rs` honors the ADR-0034 revised contract end-to-end on the no-Docker boot path.** Single `#[tokio::test]` spawning envoy-bin with a synthesized bootstrap (HCM + RBAC ALLOW policy `pass_with_header` + router + direct_response); issues 4 sequential `GET /` requests with the varying `x-rbac-pass` header values; asserts (a) status sequence `[403, 200, 403, 200]` (lines 239-246); (b) body byte-exact `"RBAC: access denied"` (19 bytes per ADR-0034) on 403 probes; (c) body byte-exact `"ok\n"` on 200 probes. All status + body assertions land bilaterally identical to the fixture 0017 contract. Runtime ~0.77s locally (per PROGRESS line 1521 quoted output); ~0.05s on the state-4 CI anchor (per PROGRESS line 1389-1391). The subprocess-cleanup discipline (kill_on_drop + explicit `child.kill().await` + `child.wait().await` on both success + failure paths at lines 251-252 + 208-209) ensures no envoy-bin subprocess leaks under panic or normal exit.

8. **The Task 6 fuzz corpus seed + the same-commit `.gitignore` + SUCCESS-array extensions complete the parse-time exercise wiring symmetrically with the 08.2 Task 9 / 07.2 Task 6 / 09 Task 6 cadence + honors the 09 Task 6 follow-up lesson.** The seed YAML at `crates/envoy-config/fuzz/corpus/parse_bootstrap/hcm_rbac_filter.yaml` mirrors fixture 0017's bootstrap shape (1 ALLOW policy + `permission: any: true` + `principal: header: x-rbac-pass / exact: yes`); exercises the Task 1 schema + the Task 1 validator end-to-end through `parse_bootstrap`. The `.gitignore` allow-list entry per the curated-seed pattern. **Crucially**, the SUCCESS-array entry at `crates/envoy-config/src/bootstrap.rs:3287` is added in the SAME Task 6 substantive commit (NOT a follow-up commit) per PLAN lock-in #38 + the **09 Task 6 follow-up lesson**: phase-09 split this into Task 6 + Task 6 follow-up `1effb0f`; phase-10 lands all 3 file edits in one Task 6 substantive commit, avoiding the split. The fuzz target at the state-4 CI anchor ran `cargo +nightly fuzz run parse_bootstrap -- -max_total_time=30` GREEN with the 17-seed corpus (`Done 223434 runs in 31 second(s)`; ~7207 exec/s effective).

9. **The 5-gate per-task PROGRESS attestation discipline is honored mechanically across the entire 11-commit phase-10 arc.** Every code-changing task (Tasks 1, 2, 3, 4, 5, 6, 7) quotes the full 5-command workspace gate (cargo fmt --all -- --check; cargo clippy --workspace --all-targets --all-features -- -D warnings; cargo build --workspace --all-targets; cargo test --workspace; cargo deny check) including the full `cargo deny check` output — the 07.1-REVIEW doctrine reminder honored at every Task 1-7 PROGRESS attestation. Per-task workspace test counts inline; carryforward closures attributed at the named landing commits (Task 4 attributes the 09 REVIEW M2 closure; Task 7 attributes the 09 REVIEW M3 closure). The Task 8 state-4 evidence anchor is comprehensive — real CI URL + HEAD SHA + per-job IDs + per-step disposition + per-fixture 17-row timestamp table (the matrix at PROGRESS lines 1413-1431) + per-gate quoted output for (a)-(e) + state-5 entry routing + local re-run command outputs cross-verifying the CI claims.

10. **Zero new top-level Cargo deps + zero new workspace path-deps; one ADR landing (ADR-0034 at state-2 PLAN-write, projected per SPEC §7 option A); D-3.8 + D-3.9 unaffected; D-3.2 inviolate.** `git diff Cargo.toml Cargo.lock 55abc61..56d381e` returns empty across the entire phase-10 reviewed range — Cargo.toml + Cargo.lock untouched per PLAN lock-in #45. No top-level workspace Cargo.toml changes; no `envoy-*` crate manifest changes. `rust-toolchain.toml` and `docs/envoy-rust/ENVOY_TARGET.md` untouched (D-3.9 + D-3.7 unchanged). All workspace crates' root files carry `#![forbid(unsafe_code)]` (verified inline at `crates/envoy-filter/src/lib.rs:1` + `crates/envoy-config/src/lib.rs:1` + `crates/envoy-bin/tests/http_filter_rbac.rs:21`); zero `unsafe` blocks introduced anywhere in the review range. DECISIONS.md ledger head advanced **ADR-0033 → ADR-0034** at state-2 PLAN-write `55abc61` (the projected ADR per SPEC §7 option A — empirical-verification 1-byte 403-body correction); zero further ADRs in state-3 per PLAN lock-in #41. Project-cumulative ADR count: 34 across phases 00-10. Next available number: **ADR-0035**.

11. **PROGRESS.md is self-narrating and disclosure-disciplined across all 7 substantive tasks + 3 fixup/SHA-record commits + 1 Task 8 docs-only commit + 1 Task 8 fixup.** The Task 1 preamble enumerates 3 §6.2 empirical-verification findings + 7 PLAN-write SPEC corrections + 46 architecture-decision lock-ins + 16-row carryforward dispositions table — strong cold-start onboarding for the state-3 implementer + the state-5 reviewer. Per-task `### Task N — <name>` subsections carry per-task deviations + LoC delta + 5-gate attestation. PROGRESS Task 5 honestly discloses 4 PLAN deviations adapted at dispatch time (probe field name + harness placeholders + wrapper signature + YAML quoting); PROGRESS Task 7 honestly discloses 3 deviations (`codec_type: HTTP1` schema requirement + `Stdio::piped()` on stderr per precedent + `#[tokio::test]` not `multi_thread`); PROGRESS Task 8 state-4 evidence anchor is auditable against the CI log + cross-checked against `gh run view` JSON (verified at this state-5 review).

12. **The state-4 CI anchor is comprehensive: both jobs green; all 17 Docker-gated fixtures green simultaneously; h2spec held at 99.31%; parse_bootstrap fuzz clean on 17-seed corpus.** CI run `26070911153` at HEAD `064a8a5` `success`, completed `2026-05-19T01:41:27Z` (wall ~2m 2s). Both jobs green: `build + test + lint` (~1m 59s) + `fuzz (parse_bootstrap, 30s)` (~1m 20s). The 17-row timestamp table at PROGRESS lines 1413-1431 enumerates every fixture wrapper with its CI timestamp + result + runtime — the new `tests/http_filter_rbac.rs` (fixture 0017) ran `1 passed; 0 failed; finished in 0.79s` at `2026-05-19T01:40:52.0171664Z`. All 17 fixtures (`0001-0017`) green simultaneously inside the unified `cargo test --workspace` invocation. Workspace test count `781 passed / 0 failed / 2 ignored` (+1 over the predecessor baseline 780; the +1 is Task 7's in-process backstop). The CI evidence has been independently re-verified at this state-5 review via the PROGRESS-quoted job IDs (job 76651911980 + job 76651911990) — every claim is auditable. Two consecutive subsequent CI runs (`26086762460` on Task 8 substantive HEAD `767687a` + `26087467319` on Task 8 fixup HEAD `56d381e`) both GREEN on both jobs, providing a 3-run consecutive GREEN attestation across the Task 7 → Task 8 → Task 8 fixup chain.

13. **The 2 BEHAVIOR_CONTRACT additions are substantively rationaled and bilateral-parity-anchored.** `docs/envoy-rust/BEHAVIOR_CONTRACT.md:112-117` adds 2 rows under "**10 entries (RBAC filter):**" with explicit rationale citing the SPEC §5.6 decision matrix + the decision-site `allowed + denied == total_requests_to_filter` invariant. Both rows carry `value-exact` disposition matching upstream Envoy v1.33's stat tree (deterministic-counter equivalence). The `allowed` row's rationale explicitly distinguishes "Allow-action match" vs "Deny-action no-match" upstream-parity semantic; the `denied` row's rationale explicitly distinguishes "Deny-action match" vs "Allow-action no-match". No new Header allow-list row per SPEC §2.2's posture — the 04.1-landed `server` + `date` rows cover cross-proxy implementation-identifying differences; the 5 standard 403 headers (`server`, `date`, `content-length`, `content-type`, `connection`) are value-exact across both proxies under the deterministic burst with `Connection: close` harness framing.

14. **The state-4 evidence anchor + state-5 entry routing are clean.** PROGRESS Task 8 state-4 evidence anchor at lines 1289-1714 mirrors the phase-09 Task 8 `a5ebddd` shape exactly: Commit + Parent block + Work summary + CI evidence anchor + §7.5 6-gate table + per-gate prose subsections `#### (a)` through `#### (f)` + 17-fixture per-binary CI timestamp table + Task 1-7 execution-arc summary table + Carryforward closures (M2 + M3) + DECISIONS.md ledger + State-5 entry routing + Differential surface delta + Cumulative review backlog + diff-impact summary + Total LoC delta line per the fixup convention. STATE.md modifications preserve all prior subsections verbatim per D-3.5 (append-only) + D-3.4 (context isolation) — including `### Phase-10 state-1 brainstorm` (STATE.md line 1507), `### Phase-10 state-2 PLAN-write` (line 1576), `### Phase-10 state-3 execution arc` (line 1635) + the full phase-09 + phase-08 + earlier-phase historical narratives unchanged.

## §3. Issues

### Critical (must fix before phase commit)

None.

### Important (close in 10 or carry forward with named owner phase)

None.

### Minor (awareness-only / track forward)

**M1. Task 7 in-process backstop omits the PLAN lock-in #39 "5-header presence on 403 probes" assertion as an UNDISCLOSED deviation.** `crates/envoy-bin/tests/http_filter_rbac.rs:213-247` — the backstop test asserts status sequence `[403, 200, 403, 200]` + body byte-exact `"RBAC: access denied"` on 403 probes + body byte-exact `"ok\n"` on 200 probes, but does NOT include any explicit per-probe 5-header presence assertion for the standard HTTP/1.1 response headers (`server`, `date`, `content-length`, `content-type`, `connection`) on the 403 probes. PLAN lock-in #39 is explicit: *"assertions: status sequence + body + 5-header presence on 403 probes"*. PROGRESS Task 7 deviation list (3 items at PROGRESS lines 1253-1277) does NOT enumerate this omission as deviation #4. **Why it matters:** documentation/discipline gap — the lock-in should be either honored or disclosed; a future maintainer reading the lock-in in isolation against the on-disk backstop would see a contract-vs-implementation gap. **Why it does NOT rise above Minor at phase-10 scope:** the 5-header bilateral assertion IS covered end-to-end at the Docker-gated wrapper `tests/differential/tests/http_filter_rbac.rs` via the `expected_headers: set_equal_modulo_allow_list` rule (which verifies cross-proxy header-set equivalence including the 5 standard headers via the existing 04.1 `server` + `date` allow-list); the in-process backstop's primary value is no-Docker boot-path verification, which is satisfied by the status + body assertions; the omission is doctrinally defensible per parent-08 SPEC §6.1 alternative (vi) accept-drift if disclosed. **Fix sketch:** either (a) add the 3-LoC `let h = resp.headers(); assert!(h.get("server").is_some()); ...` block on each 403 probe assertion site (~12-15 LoC total + 1 PROGRESS deviation #4 entry), OR (b) update PROGRESS Task 7 retroactively with a disclosed deviation #4 explaining the redundancy-with-Docker-wrapper rationale (~5 LoC docs edit; recommended per parent-08 §6.1(vi) accept-drift posture). **Disposition:** awareness-only discipline gap; **close at next HTTP-filter-family phase touching `crates/envoy-bin/tests/http_filter_*.rs` backstops**, OR opportunistically. Named owner: future HTTP-filter-family phase touching backstops.

**M2. H1 HCM call-site widening at `crates/envoy-http1/src/hcm.rs:184-188` lacks an inline doc-comment on the new `hcm_stat_prefix` 3rd argument.** The single H1 HCM production call site threads `&cfg.stat_prefix` correctly per PLAN lock-in #5 + PLAN-write SPEC correction #3, but the call site itself has no inline comment explaining why the new 3rd argument is threaded. The widening sites in `crates/envoy-filter/src/instance.rs:67-72` + `crates/envoy-filter/src/pipeline.rs:36-39` DO carry doc-comments explaining the new `hcm_stat_prefix` parameter + naming the single production call site, so a future maintainer following the call chain would find the explanation one level up. **Why it matters:** D-3.4 stranger-readability — the 3-arg `build_from_config` signature is a new precedent for any filter whose stat namespace embeds the HCM's stat_prefix at register time (vs filters like LocalRateLimit whose stat_prefix is a filter-level config field); the H1 HCM call site is the load-bearing wire-up + future maintainers might add a 5th filter that needs similar threading. **Why it does NOT rise above Minor:** the upstream widening sites already carry the explanation; the call site itself is one `&cfg.stat_prefix` argument addition and is obvious-to-stranger-reader in context. **Fix sketch:** add a 2-line inline doc-comment at `hcm.rs:187` explaining the new 3rd argument (~2 LoC). **Disposition:** awareness-only stranger-readability polish; **close opportunistically** at the next HTTP-filter-family phase touching `hcm.rs:185`. Named owner: future HTTP-filter-family phase touching the call site.

**M3. `Permission` + `Principal` hand-rolled `Deserialize` impls drop the enum-level `#[serde(deny_unknown_fields)]` attribute.** `crates/envoy-config/src/bootstrap.rs:583` (for `Permission`) + `bootstrap.rs:655` (for `Principal`) — both enums have `#[derive(Debug, Clone, Serialize, PartialEq)]` but the `Deserialize` derive is replaced by a hand-rolled `impl<'de> Deserialize<'de> for Permission` + symmetric for `Principal`. The hand-roll is required per PROGRESS Task 1 deviation #1 (a real `serde_yaml` 0.9 limitation on externally-tagged enums via plain YAML maps; sidesteps it via a custom visitor; mirrors the 04.2 `HeaderMatcher` precedent). The visitor at `bootstrap.rs:597-641` (for `Permission`) + `bootstrap.rs:669-713` (for `Principal`) DOES enforce single-key + known-key with explicit error returns via `M::Error::unknown_field(other, KEYS)`; functionally equivalent to enum-level `deny_unknown_fields`. **Why it matters:** D-3.4 stranger-readability — a future maintainer might add a 6th variant to `Permission` without updating the visitor's KEYS array, silently breaking the unknown-key rejection; the enum-level attribute (if it were applied) would handle this automatically. **Why it does NOT rise above Minor:** the visitor logic is functionally equivalent + tested (`deserialize_rbac_rejects_unknown_field` exercises the schema-level unknown-key path); the PROGRESS narrative names the 04.2 precedent + the documented decision; the visitor's KEYS array is small + obvious-to-edit when adding a variant. **Fix sketch:** either (a) accept as a documented decision (current posture; no action); OR (b) extract a `const PERMISSION_KEYS: &[&str] = &["any", "header", "and_rules", "or_rules", "not_rule"]` constant at module scope + reference from both the visitor + a derive-equivalent test (~5 LoC). **Disposition:** awareness-only documented decision; **no action needed**. The 04.2 precedent's discipline ratifies the posture.

**M4. 5 RbacFilter runtime tests duplicate `use envoy_stats::StatsRegistry; use std::collections::BTreeMap; use std::sync::Arc;` inline.** `crates/envoy-filter/src/rbac.rs:382-597` — the 5 Task-3 runtime tests (`build_rbac_succeeds`, `decode_headers_allow_action_match`, `decode_headers_deny_action_no_match`, `decode_headers_counters_increment_correctly`, plus one more) each carry the same 3-line `use` block. Task 4 PROGRESS (~line 944) explicitly defers this to Task 8 cleanup per the Task 3 code-quality review's Minor #1; Task 8 lands as a pure docs-only commit and does NOT touch `rbac.rs`. **Why it matters:** D-3.4 stranger-readability — duplicate `use` blocks are a minor code-style blemish; the obvious refactor is to hoist them to the `tests` mod scope. **Why it does NOT rise above Minor:** test code; no production impact; the deferral is documented in Task 4 PROGRESS. **Fix sketch:** hoist the 3 `use` statements to `mod tests { ... }` scope (~5 LoC). **Disposition:** awareness-only test-style cleanup; **close opportunistically** at the next HTTP-filter-family phase touching `rbac.rs::tests`. Named owner: future HTTP-filter-family phase extending RBAC's test surface.

**M5. PROGRESS narrates "11 commits over the state-3 execution arc" but `git log --oneline 55abc61..56d381e | wc -l` returns 14 (2 non-phase-10 perf-interlude commits interleave between Tasks 5-6).** `docs/envoy-rust/phases/10-http-filter-rbac/PROGRESS.md:1581-1587` + `docs/envoy-rust/STATE.md:1637` (the `### Phase-10 state-3 execution arc` subsection) both narrate "11 commits over the state-3 execution arc" — which is correct for the **phase-10-task** count (7 substantive + 3 fixup/SHA-record + 1 Task 8 docs-only + 1 Task 8 fixup = 11 phase-10 commits, counting the Task 8 substantive + fixup separately). The actual reviewed range `55abc61..56d381e` is **14 commits** total, including 2 non-phase-10 perf-interlude commits (`2f28bbf` `perf(http1): TCP_NODELAY + per-connection upstream reuse + bench harness` + `6c24cd1` `fix(http1): cargo fmt + clippy collapsible_if on perf-reuse code`) that landed between Task 5 `c27f2d4` and Task 6 `f8be570`. **Why it matters:** D-3.4 stranger-readability — a future maintainer reading PROGRESS + STATE in isolation against `git log` would see a count discrepancy; the actual reviewed range is wider than the phase-10-task count suggests. **Why it does NOT rise above Minor:** the 11-commit count IS correct for phase-10-task commits (the natural scoping); the 2 perf-interlude commits are NOT part of phase-10 surface (the perf hardening is an orthogonal H1-runtime improvement unrelated to RBAC); this REVIEW.md's `Reviewed range` block above explicitly calls out the 14-commit total + the 2 non-phase-10 commits + their disposition (NOT assessed). The narrative gap is clarified by THIS REVIEW. **Fix sketch:** if a state-6 fold-in pass touches PROGRESS or STATE, append a 1-sentence clarification ("Note: the reviewed range `55abc61..<HEAD>` contains 14 commits total; 11 are phase-10 commits and 2 are non-phase-10 perf-interlude commits `2f28bbf` + `6c24cd1` between Tasks 5-6"). **Disposition:** awareness-only narrative-clarity polish; **close opportunistically** at the state-6 close-out OR a future doc-staleness fold-in pass. Named owner: state-6 close-out fold-in OR future doc-staleness pass.

### Doc-staleness (track-forward)

**D1. `#[allow(dead_code)]` on `RuntimePolicy::name` lasts longer than its rationale suggests.** `crates/envoy-filter/src/rbac.rs:115` — the field is loaded at `lower_permission`/`build_from_config` time (`rbac.rs:141`) but never read by the runtime; the `#[allow(dead_code)]` comment says "retained for future tracing::debug! diagnostics" per PLAN lock-in #5. Task 3 implemented the `RbacFilter` runtime without wiring the projected `tracing::debug!("rbac policy {name} matched")` instrumentation. **Why it matters:** the comment promises future instrumentation that didn't land in phase 10; the allow is structurally legitimate (per lock-in #5) but the rationale text suggests a closer landing horizon than reality. **Why it does NOT rise above Minor:** documented decision; not a defect. **Disposition:** awareness-only doc-rationale drift; **close at whichever future phase wires `tracing::debug!` policy-match diagnostics** (likely a future RBAC enhancement phase OR a generic filter-instrumentation phase). Named owner: future RBAC-enhancement OR filter-instrumentation phase.

**D2. Task 6 fixup `8805df7` commit message enumerates 3 Minor controller-deferred (LoC off-by-one 38 vs 39 in fuzz seed; YAML indentation style vs 2 most-recent precedents; PLAN "alphabetical position" instruction ambiguity).** All 3 were surfaced by the Task 6 two-stage code-quality reviewer + controller-deferred per `feedback_pick_recommendation`. They are recorded in the fixup commit message + carry forward unchanged. **Why it matters:** D-3.4 narrative-completeness — these 3 items are not load-bearing for the phase-10 surface, but they constitute the deferred-cleanup backlog from Task 6. **Why it does NOT rise above Minor:** the 3 items are themselves Minor — LoC count nit (39 lines but described as 38; off-by-one), YAML indentation style (cosmetic; both parse cleanly), PLAN-prose ambiguity (the alphabetical position of the fuzz seed entry is interpretation-sensitive; no functional impact). **Disposition:** awareness-only carry-forward; **close opportunistically** at whichever future phase touches the fuzz corpus seed shape OR re-orders the SUCCESS-array. Named owner: none.

### Test/audit-trail items (track-forward)

**T1. Module-level doc-comment at `crates/envoy-bin/tests/http_filter_rbac.rs:5-6` could be clearer about the stdout=null + stderr=piped split.** The doc-comment prose explains the subprocess discipline at a high level but does not explicitly distinguish stdout vs stderr Stdio variant routing. A future maintainer reading the doc-comment in isolation might be unclear about which pipe drains and which is null. **Why it matters:** D-3.4 stranger-readability — the discipline detail IS load-bearing (the un-drained-stdout-pipe risk is what 09 REVIEW M3 named). **Why it does NOT rise above Minor:** cosmetic; the actual subprocess discipline is correct per inline code at lines 180-186 + the PROGRESS narrative at lines 1202-1216. **Fix sketch:** 1-2 line doc-comment edit at the module-level docstring (~2 LoC). **Disposition:** awareness-only doc-comment polish; **close opportunistically**. Named owner: none.

**T2. Total LoC delta line at PROGRESS Task 8 fixup `56d381e` uses "+588 insertions / 36 deletions" prose presentation.** PROGRESS line 1705 reads `+588 insertions / 36 deletions across 2 files`; the strict-canonical `git diff --shortstat` rendering would be `+588 / -36` with explicit sign markers. The fixup intent is precisely right (the prose preserves the literal numbers from `git diff --shortstat 064a8a5..767687a`); the presentation nit is whether `36 deletions` vs `-36` is the canonical project convention. **Why it matters:** mechanical-consistency — the Task 6 fixup convention (which Task 7 + Task 8 fixups all honor) is to use the literal `git diff --shortstat` output; the strict literal output has `-36`. **Why it does NOT rise above Minor:** semantic intent is preserved; the prose is unambiguous. **Disposition:** awareness-only presentation-style nit; **close opportunistically**. Named owner: none.

**T3. Cumulative per-task review backlog summary at PROGRESS Task 8 lines ~1656-1670 cites approximate count `~32 review items surfaced` but the per-task breakdown sums to: 10 + 3 + 2 + 4 + 4 + 3 + 5 + 5 = 36 Minor controller-deferred.** Minor count-arithmetic drift; the `~` prefix on `~32` makes the count approximate so the drift is doctrinally inert. **Why it matters:** D-3.4 audit-trail-precision — the cumulative backlog is a load-bearing summary input for THIS REVIEW.md's calibration. **Why it does NOT rise above Minor:** the `~` makes the count explicitly approximate; the per-task breakdown is preserved verbatim so the underlying inputs are auditable. **Disposition:** awareness-only count-precision polish; **close opportunistically** at the state-6 close-out fold-in OR a future doc-staleness pass. Named owner: state-6 close-out fold-in OR future doc-staleness pass.

## §4. Carryforward inventory

For each Minor / doc-staleness / test item: closure plan. (No Critical or Important items.)

| ID | Severity | Item | Closure plan |
|---|---|---|---|
| **M1** | Minor | Task 7 backstop omits PLAN lock-in #39 "5-header presence on 403 probes" assertion as an UNDISCLOSED deviation | **Close at next HTTP-filter-family phase touching `crates/envoy-bin/tests/http_filter_*.rs` backstops** OR opportunistically. Either (a) add 3-LoC per-probe header presence assertions (~12-15 LoC) + 1 PROGRESS deviation #4 entry, OR (b) update PROGRESS Task 7 retroactively with a disclosed deviation #4 explaining the redundancy-with-Docker-wrapper rationale (~5 LoC docs edit; preferred per parent-08 §6.1(vi)). Named owner: future HTTP-filter-family phase touching backstops. The 5-header bilateral assertion IS covered end-to-end at the Docker wrapper; the omission is a documentation gap, not a functional gap. Non-blocking. |
| **M2** | Minor | H1 HCM call-site widening at `hcm.rs:184-188` lacks inline doc-comment on the new `hcm_stat_prefix` 3rd argument | **Close opportunistically** at the next HTTP-filter-family phase touching `hcm.rs:185`. 2-LoC inline doc-comment. Named owner: future HTTP-filter-family phase touching the call site. The widening sites in `instance.rs` + `pipeline.rs` already carry the explanation; the call site itself is obvious-in-context. Non-blocking. |
| **M3** | Minor | `Permission` + `Principal` hand-rolled `Deserialize` impls drop enum-level `#[serde(deny_unknown_fields)]` | **No action needed.** Documented decision per the 04.2 `HeaderMatcher` precedent; the hand-rolled visitor enforces single-key + known-key equivalently + is tested. The KEYS array is small + obvious-to-edit when adding a variant. Named owner: none. |
| **M4** | Minor | 5 RbacFilter runtime tests duplicate `use envoy_stats::StatsRegistry; use std::collections::BTreeMap; use std::sync::Arc;` blocks | **Close opportunistically** at the next HTTP-filter-family phase touching `rbac.rs::tests`. ~5 LoC hoist. Named owner: future HTTP-filter-family phase extending RBAC's test surface. Deferred from Task 3 code-quality review's Minor #1 to Task 8 cleanup (Task 8 was docs-only; did not touch). Non-blocking. |
| **M5** | Minor | PROGRESS narrates "11 commits" but `git log --oneline 55abc61..56d381e \| wc -l` returns 14 (2 non-phase-10 perf-interlude commits `2f28bbf` + `6c24cd1` interleave) | **Close opportunistically** at the state-6 close-out fold-in OR a future doc-staleness fold-in pass. 1-sentence clarification in PROGRESS + STATE. Named owner: state-6 close-out fold-in OR future doc-staleness pass. The 11-commit count IS correct for phase-10-task commits; THIS REVIEW.md `Reviewed range` block clarifies the 14-commit total. Non-blocking. |
| **D1** | Minor | `#[allow(dead_code)]` on `RuntimePolicy::name` retained for "future tracing::debug! diagnostics" — Task 3 implemented runtime without wiring tracing | **Close at whichever future phase wires `tracing::debug!` policy-match diagnostics** (likely a future RBAC enhancement phase OR a generic filter-instrumentation phase). 1-line allow drop. Named owner: future RBAC-enhancement OR filter-instrumentation phase. Documented decision; not a defect. |
| **D2** | Minor | Task 6 fixup `8805df7` carries 3 controller-deferred Minor (LoC off-by-one 38 vs 39; YAML indentation style; PLAN alphabetical-position ambiguity) | **Close opportunistically** at whichever future phase touches the fuzz corpus seed shape OR re-orders the SUCCESS-array. Each is 1-2 LoC. Named owner: none. All 3 items are Minor; non-load-bearing. |
| **T1** | Minor | Module-level doc-comment at `crates/envoy-bin/tests/http_filter_rbac.rs:5-6` could be clearer about stdout=null + stderr=piped split | **Close opportunistically**. 1-2 LoC doc-comment edit. Named owner: none. |
| **T2** | Minor | Total LoC delta line at PROGRESS Task 8 fixup `56d381e` uses "+588 insertions / 36 deletions" prose vs strict `git diff --shortstat` "+588 / -36" rendering | **Close opportunistically**. Presentation nit; semantic intent preserved. Named owner: none. |
| **T3** | Minor | Cumulative per-task review backlog summary cites `~32` but per-task breakdown sums to 36 Minor (10+3+2+4+4+3+5+5) | **Close opportunistically** at state-6 close-out fold-in. The `~` makes the count approximate. Named owner: state-6 close-out fold-in OR future doc-staleness pass. |

**Process note (awareness-only, no carryforward).** The phase-10 state-3 arc landed clean on the first push per task — no mid-execution corrective sequence, in contrast to phase-09's 4-commit ADR-0033 mid-execution sequence A→B→C→D. This is the dividend from the **SPEC §6.2 empirical-verification-at-PLAN-write process improvement**, which surfaced the 1-byte 403-body projection error at state-2 PLAN-write + ratified ADR-0034 inline at the state-2 commit `55abc61`, AVOIDING the phase-09 process gap. Phase 10 IS the first phase to leverage the process-improvement directly + validates the verify-at-PLAN-write doctrine as the future-state-1-brainstorm standard for any phase introducing a novel filter surface with empirically-discoverable wire-level projections. Awareness-only; no further action — the doctrine works as designed.

**Standing carryforwards inherited but not closed in 10** (per phase-10 PROGRESS Task 1 preamble carryforward dispositions table, lines 289-300):

- **09 REVIEW M2** — **closed in 10 at Task 4 (`84b508e`).** See §5 below.
- **09 REVIEW M3** — **closed in 10 at Task 7 (`dd95673`).** See §5 below.
- **09 REVIEW M1** (token-bucket CAS-shape race on refill path) — **carries forward indefinitely.** Phase 10 did not touch the token bucket (RBAC does not use rate limiting).
- **09 REVIEW M4 / M5** (SPEC §3 D8.3 stale text + fixture envoy.yaml:7 stale comment) — **already CLOSED at phase-09 state-6 commit `518140c` via fold-in.** Not engaged by phase 10. Recorded for completeness.
- **09 REVIEW D1 / D2** (fictional `RequestPath::SynthFromEncode` reference + Cargo.lock cadence refinement) — **carry forward indefinitely.** Neither engaged.
- **09 REVIEW T1 / T2 / T3** (parse_duration silent `+` acceptance; torture test refill-path coverage gap; Task 3 brittle exact-length assertion) — **carry forward indefinitely.** None engaged by phase-10's surface.
- **08.1 REVIEW M3** (forward-looking `Arc<BTreeMap<...>>` on `command_line_options`) — indefinite carry; not engaged.
- **08.2 REVIEW M1-M8 + T1-T3** — **all carry forward indefinitely.** None engaged by phase 10.
- **07.2 REVIEW M1** — **CLOSED in 09 at Task 4 (`78128f4`).** Chain ended.
- **07.2 REVIEW M2 / M3** — carry forward indefinitely.
- **06.3 REVIEW I2** (synthetic 5xx backend + 4-class `pre_requests` deferred) — upstream-robustness family natural close site. Not engaged.
- **06.2 REVIEW M1 / M2 / M4 / M5**, **06.1 REVIEW M2 / M3 / M5 / M6**, **05.3 REVIEW I2**, **05.2 REVIEW I1 / I2 / I3**, **04.1 REVIEW M5 / M9 / M-claim / M1 / M2 / M4 / M7**, **02.2 REVIEW M1**, **Phase-00 I3 SIGKILL → SIGTERM** — carry forward indefinitely unless coincidentally engaged.
- **04.1 REVIEW M5 / M9 Cargo.lock cadence ratification ADR** — carries forward unchanged through phase 10. Zero new top-level Cargo deps; zero new workspace path-deps; Cargo.lock diff at the phase-10 reviewed range is EMPTY per PLAN lock-in #45 (verified via `git diff Cargo.toml Cargo.lock 55abc61..56d381e`).

## §5. Disposition decisions

**Closed in 10** (2 carryforwards from 09 REVIEW — confirmed by the holistic review):

- **09 REVIEW M2** (H2 HCM filter-synth header decoration gap + ADR-0033 Consequences misrepresentation as "naturally inherits via the shared Http1HCMConfig re-export") — **CLOSED at Task 4 (`84b508e`)** per phase-10 SPEC §3 D5 + PLAN lock-in #32 + #37 via D5 ADR-0033 Consequences §iii(c)-end in-place amendment per preferred close shape (a). The amendment paragraph at `docs/envoy-rust/DECISIONS.md:699` is inserted between ADR-0033's Consequences (line 697) and Provenance (line 701); ADR-0033's Decision + Rationale + Provenance preserved verbatim; DECISIONS.md ledger head stays at ADR-0034 (no new superseding ADR-0035). The amendment correctly identifies the H2 writer-path empirical evidence (H2 `build_http_response` at `crates/envoy-http2/src/response.rs:29-50` returns the filter response verbatim and does NOT decorate with `server`/`date`/`content-type`) + names the close site ("next HTTP-filter-family phase exercising filters bilaterally on H2; the H2 HCM `decorate_filter_synth_response_h2` analogue lands as a ~50-70 LoC + 2-test follow-up at that phase"). The phase-09 PROGRESS cross-ref note at `docs/envoy-rust/phases/09-http-filter-local-rate-limit/PROGRESS.md:~1204` is the symmetric forward-reference. **The chain 09 → 10 ENDS for M2.** This REVIEW.md ratifies the closure.

- **09 REVIEW M3** (Task 7 in-process backstop subprocess discipline regression from 07.2/08.2 precedents — `std::process::Command` instead of `tokio::process::Command + kill_on_drop(true) + Stdio::null()` for stdout) — **CLOSED at Task 7 (`dd95673`)** per phase-10 SPEC §3 D8.3 + PLAN lock-in #39 + #40 via `tokio::process::Command + .kill_on_drop(true) + stdout: Stdio::null() + stderr: Stdio::piped()` discipline adopted directly from the 07.2 + 08.2 precedents. The direct code-spot-check of the 07.2 + 08.2 backstop precedents was performed at Task 7 dispatch time + recorded in PROGRESS lines 1202-1216 per the 09 REVIEW M3 Process note (the doctrine-discipline lesson: "PROGRESS narratives that cite a precedent for an IMPORTANT-track finding should verify the precedent shape via direct code-spot-check before claiming 'same pattern'"). Verified at this state-5 review via direct code-spot-check at `crates/envoy-bin/tests/http_filter_rbac.rs:180-186` (the discipline matches 07.2 `crates/envoy-bin/tests/http_filter_header_mutation.rs:181-189` + 08.2 `crates/envoy-bin/tests/admin_drain_listeners.rs:153-161` exactly). **The chain 09 → 10 ENDS for M3.** This REVIEW.md ratifies the closure.

**Continuing carryforwards by named owner phase:** see §4 standing carryforwards inventory above.

**New carryforwards generated by 10** (5 Minor + 2 doc-staleness + 3 test items — none with hard-gated owners):

- **M1** (Task 7 backstop omits 5-header presence assertion as undisclosed deviation) — close at next HTTP-filter-family phase touching backstops OR opportunistically; either disclose-deviation or implement-assertion.
- **M2** (H1 HCM call-site widening inline doc-comment polish) — close opportunistically at next HTTP-filter-family phase touching `hcm.rs:185`.
- **M3** (`Permission`/`Principal` hand-rolled Deserialize enum-level `deny_unknown_fields` drop) — no action; documented decision per 04.2 precedent.
- **M4** (5 RbacFilter test `use` block duplication) — close opportunistically at next phase touching `rbac.rs::tests`.
- **M5** (PROGRESS "11 commits" vs `git log` 14-commit narrative gap) — close opportunistically at state-6 close-out fold-in.
- **D1** (`#[allow(dead_code)]` on `RuntimePolicy::name` doc-rationale drift) — close at future tracing-debug-wiring phase.
- **D2** (Task 6 fixup's 3 controller-deferred Minor: LoC off-by-one + YAML indentation + PLAN alphabetical-position) — close opportunistically.
- **T1** (Module-level doc-comment Stdio variant clarity) — close opportunistically.
- **T2** (Total LoC delta line presentation nit) — close opportunistically.
- **T3** (Cumulative review backlog count arithmetic drift `~32` vs sum-36) — close opportunistically at state-6 fold-in.

**None of the new carryforwards gate the state-6 close-out OR any future phase.** All are awareness-only / close-opportunistically; named owners per §4 are advisory, not gating.

## §6. Phase-done gate (§7.5) verification

Per `BOOTSTRAP_PROMPT.md` §7.5 and phase-10 SPEC §1 acceptance signal (a)-(f), re-attested at the Task 8 state-4 evidence anchor — PROGRESS Task 8 cites CI run **`26070911153`** at HEAD `064a8a5a6548380b2ae4ed61c0a2019b5fc725dd` (Task 7 fixup; immediate predecessor of the docs-only Task 8 commit `767687a`), conclusion `success`, completed `2026-05-19T01:41:27Z`. CI evidence cross-checked at this state-5 review via the PROGRESS-quoted job IDs (`build + test + lint` = job 76651911980; `fuzz (parse_bootstrap, 30s)` = job 76651911990) — every PROGRESS Task 8 claim matches the quoted job output. Two consecutive subsequent CI runs (`26086762460` at Task 8 substantive HEAD `767687a` + `26087467319` at Task 8 fixup HEAD `56d381e`) both GREEN on both jobs, providing a 3-run consecutive GREEN attestation across the Task 7 → Task 8 → Task 8 fixup chain.

**(a) New differential fixture `0017-http-filter-rbac` green.** ✓ — `http_filter_rbac_fixture` passed in CI run `26070911153` / job 76651911980 / `test (includes differential harness → Docker)` step at `2026-05-19T01:40:52.0171664Z`: `test http_filter_rbac_fixture ... ok` + `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.79s`. The fixture asserts the ADR-0034 revised contract: status sequence `[403, 200, 403, 200]` + body byte-exact `"RBAC: access denied"` (19 bytes per ADR-0034) on deny probes 1+3 + body byte-exact `"ok\n"` on allow probes 2+4 + `set_equal_modulo_allow_list` headers (5 standard 403 headers covered by 04.1 `server` + `date` allow-list rows). **First non-LocalRateLimit BILATERAL consumer** of the H1 HCM `decorate_filter_synth_response` helper landed at phase-09 ADR-0033 Commit C `ae2cef0`.

**(b) Pre-existing 16 differential fixtures (`0001-0016`) green simultaneously.** ✓ — all 16 (`0001-tcp-echo` through `0016-http-filter-local-rate-limit`) GREEN simultaneously alongside the new fixture 0017 inside the unified `cargo test --workspace` invocation in CI run `26070911153`; job conclusion `success`. PROGRESS Task 8 enumerates all 17 by name with CI timestamp + result + runtime at the 17-row matrix (lines 1413-1431). **17/17 in one run** — the full post-MVP-trunk differential surface (the phase-10 fixture 0017 extending the 09 baseline 16-fixture surface). Wall ~49s for the full differential bucket. The full Docker-gated differential surface (17 fixtures) is bilaterally proven against upstream Envoy v1.33 at a single CI execution boundary.

**(c) `h2spec` ≥95% pass with `known-failures.txt` unchanged.** ✓ — 99.31% (`h2spec_pass_rate_gate` PASS at `2026-05-19T01:41:15.3108025Z`; 05.2 baseline 99.31% carried forward unchanged; `known-failures.txt` unchanged — phase 10 engages no H2-framing surfaces; the RbacFilter operates on the post-codec `FilterRequest` / `FilterResponse` abstraction per phase-07.1 ADR-0031; the fixture 0017 listener is H1 only per phase-10 SPEC §3 D8.1).

**(d) `parse_bootstrap` fuzz target clean for short-budget CI run.** ✓ — Job 2 `fuzz (parse_bootstrap, 30s)` conclusion `success` (wall ~1m 20s); the Task 6 `hcm_rbac_filter.yaml` seed is in the corpus and was exercised. `Done 223434 runs in 31 second(s)` — ~7207 exec/s effective. In-tree corroboration via `crates/envoy-config/src/bootstrap.rs::tests::fuzz_corpus_seeds_parse_or_reject_cleanly` SUCCESS-walk (the seed is the 17th entry in the SUCCESS array per the Task 6 commit `f8be570` and parses cleanly through `parse_bootstrap`). No new fuzz target ships in 10 (Task 6 extended the existing `parse_bootstrap` corpus per the 08.2 Task 9 / 09 Task 6 cadence).

**(e) Stable-toolchain gates clean.** ✓ — Job 1 `build + test + lint` includes all five gates: `cargo fmt --all -- --check` (clean); `cargo clippy --workspace --all-targets --all-features -- -D warnings` (clean); `cargo build --workspace --all-targets` (clean); `cargo test --workspace` (**781 passed / 0 failed / 2 ignored** at HEAD `064a8a5` — including the 17 differential integration buckets, 16 `envoy-bin` integration buckets, lib buckets, h2spec conformance bucket, helper-crate buckets; 0 failed across the workspace); `cargo deny check` (`advisories ok, bans ok, licenses ok, sources ok`). Every per-task PROGRESS attestation (Tasks 1-7 substantive + 3 fixup/SHA-record + Task 8 + Task 8 fixup) quotes the full `cargo deny check` output verbatim per the 07.1-REVIEW doctrine reminder.

**(f) REVIEW.md verdict** lands at state-5 in this session per the `SKILL_ROUTING.md` state-5 transition. **Verdict per §1: Approved with M-track follow-ups** (zero Critical; zero Important; 5 Minor M1-M5 + 2 doc-staleness D1-D2 + 3 test/audit-trail T1-T3 — all awareness-only / close-opportunistically; none has a hard-gated owner; none gates the state-6 close-out). Gate (f) closes at this REVIEW.md verdict.

**All six §7.5 gates disposition: (a) PASS / (b) PASS / (c) PASS / (d) PASS / (e) PASS / (f) CLOSE-at-this-REVIEW.md.**

## §7. Per-commit code-quality reviewer finding dispositions

The phase-10 state-3 arc ran the 14-commit reviewed range (11 phase-10 commits + 2 non-phase-10 perf-interlude commits, the latter NOT assessed) through a 4-cluster subagent review at state 5 (Cluster A Tasks 1-2; Cluster B Tasks 3-4; Cluster C Tasks 5-6; Cluster D Tasks 7-8). Disposition per cluster:

**Cluster A — Tasks 1-2 reviewer — Zero Critical + Zero Important + 3 Minor + 5 Awareness-only.** Verdict: APPROVED. The reviewer verified: SPEC §3 D1 schema projections match landed schema (8 schema items + `#[serde(deny_unknown_fields)]` on 6 wrapper structs + `Box` only on `NotRule`/`NotId` + `BTreeMap` deterministic iteration per lock-in #21); SPEC §3 D2 validator projections match (6 new `ConfigError` variants + `RBAC_TREE_MAX_DEPTH = 16` + recursive descent + depth-bound enforced at the `depth_too_deep` test); SPEC §3 D3 recursive evaluator projections match (synchronous pure-compute; no `unsafe`; uses `HeaderMatcher::matches(&[(String, String)])` per PLAN-write SPEC correction #1; `Iterator::all`/`Iterator::any` short-circuit; `Any(bool)` constant). The 3 Minor findings (`#[allow(dead_code)]` on `RuntimePolicy::name`, hand-rolled `Deserialize` enum-level `deny_unknown_fields` drop, `reason: Option<&'static str>` constraint not in SPEC prose) are absorbed into D1 + M3 + recorded as awareness-only in §3 above. The reviewer's exhaustive PLAN lock-in compliance verification (lock-ins #6-#10 + #16-#29) is the load-bearing positive confirmation for the Tasks 1-2 surface correctness. **No residue from this cluster.**

**Cluster B — Tasks 3-4 reviewer — Zero Critical + Zero Important + 3 Minor + 2 Awareness-only.** Verdict: APPROVED. The reviewer verified: SPEC §3 D3 runtime projections + PLAN lock-ins #3-#5 + #10-#15 match (`RbacFilter` struct shape with `Arc<Vec<RuntimePolicy>>` + 2 `Arc<Counter>` + `RuntimeAction` Copy; `decode_headers` short-circuits via `Decision::StopAndSend(synth_403())` on deny; `encode_headers` returns `Decision::Continue` unconditionally; counter increments EXACTLY ONCE per request at the decision site); SPEC §6.5 + PLAN lock-in #15 stats wiring (`http.<hcm_stat_prefix>.rbac.{allowed,denied}` registered idempotently); SPEC §2.1 BEHAVIOR_CONTRACT rows (2 rows with explicit rationale); SPEC §3 D4 + PLAN lock-in #30 variant placement (between `LocalRateLimit` and test-util block); signature widening at the SINGLE H1 HCM production call site (`hcm.rs:185`; H2 reuses via `Http1HCMConfig` re-export; no second call site verified by `grep -n "FilterPipeline::build_from_config\|HttpFilterInstance::build" crates/` returning only `hcm.rs:184` + 2 test-cfg sites); the 12 Tasks-3-4 interim `#[allow(dead_code)]` retired at Task 4; SPEC §2.3 + §3 D5 + PLAN lock-ins #32 + #37 ADR-0033 amendment landed in-place at `DECISIONS.md:699` preserving ADR-0033's Decision + Rationale + Provenance verbatim. The 3 Minor findings (test `use` block duplication, naming asymmetry `AndRules`/`AndIds` per upstream proto, H1 HCM call-site doc-comment polish) are absorbed into M2 + M4 + awareness-only in §3 above. **No residue from this cluster.**

**Cluster C — Tasks 5-6 reviewer — Zero Critical + Zero Important + Zero new Minor + 3 Awareness-only.** Verdict: APPROVED (the cleanest cluster of the 4). The reviewer verified: PLAN lock-ins #33-#36 fixture shape (bootstrap HCM + RBAC ALLOW policy + router + direct_response; 4-probe `Http1ProbeList` with per-probe `extra_headers`; expected per-probe statuses `[403, 200, 403, 200]` + byte-exact bodies + `set_equal_modulo_allow_list` headers); PLAN lock-in #35 per-probe header threading (the harness's `Http1Probe` struct at `tests/differential/src/lib.rs:619-635` already declares `extra_headers: Vec<(String, String)>` with `#[serde(default)]` per phase-04.2; PLAN's anticipated ~10-20 LoC harness extension was NOT needed; PROGRESS Task 5 deviation #1 documents the rename `request_headers` → `extra_headers` honestly); PLAN lock-in #38 same-commit fuzz seed + `.gitignore` + SUCCESS-array edit pattern per the **09 Task 6 follow-up lesson** (all 3 file edits in one commit; SUCCESS-array grows 16 → 17); bilateral assertion honesty (per-side YAML asymmetries documented inline at `envoy.yaml:34-40` + `envoy-rust.yaml:1-10` + `README.md:104-126`; all 3 are non-load-bearing for the wire-level assertion); fuzz corpus seed parses cleanly through `parse_bootstrap`. The 2 Important findings from Task 6's two-stage code-quality review were closed at fixup `8805df7` (parent-prose reversal + 1-vs-2 ignored count). The 3 carry-forward Minors deferred at the Task 6 fixup are absorbed into D2 in §3 above. **No residue from this cluster.**

**Cluster D — Tasks 7-8 reviewer — Zero Critical + Zero Important + 4 Minor + 1 Awareness-only.** Verdict: APPROVED with M-track follow-ups. The reviewer verified: Task 7 subprocess discipline closes 09 REVIEW M3 doctrinally clean (`tokio::process::Command` + `.kill_on_drop(true)` + `stdout: Stdio::null()` + `stderr: Stdio::piped()` per PLAN lock-in #39; direct code-spot-check of 07.2 + 08.2 precedents performed per PLAN lock-in #40 + 09 REVIEW M3 Process note + cross-verified at this state-5 review); Task 7 test shape (4 sequential GET probes; status sequence `[403, 200, 403, 200]` + body assertions); workspace test count 780 → 781; Task 8 state-4 evidence anchor complete per phase-09 Task 8 `a5ebddd` shape precedent (CI run `26070911153` + 6-row §7.5 gate table + 17-fixture per-binary CI timestamp table + Task 1-7 execution-arc summary + carryforward closures + DECISIONS.md ledger + state-5 entry routing); STATE.md modifications preserve all prior subsections verbatim per D-3.5 + D-3.4; Task 7 + Task 8 fixups (`064a8a5`, `56d381e`) properly close per-task Importants inline per the Task 6 fixup convention. The 4 Minor findings (Task 7 backstop omits lock-in #39 "5-header presence" as undisclosed deviation; PROGRESS "11 commits" vs 14-commit narrative gap; module-level doc-comment Stdio variant clarity; Total LoC delta presentation nit) are surfaced as M1 + M5 + T1 + T2 in §3 above. The 1 Awareness-only finding (pre-existing `crates/envoy-config/fuzz/Cargo.lock` untracked carryforward) is recorded as a 05.1 REVIEW carryforward; not phase-10-introduced.

**Carryforward summary across the 4-cluster reviewer findings:** 0 Importants surfaced across the 4 clusters (in contrast to phase-09's holistic pass which downgraded 6 cluster-surfaced Importants to Minor + carry-forward — phase-10's clean cluster-reviewer dispositions reflect the maturity dividend of the SPEC §6.2 process-improvement + the doctrinally-clean closures of 09 REVIEW M2 + M3). 10 cluster-Minor + 11 cluster-Awareness consolidated into the 10 named items (5 Minor M1-M5 + 2 doc-staleness D1-D2 + 3 test/audit-trail T1-T3) in §3 above. The downgrade rationale per finding is explicit: phase-10-scope-unexercised (T3 count drift), test-helper-only (M4 test use blocks; T1 module doc-comment), doc-staleness (D1 dead_code rationale; D2 Task 6 fixup carry-forwards; T2 presentation nit), undisclosed-but-defensible-deviation (M1 5-header omission), polish-only (M2 call-site doc-comment; M3 documented decision; M5 narrative-clarity). **Per `BOOTSTRAP_PROMPT.md` §5.2 the phase does NOT re-enter state 3** — no Critical or Important findings survive the cluster reviewers' first pass (no holistic downgrade required at all; the cluster reviewers themselves applied the calibrated severity classification correctly). No per-cluster finding is propose-cleanup-in-10 — the state-4 evidence anchor at `064a8a5` / CI run `26070911153` is complete; reopening would force a re-run of the state-4 gate without a commensurate quality gain. The cumulative per-task two-stage review backlog (~36 Minor controller-deferred across Tasks 1-8 per PROGRESS Task 8 cumulative summary) carries forward as the standing per-task disposition record; none is propose-cleanup-in-10.

## §8. State-4 evidence re-attestation

Per the state-5 reviewer's mandate to re-attest each of the §7.5 phase-done gate's six sub-gates against the Task 8 state-4 anchor (PROGRESS Task 8; CI run `26070911153` HEAD `064a8a5` `success` `2026-05-19T01:41:27Z`):

| Gate | Disposition | Evidence |
|---|---|---|
| **(a)** New differential fixture `0017` green | **PASS** | `http_filter_rbac_fixture` (0017) passed in CI run `26070911153` / job 76651911980 / `test` step at `2026-05-19T01:40:52.0171664Z`: `1 passed; 0 failed; 0.79s`. The ADR-0034 contract is bilaterally verified: status `[403, 200, 403, 200]` + body byte-exact `"RBAC: access denied"` (19 bytes) on deny probes + body `"ok\n"` on allow probes + `set_equal_modulo_allow_list` headers. |
| **(b)** Pre-existing 16 fixtures green simultaneously | **PASS** | All 16 (`0001-0016`) + the new `0017` GREEN simultaneously inside the unified `cargo test --workspace` in CI run `26070911153`; job conclusion `success`. PROGRESS Task 8 enumerates all 17 by name with CI timestamp + result + runtime (17-row table at lines 1413-1431). **17/17 in one run** — the full post-MVP-trunk differential surface (the phase-10 fixture 0017 extending the 09 baseline 16-fixture surface). |
| **(c)** h2spec ≥95% with known-failures.txt unchanged | **PASS** | 99.31% (`h2spec_pass_rate_gate` PASS at `2026-05-19T01:41:15.3108025Z`; 05.2 baseline carried forward; `known-failures.txt` unchanged — phase 10 engages no H2-framing surfaces; the RbacFilter operates on the post-codec abstraction per phase-07.1 ADR-0031; the fixture 0017 listener is H1 only). |
| **(d)** parse_bootstrap fuzz clean for short-budget CI run | **PASS** | Job 2 `fuzz (parse_bootstrap, 30s)` conclusion `success` (wall ~1m 20s); the Task 6 `hcm_rbac_filter.yaml` seed in corpus, exercised. `Done 223434 runs in 31 second(s)` — ~7207 exec/s effective. In-tree corroboration: `fuzz_corpus_seeds_parse_or_reject_cleanly` SUCCESS array now 17 entries (was 16 at 09 close) per the Task 6 commit `f8be570`. |
| **(e)** Stable-toolchain gates clean (fmt / clippy / build / test / deny) | **PASS** | All 5 within Job 1 `build + test + lint` clean: fmt clean; clippy `-D warnings` clean; build clean; test `781 passed; 0 failed; 2 ignored` at HEAD `064a8a5` (+1 over the predecessor Task 6 baseline 780; the +1 is Task 7's in-process backstop); cargo deny `advisories ok, bans ok, licenses ok, sources ok`. Every per-task PROGRESS attestation (Tasks 1-7 substantive + 3 fixup/SHA-record + Task 8 + Task 8 fixup) quotes the full `cargo deny check` output verbatim. |
| **(f)** REVIEW.md approved | **CLOSE at this REVIEW.md** | Verdict per §1: **Approved with M-track follow-ups** (zero Critical; zero Important; 5 Minor M1-M5 + 2 doc-staleness D1-D2 + 3 test/audit-trail T1-T3 — all awareness-only / close-opportunistically; none has a hard-gated owner; none gates the state-6 close-out). Gate (f) closes at this verdict. |

**All six §7.5 gates: (a)-(e) PASS / (f) CLOSE-at-this-REVIEW.md.** Phase 10's state-4 evidence is complete; state-5 closes at this REVIEW.md commit; state-6 (the phase-10 close-out + ROADMAP row flip + STATE.md advance + optional fold-in of opportunistic doc-staleness items per the 08.1 / 08.2 / 09 state-6 fold-in precedent) is a separate session per `BOOTSTRAP_PROMPT.md` §5.1.

---

*End of REVIEW.md. Phase 10 state-5 complete. Verdict: **Approved with M-track follow-ups** (zero Critical; zero Important; 5 Minor M1-M5 + 2 doc-staleness D1-D2 + 3 test/audit-trail T1-T3). Per `BOOTSTRAP_PROMPT.md` §5.2, the phase does NOT re-enter state 3 — no Critical or Important findings. The 09 REVIEW M2 + M3 carryforwards are confirmed closed at Task 4 `84b508e` (D5 ADR-0033 Consequences in-place amendment per preferred close shape (a)) + Task 7 `dd95673` (D8.3 `tokio::process::Command + kill_on_drop(true) + Stdio::null/piped` discipline) respectively; both chains 09 → 10 ENDED. The state-4 evidence anchor at CI run `26070911153` HEAD `064a8a5` is structurally complete + bilaterally green across the full 17-fixture Docker-gated surface + h2spec ≥95% + parse_bootstrap fuzz clean on the 17-seed corpus + 5 stable-toolchain gates + workspace `781 passed / 0 failed / 2 ignored`. The next session enters the **phase-10 state-6 close-out** — flips ROADMAP row `10` `in-progress → done` (standalone-phase invariant; phase 10 is NOT a sub-phase); advances STATE.md to the next feature-family phase pick per `BOOTSTRAP_PROMPT.md` §9 (brainstorm-driven) OR to `_none_ — awaiting next planning` sentinel; lands the state-6 close-out commit with title per the phase-10 SPEC §9 commit message format: `phase 10: envoy.filters.http.rbac + fixture 0017 + 09 REVIEW M2 + M3 close`. The 10 new carryforwards (5 Minor M1-M5 + 2 doc-staleness D1-D2 + 3 test/audit-trail T1-T3) are all awareness-only / close-opportunistically with no hard-gated owner — they do NOT gate the state-6 close-out and are recorded in §4 for the state-6 STATE.md "Phase-10 rollovers" subsection. Phase 10 IS the second post-MVP-trunk feature-family REVIEW.md, continuing the cadence set at phase-09 state-5 commit `10bb71c` + ratifying the SPEC §6.2 empirical-verification-at-PLAN-write process-improvement (validated end-to-end by the clean state-3 arc with no mid-execution ADR drama).*
