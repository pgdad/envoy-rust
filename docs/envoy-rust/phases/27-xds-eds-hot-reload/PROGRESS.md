# Phase 27 (`27-xds-eds-hot-reload`) — PROGRESS

> State-by-state + task-by-task progress log for phase 27 (file-based EDS endpoint hot-reload).
> The state-2 PLAN-write entry (incl. the §6.2 empirical-verification transcript) is below;
> each state-3 task appends its own entry (one code commit + one PROGRESS commit per task,
> per `feedback_execution_style`).
> **NOTE:** UNLIKE phase 26 (whose §6.2 was deferred to state-3 Task 1), phase 27's §6.2
> verification was run AT the state-2 PLAN-write — this dev host is native Linux (Docker
> Desktop), so the ADR-0066 container-internal `docker exec` atomic-rename methodology makes
> the reload observable. See the §6.2 transcript below + the PLAN STATUS banner.

---

## State-2 PLAN-write (this commit) — §6.2 VERIFIED at PLAN-write, ADR-0068 FIRED

- **Skill:** `superpowers:writing-plans`.
- **Authored:** `PLAN.md` (header + goal + architecture + the §6.2-VERIFIED facts V1–V6 + the PLAN-time design decisions D1-handle-type / D2-watcher-placement / D3-mirror-empty / D4-update_rejected / EDS+HC/OD-no-watcher + the SPEC-correction anchors C1–C5 + the §6.1 single-phase confirmation + the file structure + Tasks 1–9 + self-review) + this `PROGRESS.md` (skeleton + the §6.2 transcript + the Task-2 preamble) + **ADR-0068** (the §6.2 reconciliation).
- **§6.2 empirical verification: DONE this session** (transcript below). **ADR-0068 FIRES** — the empty-assignment disposition and the config_dump `last_updated` projection diverged from the SPEC; reconciled (empty-assignment MIRRORED to a MATCH; config_dump `last_updated` correction; `update_rejected` promoted to emitted; EDS+HC/OD = no watcher; §6.1 single-phase confirmed).
- **ADR posture:** **ADR-0068 FIRED** at this PLAN-write (DECISIONS.md ledger head → ADR-0068, count 69). **ADR-0069 (the §6.1 split) reserved-but-UNFIRED** (single phase confirmed). ADR-0014 in force; ADR-0028 open.
- **ROADMAP:** row `27` flips `planned → in-progress` at this commit (STATE now points at it). **STATE:** advances to `27` state-2-complete / state-3-next (next skill `superpowers:subagent-driven-development`). Superseded state-1/state-2 top-section blocks relocated to `STATE_HISTORY.md` (ADR-0035 / §4.1 inv. 9).
- **No production/test change at state-2** (docs-only PLAN-write commit — PLAN + PROGRESS + ADR-0068 + ROADMAP + STATE).

### State-2 §6.2 empirical-verification transcript (the PROTOCOL + the locked findings)

Ran the SPEC §6.2 6-item checklist against `envoyproxy/envoy:v1.33.0` (digest `sha256:56da5afd7df364350ff92de4fb49a9b09957c17295f2899f0a31cd12c28770c2`) via the **ADR-0066 Docker-Desktop methodology** — this dev host runs Docker Desktop (linuxkit VM), where host bind-mounts deliver no inotify (the `host-docker-desktop-virtiofs-no-inotify` finding), so real reloads were obtained by placing the EDS file on the Envoy container's OWN overlayfs (written by an entrypoint shell script, NO host bind-mount) and reloading it via `docker exec` write-temp-then-`mv` (atomic-rename) inside the container.

**Rig:** docker network `eds-probe-net`; `backend1`/`backend2` = `hashicorp/http-echo -text=BACKEND_ONE|BACKEND_TWO` (port 5678, single endpoint each), discovered by numeric IP (172.22.0.2 / 172.22.0.3); a 3 s-sleep python backend for the in-flight test; `envoy-probe` = `envoyproxy/envoy:v1.33.0` on the same network (`--entrypoint /bin/sh` writing `/tmp/bootstrap.yaml` + the initial `/tmp/eds/eds.yaml` then `exec envoy`); admin `:9901`, H1 listener `:10000`; `type: EDS` cluster `eds_backend` (`eds_cluster_config.eds_config.path_config_source.path: /tmp/eds/eds.yaml`, NO HC / NO OD), route `/`→`eds_backend`, router-only chain. Host left clean (all containers + network removed).

**Findings (the 6-item checklist — LOCKED; full reconciliation in ADR-0068):**

- **V1 (reload + readiness) — MATCHES.** Atomic-rename `[backend1]→[backend2]` re-targets live traffic, NO restart; `/probe` body flips `BACKEND_ONE`→`BACKEND_TWO` on the first 50 ms poll; settle **~7–8 ms** (`mv`→effective; a recovery flip ~6.7 ms); **zero 503 gap** (400-request hammer across a live swap = 0 non-200s); cluster stays up. → harness wait bound = 50 ms poll grid, ~2 s ceiling.
- **V2 (observability) — second-backend body-marker swap CONFIRMED over liveness-flip.** Markers cleanly distinguish; the swap re-targets observably. Liveness-flip (→ unreachable `127.0.0.1:1` → 503 in ~55 ms) IS observable but noisier (request-time connect-refused, membership stays `1/1`, does not prove the new endpoint serves). **D6 uses the second backend**; 503 reserved as the empty-assignment expected result.
- **V3 (`update_*` values) — MATCHES.** `update_attempt`/`update_success` = `1/1`→`2/2`→`3/3`; failure/rejected/empty = 0 on success. **Membership Envoy-only:** Envoy emits `membership_total`/`_healthy`/`_change` (`membership_change` ticks on every set change; `membership_total`→0 on empty-apply) — envoy-rust emits no `membership_total` (`cluster.rs:963`), so assert ONLY `update_*`.
- **V4 (bad-reload taxonomy) — ONE MATERIAL DIVERGENCE (empty-assignment), reconciled to a MATCH:**
  - (a) malformed YAML → `update_failure` +1, last-good 200 (`Filesystem config update failure: ... yaml-cpp: error`).
  - (b) wrong/absent `cluster_name` (`NOT_eds_backend`) → `update_rejected` +1, last-good 200 (`Filesystem config update rejected: Unexpected EDS cluster (expecting eds_backend): NOT_eds_backend`).
  - (c) unparseable address (`not-an-ip`) → `update_rejected` +1, last-good 200 (`malformed IP address: not-an-ip`).
  - (d) **EMPTY endpoint list (CLA present, `endpoints: []`)** → Envoy **APPLIES** it: `update_success` +1, `membership_total` 1→0, `/probe` → **503 "no healthy upstream"**. DIVERGES from the SPEC warm-reject projection. **RECONCILED — envoy-rust MIRRORS** (apply-empty → `update_success` → `pick()` None → `synth_no_healthy_upstream` 503; safe because `pick()` already handles empty + the 503 path exists; D-3.3 prefers the faithful mirror). See ADR-0068 V4(d).
  - (e) truly-empty envelope (`resources: []`) → Envoy `update_empty` +1, last-good 200. Envoy DISTINGUISHES (e) from (d); envoy-rust mirrors.
  - Recovery: a final good reload restored 200 on the first poll (~6.7 ms), `update_success` resumed ticking.
- **V5 (config_dump) — MATCHES wire shape; SPEC `last_updated` projection WRONG.** EDS only under `?include_eds`, under `static_endpoint_configs[]`; the embedded assignment reflects the new endpoints. **NO `last_updated`, NO `version_info`** on the EndpointsConfigDump entry (already the phase-21 shape). → D5 shrinks to read-through-handle. Post-reload subtree: `{"@type":"…EndpointsConfigDump","static_endpoint_configs":[{"endpoint_config":{"@type":"…ClusterLoadAssignment","cluster_name":"eds_backend","endpoints":[{…new addr…}],"policy":{"overprovisioning_factor":140}}}]}`.
- **V6 (in-flight isolation) — MATCHES.** A request begun pre-reload (3 s-sleep backend) completed against the OLD endpoint; the next request hit the NEW endpoint. Cursor-bounds on a shrinking set is envoy-rust-side (`i % total` over the read-once snapshot, empty short-circuited) — D8 backstop-asserted.

**Divergence summary (drives ADR-0068):** (1) empty-assignment apply-vs-reject — reconciled to a MATCH by mirroring; (2) config_dump has no `last_updated` (projection wrong) — D5 shrinks; (3) `membership_*` Envoy-only — assert `update_*` only; (4) `update_rejected` must be registered (phase-21 omitted it). Items V1/V2/V3/V6 match the projection.

### Task-2 preamble — the first state-3 implementation task (D1 endpoint-handle migration)

The state-3 arc dispatches PLAN Tasks 2–9 to fresh subagents SERIALLY (`feedback_serial_subagent_dispatch`), each with two-stage review (spec-compliance THEN code-quality), TDD per task, one code commit + one PROGRESS commit per task. **Task 1 (§6.2) is DONE** (this state-2 commit). The next unstarted task is **Task 2** (the D1 endpoint-set-handle migration — `Cluster.endpoints: Vec<SocketAddr>` → `RwLock<Arc<Vec<SocketAddr>>>`, the §6.2-independent foundation; regression witness = fixtures 0020–0029 green incl. 0029's idle watcher). Tasks 2 and 3 are §6.2-independent and may run in either order; Task 4 depends on both.

---

## Task 2 — D1 endpoint-set-handle migration (`Vec<SocketAddr>` → `RwLock<Arc<Vec<SocketAddr>>>`)

- **Skill:** `superpowers:test-driven-development` (failing test FIRST, watched it fail, then implemented).
- **What migrated (`crates/envoy-cluster/src/cluster.rs`):**
  - **Field:** `Cluster.endpoints: Vec<SocketAddr>` → `RwLock<Arc<Vec<SocketAddr>>>` (the phase-26 `HCMConfig.route_config` precedent, std-only — no `arc-swap`).
  - **New accessors on `Cluster`:** `pub fn current_endpoints(&self) -> Arc<Vec<SocketAddr>>` (read-once: `self.endpoints.read().unwrap_or_else(|p| p.into_inner()).clone()`) + `pub fn store_endpoints(&self, eps: Arc<Vec<SocketAddr>>)` (single-statement `*guard = eps` swap, same poison-recovery form). Nothing calls `store_endpoints` yet except the new tests (the reload pipeline lands in a later task).
  - **New delegate on `ClusterHandle`:** `pub fn current_endpoints(&self) -> Arc<Vec<SocketAddr>>` → `self.inner.current_endpoints()` (mandatory public reach for envoy-admin's later config_dump; `inner` is `pub(crate)`).
  - **Read sites adapted (all enumerated via `grep -n 'endpoints'`):**
    - `pick()` — snapshots ONCE at entry (`let eps = self.current_endpoints();`); empty short-circuit → `None` BEFORE any modulo (V4(d)/V6); `total = eps.len()`; fast-path + both slow-path index reads use `eps`. The slow-path eligibility arrays (`self.endpoint_health` / `self.outlier_detection.endpoints`) stay read from `self` (they do NOT live behind the handle — only the address Vec does); index-aligned with the `eps` snapshot.
    - `record_response()` — snapshots `eps` once; `eps.iter().position(..)` for the index + `total = eps.len()` for the cap; OD per-endpoint array reads unchanged.
    - `ClusterHandle::health_probe_targets()` — snapshots `eps` once, zips with the index-aligned health array (bootstrap-time; HC clusters are not reloadable in phase 27).
  - **Construct/seed sites:** `from_bootstrap` struct literal + 4 test struct literals → `endpoints: RwLock::new(Arc::new(endpoints))`. The `from_bootstrap` empty-reject (startup all-fatal) is UNCHANGED.
  - **Left untouched (verified):** `OutlierDetectionState.endpoints` (the per-endpoint ejection Vec), `load_assignment.endpoints` / `LocalityLbEndpoints` config literals, the test-helper local `endpoints` vars.
- **Tests added (`#[cfg(test)]`, all PASS):** `endpoint_handle_store_is_visible_to_next_pick` (§5.4 a+b), `endpoint_handle_inflight_snapshot_is_isolated_from_swap` (§5.4 c — read-once isolation), `endpoint_handle_store_empty_yields_none_next_pick` (§5.4 d — V4(d) apply-empty foundation), `endpoint_handle_shrinking_set_keeps_cursor_in_bounds` (§5.4 e — V6 cursor-bounds 2→1).
- **TDD evidence:** Step 2 ran `cargo test -p envoy-cluster endpoint_handle` → FAIL (6 `E0599` no-method errors — `store_endpoints`/`current_endpoints` absent). After Step 3 → 4 passed.
- **Commands + results:**
  - `cargo test -p envoy-cluster endpoint_handle` → **4 passed**.
  - `cargo test -p envoy-cluster` → **95 passed, 0 failed**.
  - `cargo clippy -p envoy-cluster --all-targets -- -D warnings` → **clean** (no `needless_borrow` / lock-guard / modulo lints).
  - `cargo build -p envoy-config -p envoy-cluster` (isolated) → green; `cargo build --workspace --all-targets` → green.
  - `cargo test --workspace` → all green EXCEPT one pre-existing, environment-dependent differential failure (see Concerns).
  - Endpoint-set consumer crates `cargo test -p envoy-cluster -p envoy-http1 -p envoy-http2 -p envoy-admin -p envoy-bin` → **all green**.
- **Code-quality review follow-up (Minor #1):** Both accessors now use poison-recovery (`.read()/.write().unwrap_or_else(|poison| poison.into_inner())`) to match the phase-26 `HCMConfig::current_route_config` / `store_route_config` precedent (`crates/envoy-http1/src/hcm.rs:248-278`) exactly — replacing the earlier `.expect("endpoints RwLock poisoned")`. Rationale (now in the accessor doc comment): a *reader* must never inherit a panic from an unrelated writer-side poison, which matters concretely because Task 4 adds a second writer (the EDS reload pipeline calls `store_endpoints`), so the lock degrades gracefully instead of becoming a latent panic site. `cargo test -p envoy-cluster` → **95 passed**; `cargo clippy -p envoy-cluster --all-targets -- -D warnings` → **clean** after the change.
- **Concerns:** `differential::admin_config_dump_server_info` FAILS on this dev host — but it fails IDENTICALLY on the clean HEAD `f43d1a6` (verified via `git stash`), with Docker-internal host data (`192.168.65.2`, `host.docker.internal`). This is the known `host-docker-desktop-virtiofs-no-inotify` / Docker-differential class: native-CI-authoritative, NOT caused by this migration. The behavior-preserving migration (fixtures 0020–0029 read paths) leaves every non-reloading request green.


## Task 3 — watcher generalization (XdsFileWatcher) [D2, §6.2-INDEPENDENT, ADR-0067]

Pure refactor — NO behavior change. The phase-26 poll/mtime/cancel watcher core is lifted into a reusable, domain-free `envoy_cluster::XdsFileWatcher` that both RDS (migrated here) and EDS (a later task) consume — the "second instance reveals the abstraction" generalization, folding the M19-1/M20/M21/M26 watcher-generalization debt.

- **Extracted to `envoy-cluster` (NEW `crates/envoy-cluster/src/xds_watch.rs`; exported from `lib.rs`):** the domain-free core — `pub struct XdsFileWatcher { handles, cancel }` with `spawn(targets, cancel) -> Self` / `shutdown(self)` / `task_count()`; the `watch_loop` (`tokio::select!` cancel-vs-`interval.tick()` poll loop with the t=0 first-tick burn + spawn-time `last_mtime` seed + CHANGED-present-mtime gate); `read_mtime`; `pub const POLL_INTERVAL` (1s, unchanged). The generic target is the **lower-ceremony boxed closure**: `pub struct WatchTarget { pub path: PathBuf, pub reload: Box<dyn FnMut() + Send> }` (hand-written `Debug` since the closure isn't `Debug`). The M26-3 one-mtime-tick coalescing caveat + the `read_mtime` note are carried forward in the doc comments. NO `HCMConfig`/RDS types — so `envoy-cluster` gains NO `envoy-http1` dep (no cycle). No new runtime deps (tokio + tokio-util already present); dev-deps `filetime` + `tempfile` + tokio `test-util` feature added for the paused-clock tests.
- **Stayed in `envoy-http1` (`crates/envoy-http1/src/rds_watcher.rs`):** the RDS-DOMAIN pieces — `RdsCounters`, the RDS `WatchTarget { path, route_config_name, store: Arc<HCMConfig>, counters }`, and the `reload()` §6.2 pipeline (reparse → revalidate → atomic `store_route_config` swap + warm-reject taxonomy ticks) — **byte-identical logic**.
- **RDS migration: SUCCEEDED (not the fallback).** `RdsWatcher` is now a thin adapter — `struct RdsWatcher { inner: XdsFileWatcher }`. `RdsWatcher::spawn` maps each RDS `WatchTarget` into a generic `envoy_cluster::WatchTarget` whose reload closure runs the existing `reload()` and logs the warm-reject (with the RDS `path` + `route_config_name` context — the central `tracing::warn!` MOVED out of the loop and INTO the closure to keep the core domain-free), then constructs the inner `XdsFileWatcher`; `shutdown`/`task_count` delegate to it. The envoy-bin call site is UNCHANGED (`envoy_http1::RdsWatcher::spawn(rds_targets, token.clone())` at main.rs:517 + `rds_watcher.shutdown().await` at main.rs:583), protecting the 0034 baseline. Refreshed the stale skeleton/no-op-stub comments (M26-5).
- **Tests added (4, paused-clock `start_paused = true`, all PASS) in `xds_watch.rs`:** `idles_when_mtime_stable_fires_no_reload_and_shuts_down` (stable mtime ⇒ 0 reloads, prompt shutdown), `empty_targets_spawn_no_tasks` (§5.2 inertness), `cancel_token_terminates_loop` (external cancel terminates), `mtime_change_fires_reload_exactly_once` (atomic-rename + explicit `filetime::set_file_mtime` bump ⇒ closure-incremented `Arc<AtomicUsize>` == 1, stable thereafter). The two RDS lifecycle tests + 4 `reload` pipeline tests in `rds_watcher.rs` stay (now exercising the adapter delegation), importing `envoy_cluster::xds_watch::POLL_INTERVAL`.
- **TDD evidence:** Step 1 wrote the 4 lifecycle tests; the mtime-change test FAILED first (`left: 0, right: 1` — the closure never fired because the rename raced the spawn-time seed); fixed by yielding to let the task seed `last_mtime` before mutating the file (paused-clock determinism), then all 4 PASS.
- **Commands + results:**
  - `cargo test -p envoy-cluster xds_watch -- --nocapture` → **4 passed**.
  - `cargo test -p envoy-cluster -p envoy-http1` → **all green** (envoy-cluster 99 + envoy-http1 120, 0 failed).
  - `cargo build --workspace --all-targets` → green; `cargo test --workspace` → all green EXCEPT the one pre-existing `differential::admin_config_dump_server_info` (see Concerns).
  - Differential backstops verified isolated: `xds_file_based_rds` → **1 passed**; `xds_rds_hot_reload` (0034 RDS reload backstop) → **passed** (isolated, repeatedly); `xds_file_based_eds` (0028/0029 idle witness) → **1 passed**.
  - `cargo clippy --workspace --all-targets -- -D warnings` → **clean** (no boxed-closure / async-lint warnings).
  - `cargo fmt --all -- --check` → clean.
- **Concerns:** `differential::admin_config_dump_server_info` FAILS on this dev host — the known `host-docker-desktop-virtiofs-no-inotify` Docker-differential class, native-CI-authoritative, NOT caused by this refactor (this task touches no admin/config_dump path). Separately, `xds_rds_hot_reload` flaked ONCE under the heavy parallel `cargo test --workspace` load (11.4s vs ~2s isolated) then passed on every isolated re-run — a Docker-startup timing flake under load, not a behavior regression (the refactor is byte-identical and the watcher is poll-based, not inotify-based).


## Task 4 — EDS reload pipeline + envoy-bin wiring [D3 + D4, §6.2-LOCKED, ADR-0068 V4]

Folds D3 (the reparse → select → validate → mirror-apply reload pipeline) + D4 (the stat-tick + the NEW `update_rejected` counter) + the in-crate watch-target builder + the envoy-bin wiring. Mirrors the phase-26 RDS reload discipline (single atomic store on success; warm-reject + class-counter on failure), with EDS's distinct apply-empty disposition.

- **Added to `Cluster` (`crates/envoy-cluster/src/cluster.rs`):** a `pub(crate) eds_reload: Option<EdsReloadState>` field — `Some` ONLY for `type: EDS` clusters (`None` for STATIC/STRICT_DNS, the §5.2 inert-when-unconfigured discipline). The NEW `pub(crate) struct EdsReloadState` bundles (per resolved decision #1, a single `Option<struct>` rather than 7 loose fields): the EDS assignment-file `path: PathBuf`, the CLA `selection_name: String`, and the 5 `Arc<Counter>` handles (`update_attempt/success/failure/empty/rejected`). All fields `pub(crate)` so the in-crate `build_eds_watch_targets` reads them. Added `pub(crate) fn ClusterHandle::into_inner(self) -> Arc<Cluster>` so the builder can reach the plainness fields + `eds_reload` (envoy-bin cannot — `inner` is `pub(crate)`).
- **`update_rejected` registration:** the phase-21 EDS-counter block (`cluster.rs` ~`:1047`) now captures all FIVE handles and retains them in `EdsReloadState`. `update_rejected` is ADDED at INITIAL VALUE 0 (the `mk("update_rejected")?` form — NO `.add(1)`, like `update_failure`/`update_empty`); `update_attempt`/`update_success` keep their `.add(1)`. The V4 "failure/empty/rejected trio = 0 on a successful initial load" witness is preserved (post-initial-load state `1/1/0/0/0`).
- **Factored numeric-IP helper:** `pub(crate) fn parse_numeric_endpoint(cluster, address, port_value) -> Result<SocketAddr, ClusterError>` lifted out of `from_bootstrap`'s `Static | Eds` endpoint loop. The startup path keeps `?`-propagating `ClusterError::EndpointParse` (startup all-fatal, UNCHANGED); the reload path CONSUMES the `Result` LOCALLY and maps a parse failure to the V4(c) reject disposition (it must NOT `?`-propagate — that would kill the watch loop).
- **NEW module `crates/envoy-cluster/src/eds_reload.rs` (exported from `lib.rs`):** holds the EDS reload pipeline (`fn reload`) + the in-crate `pub fn build_eds_watch_targets(cluster_mgr: &ClusterManager) -> Vec<xds_watch::WatchTarget>`. **Module-placement refinement (resolved decision #3):** the PLAN said "in `xds_watch.rs`", but Task 3 deliberately made `xds_watch.rs` a domain-free generic core (preserved pristine). The EDS domain code is envoy-cluster's OWN domain (parallel to RDS domain code in envoy-http1's `rds_watcher.rs`); it lives in `eds_reload.rs` and produces generic `xds_watch::WatchTarget`s whose closure runs the pipeline + logs the warm-reject with EDS context (path + selection name). This honors the PLAN's intent (EDS code in envoy-cluster) while keeping the generic core domain-free.
- **CLA selection-name mechanism (traced + mirrored, resolved decision #4):** the phase-21 initial load in `crates/envoy-config/src/lib.rs` (`load_dynamic_resources`, ~`:875-879`) selects the `ClusterLoadAssignment` by `eds.service_name.clone().unwrap_or_else(|| cluster.name.clone())` — `service_name` if set, else the cluster name. The reload captures that EXACT key as `EdsReloadState.selection_name` and selects the CLA where `cluster_name == selection_name`. The V4(b) "no CLA matches" reject fires when no CLA in the file has that name. (Test `reload_selects_by_service_name` proves the `service_name` branch.)
- **V4 taxonomy as implemented (class → counter → disposition):**
  - (a) IO read / parse / malformed / bad `@type` → `update_attempt`+`update_failure`, KEEP last-good.
  - (b) no CLA matches `selection_name` (envelope non-empty) → `update_attempt`+`update_rejected`, KEEP.
  - (c) matched CLA has an unparseable (non-numeric) endpoint → `update_attempt`+`update_rejected`, KEEP (parse error caught LOCALLY).
  - (d) matched CLA has `endpoints: []` → `update_attempt`+`update_success`, **APPLY the empty set** (`store_endpoints(Arc::new(vec![]))` → next `pick_endpoint()` returns `None` → 503). MIRRORS Envoy — a SUCCESS, not a reject.
  - (e) `resources: []` (zero CLAs) → `update_attempt`+`update_empty`, KEEP. Distinct from (d).
  - Happy (V3): `update_attempt`+`update_success` each +1; single `store_endpoints(Arc::new(new))`. Post-one-reload state `2/2/0/0/0`.
  - **Lock discipline:** reparse+select+validate produce the candidate `Vec<SocketAddr>` OUTSIDE any lock; on success/apply-empty the ONLY lock touch is the single `store_endpoints` (the write critical section is NOT widened — mirrors the phase-26 `reload()`).
- **Plainness filter (ADR-0068 Decision-5):** `build_eds_watch_targets` walks `cluster_mgr.clusters()`, takes the inner `Arc<Cluster>` via `into_inner()`, and emits a target IFF `eds_reload.is_some() && endpoint_health.is_none() && outlier_detection.is_none()`. An EDS cluster WITH active HC or outlier detection is SKIPPED (endpoints stay frozen at initial load — the deferred non-goal; NOT rejected). §5.2 inertness: a no-plain-EDS bootstrap yields an empty target list → `XdsFileWatcher::spawn` spawns zero tasks.
- **envoy-bin wiring (`crates/envoy-bin/src/main.rs`):** built + spawned beside the RDS watcher (after the `rds_watcher` spawn):
  - Build + spawn: `let eds_targets = envoy_cluster::build_eds_watch_targets(&cluster_mgr);` then `let eds_watcher = envoy_cluster::XdsFileWatcher::spawn(eds_targets, token.clone());`
  - Drain (after `rds_watcher.shutdown().await;`): `eds_watcher.shutdown().await;`
  Inert/empty when no plain EDS cluster exists; drained on BOTH clean-exit and error-exit paths via the shared signal token.
- **Tests added (12, in `eds_reload.rs`, all PASS):** `reload_happy_swaps_endpoints_and_ticks` (2/2/0/0/0 + new set applied), `reload_selects_by_service_name` (the `service_name` selection branch), `reload_io_failure_keeps_last_good_ticks_failure` (a / file deleted), `reload_malformed_keeps_last_good_ticks_failure` (a / bad YAML), `reload_no_matching_cla_keeps_last_good_ticks_rejected` (b), `reload_unparseable_endpoint_keeps_last_good_ticks_rejected` (c / non-numeric address caught locally), `reload_apply_empty_stores_empty_and_ticks_success` (d / empty set applied + `pick_endpoint()` None + 2/2/0/0/0), `reload_empty_envelope_keeps_last_good_ticks_empty` (e), and 4 builder tests (`builder_plain_eds_yields_one_target`, `builder_eds_with_health_check_yields_no_target`, `builder_eds_with_outlier_detection_yields_no_target`, `builder_no_eds_cluster_yields_no_target`). The KEEP-disposition tests assert the live set is byte-unchanged; apply-empty asserts the empty set + `pick_endpoint()` None. Tests call `reload()` directly (no watcher loop needed for the pipeline) and run the EDS initial load via `parse_bootstrap → load_dynamic_resources` (the same envoy-bin startup sequence) so `load_assignment` is populated.
- **Commands + results:**
  - `cargo test -p envoy-cluster` → **111 passed, 0 failed** (12 new EDS tests).
  - `cargo test -p envoy-cluster -p envoy-bin` → green (envoy-bin: all integration tests pass, including `xds_file_based_eds` — the 0028/0029 idle-EDS witness — at 8 passed).
  - `cargo clippy --workspace --all-targets -- -D warnings` → **clean** (exit 0).
  - `cargo build --workspace --all-targets` → green; `cargo test --workspace` → all green EXCEPT the one pre-existing `differential::admin_config_dump_server_info` (see below).
- **Code-quality review follow-up (Minor):** the warm-reject `tracing::warn!` log field was renamed `cluster_name` → `selection_name` in `eds_reload.rs` to match the variable's own name and the doc vocabulary (the value is the EDS `service_name` when set, else the cluster name — labeling it `cluster_name` could mislead an operator during incident triage). The rest of the log line (path, error, message) is unchanged. `cargo test -p envoy-cluster eds_reload` → 12 passed; `cargo clippy -p envoy-cluster --all-targets -- -D warnings` → clean.
- **Concerns:** `differential::admin_config_dump_server_info` FAILS on this dev host — the known `host-docker-desktop-virtiofs-no-inotify` Docker-differential class, native-CI-authoritative, fails identically on clean HEAD; NOT caused by this task (it touches no admin/config_dump path). One transient parallel-port-bind flake on `admin_ready` under the full `cargo test --workspace` load resolved on isolated re-run (and on a full re-run) — a test-harness binding race, not a behavior regression.

## Task 5 — config_dump reads through the swappable handle [D5, §6.2-LOCKED V5, ADR-0068]
- **Goal (D5, NARROWED):** make `/config_dump?include_eds`'s `EndpointsConfigDump` render the EDS cluster's endpoints THROUGH the LIVE, swappable endpoint handle so a post-reload dump reflects the hot-reloaded set, not the frozen startup bootstrap `load_assignment`. NO `last_updated`, NO `version_info` (the phase-21 shape is unchanged); ONLY the endpoint source changes.
- **Live-read mechanism:** the renderer (`crates/envoy-admin/src/endpoint.rs`, the EndpointsConfigDump block in `render_config_dump`) reaches the live cluster through the `AdminHandler`'s `cluster_manager()` accessor: `handler.cluster_manager().get(&c.name)` → `ClusterHandle` → `current_endpoints()` → read-once `Arc<Vec<SocketAddr>>`. Mirrors the phase-26 `RouteSnapshot` precedent: because `ClusterLoadAssignmentBody.endpoints` borrows `&Vec<LocalityLbEndpoints>`, we first materialize an OWNED `Vec<EndpointSnapshot>` (each owning a reconstructed `Vec<LocalityLbEndpoints>` + the borrowed `cluster_name`) that outlives serde serialization, then build the borrowing entries from it.
- **Shape approach — RECONSTRUCT (not project):** the live data is a flat `Vec<SocketAddr>`, but the phase-21 entry serializes `&Vec<LocalityLbEndpoints>` (the rich locality structure). We reconstruct ONE synthetic `LocalityLbEndpoints` from the live addresses (each `SocketAddr` → an `LbEndpoint` with `endpoint.address.socket_address.{address,port_value}` via `sa.ip().to_string()` / `sa.port()`) and feed the EXISTING `ClusterLoadAssignmentBody` serializer. Reusing the exact phase-21 serializer is what guarantees byte-identical JSON for an idle cluster — the lowest-risk path. `cluster_name` is the cluster identity (unchanged by reload), so it stays sourced from the bootstrap `la.cluster_name`; only the ENDPOINTS come from the live handle. A defensive fallback reconstructs from the bootstrap flat addresses when the cluster is absent from the manager (the `ClusterManager::empty()` test path; in production every EDS cluster has a live handle).
- **Why it preserves fixture-0029 byte-parity:** 0029 (`expectations.yaml`) is the idle-watcher witness — its EDS file never changes mid-test, so its live endpoints == its initial endpoints. Its `/config_dump?include_eds` rule is a `json_shape` rule whose `required_subtree` pins ONLY `…endpoint_config.cluster_name == eds_backend` (per-side `path_envoy: configs.2…` / `path_envoy_rust: configs.1…`) with `value_may_differ_keys: ["configs"]` — the differential does NOT compare endpoint addresses, and `cluster_name` is unchanged. The byte-parity bar that DOES compare the endpoint shape is the in-crate unit assertion (`eds_cluster_emits_endpoints_config_dump`, which pins `…socket_address.address == 127.0.0.1`); the reconstruct-through-the-phase-21-serializer approach keeps that green (a numeric-IP round-trip `127.0.0.1` → `ip().to_string()` is identity).
- **Cross-crate swap API:** added a `pub ClusterHandle::store_endpoints` delegate to `Cluster::store_endpoints` (the production reload pipeline drives the swap through the in-crate `eds_reload` module reaching `inner` directly; `inner` is `pub(crate)`, so cross-crate callers/tests holding only a handle need the public delegate).
- **Spec-review encapsulation fix (applied):** the `store_endpoints` delegate was marked `#[doc(hidden)] pub` (was bare `pub`) so it reads as cross-crate TEST reach only — NOT a sanctioned production API — matching the same-file `ClusterManager::empty()` / `is_endpoint_ejected_for_test` precedents. This honors the PLAN's locked `ClusterHandle` public surface: `current_endpoints()` is the production READ accessor; production endpoint WRITES stay in-crate via `eds_reload`. API-visibility only, no behavior change (`cargo test -p envoy-admin` → 97 passed; `cargo clippy --workspace --all-targets -- -D warnings` → clean).
- **Test added (TDD core):** `config_dump_reflects_hot_reloaded_endpoints` (`#[tokio::test]` in `endpoints_config_dump_tests`) builds an `AdminHandler` over a LIVE `ClusterManager` (`envoy_cluster::from_bootstrap` with the `eds_backend` EDS cluster), renders + asserts the initial endpoint (`127.0.0.1:8080`), swaps via `cluster_manager().get("eds_backend").store_endpoints(Arc::new(vec![10.0.0.7:9443]))`, re-renders + asserts the NEW endpoint (address `10.0.0.7`, port `9443`) while `cluster_name` stays `eds_backend`. FAILED first against the bootstrap-snapshot renderer (post-swap showed stale `127.0.0.1`), PASSES after re-pointing at the live handle. Added a `handler_with_live_manager` helper (mirrors `handler_from_bootstrap` but threads the real manager).
- **Results:**
  - `cargo test -p envoy-admin` → **97 passed, 0 failed** (the new test + all phase-21 EDS byte-parity tests green).
  - `cargo clippy --workspace --all-targets -- -D warnings` → **clean** (exit 0).
  - `cargo build --workspace --all-targets` → green; `cargo test --workspace` → all green EXCEPT the one pre-existing `differential::admin_config_dump_server_info` (the known `host-docker-desktop-virtiofs-no-inotify` Docker-differential class, native-CI-authoritative, fails identically on clean HEAD; NOT this task).
- **Concerns:** the fixture-0029 `/config_dump?include_eds` differential is Docker-gated (native-CI-authoritative on this host) — its byte-parity is enforced locally by the in-crate unit assertions + by construction (reusing the phase-21 serializer); the differential itself runs authoritatively in CI at the state-4 gate.

## Task 6 — harness: EDS reload step + second distinguishable backend [D6, §6.2-LOCKED V2, ADR-0068]
- **Goal (D6):** build the DIFFERENTIAL-HARNESS capability Task 7's fixture 0035 consumes — a second distinguishable single-endpoint echo backend + an "EDS reload step" that atomic-renames new per-side EDS contents over the mounted path then settles on the discriminating observable (the EDS sibling of the phase-26 RDS reload step). Reuses the phase-26 atomic-rename + bounded wait-for-convergence machinery.
- **New Driver/step (`tests/differential/src/lib.rs`):** `Driver::Http1EdsReload { pre_probes, reload: EdsReloadStep, post_probes }` mirrors `Http1RdsReload`. `EdsReloadStep` is `#[serde(deny_unknown_fields)]` with `reload_file` (default `default_eds_reload_file` → `eds-reload.yaml`), `settle_budget_ms` (the ~8 ms settle with generous slack, e.g. 2000), and `discriminator: Http1Probe`. The `kind: http1_eds_reload` driver routes through the same HCM `{{PORT}}` listener as the other HTTP drivers (added to the `port_key` match disjunction).
- **Dispatch arm (mirrors `Http1RdsReload`):** runs `pre_probes` bilaterally (backend_1 serving) → renders the post-reload EDS template per-side (`upstream_kvs_refs` / `subject_kvs_refs`, same residual-marker guard) → atomic-renames BOTH sides (`atomic_rename_over(&subject_eds_path, ..)` for the host file + `upstream.reload_eds_atomic(..)` for the in-container file via docker exec) → bounded `wait_for_reload_convergence` on the discriminator each side → `post_probes` bilaterally (backend_2 serving). Reuses `run_http1_probe_bilateral`, `atomic_rename_over`, `wait_for_reload_convergence` verbatim. Bails up front if the fixture is not EDS-based (`upstream_eds_path.is_none()`).
- **M26-2 GUARD (folds the phase-26 fix the RDS arm omitted):** new `pub fn eds_reload_discriminator_is_load_bearing(probe)` returns `expected_status.is_some() || expected_body.is_some()`. The dispatch arm calls it at its START (BEFORE driving anything) and `bail!`s if false — a both-`None` discriminator makes `wait_for_reload_convergence` return Ok on the first poll (`status_ok && body_ok == true` with both expectations absent), reporting spurious instant "convergence" before the reload took effect. The RDS arm did not guard this (M26-2); this folds the fix into the EDS arm.
- **`upstream::reload_eds_atomic` (`tests/differential/src/upstream.rs`):** EDS sibling of `reload_rds_atomic` — base64-encodes on the host, decodes into a container-side `.reload-tmp` sibling of `EDS_CONTAINER_PATH`, `mv -f` (same-fs atomic rename; the only rewrite Envoy's default file-watch observes), `CmdWaitFor::exit_code(0)` to block until the exec completes. Must run INSIDE the container (virtiofs bind-mounts don't propagate inotify). NOT locally unit-tested (requires Docker).
- **Second distinguishable backend:**
  - **http1-echo-server helper change (`tests/helpers/http1-echo-server/src/main.rs`):** new optional `--body-marker <s>` arg. When set, `build_echo_body` prepends a leading `backend: <marker>\n` line so a `GET /probe` response identifies WHICH backend served it; `None` ⇒ the body is byte-identical to the pre-27 SPEC §3 D3 shape, so every existing fixture's differential equality holds. (`build_echo_body` gained a third `body_marker: Option<&str>` param; `run`/`handle_connection` thread the marker via an `Arc<Option<String>>`.)
  - **`Http1EchoBackend::spawn_with_marker(marker)` (`backend.rs`):** spawns the helper with `--body-marker <marker>`; `spawn()` is the no-marker shim (both delegate to a private `spawn_inner`).
  - **Marker convention (`run_fixture`):** `backend_1` / `backend_2` are spawned independently, each gated on its own `{{HTTP1_BACKEND_1_PORT}}` / `{{HTTP1_BACKEND_2_PORT}}` marker (mirroring the single-backend `{{HTTP1_BACKEND_PORT}}` convention; the bare-token `scan_needs_marker` does NOT alias the existing `HTTP1_BACKEND_PORT` token, so all pre-27 fixtures stay inert). Both ports are injected into both per-side kv maps, and the `BACKEND_HOST` gate is widened to fire for them so the EDS file's `[backend_1]`→`[backend_2]` swap is a real endpoint swap.
- **Locally tested (5 new harness-unit tests, all PASS):**
  - http1-echo-server: `argv_parses_body_marker`, `body_marker_prepends_a_marker_line` (and `argv_parses_full_invocation` extended for the `None` default) — 7 passed.
  - `backend::tests::http1_echo_backends_are_distinguishable_by_marker` — spawns backend_1 + backend_2, asserts distinct ports + per-backend `backend: <marker>` body lines.
  - `eds_harness_tests`: `driver_http1_eds_reload_round_trips_through_serde` (schema + `eds-reload.yaml` default + load-bearing discriminator), `eds_reload_step_rejects_unknown_field` (deny_unknown_fields), `eds_reload_template_renders_per_side_and_swaps_backend` (per-side `{{EDS_BACKEND_IP}}` + backend_2 port swap), `eds_reload_discriminator_both_none_is_rejected` (the M26-2 guard: both-None rejected, status-only / body-only accepted).
- **Deferred to CI (native-Linux-authoritative):** the FULL reload differential (Envoy-in-Docker observing the in-container EDS file atomic-rename) is NOT locally observable under Docker Desktop virtiofs (this dev host — `host-docker-desktop-virtiofs-no-inotify`); fixture 0035 (Task 7) provides the differential evidence at the state-4 CI gate. Task 6's LOCAL bar is the plumbing above (backend spawn + distinguishability, atomic-rename helper, the M26-2 guard, schema deserialization).
- **Commands + results:**
  - `cargo test -p http1-echo-server` → **7 passed**.
  - `cargo test -p differential --lib` → **145 passed, 0 failed, 2 ignored** (the 2 ignored are the Docker-gated `discover_host_gateway_ip_returns_numeric_ipv4` + one other; the 5 new tests included).
  - `cargo clippy --workspace --all-targets -- -D warnings` → **clean** (exit 0).
  - `cargo build --workspace --all-targets` → green; `cargo fmt --check` → clean.
  - RDS path UNTOUCHED: `driver_http1_rds_reload_round_trips_through_serde` still passes; fixture 0034 (`tests/fixtures/0034-xds-rds-hot-reload`) unchanged; the additions are purely additive (a new Driver variant + a second-backend capability gated on new markers).
- **Concerns:** none for the local bar. The full EDS reload differential is Docker-gated / native-CI-authoritative (this host can't observe the bind-mount reload). The pre-existing `differential::admin_config_dump_server_info` failure (same virtiofs-no-inotify class) is NOT a Task-6 regression — Task 6 touches no admin/config_dump path.
- **Code-review fix (applied, Minor #1):** added an inline pointer comment to `reload_eds_atomic` (`tests/differential/src/upstream.rs`) referencing `reload_rds_atomic` for why `CmdWaitFor::exit_code(0)` is load-bearing (exec returns before the in-container `mv` completes, so `exit_code()` reads `None` and the reload spuriously fails) and why the exit code is re-read as a belt-and-suspenders guard — prevents a future reader simplifying the EDS helper from reintroducing that bug. Comment-only, no logic change; build + clippy still green.

## Task 7 — fixture 0035 + in-process backstop [D7 + D8, §6.2-LOCKED, ADR-0067/0068]
- **Goal (D7 + D8):** author the differential fixture `0035-xds-eds-hot-reload` (the EDS-reload bilateral evidence, native-Linux-CI-authoritative) + the Docker-gated wrapper + the in-process backstop (the local complement carrying the counter / config_dump / bad-reload-taxonomy proofs the Docker differential can't see).
- **Fixture `tests/fixtures/0035-xds-eds-hot-reload/`** (mirrors 0034's hot-reload structure + 0029's EDS topology):
  - `envoy-rust.yaml` / `envoy.yaml`: ONE static H1 HCM listener INLINE-routing `/probe` → a PLAIN `type: EDS` cluster `eds_backend` (NO inline `load_assignment`, NO `health_checks`, NO `outlier_detection` — only a plain EDS cluster gets a reload watcher, §0 finding 3 / Decision-5). Port-0 admin on BOTH sides (the `Http1EdsReload` driver never scrapes admin, so `needs_admin_port` does NOT substitute `{{ADMIN_PORT}}` — a literal would crash Envoy / leave a residual marker; mirrors 0034). `envoy.yaml` carries the 0029 per-side byte-exact-body stripping knobs (`generate_request_id: false` + a header_mutation `request_mutations` filter + `suppress_envoy_headers: true`); `envoy-rust.yaml` omits them (envoy-rust strips natively). Dropped 0029's `response_mutations` re-add (the discriminator asserts body only, not `x-envoy-upstream-service-time` presence).
  - `eds.yaml` (initial, endpoint → `{{HTTP1_BACKEND_1_PORT}}`) + `eds-reload.yaml` (post-reload, endpoint → `{{HTTP1_BACKEND_2_PORT}}`): SINGLE SHARED templates rendered per-side; `{{EDS_BACKEND_IP}}` → host-gateway IP (upstream) vs `127.0.0.1` (subject), reusing 0029's numeric-IP rendering (EDS rejects hostnames). `eds.yaml` references `{{HTTP1_BACKEND_2_PORT}}` **in a comment** so the harness (which scans ONLY `eds.yaml` for backend-spawn) spawns backend_2 and binds its port key — otherwise the reload render would bail on the unsubstituted token.
  - `expectations.yaml`: `Driver::Http1EdsReload` — `pre_probes` (`/probe` byte-exact body `backend: backend_1\n…`), `reload` (`reload_file` defaults to `eds-reload.yaml`, `settle_budget_ms: 5000`, discriminator `/probe` byte-exact `backend: backend_2\n…` — load-bearing, passes the M26-2 guard), `post_probes` (byte-exact `backend: backend_2\n…`). Validated: parses through the harness `Expectations` deserializer; discriminator is load-bearing.
  - `README.md`: documents the three-phase sequence, the two-distinguishable-backend topology, the numeric per-side IP rendering, the eds.yaml-comment backend-spawn trick, the Native-Linux-CI-authoritativeness, and what lives in the backstop vs the fixture.
- **Differential wrapper `tests/differential/tests/xds_eds_hot_reload.rs`:** mirrors `xds_rds_hot_reload.rs`, points at fixture 0035, comments the Docker-Desktop-virtiofs local-unobservability. Docker-gated at the harness cluster level (no per-test cfg gate).
- **In-process backstop `crates/envoy-bin/tests/xds_eds_hot_reload.rs`** (boots real `envoy-bin` as a NATIVE subprocess — observes the ~1s mtime-poll watcher on a host-side atomic-rename; combines the RDS-reload + EDS-load backstop patterns). **8 tests, ALL GREEN:**
  - happy reload (#1): counters `cluster.eds_backend.update_{attempt,success,failure,rejected,empty}` `1/1/0/0/0` → `2/2/0/0/0` (counter level); endpoint flips backend_1 → backend_2 (data-plane level); `/config_dump?include_eds` EndpointsConfigDump reflects the new endpoint port + `cluster_name == eds_backend`, carries NEITHER `last_updated` NOR `version_info` (config_dump level, scoped to the EDS subtree — `last_updated` legitimately appears elsewhere).
  - V4(a) malformed (#2): `update_failure`+1, last-good kept (counter + data-plane).
  - V4(b) no-matching-CLA (#3): `update_rejected`+1, last-good kept.
  - V4(c) unparseable endpoint (#4): `update_rejected`+1, last-good kept.
  - V4(d) apply-empty (#5): `update_success`+1 (an empty assignment is a SUCCESS, mirroring Envoy), APPLY the empty set → `/probe` is a synth-503 `no healthy upstream` (19 bytes, byte-asserted).
  - V4(e) empty-envelope (#6): `update_empty`+1, last-good kept (200, not 503).
  - in-flight isolation (V6, #7): a request that picked a SLOW backend_1 completes 200 against it across a concurrent reload to backend_2; the next pick sees backend_2.
  - cursor-bounds shrinking 2→1 (#8): advance the round-robin cursor over 2 endpoints, reload to 1; every subsequent pick is 200 (`i % total` re-bounds), no panic, counters `2/2/0/0/0`.
  - Header records the M18-9 / M26-6 deferred shared-test-support-crate extraction pressure (now the 3rd atomic-rename-helper user / ≥7th copy of the reserve_port/wait_ready/http1_oneshot/scrape block) — extraction stays a future hardening task.
- **Locally GREEN (this Docker-Desktop host):**
  - `cargo build --workspace --all-targets` → green (pre-built `tests/helpers/*` first per the cold-helper-compile flake note).
  - `cargo test -p envoy-bin --test xds_eds_hot_reload` → **8 passed, 0 failed** (deterministic across 2 runs).
  - `cargo build -p differential --all-targets` → green (the wrapper compiles).
  - `cargo clippy --workspace --all-targets -- -D warnings` → clean (exit 0); `cargo fmt --check` → clean.
- **CI-DEFERRED (native-Linux-authoritative):** the fixture-0035 reload differential (Envoy-in-Docker observing the in-container EDS atomic-rename) is NOT observable under Docker Desktop virtiofs (`host-docker-desktop-virtiofs-no-inotify`); it runs authoritatively at the state-4 CI gate. The known pre-existing `differential::admin_config_dump_server_info` failure (same virtiofs class) is NOT a Task-7 regression — Task 7 is purely additive (a new fixture + two new test files; no existing file touched).
- **Concerns:** the bad-reload **bilateral** class V4(a) (both proxies keep last-good) cannot be expressed in the single-reload `Http1EdsReload` driver schema (locked at Task 6 — it has exactly pre/reload/post, no second-reload slot), so that bilateral assertion lives in the backstop (which drives a malformed reload directly) rather than in the fixture. Documented in the fixture README. No other concerns for the local bar.

## Task 8 — BEHAVIOR_CONTRACT EDS-reload extensions

DOCS-ONLY (no source/test changes). Extended `docs/envoy-rust/BEHAVIOR_CONTRACT.md` with the phase-27 EDS-hot-reload semantics, mirroring the phase-26 RDS precedent in BOTH sections.

- **§2.1 Stat-name mapping — new "27 entries (EDS hot-reload …)" block** added immediately AFTER the phase-21 EDS `cluster.<name>.update_*` rows. States the per-reload increment semantic (post-boot `update_attempt/success/failure/rejected/empty` = `1/1/0/0/0` → `2/2/0/0/0` after one reload → `3/3/0/0/0` after two), the **`update_rejected` PROMOTION** from Envoy-only to REGISTERED+EMITTED by envoy-rust (ADR-0068 §Decision-3, stated as a phase-27 CHANGE — the phase-21 row is NOT edited), and the full V4 five-class taxonomy table (Successful / malformed→`update_failure` / no-matching-CLA→`update_rejected` / unparseable-address→`update_rejected` / apply-empty→`update_success`+503 / empty-envelope→`update_empty`). Notes the taxonomy MIRRORS Envoy in ALL FIVE classes (no behavioral divergence; only the mechanism divergence remains) and that the conditional-registration / membership-gauge / Envoy-only narrowings are unchanged.
- **§2.2 xDS-wire-state-machine — new "### Filesystem transport (`path_config_source`) — phase 27 EDS HOT-RELOAD extension"** subsection added AFTER the phase-21 EDS subsection. Lettered subsections mirror the phase-26 RDS block: (g) watch trigger + mtime-poll-vs-inotify mechanism divergence (carried from ADR-0066; atomic-rename harness op; `XdsFileWatcher` generalization), (h) atomic-apply + in-flight read-once isolation behind `RwLock<Arc<Vec<SocketAddr>>>` (V6), (i) the warm-reject/apply-empty taxonomy table (apply-empty MIRROR headline + the deliberate startup-rejects-empty / reload-applies-empty asymmetry; explicitly contrasted with the phase-26 RDS unknown-cluster RECORDED DIVERGENCE), (j) `EndpointsConfigDump` reflects NEW endpoints through the live handle with NO `last_updated`/`version_info` change (ADR-0068 §Decision-2), (k) MVP plain-cluster-only scope + EDS+HC/OD-no-watcher deferred non-goal.
- **Nice-to-have:** amended the existing line-684 `EndpointsConfigDump` `/config_dump` row with a phase-27-reload note (reflects POST-RELOAD endpoints through the live handle; still no `version_info`/`last_updated`).

**Key facts locked accurately (ADR-0068):** apply-empty MIRRORS Envoy (`update_success`+1, apply empty → 503 "no healthy upstream" 19 bytes, last-good NOT kept); empty-envelope (`resources: []`) is DISTINCT (`update_empty`+1, last-good kept); `update_rejected` promoted (post-boot `1/1/0/0/0`); fixture 0035 asserts `2/2` (data-plane flip is the differential proof, the rest backstop-only); NO config_dump `last_updated` change; EDS reload has NO behavioral divergence (only inotify-vs-mtime mechanism). Did NOT contradict the phase-21 rows — the `update_rejected` promotion is written as an explicit phase-27 CHANGE.

Two commits (per-task cadence): `docs(contract): phase 27 EDS hot-reload semantics (§2.1 stat rows + §2.2 wire-state)` + this PROGRESS append.

## Task 9 — state-4 verification gate (§7.5) — GREEN

- **Skill:** `superpowers:verification-before-completion` (evidence before assertions — every gate command run, output confirmed, before any "complete" claim).
- **Scope:** verification + STATE-advance only. The phase-27 code is ALREADY committed in the Task 2–8 chain (`7226c05`…`acac6d4`, pushed to `origin/main`); this state-4 step touches ONLY docs (STATE.md + PROGRESS.md). No source/test/ROADMAP/DECISIONS change.

### Local gates (this Docker-Desktop dev host)

- `cargo fmt --all -- --check` → **exit 0** (clean — no diff).
- Standalone-crate builds `cargo build -p envoy-config -p envoy-cluster -p envoy-http1 -p envoy-admin -p envoy-bin` → **exit 0** (each crate compiles in isolation — the §0 standalone-buildability discipline).
- `cargo build --workspace --all-targets` → **exit 0**.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` → **exit 0** (clean — `--all-features`).
- `cargo deny check` → **exit 0** (`advisories ok, bans ok, licenses ok, sources ok`).
- Non-Docker `cargo test --workspace --exclude differential --no-fail-fast` → **1182 passed**. The single transient failure `envoy_http2 …send_request_maps_h2_handshake_failure_to_typed_error` is a KNOWN load-induced flake (passes 3/3 in isolation; untouched by phase 27; green on CI). The local `differential::admin_config_dump_server_info` failure is the known Docker-Desktop-virtiofs / host-gateway environmental divergence (`host-docker-desktop-virtiofs-no-inotify`) — NOT a regression; green on Linux CI.

### AUTHORITATIVE Linux CI — run `27818702552` @ code HEAD `acac6d4`

BOTH jobs GREEN:
- **"build + test + lint"** (4m3s): the full `cargo test --workspace` ran on Linux with **ZERO failures**, INCLUDING the Docker-gated differential — **`test xds_eds_hot_reload_fixture … ok`** (fixture 0035's EDS-reload differential — the never-locally-observable atomic-rename reload, bilaterally equivalent to upstream Envoy v1.33.0), all 35 Docker-gated fixtures `0001`–`0035`, and **`test h2spec_pass_rate_gate … ok`** (conformance ≥95%).
- **"fuzz (parse_bootstrap + jwt_parse, 30s each)"** (3m43s): clean.

### §7.5 gate verdict — GREEN

- (a) fixture 0035 (`xds_eds_hot_reload_fixture`) green ✓
- (b) all pre-existing fixtures (`0001`–`0034`) green ✓
- (c) h2spec pass-rate ≥95% ✓
- (d) fuzz job clean ✓
- (e) build / clippy (`--all-features`) / fmt / `cargo test` / `cargo deny check` clean ✓
- (f) `REVIEW.md` ABSENT → next step is the state-5 code review ✓

**Outcome:** STATE advances to **state-4 verification COMPLETE / state-5-next**; ROADMAP row `27` stays `in-progress` (flips `done` only at state-6). The next session runs `superpowers:requesting-code-review` (authors `REVIEW.md`; if issues → back to state-3 per §5.2; if approved → state-6 close-out). The superseded state-2 PLAN-write top-section narratives are relocated verbatim to `STATE_HISTORY.md` (ADR-0035 / §4.1 inv. 9); the `### Phase-27 state-2 PLAN-write` + `### Phase-27 state-1 brainstorm` Notes subsections STAY in STATE.md (phase 27 is still in-progress — they relocate at the state-6 close-out).

## State-5 — code review (`superpowers:requesting-code-review`) — APPROVED

The state-5 holistic phase review. **Review model:** each of the 8 state-3 tasks (D1–D8) was already individually two-stage-reviewed (spec-compliance THEN code-quality) during execution, so state-5 is the cross-cutting system-level pass — a single fresh `superpowers:code-reviewer` subagent given crafted context (not session history), tasked with the six integration seams + triage of the per-task Minors.

- **Review range:** `f43d1a6` (state-2 PLAN-write base) … `acac6d4` (Task-8 BEHAVIOR_CONTRACT) — the full phase-27 production + test diff (+3682 / −179, 23 files). The state-4 STATE-advance commit `334c093` is docs-only, out of code scope.
- **Differential anchor:** the AUTHORITATIVE native-Linux CI run **`27818702552`** @ `acac6d4` (fixture 0035 EDS-reload differential `xds_eds_hot_reload_fixture … ok` + all 35 fixtures + h2spec ≥95% + fuzz, GREEN).

### Verdict — APPROVED (0 Critical / 0 Important / 3 Minor non-blocking)

All SIX cross-cutting focus areas verified PASS:
1. **D1 endpoint-handle correctness** — `pick()` snapshots the `Arc<Vec>` once at entry and returns `None` on empty BEFORE any `% total`, so a shrinking endpoint set (2→1) can neither index out of bounds nor divide by zero on either LB path; poison recovery (`.unwrap_or_else(|p| p.into_inner())`) consistent across read AND write sites; the hot-path RwLock read is the SPEC §5.1-accepted tradeoff.
2. **D2 XdsFileWatcher generalization** — `xds_watch.rs` is genuinely domain-free; the `RdsWatcher` reload pipeline is byte-equivalent after the reseat (RDS witness = fixture 0034, green); NO crate cycle (`envoy-cluster` gained no `envoy-http1` dep; only dev-deps `filetime` + `tempfile` added).
3. **V4 bad-reload taxonomy fidelity** — all IO/parse/validate outside the lock, single-move swap, no TOCTOU; the five dispatch arms tick exactly the right counter (apply-empty→`update_success`+503 MIRROR; IO/parse→`update_failure`; wrong-name/bad-IP→`update_rejected`; empty-envelope→`update_empty`); 503 body the correct 19-byte `"no healthy upstream"`.
4. **C-1/C-2 encapsulation boundary** — writes only in-crate; reads via `pub current_endpoints()`; `store_endpoints` `#[doc(hidden)] pub`; `into_inner` `pub(crate)`.
5. **EDS+HC/OD no-watcher safety** — `build_eds_watch_targets` emits a target only when `endpoint_health.is_none() && outlier_detection.is_none()`; desync closed by construction.
6. **config_dump byte-parity** — `EndpointsConfigDump` reconstructs through the phase-21 serializer; fixture 0029 idle witness green.

**Minors (M27-1..M27-3, all non-blocking hardening nits → phase-28 carry-forwards):** M27-1 tighten `Cluster::store_endpoints` `pub` → `pub(crate)` (effectively unreachable cross-crate today, not an actual leak); M27-2 add a slow-path `debug_assert_eq!(eps.len(), health.len())` length-coupling (defense-in-depth); M27-3 the 400ms in-flight-isolation backstop sleep is the sole non-bounded timing assumption (well-cushioned by a 2s slow-backend delay). The per-task Minors logged during state-3 (Task 5 config_dump fallback/substring; Task 6 RDS/EDS dispatch-arm duplication; Task 7 backstop-vs-fixture placement + the now-3rd atomic-rename-helper user + body-agnostic cursor test) were re-triaged and CONFIRMED non-blocking; the reviewer additionally reproduced the documented port-TOCTOU flake once under full parallel load (passes 8/8 isolated; CI-authoritative; reinforces — does not block — the deferred shared-test-support-crate extraction).

This is the **twelfth consecutive clean state-5** (after 17–26). Per §5.2 an approved review with no Critical/Important does NOT re-enter state-3. **§7.5 gate (a)–(e) GREEN at CI `27818702552`; (f) satisfied by the approved `REVIEW.md`.**

**Outcome:** STATE advances to **state-5 review COMPLETE (`REVIEW.md` APPROVED) / state-6-next**. The next session performs the state-6 deterministic close-out (one docs-only commit `phase 27: file-based EDS endpoint hot reload [ADR-0067, ADR-0068]` per §5.3; flip ROADMAP row `27` `in-progress` → `done`; advance STATE to the next phase / awaiting-next-planning; relocate the `### Phase-27 …` Notes subsections per ADR-0035; push). This docs-only state-5 commit adds `REVIEW.md` + this PROGRESS entry + the STATE advance (the superseded state-4 top-section blocks relocated verbatim to `STATE_HISTORY.md` per ADR-0035).
