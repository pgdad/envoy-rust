# Phase 06.3 (`06.3-stats-wiring-and-close`) — REVIEW

- **Phase id:** `06.3`
- **Slug:** `06.3-stats-wiring-and-close`
- **Reviewed range:** `3a964cc..42fc726` (17 files, 3057 insertions / 90 deletions across 13 commits — `1516cb8` Task 2 validator gate / `c995cce` Task 3 `BodyRule::PrometheusExposition` value-side / `a23a21b` Task 4 per-class HCM counters + 06.2 I1 / `45cb096` Task 5 listener `cx_active` / `b32f096` Task 6 cluster `cx_active` + `ConnGaugeGuard` / `6bb0aa7` Task 7 upstream router counters + H2 inline / `fb9f277` Task 8 listener accept-failure / `3d36c11` Task 9 admin idle read timeout / `d050e23` Task 10 access_logs counters + 06.2 I2 / `06bdf14` Task 11 BEHAVIOR_CONTRACT + fixture 0011 value-exact + 06.2 M3 / `b46025e` Task 11 fixup empty `value_exact` / `7cdc1a8` Task 12 state-4 verification / `42fc726` state-advance STATE.md). The state-3 substantive arc spans 11 commits (`3a964cc..7cdc1a8`); the state-advance commit `42fc726` is doc-only (STATE.md edit) and falls naturally inside the review range per the established cadence.
- **CI evidence cited:** PROGRESS.md Task 12 records local-gate evidence only at HEAD `7cdc1a8` — all 5 stable-toolchain gates clean locally (`cargo build` / `cargo clippy --workspace --all-targets --all-features -- -D warnings` / `cargo fmt --all -- --check` / `cargo test --workspace --lib --bins` reporting 499 passed / 0 failed / 2 ignored / `cargo deny check` advisories+bans+licenses+sources ok); all 12 Docker-gated differential fixtures (0001-0012) GREEN simultaneously; h2spec ≥95% carries from 05.2 D7 baseline 99.31% (no H2 surface engaged in 06.3); `parse_bootstrap` fuzz unchanged at 17 seeds (no new target per architecture decision 20). The canonical §7.5 (a)-(e) CI run URL is deferred to a state-4-followup in a session with explicit push authorization (the executor session did not push to `origin/main` per shared-state-impact discipline). See §3 Important I3 below.
- **Reviewed:** 2026-05-12.

## §1. Verdict

**Approved with M-track follow-ups.**

Sub-phase 06.3 ships its seven SPEC deliverables (D14.3 `Http2ClusterFromHttp1Listener` parse-time validator gate closing 05.3 REVIEW I1; D15.3.a-e comprehensive HCM/router/listener/cluster stats wiring — per-response-class HCM counters, connection-lifetime gauges with `ConnGaugeGuard` RAII, upstream-side router counters, access-log line counter + sibling failure counter, listener accept-failure counter; D16.3 BEHAVIOR_CONTRACT.md `Stat-name mapping` 10-row extension; D17.3 fixture 0011 `expectations.yaml` extension with `value_exact` / `value_must_be_zero` / `value_present_only` schema fields; D18.3 `BodyRule::PrometheusExposition` value-side harness extension; D19.3 06.1/06.2 carryforward closures; D20.3 state-4 phase-done verification) substantively and cleanly. The cross-sub-phase architectural rules from parent-06 SPEC §6 all hold: consumers register and increment (envoy-stats exports primitives only); counter/gauge ops lock-free; representative subset → comprehensive; access-log line counter increments at queue-enter time per Rule 4 (BEFORE the per-sink `sink.emit(...).await` loop); parent close happens at THIS sub-phase's state-6 commit (still pending — flips ROADMAP rows `06.3` and `06` to `done` simultaneously). All 11 workspace crates' root files carry `#![forbid(unsafe_code)]`; zero `unsafe` blocks introduced anywhere in the review range; zero new top-level Cargo deps (the only Cargo.toml addition is a `"time"` feature flag on tokio dev-deps in `crates/envoy-cluster/`; Cargo.lock unchanged); DECISIONS.md ledger head remains **ADR-0029** — no in-execution ADRs landed, the recommended no-foundations-grant posture per parent-06 SPEC §7 + 06.3 SPEC §7 honored end-to-end. Phase-done gate (a)-(e) all GREEN locally; (c) and (a)+(b) re-verification under CI deferred to state-4-followup per §3 Important I3. State-4 evidence material at HEAD `7cdc1a8`; on-disk fixture set verified to contain `0001` through `0012`; BEHAVIOR_CONTRACT.md `Stat-name mapping` table verified to grow exactly the 10 rows projected by SPEC §2; fixture 0011 `allowlist_envoy_rust_only` holding-pattern entries verified to each carry forward-pointing comments cross-referencing the StatsTagExtractor deferral.

**Zero Critical findings.** **Three Important findings**: I1 the Task 11 → Task 11-fixup pattern arose because the substantive task's "workspace tests clean" claim did not include the Docker-gated `admin_stats_prometheus` fixture — the bilateral `value_exact` mismatch (`envoy=None, envoy-rust=Some(N)` on the 3 envoy-rust-only stat names that were already in `allowlist_envoy_rust_only`) only surfaced when the downstream code-quality reviewer ran the Docker-gated test; this is the second recurrence of a verification-discipline gap in modern-phase REVIEW (after 06.2 Task 6's read-only-handle ergonomics catch), and a third would land Critical-severity; I2 the PLAN's Task 11 scope (4-class `pre_requests` extension + synthetic 5xx backend cluster + `{{SYNTHETIC_5XX_PORT}}` harness template) was narrowed to docs + `value_exact` (later emptied per fixup) + fixture 0012 README + 06.2 M3, so the per-class `downstream_rq_3xx/4xx/5xx` and `cluster.<name>.upstream_rq_5xx` counters' bilateral fixture-level coverage stays unrealized until a future phase ships the synthetic backend — the per-class counter math IS unit-tested end-to-end at Tasks 4 + 7, but the wire-level differential evidence is missing; I3 the state-4 phase-done gate evidence is local-only (the canonical CI run URL is deferred to a state-4-followup), structurally similar to 05.3 REVIEW I3's placeholder-vs-real shape (different surface — local-vs-CI rather than placeholder-vs-real — but the same evidence-completeness gap), the 06.1 (`25625271032`) and 06.2 (`25670699370`) state-4 evidence each included a real CI run ID and 06.3's local-only evidence is a step backwards from that posture. **Seven Minor findings** (M1-M7). Severity calibration matches the modern-phase REVIEW arc (05.1 / 05.2 / 05.3 / 06.1 / 06.2 all landed Approved-with-M-track). 06.3 is solid for the closing sub-phase of parent-06 — 11 substantive task commits + 1 fast-recovery fixup commit + 1 state-4 verification commit across ~3000 LoC of net change with disciplined PROGRESS narration, 5 cross-phase carryforwards closed, and zero new doctrine deviations.

## §2. Strengths

1. **The 05.3 REVIEW I1 close at Task 2 (`1516cb8`) honors the I1 fix sketch verbatim.** `crates/envoy-config/src/lib.rs:219-231` lands `ConfigError::Http2ClusterFromHttp1Listener { listener: String, cluster: String }` exactly as the 05.3 REVIEW I1 fix sketch specified (both fields named, error message cross-references ADR-0028 so operators get a doctrine pointer at reject time). The validator extension at `crates/envoy-config/src/bootstrap.rs:1403-1421` rejects H1-listener × H2-cluster AND AUTO-listener × H2-cluster; passes H2-listener × H1-cluster (the load-bearing 05.3 D4 combination — tested by `validates_h2_listener_with_h1_cluster_passes` with a comment citing D4); skips TCP-proxy listeners via the codec_type-presence gate. Six unit tests ship per SPEC §3 D14.3 projection (5 SPEC-mandated + 1 regression-guard for the TCP-proxy carve-out). Mirrors phase-05.1 Task-1's posture toward phase-02.1 REVIEW I3 cleanly — a previously-identified gap closed cheaply at the start of an unrelated sub-phase.

2. **Task 4's H1 state-init tightening (06.2 REVIEW I1 closure) is structurally complete.** `crates/envoy-http1/src/hcm.rs:371-372` drops the `mut x = 0/default` sentinels for `response_status_for_log` and `response_body_len` to `let x;` mirroring H2's stricter posture at `crates/envoy-http2/src/hcm.rs:134`. The restructuring of the proxy arm from `match pick_endpoint() { None => { assign; None } }` + `if let Some` into `if let Some(endpoint) = ... { ... } else { ... }` (lines 395-487) preserves semantics (same warn log, same 503 synthesis, same fall-through to dispatch site) while giving Rust's flow analysis a clean two-branch split. Each branch assigns once; Rust's flow analysis enforces write-before-read at compile time (E0381). Test `hcm_h1_state_init_writes_in_all_5_writer_arms` (line ~1878) provides a named runtime-witness anchor; the real guarantee is compile-time. Mechanical co-location with the per-class counter wiring at Task 4 is exactly the close site 06.2 REVIEW §7 R-3 recommended.

3. **The per-response-class HCM counters (D15.3.a) increment at the right factored site on both protocols.** `crates/envoy-http1/src/hcm.rs:527-533` (H1) and `crates/envoy-http2/src/hcm.rs:363-371` (H2) both increment AFTER all writer arms populate `response_status_for_log` and BEFORE the access-log dispatch — single increment-site per protocol, matching the SPEC §3 D15.3.a pseudocode and PLAN-write SPEC correction 1. The status-class bucketing via `match status / 100 { 2|3|4|5 => ..., _ => {} }` correctly no-ops on 1xx informational and non-standard 6xx per Envoy v1.33.0 docs; status 0 (sentinel-leak guard) and any out-of-band value also silently no-op. Five unit tests ship in Task 4: one per status class (2xx/3xx/4xx/5xx via `direct_response` routes) + the state-init regression witness.

4. **The `ConnGaugeGuard` RAII pattern (D15.3.b cluster-side) is structurally clean and avoids the 05.3 ADR-0028 cycle.** `crates/envoy-cluster/src/cluster.rs:16-23` defines a single struct owning `Arc<envoy_stats::Gauge>` (no `Cluster` borrow → no Arc-cycle concern that ADR-0028 names). `Drop` (cluster.rs:20-22) covers both success and error paths uniformly. Construction via `Cluster::cx_active_guard()` (lines 91-96) atomically inc + Arc-clone. Placement at all three call sites is correct: H1 HCM proxy arm (`crates/envoy-http1/src/hcm.rs:411` — inside `if let Some(endpoint)` block, before `Client::connect`); H2 HCM (`crates/envoy-http2/src/hcm.rs:219` — single guard above the `match cluster.upstream_protocol()` block, covering both H1 and H2 dispatch arms via lexical scope); TCP-proxy dial site (`crates/envoy-tcp/src/lib.rs:90` — before `TcpStream::connect`). All three placements ensure the guard fires only when an upstream connection is actually attempted, and Drop on the `?`-exit error path is preserved.

5. **The listener-side `cx_active` gauge (D15.3.b listener-side) data-path-only scoping via code-path is a clean deviation that avoids a config-shape change.** `crates/envoy-bin/src/main.rs:324-345` constructs the admin listener via `tokio::net::TcpListener` + `envoy_admin::serve` directly, never traversing `envoy_listener::Listener::bind` — so the new `cx_active` gauge is automatically scoped to data-path listeners. The PLAN-projected `ListenerConfig.count_active: bool` flag becomes vestigial; PROGRESS Task 5 dev 1 documents the deviation per D-3.5. There is no existing `ListenerConfig` type in envoy-listener — the constructor takes `cfg: &envoy_config::Listener` directly — which further ratifies the deviation as the simpler design.

6. **The upstream-side router counters (D15.3.c) honor the synth-vs-proxy bypass semantics precisely.** `crates/envoy-http1/src/router.rs:85-90` fires `upstream_rq_total.inc()` unconditionally at `write_proxied_response` prologue with the 5xx conditional gated on `upstream_response.status / 100 == 5`. Synth-502/503 paths at `crates/envoy-http1/src/hcm.rs:480, 495, 508` use `Http1Response::write_to` (not `write_proxied_response`) so they correctly bypass. H2 inline 2-line increments at `crates/envoy-http2/src/hcm.rs:286-289` land immediately after `let upstream_resp = match upstream_resp_result { Ok(r) => r, ... }` resolves Ok and BEFORE the header-copy loop; synth-502 paths early-return via `finalize_h2_stream` (lines 256-278) before reaching the increment. The protocol-agnostic join-point means both H1-upstream and H2-upstream dispatch arms feed the same increment. Per parent-06 SPEC §1 layer 4 "upstream_rq_5xx is *upstream returned a 5xx*, not *router synthesized a 5xx*" — honored at both H1 and H2 sites.

7. **The listener accept-failure counter (D15.3.d) honors signpost 6 verbatim.** `crates/envoy-listener/src/lib.rs:209` places `cx_accept_failed.inc()` as the **first statement** in the `Err(err)` arm of the accept loop, BEFORE `tracing::warn!` (the SPEC signpost-6 ordering — captures the regression of someone moving the increment to the `Ok` arm or before the match). No per-errno carve-out per the recommended "all accept errors count" posture matching Envoy's documented behavior. The unit test's "counter-registered + zero-init + no-spurious-increment under 3 successful connections" pattern is acknowledged-limited (it verifies the increment is NOT in the `Ok` arm but doesn't deterministically verify it IS in the `Err` arm); PROGRESS Task 8 narrates this honestly, matching the 06.1 / 06.2 precedent for surfaces where deterministic OS-error injection requires a trait-abstracted `Accept` injection that's out of scope.

8. **The admin handler idle read timeout (Task 9, 06.1 REVIEW I1 closure) is mechanically minimal and well-placed.** `IDLE_READ_TIMEOUT: Duration = Duration::from_secs(5)` at `crates/envoy-admin/src/handler.rs:25` mirrors `crates/envoy-http1/src/hcm.rs:24` exactly (single-tier idle budget across the codebase, avoiding a two-tier timeout landscape). The `tokio::time::timeout` wrap at lines 62-73 propagates `Elapsed` through a typed `io::Error::new(ErrorKind::TimedOut, ...)` that flows through `handle_inner`'s existing `read_request` error arm — zero new code paths. The 3-outcome regression test (`admin_handler_idle_read_times_out_at_5s` at lines 407-453) is correctly justified: `Ok(0)` / `Ok(n)` / `Err` are all valid liveness witnesses because `handle_inner` attempts a 400 response before shutdown, and TCP buffering decides what the client observes. Observed 5.02s per PROGRESS Task 9 confirms `IDLE_READ_TIMEOUT` is the load-bearing budget; the 7s hard deadline catches a regression where the constant is removed or the wrap is misapplied.

9. **The access-log line counter (D15.3.e) honors parent-06 SPEC §6 Rule 4 verbatim.** `crates/envoy-http1/src/hcm.rs:565-568` (H1) and `crates/envoy-http2/src/hcm.rs:420-423` (H2) increment `access_logs_total` via `Counter::add(config.access_log.len() as u64)` BEFORE the per-sink `sink.emit(...).await` loop — the queue-enter site is structurally upstream of the await. Bulk-increment via `Counter::add(N)` (not N × `inc()`) per 06.1 REVIEW §7 R-8. The sibling `access_logs_failed.inc()` at `hcm.rs:572` (H1) / `hcm.rs:427` (H2) lands inside the per-sink `Err(_)` arm, alongside but BEFORE `tracing::warn!`, matching the BEHAVIOR_CONTRACT.md row 81 disposition. Two unit tests (success path + failure path via `FileSink::from_file_for_test` with read-only handle) ship in Task 10; the failing-sink test installs `NoSubscriber::set_default` to prevent thread-local-subscriber interference with the sibling test's `WarnCapture` — a test-hygiene fix, not a production-code change, narrated honestly in PROGRESS Task 10 deviation note.

10. **The 06.2 REVIEW I2 closure (User-Agent fixture-vs-contract divergence) is empirically resolved.** Task 10 PROGRESS at `docs/envoy-rust/phases/06.3-stats-wiring-and-close/PROGRESS.md` Task 10 narrates the diagnosis: `tests/differential/src/lib.rs::drive_http1` at lines 888-908 formats the wire request without any `User-Agent:` header injection. Both proxies see no User-Agent and both emit `"-"`. The wildcard rule at `tests/fixtures/0012-access-log-file-sink/expectations.yaml` row 12 was unnecessarily loose; tightened to `exact: "-"` matching the existing BEHAVIOR_CONTRACT.md row 12 `value-exact` disposition (which was already correct — the fixture was behind the contract). The PROGRESS rationale corrected the empirically-wrong claim in the 06.2 REVIEW I2 disposition ("`drive_http1` sets `User-Agent` to a default") via code inspection at task time.

11. **The BEHAVIOR_CONTRACT.md `Stat-name mapping` extension (Task 11) is comprehensive and per-row rationale-justified.** `docs/envoy-rust/BEHAVIOR_CONTRACT.md:72-86` adds exactly the 10 rows projected by 06.3 SPEC §3 D16.3 + SPEC §2 (4 per-class HCM counters + 2 access_logs counters + 1 listener cx_active gauge + 1 listener cx_accept_failed counter + 1 cluster cx_active gauge + 2 cluster upstream_rq counters), each with file-site / increment-condition / synth-vs-proxy boundary commentary. The `5xx-class symmetric on response_status_for_log, agnostic to synth-vs-proxy origin` clarification (line 79) is load-bearing — it justifies why per-class `downstream_rq_5xx` includes both upstream-emitted 5xx AND proxy synth-502/503, while `cluster.<name>.upstream_rq_5xx` (line 86) explicitly excludes synth-502. The contrast is stranger-readable doctrine.

12. **The Task 11 fixup commit (`b46025e`) demonstrates exemplary fast-recovery discipline.** The initial Task 11 commit `06bdf14` populated `value_exact` with 3 envoy-rust-only stat names that already lived in `allowlist_envoy_rust_only` (the Prometheus name-vs-label projection divergence documented in BEHAVIOR_CONTRACT.md §06.1). Bilateral `value_exact` requires both proxies to emit the same name; the Docker-gated `admin_stats_prometheus` fixture surfaced `envoy=None, envoy-rust=Some(N)`. The fixup emptied `value_exact` within ~17 minutes of the regression and lands a structurally-correct inline comment at `tests/fixtures/0011-admin-stats-prometheus/expectations.yaml:38-49` cross-referencing `allowlist_envoy_rust_only` + BEHAVIOR_CONTRACT.md "Prometheus exposition shape divergence". The deferral rationale (bilateral assertion gated on a `StatsTagExtractor`-equivalent dropping the paired allow-list entries together) is doctrinally tight. Mid-execution review-fixups landing as small named commits (not amendments to the original task) is the 06.2 `f73face` precedent applied — D-3.5 / D-3.6 in action.

13. **The fixture 0011 `allowlist_envoy_rust_only` holding-pattern entries each carry forward-pointing comments.** Empirical: all 11 new envoy-rust-only allow-list entries added across Tasks 4 + 5 + 6 + 7 + 8 + 10 carry per-entry or per-group paragraphs cross-referencing the StatsTagExtractor deferral and BEHAVIOR_CONTRACT.md "Prometheus exposition shape divergence". Task 4's 4 per-class entries (`_rq_2xx/3xx/4xx/5xx`) are covered by a single forward-pointing paragraph naming the `envoy_http_downstream_rq_xx` (generic, label-keyed) upstream counterpart + the StatsTagExtractor drop-together invariant. Tasks 5 + 6 + 7 + 8 + 10 each carry their own forward-pointing paragraph naming the upstream Envoy stat shape and the deferral. The pair-drop atomicity invariant from 06.1 REVIEW §7 R-2 (paired allow-list entries must drop together with their upstream-Envoy bare-name counterparts) is preserved as a coherent block ready for the future StatsTagExtractor-equivalent landing.

14. **The 06.2 REVIEW M3 closure (fixture 0012 README path correction) is precise.** `tests/fixtures/0012-access-log-file-sink/README.md` paths corrected to `/tmp/0012-envoy-mount/access.log` + `/tmp/0012-envoy-rust-mount/access.log` (matches the bind-mount wiring at `tests/differential/src/lib.rs`); a one-line note added explaining the parent-directory bind-mount strategy. The README is now stranger-readable end-to-end and the fixture's documented paths are coherent with the post-CI-fix paths landed at 06.2 commit `4aba10b`.

15. **PROGRESS.md is exemplary self-narrating throughout the 06.3 state-3 arc.** Each task carries deviation disclosures per D-3.5: Task 2 dev 1-2 (clippy `collapsible_if` collapse + 2 pre-existing tests updated); Task 3 dev 1-2 (rustfmt body reformat + 3 existing-test field additions); Task 4 dev 1-2 (proxy-arm restructure for state-init tightening + fixture 0011 allow-list); Task 5 dev 1 (data-path scope via code-path, no `ListenerConfig.count_active` field); Task 6 dev 1-3 (test simplification, concurrent-test barrier choice, fixture allow-list); Task 7 dev (parameter ordering, H2 test scaffolding decision, `drive_proxy` async); Task 8 testing-limitation narrative; Task 9 design narrative for the 3-outcome test; Task 10 test-isolation narrative + empirical 06.2 I2 diagnosis; Task 11 + fixup narratives covering the scope-narrowing AND the bilateral-mismatch recovery; Task 12 phase-done evidence with honest CI-deferral acknowledgment. The 5 PLAN-write SPEC corrections in the Task 1 preamble + the per-task deviations make D-3.5 mechanically observable.

16. **State-4 phase-done gate evidence (Task 12, `7cdc1a8`) is materially substantive at the local level.** All 5 stable-toolchain gates clean; 12/12 Docker-gated fixtures green simultaneously (cross-checked against on-disk fixture set `0001` through `0012`); h2spec ≥95% carry-forward from 05.2 D7 baseline preserved (no H2 surface engaged in 06.3); `parse_bootstrap` fuzz unchanged at 17 seeds per architecture decision 20 (no new fuzz target — the validator's reject-path is structural and serde-walk covers it). The CI URL deferral is honestly narrated and dispositioned (see §3 Important I3).

17. **Zero new top-level Cargo deps; zero new ADRs; D-3.8 + D-3.9 unaffected.** `Cargo.lock` is unchanged across the entire state-3 arc; the only `Cargo.toml` modification is a `"time"` feature flag added to `crates/envoy-cluster/Cargo.toml`'s dev-deps tokio block (required by `tokio::time::sleep` in the Task 6 concurrent test) — strictly dev-only, no production-surface impact. DECISIONS.md ledger head stays **ADR-0029**; the recommended no-foundations-grant posture per parent-06 SPEC §7 + 06.3 SPEC §7 honored end-to-end. `rust-toolchain.toml` and `docs/envoy-rust/ENVOY_TARGET.md` untouched.

## §3. Issues

### Critical (must fix before phase commit)

None.

### Important (close in 06.3 or carry forward with named owner phase)

**I1. Task 11 → Task 11-fixup pattern reveals a verification-discipline gap: workspace-tests-clean does not include Docker-gated bilateral fixture runs, and the bilateral `value_exact` mismatch only surfaced when the downstream reviewer ran the fixture.** PROGRESS Task 11 for the substantive `06bdf14` commit cites "workspace tests clean" but the Docker-gated `admin_stats_prometheus` fixture was not run before commit; the bilateral assertion failure (`envoy=None, envoy-rust=Some(N)` on the 3 envoy-rust-only stat names already in `allowlist_envoy_rust_only`) only manifested in the Docker-gated run. The fixup (`b46025e`) landed within ~17 minutes, so blast radius is minimal — but the pattern (`workspace tests pass` ≠ `bilateral fixture passes` for any commit that edits a fixture's `value_exact` / `value_must_be_zero` / `value_present_only` blocks) is now recurring: 06.2 Task 6's read-only-handle ergonomics catch was the prior occurrence, and a third would land Critical-severity since these blocks are the bilateral-assertion entry point of the differential harness. **Fix sketch:** R-track recommendation per §7 R-1 — make per-task PROGRESS explicitly attest which test buckets ran, especially for tasks that touch fixture YAML's value-side assertion blocks. Either (a) signpost in the SPEC/PLAN template ("any commit editing fixture `expectations.yaml`'s value-side blocks MUST be Docker-gated-fixture-run before commit") or (b) add a per-task PROGRESS subsection naming the test buckets exercised. **Disposition:** carry forward to whichever future phase first establishes the discipline (it does not gate state-5; the fixup pattern shows the in-phase recovery cadence works).

**I2. Task 11 scope-narrowing — multi-class fixture coverage stays unrealized until a future phase ships the synthetic 5xx backend.** The PLAN's Task 11 projected extending fixture 0011's `pre_requests` to drive 4 status classes + adding a synthetic 5xx backend cluster + `{{SYNTHETIC_5XX_PORT}}` harness template extension. The executor narrowed Task 11 to docs + `value_exact` (later emptied per fixup) + fixture 0012 README + 06.2 M3. The per-class counter wiring IS unit-tested end-to-end at Tasks 4 (`hcm_increments_downstream_rq_{2,3,4,5}xx_on_{2,3,4,5}xx_response`) + 7 (`write_proxied_response_increments_upstream_rq_5xx_on_503`), but the wire-level bilateral differential coverage for `downstream_rq_3xx/4xx/5xx` and `cluster.<name>.upstream_rq_5xx` remains untested at fixture level. BEHAVIOR_CONTRACT.md lines 76-79 declare these counters `value-exact`, but the only artifact verifying that today is unit tests inside `envoy-http1/src/hcm.rs` and `envoy-http1/src/router.rs` — there is no bilateral evidence that envoy and envoy-rust emit byte-equal values for the per-class counters under the same request load. **Fix sketch:** do NOT reopen at state-5 — the unit-test coverage suffices to ratify the implementation contract; the missing wire-level layer requires fixture infrastructure (synthetic 5xx route, possibly a second listener) that is a sizable extension worth its own task. **Disposition:** **carry forward to a named owner phase**. The natural close site is whichever phase first surfaces the synthetic backend (a fixture-extension task; could be folded into the upstream-robustness family alongside connection pooling, which would also tighten `cluster.<name>.upstream_cx_total` disposition from `name-required, value-may-differ` to `value-exact` per BEHAVIOR_CONTRACT.md line 69). Track explicitly in the parent-06 close-out R-track inventory.

**I3. State-4 phase-done gate evidence carries no CI run URL — local-only verification.** PROGRESS Task 12 records local-gate evidence only; the canonical §7.5 (a)-(e) re-verification CI run URL is deferred to a state-4-followup. STATE.md acknowledges the deferral and flags shared-state-impact discipline as the reason (the executor session did not push to `origin/main`). The 06.1 (CI run `25625271032` HEAD `36fedd8`) and 06.2 (CI run `25670699370` HEAD `4aba10b`) state-4 evidence each included a real CI run ID + timestamp + h2spec actual count; 06.3's local-only evidence is a step backwards from that posture. Structurally similar to 05.3 REVIEW I3 (placeholder CI URL) — different surface (local-vs-CI rather than placeholder-vs-real) but the same evidence-completeness gap. **Fix sketch:** R-2 below. Land a single PROGRESS append + STATE.md update commit (the state-4-followup commit) after a user-authorized push to `origin/main` fires the §7.5 CI run. Capture the CI run ID + HEAD SHA + timestamp alongside the local-gate evidence. **Disposition:** acceptable to land before state-5 REVIEW.md OR alongside state-6 close-out. Preferred: land before state-6 close-out so the [parent 06 done] commit's evidence chain doesn't carry "deferred to followup" as the canonical state-4 anchor. The local-gate results in Task 12 ARE substantive (all 5 stable-toolchain gates + 12/12 Docker-gated fixtures + h2spec carry-forward) — the missing layer is canonical CI verification under the project's standard environment.

### Minor (awareness-only / track forward)

**M1. The inline comment block at `crates/envoy-http1/src/hcm.rs:357-365` claims `mut` is required for the H1 state-init tightening, but the actual declarations at lines 371-372 use `let x;` (no `mut`) and compile green.** The comment block (15 lines starting "Note: `mut` is required (not plain `let x;`)...") asserts that Rust's flow analysis requires `mut` because the Proxy arm has two structurally-separate paths that each assign these vars. The actual code at lines 371-372 is `let response_status_for_log: u16;` and `let response_body_len: u64;` (no `mut`), and the workspace compiles without errors — so the comment narrates a previous draft state and was never updated when the proxy-arm restructure (Task 4 dev 1) made plain `let x;` viable across all 5 writer arms. **Why it matters:** D-3.4 stranger-readability — future maintainers reading the comment see an alleged constraint that the code immediately below disproves. **Fix:** trim the comment block at lines 357-365 down to the simple version naming the restructure and citing E0381 as the compile-time guarantee; ~10 LoC of comment edit. **Disposition:** carry forward to whichever future task touches `crates/envoy-http1/src/hcm.rs` near this block.

**M2. PROGRESS Task 2 narrates the `iter().find()` cluster-lookup choice in "Implementation notes" rather than "Deviations from PLAN".** SPEC §3 D14.3 line 135 + SPEC §7 signpost 1 + PLAN architecture decision 1 + PROGRESS Task 1 SPEC correction 4 all direct the executor to build an upfront `HashMap<&str, &Cluster>` at `validate()` entry. The actual Task 2 implementation uses `clusters.iter().find(...)` per-route, consistent with the existing adjacent `UnknownCluster` check at `bootstrap.rs:1400` which already uses `iter().any()`. PROGRESS Task 2 mentions the deviation under "Implementation notes" ("`iter().find()` consistent with the adjacent code per the PLAN's minimal-diff guidance") but does not list it under "Deviations from PLAN". **Why it matters:** D-3.5 requires deviations from PLAN to be recorded as deviations; the worst-case-corpus argument (large clusters × listeners × routes lookup degradation) is asymptotically real, though dominated by the surrounding code's existing `O(listeners × routes × clusters)` complexity. **Fix:** PROGRESS Task 2 cosmetic edit — add a bullet under "Deviations from PLAN" with the minimal-diff rationale and cross-reference to line 1400; OR introduce the HashMap as planned. Recommendation: PROGRESS edit, since the adjacent UnknownCluster check already determines the upper bound. **Disposition:** record at REVIEW level; close opportunistically.

**M3. BEHAVIOR_CONTRACT.md row 85 (`cluster.<name>.upstream_rq_total`) narrates the H2 increment as living in `finalize_h2_stream`, but the increment actually lives in `handle_one_stream` at line 286-289.** `docs/envoy-rust/BEHAVIOR_CONTRACT.md:85` reads: *"H2: fires inline at the post-dispatch success site in `finalize_h2_stream`"*. `finalize_h2_stream` (at `crates/envoy-http2/src/hcm.rs:338-352`) is the unified send + access-log dispatch at the tail of an H2 stream; the increment site is upstream of that, in `handle_one_stream` immediately after `upstream_resp_result` resolves Ok and before the header-copy loop. Cosmetic narrative error in the contract row, no behavioral impact. **Fix:** Task 11 follow-up — tighten the row text to "H2: fires inline at the post-dispatch success site in `handle_one_stream`, before the header-copy loop, with the synth-502 path returning early via `finalize_h2_stream` so synth-502 bypasses". ~3 LoC of contract edit. **Disposition:** carry forward; close opportunistically.

**M4. The Task 6 test name `cluster_cx_active_round_trip_through_h1_call` (in `crates/envoy-cluster/src/cluster.rs`) misnames what the test exercises.** The test does NOT exercise an H1 client call — it holds a `ConnGaugeGuard` across `tokio::time::sleep(10ms)` (the deviation rationale: an actual H1 client call would require adding `envoy-http1` as a dev-dep on `envoy-cluster`, inverting crate-dep direction). PROGRESS Task 6 dev 1 narrates this honestly, but the test-function name still claims "round-trip through H1 call." **Why it matters:** D-3.4 stranger-readability — a future maintainer searching for an H1-roundtrip witness gets a sleep-based test. **Fix:** rename to `cluster_cx_active_observable_across_async_yield` or `cluster_cx_active_guard_holds_value_across_await`. ~1 LoC. **Disposition:** carry forward; close opportunistically.

**M5. `parse_prometheus_samples` (`tests/differential/src/lib.rs`) silently drops non-`u64`-parseable values, which would surface as silent missing-key in `value_exact` assertions if a future fixture asserts on histogram-quantile floats or floating-point gauges.** Current Envoy stat set in 06.3 is all-integer (Counter / Gauge u64), so no observable impact today; the parser's `silently skip on parse failure` shape is documented in `BodyRule::PrometheusExposition` rustdoc and PROGRESS Task 3. **Why it matters:** future fixtures asserting on histogram quantile values or float-valued gauges would see a silent missing-key error path. **Fix:** either (a) narrow the docstring ("u64 stats only; histogram-quantile floats silently dropped") or (b) add a debug-mode `tracing::debug!` log on parse-failure for forward-traceability. ~5 LoC. **Disposition:** track forward to whichever future phase first wires float-valued stats (histogram family is in the observability sprint queue post-08).

**M6. Test settle windows in Task 5 (50-200 ms `tokio::time::sleep` for gauge-peak observation) continue the standing settle-sleep pattern from 06.1/06.2 (M5 chain via 02.2 REVIEW M1).** `crates/envoy-listener/src/lib.rs:550-640`'s `HoldHandler` tests sleep 50 ms after connect (waiting for accept+inc), 100 ms after release (waiting for handler-task termination), and 200 ms in the burst variant. PROGRESS Task 5 dev 1 acknowledges `HoldHandler` is needed for deterministic peak observation; the settle sleeps themselves are still timing-coupled. **Why it matters:** flake risk on slow CI runners; concurrency cliff hidden under nominal-load test runs. **Fix sketch:** replace settle sleeps with `JoinSet::join_next()` poll loops or a `tokio::sync::Notify`-based handshake. **Disposition:** carry forward indefinitely as the 02.2 M1 / 06.1 M5 / 06.2 M5 chain continues; close in a CI-stabilization phase post-08 alongside the other settle-sleep instances.

**M7. `assert_body_rule`'s `BodyRule::PrometheusExposition` arm re-parses both bodies up to 3 times when all three value blocks (`value_exact` / `value_must_be_zero` / `value_present_only`) are populated.** `tests/differential/src/lib.rs` calls `parse_prometheus_samples` separately under each conditional block. Each parse is `O(N_lines)`; 06.3 scrapes are bounded but not tiny (the 204-entry envoy-only allow-list + envoy-rust's full stat output). **Why it matters:** performance under repeated assertions; current 06.3 fixture 0011 has `value_exact: []` / `value_must_be_zero: []` / `value_present_only: []` post-fixup so the re-parse is zero-cost, but a future fixture with non-empty value blocks would pay the cost. **Fix:** lift the two `parse_prometheus_samples` calls above the three conditional blocks, gated on `!(value_exact.is_empty() && value_must_be_zero.is_empty() && value_present_only.is_empty())`. ~5 LoC. **Disposition:** awareness-only; close opportunistically whenever the next harness touch lands.

## §4. Carryforward inventory

For each Important / Minor item: closure plan.

| ID | Severity | Item | Closure plan |
|---|---|---|---|
| I1 | Important | Task 11 → Task 11-fixup verification-discipline gap: workspace-tests-clean does not include Docker-gated bilateral fixture runs | **Carry forward to a named owner phase.** R-track recommendation: per-task PROGRESS should explicitly attest which test buckets ran (workspace tests, Docker-gated fixture, in-process backstop, fuzz). The 06.3 fixup pattern shows the in-phase recovery cadence works (~17 minutes from regression to green); the structural fix is a signpost in the SPEC/PLAN template. Track in parent-06 close-out R-1. |
| I2 | Important | Task 11 scope-narrowing — multi-class `pre_requests` + synthetic 5xx backend deferred from PLAN scope | **Carry forward to a named owner phase.** Natural close site is whichever phase first surfaces synthetic backend infrastructure (likely upstream-robustness family alongside connection pooling, which also tightens `cluster.<name>.upstream_cx_total` to `value-exact`). The per-class counter math is unit-tested at Tasks 4 + 7; the missing layer is wire-level bilateral verification, which requires fixture infrastructure outside 06.3's scope. |
| I3 | Important | State-4 phase-done gate evidence is local-only (no CI run URL) | **Close before state-6 close-out.** A state-4-followup commit captures the CI run ID + HEAD SHA + timestamp after a user-authorized push fires CI. Acceptable to land alongside state-6 or as a standalone state-4-followup commit before state-6. Mechanical: ~5 LoC PROGRESS append + STATE.md update. Local-gate results in Task 12 ARE substantive material verification; CI re-verifies under the project's standard environment. |
| M1 | Minor | hcm.rs:357-365 comment block claims `mut` is required but actual code uses `let x;` | **Close opportunistically.** ~10 LoC comment edit; fold into any future task touching `crates/envoy-http1/src/hcm.rs` near this block. |
| M2 | Minor | PROGRESS Task 2 narrates `iter().find()` deviation under "Implementation notes" rather than "Deviations from PLAN" | **Record at REVIEW level; close opportunistically.** PROGRESS edit to move bullet into Deviations subsection. ~3 LoC. |
| M3 | Minor | BEHAVIOR_CONTRACT.md row 85 H2 narrative says `finalize_h2_stream` but increment is in `handle_one_stream` | **Close opportunistically.** ~3 LoC contract edit. |
| M4 | Minor | Test name `cluster_cx_active_round_trip_through_h1_call` doesn't reflect actual content (sleep-based, no H1 call) | **Close opportunistically.** ~1 LoC rename. |
| M5 | Minor | `parse_prometheus_samples` silently drops non-`u64` values | **Track forward to histogram-family phase.** ~5 LoC docstring or debug-log; close when float-valued stats first land. |
| M6 | Minor | Task 5 settle-sleep windows (50-200 ms) — 02.2 M1 / 06.1 M5 / 06.2 M5 chain continues | **Carry forward indefinitely.** Close in a CI-stabilization phase post-08 alongside other settle-sleep instances. |
| M7 | Minor | `assert_body_rule` re-parses bodies up to 3x when all 3 value blocks populated | **Close opportunistically.** ~5 LoC harness factoring. |

**Standing carryforwards inherited but not closed in 06.3** (per parent-06 SPEC §4 + 06.1/06.2 REVIEW §4 + 05.x REVIEW §4 inventories):

- **Phase 06.2 REVIEW M1** (`Http1Error::AccessLogOpen` source-chain typing loss) — **carries forward indefinitely.** 06.3 does not refactor typed-error chains.
- **Phase 06.2 REVIEW M2** (`BodyRule::ByteExact` doesn't assert literal body) — **carries forward indefinitely.** Pre-existing harness pattern; 06.3 does not promote `ByteExact` to `ByteExactValue { value: String }`.
- **Phase 06.2 REVIEW M4** (`/tmp/0012-envoy-mount` process-shared path; activates under nextest sharding) — **carries forward indefinitely.** Today's testcontainers serialization keeps the collision dormant; whichever future CI-infra phase parallelizes Docker-gated tests closes.
- **Phase 06.2 REVIEW M5** (H2 access-log test 200ms sleep) — **standing 02.2 M1 chain.** M6 above is the 06.3-specific shape of the same chain.
- **Phase 06.1 REVIEW I2** (`serialize_response` injects 5 standard headers unconditionally — duplicates if future endpoint sets the same name) — **carries forward to phase 08** (full admin endpoint surface).
- **Phase 06.1 REVIEW M1** (`resp.reason.unwrap_or("OK")` wrong for non-200) — **carries forward to phase 08** alongside I2.
- **Phase 06.1 REVIEW M2** (`find_crlf_crlf` is O(n²) in worst case) — **carries forward indefinitely.** Sub-millisecond worst case under 8 KiB cap; not a hot path.
- **Phase 06.1 REVIEW M3** (`SocketAddr` parse rejects hostnames) — **carries forward indefinitely.** Project-wide consistency; close with whichever xDS-family phase first ships hostname-resolving listener bind.
- **Phase 06.1 REVIEW M4** (`DRAIN_BUDGET` constant duplicated between `envoy-admin` and `envoy-listener`) — **carries forward to phase 08** (graceful drain consolidation).
- **Phase 06.1 REVIEW M5** (in-process backstop SIGKILL-on-Drop posture; 02.2 M1 chain) — **standing carryforward** inherited.
- **Phase 06.1 REVIEW M6** (empty `payload.bin` in fixtures 0010 / 0011; harness-shape consistency only) — **carries forward indefinitely.**
- **Phase 05.3 REVIEW I2** (typed-error chain dissolution at H2 dispatch site via `format!("{e}")`) — **carries forward** to whichever phase next touches `crates/envoy-http2/src/hcm.rs:167-180` upstream dispatch site. 06.3's `ConnGaugeGuard` placement at line 219 is structurally separate from the I2 site.
- **Phase 05.2 REVIEW I1** (h2spec tarball SHA-256 verification in CI) — **carries forward.** 06.3 does not edit `.github/workflows/ci.yml`.
- **Phase 05.2 REVIEW I2 / I3 / M2 / M3 / M5 / M6 / M10 / M11 / M12** — not engaged by 06.3; carries forward unchanged.
- **Phase 04.1 REVIEW M5/M9** (Cargo.lock cadence ratification ADR) — **carries forward unchanged.** 06.3 introduced no new top-level Cargo deps; the Cargo.lock diff at `3a964cc..42fc726` is empty.
- **Phase 04.1 REVIEW M7** (TLS+H2 ALPN dispatch generalization) — **carries forward** to phase that ships H2+TLS.
- **Phase 04.1 REVIEW M-claim** (`drive_http1` per-function unit test never landed) — **carries forward indefinitely.** 06.3 does not add a third `Driver::Http1` consumer.
- **Phase 04.1 REVIEW M1/M2/M4** (`diff_headers` value-comparison, body-drain idle timeout, `strip_port` IPv6 Host) — **not engaged by 06.3**; carries forward unchanged.
- **Phase 02.2 REVIEW M1** (`*EchoBackend::Drop` polling loop blocks on `std::thread::sleep`) — **standing carryforward.** M6 above is the 06.3-specific shape; the chain continues to grow at every phase that adds a settle-sleep without addressing the root cause.

## §5. Disposition decisions

**Closed in 06.3** (substantively or structurally — 5 carryforwards):

- **Phase 05.3 REVIEW I1** — closed at Task 2 (`1516cb8`) via `ConfigError::Http2ClusterFromHttp1Listener` parse-time validator gate with both `listener` and `cluster` context fields, 6 unit tests (5 SPEC-mandated + 1 TCP-proxy regression-guard), and explicit ADR-0028 cross-reference in the error message.
- **Phase 06.2 REVIEW I1** — closed at Task 4 (`a23a21b`) co-located with per-class HCM counter wiring. H1 state-init at `crates/envoy-http1/src/hcm.rs:371-372` tightened from `let mut x = 0/default` to `let x;` mirroring H2's posture; the Proxy arm restructured from `match pick_endpoint` + `if let Some` into `if let Some(endpoint) ... else ...`; named regression test anchors the fix. (M1 of this REVIEW flags a comment-block stale-narrative artifact of the restructure.)
- **Phase 06.2 REVIEW I2** — closed at Task 10 (`d050e23`) via empirical diagnosis. Fixture 0012 row 12 tightened from `wildcard` to `exact: "-"`. BEHAVIOR_CONTRACT.md row 12 was already correct (`value-exact`); no doc edit needed. PROGRESS corrects the empirically-wrong 06.2 REVIEW I2 rationale (`drive_http1` does NOT inject `User-Agent`).
- **Phase 06.2 REVIEW M3** — closed at Task 11 (`06bdf14`) via fixture 0012 README path correction (`/tmp/0012-envoy-mount/access.log` + `/tmp/0012-envoy-rust-mount/access.log`) + one-line bind-mount note.
- **Phase 06.1 REVIEW I1** — closed at Task 9 (`3d36c11`) via `IDLE_READ_TIMEOUT = Duration::from_secs(5)` constant + `tokio::time::timeout` wrap on `read_request`'s read loop, mirroring HCM's 5s posture exactly. Test `admin_handler_idle_read_times_out_at_5s` provides the named anchor (observed 5.02s).

**Deferred to state-4-followup or state-6:**

- **State-4 CI URL capture** (I3) — land before state-6 close-out via a state-4-followup commit that pushes to `origin/main`, captures CI run ID + HEAD SHA + timestamp, and appends to PROGRESS Task 12. Mechanical, ~5 LoC.

**Continuing carryforwards by named owner phase:** see §4 standing carryforwards inventory above.

## §6. Phase-done gate (§7.5) verification

Per `BOOTSTRAP_PROMPT.md` §7.5 and 06.3 SPEC §1 acceptance signal (a)-(f):

**(a) Extended differential fixture `0011-admin-stats-prometheus` green.** ✓ at the local Docker-gated level per PROGRESS Task 12 (`test result: ok. 1 passed; 0 failed`). The fixture's `expectations.yaml` extension with the new `value_exact` / `value_must_be_zero` / `value_present_only` schema fields parses cleanly (post-Task 11 fixup with empty value blocks); the 204-entry envoy-only allow-list + 11-entry envoy-rust-only allow-list with forward-pointing comments hold. **CI re-verification under project's standard environment is deferred to state-4-followup** (see I3).

**(b) 11 pre-existing fixtures green simultaneously.** ✓ at the local Docker-gated level per PROGRESS Task 12 — all of `echo`, `tcp_proxy`, `tls_downstream`, `tls_upstream`, `tls_sni`, `http1_direct_response`, `http1_router_upstream`, `http2_direct_response`, `http2_router_upstream`, `admin_ready`, `access_log_file_sink` report `test result: ok. 1 passed; 0 failed`. **All 12 fixtures (0001-0012) green simultaneously** is the parent-06 differential surface in full; cross-checked against on-disk fixture set `ls tests/fixtures/` showing directories 0001 through 0012.

**(c) `h2spec` ≥95% pass with `known-failures.txt` unchanged.** ✓ carry-forward from 05.2 D7 baseline 99.31% (144 passed / 1 failed / 1 skipped of 146). 06.3 does NOT engage H2-framing surfaces — the comprehensive-stats-wiring surface is protocol-agnostic (the per-class HCM counters fire on both H1 and H2 via the shared `HCMConfig` / `HCMStats` plumbing; the upstream-side router counters fire on both H1 and H2 via the shared `Cluster` field). The h2spec runner output is unchanged modulo timing per PROGRESS Task 12. **CI re-runs h2spec at state-4-followup** per I3.

**(d) `parse_bootstrap` fuzz target clean for short-budget run.** ✓ — seed corpus unchanged at 17 entries (no new fuzz target per architecture decision 20; the validator's reject-path at Task 2 is structural and serde-walk already covers it). PROGRESS Task 12 records the fuzz status as unchanged.

**(e) Stable-toolchain gates clean.** ✓ — all five (`cargo build`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check`, `cargo test --workspace --lib --bins` 499 passed / 0 failed / 2 ignored, `cargo deny check` advisories+bans+licenses+sources ok) reported clean in PROGRESS Task 12's local-run results at HEAD `7cdc1a8` (extending through `b46025e` which is the substantive HEAD; `7cdc1a8` is the verification commit that landed only PROGRESS).

**(f) REVIEW.md verdict** lands at state-5 in this session per the `SKILL_ROUTING.md` state-5 transition. Verdict per §1: **Approved with M-track follow-ups** (zero Critical; three Important — all scoped to verification-discipline / scope-narrowing / evidence-completeness, none gating state-6 except I3 which requires close before parent close-out per the [parent 06 done] commit's evidence-chain hygiene; seven Minor).

**Gates (a) + (b) + (c) verified locally; CI re-verification deferred to state-4-followup per Important I3.** Local-gate results are the substantive verification material; the canonical CI run URL + HEAD SHA + timestamp is the missing layer.

## §7. Doctrine compliance

Per `docs/envoy-rust/MISSION.md` §3 (D-3.1 through D-3.9):

- **D-3.1 (superpowers-first process).** ✓ — PROGRESS Task 1's preamble narrates the use of `superpowers:writing-plans` for PLAN-write; subsequent Tasks 2-12 narrate `superpowers:subagent-driven-development` / `superpowers:test-driven-development` per the established 06.1 / 06.2 cadence (tests-first observable in each task's LoC tally — impl + tests landed together). State-4 verification via `superpowers:verification-before-completion` (Task 12 quotes the gate evidence inline). State-5 review via `superpowers:requesting-code-review` (this document, with 4 parallel reviewer subagents per the user-preferred subagent-driven posture).

- **D-3.2 (hybrid implementation stance).** ✓ — Phase 06 lists *Stats subsystem*, *Access log formatters and sinks*, and *Admin API* on the **Must be written from scratch** list. Verified: 06.3 extends 06.1's hand-rolled `envoy-stats` primitives (`Counter::add(N)` for bulk-increment per R-8) + 06.2's hand-rolled access-log dispatch surface + 06.1's hand-rolled `envoy-admin` handler — **no new top-level Cargo deps**; the only Cargo.toml addition is a `"time"` feature flag on tokio dev-deps in `crates/envoy-cluster/`. Cargo.lock diff at `3a964cc..42fc726` is empty. The recommended no-foundations-grant posture per parent-06 SPEC §7 + 06.3 SPEC §7 honored end-to-end. Both 06.1 and 06.2 outcomes ratified this posture; 06.3 ratifies it for the third time within parent-06.

- **D-3.3 (differential correctness beats internal fidelity).** ✓ — BEHAVIOR_CONTRACT.md `Stat-name mapping` extension at Task 11 lands 10 new rows, each with per-row equivalence disposition + rationale + synth-vs-proxy boundary commentary. Fixture 0011 `expectations.yaml` extension drives `value_exact` / `value_must_be_zero` / `value_present_only` schema fields. The Task 11 fixup pattern is a D-3.3 application in real-time: the bilateral assertion on envoy-rust-only stat names produced `envoy=None, envoy-rust=Some(N)` — the harness caught the contract-vs-allow-list mismatch and the fixup honored D-3.3 by deferring bilateral assertion to a future StatsTagExtractor-equivalent landing. The dot-tree contract (`http.<stat_prefix>.downstream_rq_Nxx = value-exact`) remains authoritative; the allow-list captures only the Prometheus emitter-side projection difference.

- **D-3.4 (context isolation).** ✓ — SPEC, PLAN (1843 lines), PROGRESS (1050 lines), REVIEW.md (this), code comments, BEHAVIOR_CONTRACT.md edits, fixture 0012 README — all stranger-readable. PROGRESS Task 1 preamble names 5 PLAN-write SPEC corrections + 22 architecture decisions. Each per-task PROGRESS section carries Work-summary / Tests-landed / Deviations / LoC-delta / Carryforward-closures subsections. Task 11 fixup PROGRESS narrates the bilateral-mismatch recovery candidly. M1 + M3 + M4 of this REVIEW flag three localized stranger-readability nits (stale inline comment, contract-row narrative, test-name); all are cosmetic.

- **D-3.5 (decisions are written, not remembered).** ✓ — Ledger head before 06.3 = ADR-0029. Ledger head after 06.3 = ADR-0029 — **no new ADRs landed.** `git log 3a964cc..42fc726 -- docs/envoy-rust/DECISIONS.md` is empty. Recommended posture (no foundations grants under hand-roll discipline) honored — counters/gauges extend 06.1's `Arc<Counter>` / `Arc<Gauge>` primitives; `ConnGaugeGuard` is a fresh RAII struct that owns only an `Arc<Gauge>` (no foundation pressure); per-class status bucketing is `match status / 100` (no float / chrono / time deps). Conditional ADR-0030 (foundations grant) + ADR-0031 (Cargo.lock cadence) stay available. The 5 PLAN-write SPEC corrections + the per-task PLAN deviations (Tasks 2-11 each carry 1-4 deviations) all narrated in PROGRESS in append-only fashion — the SPEC and PLAN remain in-tree unedited. M2 of this REVIEW flags one PROGRESS-narrative gap (the `iter().find()` deviation should have been recorded under "Deviations from PLAN" rather than "Implementation notes"); cosmetic.

- **D-3.6 (every phase is a green build).** ✓ — Task 12's local-run results at HEAD `7cdc1a8` show all 5 stable-toolchain gates clean + 12/12 Docker-gated fixtures green simultaneously. The Task 11 → Task 11-fixup loop landed within ~17 minutes of the bilateral-mismatch regression — the green-build invariant was preserved without amending Task 11's commit (the fixup is a separate named commit per the 06.1 `6b957ae` / 06.2 `f73face` precedent). I3 of this REVIEW flags the CI re-verification deferral; the local-gate results are substantive verification material. Per-task fmt discipline followed (R-9 from 06.1 in effect throughout 06.3).

- **D-3.7 (version pinning).** ✓ — `docs/envoy-rust/ENVOY_TARGET.md` not touched (`git log 3a964cc..42fc726 -- docs/envoy-rust/ENVOY_TARGET.md` empty).

- **D-3.8 (`unsafe` is forbidden).** ✓ — All 11 workspace crates' root files (`crates/{envoy-accesslog,envoy-admin,envoy-bin,envoy-cluster,envoy-config,envoy-http1,envoy-http2,envoy-listener,envoy-stats,envoy-tcp,envoy-tls}/src/{lib.rs,main.rs}`) carry `#![forbid(unsafe_code)]`. Zero `unsafe` blocks introduced anywhere in the review range — verified empirically: `git diff 3a964cc..42fc726 -- 'crates/**/*.rs' | grep -E "^\+.*unsafe"` returns empty.

- **D-3.9 (toolchain pin).** ✓ — `rust-toolchain.toml` not touched. Local + CI both ride the workspace-pinned stable toolchain.

**All nine doctrine rules pass.** No ADRs needed; no exemptions requested; no doctrine deviations recorded.

## §8. Recommendations for state-6 close-out / phase 07 / future phases

Items 06.3 surfaces that the state-6 close-out, phase 07 brainstorm, or a named future-phase carryforward must take into account:

1. **Per-task PROGRESS test-bucket attestation discipline (per I1).** Land a signpost in the SPEC/PLAN template — any commit that edits a fixture `expectations.yaml`'s `value_exact` / `value_must_be_zero` / `value_present_only` blocks (or any bilateral-assertion entry point) MUST be Docker-gated-fixture-run before commit, not just `cargo test --workspace`. Per-task PROGRESS should explicitly enumerate which test buckets ran (workspace tests, Docker-gated fixture, in-process backstop, fuzz). The Task 11 → Task 11-fixup pattern is the second occurrence of the `workspace tests clean ≠ bilateral fixture passes` shape (after 06.2 Task 6's read-only-handle ergonomics); a third would land Critical. Track as parent-06 close-out R-1.

2. **State-4-followup CI URL capture (per I3).** Land a single PROGRESS append + STATE.md update commit after a user-authorized push to `origin/main` fires the §7.5 CI run. Capture the CI run ID + HEAD SHA + timestamp alongside the local-gate evidence (mirrors the 06.1 `25625271032` / 06.2 `25670699370` shape). Acceptable to land either before state-5 REVIEW.md merge (preferred — keeps REVIEW grounded in CI evidence) or alongside state-6 close-out (acceptable — the local-gate results in PROGRESS Task 12 ARE substantive). Mechanical, ~5 LoC.

3. **Standard parent-06 close-out tag at state-6.** Per the 04 / 05 precedent, the state-6 commit title carries `[parent 06 done]` and flips ROADMAP rows `06.3` `in-progress` → `done` AND `06` `in-progress` → `done` simultaneously. Recommended literal: `phase 06.3: comprehensive stats wiring + 05.3 I1 closure + parent-06 close [parent 06 done] [ADR-0029]`. STATE.md advances to phase 07 lifecycle state 1; next-skill `superpowers:brainstorming` scoped to phase 07's `BOOTSTRAP_PROMPT.md` §8 row-07 charter (filter chain framework). The state-6 commit is doc-only.

4. **Task 11 deferred-scope handoff (per I2).** Fold the deferred synthetic 5xx backend + 4-class `pre_requests` extension into whichever future phase first surfaces synthetic backend infrastructure — likely the upstream-robustness family alongside connection pooling (which also tightens `cluster.<name>.upstream_cx_total` disposition from `name-required, value-may-differ` to `value-exact` per BEHAVIOR_CONTRACT.md line 69). The same task can also lift fixture 0011's `value_exact` block from `[]` to populated bilateral assertions if the StatsTagExtractor-equivalent lands in parallel.

5. **StatsTagExtractor-equivalent scoping (R-5 from Reviewer D).** The 11 envoy-rust-only allow-list entries added across Tasks 4-10 all defer bilateral assertion to a future StatsTagExtractor-equivalent that projects the dynamic name segment back into a Prometheus label at scrape time. The holding pattern grows monotonically (06.1: 2 entries; 06.3: +11 entries = 13 total). When the extractor lands, all 13 paired allow-list entries on the envoy-only side AND the 13 envoy-rust-only entries on the rust-only side drop together atomically (per 06.1 REVIEW R-2 pair-drop atomicity). Worth scoping as its own task in a future stats-evolution phase.

6. **The `ConnGaugeGuard` RAII pattern is reusable for future RAII-decremented gauges.** Per Task 6's design (single struct owning `Arc<Gauge>`, `Drop` fires `dec()`, construction via accessor that inc()s + Arc-clones). Future per-route active-request gauges, per-cluster active-RPC gauges, or per-listener active-connection-by-class gauges should reuse the pattern rather than rolling per-counter inc/dec sites. The pattern's structural advantage (covers both success and error close paths uniformly via Drop on `?`-exit) is the principal reason 06.3 stat values are deterministic across panic/error paths.

7. **Settle-sleep chain (M6 / phase-02.2 M1).** Has now extended through 06.1 / 06.2 / 06.3 unchanged (the 06.3 Task 5 `HoldHandler` tests sleep 50-200 ms). Worth scoping as a dedicated CI-stabilization task post-08 — the cumulative flake risk under loaded runners compounds across phases. Replacement pattern: `JoinSet::join_next()` poll loops or `tokio::sync::Notify`-based handshake; the Echo-backend Drop polling chain (02.2 M1) is the structural predecessor.

8. **The `parse_prometheus_samples` float-drop silent path (M5) needs a docstring/log addition before a histogram-family phase lands.** Today's all-integer stat set keeps the path dormant; histogram-family phases would surface silent missing-key errors in `value_exact` assertions. ~5 LoC fix; close opportunistically.

9. **Cargo.lock cadence ratification (04.1 M5/M9 standing carryforward).** 06.3 introduces no new top-level Cargo deps (Cargo.lock diff is empty across the entire state-3 arc); the cadence pick is not forced at 06.3 close. The carryforward continues unchanged through parent-06 close. Future phase that first introduces a new top-level workspace dep is the natural close site.

10. **The 06.3 PROGRESS Task 12 evidence narrative is structurally a good template for state-4 verification across the project.** Quotes each gate's command output inline; cross-references the §7.5 acceptance criteria mechanically; honestly narrates the CI deferral with the shared-state-impact discipline rationale. The narrative could be promoted into a `BOOTSTRAP_PROMPT.md` §7.5 example shape for future-phase planners.

---

*End of REVIEW.md. 06.3 phase-done state-5 complete. Verdict: Approved with M-track follow-ups. Next session enters state 6 — flips ROADMAP row `06.3` to `done` AND parent row `06` to `done` simultaneously (closing-sub-phase invariant); advances STATE.md to phase 07 lifecycle state 1 (next-skill `superpowers:brainstorming` scoped to phase 07's filter-chain-framework charter); writes the state-6 close-out commit per SPEC §9 with the `[parent 06 done]` tag. State-4-followup CI URL capture (per Important I3) lands either before state-5 REVIEW.md merge OR alongside state-6 close-out.*
