# Phase 64 — `64-accesslog-h2-uc-upstream-reset` — SPEC

**Pick (ADR-0121):** differentially WITNESS the **SIXTH and FINAL H2 `%RESPONSE_FLAGS%` value — `UC` (UpstreamConnectionTermination)** — byte-exact on the H2 upstream-disconnect-before-headers 503 path (an H2-upstream `STRICT_DNS` cluster whose single endpoint is a SPAWNED backend that completes a genuine H2 handshake, accepts the request stream, then drops it WITHOUT ever sending a response — a mid-stream reset; NO `retry_policy`, NO `circuit_breakers` → envoy-rust connects and dispatches the request, the upstream terminates the stream before delivering a response, and the reset synth is returned downstream), by (i) **renaming `synth_h2_502()` → `synth_h2_reset()` in place and correcting its status literal `502` → `503`** (its SOLE remaining call site, `crates/envoy-http2/src/hcm.rs:384`-`395`, the `AcquireOutcome::Sent(Err(e))` post-connect send/recv-failure arm — this closes the last `synth_h2_502` name in the codebase, since phase 63 already retired the OTHER call site), (ii) declaring a NEW per-stream boolean `reset_for_log_h2` and setting it post-loop from the ALREADY-EXISTING loop-scoped `final_outcome_h2` capture (phase 63, ADR-0120 — `matches!(final_outcome_h2, Some(AttemptOutcome::Reset))`; **NO new loop-scoped state needed** — `final_outcome_h2` already captures every iteration's outcome generically, not scoped to `ConnectFailure`), (iii) threading the new boolean through `finalize_h2_stream`'s sole call site, and (iv) extending the phase-56/57/58/61/63 H2 `response_flags_for_log_h2` derive with an `else if reset_for_log_h2 { "UC" }` branch ordered AFTER the existing `UF` branch (mirroring H1's phase-53 `UF`-before-`UC` ordering exactly). This **CLOSES carry-forward M56-1** (the last H2 `%RESPONSE_FLAGS%` value — `NR`/`UH`/`UO`/`URX`/`UF` already consumed by phases 56/57/58/61/63) and ALSO folds in the standing **M63-1** cosmetic fix (the stale `hcm.rs:836` "connect-failure synth-502" comment — corrected here alongside this phase's own reset-arm status-literal edit to the SAME comment block). NEW test-harness surface: a `--close-before-response` mode on `tests/helpers/http2-echo-server` + a NEW `Http2CloseBackend` harness struct + a NEW `H2_CLOSE_BACKEND_PORT` marker wired into `tests/differential/src/lib.rs`'s marker-scan/launch-arm mechanism (empirically verified this session — see below); NO new `Op`/`AccessLogRecord` field/crate/dependency/`ConfigError` variant.

> **State-0 recon — TWO empirically-verified findings this session (both required to lock this phase's scope; a naive reuse of phase 53's H1 approach would have been WRONG for H2):**
>
> **Finding 1 (live Docker, `envoyproxy/envoy:v1.33.0`, digest `sha256:56da5afd…`, confirmed matching `ENVOY_TARGET.md`'s pin, sibling-container topology per memory `state0-recon-backend-sibling-container`):** an H2C listener (`codec_type: HTTP2`) routing to a `STRICT_DNS`+`V4_ONLY` H2-upstream cluster (`typed_extension_protocol_options` → `explicit_http_config.http2_protocol_options: {}`) whose single endpoint is a sibling container running `alpine/socat TCP-LISTEN:8080,reuseaddr,fork EXEC:/bin/true` (the SAME raw accept-then-close backend fixture 0061's `TcpCloseBackend` uses for H1) — driven via `curl --http2-prior-knowledge` — emits, byte-stable across 3 repeats AND a full container restart: `{"method":"GET","proto":"HTTP/2","rc":503,"rcd":"upstream_reset_before_response_started{connection_termination}","rf":"UC"}`. **IDENTICAL rc/rf/rcd to the already-landed H1 `UC` witness** (fixture 0061, phase 53) — Envoy's own disposition is protocol-agnostic on this trigger, exactly as phases 58/61/63 found for the overflow/retry-limit-exceeded/connect-failure dispositions.
> - **Finding 2 (the make-or-break design correction — DIRECT reuse of fixture 0061's raw `TcpCloseBackend` would be WRONG for envoy-rust's OWN H2 client, unlike H1).** Code-read of `crates/envoy-http2/src/client.rs:19`-`56` (`Client::connect`) shows the TCP-connect AND the `h2::client::handshake` are BOTH folded into the SAME `Client::connect` call, with an explicit 10ms `select!` window "to detect immediate handshake failures" (the code's own comment). An in-process spike THIS session (`crates/envoy-http2/src/client.rs`, added and then FULLY REVERTED — `git status --porcelain` clean afterward, confirmed) proved: connecting to a raw accept-then-close backend (no H2 bytes at all) is classified ENTIRELY inside `Client::connect` — i.e. it would land in the ALREADY-FIXED (phase 63) `AcquireOutcome::ConnectFailure` arm, producing `UF` again, NOT the still-buggy `Sent(Err(e))`/`Reset` arm this phase needs to fix. A SECOND spike confirmed the correct design: a backend that completes a GENUINE `h2::server::handshake` (survives the client's 10ms detection window), accepts the request stream, then drops the `SendResponse` handle WITHOUT calling `send_response` (an implicit stream reset) — against THIS backend, `Client::connect` **SUCCEEDS** and the SUBSEQUENT `client_stream.send_request(req).await` call **FAILS** ("stream closed because of a broken pipe") — i.e. lands in `AcquireOutcome::Sent(Err(e))` → `AttemptOutcome::Reset`, exactly the arm this phase fixes. **A THIRD spike re-ran the live-Envoy recon (Finding 1) against THIS SAME handshake-then-reset backend** (a temporary `--close-before-response` mode added to `tests/helpers/http2-echo-server`, then FULLY REVERTED this session — `git status --porcelain` clean, confirmed) and got the IDENTICAL Envoy disposition: `503`/`rf:"UC"`/`rcd:"upstream_reset_before_response_started{connection_termination}"`. **Conclusion: the fixture's spawned backend MUST be H2-protocol-aware** (complete the handshake, accept the stream, then reset it) — NOT the raw TCP-level `TcpCloseBackend` H1 reused — for BOTH proxies to converge on the SAME `UC` disposition. This is captured as a NEW `--close-before-response` mode + a NEW `Http2CloseBackend` harness struct (§C below), distinct from H1's `TcpCloseBackend`.
> - **Code-read finding — envoy-rust's H2 reset arm is currently WRONG (502, not 503) and UNTHREADED for `%RESPONSE_FLAGS%`.** The `AcquireOutcome::Sent(Err(e))` arm (`hcm.rs:384`-`395`) calls `synth_h2_502()` (status 502) — the SAME class of previously-unvalidated status divergence phases 52/57/63 each fixed for their own arms. This is `synth_h2_502()`'s SOLE remaining call site (confirmed via `grep -n "synth_h2_502()"` — phase 63 already redirected the OTHER call site to `synth_h2_connect_failure()`).
> - **No new loop-scoped capture needed (smaller than phase 63).** The `final_outcome_h2: Option<AttemptOutcome>` local phase 63 added (`hcm.rs:697`, captured every iteration at `hcm.rs:779` — `final_outcome_h2 = attempt.outcome;`, GENERIC, not scoped to any one outcome variant) already carries `Some(AttemptOutcome::Reset)` on the final reset attempt. This phase reads it a SECOND time (mirroring H1's phase-53 reuse of its own `final_outcome` capture from phase 52) — `reset_for_log_h2 = matches!(final_outcome_h2, Some(envoy_config::AttemptOutcome::Reset));`.

## §1 — Why this pick

The candidate menu (`STATE.md`, left by the phase-63 close-out) weighed: **M56-1**'s final open slice `UC` (now the ONLY remaining H2 `%RESPONSE_FLAGS%` value — `NR`/`UH`/`UO`/`URX`/`UF` consumed by phases 56/57/58/61/63); **M63-1** (a cosmetic stale-comment fix, folded into this phase's own §A edit since it touches the same comment block); **M57-1** (a trivial `content-length` header fix, unrelated surface); **M53-2** (a trivial BEHAVIOR_CONTRACT doc-only qualifier); the `DC` downstream-disconnect flag (timing-dependent, rejected at every prior consideration through ADR-0118); and heavier family pivots (gRPC ALS/OTLP/tracing, CDS/LDS hot-reload, non-deterministic LB policies, the HTTP-filters-family carry-forwards). `UC` is the cheapest-strong-differential leaf and the natural close of the entire H2-`%RESPONSE_FLAGS%` sub-family opened at phase 56:

- **It is the DIRECT sequel to phase 63's `UF`**, exactly mirroring the H1 cadence phases 49→50→51→52→53 (`UC` is H1's SIXTH and final flag, phase 53) applied to the H2 cadence 56→57→58→61→63 (`NR`→`UH`→`UO`→`URX`→`UF`, now `UC`). **After this phase, M56-1 is FULLY CLOSED** — no H1/H2 asymmetry remains in the `%RESPONSE_FLAGS%` witness set.
- **State-0 recon confirms the wire shape is IDENTICAL to the already-landed H1 witness** (fixture 0061) — same status (503), same flag (`UC`), same DETERMINISTIC rcd (`upstream_reset_before_response_started{connection_termination}` — no OS-derived text, unlike the `UF` connect-failure rcd). Per H1's own phase-53 minimum-viable choice, this phase likewise DEFERS witnessing the deterministic rcd (logs `{method, proto, rc, rf}` only) to keep the witness minimal and one-flag-per-phase — deferred as a NEW carry-forward **M64-1** (the H2-side deterministic `UC` rcd; NOT a resurrection of M53-1, which was already CONSUMED by phase 54 for the H1 side — confirmed via `STATE.md`/`BEHAVIOR_CONTRACT.md`, both recording "M53-1 CONSUMED by phase 54").
- **The fix is a genuine, previously-unvalidated correctness bug** (502 instead of 503) on `synth_h2_502()`'s LAST remaining call site — closing out the whole per-arm status-correction sweep phases 52/57/63 have progressively done across every H2 synth arm.
- **The topology needs a NEW but SMALL test-harness backend** (§C) — the ONLY thing preventing a trivial "just reuse fixture 0061's raw `TcpCloseBackend`" shortcut is the client-side 10ms-handshake-detection nuance uncovered by this session's spikes; the fix (a `--close-before-response` H2-aware mode) is a small, well-scoped addition mirroring the EXISTING `Http2EchoBackend`'s spawn/readiness-probe shape almost verbatim.
- **The `DC` flag remains genuinely harder** (timing-dependent, nine consecutive rejections through ADR-0118) — nothing this session changes that assessment; it remains the sole open M45-2 slice for a future phase.

## §2 — Scope (minimum-viable, ADR-0121)

**Rename+correct one synth helper + reuse an existing loop-scoped capture for a new boolean + one derive branch + a NEW H2-aware close-before-response backend (test harness only) + one new fixture `0069` + a differential test + one new in-process backstop + BEHAVIOR_CONTRACT updates — NO new `Op`/`AccessLogRecord` field/crate/dependency/`ConfigError` variant:**

- **§A — rename `synth_h2_502()` → `synth_h2_reset()` in place, correct its status `502` → `503`.** This is the function's SOLE remaining call site (`hcm.rs:384`-`395`, the `AcquireOutcome::Sent(Err(e))` arm) — after phase 63 redirected the connect-failure arm away from it, `synth_h2_502` has exactly one caller left, so renaming AND correcting in place (rather than adding yet another dedicated-name wrapper) avoids a permanently-misleading `synth_h2_502` function that would actually return 503. Update the arm's call site + its adjacent comment (currently: `// The H2 synth-502 preserves the pre-phase-16 dispatch-failure shape.` → `// The H2 synth-503 preserves the pre-phase-16 dispatch-failure shape, corrected to Envoy's actual status (64, ADR-0121).`) + the `tracing::warn!` string (`"H2 listener: upstream dispatch failed — emitting 502"` → `"— emitting 503"`). **ALSO fold in the standing carry-forward M63-1** while this exact comment region is being touched: the post-loop reconciliation comment block (`hcm.rs:~836`) still reads `"...no-healthy-upstream synth-503, connect-failure synth-502, reset synth-502, and overflow synth-503 paths..."` — correct BOTH stale `synth-502` mentions to `synth-503` (connect-failure was already fixed at phase 63; reset is fixed by THIS phase's own §A). **Sweep every OTHER reference to `synth_h2_502` in the file** (the historical phase-57/63 doc comments narrating "as of phase NN, `synth_h2_502()`'s other call site is still 502" — these are backward-looking narrative about THOSE phases' own state at authoring time; PLAN decides whether to leave them verbatim as historical prose [the established project convention — comments are not rewritten retroactively per D-3.4/D-3.5] or add a forward pointer noting the rename; the ACTIVE-state doc comment on `synth_h2_connect_failure()`, currently claiming the reset arm "is UNCHANGED — still 502, deferred as the continuing M56-1 `UC` slice (a future phase)" — IS active-state prose describing TODAY and MUST be updated since this phase is that future phase).
- **§B — declare `reset_for_log_h2` and set it post-loop from the EXISTING `final_outcome_h2` capture.** `let mut reset_for_log_h2: bool = false;` declared alongside `connect_failure_for_log_h2` (`hcm.rs:~565`). Set `reset_for_log_h2 = matches!(final_outcome_h2, Some(envoy_config::AttemptOutcome::Reset));` at the SAME post-loop site (`hcm.rs:~876`, immediately after the existing `connect_failure_for_log_h2` assignment) — **NO new loop-scoped state** (unlike phase 63, which had to ADD `final_outcome_h2`; this phase just reads it a second time, mirroring H1's phase-53 reuse of ITS OWN `final_outcome` capture from phase 52). `AttemptOutcome` is `Copy` (unchanged); the reset final-outcome rule is REPLAY-SAFE identically to `connect_failure_for_log_h2` — a reset RETRIED to eventual success has `final_outcome_h2 = Some(Response)` → not flagged.
- **§C — thread `reset_for_log_h2: bool` through `finalize_h2_stream` (the function signature and its SOLE call site).** A new `Copy` primitive parameter, added alongside `connect_failure_for_log_h2` (phase 63's own precedent for adding exactly this shape of parameter).
- **§D — extend the H2 `response_flags` derive with an `else if reset_for_log_h2 { "UC" }` branch, ORDERED AFTER `UF`.** Today (post-phase-63): `if retry_limit_exceeded_for_log_h2 { "URX" } else if connect_failure_for_log_h2 { "UF" } else { match response_code_details_for_log_h2.as_deref() { ... } }`. This phase becomes: `if retry_limit_exceeded_for_log_h2 { "URX" } else if connect_failure_for_log_h2 { "UF" } else if reset_for_log_h2 { "UC" } else { match ... /* unchanged three rcd arms */ }` — mirroring H1's phase-53 `UF`-before-`UC` ordering (`crates/envoy-http1/src/hcm.rs:1343`) exactly. Because `reset_for_log_h2` is set ONLY on the reset final-outcome path (where `response_code_details_for_log_h2` stays the shared `via_upstream` → the rcd-match's `_ => "-"` arm is what it would otherwise fall to), the `URX`/`UF`/`NR`/`UH`/`UO` arms are unreachable-with-it-set and stay byte-identical.
- **§E — the H2-aware close-before-response backend (the NEW test-harness surface, empirically verified this session — §0 above).**
  - **(i) `tests/helpers/http2-echo-server` gains a `--close-before-response` boolean flag** (extending `parse_argv` + its unit tests, mirroring `tcp-echo-server --close-on-accept`'s naming convention). When set, `handle_connection`'s per-connection task runs a NEW `handle_connection_close_before_response` path: perform the real `envoy_http2::codec::server_handshake`, `conn.accept()` the first stream, then `drop(send_response)` WITHOUT calling `send_response.send_response(...)` (an implicit stream reset) — verified this session (spike, fully reverted) to reliably drive envoy-rust's OWN H2 client into `AcquireOutcome::Sent(Err(e))` → `Reset`, AND to drive live upstream Envoy v1.33.0 to the IDENTICAL `503`/`UC`/deterministic-rcd disposition as the raw `TcpCloseBackend` trigger.
  - **(ii) The harness backend struct.** A NEW `Http2CloseBackend` in `tests/differential/src/backend.rs`, mirroring `Http2EchoBackend`'s exact spawn/readiness-probe shape (`reserve_port` + spawn `http2-echo-server --port <p> --close-before-response` + the EXISTING `wait_h2_accept_ready` readiness probe, which only performs `h2::client::handshake` and never opens a stream — confirmed compatible with the close-before-response mode, since that mode only resets upon `conn.accept()`, i.e. a STREAM open, not merely a handshake) + `kill_on_drop(true)` + `container_host() = "host.docker.internal"`.
  - **(iii) The harness wiring.** A NEW marker `H2_CLOSE_BACKEND_PORT` (distinct from H1's `CLOSE_BACKEND_PORT`, since the two backends are fundamentally different — raw-TCP-close vs genuine-H2-handshake-then-reset) + a NEW `scan_needs_marker`/launch-arm block in `tests/differential/src/lib.rs`'s marker-driven backend-launch section (the closest model is the adjacent `needs_http2_backend`/`_http2_backend` block, `lib.rs:~3287`-`3296` — PLAN cites the exact insertion site + confirms the per-side substitution-map wiring, mirroring how `http2_backend_port_str` feeds the upstream/subject KV maps).
- **§F — the fixture `0069-accesslog-h2-uc-upstream-reset`** (paired `envoy.yaml`/`envoy-rust.yaml`/`expectations.yaml`/`README.md`): an H2C listener (`codec_type: HTTP2`, `http2_protocol_options: {}`, the fixture-`0064`-`0068` listener shape) routing to a `STRICT_DNS`+`V4_ONLY` H2-upstream `backend_cluster` (`typed_extension_protocol_options` → `explicit_http_config.http2_protocol_options: {}`) whose single endpoint is the §E `Http2CloseBackend` (`{{BACKEND_HOST}}`/`{{H2_CLOSE_BACKEND_PORT}}` markers — `host.docker.internal` on the reference side, `127.0.0.1` on the subject side), with NO `circuit_breakers` and NO `retry_policy`. `json_format`: `{method, proto, rc, rf}` — `rcd` OMITTED (deterministic but deferred per §1/M64-1, mirroring fixture 0061's precedent exactly). ONE probe: `GET /`, `expected_status: 503`, cross-proxy-equal whole-line `{"method":"GET","proto":"HTTP/2","rc":503,"rf":"UC"}`.
- **§G — the differential test** `tests/differential/tests/access_log_h2_uc_upstream_reset.rs`: a thin `differential::run_fixture(&dir)` wrapper, structural clone of fixture `0068`'s test (`access_log_h2_uf_connect_failure.rs`), driven by the EXISTING `Driver::Http2AccessLogByteExact` (no driver change — only the NEW backend-launch wiring from §E(iii)). Per the phase-53/H1 precedent, expect this fixture to be **LOCAL-RED on this dev host** (a backend-spawning fixture → subject to the host-bridge-IP differential flake, memory `differential-host-bridge-ip-192-168-65-2`) and **GREEN on CI** — CI is authoritative.
- **§H — a NEW in-process backstop**, `h2_upstream_reset_access_log_carries_uc_flag`, mirroring the naming convention and structure of H1's `h1_upstream_reset_access_log_carries_uc_flag` (phase 54) and the H2 phase-56/57/58/61/63 backstop precedent. Drives an in-process loopback pair: a TEST-LOCAL listener performing the SAME `server_handshake`-then-`drop(send_response)` sequence as §E(i) (inline in the test, mirroring how the phase-63 backstop built its own inline config rather than reusing a shared helper), an access-log `FileSink` with a `{rc, rf}` `json_format`, drives one H2 request via the existing `drive_h2_once` test helper, and asserts the scraped line is `{"rc":503,"rf":"UC"}`.
- **§I — BEHAVIOR_CONTRACT updates.** The `%RESPONSE_FLAGS%` row: add H2 `UC` to the witnessed-on-H2 list (now `NR`/`UH`/`UO`/`URX`/`UF`/`UC` — the FULL six-flag set, matching H1's own six-flag completion at phase 53), citing fixture `0069`; note the per-flag equivalence rule (derived from the reset final-outcome boolean, NOT from `%RESPONSE_CODE_DETAILS%`, which stays the shared `via_upstream`; rcd/body NOT logged/compared this phase — deferred, NEW carry-forward M64-1). Update carry-forward **M56-1** to CLOSED (fully consumed — no H2 `%RESPONSE_FLAGS%` value remains open). The `%RESPONSE_CODE_DETAILS%` row is UNCHANGED by this phase (no new rcd value introduced; the reset rcd stays the shared `via_upstream` on envoy-rust's side, same as `UF`).
- **§J — no new fuzz target.** `%RESPONSE_FLAGS%` is an existing operator; the H2 codec itself is unchanged. SKIP, matching the phase-56/57/58/61/63 precedent for this sub-family.

**Load-bearing additivity invariant:** all `0001`-`0068` stay byte-identical. This phase's ONLY behavior changes are (1) the reset-arm status correction 502→503 — gated on `AcquireOutcome::Sent(Err(e))`, a path NO existing fixture `0001`-`0068` triggers (confirmed this session: no existing H2 fixture's backend ever completes a handshake then resets mid-stream without responding — every existing spawned H2 backend, `Http2EchoBackend`, always responds 200; every dead-endpoint fixture like `0068` hits `ConnectFailure`, not `Sent(Err)`), and (2) `reset_for_log_h2 = true`, gated on the SAME `Reset` final-outcome condition. PLAN (§3.2) re-greps every existing H2 fixture to confirm none triggers an H2 reset disposition today.

## §3 — PLAN-VERIFY items (state-2 §6.2)

1. **Confirm `hcm.rs`'s current line numbers** for every site named above (`synth_h2_502`'s definition + sole call site + adjacent comments, the `*_for_log_h2` locals block, the post-loop set site, `finalize_h2_stream`'s signature + call site, the `%RESPONSE_FLAGS%` derive, the M63-1 comment-block fix site) against the tree at PLAN-write time (re-grep for drift).
2. **Re-grep EVERY existing H2 fixture** for any spawned-backend or dead-endpoint topology that might newly trigger `AcquireOutcome::Sent(Err(e))` — confirm none newly diverges; PLAN re-derives the list itself (`0009`/`0010`/`0018`/`0021`/`0064`-`0068`) rather than trusting this SPEC's enumeration.
3. **Confirm `finalize_h2_stream` still has exactly ONE call site** (re-run `grep -n "finalize_h2_stream("`) before committing to the single-new-`bool`-parameter form.
4. **Confirm `synth_h2_502()` has exactly ONE call site** (re-run `grep -n "synth_h2_502()"`) before renaming it in place — if a sibling session's concurrent work has added a second caller, PLAN reconsiders the rename-vs-new-helper choice (a fresh `synth_h2_reset()` wrapper instead, mirroring phase 63's own new-helper approach, would be the fallback).
5. **Re-verify the §E backend design empirically** (the state-0 recon spikes are recorded above but PLAN should re-run its OWN quick confirmation before committing test-harness code, per the standing PLAN-VERIFY discipline of not blindly trusting a prior session's recon) — confirm `wait_h2_accept_ready`'s handshake-only probe does not race with the close-before-response mode's stream-level reset.
6. **Decide the §6.1 split** — count actual tasks/LoC once scoped in detail; projected NOT to fire (see §6).
7. **Fixture naming/number** — confirm `0069` is still the next-free fixture number at PLAN-write time (standing concurrency guard).
8. **Confirm the exact §A comment-sweep scope** — decide which historical (phase-57/63-authored, "as of that phase" narrative) `synth_h2_502` mentions are left verbatim vs which ACTIVE-state doc comments (describing today's mechanism) must be corrected, per the D-3.4/D-3.5 convention of not rewriting past narrative.

## §4 — Rejected / deferred

- **Witnessing the deterministic `UC` `%RESPONSE_CODE_DETAILS%`** (`upstream_reset_before_response_started{connection_termination}`) — recon-confirmed deterministic (same as H1's own deferred rcd), but this phase scopes the fixture to `{method, proto, rc, rf}`-only to keep the witness minimum-viable, mirroring H1's own phase-53 choice. Deferred as a NEW carry-forward **M64-1** (the H2-side deterministic `UC` rcd — distinct from M53-1, which was already CONSUMED by phase 54 for the H1 side; a future phase could witness the H2 rcd alone, or fold it in alongside any other H2 rcd work).
- **The retry-exhausted-reset combination** (both `retry_limit_exceeded_for_log_h2` AND `reset_for_log_h2` set simultaneously) — un-recon'd on live Envoy (mirrors H1's own un-recon'd combination for `UC`, the M53-3-equivalent); the §D ordering (`URX` checked first) renders `URX` deterministically regardless, so envoy-rust's behavior is well-defined even though the EXACT Envoy disposition for this combination has not been empirically verified. Deferred, matching H1's standing disposition.
- **A hard RST (SO_LINGER-style abrupt reset) vs the graceful stream-reset this phase uses** (`drop(send_response)`, an implicit `RST_STREAM`) — un-recon'd whether Envoy renders a DIFFERENT reset-reason brace/flag on a hard RST; out of scope (the graceful implicit-reset path is the clean, deterministic, spike-verified `UC`).
- **The `DC` downstream-disconnect flag** (M45-2 slice) — timing-dependent, hard to drive deterministically; rejected at every prior consideration (ADR-0102 through ADR-0118); no new information this session changes that.
- **M57-1's `content-length` header omission** and **M53-2's BEHAVIOR_CONTRACT "(H1)" qualifier** — trivial standalone fixes, deliberately NOT bundled into this phase (this phase does not touch `synth_h2_no_healthy_upstream()` or the H1-side reset-rcd BEHAVIOR_CONTRACT rows); left as cheap future picks or fold-ins whenever a phase next touches those surfaces for an unrelated reason.
- **A family pivot to xDS CDS/LDS hot-reload or LB `least_request`/`random`** — both viable, both carry larger up-front design cost than this session's recon-narrowed fix; deferred to a future pick, not rejected outright.

## §5 — Acceptance (§7.5, re-run at state-4)

(a) fixture `0069` green (cross-proxy-equal status `503` + whole-line `{"method":"GET","proto":"HTTP/2","rc":503,"rf":"UC"}`) + (b) all `0001`-`0068` green simultaneously (additive — the §2 load-bearing invariant, re-verified §3.2) + (c) h2spec ≥95% (NO H2 codec/framing change) + (d) no new fuzz target (SPEC §J) + (e) build/clippy/fmt/test/deny clean (including the §A rename + the §E-(i) `http2-echo-server` argv extension + its unit tests) + (f) `REVIEW.md` approved. **Fixture `0069` is a backend-spawning fixture → expect LOCAL-RED on this dev host (the `differential-host-bridge-ip-192-168-65-2` flake, per phase 53's own precedent for its H1 sibling) and GREEN on CI — CI is AUTHORITATIVE.** `#![forbid(unsafe_code)]` holds; NO new crate/dependency/`Op`/`AccessLogRecord` field/`ConfigError` variant. **If §3.2's PLAN-write re-grep finds an existing H2 fixture would newly diverge, or §3.5's re-verification overturns the backend design**, a §6.2 reconciliation ADR fires and §A-§I are re-scoped — PLAN scopes that contingency if it triggers.

## §6 — Process

- **§6.1 split — projected NOT to fire.** Smaller in `crates/` scope than phase 63 (no new loop-scoped capture needed, and the status-fix is a rename-in-place rather than a new helper) but LARGER in test-harness scope (a NEW H2-aware close-before-response backend mode + a NEW harness struct + a NEW marker-wiring block, none of which phase 63 needed since its `0068` fixture used a literal dead endpoint with NO backend spawn). Projected ~10-14 tasks / ~200-320 LoC — comparable to phase 53 (H1's own `UC` phase, which needed the SAME accept-then-close backend surface, ~12-16 tasks/~250-400 LoC) minus H1's TCP-level backend's simplicity plus H2's extra handshake-survival requirement — well under the ~25-task/~1500-LoC gate. PLAN-write (§3.6) makes the final call; **if it splits**, `64.1` = the source fix (§A-§D, no new fixture), `64.2` = the backend/fixture/test/backstop/BEHAVIOR_CONTRACT + parent close.
- **§6.2 reconciliation:** the state-0 recon THIS session already empirically resolved the make-or-break design question (the H2-aware close-before-response backend requirement, via three in-process/live-Docker spikes, all fully reverted) — NOT expected to fire, but reserved (a fresh ADR number, shared slot with the split per the standing lapsed-reservation convention) in case §3's PLAN-write re-verification overturns a §A-§I fact.
- **Carry-forward consumption:** this phase CLOSES **M56-1** (consumes the FINAL `UC` slice — no H2 `%RESPONSE_FLAGS%` value remains open) and CONSUMES **M63-1** (the stale `hcm.rs:836` comment, fixed alongside this phase's own §A edit). **NEW carry-forward M64-1** is opened (the H2-side deterministic `UC` rcd, deferred per §4 — M53-1 stays CONSUMED, unchanged, since it was already closed at phase 54 for the H1 side). M57-1 + M55-1 + M53-2 + M53-3 + M48-2 + M42-1 + the `DC`/retry-budget-overflow slices of M45-2 + the phase-58 candidate carry-forward (H2 request-budget arm's own differential fixture) + M40-1 + M39-1/M39-2 + M38-1/M38-2 + CF-39-1 + the HTTP-filters-family (1)-(4) + older stay live; NONE blocks.
- Pick + §A-§J ground-truth locked by **ADR-0121** — the standing next-sequential number (`ADR-0121` was reserved-but-unfired for phase 63's own §6.1 split, which did NOT fire — reclaimed here per the lapsed-reservation convention, confirmed via a fresh `grep -oE '^## ADR-[0-9]+' DECISIONS.md` re-scan this session: highest fired entry `0120`; no sibling draft claims `0121`).
- **Phase number `64`** — confirmed via `ls docs/envoy-rust/phases/` this session: highest existing names are `59`/`60`/`61-accesslog-h2-urx-retry-exhausted`/`61-perf-listener-so-reuseport`/`62-upstream-idle-timeout-config`/`63-accesslog-h2-uf-connect-failure`, no `64` — the sibling H1-perf-work directories `59`/`60`/`61-perf`/`62` remain disjoint, no-ROADMAP-row, other-sessions' concern (confirmed via `grep` — no ROADMAP rows exist for them yet).

_Scope locked by ADR-0121. The state-2 PLAN-write (`superpowers:writing-plans`) re-confirms §3's PLAN-VERIFY items (line-number drift, the additivity re-grep, the `synth_h2_502` single-call-site re-check, the §E backend-design re-verification, the §6.1 split decision, the fixture-`0069` numbering, the comment-sweep scope) and authors `PLAN.md`. The state-3 implementation is the session after (or `64.1`'s state-3, if the split fires)._
