# Phase 56 — `56-accesslog-h2-response-flags-driver` — State-5 Code Review

**Reviewer:** fresh `general-purpose` subagent acting as senior code reviewer (no session history — independently re-derived every load-bearing claim against the actual tree/CI, not just the SPEC/PLAN/PROGRESS narrative)
**Diff range:** `dff7e4f..0de4a0e` (phase-56 state-2 PLAN-write commit → phase-56 state-3 Task-4 commit) — spans Task 1 `d1a3729` (the `Driver::Http2AccessLogByteExact` harness variant), the Task-1 fix round `0954905` (the `upstream_access_log_mounts` match-site wiring a task reviewer found missing from PLAN.md's own brief), Task 2 `da126bb` (the H2 one-arm `NR` `response_flags` derive + 2 TDD backstops), Task 3 `c7d2ad4` (fixture `0064` + its differential test), and Task 4 `0de4a0e` (`BEHAVIOR_CONTRACT.md` updates)
**Reviewed against:** `SPEC.md` + `PLAN.md` + `PROGRESS.md` + `BEHAVIOR_CONTRACT.md` (`%RESPONSE_FLAGS%`/`%RESPONSE_CODE_DETAILS%` rows) + `DECISIONS.md` ADR-0113 (pick + scope) + project coding standards (D-3.8 `#![forbid(unsafe_code)]`)
**Authoritative CI:** run `28582987206` — headSha `4040755` (the state-4-transition marker commit, NOT `0de4a0e` directly). The reviewer independently diffed `0de4a0e..4040755` and confirmed it touches only `STATE.md`/`STATE_HISTORY.md`/`PROGRESS.md` — no code — so the green CI run is valid evidence for the code diff reviewed here. Full job log independently pulled and re-derived, not just the job-summary conclusion.

## VERDICT: ✅ **APPROVED** (0 Critical / 0 Important / 3 Minor — all informational, no action required)

The implementation faithfully executes SPEC §A–§G and PLAN Tasks 1–4 with no scope drift. The reviewer independently re-derived — not just re-read — every load-bearing claim: the "one-arm, not six-arm" `response_flags` derive's actual scope in `crates/envoy-http2/src/hcm.rs` (confirmed to match the phase-48 H1 precedent's *original* one-arm form, not H1's current six-arm derive); the additivity invariant across every existing H2 fixture (grepped all fixtures carrying `codec_type: HTTP2`, confirmed none besides the new `0064` carries an `access_log` block, so no pre-existing fixture can hit the new `Some("route_not_found") => "NR"` arm); the borrow/move correctness of the derive (verified the `response_code_details_for_log_h2.as_deref()` match resolves before the value is later moved into the `AccessLogRecord` literal); the completeness of the Task-1 fix round (grepped every `Http1AccessLogByteExact` site in `tests/differential/src/lib.rs` and confirmed a matching `Http2AccessLogByteExact` entry at each); that the two new backstop tests exercise genuinely distinct routing branches (route-miss vs. host-miss), not a duplicate assertion; and the CI green run (pulled the full job log for `28582987206`, independently found `test access_log_h2_rf_no_route ... ok`, `test h2spec_pass_rate_gate ... ok`, and zero `FAILED` occurrences in the entire log — reconciling the `4040755` vs. `0de4a0e` headSha discrepancy by diffing the two commits itself before trusting the citation). All held up. No Critical or Important findings surfaced; three Minor notes are stylistic/informational and do not block. → Advance STATE to the §5 state-6 close-out.

---

## Strengths

- **Scope discipline matches SPEC/PLAN precisely.** All 6 SPEC items (§A-§G) map 1:1 to the 4 PLAN tasks; the landed diff is essentially a byte-for-byte realization of PLAN.md's prescribed code. No scope creep, no missing pieces.
- **The "one-arm, not six-arm" derive scope decision is genuinely respected**, not just claimed. `crates/envoy-http2/src/hcm.rs`'s `response_flags_for_log_h2` derive is exactly `Some("route_not_found") => "NR", _ => "-"` — the phase-48 H1 derive's ORIGINAL one-arm scope, not a port of H1's current six-arm version. The doc comment correctly attributes the remaining flags to the new carry-forward M56-1.
- **Borrow/move correctness independently verified by direct code inspection**, not trusted from PROGRESS.md's claim: `response_code_details_for_log_h2.as_deref()` is consumed by the `match` (producing the derive's result) before `response_code_details_for_log_h2` itself is moved into the `AccessLogRecord` literal several lines later. Compiles and is sound.
- **Additivity invariant independently re-verified.** Every existing H2 fixture carrying `codec_type: HTTP2` was grepped for an `access_log` block; only the new `0064` carries one. No pre-existing fixture (`0001`-`0063`) can newly diverge from the added derive arm.
- **The Task-1 fix round was actually complete.** Every `Http1AccessLogByteExact` site in `tests/differential/src/lib.rs` (definition, `port_key` match, `upstream_access_log_mounts` match, dispatch arm) has a matching, correctly-wired `Http2AccessLogByteExact` counterpart — confirming fix commit `0954905` genuinely closed the gap its own task reviewer found.
- **The two backstop tests are genuinely distinct**, not a copy-paste duplicate: `h2_route_miss_access_log_carries_nr_flag` drives a host that matches the vhost but misses the route; `h2_host_miss_access_log_carries_nr_flag` drives a host that misses the vhost lookup itself — two different branches of the shared `build_response` routing logic.
- **`#![forbid(unsafe_code)]` holds** in both touched crate roots; no new crate/dependency/`Op`/`AccessLogRecord` field/`ConfigError` variant anywhere in the diff.
- **`BEHAVIOR_CONTRACT.md`'s edit is surgical**, exactly as PLAN.md Task 4 instructed: only the two targeted M45-1 mentions (in the `%RESPONSE_FLAGS%` and `%RESPONSE_CODE_DETAILS%` rows) were replaced with M56-1; unrelated M45-1/M45-2 mentions elsewhere in the document were correctly left untouched.
- **CI ground truth independently reproduced, including reconciling a headSha subtlety.** `gh run view 28582987206` ran against `4040755`, not `0de4a0e` directly — the reviewer diffed the two commits itself (`0de4a0e..4040755`) and confirmed the gap is docs-only (`STATE.md`/`STATE_HISTORY.md`/`PROGRESS.md`), so the green run is valid evidence for the code reviewed here. Pulled the full job log directly: `test access_log_h2_rf_no_route ... ok`, `test h2spec_pass_rate_gate ... ok`, `cargo deny check` → `advisories ok, bans ok, licenses ok, sources ok`, zero `FAILED` occurrences anywhere in the log, both jobs `success`.
- **Fixture `0064`'s `envoy.yaml`/`envoy-rust.yaml` are equivalent modulo documented per-side differences** (admin block, listener bind address, log path) — the route table, access-log format, and HCM config are otherwise identical, matching the established fixture convention.

---

## Findings

### Minor 1 (informational — no action required) — a Task-1-scoped doc comment reads oddly once later tasks land

A `tests/differential/src/lib.rs` doc comment on the new `Driver` variant, written at Task 1 commit time, describes state as of that commit in isolation (no fixture yet references the new variant) — by Task 3 that's no longer literally true, but the comment is scoped to describing Task 1's own contribution at the time it landed, not asserting a standing invariant, and is not misleading in context. Typical of this project's incremental-commit style. Not worth a follow-up commit.

### Minor 2 (informational — no action required) — backstop-test boilerplate duplication

The two new backstop tests in `crates/envoy-http2/src/hcm.rs` duplicate roughly 75 lines of HCM-config-construction boilerplate between each other (an identical `HttpConnectionManagerConfig` literal, differing only in the request URI). A shared helper parameterized on the request URI would be slightly more idiomatic, but this mirrors the H1 precedent's own established style (the PLAN explicitly cited `h1_route_miss_access_log_carries_nr_flag`/`h1_host_miss_access_log_carries_nr_flag` as the pattern to mirror), so it is consistent with existing project convention rather than a new problem. Optional cleanup only if a future phase happens to touch this test module anyway — not worth a dedicated task.

### Minor 3 (informational — no action required) — `PROGRESS.md`'s HEAD/"state-3 commit" terminology blur

`PROGRESS.md`'s `## State-4 verification` section cites `HEAD` as `4040755` and describes it as "the phase-56 state-3 commit." Strictly, `0de4a0e` (Task 4) is the last state-3 *implementation* commit; `4040755` is the immediately-following state-4-transition marker commit, sharing an identical code tree. Harmless — both point at the same reviewed diff — but could mildly confuse a future reader trying to locate "the state-3 HEAD" purely by commit message. Not worth a fix; noted for awareness only.

---

## Acceptance-gate cross-check

Per PROGRESS.md's `## State-4 verification` section, CI run `28582987206` @ `4040755` is documented GREEN and was independently reproduced by this review: fixture `0064` `test access_log_h2_rf_no_route ... ok` (both no-route probes byte-exact against live Envoy v1.33.0), **zero `FAILED` occurrences anywhere in the full job log** (every one of `0001`-`0063` plus the new `0064` passed in CI's environment, including the 3 fixtures that are documented to flake on the local dev host), `h2spec_pass_rate_gate ... ok` (unmoved — no H2 codec/framing change this phase, `known-failures.txt` unmodified), `cargo deny check` clean, both jobs (`build + test + lint`, `fuzz`) `success` (no new fuzz target, per SPEC §G SKIP). CI is authoritative per project convention; the documented evidence, independently reproduced by this review, satisfies §5 gate (a)–(e). (f) REVIEW.md is this review.

**Disposition:** Approved for close-out. All 3 findings are informational Minors requiring no action. Carry-forward **M45-1 remains CONSUMED** (the H2 access-log differential driver now exists, `NR` witnessed byte-exact); **M56-1 remains OPEN** (the remaining H2 `%RESPONSE_FLAGS%` values `UH`/`UO`/`URX`/`UF`/`UC` + H2 failure-path `%RESPONSE_CODE_DETAILS%` strings beyond `route_not_found`, now unblocked for future one-flag-at-a-time phases — the same cadence phases 49-54 established for H1). M55-1 + M53-2 + M53-3 + M48-2 + M42-1 + the `DC`/retry-budget-overflow slices of M45-2 + M40-1 + M39-*/M38-*/CF-39-1 + older remain live from before this phase; none blocks. → Advance STATE to the §5 state-6 close-out.
