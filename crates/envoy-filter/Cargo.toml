[package]
name = "envoy-filter"
version = "0.1.0"
edition = "2024"
publish = false
license = "Apache-2.0"

[dependencies]
bytes = "1"
thiserror = "2"
envoy-config = { path = "../envoy-config" }

[dev-dependencies]
