[package]
name = "envoy-http2-fuzz"
version = "0.0.0"
edition = "2024"
publish = false
license = "Apache-2.0"

[package.metadata]
cargo-fuzz = true

# Prevent Cargo from treating this subcrate as part of the parent workspace.
# cargo-fuzz invokes cargo with `--manifest-path` pointing here; without an
# explicit empty `[workspace]`, cargo would walk up to the root Cargo.toml and
# error even though that root has `exclude = ["crates/envoy-http2/fuzz"]`.
[workspace]

[dependencies]
libfuzzer-sys = "0.4"
envoy-http2 = { path = ".." }

[[bin]]
name = "grpc_health_decode"
path = "fuzz_targets/grpc_health_decode.rs"
test = false
doc = false
bench = false
