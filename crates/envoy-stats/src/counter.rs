//! envoy-stats `Counter` primitive (lands at Task 3).

// Placeholder; Task 3 ships the real surface. The `pub use counter::Counter;`
// re-export in `lib.rs` is satisfied by Task 3's contents.
pub struct Counter;
